# P3 handoff — fully rewritten 2026-08-26 (session wind-down by operator order, context full; nothing in flight, all agents landed, everything pushed): **TWO STREAMS, BOTH WAITING ON THE OPERATOR. (A) THE LN LANE SITTING IS READY — the GLM/Kimi lane campaign's build work is COMPLETE; the operator's next act is ONE command (`./P3/gates/lane-test-door.sh`), two key pastes, then clicking tasks onto real GLM/Kimi runs, and the LN GATE BATCH is presented at that same sitting. (B) THE B6 GATE STREAM stands where rounds 4–5 left it — F1a+F1b first and alone in a fresh session, then the W1–W4 merged act (Nexus walk → taxonomy v4 → editable understood-spec surface → open-points rebuild). A fresh session saying "continue implementation" asks the operator WHICH stream (or presents the LN gate batch if the operator has run the door).**

**Read this first, then `P3/STATE.md`.** This file is a *snapshot*; **`P3/STATE.md` is the single source of truth and outranks anything here.** If they disagree, STATE wins and this file gets corrected.

> **THE LN LANE-EXPANSION CAMPAIGN — COMPLETE (operator-ordered 2026-08-23 "only the Claude lane is not sufficient for reliable testing"; all eight packets landed + pushed through `eb65cf3`, four-stage pipeline throughout).** **LN-1** opencode substrate (`internal/adapters/opencode`, pin 1.18.3, tiers F/R/L) · **LN-2A/2B** the Z.AI/GLM lane (provider DATA incl. GLM-5.3 re-stamp; `{env:VAR}` broker resolution measured; limit classifier; tier-3 plan gauge — never the provider window; flat-lane pressure routing; lane→substrate commissioned-only DATA) · **LN-3** the Kimi lane (Gate A–C audit PASS class-2, `P3/measurements/2026-08-24-kimi-lane-gate-audit.md`; documented-class message classifier w/ mutation-verified revocation belt — Kimi signals quota on 403; A11 applied w/ veto window; C5 no-household-personal-data rider recorded NOT-enforced) · **LN-CEREMONY** `P3/gates/lane-key-ceremony.sh` (guided key placement; per-lane tier-L smoke behind typed costs; the Kimi wire-capture / Z.AI-calibration / model-list steps remain standalone operator acts — the door below runs only its KEY steps; `SINET_CEREMONY_USER` moves the person, the tier-L smoke has no Go person seam and skips honestly under it) · **LN-4** placed key = ROUTABLE lane at control-plane startup (secret-free presence read in `internal/broker/placed.go`; `opencode.Commission` + hoisted derivations; commissioning INFO line names people/lanes/unmatched profiles; startup-bound — restart picks up new keys) · **LN-5+5b** `P3/gates/lane-test-door.sh` — ONE command cold-to-clickable (fresh marker-guarded world `~/.sinet-lane-test` :8485 person `alice`; per-person `sinet broker --user`; ceremony pointed at world+person; plane restarted AFTER keys w/ the commissioning line shown; `budget` step declares plan budgets from PLATFORM PROPOSALS on every budgetable window + the anthropic token budget (Fleet-precedented 250000/30d, loudly labeled test-world/UI-editable); walk derives sign-in from the running world and tells the tie-then-alternate selection truth; `stop`/`clean` verbs; B6-clickthrough.sh NEVER touched) · **LN-6** the plan-budget declaration path (migration **0025**; `metering.PlanBudgets` per (person, lane, window); `POST /api/meters/plan-budget` w/ `from_proposal`; routing pressure consumes declared rows at both call sites; unit-coherence predicate at THREE layers — a requests-vs-credits mismatch can never steer dispatch; expiry refused at the verb + honest on the wire via `inapplicable_note` and `LanePressure.Reason`; max-binds aggregation, reversal confined to one function+test). CONVENTIONS grew **§61–§66**. Everything ran $0; no live provider call has ever been made — the operator's door run is the first.
>
> **⏭️ THE LN CONTINUATION POINT (2026-08-26, authoritative).** Batteries at close, coordinator-run: **Go 47 pkgs serial EXIT 0 · vitest 801/801 · tsc clean · lockgate 39 · ⚙ 118/33 · migrations 0001–0025.** The operator was handed the sitting instructions (chat, 2026-08-25): run **`./P3/gates/lane-test-door.sh`** from the repo root — it asks for the Z.AI key and the Kimi key (Extra-Usage-OFF typed `off` first), and ends with the URL + sign-in (alice, seed PIN) + click path. Expectations the operator was given: the FIRST task lands on anthropic (fresh world ties at 0%, strict `<`, tie → configured order — BY DESIGN); from the second on the less-consumed lane wins, receipt's Lane column is the proof; 5-hour windows expire — `./P3/gates/lane-test-door.sh budget` re-declares. A new session's duty: if the operator has run the door (or asks), **present the LN GATE BATCH** (plain-language walkthrough first; free-text authoritative; record in a gate file + STATE): **1** A11 amendment veto window (applied per the 2026-08-23 order) · **2** C5 no-household-personal-data rider — needs the AFFIRMATIVE yes, and told plainly it is recorded NOT machine-enforced (no per-lane data-policy seam exists) · **3** Balanced thinking-lever default (one-line data edit) · **4** cosmetic prompts→credits S00.9 · **5** the R28 fixture-regeneration deviation (§63) · **6** observed tier/model lists (ceremony step 7 instructions; seed ids `k3-256k`/`kimi-for-coding` are seed-only-pending-observation) · **7** the LN-1 skip-absorbs-defect lesson · **8** the OQ1 person-namespace question (broker `who` = OS name vs platform `auth.User.ID` vs `buildAcceptSurface` env read — LN-4 keys by broker `who`, visible in the startup line; the household mapping is unsettled) · **9** the ⚙ knob-split S00.9 proposal (`budget.background_window_fraction` double duty found at LN-6 — seeding fraction vs background ceiling; compounding is conservative; split = amendment + S18 sweep) · **10** the max-binds multi-window choice (alternatives named in the LN-6 brief §OQ-2 disposition) · **11** the sweep REPORTED-GAP: `POST /api/meters/plan-budget` awaits its 13.4 settings-tab consumer (gap pins moved 0→1 deliberately) · **12** the vitest-debt lesson — backend packets that move `web/src` fixtures owe the frontend battery (adopt as standing machinery) · **13** the ceremony-fed follow-up packets (Kimi wire-body reconciliation after `lane-key-ceremony.sh kimi-wire`; model-list reconciliation; Z.AI calibration recording). The parallel B6 gate stream (F1a+F1b first and alone, then W1–W4) is a SEPARATE queue, untouched by and not touching the LN work.

> **⏭️ 2026-08-27 UPDATE (supersedes the ⏭️ paragraph above where they differ; STATE's 2026-08-26/27 entries are the full record).** On the operator's 2026-08-26 order ("implement Kimi K3 not just via API but also via CLI, similar to claude code … then we test what is performing best") the LN campaign grew five more landed+pushed packets: **LN-7** the `kimi-cli` substrate (`internal/adapters/kimicli`, pin `@moonshot-ai/kimi-code@0.38.0`, A12) + lane `kimi-cli` sharing the `kimi-code` profile/pool · **LN-8** the door's engine step (8 steps now) + pool-aware budget + honest walk · **LN-9** the per-task lane pin (`pinned_lane` on POST /api/intake/requests, A13) + the `runs.lane`/`substrate` honesty fix (receipts now name the lane that RAN) · **LN-10a** `GET /api/intake/pinnable-lanes` · **LN-10** the lane-pin picker on the give-work door (design-reviewed; F2 pre-dispatch pin-invisibility = reported gap, follow-up LN-10b) + the walk's A/B RECIPE (paired same-goal tasks pinned kimi vs kimi-cli, alternated; tokens + "ran for" + Lane column; within-pool consumption caveat). **OQ-0 (Moonshot's interactive-only clause, found at LN-7 grounding — it binds BOTH kimi lanes) was RULED IN-SESSION: proceed, recorded acceptance (A12's Gate-A addendum).** Amendments now A1–**A13**; gate batch items 1–**15** (+the LN-10b disclosure); counters: lock **40** · vitest **810** · Go **48 pkgs** · §**69**. The operator's sitting: `./P3/gates/lane-test-door.sh`, two key pastes, the walk is the recipe. Deferred adds: LN-10b; stage.New commissioned-lane-without-seat guard; the 2026-08-26 17:57 opencode-install drift attribution (restored to pin 1.18.3); the stray npm kimi-code in the operator's `~/.npm-global`.

Authority order: **`Spec/core-architecture-v1.md` (frozen v1 + A1–A13; drafts canonical)** → BENCH-REG (signed) and FC-v1 → `P3/CONVENTIONS.md` (**§1–§66**) → frontend work: `.claude/skills/p3-implementation/FRONTEND.md` → `P3/design/product-map.md` v3 → **`P3/design/coldwalk-findings-2026-08-17.md` (the walk ledger) + `P3/design/rw19-rewalk-findings-2026-08-18.md` (the RW-19 exit walk)** → `P3/STATE.md` → this file. `Research/` closed archive; `Docs/` read-only.

---

## 1. Where the build is (2026-08-22, everything pushed through `c310b5f`)

**The interview-rework campaign that began at checkpoint 3 is COMPLETE through nine backend packets + six builder rounds + a five-walk exit gauntlet + the RW-19 exit walk. Every walk errand passes, none excepted.**

- **RW-13** classification runs zero-touch on the RUNNING intake run (no family question in the common path; the card is the honest fallback).
- **RW-14 A+B** no task wedges without a door (ladder/verify-outage cards, retry/cancel integrated with task lifecycle) + billed work lands or dies at $0 (stripped-revision verification, wrote-nothing V0 gate, write-aware routing, rank-echo fix). **RW-15** fresh worlds compose their first check. **RW-16** the engine's brace-short JSON heals in-session at $0 (bounded delimiter-only completion — provably never a value alteration); fired 3× live during a walk, invisible to the walker. **PH-1** the phrase seat works live (6.6 s/card at 90% headroom; degrades loud). **RW-17 + finish** the owner's accept reaches payload-pinned work (content hash = the pin; nothing pushed; door/card/verb tell one truth). **RW-18** the reader can finish the question: cancel who/why served (15.6-attributed, two-leg S02.2 derive covers pre-parity rows), deliverables born with their project, findings file once, History answers the cancel ask or refuses honestly.
- **RW-19 (2026-08-18, the final residue closed):** the cancel's why is HUMAN end-to-end. Backend (`63deabc..41f56f1`, four-stage, eval 5 findings none>low, 1 drained): History's `status.tasks_cancelled` serves `cancelled_by` by the SAME two-leg reading as the task page (pre-parity "—" dead); an optional one-line reason is captured at every cancel affordance through the ONE constructor `run.CancelDetail` (all four mint paths, additive `$.detail.reason`), served as `human_reason` on the task page (redacted at the edge) and as human words or the frozen `no reason given` + a plain-words `cause` column in History; over-cap refuses (400 `reason_too_long`), never truncates. FE (`7fee71f`): a shared `CancelWhy` ceremony piece on every cancel affordance + the telling surfaces render the words or the honest absence. **Targeted re-walk PASS — both routes answer who AND why and AGREE** ("This task was cancelled by alice — 'we changed our minds about this one'" / "…no reason was given").
- **Builder rounds:** the journey UI, the design-review drain, the cold-walk round (sign-in-first doors), the last-mile round (the result renders FIRST, downloads, plain-words accept), the final polish (preview letterbox dead structurally), and the RW-19 touch. Battery at close: **vitest 756/756, tsc clean, go 45 pkgs `-p 1` 0 FAIL, gofmt/vet clean.**
- **Exit gauntlet:** live design review (ship-bar MET) → five cold/return walks → the RW-19 re-walk. Journey to Done ✓, view/download/accept ✓ (real accept with PIN at $0), find the cancelled thing ✓, **the cancel's who/why ✓**, costs ✓, judgment trusted ✓.

## 2. The current act and what follows

0. **STREAM A (newest, ready): the LN lane sitting** — the door command, the click test, the LN gate batch (the ⏭️ continuation point above is the complete script for it).
1. **STREAM B: THE OPERATOR GATE SESSION — IN PROGRESS as of 2026-08-22** (free-text authoritative; the operator started the click-through themselves; rounds 4–5 recorded in STATE — F1a+F1b first and alone in a fresh session, then W1–W4). Fresh world via `./P3/gates/B6-clickthrough.sh` (sign in FIRST — the door enforces it); `P3/gates/B6-walkthrough.html` is the script. The gate bundles: **checkpoint 3** (the full give-work journey incl. view/download/accept), **the RW-12 content verdict** (= the S09 8.3 approval; the latency number to present is 6.6 s/card, not ~50 s), **the two queued S00.9 amendments** (bootstrap verification posture for command-less first tasks; intake spine check (e) for write-set-vs-class contradictions), and **operator-eyes items** (witness one PIN arming live; one wrong-PIN login failure; one real-keyboard End-key scroll check — CDP couldn't drive those). **Disclosure to make at the gate:** the clickthrough seed's `ask-brief` verify card 404s on any answer (pre-existing seed defect, prefix-less mint; the walkthrough never directs to it — don't judge the platform by that card). A future session entering here: read STATE's newest entries first — the operator's click-through findings and free-text answers are authoritative wherever they landed (chat, gate file, or STATE); record them in the gate record + STATE, close, and only then move to act 2.
2. **Then the deferred queue:** D6 upgrade ceremony (`sudo -v`, prod-DB backup first; `/usr/local/bin/sinet` is still the 20-July B2-gate binary) → bring-up per S19.6 (incl. intake-triage calibration — the classifier filed a website goal as CONTENT family; the S12.10 engine-bump check for the brace-short emission, OQ-e).

## 3. The deferred ledger (do NOT do unless asked)

Lives in STATE log entries + the walk ledgers' G-notes. Highlights, newest first:

- **NEW (LN-6 D10): the routing hot-path double-scan** — `bindingPlanReading` runs `ReadPlanUnits` once per quota, each a full per-user checkpoint scan (was 1 scan per plan lane per routing decision, now 2). Logged as a deferred efficiency note; never optimize it in a drain.
- **NEW (LN-4 eval R4, observed twice): two `internal/stage` live local-classifier tests bind to the host's llama-swap** (`TestLivePhraseAndSummarize`, `TestLiveIntakeTriageClassifiesWebshop`) — they fail when the host service is wedged or VRAM-starved and behave differently under `-run` filters vs full runs; a battery test bound to a live GPU service deserves a gate look under the serial-tests directive.
- **NEW (RW-19): the seed-fix micro** — the clickthrough seed mints its `ask-brief` verify card WITHOUT the `ask-verify-` prefix the answer route dispatches on (404 on any answer; builder-proven pre-existing). The same micro must determine whether ANY non-seeded ask can survive a cancel (`cancel.go:413` closes asks; the re-walk still saw "Answer its open card" dangle on a cancelled seeded task — friction-1 evidence says seed-shape, but prove it).
- **NEW (RW-19 re-walk G-items):** History answers wear raw DB column headers with who/why behind unsignaled sideways scroll (joins the answer-table clip G-note); the "CANCELLED — OPEN FOR WHY" board chip is stale now the why IS served (next builder pass copy touch); own-words asks bounce to a catalog pick more readily than the walker liked (RW-18 floor working as designed — routing-quality observation); spec citations still leak in receipts fine print + the card page (existing wire-side G-family).
- **NEW (LN-3): three carried items from the Kimi lane.** (i) **The C5 no-household-personal-data routing rider is RECORDED, NOT ENFORCED** — it rides `lanedata/kimi.json`'s `data_policy` row with the audit citation and `enforced: false`, and a grep confirmed no per-lane data-policy enforcement point exists anywhere in the tree; enforcement lands with the routing-policy seam, and the operator must be told that plainly at the gate rather than reading "rider applied". (ii) **OQ1(b), the pre-registered generalisation** — a ratified `semantics` member on `LaneSignalRow` replacing `classifyZAICode` with one table-driven classifier; it re-opens the zai path, so it is an S00.9 amendment for when a THIRD coded lane arrives, never smuggled in. (iii) **The kimi classifier rests on DOCUMENTED message strings, not observed wire bodies** — the ceremony's live single-request probe (one real 401, one real 429, one real 403 body if reachable, plus a header check for any reset stamp) must run before the table is treated as final; audit U3/U4/U5.
- Standing: the RW-16 forensic follow-ups (D7 finish_reason marker + seat-degradation run event); RW-15-F1 (per-run srt-config filename under raised lane caps); RW-13-F4 (ledger-decoration loss in the classify crash window); identityKey scope hardening (RW-18 F2, reinforced by RW-19 eval F5 — forged cross-owner event rows attribute within owner scope, unreachable from platform writers); Inbox risk-ranking (opt-in pitch above urgent; the month-old HIGH alarm "the platform arm is losing its own blind comparison" — REAL, in the operator's queue); drift-flood triage; `elapsed_s` wire fix; the rescan HTTP door (pairs with the bootstrap amendment); RW-12-R2 os.Remove; settings-tab clamped-⚙ amendment (now ALSO owed `cancelReasonMaxRunes = 280`, RW-19's flagged structural constant); GPU fleet seam; move-task-between-projects.

## 4. Machinery + constraints

- **Two pipelines, never mixed:** backend = SKILL.md four-stage (grounding → opus executor → Fable eval → drain, cap 2; amendments A–E live — tests-first, brief lifecycle, probe/tamper duties); frontend = FRONTEND.md (single Fable builder on committed canon, live review + cold walks gate). Walk personas (blind non-programmer / spouse) are the proven exit instrument — findings files under `P3/design/`, coordinator triages, ONE targeted re-walk per failed errand.
- **OPERATOR DIRECTIVES (standing, hard):** tests SERIAL (`go test -p 1`; `-race` one package at a time; one world at a time; never two batteries); local inference GPU-ONLY via `./P3/gates/local-stack.sh` (port changes per boot — use ITS printout); paid tests need their own explicit opt-in env (`SINET_RW14_LIVE_REWALK=1` class), never env-presence; reap orphans after every agent death (pkill patterns self-match, expect exit 144); memory ≥8 GiB before live legs. See memory `serial-tests-gpu-only`.
- **Worlds inventory** (state dirs under `$HOME`, all STOPPED): `.sinet-rewalk-a` (alice's ACCEPTED website — the pinned-accept fixture), `.sinet-b6-final` (the kept oversight world: cancelled task `t-fe5ff6c325967c3e`, shipping-policy deliverable, verdict #275), `.sinet-coldwalk-w1` (historical), `.sinet-builder` (untouchable step-3 evidence), `.sinet-drain-r1` (RW-16's crash evidence), **`.sinet-rw19-walk` (KEEP — the RW-19 exit-walk world; `walk-shots/` inside is the walk's screenshot evidence)**, `.sinet-rw19-builder` (builder throwaway :8484 — operator may rm at leisure), `.sinet-b6-clickthrough` (the operator's gate world, live during the gate sitting), **`.sinet-lane-test` (the LANE-TEST DOOR's world :8485 — exists only once the operator runs the door; marker-guarded, door-owned; `lane-test-door.sh stop|clean` are its verbs; its broker + control plane deliberately outlive the script)**. Never reseed evidence worlds; fresh walks use fresh dirs. `tools/dbq` = read-only SQL peek (never staged).
- **Cross-stream walls:** `web/src/fixtures/` writes are backend-only (contract snapshots); `web/src/kanban.ts` never moves; the operator's `B6-clickthrough.sh` hunks stay uncommitted; explicit-pathspec staging + index.lock retry; briefs are single-use (EXPIRED stamps — never read old ones as truth).
- Chrome pairing dies on reboot — the operator re-pairs with `/chrome` (it was live 2026-08-18; assume dead after any reboot since). The engine-hook fallback fork-bomb is pinned in the harnesses (HookCmd) — never remove those pins.

## 5. Invariants (load-bearing)

The spec wins; D1–D10 fixed; adopt-don't-fork + exact pins + lockgate (engine pin 2.1.218 — brace-short handled OUR side per RW-16; the next S03.3 bump re-runs the RW-9 T1–T4 tripwire AND checks OQ-e); every ⚙ through the registry (**118/33**); money read never computed; honest absence fails loud; NO auto-kill (the wall test enumerates `CancelTaskAtCard`); content-vs-telemetry; escape-by-default; secrets never committed; host changes proposed-then-approved; seeded worlds dev-only by compile boundary; model output is NEVER repaired beyond RW-16's provably-lossless delimiter completion (§60). **RW-19 frozen wire literals:** `human cancel (4.5)` + detail keys `cause/actor/ladder_invoked/ask_id/reason` (last two omitted-when-empty), served `human_reason` (absent = none given), History's `no reason given`, refusal code `reason_too_long`; the two ledger cancel prefixes are pinned printable-ASCII/non-whitespace-led (the divergence pin — a mint rewording trips the test, never silently splits History from the task page). Counters: migrations 0001–**0025** · lock **39** · CONVENTIONS §1–**§66** · vitest **801** (fully green — the campaign debt was closed 2026-08-25) · go **47 pkgs**. **New standing rule (gate item 12): a backend packet that moves `web/src` fixtures owes the frontend battery (vitest) at its landing, not just the Go suite.**

## 6. Host and environment state you inherit

- **Production untouched** through all of RW-19 (`tailscale serve` → Caddy :8481 → unit :8482; `/usr/local/bin/sinet` = the 20-July binary until D6). The gate click-through runs a THROWAWAY world on :8483 — it refuses production ports/paths by design.
- Local tier: user-level, STOPPED (restart via `local-stack.sh`; wisprflow can hold ~5 GiB VRAM — 4B fits beside it, 9B may not). llama-swap pin v241; weights in `~/.sinet-b45`.
- **Disk: root is 91% full (39 GB free, 2026-08-26)** — before anything disk-hungry (model pulls, new worlds, Timeshift) check first; the only Timeshift snapshot is `2026-08-21_23-47-22` and it INCLUDES `/home/sinep` (restoring rolls back projects too).
- Long-standing operator files, never staged: `Presentation/`, `Research/Presentation/`, `Sinet-Logo.jpeg`, `tools/dbpeek|dbq|rescanseed/`, the `B6-clickthrough.sh` hunks, `P3/gates/rw10-heal.sh`.
- GitHub `darian-gajgic`; everything through the **2026-08-26 wind-down commit** pushed (`git log` is the truth; the LN campaign closed at `eb65cf3`). A Fable-5 usage-limit stop is possible under heavy agent load — the window rolls over; on a trip, read-only work runs on opus, builder waits (STATE 2026-08-17 incident entries).

## 7. Model routing

Coordinator max effort. Backend: executors/finalizers `opus`; grounding/evaluation inherit Fable (`opus` when S10/S11-dense; lossless opus relaunch on any classifier trip). Frontend: builder/reviewers/walkers `fable`. Judge ≥ executor; fresh contexts; evaluation re-checks by the resumed evaluator; agents pause on their own background jobs — nudge them to run finishing batteries in the FOREGROUND.
