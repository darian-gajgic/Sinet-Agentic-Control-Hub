# P3 handoff — last rewritten 2026-08-18 (session close on the operator's context-budget directive): **THE GIVE-WORK JOURNEY IS RELEASE-QUALITY END-TO-END AND WALK-PROVEN; ONE SMALL RESIDUE (the cancel's human "why") STANDS BETWEEN HERE AND THE OPERATOR GATE SESSION.**

**Read this first, then `P3/STATE.md`.** This file is a *snapshot*; **`P3/STATE.md` is the single source of truth and outranks anything here.** If they disagree, STATE wins and this file gets corrected.

Authority order: **`Spec/core-architecture-v1.md` (frozen v1 + A1–A10; drafts canonical)** → BENCH-REG (signed) and FC-v1 → `P3/CONVENTIONS.md` (**§1–§60**) → frontend work: `.claude/skills/p3-implementation/FRONTEND.md` → `P3/design/product-map.md` v3 → **`P3/design/coldwalk-findings-2026-08-17.md` (the walk ledger — every walk, finding, and triage)** → `P3/STATE.md` → this file. `Research/` closed archive; `Docs/` read-only.

---

## 1. Where the build is (2026-08-18, everything pushed through `54d4e21`)

**The interview-rework campaign that began at checkpoint 3 is COMPLETE through eight backend packets + five builder rounds + a five-walk exit gauntlet:**

- **RW-13** classification runs zero-touch on the RUNNING intake run (no family question in the common path; the card is the honest fallback).
- **RW-14 A+B** no task wedges without a door (ladder/verify-outage cards, retry/cancel integrated with task lifecycle) + billed work lands or dies at $0 (stripped-revision verification, wrote-nothing V0 gate, write-aware routing, rank-echo fix). **RW-15** fresh worlds compose their first check. **RW-16** the engine's brace-short JSON heals in-session at $0 (bounded delimiter-only completion — provably never a value alteration; the latent pump wedge dead); fired 3× live during a walk, invisible to the walker. **PH-1** the phrase seat WORKS LIVE for the first time (think-phase decoupled: 6.6 s/card at 90% headroom where it silently produced nothing at 69 s; help fixed identically; degrades loud). **RW-17 + finish** the owner's accept reaches payload-pinned work (content hash = the pin; nothing pushed; door/card/verb tell one truth). **RW-18** the reader can finish the question: cancel who/why served (15.6-attributed, two-leg S02.2 derive covers pre-parity rows), deliverables born with their project (legacy heals at serve — the accept door answers the repo arm correctly), findings file once, History answers the cancel ask or refuses honestly (`status.tasks_cancelled` + the lexical no-match floor; the confident-mismatch class dead both ways).
- **Builder rounds:** the journey UI (family form, composing faces, error boundaries, the dialog-shield kill), the design-review drain (plan-card actual-assumed-values, armed PIN step-up, derived task names, one-row narrow topbar, honest cross-grain diff), the cold-walk round (sign-in-first doors — the dev-fallback approval circle cannot be entered; board fold cues + status-matching search), the last-mile round (the result renders FIRST, downloads, plain-words accept), and the final polish (the preview letterbox dead structurally; the two-walk scroll class root-caused to css-overflow-3 body propagation). Battery at close: **vitest 744/744, tsc clean, go 45 pkgs `-p 1` 0 FAIL**.
- **Exit gauntlet:** live design review (grades A−/B+/B−/B+, ship-bar MET after one drain) → five cold/return walks. Walk ledger: **every errand now passes** — journey to Done ✓, view/download/accept the result ✓ (alice completed a REAL accept with PIN at $0), find the cancelled thing ✓, costs ✓, judgment trusted ✓ — **except ONE: the cancel's "why"** (final gate walk 2026-08-18, below).

## 2. The next acts, in order

1. **RW-19 (micro, backend + builder touch) — the cancel's why becomes human.** Two walker-named fixes: (a) History's `status.tasks_cancelled` view serves `cancelled_by` via the SAME two-leg derive the task page uses (today pre-parity rows render "—" while the task page says "cancelled by op" — the view lacks the ledger-leg fallback); (b) a one-line optional human reason captured at cancel time (the {retry,cancel} card verbs + the task cancel), served into HUMAN DECISIONS and the History row, with the honest "no reason given" line when absent, and the mechanical cause ("finalize-with-card (4.5)") rendered in plain words. Builder touch: the reason input on the cancel affordances + rendering. Then ONE targeted re-walk of the errand.
2. **THE OPERATOR GATE SESSION** (free-text authoritative): fresh world via `./P3/gates/B6-clickthrough.sh` (sign in FIRST — the door now enforces it), `P3/gates/B6-walkthrough.html` is the script. It bundles: **checkpoint 3** (the full give-work journey incl. view/download/accept), **the RW-12 content verdict** (= the S09 8.3 approval; NOTE the gate read's latency number is now 6.6 s/card, not ~50 s), **the two queued S00.9 amendments** (bootstrap verification posture for command-less first tasks; intake spine check (e) for write-set-vs-class contradictions), and **operator-eyes items** (witness one PIN arming live; one wrong-PIN login failure; one real-keyboard End-key scroll check — CDP couldn't drive those).
3. **Then the deferred queue:** D6 upgrade ceremony (`sudo -v`, prod-DB backup first; `/usr/local/bin/sinet` is still the 20-July B2-gate binary) → bring-up per S19.6 (incl. intake-triage calibration — the classifier filed a website goal as CONTENT family, gate-read observation; the S12.10 engine-bump check for the brace-short emission, OQ-e).

## 3. The deferred ledger (do NOT do unless asked)

Lives in STATE log entries + the walk ledger's G-notes. Highlights: the RW-16 forensic follow-ups (D7 finish_reason marker + seat-degradation run event — one wire-contract micro); RW-15-F1 (per-run srt-config filename under raised lane caps); RW-13-F4 (ledger-decoration loss in the classify crash window); identityKey scope hardening (RW-18 F2); spec-cite strings in served prose (G-class, several sites); Inbox risk-ranking (opt-in pitch above urgent; month-old HIGH alarm never ages up — REAL alarm, in the operator's queue: "the platform arm is losing its own blind comparison"); drift-flood triage; `elapsed_s` wire fix; the rescan HTTP door (no operator-walkable capture→retry loop yet — pairs with the bootstrap amendment); RW-12-R2 os.Remove; the old standing items (settings-tab clamped-⚙ amendment, GPU fleet seam, move-task-between-projects).

## 4. Machinery + constraints

- **Two pipelines, never mixed:** backend = SKILL.md four-stage (grounding → opus executor → Fable eval → drain, cap 2); frontend = FRONTEND.md (single Fable builder on committed canon, pixels are done, live review + cold walks gate). Walk personas (blind non-programmer / spouse) are the proven exit instrument — findings files under `P3/design/`, coordinator triages, builders/executors fix, ONE targeted re-walk per failed errand.
- **OPERATOR DIRECTIVES (standing, hard):** tests SERIAL (`go test -p 1`; `-race` one package at a time; one world at a time; never two batteries); local inference GPU-ONLY via `./P3/gates/local-stack.sh` (port changes per boot — use ITS printout); paid tests need their own explicit opt-in env (`SINET_RW14_LIVE_REWALK=1` class), never env-presence; reap orphans after every agent death (killing an agent does NOT kill its children — pkill patterns self-match, expect exit 144); memory ≥8 GiB before live legs. See memory `serial-tests-gpu-only`.
- **Worlds inventory** (state dirs under `$HOME`, all STOPPED): `.sinet-rewalk-a` (alice's ACCEPTED website — the pinned-accept fixture), `.sinet-b6-final` (the kept oversight world: cancelled task `t-fe5ff6c325967c3e`, shipping-policy deliverable, verdict #275), `.sinet-coldwalk-w1` (the approval-circle trap world, historical), `.sinet-builder` (untouchable step-3 evidence), `.sinet-drain-r1` (RW-16's crash evidence). Never reseed evidence worlds; fresh walks use fresh dirs. `tools/dbq` = read-only SQL peek (never staged).
- **Cross-stream walls:** `web/src/fixtures/` writes are backend-only (contract snapshots); `web/src/kanban.ts` never moves; the operator's `B6-clickthrough.sh` hunks stay uncommitted; explicit-pathspec staging + index.lock retry; briefs are single-use (EXPIRED stamps — never read old ones).
- Chrome pairing dies on reboot — the operator re-pairs with `/chrome`. The engine-hook fallback fork-bomb is pinned in the harnesses (HookCmd) — never remove those pins.

## 5. Invariants (unchanged, load-bearing)

The spec wins; D1–D10 fixed; adopt-don't-fork + exact pins + lockgate (engine pin 2.1.218 — the brace-short emission is handled OUR side per RW-16; the next S03.3 bump re-runs the RW-9 T1–T4 tripwire AND checks OQ-e); every ⚙ through the registry (**118/33**); money read never computed; honest absence fails loud; NO auto-kill (the wall test enumerates `CancelTaskAtCard` now); content-vs-telemetry; escape-by-default; secrets never committed; host changes proposed-then-approved; seeded worlds dev-only by compile boundary; model output is NEVER repaired beyond RW-16's provably-lossless delimiter completion (§60). Counters: migrations 0001–**0024** · lock 38 · CONVENTIONS §1–**§60**.

## 6. Host and environment state you inherit

- **Production untouched** all session (verified across a real power-loss reboot — units auto-started, ladder swept clean; that's a live crash-safety proof for the gate read). `tailscale serve` → Caddy :8481 → unit :8482; `/usr/local/bin/sinet` = the 20-July binary until D6.
- Local tier: user-level, STOPPED (restart via `local-stack.sh`; wisprflow can hold ~5 GiB VRAM — 4B fits beside it, 9B may not). llama-swap pin v241; weights in `~/.sinet-b45`.
- Long-standing operator files, never staged: `Presentation/`, `Research/Presentation/`, `Sinet-Logo.jpeg`, `tools/dbpeek|dbq|rescanseed/`, the `B6-clickthrough.sh` hunks, `P3/gates/rw10-heal.sh`.
- GitHub `darian-gajgic`; everything through `54d4e21` pushed. A Fable-5 usage-limit stop is possible under heavy agent load — the window rolls over; on a trip, PH-1-class read-only work runs on opus, builder waits (see STATE 2026-08-17 incident entries).

## 7. Model routing

Coordinator max effort. Backend: executors/finalizers `opus`; grounding/evaluation inherit Fable. Frontend: builder/reviewers/walkers `fable`. Judge ≥ executor; fresh contexts; evaluation re-checks by the resumed evaluator; agents pause on their own background jobs — nudge them to run finishing batteries in the FOREGROUND.
