# P3 handoff — last rewritten 2026-08-06 (weekly-limit stop, mid-rework-step-2): **CHECKPOINT 2 FAILED → ANALYZED → OPERATOR ANSWERS → MAP v3; the working journey (map §7 step 2) is ~90% committed; P3-RW-2 (projects HTTP family) is BUILT and awaiting a FRESH evaluation relaunch. The next session: relaunch the RW-2 evaluation → triage/drain/land, relaunch the builder to finish step 2, then CHECKPOINT 3 = the operator completes the give-work journey end-to-end.**

**Read this first, then `P3/STATE.md`.** This file is a *snapshot* to orient a fresh session in two minutes. It goes stale; **`P3/STATE.md` is the single source of truth and outranks anything here.** If they disagree, STATE wins and this file gets corrected.

Authority order: **`Spec/core-architecture-v1.md` (frozen v1 + A1–A10; drafts canonical)** → BENCH-REG (signed) and FC-v1 → `P3/CONVENTIONS.md` (§1–§53; §3 now carries the amendment-A red-commit carve-out) → **frontend work: `.claude/skills/p3-implementation/FRONTEND.md`** → **`P3/design/product-map.md` v3 (the APPROVED product contract — v3 amendments ▲-marked, §8 record)** → `P3/design/rework-checkpoint2-findings-2026-08-06.md` (the 13-finding ledger + process rules P1–P4) → `P3/STATE.md` → this file. `Research/` closed archive; `Docs/` read-only.

---

## 1. Where the build is

- **Checkpoint 2 story (2026-08-06):** the operator walked the FULL app at 13%-rebuilt, failed it with 13 findings (ALL verified accurate — ledger in the findings doc), then on the root-cause analysis confirmed the scope misread, **confirmed the shell+Home design language verbatim ("very good, well done")**, and answered the three product questions. **Map v3 applied:** Describe-a-goal = button not tab (nav = 14 entries); Board = Backlog/Executing/Verifying/Needs-attention/Done, bounded scrolling columns, NO Cancelled column (cancelled renders in Backlog with a sign + why), Done visible, cards expand to plan-step/stage sub-items (D-A); task click = structured overlay card, never a page swap, no raw errors ever; Projects gains the create/onboard door.
- **Process rules P1–P4 bound** (findings doc §4): fence banners on every unreworked surface at any operator touchpoint; structure decisions rendered as pixels, never approved as prose; every checkpoint demonstrates a completable journey; seed hygiene (no test artifacts in operator walks).
- **P3-RW-1 (intake project pin): LANDED + VALIDATED** (`1ffa68a`+`a141e1d`+`04d56b0`; eval PASS after 1 round). Optional verbatim `project` on intake Submit; owner/member+ACTIVE server-side; loud 404/409/500; no task born on refusal; brief EXPIRED.
- **P3-RW-2 (projects HTTP family): EXECUTOR DONE, EVALUATION PENDING RELAUNCH.** Executor commits **`8f2a8f4`** (11 red acceptance tests incl. the HTTP visibility sweep) + **`0fc74d2`** (implementation): `GET /api/projects` (list: entry facts + capture_version + summary), `GET /api/projects/{project}` (full capture; members full depth; `store_path` served to NO ONE), `POST /api/projects` (`{project_id, name, remote_url?}`; `source` refused — fresh store always; dev identity refused; repeat-POST pending → 200 with existing refs, active → 409). OQ1–9 dispositions in STATE (log entry `081f52c`). **Three declared deviations + one executor-found security fix await the evaluation** (composition-root `onboardDoor` seam; `visibleProjects` delegation; brief's "redactionEdgeFiles exactly three" is a brief defect — it's been four since B6-7). The evaluation agent launched 2026-08-06 DIED WITH THE SESSION mid-run — **relaunch it FRESH** (not a failure-ladder strike).
- **Builder (rework step 2, "the working journey"): committed through the task card.** `981d831` fence banners (P1) · `f6e3dae` nav v3 + 900px density + Home door button · `b4faab0` the real Kanban (bounded columns, D-A sub-items, honest filters) · `6b30d42` the give-work door (submit → interview → plan card → approval; kills finding-5's dead submit) · `0d9bd74` Projects (cards + create/onboard door, already calling `POST /api/projects`) · `87b5a63` the task overlay card. The give-work journey was verified live through the plan card; the approve leg was verified blocked only by the (now fixed) `path_globs` seed defect. **Remaining for step 2:** finish/commit the in-flight `web/src/api.ts` RW-2 wiring, the `web/src/sweep.test.tsx` consumed-or-excepted disposition for the three new routes (the builder's, not a backend packet's), full screenshot verification of the whole journey on a RESEEDED world, checkpoint-3 PNGs to `P3/design/rework-screens/step2/`.
- **Seed hygiene (light path, both landed):** `cd7d527` (the fake "moonshot"-status task leaves the demo world + an undeclared-status seed guard) and `6065b8f` (`artifact_claims.path_globs` seeded as JSON — raw `**` 500'd plan approval; builder-found). **The live `~/.sinet-b6-clickthrough` world predates both — run `./P3/gates/B6-clickthrough.sh --clean` then relaunch before any operator walk.**
- Backend health at stop: `go test ./...` 45 packages green at `0fc74d2`; lockgate 38; settings 118/33; migrations 0001–0021; `internal/project` byte-untouched by RW-2.

## 2. The next session's first acts, in order

1. **Entry inspection (roll forward, never discard):** `git status` — expected dirt beyond the long-standing operator files (`Presentation/`, `Research/Presentation/`, `Sinet-Logo.jpeg`, `tools/dbpeek/`, operator's `B6-clickthrough.sh` edit + `B6-walkthrough.html` — never staged): possibly `web/src/api.ts` (builder in-flight RW-2 wiring — leave for the new builder unless already coherent) and possibly reverted GO files (= dead evaluation's un-restored mutation probe → `git checkout` them, NEVER commit probe residue). The builder was told to commit at stop; it may or may not have.
2. **Relaunch the P3-RW-2 evaluation FRESH** (stage 3; Fable inherit): executor content = exactly `8f2a8f4`+`0fc74d2`; verify against `P3/briefs/P3-RW-2.md` + OQ1–9 dispositions (STATE log `081f52c`) + the three deviations + the security fix; amendment-C duties (≥3 held-out probes, test-tamper diff — grounding tests' assertions byte-immutable, only `server()`/`projEnv` wiring was sanctioned). Then coordinator triage → drain (the executor context is DEAD → any drain round goes to a **fresh opus finalizer** per the dead-executor rule) → landing checklist (battery, spot-diff, brief EXPIRED, STATE, push).
3. **Relaunch the builder** (fresh `model: fable`, the committed-code-as-canon recovery proven 2026-08-06 — its committed commits + step-1/step-2 screenshots + Nexus source + map v3 + findings doc ARE its continuity; the operator's design-language confirmation rides verbatim). Scope: finish step 2 (list above), then checkpoint-3 prep. **The session needs Chrome tools live (`claude --chrome` or `/chrome`).**
4. **Checkpoint 3 (operator):** they COMPLETE the journey on the reseeded world — create/see a project → describe a goal into it (button) → interview → approve the plan → watch the board → open the task card. Free-text answer authoritative. Then autonomous through map §7 steps 3–6 (Inbox → Reviews → insight surfaces → system surfaces).
5. **Exit per FRONTEND.md rule 5** (live design review → cold walks → machine battery → operator click-through), **then the deferred queue:** D6 upgrade ceremony (`sudo -v`, prod-DB backup first, binary still the 20-July B2-gate build) → bring-up per S19.6 (D2 paid sweep ~$2.39 spent of the ratified lines, local-tier wiring in `~/.sinet-b45`, calibration writes, B6 report §8 standing items).

## 3. Operator-deferred (do NOT do unless asked)

- GPU/VRAM + local-seat fleet-seam wiring; CPU/RAM host monitoring (S00.9; never re-propose unprompted); move-task-between-projects; the settings-tab constants-as-clamped-⚙ amendment (`settings-tab-see-change-everything`).
- The assistant's conversational brain (Jarvis behavior) is a BRING-UP item — the rework fixes its form/flow/styling only; the operator knows and accepted this framing.

## 4. Machinery + constraints (two pipelines — do not mix)

- **Frontend:** FRONTEND.md (one long-lived Fable author — relaunch-with-committed-canon is the sanctioned recovery from transcript eviction, twice used; reference-over-prose; screenshot loop, built artifact, two widths; Banned list load-bearing) + P1–P4.
- **Backend:** SKILL.md four-stage with A–E (tests-first red commits per CONVENTIONS §3 carve-out; executor opus; evaluator ≥3 probes + tamper-diff; briefs single-use → EXPIRED at landing; light path for no-behavior changes — both seed fixes rode it, coordinator-inline with full battery).
- **Cross-stream constraints:** `web/src/kanban.ts` must not MOVE or rename (the Go seed guard reads its declared-column list from that path); the sweep consumed-or-excepted disposition for `/api/projects` belongs to the BUILDER; concurrent agents stage by explicit pathspec only, retry on index.lock; the `release-notes` seeded project holds an active `["**"]` W claim — a plan whose globs intersect it hits the honest S02.8 collision consequence, DESIGNED behavior, and a fresh project approves clean.
- STATE discipline unchanged: update before/after every step; commit; push after milestones (coordinator pushes; the step-1 builder self-pushed once — logged process note, don't repeat).

## 5. Invariants (unchanged, load-bearing)

The spec wins; D1–D10 fixed; adopt-don't-fork + exact pins + lockgate; every ⚙ through the registry; no load-bearing metered paths; money read never computed; honest absence fails loud; live-verify real-world facts; no auto-kill; content-vs-telemetry (it decided RW-2's OQ3: no operator limb on project content); escape-by-default (allowlist EMPTY; preview iframe = the one raw-HTML channel); capability URLs are secrets; secrets never committed; host changes proposed-then-approved; bundled assets only; AGPL ideas-only; seeded world dev-only by compile boundary; TAILWIND SCANS PROSE; cascade-collision class → verify in the BUILT artifact in real Chrome.

## 6. Host and environment state you inherit

- **Production untouched** — `tailscale serve` → Caddy 127.0.0.1:8481 → unit on 8482; `sinet-control`+`sinet-broker` active; `/usr/local/bin/sinet` = 20-July B2-gate binary until D6.
- A stale seeded world may be serving on **:8483** (this session relaunched it with held-open stdin; it predates both seed fixes) — `--clean` + relaunch before any operator-facing use; the operator's own created rows there (`t-2aa79cba8f813bb1` "Webshop car parts", the chat session) die with the wipe, which is fine.
- Local inference tier installed user-level, NOT running (`~/.sinet-b45`). B5 organs installed, canaries disarmed. AppArmor userns finding still parks confined service-context runs (hardening session).
- Counters at stop: Go 45 pkgs green at `0fc74d2` · lock 38 · ⚙ 118/33 · migrations 0001–0021 · escape allowlist EMPTY · CONVENTIONS §1–§53 (§3 amended) · web vitest was 709/31 pre-step-2 (presentation-coupled suites being rewritten as the design settles; the RW-2 route sweep may be red until the builder's disposition lands — expected, out of Go scope).
- GitHub identity: active `gh` account must be `darian-gajgic`. Everything through `cf90fdb` + the builder's step-2 commits is pushed (verify at entry).

## 7. Model routing

Coordinator at max effort. Backend: executors/finalizers `opus`; grounding/evaluation inherit Fable (S13/S15 scope — not S10/S11-dense; lossless opus relaunch on any classifier trip — zero trips so far). Frontend: builder/reviewers/walkers `fable`. Judge ≥ executor everywhere; independence = fresh context + hazards named in advance.
