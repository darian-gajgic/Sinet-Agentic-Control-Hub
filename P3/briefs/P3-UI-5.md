# P3-UI-5 — D8 pass I: the oversight surfaces — mission control (+ personal filters), board, task detail (+ the timeline rail), fleet (+ the session-read hoist), workforce

**Grounded 2026-08-05.** Fifth packet of the UI batch, first of the three D8 passes. Contract: `Spec/core-architecture-v1.md` (drafts in `Spec/drafts/` canonical) — §S15.5 (drafts/S15-frontend-api.md:74–84), §S15.10 (:125–127), §S15.3 (:49–53), §S15.12 (:137–142). Design authority: the operator-APPROVED `P3/design/design-approach-proposal.md` (§1 look, §3 pattern vocabulary, §5 binding constraints — subordinate to the spec on all behavior). Mandate: the B6 gate record `P3/gates/B6-report.md` §9 D8 + A-1 ("the design pass owns intelligibility, not just looks — surfaces and empty states that teach what they are, not only admit they are empty") + A-2's first clause ("every surface teaches its contract"). Scope source: the `P3/STATE.md` UI-batch queue row for P3-UI-5 (:150). Idiom inheritance: CONVENTIONS §47 (kit + tokens + motion + D5), §48 (controls idioms; the Fleet deviation this packet closes), §49/§50 (held≠busy; honest-assertion discipline). This brief is self-contained: the executor and the evaluation see no chat history. The CONVENTIONS record this packet writes is **§51**.

**THE FRAME: restyle and make self-explaining WITHOUT changing behavior.** API contracts, events, scoping, routes, verbs all FROZEN — no new route, no new event type, no scoping change, **zero production `.go` change expected** (everything this packet renders is already served; if an honest render is impossible without a read, STOP and raise it under the §48 OQ1 narrow-read mechanism — an EXISTING store reader at minimal shape, never a blind addition). The one sanctioned behavior-adjacent change is the **App.tsx hoist** (wall 2 below). The full tour/one-shot-hints layer and ToastProvider's production mount are **UI-7's** — what rides HERE is each surface's one-line "what this is" header line and teaching empty states (the kit `EmptyState`'s what+why), per proposal §3's self-teaching layer.

**THE THREE WALLS — state them back before building, police them at evaluation:**

1. **Byte-unchanged:** `web/src/routes.ts` · `router.tsx` · `live.ts` · `events.ts` · `api.ts` (no new member — no new read exists to declare) · `controls.tsx` · `kanban.ts` · `Login.tsx` · `Inbox.tsx` · `Chat.tsx` · `Deliverable.tsx` · `Memory.tsx` · `Settings.tsx` · `settingsForm.tsx` · `PushDevices.tsx` · `ConnectionState.tsx` · every `web/src/ui/*` production component (**`ui/Timestamp.tsx` included — the UI-4 carried cross-note rides ONLY if another named requirement legitimately opens the file; none here does, so it stays byte-unchanged and the note stays carried**) · every production `.go` file · `Spec/` entirely · `web/src/fixtures/api/*.json` (reads mint nothing — **no fixture regeneration is expected; `cursor` stays 89 with zero drift on a regeneration re-run**) · `package.json`/`package-lock.json`/`components.lock`/`go.mod`/`go.sum` (**no new dependency, no new lock entry — §47's runtime set is CLOSED; the donut and the rail are owned JSX/SVG**).
2. **Diff-censused: `App.tsx`.** The diff is EXACTLY the Fleet route arm at :201 gaining the hoisted identity props (R20) — nothing else: `navGroupOf`/`navIcons` (:42–62), the C-1 arms (`devFallback` :104, both bounce clauses :105–116, the Sign-in/Sign-out affordance :165–179), `useSession` (:284–311), `useConnection` (:319–346), `safeNext`, and every other route arm byte-untouched. The C-1 pins are the regression net: `shell.test.tsx` :385 (picker reachable), :407 (real session still bounced), :421 (Sign in never a dead Sign out), :446–450 (375px legs), :454 (the full cycle) must pass **unmodified**.
3. **Zero mutation affordances on the workforce map** (§45-B): the DOM walk and the source scan (`scans.test.ts:249–277`) both stand — the restyle adds **no button, no form, no act-pointing link** there; teaching empty states on `/workforce` use `EmptyState` WITHOUT its `action` prop.

Also binding, carried from the batch rules: bundled assets only, no CDN, no runtime fetch · escape-scan allowlist EMPTY (banned 8; file floor >50, `escape-scan.test.ts:97`) · **AGPL: Odysseus is ideas-only, forever — an explicit §51 attestation item** · every ⚙ number registry-routed (**this packet consumes NONE and creates NONE — verified: no surface here reads a registry key; the client display constants that exist are the §42 structural-constant class, see R25**) · assertion counts never drop silently · bundle measured at landing vs the UI-4 reference · Layer-1 catalog stays 50/50, `internal/history` untouched.

---

## 1. Where the packet starts (verified ground, 2026-08-05)

UI batch 4/7; all fourteen D3 controls built, sweep gap list 0 routes / 0 shapes, `declaredButUncalled()===['health']`. Counters at the UI-4 landing (the entry battery reproduced them byte-exact): vitest **592/27** · Go **45 pkgs / 14 sanctioned skips** · lockgate **38/574** · bundle **JS 1113.19 kB / gzip 335.21 · CSS 54.99 / 11.75 · index.html 1.80 · 15 font assets / 417.30** · golden `cursor` **89** · ⚙ **118/33** · inventory **102/5/107** · migrations 0001–0021 · escape allowlist EMPTY. Assertion baselines, measured this grounding by the landed method (`grep -c 'expect('`): mission 137 · board 76 · taskdetail 148 · fleet 128 · workforce 183 · filters 49 · shell 68 · sweep 79 · scans 29 · responsive 54 · design 53 · ui/kit 61 · ui/timestamp 18.

## 2. The scope reconciliation (OQ1 — dispose before the executor launches)

Three sources disagree about the five-surface list. The STATE queue row (:150) names "mission control, board, task detail, fleet, **workforce**". S15.5's own title (draft :74) is "mission control, board, task detail, fleet, **filters**" — workforce is S15.10. CONVENTIONS §47's migration ledger (:1543) assigns "oversight surfaces (B6-5) + task detail, fleet, **filters**" to UI-5 and "the S15.10 workforce map" CSS-shed to **UI-7**. The file evidence that resolves it:

- **Filters is not a route — it is mission control's own route.** `FilterBar`/`FilterView` mount inside MissionControl (`MissionControl.tsx:105–106`); the four views are `/?view=<id>` (`Filters.tsx:19–22, :45–47`; §42-B). The queue row counts ROUTES; S15.5 counts SURFACES. Restyling `/` without Filters.tsx would restyle half a page.
- **The five oversight ROUTES are `/`, `/board`, `/tasks/:id`, `/fleet`, `/workforce`** (§41-B route table) — exactly the queue row's list.
- **No later queue row carries filters or workforce**: UI-6 (:151) is inbox/settings/login+push; UI-7 (:152) is chat/review/assistant-card/self-teaching/seeded-world. Deferring either would leave it row-less — the UI-4 fourteenth-control class of miss (a count and a list reconciled by nobody, §50).
- **The ledger is a UI-1-time forecast, already drifted elsewhere**: it assigns "the assistant" to UI-6 while the queue puts chat in UI-7. STATE's queue is the binding cut.

**Recommendation: BOTH ride UI-5** — Filters.tsx restyles+sheds here (S15.5's own list, the ledger's own row, and the route it lives on), and Workforce restyles+sheds here (the binding queue row). §51 records a dated cross-note on §47's ledger row (workforce-map shed moved UI-7 → UI-5 by the queue cut; UI-7's row then covers the review surface + assistant + the remaining shared blocks). **Pre-authorized A/B seam if the sitting overruns:** A = mission control + filters + board + fleet + the App.tsx hoist + the donut; B = task detail + the rail + workforce. A clean stop at A is a stated success mode.

## 3. Requirements A — the D8 idioms, cross-surface

- **R1 — the one-line "what this is" header.** Each of the five surfaces gains one subline under its `<h1>`, in the platform's own terms, consistent in placement and treatment across all five (executor's layout/wording taste; the FACTS are pinned): mission control = one live screen of the household's work — running, queued, parked, blocked-on-a-human, recently finished — with the consumption meters beside it (S15.5 ¶1, :76); board = tasks as cards moving through their stages live from the feed; dragging re-ranks only your own queued work and never moves a stage (S15.5 ¶2, :78; §39-B); task detail = the whole story of one task — specification, plan, stage progress, live activity, every human decision, deliverables, receipts (S15.5 ¶3, :80); fleet = what is running on whose account at what burn rate, accounts always distinguished (S15.5 ¶4, :82; D2); workforce = the machinery — every worker you may see, its equipment and connections, and per-version outcomes; view-only at v0 (S15.10, :125–127). **Truth constraint (evaluation hazard #4):** the copy must not contradict the section it teaches — the board line must never claim the drag changes priority/stage (it is a tie-break hint, Board.tsx:186–189's landed sentence is the model), the task-detail line must not call a draft "confirmed" (§38 ruling (a), TaskDetail.tsx:32–36), the fleet line must not imply totals exist (accounts are never summed, Fleet.tsx:19–23), the workforce line must say view-only.
- **R2 — teaching empty states.** Every served-empty arm on the five surfaces migrates from bare `Empty`/prose to the kit `EmptyState` (`what` + `why` — what will appear AND what causes it to; `ui/EmptyState.tsx`), teaching per the A-1 mandate. The landed empty sites: mission-control buckets (`MissionControl.tsx:112` — per-bucket `why`: what puts a run in THIS bucket, from the §42 bucket semantics :54–74); board own-queue (:213) and per-column (:233); task detail stages :443, decisions :510, deliverables :555, runs :589; fleet no-lane-match :102, no-switch :205, no-budget-lane :291; workforce no-workers (`Workforce.tsx:75–84` — the landed S08.6 compose-when-earned prose is already the best teaching copy in the tree; it moves into `EmptyState` with its sentence kept). **Two hard rules:** (a) `EmptyState` renders only on a SERVED empty — the none-vs-not-loaded distinction stays (`parts.tsx:134–136`; `Freshness`/`Skeleton` own loading); (b) on `/workforce` no `action` prop (wall 3). `parts.tsx`'s `Empty` itself STAYS exported (other surfaces still consume it; its remaining call sites shed at their own packets).
- **R3 — restyle in the approved language, kit primitives only.** Panels/sections/chips/dots/tiles from `web/src/ui/` (§47 export contract :1535); tone via the one chip formula (a tone is a TOKEN NAME, §47 :1537); no raw hex outside `index.css`'s token block (scanned); JetBrains Mono `tabular-nums` on every figure, id, timestamp, status token (§47); density variables respected; motion only as feedback and only `motion-safe:`-authored (R5). The kit is not modified — a needed variant that doesn't exist is a STOP-and-flag, not an inline fork.
- **R4 — the CSS shed, consumer-scoped.** `index.css`'s per-surface sections shed as their markup moves to utilities: "oversight surfaces (B6-5)" (:633) and "task detail, fleet, filters (B6-5 part B)" (:825), plus "the S15.10 workforce map" (:1537) under OQ1's disposition. **Shed ONLY rules whose every consumer is this packet's surfaces** — shared rules stay with a dated note at the section head: `.absent`, `.owner`, `.money`, `.muted`, `.error`, `.notice`, `.warn-flag`, `.table-scroll`, `.block` (Deliverable/Memory/Settings still consume `Section`), `.run-state` and siblings where Inbox/Deliverable markup still carries them — verify each by grep before shedding, and record the shed list in §51. Nothing is renamed. The history panel's rules (:789–820) belong to mission control's section and shed with it (History.tsx restyles, R15). Bespoke pieces Tailwind shouldn't own (proposal §2) may stay in the sheet — the donut ring keyframe/geometry if CSS-expressed, inside the reduced-motion gates.
- **R5 — both motion scans green, and the kit scan's blind spot closed.** VERIFIED this grounding: `design.test.ts` gates stylesheet-declared motion; the kit `motion-safe:` scan's glob is `./*.tsx` **relative to `web/src/ui/` — it does NOT reach surface files** (`ui/kit.test.tsx:306`; the :371 glob that does reach them checks only the @base-ui import wall). The restyle authors motion utilities in surface files, so the packet must WIDEN the motion-utility scan to cover `web/src/**/*.tsx` surface sources (extend the kit scan's glob or add the sibling scan in `sweep.test.tsx` — executor's placement, both-way planted probes required, §47 :1528 model). Scans widen, never narrow. `design.test.ts` passes with any new stylesheet animation inside the gate blocks.
- **R6 — phone-first at 375px, per restyled surface.** The landed responsive assertions are structural reads of `index.css` (`responsive.test.ts` — `.columns` :107, `.filter-bar`/`.fleet-filters` :126–127, `.card-face dd` :118, `.decisions li`/`.mode-note` :136, `.block[data-stale='true']` :203, the fixed-width list :28–65) and `sweep.test.tsx`'s phone-complete duty table pins container classes per route (:777–785: board → `.columns .column .card`, fleet → `.fleet-filters .table-scroll`, task → `.block .table-scroll`, mission-control → `.filter-bar`). **Every one of those PROPERTIES must survive the restyle**: where a selector sheds, the assertion is honestly RE-POINTED at the new structural form (utility patterns asserted from source, or the remaining CSS) — never deleted, never weakened to vacuity; phone-first stays min-width-only (no `@media (max-width:` enters); wide content scrolls inside its own box; the stale marker stays VISIBLE in the stylesheet, not only as a data attribute. New surfaces of this packet (the rail, the donut, banners, headers) each get their own 375px leg.
- **R7 — honest assertion updates (evaluation hazard #1, the batch's highest-yield class).** The restyle rewrites many selectors, so markup-coupled assertions churn heavily. Discipline: per-file pre→post `expect(` counts measured by the landed method and recorded in §51 (baselines in §1); **nothing drops silently — and counts alone are not the bar**: each rewritten assertion must still assert the same FACT (or a stronger one) about served data, verbs, scoping, frames; an assertion whose selector was updated but whose predicate went vacuous is a finding. The §45-B RES-1 lesson binds: where a fact is pinned by attribute, the rendered TEXT is pinned too.

## 4. Requirements B — mission control + personal filters (+ the history panel)

- **R8 — behavior preserved, enumerated.** The two `useLive` reads (`/api/runs`, `/api/meters`) on `missionEventTypes` (MissionControl.tsx:84–95; live.ts:90); `bucketRuns`'s disjoint buckets + honest catch-all (:54–76 — every served row still placed, the driven test stays); `recentlyFinishedWindowMs` 24h unchanged (:33); `Date.now()` read per render, no ticker (:97–100); every `RunLine` fact (task link via `hrefFor`, state, wedged flag, owner, stage, lane, waiting-on-human, ParkedUntil, last-activity `Timestamp live` :133–153); `MetersPanel`'s three honesties (:156–236 — `assumed` label rides every reading; pressure only when `pressure_applicable && pressure !== null` else the absence with its denominator reason :202–208; budget remaining only when declared :209–215); the `ViewBlock` absences (:241–248); the filter bar + `?view=` URLs (Filters.tsx:45–47, 66–81); `what-needs-me` linking INTO `/inbox/:id` and `/deliverables/:id` (:139–176); `mine` sending `?person=` (:180–199, the sweep :955 pin); `localMidnightISO` staying Filters-only (sweep :1011–1021); the D5 `live` sites (Filters :155/:171/:234, MissionControl :148) keeping relative-beside-UTC.
- **R9 — the StatTile row (MAY, recommended).** Mission control may lead with kit `StatTile`s carrying the bucket counts (running/queued/blocked/parked/recently-finished). A count of the served rows on screen is presentation over the served list (the CancelTask confirm's `live.length` precedent, TaskDetail.tsx:277) — but the §45-B R2 lesson binds: the label must say what is counted ("runs on screen", the buckets' own semantics), tiles render figures from the first frame (NO count-up, §47 :1538), and no tile invents a figure no list on the page carries.
- **R10 — never-stall-silently, mission control's leg** (see §8 for the map). The blocked-on-a-human bucket and every wedged row upgrade from terse flags to the proposal's inline-banner treatment with plain words and the honest door: a `Link` to `/inbox` (the answering surface; §42-B's what-needs-me idiom). No new verb, no new read, no card-id join invented — `RunListItem` serves no card id, so row banners link to the SURFACE, and only reads that already serve an id (`ApprovalItem.id` in what-needs-me) deep-link `/inbox/:id`.
- **R11 — the history panel restyles with its host.** `History.tsx` markup/classes only: the panel stays a QUERY INSTRUMENT (`data-live="query-instrument"`, not live, no `useLive`, §42 :908); ask/search/card/answer behavior pinned by mission.test.tsx stays byte-true (card-choice SELECTS and never blind-fires; `q` verbatim; layer-2 flag both directions; server texts verbatim — §50). `AnswerView`'s render contract is shared with Chat — restyle must not change its data contract (Chat.tsx is byte-frozen this packet and still renders it).

## 5. Requirements C — the board

- **R12 — behavior preserved, enumerated.** ONE `<Droppable>` (own-queued lane, Board.tsx:195), stage columns plain lists; `hintPostsFor` refuses everything else (:100–125, the synthesized-drop negative stays); plain-ascending rank order with 0 the neutral middle (:78–87); spaced ranks + the clamp edge (:52–57); `applied:false` notes surfaced + re-read (:150–177); keyboard-drag driven tests stay; the unknown-column forward tolerance (:221–251, `data-known`); project grouping incl. `(no project)`; **the CardFace stays EXACTLY the S1.3 set** (:256–308; board.test.tsx:51 pins it "exactly") — the restyle re-skins the face (left priority rail treatment, chips, tone) but adds NO field and drops none; never-stall on a card is the landed waiting line (waiting-on-human / ParkedUntil), restyled, not widened.
- **R13 — the drag affordance honesty.** A card that cannot be reordered must not look draggable (§42 :900) — the restyle keeps drag affordances (handles, lift hover) on the own-queued lane only.

## 6. Requirements D — task detail + the timeline rail (the packet's centerpiece)

**The grain finding (binding; evaluation hazard #2).** The Odysseus idea is a "tool-call timeline"; the honest Sinet grain is **stage/lifecycle/decision** — no landed read serves a per-tool-call history (the run card serves only the CURRENT `tool` name + args digest, api.ts:445, and the one-line `last_activity`, :461; `tool.called` is registered DECLARE-ONLY with no producer, `internal/eventlog/contract.go:626`, and `tool.completed` frames are triggers, not data — REST is the truth, §42 :892). The rail therefore renders **only these served facts**, all already on the surface's two landed reads (`GET /api/tasks/{id}` + `GET /api/runs/{run}` for the active run — TaskDetail.tsx:51–56, :370–380):

| Rail material | Served source | Distinct treatment |
|---|---|---|
| step nodes | `stage_progress[]` rows: type ∈ stage.started/stage.finished/intake.state, stage, kind, ts (api.ts:333–341) | the dot-on-rail step; uppercase stage name (proposal §3) |
| **error node** | `stage.finished` with `outcome: 'error'` (contract.go:431 vocabulary completed/split/error) | visually distinct failure node |
| **split node** | `outcome: 'split'` — the S02.5 recovery-ladder fork | distinct recovery node |
| **park interval** | `receipt.park_history[]`: parked_at, resumed_at?, **served `duration_seconds`**, park_reason, resume_cause, `ongoing` (api.ts:393–400; landed render TaskDetail.tsx:655–669) | distinct stop node; `ongoing` renders "still parked" |
| **wedge flag** | active run card's `wedged: true` (api.ts:462) — the run.wedged / watchdog pause-and-flag projection (contract.go:427) | distinct stop node on the live lane |
| human nodes | `decisions[]` (actor, decision, card_type, decided_at — api.ts:355–368) + card `waiting_on_human` | decision/blocked nodes |
| terminal | `runs[].state` verbatim (api.ts:416) | terminal node; the client owns NO state vocabulary — an unrecognized state renders its served value plainly (§42/§48 forward tolerance; TaskDetail.tsx:184's list stays what it is) |
| live tail | card `last_activity.{type,line,ts}` + current stage/tool + counters (api.ts:454–461) | the rail's live node region — the collapsed-output-tail idea at the grain served |

- **R14 — the rail is the stage story's new form.** `StageBlock`'s flat list (:439–462) may become the rail; `LiveActivity`, `DecisionsBlock`, `ReceiptsBlock`, `SpecBlock`, lineage may compose around it (executor's layout) — **on the obligation that every landed served fact still renders** (stage name/type/kind/outcome/ts; unnamed-stage absence; decisions' every field incl. `(as operator)`; receipts byte-for-byte incl. park history, mode note verbatim, direct-use label as served; counters monotonic with the unpriced marking, TaskDetail.tsx:420–429) **and every landed test property survives** (honestly re-pointed per R7). Both cancel controls, their confirms, and the `useAct` idiom are byte-preserved in behavior (`controls.tsx` is walled).
- **R15 — elapsed/duration without a ticker (carried note 5; the no-ticker scans must pass UNMODIFIED).** Closed park intervals render the SERVED `duration_seconds`; the running node renders the SERVED `counters.elapsed_s` (frame-frozen, exactly as today's ":419"); an open stage's "running for…" MAY derive at render from the served start instant via the already-exported `relativeLabel` (`ui/Timestamp.tsx`, imported from the module — the parts.tsx precedent) or the `Timestamp live` primitive — freezing with the feed, corrected by the UTC beside it (the D5 design). NO `setInterval`/`setTimeout`/`requestIdleCallback` anywhere (sweep :887–901 and chat.test.tsx pass byte-unmodified). Closed-stage durations, if rendered, derive from the two served instants at render. Node instants render through `Timestamp`/`Stamp` ONLY — the `<time>` file pin stays exactly `['./parts.tsx','./ui/Timestamp.tsx']` (sweep :977) and "no view formats a date itself" (:991–1008) passes unmodified.
- **R16 — never-stall-silently, the rail's leg.** Park nodes carry reason + horizon + the door: parked-run release is an INBOX act (`api.resumeRun`, consumed at Inbox.tsx:1227–1233 — §48 OQ2: point, never re-implement) — the node links to `/inbox`. Wedge nodes say what pause-and-flag means in plain words and link to `/inbox` (the watchdog card's home). Gate-ask/blocked nodes link to `/inbox`. Cancel remains the landed control. No `Continue` verb fires from this surface.
- **R17 — the rail's fixtures.** The golden task bodies may lack error/split/park/wedge arms; those node tests use **hand-scripted per-test bodies, each citing its Go producer file:line** (outcome vocabulary `internal/stage/stageevents.go`; park history the receipt shape; wedged the run card) — the §49 error-contract precedent. **No golden regeneration** (a driven park/cancel would mint events and move `cursor` 89 — out of scope). Both directions per distinct treatment: the marked node renders distinctly AND an ordinary step does not acquire the marking.

## 7. Requirements E — fleet + the session-read hoist

- **R18 — behavior preserved, enumerated.** The one `/api/meters` read on `missionEventTypes` (Fleet.tsx:33–38); selectors narrow DISPLAY only, default-all (:41–43; sweep :920–953's exact-partition entry and the hides-nothing probe stay); limit-status table facts (:75–103); `AutomationPanel` + `PauseSwitch` (two position verbs, P-T08-4 explainer clauses, served detail verbatim, inbox pointer link — :181–256); `BudgetPanel` renders NO figure (:258–301), `servedUnit` off the wire (:311–321), `wholePositive` strict parse (:332–337), confirm copy, held≠busy; `LocalTierSeams`' two named absences byte-kept (:491–504); `MetersPanel`/`ViewBlock` shared with mission control; the money/units probes (no `$`/`usd`/… token in the editor; no arithmetic over budget figures).
- **R19 — never-stall-silently, fleet's leg.** Lanes with `parked_runs > 0` and the limit-events view render in the banner treatment with the ParkedUntil horizon and the `/inbox` door — same rules as R10.
- **R20 — the hoist (carried note 1; closes §48's recorded deviation).** `Fleet.tsx`'s `useMayAct` (:146–165) stops reading `GET /api/auth/session` itself; App.tsx hands Fleet the identity exactly as it hands Board/MissionControl/Deliverable (:194–225): `me = session.user?.user_id ?? ''` and `operator = session.dev === true || session.user?.role === 'operator'` — **the predicate byte-preserved from Fleet.tsx:152** (dev-posture equivalence INCLUDED; this is deliberately NOT Memory's users-row-role-only flag, App.tsx:232–244 — the two differ and must stay different). `may` becomes a pure derivation from props; the "nothing actionable before the session lands" property is now App's own `session === null` loading arm (:185–186) — structurally identical. Under `devFallback`, `me=''`/`operator=true` — behavior byte-equivalent to today. **Tests:** fleet.test.tsx mounts pass props; every authority leg (member-self, operator-on-other, member-offered-nobody-else's — §48 D4) re-fired not merely re-rendered; **the new negative asserted: Fleet's fetch log contains NO `/api/auth/session` read** (one read, one source — §41-B); at least one composition leg through App proves the prop plumbing (the §49 D2 composition lesson). The §48 deviation bullet gets its dated closed cross-note in §51.

## 8. Requirements F — workforce

- **R21 — behavior preserved, enumerated.** The one GET on `workforceEventTypes` (Workforce.tsx:34–40; live.ts:258–272); both scope sentences rendered (`roster_scope`/`outcome_scope`, :58–63); all truncation statements (:64–73 + versions/verdict-tally flags); domain grouping with forward tolerance (unknown domain/kind/status/maturity/verb — §45-B, all five driven tests stay); the degraded-domain marking with its three consequences (:108–124); definition-vs-granted separate renders (:141–160); superseded versions collapsed in `<details>` (:166–177); ceilings as absences never zeros; per-run money as served with the unpriced marking; the one-`useLive`-mount literal; the escape floor still names `./Workforce.tsx` (§45-B :1308). The restyle re-skins cards/grid onto kit primitives; **walls 1+3 hold: zero acts, no api member beyond `api.workforce`**.
- **R22 — teaching empties + header per R1/R2**; the no-workers state keeps the S08.6 sentence; NO `action` prop.

## 9. The threshold donut — ONE capacity idiom, one mount (carried note 3)

- **R23 — the ring renders the SERVED pressure and nothing else.** One owned component (new export in `parts.tsx`, beside `Money`/`ParkedUntil` — the §42 honesty home; the kit deliberately owns no figure primitives, ui/index.ts doc), mounted in **`MetersPanel` only** (serving both mission control and fleet at once). Ring fill derives from `lane.pressure` **only when `pressure_applicable && pressure !== null`** (api.ts:182–191); otherwise the landed absence with its denominator reason renders UNCHANGED and no ring appears — honest absence renders as absence (§42). The numeric pressure figure STAYS rendered beside the ring (mono, tabular) — the ring is glanceable redundancy, never a replacement. **No other capacity render exists on these surfaces** (run counters are monotonic with no denominator, api.ts:435–437 — no ring; workforce serves no ratio — no ring); if a later packet wants one, this component is the idiom.
- **R24 — the scans, pre-cleared.** The geometry derivation must not re-compute pressure from consumption/budget (that would be a second implementation of the S10.4 gauge — the §48/§39-B forbidden twin); it consumes the served ratio. Code hygiene: the money-word scan flags arithmetic on lines carrying usd/cost/consumption/budget/burn/money/spend (`scans.test.ts:48–52`) — keep geometry lines free of those words (derive from a local named for pressure); the progress-token scan bans percent/pct/completion_fraction/eta/progress_bar identifiers (:93) — name nothing that way (`ThresholdRing`, `data-capacity`, arc/sweep vocabulary). `parts.tsx:35–37`'s "no progress bar…" doc sentence gets its dated amendment: the ring renders a SERVED ratio, which is the exact carve-out the sentence's own rationale ("the API serves none") never covered — pressure IS served.
- **R25 — the thresholds are display constants, structural with named reasons** (§42 :905 precedent; interim under the standing settings-tab directive): green below 0.75, orange 0.75–<1.0, red ≥1.0 — 1.0 because pressure is consumption-over-declared-budget so ≥1 is the budget reached (a served semantic, not a policy guess), 0.75 as the glance-ahead band. Colors via tone tokens only (the status hues through the chip formula / `--tone` binding — no raw hex; the no-raw-hex scan holds). **The ring must NOT claim the admission threshold** — where background work actually stops being admitted is a backend ⚙ this client cannot read; the legend/title says "of declared budget", never "cutoff".
- **R26 — driven both ways.** Ring present+colored per band over served bodies (all three bands exercised, hand-scripted lanes admissible with cites); ring ABSENT with the reason line on `pressure_applicable:false` and on `pressure:null`; a planted `USD`-word arithmetic probe and a planted banned-token probe show the scans still bite (both-direction probes, the landed pattern).

## 10. Cross-cutting requirements

- **R27 — CONVENTIONS §51** written per the §48–§50 model: decisions, deviations under evaluation scrutiny, the shed list, the reconciliation disposition + §47 ledger cross-note, the §48 Fleet-deviation closure note, measurements (bundle itemized, assertion tables), AGPL attestation.
- **R28 — full battery in landing order:** `cd web && npm run typecheck && npm run test && npm run build` · `gofmt -l internal/ cmd/ tools/` empty · `go vet ./...` · `go build ./...` · `go test ./... -count=1` over the REAL new dist (45 pkgs / 14 skips) · `go run ./tools/lockgate` (38/574). Frozen-path byte-checks per wall 1 (spot-diff against the pre-packet commit).
- **R29 — bundle, measured + itemized vs the UI-4 reference** (JS 1113.19 / gzip 335.21 · CSS 54.99 / 11.75 · index.html 1.80 · fonts 15/417.30). **CSS is EXPECTED to move BOTH ways** — index.css sheds the per-surface sections while Tailwind compiles new utilities; the §51 record itemizes the two directions (sheet delta vs compiled-utility delta — Tailwind scans sources, so the utility half is identical across import-graph experiments, §49's note). JS growth is rail+donut+banners+copy scale; if beyond copy-scale, split by controlled builds (§48 method). No new package; SPA stays un-split.
- **R30 — ⚙: NONE consumed, NONE created — and that is the requirement.** The client reads no registry key; the only new client numbers are R25's two display thresholds, recorded as §42-class structural constants with reasons (and flagged to the settings-tab directive's standing list). `internal/settings/index.go` byte-unchanged at 118/33.
- **R31 — adopted components: NONE new.** §47's runtime set is CLOSED; lockgate 38/574 unmoved; the ring and rail are owned JSX/inline-SVG (the §45-B owned-rendering resolution's own allowance).
- **R32 — seams left, named:** tour + one-shot hints, ToastProvider production mount, assistant-card rework, seeded demo world → UI-7 (the queue row); inbox/settings/login-push restyle → UI-6; the shared CSS blocks + preflight-compatibility block leave with their last consumer (§47 :1534); no person-level decision feed (unlanded, §48 :1569).

## 11. Acceptance checklist (the evaluation rubric — verify each by your own run, not the executor's word)

1. **Walls:** every wall-1 file byte-identical to the pre-packet commit (spot-diff); App.tsx diff = exactly the Fleet-arm hoist props (wall 2); zero production `.go` diff; `Spec/` untouched; fixtures untouched with `cursor` 89 and a zero-drift regeneration re-run; lockgate 38/574; escape allowlist EMPTY.
2. **C-1 pins pass unmodified:** shell.test.tsx :385/:407/:421/:446–450/:454 — the hoist regressed no arm (hazard #5).
3. **Per-surface behavior preservation** per R8/R12/R14/R18/R21: every landed data consumption, verb, scoping leg, frame subscription and honesty arm still asserted; the §42/§45-B driven negatives (bucket catch-all, synthesized drop, member/operator scoping, forward tolerances, zero-mutation walk) all green.
4. **The rail maps onto served facts only** (hazard #2): every distinct node class traces to a row/field in §6's table; NO node class exists for tool-call history, watchdog cards, or any fact absent from the two landed reads; unrecognized states render verbatim-tolerant; both directions per distinct treatment (marked marked, unmarked unmarked).
5. **No ticker:** sweep :887 and chat.test.tsx no-poll scans byte-unmodified and green; elapsed/durations derive from served figures/instants at render (R15); `<time>` file pin exactly two files, no-format scan unmodified (hazard: a rail node rendering `<time>` inline).
6. **The donut** (hazard #3): rendered only from served `pressure` under its two landed gates; absent-with-reason otherwise; figure beside ring; band probes both ways; no money-word arithmetic line, no banned token; tone tokens only; the "cutoff" claim absent.
7. **Never-stall-silently:** parked/wedged/blocked/limit states carry the banner treatment with plain words + the `/inbox` door; NO new verb fires from oversight surfaces; `api.resumeRun`'s one call site is still Inbox.tsx (assert).
8. **Headers + teaching empties:** all five surfaces carry the R1 line and R2 empties; copy contradicts no S-section fact (hazard #4 — check each against S15.5/S15.10's own sentences); workforce empties carry no action; none renders over an un-served (loading/error) state.
9. **Assertion accounting** (hazard #1): per-file pre→post vs §1's baselines, measured by the landed method; nothing down silently; spot-check ≥5 rewritten assertions per big suite for predicate honesty (not merely selector churn); no scan narrowed, no floor lowered, the motion scan WIDENED per R5 with live probes.
10. **Phone 375px:** every R6 property survives re-pointing (phone-first min-width-only; no fixed-width container; scroll-inside-own-box; stale marker visible; duty-table containers current); new renders (rail, donut, banners, headers, tiles) each have a leg.
11. **Bundle** measured against the UI-4 reference with the two-way CSS itemization and JS attribution (R29); fonts unchanged; no new package.
12. **Golden fixtures zero-drift, cursor 89** — reads mint nothing; rail arms hand-scripted with Go file:line cites (R17).
13. **Battery green in landing order over the real dist**; counters: vitest ≥592 (count may grow, never shrink)/files, 45 Go pkgs, ⚙ 118/33, inventory 102/5/107, catalog 50, migrations 0001–0021.
14. **AGPL attestation in §51:** zero Odysseus code/CSS/markup/assets — the rail, donut, banners and etiquette are REIMPLEMENTED ideas in this codebase's own idiom (proposal §3's parenthetical; §5).
15. §51 written; every deviation from this brief RECORDED with its reason — a refusal-with-evidence beats silent compliance.

**Hazards named in advance (attack these):** the batch's highest-yield class — passes in jsdom / passes against a fixture, false in production — lands here as: (1) markup-coupled assertion churn silently weakening predicates behind stable counts; (2) a rail node class for an event the platform never mints (`tool.called` is DECLARE-ONLY; watchdog cards are not on the task reads); (3) a donut over a figure nobody served (or re-derived from consumption/budget — the second-gauge twin); (4) teaching copy contradicting the S-section it teaches (board-drag authority, draft-as-confirmed, implied totals, workforce editability); (5) the App.tsx hoist regressing a C-1 arm or collapsing Fleet's dev-inclusive operator predicate into Memory's role-only one; (6) an `EmptyState` rendering over a loading/error state, erasing the none-vs-not-loaded line; (7) a responsive re-point that inverts phone-first or drops the stale-marker visibility; (8) the kit motion scan left un-widened while surface files gain ungated motion utilities; (9) a shed CSS rule that still had a consumer on a frozen surface (grep every shed rule); (10) an inline `<time>` or a `toLocale*` sneaking in with the rail.

## 12. Files expected to change

`web/src/MissionControl.tsx` · `Filters.tsx` · `Board.tsx` · `TaskDetail.tsx` · `Fleet.tsx` · `Workforce.tsx` · `History.tsx` (restyle-only) · `parts.tsx` (the ring export + `Empty`-site migrations on this packet's surfaces; everything else byte-kept) · `App.tsx` (the censused hoist) · `index.css` (the shed + any bespoke ring/gate rules) · tests: `mission.test.tsx`, `board.test.tsx`, `taskdetail.test.tsx`, `fleet.test.tsx`, `workforce.test.tsx`, `filters.test.tsx`, `shell.test.tsx` (composition leg only — the C-1 pins unmodified), `sweep.test.tsx` (duty containers re-point + motion-scan widening if placed here), `responsive.test.ts` (re-points), `ui/kit.test.tsx` (glob widening if placed there), `doubles.ts` (only as test wiring needs) · `P3/CONVENTIONS.md` (§51 + the two dated cross-notes). Everything else: wall 1.

## 13. Open questions for coordinator disposition (pre-launch)

- **OQ1 — the scope reconciliation (§2).** Do Filters.tsx AND Workforce both ride UI-5? **Recommendation: yes to both** (evidence in §2: filters is mission control's own route and S15.5's/§47's own row; workforce is the binding queue row and no later row carries it), with the §47 ledger cross-note dated in §51 and the §2 A/B seam pre-authorized. Alternatives: (b) workforce → UI-7 (amend the UI-5 queue row + UI-7 row, dated) — defensible but re-opens the row-less-item class; (c) filters deferred — indefensible (no row would ever own it).
- **OQ2 — rail grain.** The rail as specified is stage/lifecycle/decision grain over the two landed reads — no per-tool-call history exists on any landed read, and wiring one (e.g. a run-events trace read) would be a NEW read beyond the §48 OQ1 precedent's "existing store reader, minimal shape" bar and beyond D8's behavior-preserving frame. **Recommendation: stage grain stands for this batch**; if the operator wants tool-grain playback later, it enters as its own sanctioned-read decision post-batch. Dispose explicitly so the executor never improvises a read.
- **Note (non-blocking):** `ui/Timestamp.tsx` is expected byte-unchanged, so the UI-4 carried cross-note does NOT ride this packet — it stays on the standing list for whichever packet next legitimately opens the file.
