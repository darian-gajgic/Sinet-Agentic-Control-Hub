# P3-B4-5 brief — S12.1–S12.4 + S12.8: local serving stack + model set + duty-alias registry

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets; format per `P3/briefs/README.md`; predecessors `P3/briefs/P3-B4-2.md`–`P3-B4-4.md`). The executor and the evaluation agent work from THIS file plus the cited spec sections (`Spec/drafts/` canonical: **S12.1–S12.4 + S12.8 owning**; S03.2 lane facts; S06.10 duty map; S01.2 unit slot; S16.1–S16.4 rail; gate records `Research/decisions/GATE-3-spec-readiness.md` Def.8 + D3.5, `GATE-2-substrate-and-adoption.md` Def.6/Def.8) — never from chat history. §7 is the evaluation rubric. The spec wins over this brief; a conflict found while executing is reported, never papered over.

**BINDING coordinator reading (`P3/STATE.md` phase status 2026-07-22 — do not relitigate):** S12.1 consumer class (a) — engine runs ON local models via the opencode adapter — has **NO v0 consumer**; the second engine adapter is NOT in this cut. Every S12.4 registry duty is class-(b) "no engine session involved". The local lane exists as metering/routing vocabulary; no engine ever dispatches onto it in this packet.

**Grounding verdict: no hard spec conflict or impossibility that blocks the packet.** Nine boundary readings are logged in §8 (each section-cited; evaluation re-checks them). The load-bearing ones: (1) `sinet-llamaswap.service` IS rendered by `internal/units` — the B0-era package-doc line "local-model units … are deliberately not generated here" is superseded for this unit by the STATE directive and S12.2's Sinet-named unit; (2) the S08.8 tie-break duty rides the `utility` alias (S12.4's registry names no routing duty — flagged); (3) `resolveSeat`'s `LocalAvailable` ERROR stub retires into honest behavior split by consumer class; (4) class-(b) $0 D7 rows ride checkpoint rows on the consuming run with a usage-block local override (lane is joined from the RUN row today — `internal/metering/ledger.go:95`); (5) the local lane prices a TRUE $0 (S12.1 free tier), distinct from the S10.3 flat-lane guard.

**HARD HOST GATE (absolute, binds every requirement):** user-level installs ONLY. NEVER any driver, kernel, system-CUDA, or system-package change; NEVER `nvidia-persistenced` (S12.8); no systemd unit installed or started; no tailscale/live-Caddy/production-unit state touched (front chain serve→8481→8482 is LIVE). A genuine host-package need (e.g. missing `gcc`, `nvidia-container-toolkit`) is a **FLAGGED OPERATOR DECISION recorded in the packet report — never performed**. Sanctioned network legs for THIS packet (differs from B4-3/B4-4): llama.cpp + llama-swap release-pin resolution, S12.3 model downloads, license-card fetches. Zero paid engine calls; the one tier-L smoke is local and $0 by construction.

---

## 1. Numbered requirements

### A — Serving stack adoption (rail) + toolchain resolution

- **R1.** NEW package (suggested `internal/local`; §2-conformant name is executor discretion) — the S12 platform-plane surface: duty-alias `/v1` client, alias resolution, llama-swap config generation, eager-unload verb, GameMode hook machinery, models manifest, conformance suite. Import wall (R24) binds.
- **R2.** **llama-swap adopted** at a **live-verified release pin** (S16.3 row: TBD-P3, "v240 observed current" — resolve from CURRENT upstream releases, never memory): user-level install of the upstream release binary (no build needed — upstream ships static binaries; verify live). Lock entry materializes per its S16.3 row (`organ-unit`, MIT — path-scoped verify at the pinned ref; funeral plan = llama-server router mode + Sinet kill-idle timer, or Ollama; watch = bus-factor-1 conformance-suite row asserting the YAML/endpoint contract on every bump). Full S16.4 walk in the entry; **operator #10 → B4 gate**.
- **R3.** **llama.cpp `llama-server` adopted** at a live-verified `b`-tag pin (S16.3 row). **The toolchain path is LIVE-RESOLVED at executor time** (S12.2: "Linux CUDA ships no prebuilt binaries — one-time source build or the project's container image"; host facts: NO nvcc, docker present, RTX 5070 Ti 12227 MiB). Decision tree, in order, from CURRENT sources: **(a)** prebuilt Linux CUDA release binaries IF upstream now ships them → user-level unpack; **(b)** user-level CUDA toolkit (runfile/tarball into a user dir — toolkit only, **never** the driver) + one-time source build, IF the host build tools suffice (probe `gcc`/`cmake`; a missing host package = flagged operator decision, `cmake` may be user-level installed); **(c)** the project's official container image via docker ONLY if (a)+(b) are blocked AND docker is usable without host changes — GPU-in-container needs `nvidia-container-toolkit`, whose absence is a flagged operator decision, never an install. Record the chosen path + rationale in the lock-entry notes AND the packet report. CUDA placement semantics (per-model `CUDA_VISIBLE_DEVICES=<GPU-UUID>`, R10) are spec-fixed — a non-CUDA build (Vulkan etc.) is NOT a substitute; if no path lands CUDA user-level, STOP and flag.
- **R4.** **Ollama standby entry** (S16.2 standby rule: a pre-registered fallback named by a funeral plan MUST exist as a `standby` entry): `kind: standby`, name + observed-current version per the S16.3 row ("v0.32.1 observed; pin on activation"), MIT core, exempt from the CI running-unit rule. R2/R3's funeral plans name it.
- **R5.** **Exactly these three lock entries** (llama-swap, llama.cpp, Ollama standby) — 14 → 17. Any further implied adoption (a D-Bus Go library, an HF download tool as a dependency, anything): **STOP and flag** (R14's `busctl` posture and R7's download mechanics avoid this). Zero new Go modules expected; `go.mod` unchanged.

### B — S12.3 model set: downloads + card-verified licenses + provenance

- **R6.** **Disk re-check FIRST** (161 GB free recorded 2026-07-22; the set is ~30 GB): re-verify free space before any pull; insufficient space = stop-and-flag, never a partial silent set.
- **R7.** **Download the v0 model set minus the embedder** (S12.3 table): workhorse **Qwen3.5-9B Q5_K_M**; bakeoff alternate **Gemma 4 12B QAT first-party int4** (B4-7 needs it); fast tier **Qwen3.5-4B Q4_K_M**; entailment **Granite Guardian 8B** (pool12-fitting GGUF quant per the S12.3 quant policy — Q5/Q6 ≤9B; KV cache stays fp16/q8_0, never quantized); entailment alternate **Bespoke-MiniCheck-7B** (CC-BY-NC 4.0 — the sole ratified license exception, recorded per-seat with its reason); CPU floor **MiniCheck-Flan-T5-0.8B** (license per card); contradiction pre-screen **DeBERTa-class NLI cross-encoder** (~100 MB; the spec names a class, not a card — executor picks the concrete model from current sources, license-verified, and records the pick as a coordinator-flagged choice for B4-7's consumer); Layer-2 SQL **Arctic-Text2SQL-R1-7B** (G3 D3.5 — enabled at v0; its query-surface consumer is S14/B5, noted in the manifest). **The embedder (Qwen3-Embedding-0.6B) is NOT pulled and NOT configured** — the G2 Def.8 vector gate (evaluate embeddings only at ≥5% lexical-miss rate or >5,000 entries) has not opened. Download mechanics are executor discretion within the host gate (direct HTTPS fetch or a user-level tool); every file's sha256 recorded.
- **R8.** **Licenses verified ON THE MODEL CARD ITSELF** at pull time (S12.3: "aggregator roundups hallucinate models and licenses"), verify date recorded. **A card contradicting a spec-table license cell (e.g. the Gemma 4 12B QAT row says Apache 2.0 — Gemma-family cards historically carry the Gemma Terms) is a spec-vs-reality conflict: record the card's actual license, flag the per-seat exception to the coordinator/B4 gate, never silently adopt.** Apache-2.0/MIT is the default rule; MiniCheck-7B is the sole pre-ratified exception.
- **R9.** **Models manifest = platform data, NOT lock entries** (S16.1: "Local models are not manifest entries"; §8 reading 6): a committed, versioned manifest (code table or strict-JSON data in the R1 package) carrying per seat: seat name, model id, card URL, license {SPDX-or-card-name, verify date}, file name(s) + sha256, quant, pool, context length. Consumed by config-gen (R10) and by the D7 row's model-hash field (R18); the S12.9/S12.10 battery+swap machinery (B4-7) keys off (duty, model hash, engine build) — this manifest is where the hash lives. **Model weights are never committed**; they live in the shared read-only model cache (S12.1/feature 4.1) — a structural-config root (default under the state/dev dir; the settings-tab-directive precedent), operator-relocatable, flagged to the gate.

### C — Config generation + the unit slot

- **R10.** **llama-swap config generated from the manifest + ⚙, never hand-written** (S12.2): per-model `env: CUDA_VISIBLE_DEVICES=<GPU-UUID>` (UUID live-read via `nvidia-smi` — never an index; indices renumber on eGPU attach); **`pool12` swap group** holding every GPU-seated model (members hold the pool exclusively — loading one unloads the others); **`resident` group EMPTY** (S12.8 revision: no always-resident slot, RTD3 mutual exclusion); `pool24` NOT configured (S12.11 dormant); per-model TTL from ⚙ `local.ttl.fast_s` (fast/CPU-class seats) and `local.ttl.workhorse_s` (workhorse-class seats); loopback listen address (structural config, P-T13-2 posture); no embedder member. The generator asserts against llama-swap's REAL config schema at the live-resolved pin (behavior/probe, never docs — P-T15-2); CPU-seat members (DeBERTa/Flan-T5) are configured only as far as llama.cpp actually serves their architecture at the pin — a non-servable seat is recorded honestly in the manifest ("no serving path at v0 pin; consumer B4-7") and flagged, never faked into config.
- **R11.** **`sinet-llamaswap.service` GENERATED, never installed** (S12.2/S01.2; STATE directive; §8 reading 1): rendered by `internal/units` into the owned generated set — `ExecStart` = the user/operator-installed llama-swap binary + generated config path (structural config, NOT the sinet multi-call binary — llama-swap is an adopted organ, no new `cmd/` or mode); loopback bind; own journal identity; `User=sinet`; the S01.2 hardening set as far as compatible (GPU device access needs exceptions, recorded in-file with reasons per S01.2); **`Slice=` placing the tier in the local-inference slice** (S12.2: the designated systemd-oomd victim; slice NAME `[coordinator-draft]` — suggest `sinet-local.slice`; render a minimal slice file with a comment that oomd/weight POLICY is S10's, deferred to B4-6/hardening — no policy values invented). The units package doc-comment line claiming local-model units are never generated is corrected (cited to S12.2 + the STATE directive). Install = B4 gate / hardening session.

### D — Eager-unload verb + GameMode hook + TBD-P3 probe

- **R12.** **Eager-unload verb = the ratified operator-wins switch** (S12.2/G2 Def.6): one control-plane action that (1) **stops local-lane admissions** — at v0 that means the R16 duty client refuses new calls with the eager-unload reason (every consumer degrades per its S12.4 row; no local-lane RUNS exist in this cut) — and (2) calls llama-swap's unload endpoint (`POST /api/models/unload` per R16 §4.5 — verify the exact route at the pin). Plus the symmetric resume. Surfaced as: a surface act constructed at the shell composition root (card DATA for B6 — the B4-3/B4-4 `buildAcceptSurface` precedent) AND a CLI verb (a `sinet` tool subcommand, §7 sense — never a new mode). CLI transport is executor discretion within S12.6's constraint (management verbs exist ONLY on the platform plane, loopback): direct llama-swap call for the unload leg + the admission-stop leg via the surface/loopback API, with the dev posture honestly documented if the full remote flip needs B6. Both actions idempotent, event-recorded (provisional names, refs-not-blobs, owner-attributed).
- **R13.** **GameMode hook** (S12.2/G2 Def.6; ⚙ `local.gamemode_hook` on/off gates the whole behavior): (a) a **generated** `gamemode.ini` `[custom]` start/end snippet calling the two R12 verbs (gamemode.ini is USER config — the snippet is rendered output + a documented operator install step, NEVER written into any home directory by the packet); (b) a control-plane **D-Bus subscription** to `GameRegistered`/`GameUnregistered` — implemented WITHOUT a new Go dependency: a supervised subprocess of systemd's own `busctl` (monitor mode) riding the existing OS-mechanisms/host-systemd posture (§8 reading 8; a D-Bus Go library would be a net-new S16.4 adoption — stop-and-flag if genuinely unavoidable). Scripts fire even while the control plane restarts; the subscription covers the daemon's own restarts (both legs per spec). Contention detection beyond GameMode (NVML graphics-process delta) is NOT built — B4-6-adjacent monitoring, seam note only.
- **R14.** **TBD-P3(GameMode user-context probe) EXECUTED LIVE** (S12.2; R16 §7-OQ7; gamemoded is present on this host): does a per-user `gamemoded`'s session-bus signal surface reach a system-scope subscriber (the control plane runs as the `sinet` system user)? Probe with user-level tooling only (`gamemoderun`/`gamemoded` + `busctl --user monitor` from the operator context vs a system-context observation; no host changes). **Result file in `P3/measurements/` (dated, G3 Def.8 discipline: question, method, raw observation, verdict, consequence).** The verdict BINDS the R13(b) implementation: reachable → wire it; not reachable without host changes → the subscription runs where it can (user context) or is recorded deferred-with-finding + flagged to the gate — the scripts leg carries the duty either way. Either outcome is spec-satisfying; an unexecuted probe is not.

### E — Duty-alias registry: platform-plane /v1 client + $0 metering

- **R15.** **Alias resolution rides ⚙ `local.alias` (TypeMap, declared with the S12.4 default map — `internal/settings/index.go:691`)**, read by dotted key at call time (CONVENTIONS §6 map-family discipline). The client resolves duty alias → seat/model via the map + manifest; unknown alias = loud error; the `embedder` alias refuses with the post-gate reason (G2 Def.8 — row declared, target not served). Alias changes land only through the S12.10 swap gate (B4-7) — no swap machinery here, the map is read-only data to this packet.
- **R16.** **Platform-plane `/v1` client** (S12.1 class (b); S12.6 planes are B4-6 — this client IS the platform plane: loopback, direct to llama-swap, no tokens/bridge): `POST /v1/chat/completions` with per-duty request shaping per the S12.4 cross-cutting rules — **temperature 0 / greedy for every classification-shaped duty; the schema appears in the prompt AND is enforced at the engine (`response_format json_schema`, GBNF where schema cannot express it — engine-enforced validity, live-verified at the pin, never assumed from docs); every schema carries an explicit abstain/`unclear` member (a model is never schema-forced to fabricate a label); free-text reasoning precedes the constrained region; per-duty length caps** (structural constants — S18 ratifies no key; the §7 `sseBatchSize` precedent, settings-tab flag). Logprobs are requested/exposed on this plane (the only v0 logprob lane — S03.2/SPIKE P2-S1; consumers: S12.5 margins B4-7, drift canary B5).
- **R17.** **Absent-stack degradation:** the local stack unconfigured (dev default) or unhealthy at call time degrades EXACTLY per each duty's S12.4/S06 row — classifier fail-closed (high tier), spot-check skipped, utility deterministic fallback text, tie-break deterministic order — with the absence/failure in the recorded reason. Existing intake/routing tests keep passing with nil seams (the shipped degradation paths are regression-pinned).
- **R18.** **Every local call writes ONE D7 ledger usage row** (S12.1: "either consumer class … model hash + engine build + tokens, priced at $0"): realized as a checkpoint row (`gates.Checkpoints`) on the **consuming run** (every v0 consumer has one: intake seams ride `<task>.intake`, tie-break rides the routing run context), whose usage block carries an explicit **local marker** — `{lane: "local", duty alias, resolved model, model file sha256 (the manifest hash), engine build (llama.cpp b-tag), input/output tokens}` — because lane is currently joined from the RUN row (`internal/metering/ledger.go` `checkpointRows`); the metering fold HONORS the per-row lane override (§8 reading 4). Tier-1 measured. A call site with no run in a checkpointable state is a surfaced defect (loud log + event), **never a silently unmetered call**.
- **R19.** **$0 pricing + zero-allowance receipt lines** (S12.1/S06.10): the metering price path treats lane `local` as the permanent free tier — **priced $0 by construction** (labeled, e.g. "free tier / zero allowance"), NOT `UNPRICED`, and explicitly distinct from the S10.3 never-price-a-flat-lane-from-$0 guard, which continues to bind PAID flat-rate lanes (§8 reading 5; a conformance test pins both behaviors side by side). Receipts itemize local duty lines (ceremony purpose derives from the run suffix as shipped) as zero-allowance lines; local rows are readable per-lane by the S10.4 pressure gauge (the D5 floor — no new pressure machinery).

### F — Consumers wired: intake seams, utility seat, TieBreak retirement

- **R20.** **Intake duty seams get REAL local implementations** (S06.10/S06.2/S06.7(a); seams shipped nil-degrading at B2-2 — `internal/intake/intake.go`: `Classifier.Classify`, `Utility.Help`, `SpotCheck.Check`): implemented in the R1 package against the alias registry (`intake-triage` for Classify — `json_schema` enums for family/stakes/size + data-bearing, the classifier may only ADD the data-bearing flag (S06.2 rule 4, enforced by the existing pipeline); `utility` for Help; `utility` for the advisory coverage spot-check per its S12.4 row). `stage.Config` gains the three seam fields passed through to `intake.Pipeline` (fields exist — `pipeline.go:46-49`); the shell wires them only when the local stack is configured. Deterministic floors and fail-closed behavior are the PIPELINE's and remain untouched.
- **R21.** **Routing TieBreak seam gets the real local-alias impl** (S08.8 step 2; B3-3 left `TieBreaker` nil — `internal/worker/routing.go:169`, `stage.Config.TieBreak`): implemented against the **`utility` alias** (§8 reading 2 — flagged), constrained to a schema of {pick: one of the candidate ids | abstain, reason: one line, length-capped}; abstain or any error → the deterministic degraded order with the reason recorded (the shipped path). The pick is advisory input to a human-visible card (S08.8 re-route/pin) — consistent with the utility row's "this seat never decides". Wired at the shell composition root into `stage.Config.TieBreak`.
- **R22.** **The utility seat joins the duty map; the `resolveSeat` stub retires honestly** (S06.10; acceptance headline "where their consumers exist"; §8 reading 3): the effective DutyMap gains `DutyUtility` = Seat{model resolved from `local.alias` "utility" + manifest, lane `local`, window from the manifest} when the local stack is configured, and `Coverage.LocalAvailable` flips true. The B3-3 ERROR branch (`routing.go:520` "local tier marked available but no local seat wiring exists") is REMOVED. Consumer-class split binds: **class-(b) duty callers (R20/R21) consume the seat for real**; **class-(a) engine dispatch onto lane `local` has NO v0 consumer** (BINDING reading) — a template/helper demanding duty `utility`/mechanical still resolves to the paid execution seat with the REFINED honest reason ("the local tier is serving; its engine lane carries no v0 consumer — S12.1 class (a); platform duty calls ride it directly"), never an error, never a fake dispatch onto a lane with no adapter. B3-3's degradation tests update to the new reasons; nothing else in selection changes (deterministic order, D5 pressure-not-dollars, metered-refusal all regression-pinned).
- **R23.** **S12.8 slice owned here, split stated:** this packet delivers TTL-warm-residency-on-AC as config (R10 TTLs), `resident` empty, the never-run-`nvidia-persistenced` posture (documented in the unit/package docs; trivially satisfied — nothing starts it), and **no periodic GPU polling anywhere in this packet's code** (monitoring never defeats RTD3; the only nvidia-smi read is R10's one-shot UUID enumeration at config-gen). Referenced-not-consumed seams, named: battery admission (⚙ `local.battery.gpu_admission`) + sleep-gated reads + kill discipline (⚙ `local.unload.term_grace_s`, `local.vram.guard_band_mb`) = **B4-6**; AC-only batch (⚙ `local.batch.ac_only`, `ConditionACPower=` on batch units) = **B4-7's batteries**; `-lgc` clock caps need root → **flagged operator/hardening item**, never executed.
- **R24.** **Import walls (AST-pinned where new, the §15/§17/§22–§24 precedent):** the R1 package imports `storage`/`eventlog`/`gates`/`settings`-interface/stdlib (+ `metering` ONLY if a shared constant genuinely requires it — prefer none); it NEVER imports `adapters`/`stage`/`intake`/`worker`/`verify`/`memory`/`broker`/`scheduler`/`review`/`project`. `worker` NEVER imports the R1 package (TieBreaker arrives via the existing interface seam); `intake` imports unchanged (implementations arrive via `stage.Config`); `units` imports unchanged. `sandbox → adapters` stays the only sandbox edge (untouched).

### G — Tests, conformance tiers, the ONE live smoke

- **R25.** **Fake-/v1 test surface** (tier F, always runs): an httptest loopback fake of llama-swap's surface (chat completions honoring the constrained-decoding request shape, model field, unload endpoint, load/swap behavior sufficient to exercise TTL/unload/eager-unload logic and every duty schema round-trip). All client, seam, metering, routing, and config-gen tests run against fixtures/fakes — hermetic, zero network, zero paid calls.
- **R26.** **Conformance suite in the §10 three tiers**, adapted to the local stack: **F** fake/fixtures (always); **R** real llama-swap+llama.cpp when installed ($0, auto-runs — post-install this host qualifies); **L** the ONE live smoke under `SINET_LIVE_SMOKE=1` ($0 by construction). Absence-skips print `SANCTIONED SKIP (CONVENTIONS §10)` — the one allowed skip class. The suite asserts **behaviorally at the pinned builds** (P-T15-2; S12.10): `/v1` logprobs present, `json_schema` enforcement actually constrains (a deliberately schema-violating prompt cannot yield an invalid payload), and the llama-swap YAML/endpoint contract (the S16.3 watch rows' substance). The package exports its pins test-coupled to the lock entries (the §10 `Pin`↔lock precedent); an installed-vs-pin delta is reported LOUDLY, never retargeted.
- **R27.** **The ONE tier-L live smoke** ($0): launch llama-swap user-level on an ephemeral loopback port with the generated config; ONE duty call end-to-end (alias → model load → temp-0 constrained response with the abstain-capable schema → the $0 D7 checkpoint row with model hash + engine build → the zero-allowance receipt line via the metering fold); then eager-unload and observe VRAM-release behavior at the llama-swap surface. Assert the $0/zero-allowance facts on the REAL row. Never touches production units, ports 8481/8482, or any system service.
- **R28.** **No migration** (0001–0009 stand — S12 adds no durable tables in this cut; battery/calibration records are B4-7's, keyed off R9's hashes). **No new ⚙ key** (the 118-key tally test stays green as proof); structural config (llama-swap endpoint/binary/config/model-cache paths, `SINET_LOCAL_*`-style overrides — the `SINET_SRT_PATH`/B4-4 precedent) is composition-root passthrough, flagged to the gate under the settings-tab directive.
- **R29.** `P3/CONVENTIONS.md` gains **§26** (append-only, section-cited: the R1 package map, the class-(b)-only posture, the $0-row/zero-allowance conventions, the generated-llamaswap-unit reading, ⚙ consumed/referenced, import walls).

## 2. Seams to respect (named; stub/absence shape only)

1. **B4-6 (S12.6/S12.7):** GPU broker planes, per-run bearer tokens, sandbox bridge, `/v1/models` allowlists, VRAM ledger, sleep-gated reads, admission math, kill-not-freeze, battery admission, scheduler GPU-admission hook — NOT here. This packet's client is the platform plane only; nothing sandbox-reachable is built.
2. **B4-7 (S12.5/S12.9/S12.10):** confidence margins/calibration, T15 battery, bakeoff execution, swap gate, entailment/contradiction measurement (the `memory.ContradictionScreen` seam stays nil), 7.3 revalidation firing. This packet DOWNLOADS what B4-7 measures and records the hashes B4-7 keys on.
3. **Second engine adapter (opencode, S03.2 local lane):** NOT in this cut (BINDING) — attaches when a consumer exists. No `internal/adapters` change.
4. **B5 (S14):** watchdog/watchlist duty consumers, drift canary, query surface for `sql-open`, event-contract finalization — event names stay provisional.
5. **B6 (S15):** card rendering for eager-unload/routing; per-user duty-map override surface. Surface DATA + composition only.
6. **Hardening session / B4 gate (operator):** unit + slice install, gamemode.ini snippet install, `-lgc` caps, #10 ratifications ×3, model-cache location ratification.
7. **S10 arbitration:** slice POLICY values, PSI/oomd tuning — none invented here (⚙ `arbitration.background_cpuweight` stays with its S10.9 consumer).

## 3. ⚙ settings

- **Consumed** (by dotted key through a narrow settings interface; all declared since B0-2, `internal/settings/index.go` S12 block): `local.alias` (TypeMap — R15/R20–R22), `local.ttl.fast_s` + `local.ttl.workhorse_s` (R10), `local.gamemode_hook` (R13).
- **Referenced, not consumed** (named seams, cited): `local.ttl.egpu_s` (dormant, pool24), `local.vram.guard_band_mb` + `local.unload.term_grace_s` + `local.battery.gpu_admission` + `local.broker.sandbox_logprobs` (B4-6 — but IF llama-swap's live config schema exposes a per-model stop-grace knob, config-gen MAY consume `local.unload.term_grace_s` there, stated in a comment), `local.batch.ac_only` (B4-7 batch units), `local.reeval.cadence_months` (B4-7 lifecycle).
- **No new ⚙ key**; tally frozen at 118 (test = proof). Per-duty length caps + slice name + endpoints/paths = structural constants/config, gate-flagged (§8 reading 7).

## 4. Files expected to change

- NEW `internal/local/` (or executor's §2-conformant name): client + duty schemas (R16–R17), alias resolution (R15), models manifest (R9), config-gen (R10), eager-unload (R12), GameMode (R13), metering marker helpers (R18), conformance suite + fake (R25–R27). Colocated `_test.go`.
- `internal/units`: `sinet-llamaswap.service` + minimal slice file join the generated set; package doc-comment correction (R11).
- `internal/worker/routing.go` (+ `routing_test.go`): the `resolveSeat` stub retirement + refined reasons (R22). No other selection change.
- `internal/stage`: `Config` gains the intake seam fields (+ pass-through in `skeleton.go`); `TieBreak` field already exists (R20–R21).
- `internal/metering`: per-row lane override in the fold + local $0 pricing + zero-allowance labeling (R18–R19).
- `internal/shell`: composition wiring (local client when configured; seams into `stage.Config`; the eager-unload surface) (R12, R20–R22).
- `internal/cli`: the eager-unload tool subcommand (R12; a tool, never a mode — the reserved-mode table stays empty).
- `components.lock`: +3 entries (R2–R5). `P3/CONVENTIONS.md`: append §26 (R29). `P3/measurements/`: the GameMode probe result file (R14).
- Expected UNCHANGED: `internal/{adapters,intake,verify,memory,broker,gates,review,project,sandbox,preview,portpool,accept,backup,recovery,scheduler,run,ledger,auth,api,storage,eventlog}` code (intake gets NO code change — seams exist), `internal/settings/index.go`, migrations (NO 0010), `go.mod`.

## 5. Adopted components touched

- **llama-swap** — NET-materialized `organ-unit` entry on its S16.3 row; live-verified pin; full S16.4 walk; **#10 → B4 gate** (R2).
- **llama.cpp `llama-server`** — NET-materialized `engine` entry on its S16.3 row; live-verified `b`-tag; toolchain path recorded; **#10 → B4 gate** (R3).
- **Ollama** — `standby` entry (S16.2 rule); named-only, nothing runs (R4).
- **Models** — S12.3's own artifact class: manifest + card-verified licenses, NOT lock entries (R7–R9).
- **systemd/busctl, nvidia-smi, gamemoded** — host mechanisms consumed as subprocesses/observations under the existing OS-mechanism/host-managed postures; NO new entries (R10, R13–R14).
- **Zero new Go modules.** Anything beyond the three entries: stop-and-flag (R5).

## 6. Dev-mode vs LIVE (host gate; operator hands-on FLAGGED)

- **Executor performs (sanctioned):** user-level binary installs (llama-swap, llama.cpp per R3's resolved path), model pulls to the cache root (disk check first), config generation, the GameMode probe, the tier-R/L legs on the just-installed stack. All under user dirs; nothing under `/etc`, `/usr`, systemd, or the driver.
- **Executor NEVER:** installs/starts units; touches 8481/8482/tailscale/production units; changes driver/kernel/CUDA-system state; runs `nvidia-persistenced`; applies `-lgc`; writes into any user's `~/.config` (gamemode.ini snippet is rendered output only); makes paid engine calls.
- **Operator hands-on (flag each in the report, due B4 gate/hardening):** (a) #10 ratification ×3 (llama-swap, llama.cpp, Ollama standby); (b) `sinet-llamaswap.service` + slice install (with the R3 toolchain-path record); (c) gamemode.ini `[custom]` snippet install into the operator's user config; (d) `-lgc` clock-cap application (root); (e) model-cache location ratification (~30 GB residency); (f) any host-package need R3 surfaced; (g) the R14 probe's consequence if system-scope reachability failed.

## 7. Acceptance checklist (the evaluation rubric)

Green battery required: `gofmt` clean, `go vet` clean, `go build ./...`, `go test ./...` (`-count=1`; `-race` on touched packages), `go run ./tools/lockgate`. Sanctioned skips only per §10/§12, printed as such.

1. [ ] llama-swap + llama.cpp lock entries materialized on their S16.3 rows at live-verified pins (never memory/`latest`); full S16.4 walks; funeral plans name Ollama; the Ollama `standby` entry exists; lockgate green at 17 entries; #10 ×3 flagged to the gate (R2–R5).
2. [ ] Toolchain path live-resolved per the R3 tree, recorded in lock notes + report; CUDA placement preserved; zero driver/kernel/system-CUDA/system-package changes anywhere in the transcript; any genuine host-package need flagged, not performed (R3).
3. [ ] Disk re-checked before pulls; the R7 set downloaded minus the embedder (G2 Def.8 cited); every license verified on the card with date; the Gemma (or any) card-vs-spec license mismatch handled as a flagged conflict, never silently adopted; MiniCheck's CC-BY-NC exception carried per-seat (R6–R8).
4. [ ] Models manifest committed (seat/model/card/license+date/sha256/quant/pool/context); weights not committed; cache root structural + operator-relocatable; manifest hashes are what R18 rows and B4-7 keying consume (R9).
5. [ ] Config-gen emits: per-model `CUDA_VISIBLE_DEVICES=<UUID>` (live-read UUID, never index), pool12 swap group, `resident` EMPTY, TTLs from the two ⚙ keys, loopback bind, no embedder; schema-asserted at the pin behaviorally; non-servable CPU seats recorded honestly, never faked (R10).
6. [ ] `sinet-llamaswap.service` + minimal slice rendered into the generated set — GENERATED never installed (no systemctl/install anywhere); hardening-set exceptions reasoned in-file; the units package doc-line corrected with citations; slice policy values NOT invented (R11).
7. [ ] Eager-unload verb: admission-stop + unload, idempotent, event-recorded; surface act at the composition root (card data for B6) + CLI tool subcommand; resume symmetric; management verbs loopback/platform-plane only (R12).
8. [ ] GameMode: ⚙-gated; generated gamemode.ini snippet (never written to a home dir); `busctl`-subprocess subscription (zero new Go deps) wired per the probe verdict; scripts leg calls the two verbs (R13).
9. [ ] The GameMode user-context probe RAN live; dated result file in `P3/measurements/` with method/observation/verdict/consequence; the R13(b) implementation matches the verdict; deferred-with-finding path flagged if blocked (R14).
10. [ ] Alias resolution via ⚙ `local.alias` TypeMap at call time; unknown alias loud; embedder alias refuses with the post-gate reason; no swap machinery built (R15).
11. [ ] Client enforces the S12.4 cross-cutting rules: temp-0 greedy on classification shapes; schema in prompt AND engine-enforced (`json_schema`/GBNF); explicit abstain member in EVERY duty schema; free-text-then-constrained; per-duty length caps as structural constants; logprobs exposed platform-plane (R16).
12. [ ] Absent/unhealthy stack degrades exactly per the S12.4/S06 rows with recorded reasons; all pre-existing nil-seam tests pass unchanged (R17).
13. [ ] Every local call lands ONE checkpoint-row D7 usage row on the consuming run with the local marker {lane local, duty alias, model, model sha256, engine build, tokens}; the fold honors the per-row lane override; tier-1; a run-less call site is a loud surfaced defect, never silent (R18).
14. [ ] Local rows price TRUE $0 (labeled zero-allowance), never UNPRICED; the S10.3 flat-lane guard still binds paid lanes (side-by-side conformance test); receipts itemize zero-allowance local lines; rows readable per-lane by the pressure gauge (R19).
15. [ ] Intake Classifier/Utility/SpotCheck get real local impls via new `stage.Config` fields, wired only-when-configured; classifier add-only data-bearing + fail-closed floors untouched in the pipeline (R20).
16. [ ] TieBreak real impl on the `utility` alias: {pick|abstain, one-line reason}; abstain/error → deterministic order with reason; wired via `stage.Config.TieBreak`; routing.decided recording unchanged (R21).
17. [ ] Utility seat joins the effective DutyMap (local lane, manifest window) when configured; `Coverage.LocalAvailable` true; the `resolveSeat` ERROR stub is GONE; class-(a) dispatch onto local still degrades to the paid seat with the refined honest reason (BINDING reading honored); selection invariants (deterministic order, pressure-not-dollars, metered refusal) regression-pinned (R22).
18. [ ] S12.8 slice: TTL-warm residency + empty resident shipped; no periodic GPU polling in packet code (one-shot UUID read only); persistenced-never posture documented; battery/kill/batch/`-lgc` seams named + flagged, not built (R23).
19. [ ] Import walls hold, AST-pinned where new: local package's allowed set; worker never imports local; intake code untouched; adapters untouched (R24; seam 3).
20. [ ] Fake-/v1 surface covers the client/seam/metering/config paths hermetically; tier F/R/L discipline with `SANCTIONED SKIP` printing; pins exported + test-coupled to lock entries; behavioral asserts for logprobs + json_schema + the llama-swap contract at the pin (R25–R26).
21. [ ] The ONE tier-L smoke ran ($0): alias→load→constrained response→$0 row with hashes→zero-allowance receipt line→eager-unload observed; asserted on real rows; no production port/unit touched (R27).
22. [ ] NO migration; NO new ⚙ key (118-tally test green); structural config via composition-root passthrough, gate-flagged (R28).
23. [ ] Zero paid engine calls; network only in the three sanctioned legs (pins, models, cards); everything else hermetic (host gate).
24. [ ] `P3/CONVENTIONS.md` §26 appended, section-cited; no earlier section rewritten (R29).
25. [ ] Every operator hands-on item from §6 flagged in the packet report; every §8 reading either honored or re-litigated as a reported conflict — never silently deviated.

## 8. Boundary readings this brief rests on (each section-cited; evaluation re-checks)

1. **`sinet-llamaswap.service` is generated by `internal/units`.** S12.2 names a Sinet-named adopted-organ unit (`[coordinator-draft]`); S01.2 carries its slot; llama-swap ships no unit file, so the unit IS Sinet configuration for an unmodified adopted binary (S16.1 config-only integration — not a fork). The B0-era `internal/units` doc line ("local-model units … deliberately not generated here") predates this and is superseded by the STATE directive ("GENERATED, not installed … install = B4 gate or hardening"); the packet corrects the comment. Caddy/changedetection stay un-generated (they ship their own units).
2. **The S08.8 tie-break duty rides the `utility` alias — FLAGGED.** S08.8 step 2: "tie-break among multiple candidates by a local duty alias" [XREF:S12]; S12.4's registry (the complete alias table) names no routing/tie-break row. Grounded resolution: `utility` (the fixed-schema screen/annotation seat under the ceremony cut line) with an abstain member; "this seat never decides" holds because the pick surfaces on the pre-execution card with re-route/pin (S08.8 "visible and overridable"). If the coordinator prefers a registry amendment instead, that is an S00.9 act — the packet builds the utility reading and reports the gap.
3. **"Where their consumers exist" splits by S12.1 consumer class.** Class-(b) callers (intake seams, tie-break) exist and go live. Class-(a) — an ENGINE session on a local model — has NO v0 consumer (BINDING STATE reading; no opencode adapter). Therefore the `resolveSeat` local branch cannot dispatch onto lane `local` and must keep an honest paid-seat degradation with the refined reason; the B3-3 ERROR stub ("local tier marked available but no local seat wiring exists") retires either way. Faking a local execution seat would violate "absent duties degrade, never faked" (S08.8/CONVENTIONS §19).
4. **$0 D7 rows ride checkpoint rows on the consuming run, with a usage-block lane override.** S12.1 requires a D7 usage row per call; the S10 ledger is a READ MODEL over `checkpoints` (CONVENTIONS §11) and lane is joined from `runs` (`ledger.go` fold) — so the row carries an explicit local marker the fold honors. Every v0 call site has a run (intake stages, routing); S12.4 duties with run-less contexts (watchdog/watchlist) arrive with THEIR consumers (B5) and will land with whatever platform-scope metering that packet grounds — not invented here.
5. **Lane `local` prices a legitimate $0.** S12.1: "priced at $0 … zero-allowance receipt lines" — the free tier's true price. The S10.3 guard ("never price a flat-rate lane's usage from an all-zero row") targets PAID flat-rate lanes' engine-reported $0 and is untouched. Both behaviors are pinned side by side in tests.
6. **Model provenance is platform data, never `components.lock`.** S16.1: "Local models are not manifest entries: the serving stack is adopted here; the model set … are S12's registry." S12.3 fixes HOW: license verified on the card at adoption, per-seat exceptions carried with reasons; S12.9/S12.10 key records on (duty, model hash, engine build) → the committed models manifest (R9) is that registry's concrete v0 record.
7. **Endpoints/paths/length-caps/slice-name are structural config/constants, not ⚙.** S18's frozen index (118 keys) ratifies exactly the eleven `local.*` keys; S12 ⚙-flags nothing else. The settings-tab directive is the resolution path (7th instance of the precedent: B0 auth, §7 sseBatchSize, B1 scheduler, B4-2 junk rules, B4-3 trailers, B4-4 preview config).
8. **The GameMode D-Bus subscription uses host systemd's `busctl` as a subprocess.** CONVENTIONS §2 stdlib-first + §8's recorded fact ("no stdlib D-Bus … needs an adoption decision"); busctl rides the existing systemd OS-mechanism posture (the socket-proxyd/timers precedent — host systemd, no lock entry). A Go D-Bus library would be a net-new S16.4 adoption: stop-and-flag, not assumed. The R14 probe decides system-scope reachability; the gamemode.ini scripts leg is the always-works layer (R16 §4.5).
9. **The embedder is excluded end-to-end by G2 Def.8.** The vector post-gate ("evaluate embeddings only when lexical misses ≥5% of tasks or corpus >5,000 entries") has not opened; S12.2/S12.3 make the embedder member/pull post-gate-only. The `local.alias` map's declared `embedder` row stays (S12.4 registry data); the client refuses it with the post-gate reason.

## 9. CONVENTIONS constraints binding this packet (`P3/CONVENTIONS.md`)

- **§1** one binary — no new `cmd/`, no new mode (llama-swap is an adopted binary with its own unit; the CLI verb is a tool subcommand).
- **§2** stdlib-first (zero new Go modules; busctl/nvidia-smi as subprocesses); short lowercase package name; ⚙ by dotted key, never constants; `// TODO(settings)` never needed — keys exist; doc comments cite spec sections.
- **§3** stdlib `testing`; fixtures under `t.TempDir()`; green build+test precondition; never commit red; zero paid calls; measurements are probe files in `P3/measurements/`, not Go tests.
- **§4** adoption rail: the three entries land in the same commit as first consumption; exact live-verified pins, never `latest`; lockgate green; removal/changes only via dated edits.
- **§5** commit: subject `P3-B4-5: <summary> (<sections>)`; explicit-path staging; never push; never touch `Docs/`, `Spec/`, `Research/`, `P3/STATE.md`, `Presentation/`, `tools/dbpeek/`; never read or stage `*-api-key.txt`.
- **§6** settings discipline (TypeMap families = one key, subkeys as map keys; consumed-by-dotted-key); event conventions (refs-not-blobs, 15.6 attribution, provisional names pending S14).
- **§7** unit generation renders from registry defaults, GENERATION NEVER INSTALLS, `dist/` convention; loopback posture on anything bound; tools-vs-modes distinction.
- **§10** conformance tiers F/R/L + `SANCTIONED SKIP` printing; pins exported + lock-coupled; assert behavior, never docs/help text; loud pin-delta reporting.
- **§11** metering: the ledger is a READ MODEL over checkpoints — aggregations are queries; purpose derivation stays in ONE place; UNPRICED honesty preserved for paid lanes; receipts per run-end idempotent.
- **§12** sanctioned-skip discipline extends to the absent local stack; structural-config override env vars follow the `SINET_SRT_PATH` precedent.
- **§14** intake seam degradation semantics (absent seams degrade as the spec directs; optional duties skipped, never faked) — preserved with real impls plugged in.
- **§19** routing: deterministic selection, dollar-free by construction, absent-duty degradation recorded in plain reasons, `routing.decided` on every dispatch — all regression-pinned through the R21/R22 changes.
- Packet-specific (STATE queue row): S12.5–S12.7 + S12.9–S12.10 = B4-6/B4-7 seams only; the no-second-adapter reading BINDS; `sinet-llamaswap.service` generated never installed; network only in the sanctioned legs; the hard host gate is absolute.

## 10. Executor live-verify at build time (never from memory)

- **llama-swap**: current release pin; the real config schema at that pin (groups/swap semantics, per-model `env`, TTL knob, aliases, unload endpoint route) — probed behaviorally, not read from docs.
- **llama.cpp**: current `b`-tag; whether prebuilt Linux CUDA binaries NOW exist (R3 step (a)); `/v1` `json_schema` (`response_format`) + GBNF + logprobs behavior at the pinned build (the R26 asserts encode what is actually observed).
- **Toolchain probes (read-only)**: `gcc`/`cmake` presence; docker usability without host changes + `nvidia-container-toolkit` presence (check only — never install); `nvidia-smi` UUID read (GPU recorded 2026-07-22: RTX 5070 Ti Laptop, 12227 MiB).
- **Disk**: free-space re-check immediately before pulls (161 GB recorded; ~30 GB set).
- **Model cards**: license text ON each card + GGUF availability at the named quants; sha256 of every pulled file; the Gemma license cell (spec says Apache 2.0) checked against the card FIRST — mismatch = flagged conflict (R8).
- **gamemoded**: present (recorded); the R14 probe runs live with user-level tooling; result file lands before the packet report.
- **After the rail edits**: lockgate green at 17 entries; the 118-key settings tally test green (proof of R28).
