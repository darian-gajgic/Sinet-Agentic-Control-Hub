# P3-B5-6 grounded brief — S14.6 watchlist executor + the API canary layer + changedetection.io adoption + registered STUDY watch rows

**Contract:** `Spec/core-architecture-v1.md` (frozen v1, tag `spec-v1`); the per-section drafts in `Spec/drafts/` are the canonical text. **Read in full before writing code:** `Spec/drafts/S14-observability-evals-watch.md` §S14.6 (plus §S14.1–S14.5 for context and the ⚙ table), `Spec/drafts/S10-metering-scheduling.md` (§S10.1/§S10.3/§S10.5/§S10.7/§S10.8), `Spec/drafts/S03-engines-adapters.md` §S03.6–§S03.7, `Spec/drafts/S16-adoption-manifest.md` §S16.2–§S16.4/§S16.8, `Research/decisions/GATE-2-substrate-and-adoption.md` items 12/15/16, `Research/decisions/GATE-4-spec-review.md` line 55, `Research/02-provider-watchlist-and-onboarding-criteria.md` §6, and **`P3/CONVENTIONS.md` in full** (§4 rail, §10 sanctioned skip, §11 scheduler/metering, §29–§33 the B5 packets). This brief plus those sections is the whole context; nothing here rests on chat history.

**G2 definitions verbatim** (`Research/decisions/GATE-2-substrate-and-adoption.md`):
- **Def.12** (item 12): *"Watchlist executor (R12-OQ3): Sinet-native feed poller on the ratified scheduler (no new DB); Miniflux pre-registered as fallback; changedetection.io + canary layer regardless."*
- **Def.15** (item 15): *"awesome-harness-engineering (R14-OQ2): watch both leading referents (`walkinglabs/…`, `ai-boost/…`); drop the row if both stall."*
- **Def.16** (item 16): *"genai-prices integration (R14-OQ5): vendor the pinned `data.json`; scheduled refresh lands as a proposal (price-table drift already triggers freshness re-validation per G1 Def.5); user edits overlay, never overwritten."*
- **G4 [schema] STUDY row verbatim** (`GATE-4-spec-review.md:55`): *"Watch row (STUDY-class), onboard at S14 watch setup (build phase B5): [schema] ARC-AGI-3 harness — world-model-as-code (mechanism-as-executable-program) as a candidate future skill/technique pattern for domain workers; treat its numbers per report-12 discipline (self-reported, public-set, scaffold-specific). Source: schema-harness.github.io."*

---

## 0. SCOPE VERDICT — this packet is NOT one sitting's work. Split A/B.

Counted deliverables: a new config-row store + migration; a from-scratch conditional-GET/hash-dedup feed poller **and** Atom/RSS parsing (the repo has **zero** outbound HTTP fetching, **zero** ETag/If-None-Match handling and **zero** feed parsing today — §9 of the survey below); an adopted third-party service with a full S16.4 walk, a REST client, a webhook ingress and a host-install question; weekly aggregator jobs including price-proposal machinery that does not exist in any form; a local-model second pass + drift cards + inbox derivation + the revalidation-runbook edge; a four-kind canary layer that is the **first production caller of `scheduler.Classify`**; the decay ladder; and the shell's "when". That is roughly two B5-3-sized packets.

**Proposed split, at the event-type seam (`FamilyDriftCanary` has exactly two declare-only types, one per half — a clean, mechanically checkable boundary):**

- **P3-B5-6A — the watchlist executor.** Requirements R1–R21. Owns `drift.finding`, the watch-row store + seeds (incl. the STUDY/awesome-harness rows), changedetection.io adoption, the feed poller, the aggregator jobs, second-pass classification, drift cards + inbox derivation, the decay posture, and the shell loop. `canary.result` stays declare-only.
- **P3-B5-6B — the API canary layer.** Requirements R22–R32. Owns `canary.result`, the four canaries (auth / behavioral / logprob / model-list), the conformance-canary scheduling tie, and the canary metering posture. Reuses A's periodic-execution substrate and A's card/inbox derivation; adds no migration.

If the coordinator declines the split, B is the half to cut — A alone discharges G2 Def.12's "watchlist executor", the S16.8 watch-row registrations, and the S14.8 drift-trigger emitter that B5-5 left as an accept-shape-only seam.

---

## 1. BINDING carried facts (verified in the tree at grounding, 2026-07-26 — do not relitigate)

1. **`drift.finding` and `canary.result` are DECLARE-ONLY today**, both in `FamilyDriftCanary`, both with provenance `producer: B5-6 (S14.6)` (`internal/eventlog/contract.go:473-474`). Minting them is the §29 flip: real provenance + a `producerTypes()` inventory entry in the same packet. Current inventory **80 minted / 13 declare-only**; A takes it to 81/12, B to 82/11 (or 82/11 in one go if unsplit).
2. **`FamilyDriftCanary` required fields, verbatim** (`contract.go:377-380`): *"source, lane(s), change class (price/terms/limits/models/endpoints/billing-regime), severity, one-line summary, incident fingerprint; canary kind + lane + pass/fail/delta"*, anchor `S2.8`. Payload conformance is asserted **at test time over representative goldens** (§29) — `Append` stays envelope-only.
3. **`registry.drift` (S13.7, project-topology drift) is a DIFFERENT type in `FamilyPlatform`**, carrying an explicit warning in `contract.go:500` that B5-6 must not mis-own it. The name collision must not mis-route.
4. **The SSE topic map already routes drift for free.** `internal/api/topics.go` `topicsForEvent` maps `eventlog.FamilyDriftCanary → topicInbox`. The moment these types are emitted with `SchemaVersion: 1` they tag `inbox` on the live tail — **`topics.go` needs no edit** (a checkable negative).
5. **The inbox drift seam already exists and is empty.** `internal/api/projection.go` `InboxSnapshot.DriftCards []any` is initialized `[]any{}` and never populated; its doc names *"the declare-only drift seam (producer B5-6)"*. Populate it by **derivation over `run_events`** — never a bare `asks` row (`asks.run_id` is NOT NULL; the B5-3 run-less-flag / B5-4 conformance-card precedent), owner-scoped per S01.9 (platform-scope ⇒ operator-only).
6. **The organ-liveness seam exists and is unwired.** `watchdog.OrganLiveness func(ctx) ([]OrganStatus, error)` (`internal/watchdog/watchdog.go:184-187`) has a doc naming *"watchlist executor"* as its first organ; `grep "Organs:" internal/shell/*.go` returns **nothing** — the shell wires nil today. Down organs surface as `watchdog.organ_absence:<organ>` digest flags (suffixed anomaly class, suppressible — `internal/watchdog/suppress.go`).
7. **The conformance registry assigned the weekly adapter-suite RUN to this packet.** `internal/conformance` package doc: *"the weekly RUN of the adapter suites is the S14.6 canary layer's (B5-6)"*; rows `adapter-anthropic` and `adapter-local` carry `Cadence: weekly`, `AffectClass: lane` (flag-now) and Notes *"this row is the scheduling home."* The `forced-escalation-e2e` row carries `CanaryCadence` = ⚙ `verification.canary_interval_hours` as **visible data only**, Notes: *"The canary RUNNER and the CanaryWatch.Silent→operator-alert wiring stay in B5-6/B6."* See OQ5.
8. **`scheduler.Classify(sig LimitSignal, cfg LimitConfig) Action` has NO production consumer today** — only `internal/scheduler/limits_test.go` calls it. B5-6's auth canary is its **first live caller**. `LimitSignal` already carries `OnValidCredentials bool` documented as *"the auth-canary fact (P-T17-1)"*. Class 4 (`ClassAuthPolicy` → `ActionLaneFreeze`) is checked FIRST so an auth event can never fall through to a retry-park (CONVENTIONS §11).
9. **`internal/evals` Runner seam + the B5-5 F2 carry.** `evals.Runner{Identity,Run}`, production impl `*evals.Promptfoo`, pin `evals.PromptfooPin = "0.121.19"`, discovery `FindPromptfoo()` = `SINET_PROMPTFOO_PATH` → `exec.LookPath("promptfoo")` → `ErrRunnerAbsent`. The evals package doc says verbatim: *"B5-6's behavioral canaries reuse the Runner seam."* **The promptfoo CLI facts (exit 100 = test failure, `results.version == 3`, the flag set) were recorded from the pinned docs/source and have never been exercised against a real binary — nothing is installed until the B5 gate host act.** B5-6 inherits that dependency; it does not re-derive those facts and does not install anything.
10. **The B5-5 F9 carry — the drift trigger maps to `FlagByModel`.** `evals.Runbook.Flag` for `TriggerDrift` calls `Hooks.FlagByModel(ctx, t.Subject, reason)` (`internal/evals/runbook.go:88-107`), and `worker.Store.FlagByModel` matches `t.Subject` against `validation_records.model` and the template's `Profile.ModelPin` (`internal/worker/lifecycle.go:521-547`). **So `Trigger.Subject` must be a MODEL ID.** A non-model drift subject flags nothing. See OQ4 for the disposition the coordinator must make; the executor implements whatever is ratified and **fakes no flag**.
11. **The B5-5 external-service precedent, binding on changedetection.io:** the install is a **host act batched to the phase gate**, an agent installs nothing, discovery goes through PATH-or-equivalent plus an env override, CI is hermetic against a stub, and the real leg is the CONVENTIONS §10 sanctioned-skip class (`SANCTIONED SKIP (CONVENTIONS §10)`).
12. **Units are generated, never installed** (CONVENTIONS §7; `internal/units` package doc). The B4-5 carve-out: an adopted binary that **ships no unit file of its own** gets a Sinet-generated unit (`sinet-llamaswap.service`, `Draft: true`), because that unit is *Sinet configuration for an unmodified adopted binary* (S16.1 config-only integration, not a fork). See §LIVE-verification fact (f) and OQ1.
13. **No new ⚙ key.** `internal/settings/settings_test.go` pins **118 keys / 33 domains / 16 owner sections** and a 118-leaf `JSONSchema()` assertion. Adding a key breaks three assertions and is an S00.9 amendment + an S18 sweep — a **stop-and-flag**, not a packet decision.
14. **Migrations are `0001`–`0012`.** A migration file, once committed, is immutable. A watch-row table is `0013_…sql`.
15. **`local.AliasWatchlist = "watchlist-triage"`** is already a declared duty in ⚙ `local.alias` (`internal/local/local.go:96`; index line 691 carries `"watchlist-triage": "fast"`). The local second pass rides `local.Duty.Call(ctx, runID, DutyRequest{Alias: local.AliasWatchlist, Schema: …, Classification: true})`; `Classification: true` forces greedy + logprobs + the abstain-member assertion. A nil `*Duty` returns `ErrStackAbsent` and every duty seam degrades honestly (B5-3 precedent: skip the annotation, log it, never fabricate a label).
16. **The logprob substrate already exists on the local lane** — `local.TokenLogprob`, `local.LabelMargin`, `local.TokenMargin`, `DutyResult.Logprobs`, and the S12.9 calibration path in `internal/local/calibrate.go`. The logprob canary composes on these; it invents no new inference path.
17. **Existing periodicity pattern (mandatory).** There is **no cron/job/periodic facility in `internal/scheduler`** — `ClassScheduled` exists but its doc says *"at v0 the only schedules are platform-internal timers"* (S10.7). Periodicity today is **shell-owned goroutines** (`walTruncateLoop`, `recoverySweepLoop`, `watchdogTailLoop`, `watchdogSweepLoop`, `watchdogDeadManLoop`) with **dueness derived from stored state, never an in-memory ticker** (B5-3 dead-man, B5-4 `conformance.Store.Due`, B5-5 `evals.Store.SweepState`). B5-6 follows that pattern exactly: the package owns the LOGIC, the shell owns the WHEN, and dueness survives restart and suspend.

---

## 2. LIVE verification (performed this session, **2026-07-26** — S16.4 checks #3/#4/#5 inputs; #10 operator ratification queues for the B5 gate)

Method: GitHub REST API (`api.github.com`), `raw.githubusercontent.com` at the exact pinned tag, PyPI JSON API, Docker Hub v2 API. Every fact below is re-checkable by the executor and evaluator with the same URL.

**changedetection.io**
- (a) **Version: `0.55.8`**, published **2026-07-13T09:26:10Z**, `draft:false`, `prerelease:false` (`GET /repos/dgtlmoon/changedetection.io/releases/latest`). PyPI `changedetection.io` latest is also `0.55.8`, `requires_python ">=3.10"`.
- (b) **License: `Apache-2.0`** (`license.spdx_id` on the repo API; PyPI classifier "Apache License 2.0"). **Path-scoping (S16.2/P-T16-2) is NOT yet done** — the executor must run the carve-out scan (`enterprise/`, `ee/`, dual-license trailers) at ref `0.55.8` and record the scoped result + date in the lock entry. Do not copy the repo-level label into `license.scope`.
- (c) **Repository health:** not archived, not disabled, `pushed_at 2026-07-24T00:23:24Z`, 32,495 stars, 365 open issues, default branch `master`. Active. Bus factor is single-primary-maintainer (`dgtlmoon`) — record that in the entry's `abandonment` criteria the way the llama-swap row does.
- (d) **Official container image: YES** — Docker Hub `dgtlmoon/changedetection.io`, publisher-owned namespace, `is_private:false`, **10,834,296 pulls**, `last_updated 2026-07-22T09:25:57Z`. (Contrast with promptfoo at B5-5, where no official image was documented.)
- (e) **REST API shape AT THE PINNED TAG**, read from source (`raw.githubusercontent.com/.../0.55.8/changedetectionio/flask_app.py`), not from docs — 13 route registrations, quoted:
  `CreateWatch → '/api/v1/watch'` · `Watch → '/api/v1/watch/<uuid_str:uuid>'` · `WatchHistory → '/api/v1/watch/<uuid_str:uuid>/history'` · `WatchSingleHistory → '/api/v1/watch/<uuid_str:uuid>/history/<string:timestamp>'` · `WatchHistoryDiff → '/api/v1/watch/<uuid_str:uuid>/difference/<string:from_timestamp>/<string:to_timestamp>'` · `WatchFavicon → '/api/v1/watch/<uuid_str:uuid>/favicon'` · `Tags → '/api/v1/tags'` · `Tag → '/api/v1/tag', '/api/v1/tag/<uuid_str:uuid>'` · `Search → '/api/v1/search'` · `Import → '/api/v1/import'` · `Notifications → '/api/v1/notifications'` · `SystemInfo → '/api/v1/systeminfo'` · `Spec → '/api/v1/full-spec'`.
  **The config-as-code driver needs exactly:** `GET/POST /api/v1/watch`, `GET/PUT/DELETE /api/v1/watch/<uuid>`, `GET /api/v1/watch/<uuid>/history` + `/difference/<from>/<to>`, `GET/POST /api/v1/tags`+`/tag`, and `GET /api/v1/systeminfo` (the liveness probe for R6/organ-absence). `GET /api/v1/full-spec` returns the merged OpenAPI document and is the pin-coupled re-verification handle.
- (f) **Authentication AT THE PINNED TAG** (`changedetectionio/api/auth.py`@0.55.8): header **`x-api-key`**; failure is **HTTP 403** with body `"Invalid access - API key invalid."`.
- (g) **Notification transport AT THE PINNED TAG:** `requirements.txt`@0.55.8 pins **`apprise==1.9.9`**. Apprise's `json://` / `jsons://` scheme is the Custom-JSON POST notification (default method POST; port 80/443; `:key=value` URL args add custom payload fields) — this is the `json://` webhook S14.6 names. **Verified from Apprise's documented scheme, not from a live POST** — the executor must assert the delivered payload shape against a stub receiver before trusting field names.
- (h) **Native LLM triage AT THE PINNED TAG: PRESENT.** `requirements.txt`@0.55.8 carries `litellm>=1.40.0,<1.83.1` under the comment *"LLM intent-based change evaluation (multi-provider via litellm)"*, plus an explicit `pydantic>=2.0,<3.0` *"Used today for LLMSettings (model/LLMSettings.py)"*. `changedetectionio/model/LLMSettings.py`@0.55.8 fields: `enabled=True`, `debug=False`, `override_diff_with_summary=True`, `restock_use_fallback_extract=True`, `thinking_budget=0`, `max_summary_tokens=3000`, `budget_action='skip_llm'`, `change_summary_default=''`, `token_budget_month=0`, `max_input_chars=100_000`, `max_tokens_per_count_period=0`, **`model=''`**, `api_key=''`, **`api_base=''`**, `provider_kind=''`, `local_token_multiplier=5`, plus cumulative token/cost counters. **`api_base` + `model` are the knobs that point it at Sinet's local OpenAI-compatible endpoint** (S14.6's "its native LLM rules pointed at Sinet's local endpoint"). No prompt/intent field lives on this model — the per-watch intent text is elsewhere and is **UNVERIFIED**; the executor must locate and verify it before generating watch bodies that depend on it.
- (i) **Ships NO systemd unit.** A recursive tree read of tag `0.55.8` (`GET /git/trees/0.55.8?recursive=1`, `truncated:false`) matched **no `*.service` and no `systemd/` path** — only `Dockerfile`, `docker-compose.yml`, `.github/test/Dockerfile-alpine`. **This directly contradicts the parenthetical in `internal/units/units.go`'s package doc, which lists "the watchlist executor" among organs that "ship their OWN units and are deliberately not generated here."** That parenthetical was an unchecked B0-3 assumption. Correcting it is a CONVENTIONS/doc fix under the B4-5 llama-swap carve-out, **not** an S00.9 amendment. See OQ1.
- (j) **Playwright / JS-walled-target fetcher: UNVERIFIED.** `requirements.txt`@0.55.8 references playwright only obliquely (a `greenlet >= 3.0.3` pin citing `microsoft/playwright-python#2096`); the browser-enabled leg appears to ride a separate image/extra that was **not** confirmed this session. S14.6 names "Playwright fetcher for JS-walled targets" — the executor MUST verify how it is enabled at `0.55.8` before claiming the escalation ladder's second rung works, and must record the result. **Do not fill this in from memory.**

**genai-prices**
- (k) **Repository `pydantic/genai-prices`, license `MIT`** (`license.spdx_id`), not archived, not disabled, `pushed_at 2026-07-25T14:12:02Z`, 331 stars, 78 open issues, default branch `main`. **Latest release `v0.0.72`, published 2026-07-22T21:06:50Z** (S16.3 records "v0.0.71 line observed" — the line has moved by one).
- (l) **Data files confirmed present and fetchable:** `prices/data.json` — HTTP 200, **348,471 bytes**; `prices/data_slim.json` — HTTP 200, 244,457 bytes (raw at `main`, 2026-07-26). Health: active, small-team/community, MIT — no carve-out observed at the repo root, but **path-scoped verification at the exact vendored ref is still the executor's S16.4 #4 obligation**.

**Not verified this session (state plainly, do not fill in):** changedetection.io's per-watch LLM prompt/intent field name (h); the Playwright fetcher's enablement mechanism at the pin (j); the live `json://` payload field names (g); any promptfoo CLI fact against a real binary (carried B5-5 F2); the models.dev `api.json` schema (needed for R14 — the executor verifies it at use).

---

## 3. Numbered requirements

Each carries its S-ref. `[A]`/`[B]` mark the proposed split half.

### A — the watch-row config store (S14.6 ¶1; G2 Def.12; report-02 §6)

- **R1 [A]** — Package `internal/watchlist` is the S14.6 home. Imports: `storage`, `eventlog`, `settings`(via a narrow local `Settings` interface only), `local`, `conformance`, `scheduler`, `metering`, `evals` + stdlib. It **never** imports `internal/api`, `internal/adapters`, `internal/worker`, `internal/stage`, `internal/verify`. Pin the wall with an AST `TestImportWall` matched exact-or-prefix with a non-tautology probe (the §31/§32/§33 precedent).
- **R2 [A]** — Migration **`0013_watchlist.sql`** creates the watch-row store: `{id, kind (page|feed|api|study), url_or_feed, parser_hint, lane_or_candidate, tier (1–4), cadence, enabled, source_group, notes}` plus per-row mutable fetch state `{last_fetch_at, last_etag, last_modified, last_content_hash, fail_streak, decay_stage, external_ref}`. Structure columns are immutable by trigger (`BEFORE UPDATE OF` every column except the fetch-state set — the `conformance_registry_structure_immutable` shape, §32/R5); a no-delete trigger enforces the S16.2 removal discipline (a watch row leaves only by a dated funeral edit). Platform-scope, `user_id='platform'` CHECK (the 0009/0011 shape) so the rows ride the S02.9 durable set and every S13.10 archive by construction. `0001`–`0012` stay byte-untouched. **G2 Def.12's "no new DB"** is satisfied: this is a table in `platform.db`, not a second store.
- **R3 [A]** — `SeedRows()` in code, seeded idempotently (`INSERT OR IGNORE`) at shell boot, unconditional (platform obligations need no D10 holder — the §32 precedent, not the S09.10 house-seed precedent). Content = the **report-02 §6 tiered list verbatim** (Tier 1 pricing/plan pages + provider release-notes/changelogs; Tier 2 GitHub issues/releases Atom canaries; Tier 3 aggregator APIs — models.dev `api.json`, Artificial Analysis, OpenRouter blog feed; Tier 4 hnrss.org keyword feeds + `r/LocalLLaMA/.rss`), **plus** every S16.8 standing row (fine-grained-PAT collaborator gap · Free-plan ruleset availability · diffity maturity · jj compat matrix · X-Wing age-CLI plugin · DBOS TS/SQLite parity · MCP 2026-07-28 final · ACP v1 line · Litestream #1083 · Python-Redlines + pandiff · @dnd-kit/react 1.0 · assistant-ui 1.0 · **awesome-harness-engineering both referents `walkinglabs/…` + `ai-boost/…`, drop-if-both-stall [G2 Def.15]** · Crush FSL→MIT · genai-prices staleness · opencode v2), **plus the `[schema]` STUDY row** (kind `study`, source `schema-harness.github.io`, notes carrying the G4 line's report-12 discipline caveat verbatim), **plus one watch row per `components.lock` entry's `watch` field** (S16.4 #9 — every entry's watch reference must resolve to a real row; assert it in a test). Every row cites its ratifying source in `notes`. Operator-editable via a strict-JSON `LoadRows` override (unknown fields fail loudly — the §14/§15/§17 seed precedent) with a `WriteSeed` dump for gate review.
- **R4 [A]** — S14.6 ¶1: rows are `{url|feed, type, parser-hint, lane|candidate}`. The **standing bias to migrate any scrape target to a feed/API the moment one exists** (P-T11-3) is recorded as a per-row field/flag and surfaced on the decay card — it is a spec-named posture, not a comment.

### A — changedetection.io adoption (S16.1–S16.4; S14.6 ¶T1; G2 Def.12)

- **R5 [A]** — `components.lock` entry, `kind: "organ-unit"` (the Caddy/llama-swap shape), **`pin: "0.55.8"`**, `license: {spdx: "Apache-2.0", scope: <path-scoped result of the executor's own carve-out scan at ref 0.55.8>, checked: <date>}`, `role: {summary: "watchlist page-diff tier", section: "S14.6"}`, `replacement:` process-seam swap per S01.3 — *"native feed poller + canary layer carry the watch duty through any gap"* [G2 Def.12], `abandonment:` explicit criteria naming the single-primary-maintainer bus factor, `watch:` the S2.8 rows R3 seeds, `last_review: <date>`. **No `modules` claim** (a Python service, not a Go module — the `sandbox-runtime`/promptfoo precedent). Lock count 19 → 20. Walk **all ten S16.4 checks** and record each in the packet report; **#10 (operator approval) queues for the B5 gate** — the packet proposes, the gate approves.
- **R6 [A]** — **Adopt, never fork.** Integration is REST + config only. `internal/watchlist/cdio.go` drives the pinned API surface quoted in §2(e): reconcile Sinet's watch rows (kind `page`) onto changedetection.io watches — create/update/delete via `POST/PUT/DELETE /api/v1/watch[/<uuid>]`, list via `GET /api/v1/watch`, read diffs via `GET /api/v1/watch/<uuid>/difference/<from>/<to>`, tag via `/api/v1/tag[s]`, and probe liveness via `GET /api/v1/systeminfo`. Auth: the **`x-api-key`** header. The external watch uuid persists in R2's `external_ref` so reconciliation is idempotent and a stale remote watch is deleted, never orphaned. Config-as-code: **Sinet's rows are the source of truth; a hand-edit in the organ's UI is reconciled away** and that must be stated in the doc comment.
- **R7 [A]** — Discovery + hermetic CI, the B5-5 precedent verbatim: base URL + API key resolve from settings/bootstrap with a **`SINET_CDIO_URL`** env override (the `SINET_SRT_PATH`/`SINET_PROMPTFOO_PATH` shape); the API key is a broker/`engine-cred`-class secret and is **never** logged, never on a struct that reaches a park record or the DB (D2/S11.5). All tests run against an **`httptest` stub** serving the pinned route shapes (the `internal/preview/caddy.go` precedent, including its AST string-literal tripwire forbidding a dial to a live host). A real-service leg auto-runs when reachable, asserts `systeminfo` version == `Pin`, and otherwise prints **`SANCTIONED SKIP (CONVENTIONS §10)`**.
- **R8 [A]** — **The host install is a B5-gate operator act. This packet installs nothing, starts nothing, and changes no host state.** Absence is honest: the reconciler degrades to "page tier absent", the feed/API tiers keep working, and the organ surfaces as `watchdog.organ_absence:watchlist` — which requires **wiring `watchdog.Config.Organs`** (currently nil, fact 6) to a liveness probe over `GET /api/v1/systeminfo`. That wiring is this packet's.
- **R9 [A]** — **Local-endpoint triage (S14.6 ¶T1).** The generated watch config points changedetection.io's native LLM rules at Sinet's local OpenAI-compatible endpoint via `LLMSettings.api_base` + `model` (§2(h)), as **first-pass** "does this diff matter?" triage only. It never replaces R12's second pass and never gates a card. Region-filtered verbatim diffs and the Playwright fetcher for JS-walled targets are configured per S14.6 — **subject to the executor's §2(j) verification; if the Playwright leg cannot be verified at the pin, say so in the report and leave the second rung of the R18 ladder unclaimed rather than assumed.**
- **R10 [A]** — **Hit ingress.** Changedetection.io posts hits to the control plane via an apprise `json://` webhook. Add exactly one route. Its auth posture is **OQ2** — do not invent one; implement what the coordinator ratifies. Whatever lands: the receiver validates and normalizes into a watch hit, is idempotent on replay, and never trusts the body for lane/severity (those derive server-side from the watch row).

### A — the Sinet-native feed poller + aggregator jobs (S14.6 ¶T2/T3/T4; G2 Def.12, Def.16; S10.7)

- **R11 [A]** — `internal/watchlist/feed.go`: a stdlib-only poller — **conditional GET** (`If-None-Match` from the stored ETag, `If-Modified-Since` from the stored Last-Modified; a `304` is a cheap no-change), **content-hash dedup** (sha256 over the normalized body; per-entry hashes for feed items), bounded response reads, explicit timeouts, and an explicit `User-Agent`. Atom + RSS parsing is `encoding/xml` structs, **forward-tolerant** (unknown elements ignored, a malformed feed is a fetch failure, never a panic and never fatal — the S03.1 discipline). **Zero new Go modules** (stdlib-first, CONVENTIONS §2). ~15 feeds per S14.6. **Never self-host hnrss** (no license file) — consume the hosted feeds.
- **R12 [A]** — **Miniflux stays the pre-registered fallback, unbuilt** [G2 Def.12; S14 Deferred]. Record it as a `standby` lock entry **only if** the coordinator wants the funeral plan pre-materialized (S16.2 requires a `standby` entry for every replacement path naming a concrete third-party component); otherwise record the deferral in the report. Its HMAC-signed webhook shape is the documented drop-in contract — do not implement it.
- **R13 [A]** — **T3 weekly aggregator-API jobs:** models.dev `api.json` diff and the price/model-list sources, on the shell-owned cadence (R20). Diffs are structural (added/removed/changed keys), never free-text.
- **R14 [A]** — **genai-prices refresh-as-PROPOSAL [G2 Def.16; S10.3; P-T08-3].** The refresh job fetches `prices/data.json` (§2(l)), diffs it against the stored baseline, and lands the result as a **durable proposal record** carrying proposed `metering.PriceRow` values **with effective dates** — never an overwrite, never a user-overlay clobber, never an auto-applied table. **A billing-regime change NEVER auto-flips a flag** (S10.2/S14.6): the operator confirms, then the rehearsed S10.2 flip runs. Price-table drift **is a first-class staleness cause** — an approved change fires the G1 Def.5 freshness trigger via `run.Fingerprint.PriceTableVersion`. The genai-prices vendoring/loader scope is **OQ3**. ⚙ `adoption.price_data_stale_days` (60 d, 14–180, S16 section) is the staleness horizon that proposes the LiteLLM-primary fallback [S16.8] — consume it by dotted key.

### A — second pass, drift cards, decay, wiring (S14.6 ¶2/¶4; S14.2; P-T11-3)

- **R15 [A]** — **Second-pass classification on the local tier.** Every hit — page diff, feed entry, API diff — goes through `local.Duty.Call` on **`local.AliasWatchlist`** with `Classification: true` and a grammar-constrained schema built **in `internal/watchlist`** (`internal/local` stays read-only — the B5-3 OQ6 precedent), producing `{relevant lane(s), change class, severity, one-line summary}` with a mandatory abstain member. **Costs no allowance** (Operating reality). An error/abstain/unparse degrades to "unclassified — operator reads it", never a fabricated label; an absent local stack (`ErrStackAbsent`) means the hit still becomes a card, honestly marked unclassified.
- **R16 [A]** — **`drift.finding` minted** (§29 flip: real provenance in `contract.go` + `producerTypes()`), platform-scope (`UserID: "platform"`, no `RunID` — a watch hit has no run), `SchemaVersion: 1`, verdict `CONFORMS` (an S14.2-named canonical type — the B5-3/B5-4 precedent). Payload satisfies the family's contract minimum verbatim (fact 2) and joins the §29 required-field golden set. **Do not touch `registry.drift`** (fact 3).
- **R17 [A]** — **Drift cards with fingerprint-per-incident-window dedup — one card per storm** (S14.6 ¶2; S10.5 "once-per-storm logging"). The incident fingerprint is a stable derivation over `{source, lane, change class}`; a repeat inside the window folds into the open card rather than raising a new one. Cards **derive from the log** — populate `InboxSnapshot.DriftCards` in `internal/api/projection.go` by a query over `drift.finding` rows, no side table, no bare `asks` row, owner-scoped per S01.9 (platform-scope ⇒ operator-only; a member never sees them). Severity routing follows S14.4's two classes: **flag-now** for a hit touching an active lane's price/terms/limits/models/endpoints or a billing-regime change; **daily digest** otherwise. `internal/api` does **not** import `internal/watchlist` (the §31/§32 derive-from-log precedent).
- **R18 [A]** — **Decay posture (P-T11-3).** Per-source fetcher escalation ladder: plain → (Playwright, per R9/§2(j)) → **flagged-decayed card proposing an alternative source**. A meta-watch flags fetch-failure streaks ≥ ⚙ **`watchlist.fetch_fail_streak`** (declared: int, default 3, clamp 2–10, unit *cycles*) — **a dead watch MUST announce itself**. Read the key by dotted key at evaluation time (live-apply).
- **R19 [A]** — **The revalidation edge (S14.8 ¶3; B5-5 F9 carry, fact 10).** Wire `evals.Runbook.Flag(ctx, evals.Trigger{Kind: evals.TriggerDrift, Subject: …, Reason: …})` from the drift emitter **exactly per the OQ4 disposition**. Whatever is ratified: (i) the subject that reaches `FlagByModel` is a model id or the call is not made; (ii) a drift class that cannot produce a model subject must be **visibly recorded as not triggering revalidation** — on the card and in the report — never silently dropped; (iii) `internal/evals` and `internal/worker` are **not modified** by this packet.
- **R20 [A]** — **The shell owns WHEN.** Add a `watchlistLoop` alongside the existing five shell goroutines; **dueness derives from stored state** (R2's `last_fetch_at` + the row's cadence), never an in-memory ticker, so it survives restart and suspend (fact 17). Cadences per report-02 §6: Tier 2/4 continuous (event-driven), Tier 1/3 weekly, the monthly re-stamp, the quarterly re-audit. These cadence constants are **structural, not ⚙** (the `sseBatchSize`/§31 precedent, interim under the standing settings-tab directive) — S18 ratifies no watchlist-cadence key and none may be invented.
- **R21 [A]** — **Watch-row hygiene ties to S16.7.** The quarterly dependency pass reads these rows; expose a read surface for it and record in `notes` that outputs are **proposals, never silent bumps** [S16.7; G2 Def.16].

### B — the API canary layer (S14.6 ¶3 — "the one genuinely novel piece")

- **R22 [B]** — `internal/watchlist/canary.go` (same package, same import wall). **`canary.result` minted** (§29 flip), platform-scope, `SchemaVersion: 1`, payload carrying the family minimum's canary half verbatim: **canary kind + lane + pass/fail/delta** (fact 2). Four kinds, per lane.
- **R23 [B]** — **Auth canary (P-T17-1; S03.6; S10.5).** A scheduled cheap real request per lane at ⚙ **`canary.auth_interval`** (declared: duration, default 86400 s, clamp 21600–604800 s). Its wire signal is normalized into `scheduler.LimitSignal` — including **`OnValidCredentials`**, the field already documented as the auth-canary fact — and classified by **`scheduler.Classify`**, whose Class-4 branch (`ClassAuthPolicy` → `ActionLaneFreeze`) fires **lane freeze + flag-now, NEVER an infinite retry-park** (S03.6 *policy-revocation-suspected*; P-T08-2's named worst case). B5-6 is Classify's first production caller (fact 8) and **must not re-implement or fork the classifier** — misclassification lives in one tested component with per-lane fixtures.
- **R24 [B]** — **Canary metering posture, stated explicitly (D4/D7; S10.1).** Canary consumption is **metered like everything** and itemized on the lane owner's meters — negligible by construction, never exempt, never off-ledger. `purpose_tag` for a canary call is **`probe`** (an existing `metering.PurposeTag` value) and the scheduler workload class is **`ClassProbe`**. The **no-load-bearing-metered-paths rule** and the **subscription-coverage rule** hold: both v0 paid lanes are flat-rate (`MeteredExceptions` is EMPTY at v0, G1 P7), so a canary consumes allowance/pressure, not dollars — and it must never be built on a metered path. **This packet spends $0** (the B5-5 precedent): the real-request leg ships **disarmed**, gated behind an explicit operator/env arm, with a **pre-registered projection and stop line** recorded in the report for the coordinator to arm at landing or at the B5 gate.
- **R25 [B]** — **Behavioral canary.** A scheduled fixed-prompt mini-eval per lane through **`evals.Runner`** (fact 9 — reuse the seam; do not build a second runner and do not re-derive promptfoo's CLI facts), at ⚙ **`canary.behavioral_interval`** (declared: duration, default 604800 s, clamp 86400–2592000 s). Alert on score drop. **This is the ONLY drift detection available to the subscription lanes** (S14.6) — state that in the doc comment. **Carried dependency:** the promptfoo binary does not exist until the B5-gate host act, so at landing this leg is stub-verified only and prints `SANCTIONED SKIP (CONVENTIONS §10)` against a real binary's absence.
- **R26 [B]** — **Logprob canary — LOCAL LANE ONLY.** One-token logprob drift tracking at ~zero cost, on `local.TokenLogprob`/`LabelMargin` (fact 16). **The reason is recorded in the spec and must be cited, not inferred:** S03.7 — *"No logprobs on the Z.AI coding endpoint (`logprobs:true` → 200 but no `logprobs` key, endpoint-wide) → the Z.AI lane is behavioral-eval-only for drift detection; only the local lane gets the logprob canary"* [SPIKE P2-S1 Probe 8]; and the wrapped Anthropic CLI exposes none either, so **both subscription lanes are behavioral-eval-only** (S14.6 ¶3). A conformance test must assert the canary **refuses** to run on a non-local lane — a checkable negative.
- **R27 [B]** — **Model-list drift (P-T17-3; S03.6).** Observed-vs-config diff **per account**, on the same schedule. S03.6: the adapter *"periodically diffs each account's observed model list against config"*; routing and 2.7 gap advice consume the observed list **with `verified-on` dates**. A diff raises a `drift.finding` with change class `models` — which is exactly the class that can carry a model-id subject into R19/OQ4. **Engine memory features and compaction behavior join the canary set** [G2 §Follow-ups; S05.7] — register them as rows, do not build new probes for them here.
- **R28 [B]** — **Conformance canaries = the S14.5 adapter suites on their weekly schedule** (S14.6 ¶3 bullet 2; fact 7). The disposition is **OQ5**; whatever is ratified, results land through **`conformance.RecordResult`** into the existing `adapter-anthropic` / `adapter-local` rows (never a new row, never a new event type — `eval.score_recorded` is already minted and co-produced), and **`internal/conformance` is not modified**.
- **R29 [B]** — **A canary failure raises a card, never a kill.** Auth-canary failure on an active lane is **flag-now** (S14.4 alert discipline). No canary terminates, tombstones, or gates a run — the S14.4/G1 D1.3 pause-and-flag posture is platform-wide, and the watchdog's `importwall_test.go` no-auto-kill invariant is the shape to mirror in this package too.
- **R30 [B]** — **The `forced-escalation-e2e` canary tie** (fact 7): that row's `CanaryCadence` (⚙ `verification.canary_interval_hours`) is visible data today. If the coordinator scopes the escalation-canary RUNNER into B (it is named as "B5-6/B6" work), it schedules and records only — `internal/verify` logic stays untouched (comment-level pointers at most).
- **R31 [B]** — **No new ⚙, no new migration, no new Go module in B.** Tally stays 118/33; migrations stay `0001`–`0013` (A's); `components.lock` unchanged by B.
- **R32 [A+B]** — **CONVENTIONS §34** appended (never rewriting §1–§33), citing S14.6/S16.3/S16.4/S10.5/S03.6–S03.7 and recording: the package map + import wall, the watch-row store + seeds, the changedetection.io adoption posture (pin, licence scope, unit disposition per OQ1, discovery/env override, hermetic-stub CI, sanctioned skip), the feed poller's conditional-GET/hash-dedup discipline, refresh-as-proposal, drift-card derive-from-log + fingerprint dedup, the four canaries with the logprob local-only citation, the canary metering posture, every structural-not-⚙ constant with its reason, and the coordinator's OQ dispositions.

---

## 4. Seams to respect (and the stub to use where the phase has not come)

| Seam | Owner | What B5-6 does |
|---|---|---|
| `evals.Runner` (`Identity`/`Run`) | B5-5, `internal/evals` | **Consume** for the behavioral canary. Real binary absent until the B5 gate → stub under `t.TempDir()`, real leg auto-runs when present, else `SANCTIONED SKIP (CONVENTIONS §10)`. Do not re-derive promptfoo's flags/exit codes/schema version. |
| `evals.Runbook.Flag` / `evals.Trigger{Kind: TriggerDrift}` | B5-5 | **Call** per the OQ4 disposition. `internal/evals` is not modified. |
| `conformance.Store.RecordResult` / rows `adapter-anthropic`, `adapter-local` | B5-4 | **Call** for canary results per OQ5. `internal/conformance` is not modified. |
| `scheduler.Classify` / `LimitSignal` / `LimitConfig` | B1-2, `internal/scheduler` | **Call** — first production caller. Never fork, never re-implement, never bypass the Class-4-first ordering. |
| `metering.PriceTable` / `EffectiveDatedTable` / `PriceRow` | B1-2, `internal/metering` | **Produce proposals against these types.** The table ships EMPTY (every row UNPRICED, tier 5 — S10.1's honest posture); the loader/overlay/persistence is metering's, not this packet's (OQ3). |
| `run.Fingerprint.PriceTableVersion` / `EvaluateFreshness` | B0-4, `internal/run` | An **approved** price change is what moves the version; the freshness trigger then fires (G1 Def.5). B5-6 does not write fingerprints. |
| `watchdog.OrganLiveness` (nil today) | B5-3, `internal/watchdog` | **Wire** the shell seam to a `GET /api/v1/systeminfo` probe. `internal/watchdog` logic is not modified. |
| `local.Duty.Call` + `local.AliasWatchlist` | B4-5/B4-6, `internal/local` | **Consume.** Build the schema in `internal/watchlist` (`internal/local` stays read-only). Nil `*Duty` degrades honestly. |
| `InboxSnapshot.DriftCards` | B5-2/B5-4, `internal/api` | **Populate by derivation.** `internal/api` does not import `internal/watchlist`. |
| `eventlog` contract registry | B5-1, `internal/eventlog/contract.go` | **Flip two declare-only types to minted** + `producerTypes()`. New families are S00.9 amendments (stop-and-flag). |
| Miniflux | S14 Deferred / G2 Def.12 | **Stub = the deferral record.** Do not build the HMAC webhook. |
| The v1 user-facing schedule/trigger surface | S10.7 / S19 | **Untouched.** ⚙ `scheduler.missed_slot_default` stays referenced-not-consumed; at v0 the only schedules are platform-internal timers. |
| OTLP projector, CodeAD rule synthesis, DeepEval swap, DuckDB sidecar | S14 Deferred | **Untouched.** |

---

## 5. ⚙ settings (verified against `internal/settings/index.go` this session — **NONE re-declared**; tally stays 118 keys / 33 domains)

**Consumed by dotted key, read live at evaluation time:**

| Key | Type / unit | Default | Clamp | Where |
|---|---|---|---|---|
| `watchlist.fetch_fail_streak` | int / cycles | 3 | 2–10 | R18 meta-watch (index:825) |
| `canary.auth_interval` | duration / seconds | 86400 (daily) | 21600–604800 | R23 (index:832) |
| `canary.behavioral_interval` | duration / seconds | 604800 (weekly) | 86400–2592000 | R25 (index:839) |
| `adoption.price_data_stale_days` | int / days | 60 | 14–180 | R14 staleness → LiteLLM-primary proposal (index:892) |
| `verification.canary_interval_hours` | — | — | — | R30 only, **if** OQ5/R30 is scoped in |

**Referenced but NOT consumed** (name the seam in a comment; never hardcode): `scheduler.missed_slot_default` (v1 schedule surface), `eval.sweep_interval` (B5-5's), `adoption.dependency_pass_months` / `adoption.abandonment_months` (S16.7's quarterly pass).

**`internal/settings/index.go` must stay untouched.** A genuinely new ⚙ is an **S18-sweep event and a stop-and-flag**, not a packet decision: it breaks `TestIndexMatchesS18Tallies` (118 keys), the domain count (33), the per-owner S14 count (15), and the 118-leaf `JSONSchema()` assertion, and requires an S00.9 amendment plus a re-run of the S18 sweep. Every cadence/window/threshold this packet needs beyond the table above is a **structural constant with a recorded reason** (the §7 `sseBatchSize` / §9 auth-constant / §11 aging-bias / §31 sweep-interval precedent), interim under the standing settings-tab directive.

---

## 6. Files expected to change

**A:** `internal/watchlist/{watchlist.go,rows.go,seed.go,fetch.go,feed.go,cdio.go,aggregator.go,prices.go,classify.go,drift.go,decay.go,store.go}` + `_test.go` + `importwall_test.go` · `internal/storage/migrations/0013_watchlist.sql` · `internal/eventlog/contract.go` (flip `drift.finding`, `producerTypes()`) + `contract_test.go` · `internal/api/projection.go` (`DriftCards` derivation) + its test · `internal/shell/shell.go` + a `watchlist_seams.go` (loop + composition + `watchdog.Config.Organs`) · `components.lock` (+1 entry) · `P3/CONVENTIONS.md` (§34) · possibly `internal/units/units.go` (**only** if OQ1 lands on "generate a unit"; the package-doc parenthetical correction from §2(i) lands regardless).

**B:** `internal/watchlist/{canary.go,canary_auth.go,canary_behavioral.go,canary_logprob.go,canary_models.go}` + `_test.go` · `internal/eventlog/contract.go` (flip `canary.result`) + `contract_test.go` · `internal/shell/watchlist_seams.go` (canary schedule) · `P3/CONVENTIONS.md` (§34 extension).

**Never touched:** `Spec/`, `Docs/`, `Research/`, `P3/STATE.md`, `internal/settings/index.go`, migrations `0001`–`0012`, `internal/evals`, `internal/conformance`, `internal/worker`, `internal/verify`, `internal/scheduler/limits.go`, `Spec/benchmark-preregistration-v1.md`, `*-api-key.txt`.

---

## 7. Adopted components touched

- **changedetection.io** — **NEW adoption** (R5). Pin `0.55.8`, Apache-2.0, `organ-unit`, no `modules`. S16.4 walk recorded; #10 queues for the B5 gate. Lock 19 → 20.
- **genai-prices `data.json`** — scope per **OQ3**. If vendored: `kind: "data"`, content-hash pinned, MIT, upstream LICENSE kept in-tree (S16.2). Lock 20 → 21.
- **Miniflux** — pre-registered fallback, `standby` only if OQ-ratified (R12).
- **promptfoo `0.121.19`** — **consumed unchanged** via `evals.Runner`; its lock entry already carries the B5-6-registered watch row `"S2.8 promptfoo drift canary [XREF:S14; registered with the B5-6 watchlist executors]"` — R3 must materialize a real watch row that resolves it.
- **All 19 existing entries** — R3 must seed a watch row for each entry's `watch` reference (S16.4 #9), asserted by test.
- **No new Go module.** `go.mod` unchanged; `go run ./tools/lockgate` green.

---

## 8. CONVENTIONS constraints binding this packet

§1 one binary, `internal/` only · §2 stdlib-first (any Go module is an S16 adoption in the same commit), gofmt/vet clean, no ⚙ as a constant · §3 stdlib `testing` only, no writes outside `t.TempDir()`, never commit red · §4 exact pins, path-scoped licence with a check date, funeral plan (`replacement` + `abandonment`), non-empty `watch`, `last_review`, removal only by funeral edit, `go run ./tools/lockgate` before commit · §5 subject `P3-B5-6[A|B]: <summary> (S14.6 …)`, trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`, stage files explicitly, **never push** · §6 one-transaction numbered migrations, immutable once committed, append-only `run_events`, `event_seq`-only ordering, platform-scope rows carry `run_id NULL` + `Generation 0`, payload validated + capped by ⚙ `state.event_payload_cap` · §7 shell composition order, units **generated never installed**, logging is `log/slog` to stderr, SSE frame contract untouched · §10 **the one sanctioned skip class** (`SANCTIONED SKIP (CONVENTIONS §10)`), suites assert behavior never docs, pin↔installed deltas reported LOUDLY and never silently retargeted · §11 metering/scheduler seams, `MeteredExceptions` EMPTY, no dollar-ranked choice between flat lanes · §29 declare-once (every minted type registered) · §30 store-raw/serve-redacted (drift payloads flow through the SSE redaction edge unchanged — do not add a second redaction site) · §31 no auto-kill, derive-from-log not side-store, dueness from stored state · §32 conformance registry is a HOME not an executor · §33 runners-never-stores.

---

## 9. OPEN QUESTIONS — coordinator dispositions required BEFORE the executor launches

**OQ1 — changedetection.io's deployment shape, and who writes its unit.**
S14.6 says "own unit [S01.2]". `internal/units`' package doc lists "the watchlist executor" among organs that ship their own units and are *deliberately not generated here*. **Live-verified this session: at tag `0.55.8` the project ships NO systemd unit — only `Dockerfile` + `docker-compose.yml`** (§2(i)). So that parenthetical is factually wrong for this component.
- **(a)** Host install (pipx/venv from PyPI `0.55.8`) + a **Sinet-generated** `sinet-watchlist.service` under the **B4-5 llama-swap carve-out** (generated because upstream ships none; `ExecStart` runs the operator-installed third-party binary, never the sinet binary; `Draft: true`; generation never installs). Correct the `units.go` parenthetical in the same commit.
- **(b)** Container via the publisher-owned Docker Hub image (10.8M pulls) + a generated `Type=exec` unit wrapping the container runtime. **Cost:** Sinet has no container runtime in `components.lock` and no ratified host container posture — that would be a second adoption and a new hardening surface.
- **(c)** Treat the unit as purely an operator artifact: generate nothing, document the required `ExecStart`/hardening in the gate proposal.
- **Recommendation: (a).** It is the exact precedent (B4-5, ratified), keeps the S01.2 hardening set + `User=sinet` + the slice under Sinet's control, adds no adoption, and keeps "generated, never installed" intact. **Consequence of (b):** a container-runtime adoption inside an observability packet. **Consequence of (c):** the organ's hardening is unversioned and drifts silently — the thing units generation exists to prevent.

**OQ2 — the `json://` webhook ingress auth posture.**
CONVENTIONS §9 makes every route outside the five pre-session endpoints require an authenticated identity, fail-closed 401; a webhook from a local organ holds no session cookie.
- **(a)** Loopback-source check + a high-entropy shared secret in a header, the secret held by the credential broker and never logged; the route is documented as a **new route class** in §9's terms.
- **(b)** No inbound route at all — Sinet **polls** `GET /api/v1/watch` + `/difference/<from>/<to>` on the R20 loop and the organ's notifications are unused. Zero new auth surface; costs the push-latency S14.6 describes.
- **(c)** A UDS-only ingress (the broker's SO_PEERCRED posture) — strongest, but changedetection.io's apprise `json://` targets a TCP host:port, so it does not fit without wrapping.
- **Recommendation: (b) for the first landing, with (a) pre-registered as the upgrade.** Polling is a client detail the spec already sanctions ("polling fallback is sanctioned", S14.3), it adds no route and no secret, and the R20 loop exists anyway. **Consequence of (a) now:** a genuinely new auth class in a packet that is already oversized. **Consequence of (b):** hits are seen at loop cadence, not instantly — acceptable at weekly/continuous-feed cadences.

**OQ3 — genai-prices scope: vendor now, or refresh-machinery only?**
S16.3 + G2 Def.16 bind "vendor the pinned `data.json`"; `internal/metering/price.go` records the vendored seed + loader + overlay + persistence as an explicit **metering follow-up**, and the table ships EMPTY on purpose.
- **(a)** Vendor `prices/data.json` at a pinned commit (content-hash) + materialize the `data` lock entry + build the refresh/proposal job. The vendored file's one consumer is the diff baseline; the loader/overlay stays metering's.
- **(b)** Build the refresh/proposal job only; the first refresh proposes the whole table; no vendored file, no lock entry until approval. **Problem:** the rail requires the entry *before* first use, and S16.3 binds the row now.
- **(c)** Vendor + loader + overlay + persistence all here — closes S10.3 completely.
- **Recommendation: (a).** It honors G2 Def.16 and the rail without pulling S10.3's loader into an S14 packet, and the diff baseline is a legitimate consumer. **Consequence of (b):** a bound S16.3 row stays unmaterialized and the first proposal is unreviewably large. **Consequence of (c):** meaningful extra scope on an already-split packet, in another section's package.

**OQ4 — does a non-model drift finding trigger revalidation at all?** (the B5-5 F9 carry)
S14.8's trigger is "a watchlist drift finding", unqualified; the only wired hook is `FlagByModel`, which needs a model id.
- **(a)** Fire the runbook **only** for change class `models` with a model-id subject; every other class raises the drift card and **records on the card that no revalidation was triggered**. No `internal/evals` change, no faked flag, checkable by a test asserting non-model classes do not call `Flag`.
- **(b)** Extend `evals.Hooks` with a non-model flag path (e.g. flag-by-lane / flag-by-engine-pin for an `endpoints` drift) — scope creep into B5-5's package and arguably a second gate.
- **(c)** Leave the whole edge as a seam; B5-6 emits findings and wires nothing.
- **Recommendation: (a).** It is exactly what the existing hook can honestly answer, it makes the residual gap visible instead of silent, and it leaves (b) available as a later, properly-scoped amendment. **Consequence of (b):** two packets' packages move in one commit and the S12.10 "no second gate" rule gets fuzzy. **Consequence of (c):** B5-5's accept-shape-only seam stays unexercised and the S14.8 trigger set is still one-third unwired after B5-6.

**OQ5 — what does "the weekly RUN of the adapter suites" mean for a Go binary?**
B5-4 assigned it here (fact 7), but the adapter conformance suites are Go tests (tiers F/R/L, §10) and the release binary cannot run `go test`.
- **(a)** The conformance canary is a **dueness surfacer**: on a due weekly row it raises a card ("adapter-anthropic suite due"), and the actual run stays an operator/CI act that records via `conformance.RecordResult`. Honest; adds no execution machinery. **But** B5-4 already ruled that dueness raises nothing at v0 (its OQ3), so this is a deliberate narrowing of that rule to the two weekly lane rows.
- **(b)** Re-implement the tier-R adapter checks as in-process probes callable from the daemon — real weekly execution, at the cost of duplicating a validated instrument (the exact thing B5-5's OQ6(a) forbade for the T15 battery).
- **(c)** Wire the daemon to shell out to a CI/`go test` invocation — rejected on sight: the release artifact is a static binary with no toolchain, and it would make the daemon a build host.
- **Recommendation: (a).** **Consequence of (b):** two implementations of the same conformance claim that can disagree — precisely the failure S14.5 "suites assert behavior" is guarding. **Consequence of (c):** a hard architectural violation. If (a) is ratified, note that B5-4's "dueness raises nothing" becomes "dueness raises nothing **except** the two weekly lane-affecting adapter rows", and record that in CONVENTIONS §34.

**Settled here, not escalated (recorded per the spec's clear implication):** the watch-row store is a `platform.db` table + in-code `SeedRows()` with a strict-JSON operator override (the §32 registry + §14/§15/§17 seed precedents; G2 Def.12's "no new DB" means no second store, not no table) · cards derive from the log with no side table (§31/§32) · dueness derives from stored state, never a ticker (§31/§32/§33) · cadence constants are structural, not ⚙ (§7/§11/§31) · the local second pass builds its schema in `internal/watchlist` (§31 OQ6) · `canary.result`/`drift.finding` are platform-scope with `user_id='platform'` (§32 `RecordResult` precedent) · this packet spends $0 and the real canary leg ships disarmed with a pre-registered projection (§33).

**No spec conflict, gap, or impossibility was found.** The one factual defect uncovered — `internal/units`' package doc asserting the watchlist executor ships its own unit — is a repo-doc error correctable under the B4-5 carve-out, **not** an S00.9 amendment path event.

---

## 10. Acceptance checklist (the evaluation rubric — each item concretely checkable)

1. `gofmt -l` empty; `go vet ./...` clean; `go build ./...` and `go test ./...` green; `go run ./tools/lockgate` prints OK with the new entry count.
2. `internal/settings/index.go` **byte-unchanged**; `TestIndexMatchesS18Tallies` passes at **118 keys / 33 domains / S14:15**; the `JSONSchema()` 118-leaf assertion passes.
3. Migrations `0001`–`0012` **byte-unchanged**; `0013_watchlist.sql` present (A only), applying in one transaction with its `user_version` bump; its structure-immutable and no-delete triggers proven by a raw-SQL attempted-violation battery.
4. `drift.finding` (A) / `canary.result` (B) are **minted** in `internal/eventlog/contract.go` with real provenance and appear in `producerTypes()`; the declare-once test passes; inventory is 81/12 (A), 82/11 (A+B). `registry.drift` is untouched and a test asserts the two are not conflated.
5. A representative payload for each new type satisfies the `FamilyDriftCanary` required-field golden (source, lane(s), change class, severity, one-line summary, incident fingerprint; canary kind + lane + pass/fail/delta).
6. `internal/api/topics.go` is **unchanged**, and a test proves a `drift.finding` event tags `inbox` through the existing `topicsForEvent`.
7. `InboxSnapshot.DriftCards` is populated **by derivation over `run_events`** — no new table, no `asks` row; owner-scoping proven (operator sees platform-scope drift cards, a member sees none); `internal/api` does not import `internal/watchlist` (AST-asserted).
8. Fingerprint dedup proven: N hits inside one incident window ⇒ **one** card; a new window ⇒ a new card.
9. `internal/watchlist`'s `TestImportWall` passes with a non-tautology probe, and the package imports none of `api`/`adapters`/`worker`/`stage`/`verify`.
10. `components.lock` carries the changedetection.io entry at pin **`0.55.8`**, `kind: "organ-unit"`, SPDX `Apache-2.0` with a **path-scoped** `scope` and a `checked` date from this packet's own scan, a named `replacement` + `abandonment`, non-empty `watch`, and no `modules`. The S16.4 ten-check walk is recorded in the packet report; #10 is marked "queued for the B5 gate".
11. Every `components.lock` entry's `watch` reference resolves to a real seeded watch row (test).
12. The seeded rows include the **`[schema]` STUDY row** (source `schema-harness.github.io`, report-12 caveat in `notes`) and the **awesome-harness-engineering both-referents row** (`walkinglabs/…` + `ai-boost/…`, drop-if-both-stall), plus every S16.8 row and the report-02 §6 tiers (test asserts the count and the presence of each named row).
13. changedetection.io integration is **REST + config only** — no vendored, patched, or forked upstream code anywhere in the diff.
14. All changedetection.io tests run against an `httptest` stub; an AST/string-literal tripwire forbids dialing a live host; the real leg prints `SANCTIONED SKIP (CONVENTIONS §10)` when unreachable. **Nothing was installed, no host state changed.**
15. `watchdog.Config.Organs` is wired to a `GET /api/v1/systeminfo` probe; an absent organ produces a `watchdog.organ_absence:watchlist` digest flag (test), and the feed/API tiers keep working with the page tier absent.
16. The feed poller sends `If-None-Match`/`If-Modified-Since` from stored state and treats `304` as no-change (test); content-hash dedup suppresses a byte-identical refetch (test); a malformed feed is a fetch failure, never a panic (test).
17. `go.mod` is **unchanged** — zero new Go modules.
18. The genai-prices refresh produces a **proposal record**, never a table write; a test proves no code path auto-applies a price change and none auto-flips a billing-regime flag.
19. `watchlist.fetch_fail_streak`, `canary.auth_interval`, `canary.behavioral_interval`, `adoption.price_data_stale_days` are read **by dotted key at evaluation time** (live-apply proven by a test that changes the value mid-run); no numeric literal of any of them appears in code.
20. Dueness derives from stored state — a test proves the loop resumes correctly across a simulated restart and a wall-clock jump; **no `time.Ticker` drives cadence**.
21. **B:** the auth canary calls `scheduler.Classify` and an auth-shaped signal on valid credentials yields `ClassAuthPolicy`/`ActionLaneFreeze` + a flag-now card — and **never** a retry-park (explicit negative test); `internal/scheduler/limits.go` is unchanged.
22. **B:** the logprob canary **refuses** a non-local lane (explicit negative test), and its doc comment cites **S03.7** for the reason (Z.AI exposes no logprobs endpoint-wide; the wrapped Anthropic CLI exposes none) rather than asserting it.
23. **B:** canary calls carry `purpose_tag` `probe` and workload class `ClassProbe`, are metered like every other call, and the real-request leg ships **disarmed** — the report records $0 spent plus the pre-registered projection and stop line.
24. **B:** the behavioral canary consumes `evals.Runner` and does not construct a second runner; `internal/evals` is unchanged.
25. **B:** canary results land through `conformance.RecordResult` into existing rows; `internal/conformance` is unchanged; no new event type beyond `canary.result`.
26. The drift → revalidation edge matches the ratified OQ4 disposition exactly; a non-triggering drift class is **visibly recorded** as not triggering; `internal/evals` and `internal/worker` are unchanged.
27. `P3/CONVENTIONS.md` §34 is **appended** (§1–§33 byte-unchanged) and records every item in R32 including the coordinator's OQ dispositions and each structural-not-⚙ constant with its reason.
28. `Spec/`, `Docs/`, `Research/`, `P3/STATE.md`, `Spec/benchmark-preregistration-v1.md` are untouched; nothing was pushed; no `*-api-key.txt` was read or staged.
29. The packet report lists every live fact re-verified at build time with its date and URL, and states plainly which of §2's UNVERIFIED items (the per-watch LLM intent field, the Playwright enablement, the `json://` payload shape) were closed and which remain open.
30. Commit subject matches `P3-B5-6[A|B]: <summary> (S14.6, …)` with the CONVENTIONS §5 trailer; files staged explicitly.
