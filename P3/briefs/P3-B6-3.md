# P3-B6-3 brief — S15.2 content families: settings + deliverables/preview + memory APIs

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.2 + S15.8 + S15.9** owning (`Spec/drafts/S15-frontend-api.md`); **S01.10** (`S01-process-architecture-shell.md`); **S10.2–S10.4** (`S10-metering-scheduling.md` — the price table); **S13.1–S13.4, S13.6, S13.8, S13.9** (`S13-deliverables-review-git-backup.md`); **S09** (`S09-memory-knowledge.md` — whole section; the write gate, scopes, provenance, removal); **S18** (`S18-settings-registry.md` — the index, S18.3 data surfaces, S18.4 R9); **`Spec/frontend-components-v1.md` (FC-v1) §2 + §4**. `P3/CONVENTIONS.md` binds: **§5, §6, §7, §9, §11, §16, §17, §22, §23, §24, §25, §29, §30, §38, §39 (+ -B/-C)** (constraints extracted in §6 below). **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is packet 3/9 of B6** (queue cut 2026-07-28, `P3/STATE.md` §B6; row 5 of 11 counting the B6-2 A/B/C split — B6-1/2A/2B/2C are landed). It completes the **Go API's content families**: `/api/settings` (registry schema + UISchema + values + bounds + audit + validated writes + the price-table surface), `/api/deliverables` (revisions, diff data, anchored comments, accept behind the High-tier re-prompt, preview sessions), `/api/memory` (scoped entries through the S09 write gate). It is **NOT** the SPA or any rendering (B6-4+; JSON Forms consumes what this packet emits — NO React/HTML/renderer code here), **NOT** the settings/inbox UI (B6-6), **NOT** the assistant (B6-7), **NOT** the review/try-it UI (B6-8), and **NOT** a rebuild of anything landed — `settings.Registry`, `review.Store`, `accept.Accepter`, `preview.Manager`, `memory.Store/Gate`, `intake.FollowUp`, the approvals answer machinery (`stepUp`/`checkPin`/`gates.CanonicalHash`) are consumed/extended, never re-implemented.

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit — S15 content scope is not S10/S11 security-dense; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-28 — do not relitigate)

- **Held-not-routed seams THIS packet routes** (`internal/api/api.go`, read in full): `Config.Accept *accept.Accepter` ("held for the B6-3 deliverable content family", api.go:198–199), `Config.Preview *preview.Manager` (F17, api.go:214–216). `Config.Settings` is a **narrow `Duration(key)` interface only** — the full registry surface needs a NEW held seam (`internal/settings` is a leaf: storage+eventlog; importing it in `internal/api` creates no cycle — the §39 `internal/gates` narrow-held-dependency precedent). NO `/api/settings`, `/api/deliverables` (except follow-up), or `/api/memory` route exists today.
- **Landed API patterns to reuse verbatim** (§38/§39): `readScope`/`authorizeOwner` (404-before-403), `writeReadRedacted`/`writeReadJSON`, `badRequest`/`SurfaceError`/`httpCode`, `readLimit` (`readPageDefault` 200 / `readPageMax` 1000), `s.stepUp` (High-tier PIN verify-at-act via `auth.Store.VerifyPIN`, purpose-tagged `auth.reprompt`; the dev identity can never step up), `checkPin` + `gates.CanonicalHash` (the ONE canonicalization — never a second), `resolvedAnswer` (repeat → 200 read-back), `redactionEdgeFiles` = enumerated `{sse.go, reads.go, approvals.go}`.
- **Settings registry** (`internal/settings`, read in full): `JSONSchema()` EXISTS (schema.go — nested objects; clamps as minimum/maximum; help as description; defaults; `x-sinet` = {key, section, type, unit, auto, perUser, restartRequired, dormant, ratifiedBy}). **NO UISchema emitter exists** — the S15.9 gap this packet closes. `Decls()/Decl(key)/Domains()/Bounds(key)` are exported reads; **no bulk effective-values read exists** (`effective` is unexported — a small exported read is needed). `Set(SetRequest{Key, ForUser, Value, Actor, Reason})`: validated + clamped (declared clamp ∪ operator bounds override) + relational rules (`checkRules`) + ONE tx {override cell, `settings_events` row, `settings.changed` event}; `Value nil` = reset-to-default (row deleted); **Actor kinds are `operator` | `automation` ONLY — no member kind exists** (`Actor.validate`); automation: auto-flagged keys only, value moves only, no reset, no per-user. `SetBounds` = the operator-owned bounds edit. `settings_events` (0001:242) = {actor, key, user_id, old, new, ts, reason} + key index — the per-setting audit history is a direct SELECT. Restart-required keys: exactly 2 (`shell.inhibit_delay_max`, `shell.journal_max_use`); auto keys: 5; per-user keys exist (`intake.zero_interaction_cost_usd`, `retention.compaction_horizon`). **`internal/settings/index.go` is byte-unchanged through all of B5+B6 (118 keys / 33 domains, test-pinned).**
- **The price table is a SEAM with no storage** (`internal/metering/price.go` + shell.go:281): `EffectiveDatedTable` is in-memory (`NewEffectiveDatedTable("empty-v0")` + `Add(rows)`); the shell composes it EMPTY (§11 ratified posture: every row prices UNPRICED, never $0). `PriceRow` carries the S10.3 fields {model, lane, unit prices, effective_from, verified_on, source}. **No persistence, no user overlay, no loader exists** — "the loader / user overlay / persistence stay S10.3's metering follow-up" (§34). The S02.6 freshness fingerprint ALREADY carries a `PriceTableVersion` member (project_seams). B5-6A's `BuildPriceProposal` lands genai-prices refreshes as PROPOSAL drift cards (never auto-apply, G2 Def.16); the vendored `internal/watchlist/pricedata/` is the DIFF BASELINE only. `MeteredExceptions` is EMPTY at v0 (G1 P7); the flat/metered flag is per-user DATA on the price table (S10.2); S18.3 names three data-valued surfaces — price table (S10.3), budgets (LANDED at B6-2B), per-model attributes (S10.2/S03.6).
- **Deliverables substrate** (`internal/review`, read in full; migration 0007): `deliverables` (state in-review/accepted/superseded; identity immutable; `current_revision` never rewinds), `deliverable_revisions` (immutable; PK (deliverable,n) contiguous; content pin = `content_sha256` XOR `objects`; `snapshot_sha`/`verdict_ref` fill-once), `review_comments` (**immutable by trigger — "only the one-way consumption stamp moves"; never deleted**), `review_comment_anchors` (write-once placements). Verbs: `Deliverable/Revisions/RevisionAt/RevisionFiles`, `Compare(deliverable, oldN, newN)` → `Comparison{Surface, Unified, OldObjects/NewObjects, Changed, Fallback/Label, PixelDiff}` — **the host-side unified git diff IS the wire product** (FC-v1 §2: react-diff-view parses unified git diffs natively; highlighting is CLIENT-side via its tokenizer; **no server-side highlighter exists in the platform**); `oldN=0` = pre-task base via the landed `BaseContentSource` seam; `AddComment(CommentInput{DeliverableID, RevisionN(0=current), Author, Body, Severity(blocker|note), Anchor?, FileLevel, Suggested, RunID(""=platform-scope)})` — claimed anchors validated server-side at birth (`validateBirth`, ⚙ `review.anchor_drift_lines`); `PlacedComments(deliverable, n)` recomputes placements on EVERY render (statuses exact/mapped/drifted/file/orphan; write-once rows must agree — mismatch is loud); `Drain` is THE one consumption path (source-scan-pinned to drain.go); `Accept/AcceptedRevision` (review/accept.go) appends **`deliverable.accepted`** (platform-scope, owner-attributed) — the type B5-7's sampler tails. Events: `artifact.produced`, `review.comment`, `review.drained` minted.
- **The accept orchestration** (`internal/accept/accept.go`, read in full; §24): `Accepter.Accept(Input{DeliverableID, AcceptingUser(+Name), ProjectID, Subject, Provenance, Engine/Model/VendorNoreply, GitProfile})` is **ONE call**: in-review check → current revision must be repo-backed (`SnapshotSHA`) → applies-cleanly onto the depth-1 merge-queue base → collision ⇒ `Outcome.Card` (`MergeCard{Reason: applies-clean-collision | lease-rejected, Options: agent-auto-resolve | resolve-manually | abort-to-new-attempt}`) → trailers rendered (structural template) → **`Journal.Propose` + `Journal.Approve` fire INLINE** (class-A effect, payload self-sufficient for crash re-drive; approval time pins the squash timestamp) → BeginExecute → squash (signing = structural per-user posture, never a call flag) → broker CAS push (`Authorized: true`; lease rejection ⇒ merge card, NEVER a new-sha retry) → Succeed → `review.Accept` → `AdvanceDefaultBranch` → sibling-accept freshness fire (`RoutedRuns`). **The doc comment names the step-up call site as the API/answer layer's** (Input:191–194): "the S01.9 PIN step-up is enforced at the API/answer layer (the §9 approvals-family call)". The accept effect carries **no RunID** (`gates.Proposal.RunID` optional, unset here) and **never rests in `proposed`** — created and approved within one request. `ReDrive/ReDriveApproved` handle the crash window at shell startup. A repeated `Accept` after success errors "deliverable is accepted, not in-review" — the transport maps this to the S15.2 repeat semantics.
- **Preview** (`internal/preview`, read in full; §25): `Manager.Launch(LaunchRequest{DeliverableID, Revision(0=current), Role, User})` → `*Session{ID, Deliverable, Revision, Role, Lane(static/dev-server/notebook/cli/sidecar/none), State(live/self-preview/requires-container-tier/no-preview/unavailable/at-capacity), Reason, URL, Ports (multi-port picker DATA), Runner, User, …}` — **every type gets a defined state, no refusal branch**; non-backed dispositions return as data with NO port held and NO start event ("nothing started"); `Stop(sessionID)` (no actor param — the transport authorizes); `List()` returns ALL sessions (**the transport must owner-scope**); `LaunchComparison(deliverableID, candidateRevision, user)` → the before-vs-after pair (accepted revision = before when one exists; else honest single-instance). Low-tier: **NO effect journal, NO approval, NO PIN** (S13.8). Events `preview.started/stopped` land for backed sessions only. ⚙ `preview.idle_stop`/`preview.max_concurrent` consumed INSIDE the package. **Hermetic posture (the standing host hazard):** the live Caddy admin API fronting 8481/8482 is NEVER dialed — `NewCaddyClient("","")` = routing disabled (the dev default); an AST routable-URL tripwire polices preview test files; api tests inherit the obligation.
- **Memory** (`internal/memory`, read in full; §17): `Store` reads (`Get`, `ListOwned(owner)` — the S4.1 "everything stored about them" read, `HouseObject`, `Search` (run-scoped — an ENGINE surface, not this family's), influence refs); `Gate` writes (`CreateManual`, `Import`, `NewVersion` (edit = supersession, in-place mutation trigger-blocked), `Adopt`, `Remove` (→ influence list), `TrueDelete` (tombstone), `ResolveConflict`) — the gate's own walls: `ErrNotHuman` (actor must hold a users row; `platform`/`dev` refused), `ErrOperatorRequired` (**house scope demands the operator — D10, enforced in `Gate.authorize`**), `ErrNotOwner` (true deletion strictly the owner), `ErrScopeDormant` (worker-overlay = v1), `ErrLayerForbidden` (no L1 writer at v0). `Entry` carries the full S09.2/S09.5 shape (scope/layer/kind/topic_key/selectors/status/version/supersedes + origin/proposer/approver + verified/expiry/last-injected + tombstone). `knowledge_conflicts` is the S09.7 question card's durable home — **NO inbox kind serves it** (the landed 8 kinds don't). Events `knowledge.{write,remove,delete,conflict,curation}` minted. `lesson_proposals` are structurally outside every read path (v0-dormant station 2). Sharing/browse/adopt surfaces are **v1** (S09.4 activation table). **The §17 import wall bars `adapters/claudecli/stage/ledger/intake` from importing memory — `internal/api` is NOT barred** (conformance_test.go:222; the S09.4 v0 row names "human-direct entries via workspace UI" as live).
- **"Request bounded revision" — both landed doors** (grounded; the S15.2 deliverables-row verb): (i) for a FINISHED deliverable, `POST /api/deliverables/{deliverable}/follow-up` with preset `revision` is LANDED AND ROUTED (B6-2A, actions.go — validates revision existence, spawns via `FollowUp.Spawn`, presets are framings over the one `task_successor_of` link); (ii) for an IN-REVIEW deliverable parked on a rework-family card, the S07.7 `revise_with_guidance` answer verb is LANDED AND ROUTED (B2-5 `Surface.Answer` → `AnswerVerifyAsk`; guidance → `RecordGuidance` → durable requester comments → THE drain; reachable via `POST /api/asks/{ask}/answer` and the B6-2A `POST /api/approvals/{id}/answer`). The phrase "bounded revision" appears NOWHERE in the spec outside the S15.2 table row itself (grep-verified) — its binding is OQ3's.
- **UISchema ownership** (grounded): S15.9 writes "the registry-emitted JSON Schema + UISchema **(S01.10)**" — the emission is the REGISTRY's (S01.10 (b): the one artifact set drives validation/UI/docs so they cannot drift). FC-v1 §4: JSON Forms' `Group`/`Categorization` (tabs) + SHOW/HIDE/ENABLE/DISABLE rules are **core, theme-independent UISchema vocabulary**; the renderer set is Sinet-owned (B6-6's). So the UISchema emitter is a NEW `internal/settings` sibling of `JSONSchema()` — not an api-layer improvisation.
- **S18.4 R9** (binding): BENCH-REG frozen values "surface in the registry marked **'registered — changing this value requires a re-registration commit'**"; only `benchmark.sampling_rate` is a live dial. `benchmark.RegisteredValues()` exists (§35); `internal/api` imports `internal/benchmark` in NEITHER direction — the `BenchmarkSurface` seam is the sanctioned bridge (§39-C). Engine pins stay `components.lock`-homed (R9 first half) — not a settings row, not this surface's to serve.
- **Counters at open** (assert before changing): ⚙ **118 keys / 33 domains** (`internal/settings/index.go` byte-unchanged); `components.lock` **21**; migrations **0001–0018** (`user_version` 18); event inventory **90 minted / 5 declare-only / 95 registered**; CONVENTIONS **§1–§39 (+ -B/-C)**.

---

## 1. Numbered requirements

### A — `/api/settings`: the registry surface (S15.9; S01.10; S18)

- **R1. `GET /api/settings` — the one registry read** [S15.9; S01.10; 13.4/13.5]. Serves, from the LANDED registry: the JSON Schema (`JSONSchema()` verbatim), the UISchema (R2), and a values block — per declared key: effective value (override-aware; per-user overrides listed for `PerUser` keys), default, **effective clamp bounds** (`Bounds(key)` — declared clamp ∪ operator override, served as data so the UI "shows each setting's clamp bounds"), restart-required, auto, per-user, dormant, unit, help (13.5), ratified-by, owning section. A small exported bulk read lands in `internal/settings` (the emission home); `internal/api` computes nothing of its own. **`index.go` stays byte-unchanged** — new exported READS only, in sibling files.
- **R2. The UISchema emitter — `internal/settings`, S01.10's emission** [S15.9; FC-v1 §4]. A new sibling of `JSONSchema()` emitting the JSON Forms core vocabulary: `Categorization` with one `Category` per domain (33), grouped/ordered stably (the registry's declared order; domain→section titles as landed in the schema emitter), one `Control` per declared key (scope `#/properties/...`), `Group` where a domain's size warrants sub-grouping (executor discretion WITHIN the vocabulary). **Completeness is test-pinned**: every `Decls()` key appears exactly once; a key added later without a UISchema entry fails the build. **v0 emits NO SHOW/HIDE rule** — no registry datum demands conditional visibility (dormant/restart/auto are BADGE data riding `x-sinet`, not visibility conditions); fabricating a rule to exercise the vocabulary is exactly what the honest-absence discipline forbids. The emitter's rule-emission capability exists structurally (the vocabulary is in the shape) with the reason recorded.
- **R3. `GET /api/settings/{key}/history` — per-setting audit history** [S15.9: "per-setting audit history renders from `settings_events`"]. A bounded, newest-first SELECT over `settings_events` by key (the landed key index; the §32 read-without-import posture is unnecessary — the rows are already the api DB's to read). Serves {actor, user_id (per-user scope), old, new, ts, reason} verbatim — settings values are CONTENT (§38 ruling (b)); nothing redacts here. An unknown key is 404 against `Decl(key)`.
- **R4. `POST /api/settings/{key}` — validated set/reset; `POST /api/settings/{key}/bounds`** [S01.10; G1 rider 1; S18.1]. Set/reset routes `Registry.Set` (body `{value | null, for_user?, reason?}` — null = reset-to-default); bounds routes `Registry.SetBounds` (operator-owned). **Actor mapping is the registry's own model** (recorded reading): the registry admits `operator`|`automation` actors ONLY — so the transport requires the operator role bit for every settings write (`Actor{Kind: operator, ID: userID}`), 403 for a member; `ForUser` targets per-user keys (operator-administered). Automation writes never enter through HTTP (they are in-process registry calls — the S12.10 swap gate); the transport never constructs an automation actor. Error mapping: `ErrUnknownKey` 404, `ErrOutOfBounds`/type/enum/rules 400 with the registry's own message, `ErrNotAttached` 503. Every write already lands {override cell + `settings_events` + `settings.changed`} in ONE tx — **no `decision.recorded` co-mint** (§39 OQ8 rule: an act with a landed canonical row is never double-minted).
- **R5. Registered-values display block** [S18.4 R9; §35]. The settings read serves the BENCH-REG frozen values as a read-only block, each marked with the registered-marker string verbatim, sourced through the `BenchmarkSurface` seam (extended additively with a registered-values read; nil Benchmark ⇒ the block is an honest absence with its reason). **No registered number is restated in `internal/api`** (the §39-C source-scan discipline extends). Engine pins are NOT served here (manifest home `components.lock` — R9's own words); the settings UI's pin display, if ever wanted, is a later surface with its own grounding.
- **R6. The price-table surface — per OQ1's disposition** [S15.2 settings row: "price table … price-table edits"; S10.3; S01.10: "the D5 price table is its own owner-attributed table under the same audit pattern"; S18.3]. Read: the stored effective-dated rows (empty ⇒ an honest empty table with the UNPRICED-posture reason served as data). Write: operator edits (add a future-dated row; never in-place mutation of a priced past — effective-dating IS the edit model), each audited per OQ1's audit shape. **Refresh-as-proposal interplay** (G2 Def.16; §34): the B5-6A drift proposals stay proposals — the edit verb is the OPERATOR'S act of applying rows; no code path auto-applies, and the drift-card path is untouched. A price change bumps the served table version (the S02.6 `PriceTableVersion` fingerprint member's input — the freshness trigger machinery is landed; wiring the version into the composed table is the read-through's job per OQ1). The S18.3 per-model attributes (flat/metered flag, overflow_mode, region_model_gate) are **deferred with their reason** per OQ1 — their consumer (the metered-exception fold) is ratified EMPTY at v0 (G1 P7).

### B — `/api/deliverables`: reads, diff data, comments (S15.2 row; S13.1–S13.4; FC-v1 §2)

- **R7. List + detail** [S13.1; S15.8 data needs]. `GET /api/deliverables` (owner-scoped, filterable by project/state/type, bounded, newest-first) and `GET /api/deliverables/{deliverable}`: the deliverable row + **immutable numbered revisions** (n, pins, minting run/attempt, verdict_ref, created) + state + task/project refs + the follow-up lineage refs (the B6-1 taskLineage shape reused) + **the live DOORS as data** (the §39-B D3 principle — a surface must name its live verbs): accept (present when in-review + repo-backed, naming R13's route), request-revision (per OQ3 — the landed door(s) with preconditions: the open rework card id when parked; the follow-up route when finished), follow-up (the landed B6-2A route), preview (R16's routes). Member sees own; operator sees all (the landed read scoping); 404-before-403.
- **R8. Diff data for react-diff-view** [S13.2; FC-v1 §2; S15.8]. `GET /api/deliverables/{deliverable}/compare?old=N&new=M` routes `review.Compare` and serves the `Comparison` shape VERBATIM: `line-diff` carries the host-side **unified git diff text** (the client parses with gitdiff-parser and highlights client-side — **the server ships NO highlighter, no HTML, no tokenized form**); `extracted-text-diff` (PDF + fallback, labeled), `image-pair` (per-side object refs — the S15 trio's data), `binary-cards` (metadata + changed-by-hash). Defaults: `new` = current revision, `old` = new−1 (the FC-v1 §2 round-over-round default is thereby the default READ; revision-over-revision navigation = any explicit pair, a schema capability); `old=0` = the pre-task base via the landed seam. Owner-scoped as R7.
- **R9. Placed comments** [S13.3]. `GET /api/deliverables/{deliverable}/comments?revision=N` routes `PlacedComments` — server-side re-validation on EVERY render (the landed recompute; client positions are never placement authority), serving comments + placements with the five statuses verbatim; orphan/file rows carry their kept quote + original-revision link (the synthetic-strip DATA — rendering is B6-8's). Consumed comments serve with their `consumed_at`/attempt ref/finding number (the "what did that rework receive" audit read).
- **R10. Comment create** [S13.3; 6.2; S15.2 "comment CRUD (own comments)"]. `POST /api/deliverables/{deliverable}/comments` routes `AddComment`: author = the authenticated identity (owner-attributed, 15.6); body + severity (`blocker`|`note`) + optional claimed anchor (server-validated at birth; ⚙ `review.anchor_drift_lines` inside the package) + optional suggested change; `RunID` empty ⇒ the platform-scope `review.comment` event (the landed human-surface shape). Write visibility = read visibility (the deliverable's owner; the operator — a comment is attributed to its actor either way). Severity vocabulary is closed; anything else 400.
- **R11. Comment lifecycle is S13.3's — no update, no delete** (recorded reading; OQ2 confirms). The landed 0007 triggers make a comment **immutable** ("only the one-way consumption stamp moves") and **never deleted** (P-T12-2); consumption happens ONLY through THE drain (source-scan-pinned). So S15.2's "comment CRUD" grounds to Create + Read under the S13.3 lifecycle `open → consumed` — the transport ships NO edit/delete verb, and the brief's rubric checks their ABSENCE (a route-table negative), not their presence. "Own comments" = authorship attribution (every comment carries its author), not a mutation right.
- **R12. Request bounded revision — per OQ3's disposition** [S15.2 deliverables row; S07.7; S13.9]. Both landed doors exist and are routed (carried fact above). Whatever OQ3 ratifies (doors-as-data on the detail vs a dispatching convenience verb), the machinery is CONSUMED: guidance enters as durable requester comments through `RecordGuidance` + the drain (§22), rework rounds stay ⚙-bounded (`verification.rework_rounds` — consumed inside verify, §16), and the follow-up preset path stays byte-unchanged. Nothing new drains, numbers, or consumes comments.

### C — Accept (S13.6; S02.7; S15.2 contract rules; §24)

- **R13. `GET /api/deliverables/{deliverable}/accept-card`** [S13.6 step 3: trailers "displayed on the card BEFORE accept"; S15.2 retry-safety]. Serves the accept decision data for an in-review, repo-backed deliverable: current revision (n + content pin), project + protected ref, **the rendered attribution trailers** (deterministic platform facts: engine/model derive from the revision's minting run's routing record + substrate — never model output; vendor-noreply is a structural per-lane constant with its named reason), the signing posture (secret-free), the High-tier statement, and **`payload_hash`** = `gates.CanonicalHash` over the canonical accept-card core `{deliverable_id, revision_n, content_pin, trailers}` — what the reviewer SAW is what the answer must quote. (The expect-sha/HEAD is deliberately NOT in the pin — a moved HEAD is the applies-clean/CAS path's answer, a merge card, not a stale-card 409; recorded reading.)
- **R14. `POST /api/deliverables/{deliverable}/accept` — one action, High tier** [S13.6; S15.6 High; S01.9]. Body `{payload_hash, pin, subject?, provenance?}`. Order: owner scope (the deliverable's OWNER accepts — the accept is the owner's own outward act on their own deliverable pushed with their own credentials, D9/ToS; **no operator co-approval is due** — recorded reading: the §39 platform-level derivation ("no run attribution ⇒ platform-level") is scoped to effects RESTING in `proposed` for the inbox, and an accept never rests there; S13.6 defines the complete flow without a co-approver) → **PIN step-up** (the landed `stepUp` pattern; missing PIN → `pin_required`; wrong PIN → refused + `auth.reprompt_failed` audit) → **pin check** (current canonical hash ≠ quoted → 409 `stale_payload` + the fresh card) → `Accepter.Accept`. Outcomes: accepted `{commit, effect_id, superseded, routed_runs}`; **merge card** as 200-class DATA (`MergeCard` verbatim — reason + the three options; never a silent overwrite) per OQ4's durability disposition. **Repeat semantics** [S15.2]: a POST against an already-`accepted` deliverable returns the resolved state at 200 (`applied:false`, state, the accepted revision + commit from `AcceptedRevision` + the effect result) — a phone retry can never double-fire (the deliverable-state guard inside `Accept` is the structural backstop; the transport serves the honest read-back instead of its error).
- **R15. Accept exits through the LANDED journal/broker path — and only there** [D7; S15.2 "no direct outward-effect verb"; §24]. The transport calls `Accepter.Accept` and NOTHING else: no `internal/api` code touches `internal/broker`, `internal/project`, or the journal's execute verbs for this route (the Accepter composes Propose→Approve→BeginExecute→push→Succeed|Fail itself; the class-A payload + `ReDriveApproved` crash story is landed and untouched). The merge-card OPTIONS have **no landed executor** (agent-auto-resolve = an unbuilt S07-seam rework; abort-to-new-attempt = unbuilt) — the card's options are served as data with `answerable:false`-style honesty per OQ4; wiring option verbs is NOT this packet.

### D — Preview sessions (S13.8; S15.2 "preview sessions"; §25)

- **R16. Preview verbs over the held Manager** [S13.8]. `POST /api/deliverables/{deliverable}/preview` (body: revision?; routes `Launch` with User = the authenticated identity) — **Low-tier: no journal, no approval, no PIN** (S13.8: "launching one is a Low-tier, reversible action"); every type answers a defined `Session` state as DATA (no-preview / requires-container-tier / unavailable / at-capacity are honest 200s, never errors). `POST /api/deliverables/{deliverable}/preview/compare` routes `LaunchComparison` (before-vs-after; single-instance honesty when nothing is accepted). `GET /api/previews` — `List()` **owner-scoped by the transport** (member: own sessions; operator: all — Session.User is the key). `POST /api/previews/{session}/stop` — session owner or operator (the §39-B budget "own + operator-any" shape; recorded reading), 404-before-403 on unknown/other-owner ids. Launch visibility = deliverable read visibility (R7's scope).
- **R17. Hermetic preview tests — NEVER the live Caddy** [§25; the standing host hazard]. Every api-level preview test composes the Manager exactly as `internal/preview`'s own harness does: `NewCaddyClient("","")` (routing disabled) or an `httptest` fake admin; in-process/file-stub ports; stub tools; fixture checkouts under `t.TempDir()`. **No test dials 127.0.0.1:8481/8482 or any routable admin endpoint** — the preview package's AST tripwire discipline extends to the new test files (assert it: the tripwire's file set or an equivalent scan covers `internal/api`'s preview tests). The `preview.started/stopped` events assert for backed sessions and their ABSENCE for non-backed dispositions.

### E — `/api/memory` (S15.2 memory row; S09; §17)

- **R18. List + detail** [S09.2/S09.5; S4.1]. `GET /api/memory` — the caller-visible set per OQ5's disposition (recommended: own entries across owned scopes = `ListOwned`, plus house-scope entries, plus project-scope entries for projects the caller owns/is member of via the registry; **the operator role bit does NOT expose other members' user-scope content** — memory is personal data, not observability; S09.6's browse surface is v1). `GET /api/memory/{entry}`: the full Entry shape — content/file ref, **provenance** (origin, origin ref, proposer model, approver + ts, version chain, file commit), **gate status** (status verbatim: proposed/active/retired/removed + tombstone), verification fields, selectors, topic key, `last_injected_at`, and open conflict edges (R21). Filterable by scope/kind/status, bounded.
- **R19. Create + edit through the S09 write gate — never around it** [S09.4 station 3; S15.2 memory row; D10]. `POST /api/memory` routes `Gate.CreateManual` (actor = the authenticated identity; origin `human_direct`); `POST /api/memory/{entry}/new-version` routes `Gate.NewVersion` (edit = supersession; in-place mutation is trigger-blocked). **Tier reading (recorded): own-store writes are Medium** — the owner acting individually; Medium demands NO PIN step-up (that is High's) and NO batch verb exists (Medium is never batched, S15.6) — the manual human-direct write IS the individual owner approval (S09.4's v0 activation row: "human-direct entries via workspace UI … live"; the operator-import-is-the-approval precedent). **House scope is operator-only (D10)** — enforced INSIDE `Gate.authorize` (`ErrOperatorRequired` → 403); the transport adds scope mapping, never a second authority implementation. Gate walls map honestly: `ErrNotHuman` → 403 (the dev identity cannot write memory), `ErrScopeDormant`/`ErrLayerForbidden` → 400 with the gate's own reason.
- **R20. Owner rights: remove + true deletion** [S09.5; S4.1; G2 Def.9]. `POST /api/memory/{entry}/remove` routes `Gate.Remove` (owner-or-operator per §17 reading 4) — the response carries the returned **influence list** (the S09.5 surface: runs/manifest joins; "offers to queue re-verification" is B6-6 UI over this data). `POST /api/memory/{entry}/delete` routes `Gate.TrueDelete` — **strictly the owner** (the gate refuses even the operator; the transport must not soften it): tombstone retained, content purged. Both 404-before-403.
- **R21. Conflicts surfaced, never silently resolved** [S09.7]. Entry detail (R18) carries open `knowledge_conflicts` edges; per OQ6's disposition a resolve verb routes `Gate.ResolveConflict` (the affected owner; conflicts are "surfaced … question card to the affected owner"). The inbox-kind integration (a ninth derived kind) is B6-6-adjacent scope per OQ6 — named, not built here unless OQ6 says otherwise.
- **R22. What stays S09-internal** (checkable negatives): `lesson_proposals` get NO route (dormant station 2 — proposals are not memory and are read into nothing); `Store.Search` gets NO route here (it is the run-scoped ENGINE tool — the assistant's history search is B6-7's over `internal/history`); `Adopt`/`Import` stay unrouted (v1 sharing surfaces / the N20 operator import ceremony — named seams, OQ5 note); no L1 write path exists (the gate refuses; no transport shim).

### F — Cross-cutting contract rules (every verb this packet adds)

- **R23. Owner-scoped server-side, fail-closed** [S15.2; S01.9]: every route behind `requireIdentity`; per-verb scope as specified; 404-before-403 on other-owner subjects; internal failure is 500, never a downgrade. **Per-verb three-way cross-owner tests** (operator / owner / other-member) in the non-tautological direction (§38: the owner's own act/read must SUCCEED and the member's own rows must be PRESENT, so an all-refusing or all-empty handler cannot pass).
- **R24. No direct outward-effect verb** [S15.2; D7]: the accept is the ONLY outward act and it exits through the landed journal→broker path (R15); settings/memory/comments/preview are control-plane-internal state acts; preview launches touch nothing outside the host (S13.8 read-only-with-respect-to-the-world).
- **R25. Retry-safety scope** [S15.2; G2 D2.1] (recorded reading): the hash-pin rule binds ANSWERABLE acts — here, the accept (R13/R14, via the ONE canonicalization). Settings set and memory/comment creates are not answerable cards; a repeated set is two audit rows converging on one value (honest), and creates are ordinary POSTs (the B6-2A follow-up-spawn precedent). No second canonicalization function may exist (source scan).
- **R26. Every mutation → event log, owner-scoped** [S15.2; queue row]: an inventory-walk test drives every new mutating route and asserts its landed canonical row — `settings.changed` (set/reset/bounds), OQ1's shape (price edits), `review.comment` (comment), `deliverable.accepted` + the journal row (accept), `preview.started/stopped` (backed sessions; ABSENCE for non-backed), `knowledge.write/remove/delete/conflict` (memory) — and **ZERO `decision.recorded` mints** (§39 OQ8: every act here has a landed canonical row; a double mint is the defect the rule exists to prevent). Inventory stays **90/5/95** unless OQ1 dispositions otherwise (then §29 same-packet mechanics + pins in lockstep).
- **R27. Escape-by-default; the content-vs-projection line** [S15.12; §30; §38 ruling (b); §38/§39 R20]: this packet serves DATA — no HTML anywhere; the preview iframe stays the ONE raw-HTML channel and rendering it is B6-8's. Deliverable content (diffs, comment bodies, revision files), settings values + audit rows, and memory entry content are **CONTENT — served unwrapped**; payload-DERIVED projection strings (if any new response lifts `run_events` payload text — none is expected: the deliverable/settings/memory families read their OWN tables) redact per value, and `redactionEdgeFiles` stays the honest enumeration (widened ONLY if such a lift exists, with the both-directions test extended).
- **R28. No new things without an OQ** [§29–§39 precedent; standing settings-tab directive]: NO new ⚙ key (**118/33, `index.go` byte-unchanged** — a test-pinned invariant); NO new dependency (`components.lock` **21**, `go.mod` unchanged); NO migration beyond OQ1's disposition (0001–0018 byte-untouched; the preview never-adds-a-migration guard moves in lockstep if OQ1 opens 0019); structural constants (page caps reuse `readPageDefault`/`readPageMax`; any new bound) carry named reasons, interim under the settings-tab directive. CONVENTIONS **§40** appended (readings/decisions; earlier sections never rewritten).
- **R29. Battery + spend** [§3; §5]: `gofmt -l internal/ cmd/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` (hermetic — no live Caddy/units/network; host git in `t.TempDir()` per §23 is sanctioned) · `go run ./tools/lockgate`. **$0** — tier-F fakes only; no paid call, no live seat, no push, no real route added to the host Caddy.

## 2. Seams — what belongs to LATER packets (leave named, never build)

| Later packet | Owns | B6-3 leaves |
|---|---|---|
| **B6-4** | the SPA scaffold; npm rail; JSON Forms/react-diff-view INSTALLED | this packet emits the artifacts they consume (UISchema, unified diffs) — no React/HTML/npm anywhere |
| **B6-6** | the settings UI (renderer set, badges, bounds editing UX) + inbox UI; the re-verification-queue offer over R20's influence data; conflict-card UI | R1–R6 serve everything it renders; SHOW/HIDE rules stay structurally available |
| **B6-7** | the assistant (history layers; open-SQL wire semantics N13) | `Store.Search`/history untouched |
| **B6-8** | review surfaces + try-it UI + accept UI (dual-iframe render; merge-card render; synthetic strip; gutter hooks) | R7–R17 serve the data + verbs; the ONE raw-HTML channel stays unrendered |
| **v1 / post-B6** | memory sharing/browse/adopt UI (S09.4); L1/stations 1–2/5; the S18.3 per-model attributes surface (per OQ1); merge-card option executors (agent-auto-resolve rework); metered-exception activation | honest absences named in code |
| **Not this packet** | any `internal/history`/`internal/redact`/SSE-frame change; the drain/minting/verify machinery; BENCH-REG numbers; `registered.go`; broker/project internals | — |

## 3. ⚙ settings consumed (by exact dotted key; none invented)

- **Consumed through landed packages (unchanged call sites):** `review.anchor_drift_lines` (inside `AddComment`/`PlacedComments`), `preview.idle_stop` + `preview.max_concurrent` (inside the Manager), `effects.approval_expiry` (gates-owned; applies to the accept effect at `BeginExecute`), `freshness.max_age` (the sibling-accept producer inside the Accepter), `obs.sse_keepalive` (SSE).
- **Newly read in `internal/api`/`internal/settings` sibling files:** none by dotted key beyond the registry's OWN bulk reads (R1 reads every declaration through exported registry verbs — that is the surface's job, not a ⚙ consumption in the §11 sense).
- **Referenced-not-consumed:** `verification.rework_rounds` (verify-owned; bounds the OQ3 path), `state.event_payload_cap` (eventlog's own gate — R26 events are refs-not-blobs), `frontend.dependency_pass_interval` (B6-4's).

## 4. Files expected to change

- `internal/settings/` — the UISchema emitter (new sibling file) + a bulk effective-values/bounds exported read; **`index.go` BYTE-UNCHANGED**; tests (completeness, UISchema↔Decls lockstep, values read).
- `internal/api/` — new content-family handler files (settings, deliverables+accept, preview, memory); `api.go` (routes + new held Config seams: the registry, `*memory.Store`/`*memory.Gate`, per OQ1 the price store; `Accept`/`Preview` flip held→routed); per-verb test batteries; the route-table/negative scans extended.
- `internal/metering/` — per OQ1 only (the price-row store + table composition read-through).
- `internal/shell/` — composition: the new Config seams wired; per OQ1 the price read-through.
- `internal/storage/migrations/0019…` — per OQ1 ONLY; 0001–0018 byte-untouched; the preview migration guard moves in lockstep.
- `internal/eventlog/contract.go` — ONLY if OQ1 dispositions a new/co-produced type (§29 same-packet mechanics).
- `P3/CONVENTIONS.md` — §40 appended.
- Expected NOT to change: `internal/review` (consumed as-is; its verbs suffice), `internal/accept`, `internal/preview` (unless a narrow exported read is genuinely missing — a REPORT, not a silent widening), `internal/memory` internals, `internal/broker`, `internal/project`, `internal/history`, `internal/redact`, `internal/benchmark/registered.go`, `internal/watchlist`, `components.lock`, `go.mod`, `Spec/*`, `P3/STATE.md`.

## 5. Adopted components

**Expected: none touched, none added.** Stdlib + landed packages only. JSON Forms / react-diff-view are B6-4 npm adoptions — this packet only emits what they will consume. A new dependency is an OPEN QUESTION for the coordinator (S16.4 rail + operator ratification), never an executor decision.

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format, no push, never edit Spec/STATE/Docs/Research, never read `*-api-key.txt`.
- **§6** — settings discipline: declare-once; one-tx write {cell + audit + event}; actors operator/automation; bounds operator-owned; reset = row delete; the 118/33 tally test-pinned.
- **§7/§9** — unversioned API; every new route session-required, fail-closed; step-up = verify-at-act (`VerifyPIN`, purpose-tagged), NO stored elevation; dev identity never steps up and never writes memory.
- **§11** — metering is a READ MODEL; money is read, never computed in api (§38 R8 scan extends over every new money-shaped response — the price table serves stored rows verbatim); D5: no dollar figure on a flat lane invents itself.
- **§16** — verify-ask answer semantics untouched (`revise_with_guidance` stays the landed path OQ3 routes into).
- **§17** — memory capability walls: writes on the Gate only, actor must hold a users row, house = operator (D10), no L1 writer, worker-overlay dormant; the api import is sanctioned (the wall bars engine-facing packages).
- **§22** — review schema invariants: revisions immutable, comments immutable + never dropped, THE drain is the one consumer, `EscapeFirst` recorded, `review.anchor_drift_lines` read per port.
- **§23** — host git hermetic per-invocation (accept tests: real git in `t.TempDir()`, file transport only).
- **§24** — the accept flow's landed shape: merge-queue depth-1, trailers structural, class-A effect self-sufficient, lease rejection ⇒ merge card never a new-sha retry, signing structural per-user; the broker is the guardrail (P-T12-1 test untouched).
- **§25** — preview walls: read-only consumer, zero mutation, hermetic tests, live Caddy NEVER dialed, Low-tier, no durable preview state.
- **§29** — declare-once event mechanics; new/co-produced type = same-packet contract+producer entries; a new family = S00.9 amendment (STOP).
- **§30/§38/§39** — owner-scope one shape; 404-before-403; store-raw/serve-redacted at the ENUMERATED edge; content-vs-projection line (ruling (b)); no percent/fraction/ETA field in any new shape (scan extended); the ONE canonicalization; resolved-first-then-pin retry semantics; `answerable`-style honesty on served doors (D3/D9 precedents).
- Cross-phase (the 35 D6-ratified readings, binding precedent): derive-from-log/no side stores; NO tickers; cards/doors derive bounded; proposals never auto-apply (the price surface's spine); money read never computed; model output is untrusted input (no model-authored string names a record, route, or verb subject — trailer inputs are platform facts); refusal never repair; every refusal audited where an audit row exists.
- **Tests hermetic + $0**: never the live Caddy admin API/config (8481/8482) or production units; tier-F fakes; no paid call, no live seat, no real push, no real route on the host.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. `GET /api/settings`: JSON Schema + UISchema + values + effective bounds + restart/auto/per-user/dormant/help served; **every one of the 118 declared keys appears in the values block AND exactly once in the UISchema** (completeness test vs `Decls()`); `index.go` byte-unchanged.
2. UISchema: valid JSON Forms core vocabulary (Categorization/Category/Control refs resolve against the emitted schema — a resolution test walks every `scope`); no fabricated rule (scan + the recorded reason); stable across two emissions (byte-identical).
3. Settings writes: operator set/reset/bounds succeed and land {cell + `settings_events` + `settings.changed`} in ONE tx (the landed registry tests extend to the HTTP path); member write → 403; out-of-clamp → 400 with the registry message; unknown key → 404; reset deletes the override and the next read serves the default; per-user key set `for_user` works and a non-per-user key refuses it; **live-apply**: a set is visible on the next effective read with no restart.
4. Per-setting audit history: newest-first, bounded, exactly the `settings_events` columns; 404 on an undeclared key.
5. Registered-values block: served with the S18.4 R9 marker string verbatim through the seam; nil Benchmark ⇒ honest absence; **no registered number literal in `internal/api`** (source scan with probe).
6. Price surface per OQ1: reads serve stored rows verbatim (empty table ⇒ honest empty + reason); operator edit lands per the dispositioned audit shape; member edit → 403; **no code path applies a drift proposal automatically** (the §34 assertion re-run over the new surface); the composed metering table reflects an edit per the dispositioned read-through with UNPRICED honesty intact for uncovered rows.
7. Deliverables list/detail: three-way cross-owner (operator both / owner theirs PRESENT / other member zero + 404-before-403); revisions serve immutably numbered with pins + verdict refs; the DOORS block names live routes with truthful preconditions (in-review vs accepted fixtures both asserted — the D3/D9 agreement direction: no dead door, no missing door).
8. Compare: `line-diff` serves the unified git diff text for a two-revision fixture and the ROUND default (old = new−1) without params; `old=0` serves the base (empty-base honesty when no topology); PDF/image/binary fixtures serve their surfaces + labels verbatim; unknown type serves the labeled fallback — NO refusal branch (every-type walk).
9. Comments: create validates severity + anchors server-side (a claimed anchor at a wrong line lands `drifted`/`file` per the ladder, never trusted); placements recompute per render and agree with stored rows; orphan keeps quote + link and still lists; consumed comments carry the batch stamp; **no update/delete route exists** (route-table negative) and a raw-SQL update attempt still aborts (the landed trigger test stands).
10. Accept: GET card serves trailers + pin; POST without PIN → `pin_required`; wrong PIN → refused + audit; stale pin → 409 + fresh card, NOTHING fired (side-effect probe: no journal row, no push); clean accept → commit + effect succeeded + `deliverable.accepted` + review state accepted + sibling fire (fake pusher asserts CAS args + `Authorized`); collision fixture → 200 merge card with the three options, deliverable still in-review; **repeat POST → 200 resolved read-back, no second effect** (double-fire probe); lease-rejection fixture → merge card, effect failed, never a new-sha retry; cross-owner: another member 404/403, **the operator cannot accept another owner's deliverable** (authorship, not visibility — the §39-C D5 line).
11. Accept negatives: `internal/api` never imports `internal/broker`/`internal/project` (import scan); non-repo-backed revision → honest 400 naming the reason; the P-T12-1 broker refusal test and the accept package suite pass byte-unchanged.
12. Preview: launch serves a defined state for EVERY disposition fixture (live-static, no-preview, unavailable, at-capacity — each a 200 with reason); backed launch emits `preview.started` and stop emits `preview.stopped` + releases (the manager's own asserts); non-backed emits NOTHING (absence assert); list is owner-scoped three-way; stop by other member → 404/403, by operator → allowed; comparison serves before+after (and single-instance honesty); **no api test dials a routable admin endpoint** (tripwire/scan extended) and the whole battery runs with routing disabled or `httptest` only.
13. Memory: list/detail per OQ5's ratified visibility (three-way: owner sees own; operator does NOT see a member's user-scope content unless OQ5 says otherwise; house entries visible per disposition); create/new-version land through the Gate with `knowledge.write` events; house create by member → 403 (the gate's own wall, surfaced); worker-overlay/L1 → 400 with the gate reason; dev identity → 403 (`ErrNotHuman`); remove returns the influence list; true-delete by non-owner INCLUDING the operator → refused; tombstone read serves honestly; conflict edges serve on detail (+ resolve per OQ6); **no route exists for proposals/adopt/import/knowledge-search** (route-table negative).
14. Every-mutation walk: each new mutating route asserts its named canonical event row and ZERO `decision.recorded` mints (inventory stays 90/5/95 unless OQ1 moved it — then pins in lockstep).
15. Cross-cutting scans extended with probes: no money computed; no percent/fraction/ETA; one canonicalization (`gates.CanonicalHash` the only hasher of answerable pins); `redactionEdgeFiles` enumeration honest in both directions; route-table review: no direct outward-effect verb, no `/v1`.
16. Counters: ⚙ **118/33** (`index.go` byte-unchanged); `components.lock` **21**; `go.mod`/`go.sum` unchanged; migrations **0001–0018 byte-untouched** (+0019 per OQ1 only, with the preview guard moved in lockstep); CONVENTIONS §40 appended, §1–§39 untouched; `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.
17. **Full battery green** (R29) and **$0 spent** (tier-F fakes; hermetic git only; no live organ, seat, push, or Caddy).

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — the price table needs a durable home that does not exist.** S15.2 binds "price-table edits" to this packet; S10.3/S18.3 define the row shape and refresh-as-proposal; S01.10 says "its own owner-attributed table under the same audit pattern"; but the landed `EffectiveDatedTable` is in-memory and composed EMPTY (the §11-ratified v0 posture), with loader/overlay/persistence recorded as "S10.3's metering follow-up" (§34). RECOMMENDED: (a) **migration 0019: `price_rows`** (the S10.3 fields {model, lane, unit prices JSON, effective_from, verified_on, source} + row identity + created_by/ts; append-mostly — a "change" is a new effective-dated row, past rows immutable by trigger; owner-attributed `user_id`), store verbs in `internal/metering` (the S10 owner), **read-through at the shell**: the composed table builds FROM stored rows at boot + re-composes on change (live-apply; version = a deterministic digest of the row set, feeding the landed `PriceTableVersion` fingerprint member); **ships EMPTY** (no seed — the B1-gate UNPRICED posture stands; seeding from the vendored genai-prices data is a separate operator act the surface now makes possible); edit authority **operator-only** (13.4; the settings-write reading); **audit = a `settings_events` row (key = the data-surface name) + `settings.changed`** — the S01.10 "same audit pattern" letter with zero new event types (else: a new minted type via §29 same-packet mechanics — say so). **The S18.3 per-model attributes (flat/metered flag, overflow_mode, region_model_gate) are DEFERRED with the reason recorded**: their consumer (the metered-exception fold / S03.6 machinery) is ratified EMPTY/absent at v0 (G1 P7), and a flag with no consumer fakes a capability. Alternatives: (b) defer the whole price surface (REJECTED: contradicts the ratified queue row); (c) rows in `internal/api` without metering read-through (REJECTED: two price truths). If (a) lands, the mechanical companions move in lockstep (the §36/§37 precedent): the two `user_version == 18` exact pins (`internal/evals`, `internal/conformance`) → 19, and the preview never-adds-a-migration guard gains the 0019 line. If (a)'s audit shape or the trigger posture feels wrong, dispose before the executor launches.
- **OQ2 — "comment CRUD" vs landed immutability.** S15.2 says "comment CRUD (own comments)"; S13.3 + migration 0007 make comments immutable and never-deleted (P-T12-2), with `open → consumed` the only move, via THE drain alone. RECOMMENDED: R11's reading — CRUD = **C**reate + **R**ead; **no Update/Delete verb ships**, and the rubric asserts their ABSENCE. S13 is the behavior-normative owner (S15.2's own Data-owner column says "S13"). Alternative: an "edit = supersede-with-new-comment" convenience (a fresh comment + no link — the schema has no supersession member) — REJECTED as inventing schema semantics; a person simply adds another comment.
- **OQ3 — "request bounded revision": which door, or a dispatcher?** Both landed doors are routed (carried fact). RECOMMENDED: **(a) doors-as-data** — the R7 detail names, per deliverable state, the LIVE route + preconditions (parked-on-rework-card ⇒ the open ask id + the answer route with verb `revise_with_guidance`; accepted/finished ⇒ the follow-up route with preset `revision`); NO new verb — the S15.2 row's verb is satisfied by the landed machinery being reachable and NAMED (the §39-B D3 honesty rule), and a new endpoint would be a second door to the same act (the double-mint shape). Alternative (b): a convenience `POST /api/deliverables/{id}/revise` that dispatches by state into the SAME landed paths (one more route, zero new machinery; adds payload-hash pinning if treated as answerable). If (b), specify whether it must quote the parked card's pin (recommended yes — it answers a card). Alternative (c): scope it to the follow-up path alone (the queue row's read-first leans this way) — then the in-review door is B6-8's UI concern over the approvals family. Dispose explicitly; the rubric's item 7 tracks the choice.
- **OQ4 — merge-card durability.** S13.6 calls the collision "a reviewable merge card"; the landed `MergeCard` is a RETURNED value (no durable home; the fresh-collision path has no journal row — only lease-rejected/second-gate collisions leave a failed effect). RECOMMENDED: **return-only at v0** — the card is the accept response's data (B6-8 renders it in place); failed accept effects stay queryable in the journal; the three options serve with `answerable:false`-style honesty (no landed executor — agent-auto-resolve is an unbuilt S07-seam rework; resolve-manually is a human act; abort-to-new-attempt is the follow-up door); a DERIVED listing (from failed accept-effect rows) is named as B6-8-adjacent scope if the coordinator wants standing visibility. Alternative: a durable card store — REJECTED (a side store; the derive-from-log precedent).
- **OQ5 — memory visibility + the unrouted verbs.** S09.6/S09.4 make sharing/browse a v1 surface; S4.1 gives the OWNER the list-everything right; the §30 operator-sees-all precedent is an OBSERVABILITY rule. RECOMMENDED: caller-visible set = own entries (all scopes owned) + house entries (house defaults address everyone; operator curates) + project entries for projects the caller owns or is invited to (registry membership); **the operator does NOT read other members' user-scope entries through this family at v0** (memory is personal content, not telemetry; the D10 role bit is house authority, not member-memory access). `Adopt`/`Import` stay unrouted (v1 browse; the N20 house import rides `CreateManual`-at-house/`Import` by the operator — if the coordinator wants Import routed for the N20 ceremony, it is a one-line addition to R19, operator-only, origin `imported`). Dispose explicitly — this reading diverges from the observability precedent on purpose.
- **OQ6 — conflict cards: family surface now, inbox kind later?** `knowledge_conflicts` has no inbox presence (landed 8 kinds). RECOMMENDED: R21's minimal shape NOW (edges on detail + `POST /api/memory/conflicts/{id}/resolve` routing the landed `Gate.ResolveConflict`, affected-owner-scoped), and the ninth derived inbox kind NAMED as B6-6-adjacent scope (the inbox is where "surfaced" ultimately lives, S09.7 — but the memory family is where the data already is). Alternative: build the inbox kind here (grows the projection + approvals surface this packet otherwise doesn't touch).
- **OQ7 — event dispositions.** RECOMMENDED: **zero new types, zero co-productions** — every act has a landed canonical row (R26's walk), with the ONE possible exception being OQ1's price-edit audit (recommended: the `settings.changed` pattern). If the executor finds `settings.changed`'s payload contract genuinely unfit for a row-keyed data surface, that is a REPORT (a possible new type under §29 same-packet mechanics, 95→96 with pins in lockstep) — never a silent stretch.
- **OQ8 — the settings-write authority reading.** R4 grounds writes as operator-only because the registry's actor model admits no member kind (a structural fact) and S18.1 says "operator-editable". A member therefore cannot edit even a per-user key's own override (the operator administers it via `ForUser`). If the coordinator wants member self-service on `PerUser` keys, that is a registry actor-model change (an `internal/settings` widening with its own audit semantics) — dispose explicitly; the recommendation is the structural status quo.

## 9. SCOPE VERDICT

**NOT one opus sitting.** Three families, each a coherent surface with its own substrate, plus a likely migration + metering read-through (OQ1) and the accept/step-up choreography. The B6-2 A/B/C precedent applies cleanly — RECOMMENDED split at the family seams (one brief serves all parts; each independently landable, testable, and gate-presentable):

- **A — `/api/settings`** (R1–R6 + their slice of F; OQ1/OQ7/OQ8): the UISchema emitter + registry reads/writes/history + the registered-values block + the price surface with its migration/read-through. Feeds B6-6.
- **B — `/api/deliverables` + accept + preview** (R7–R17 + slice of F; OQ2/OQ3/OQ4): the largest part — reads/diff/comments, the High-tier accept choreography, the preview verbs with the hermetic-Caddy obligation. Feeds B6-8.
- **C — `/api/memory`** (R18–R22 + slice of F; OQ5/OQ6): the smallest — gate-routed writes + scoped reads + conflicts.

Order **A → B → C** (any order is safe — the families share only the F patterns; migration numbering follows landing order). If the coordinator wants exactly two sittings: **B alone, then A+C**. Cross-cutting F binds every part; a stop at any seam is a success mode, not a failure.
