# P3-UI-7 — D8 pass III: chat + review surfaces, the assistant-card rework, the self-teaching layer app-wide, background-run etiquette, the seeded demo world, and the cascade-layer fix

**Grounded 2026-08-05.** Seventh and FINAL packet of the UI batch; after it comes the exit sequence (full battery + bundle report → operator click-through on the seeded world → D6 upgrade ceremony → bring-up). Contract: `Spec/core-architecture-v1.md` (drafts canonical) — §S15.7 (drafts/S15-frontend-api.md:98–107), §S15.8 (:109–119), §S15.12 (:137–142), §S15.2's chat row (:35). Design authority: the operator-APPROVED `P3/design/design-approach-proposal.md` — §3 (the self-teaching layer :44, the Odysseus-reimplemented background-run etiquette :42, "Already binding, unchanged" :46) and §4 (the seeded exit walk :59), subordinate to the spec on all behavior. Mandate: B6 gate record `P3/gates/B6-report.md` §9 **A-2, ASSISTANT half** (:137 — the settings half landed at UI-6, §52) + A-1's teaching duty (:135). Scope source: the `P3/STATE.md` UI-batch queue row for P3-UI-7 (**:152 — READ IT VERBATIM; amended 2026-08-05 with the cascade-layer requirement**, UI-6 adjudication B) under the batch preamble (:142). Idiom inheritance: CONVENTIONS §47 (kit/tokens/motion/D5; ToastProvider :1539), §48 (confirm/outcome idioms; the OQ2 resume-lives-in-the-Inbox note :1563), §49–§50 (held-vs-busy; draft preservation; C-1), §51 (SurfaceHead/StallBanner/EmptyState; drain D1's pending-read gate :1731), **§52 (the UI-6 record; §52-B the cascade mechanism record :1777–1780; the seam list :1791)**. This brief is self-contained: the executor and the evaluation see no chat history. The CONVENTIONS record this packet writes is **§53**.

**THE FRAME.** Seven named deliverables, nothing else: (1) the **cascade-layer fix** (a sanctioned tree-wide RENDERING change — the one behavior-adjacent move, ratified in the row amendment; it changes zero markup and zero contracts); (2) the **chat + review restyle** with every carried behavior spec intact; (3) the **assistant disambiguation-card rework** in operator words (the batch preamble :142 names it a sanctioned recorded-finding exception — it changes WORDS and grouping presentation, not contracts); (4) the **self-teaching layer app-wide** (interactive do-the-thing tour, one-shot first-use hints, teaching empty states on this packet's surfaces); (5) **background-run etiquette** (the landed parked-stream behavior made visible: sidebar dots + completion toasts) with **ToastProvider's production mount**; (6) the **dev-only seeded demo world** derived from the golden-fixture producers; (7) the full exit battery + bundle. No new route, no new event type, no new REST read, no scoping change, no new dependency, **zero production `.go` change** (the seed is test-package + script — R24), no new api.ts member. The sanctioned-narrow-change ledger stands at three (§48 pause block · §49 opt_in block · §52 task_id door) — **this packet adds NONE without a coordinator OQ sanction**; an honest render found impossible without one is a STOP-and-flag.

**THE WALLS — state them back before building, police them at evaluation** (byte-verify against the pre-packet commit, HEAD `8595174` at grounding):

1. **Byte-frozen:** `routes.ts` · `router.tsx` · `live.ts` · `events.ts` · `api.ts` · `controls.tsx` · `kanban.ts` · `chatFacts.ts` · `chatRuntime.ts` (the two chat tables and the adapter are FROZEN contracts — the restyle renders them, it does not edit them) · `Login.tsx` · `Inbox.tsx` · `Settings.tsx` · `settingsForm.tsx` · `PushDevices.tsx` · `MissionControl.tsx` · `Filters.tsx` · `Board.tsx` · `TaskDetail.tsx` · `Fleet.tsx` · `Workforce.tsx` · `ConnectionState.tsx` · `Stub.tsx` · `push.ts` · `web/public/sw.js` · **every `web/src/ui/*` including `ui/Toast.tsx`** (the mount consumes it; a kit edit is a STOP-and-flag) **and `ui/Timestamp.tsx`** (byte-frozen UNLESS a named requirement legitimately opens it — none here does; if one emerges, the UI-4 carried cross-note rides: its :18–20 doc still says "applying `live` … is UI-4's row", executed since — true it dated) · **every production `.go` file** · `Spec/` entirely · **every `web/src/fixtures/api/*.json`** (the 52-file golden set is untouched; `cursor` stays **89** with zero drift on a regeneration re-run — the seed lives OUTSIDE the golden set, R23) · `package.json`/`package-lock.json`/`components.lock`/`go.mod`/`go.sum` · `escape-scan.test.ts` (allowlist EMPTY stands; the preview iframe is the one raw-HTML channel — its `src`-not-`srcdoc` posture is the landed hardening, escape-scan.test.ts:26–53) · `sw.test.ts`. **`shell.test.tsx`**: prefer UNOPENED (new test files carry the tour/etiquette legs); this packet MAY legitimately open it for composition legs — **if it opens, the C-1/login pins (:385/:407 …) still pass unmodified and the :401–402 UI-4-scoped comment ("Login.tsx is byte-unchanged…") is trued, dated** (§52 OQ3's standing instruction — its point holds, its byte-unchanged clause expired at UI-6).
2. **Diff-censused, exactly two files.** **`App.tsx`** — named hunks only: (a) the `ToastProvider` wrapper at the shell root; (b) the etiquette hook + `data-dot` render inside the existing `navSections()` loop (:130–143); (c) the tour mount + its one header affordance. The session machinery (:79–116, :293–320), `useConnection` (:328–355), `navGroupOf`/`navIcons` tables (:42–62), every route arm (:184–263), and the C-1 arms (:104–116, :165–179) are byte-region untouched. **`History.tsx`** — ONLY the `AnswerView` disambiguation branch (:283–346; the card block :305–322) for R7; `HistoryPanel` and everything else byte-region untouched. Discovering a third file mid-build is a STOP-and-flag.
3. **Every carried Chat/review behavior spec is FROZEN** (proposal §3 :46 "Already binding, unchanged") — R8/R9's per-surface enumerations are the checklist; the 668 landed tests pass with markup-coupled assertions updated honestly by ADDITION (counts never drop silently).

Also binding, carried: bundled assets only, no CDN, no runtime fetch · AGPL: **Odysseus is ideas-only, forever — the tour, spotlight, hints, dots and toasts are REIMPLEMENTED ideas, zero Odysseus code/CSS/markup/assets; an explicit §53 attestation item** · every ⚙ registry-routed (this packet consumes NONE and creates NONE — R20) · Layer-1 catalog stays **50/50** (needing a canned entry = STOP-and-flag) · no-ticker, `<time>` two-file pin, phone-first 375px with both-way probes · FC-v1 picks untouched at pins (@assistant-ui/react **0.14.27**, react-diff-view **3.3.3** — FC-v1 §2/§3; nothing in this packet changes a pin, a runtime choice, or the ExternalStoreRuntime boundary).

---

## 1. Where the packet starts (verified ground, 2026-08-05)

UI batch 6/7. Counters at the UI-6 landing, re-verified this grounding where marked: vitest **668/28 (re-run green this grounding)** · Go **45 pkgs / 14 sanctioned skips** · lockgate **38/574** · bundle **JS 1137.35 kB / gzip 342.29 · CSS 57.23 / 11.91 · index.html 1.80 · 15 fonts / 417.30** (raw byte sizes reproduced this grounding from a fresh build: 1,137,357 / 57,237 / 1,802 / 15 woff assets; gzip figures are the Vite reporter's — the entry battery must reproduce them by the landed method) · golden `cursor` **89** · ⚙ **118/33** · catalog 50/50 · escape allowlist EMPTY · migrations 0001–0021. Assertion baselines, measured this grounding by the landed method (`grep -c 'expect('`): **chat 224 · deliverable 284 · memory 179 · shell 68 · sweep 88 · escape-scan 15 · ui/timestamp 18 · ui/kit 61 · responsive 57 · design 53 · scans 29 · inbox 290 · settings 142 · push 92 · login 26**. `index.css` is 1,681 lines; this packet's sections: **the assistant :1001–1200 · the S15.8 review surface :1201–1348**; the workforce section (:1349–1659) is **UI-5's kept layout contract, NOT this packet's** (§51's shed already ran; §51 :1703's dated ledger cross-note moved workforce UI-7 → UI-5). The preflight-compatibility block is :250–285 (four rule-groups under the :246 comment). **The cascade ground, reproduced on this grounding's own fresh build:** `@layer utilities{…}` spans bytes 9620–30253 of the built sheet and contains `.m-0{`; the owned `p,dl,dd,ul,ol,blockquote{…}` sits at 34150 and the owned `input,select,button{…}` chrome at 39507, both outside every layer — §52-B byte-exact.

## 2. Requirements A — the cascade-layer fix (do this FIRST; everything else renders on top of it)

- **R1 — the fix class (row amendment, verbatim ground): the owned sheet's rules enter a cascade layer ordered below Tailwind's `utilities`, so kit-primitive utilities win as authored.** Mechanism (recommended, OQ3): wrap every owned rule below the token/`@theme` wiring block — the preflight-compat block, the content/shared section, and every per-surface section — in **`@layer base { … }`** (Tailwind v4's own `@layer theme, base, components, utilities` order already places `base` below `utilities`); a named custom layer inserted before `utilities` is the admissible alternative. The row's other arm — "the global element rules are scoped" — is **NOT available as the whole fix**: the `form`/`label`/`input,select,button` chrome (index.css:603–631) still serves every frozen surface's raw forms (§52 :1778's own record), and those surfaces do not open this packet. Token declarations on `:root` and the `@keyframes`/media machinery move only as the mechanism requires; **no owned rule is deleted or renamed by the fix itself** — deletion is R3's separate consumer-verified re-audit.
- **R2 — what the fix un-suppresses, stated in advance so the change is expected rather than discovered.** Per §52-B (:1777, drain :1796): the unlayered `input,select,button { border; background; color; width:100% }` rule currently beats every `@layer utilities` rule regardless of specificity, suppressing the kit `Button`'s border/background-gradient/colour/font/padding and imposing `width:100%` across the **11 kit-Button consumer files** (verified this grounding: `controls.tsx`, `Deliverable.tsx`, `Filters.tsx`, `Fleet.tsx`, `History.tsx`, `Inbox.tsx`, `Login.tsx`, `Memory.tsx`, `PushDevices.tsx`, `Settings.tsx`, `TaskDetail.tsx`). After the fix, every utility anywhere in shipped markup that an owned element rule was beating wins as authored (the kit Button's variants, `m-0`-class utilities on headings/paragraphs inside kit primitives — e.g. `ui/Toast.tsx`'s `m-0` Title). **The frozen-surface walls under this change mean: SOURCE stays byte-frozen; the RENDERED LOOK of every surface changes by design.** Record that sentence in §53 verbatim-equivalent. Expectation: **all 668 landed tests pass unmodified through the fix** — jsdom computes no cascade, so a landed test that moves on the fix commit is itself a finding.
- **R3 — the §52-B kept-for-cascade re-audit, rule by rule.** The six entries kept at UI-6 *because the utility form could not win* (§52 :1778): `.criteria label`/`.verdict-form label`/`.disposition-form label` · `.batch-candidates`/`.batch-outcomes` (`list-style`) · `.cat-tabs button` (`width:auto`) · `.badges` · `.setting-edit` · `.push-device label`. The fix dissolves their stated keep-reason; re-audit each with the §51/§52 grep method. **Expected outcome, stated honestly: most STAY** — UI-6 removed every inert utility it had added (§52 :1777 last sentence), so the frozen markup does not carry the utility twins, and after the fix these owned rules are still the only styling doing the work. Each gets a re-dated keep-reason ("kept: still the sole styling; its surface is frozen") or, where a compiled utility twin genuinely exists in the built sheet and markup, a consumer-verified shed. **Editing a frozen `.tsx` to add a utility twin is a wall break, not a fix.**
- **R4 — the dist-CSS cascade-relationship test (the row requires it; §52-B is the model).** A standing test that reads the **BUILT** sheet (the Go suite already runs over the real dist — that is the natural home; a vitest reading the built artifact is admissible if it reads the real bytes) and asserts the RELATIONSHIP, never byte offsets (§52 D4a's lesson — offsets did not reproduce): the `@layer utilities{` block exists and contains a known utility marker; the owned markers (`input,select,button{` and `p,dl,dd,`) sit **inside a layer block whose name the sheet's layer order places before `utilities`** (or, under the scoping alternative, match the scoped form) — i.e. no owned rule remains unlayered below the token block. **Bite-proven in both directions: the test FAILS against the pre-fix dist and passes against the post-fix dist** (run it once against the pre-packet build to prove the bite; record the run).
- **R5 — what jsdom CANNOT see, verified in the BUILT artifact by the executor.** jsdom computes no cascade — §52-B is now the PROVEN highest-yield hazard class. The executor must build, serve the real dist (`./P3/gates/B6-clickthrough.sh` or `vite preview`), and visually verify, per surface, recording per-surface results in the landing report: (a) kit `Button` variants render palette/gradient/padding and natural width on all 11 consumer files' surfaces (the §47 :1535 variant vocabulary); (b) native, utility-less form controls KEEP the owned chrome (an unlayered→layered move only changes winners where a utility competes — elements with no competing utility must look identical); (c) headings/lists/paragraphs on frozen surfaces keep their rhythm where no utility competes; (d) the restyled chat + review surfaces render as authored. The operator's own visual spot-check rides the seeded exit walk (row requirement) — the executor's check is the packet-level gate, not a substitute for it.
- **R6 — bundle accounting for the fix, itemized.** The layer restructure changes CSS bytes with zero rule changes — measure the fix commit ALONE (the A-seam ordering gives the natural measure point) so §53 can itemize three CSS movements separately: the layer-wrap delta, the chat/review shed (−), the newly compiled utilities (+). No net-only number.

## 3. Requirements B — the chat + review restyle, and the A-2 assistant-card rework

- **R7 — the assistant card, reworked in operator words (A-2's assistant half, B6-report:137; proposal §3 :44).** The seam is `AnswerView`'s disambiguation branch (History.tsx:305–322) — **one renderer, two consumers** (MissionControl.tsx:4 via HistoryPanel; Chat.tsx:36), so the rework lands once and both surfaces' landed pins keep passing. What changes: **(a) grouped rendering** — the choices group by their served `category` (api.ts:202–206 — the field is on the wire; grouping is presentation) using the `navSections()` algorithm class (App.tsx:270–281): consecutive runs over served order, every choice rendered exactly once, an empty category in its own unlabelled run — a test asserts the flattened grouped list equals the served list in order and count; **(b) the plain-language layer** — client-authored operator words saying what the card IS and what to do: the assistant could not turn the sentence into one of the platform's own questions, nothing was run, and picking a listed question asks exactly that question; **(c) the three-verb contract in operator words where a person looks** — the chat surface head (Chat.tsx:117–123) reworked to state the A-2 contract (ask about the platform · hand work to intake as a task · exchange files) — the current line omits file exchange; the SurfaceHead treatment (R11) carries it. **What does NOT change (landed pins, §50 :1637–1638):** the served `question`, `reason`, and every choice's `query`/`category`/`description` render **VERBATIM**; a choice **SELECTS and never fires** (the History panel's landed choice contract); the card renders as an ANSWER, never through the error banner; the escalation act (Chat.tsx:580–601) and its layer<2 gate are untouched. **The honest degraded-mode sentence (the hard truth constraint):** the wire carries NO machine code on the card — `Disambiguation` is `{question, reason, choices[]}` (api.ts:202–206) — and the store serves cards for at least seven distinct reason classes (internal/history/layer1.go:155 not-wired, :178/:190 parse, :202/:210 below-threshold, :232 unknown intent, :256/:264 slot-fill). Classifying by prose is banned (§38; chatFacts.ts:143–144 records the rule). Therefore **no client-authored sentence may claim WHICH cause produced the card** — the proposal's illustrative sentence ("no local model is wired, so I can't read sentences yet") cannot ship as unconditional copy; the served `reason` is the platform's own statement of cause and renders verbatim beside the client words. Overclaim negatives asserted: the client copy contains no "not wired", no "no local model", no claim that anything ran, and no free-generation promise (A-2: **no free-generation chat exists by S15.7/S14.10 design**). See OQ6 for the proposal-amendment note this narrowing needs.
- **R8 — chat restyle with every carried behavior spec intact, enumerated (assert each; the landed test is the pin).** Restyle `Chat.tsx` in the approved language on kit primitives + utilities; `chatFacts.ts`/`chatRuntime.ts` frozen. Preserved, with the landed test named: the survives-navigation inventory as a PINNED PARTITION (chatFacts.ts:49–107; chat.test.tsx:553, :580 — all six must-survive things, draft shed); stream-survives-navigation and hard-stop-is-an-act (chatFacts.ts:40–47; chat.test.tsx:710 no-unload-verb scan, :725 stop=abandon, :693 widget-cancel same verb); the error-humanization table keyed on class/status/code NEVER prose (chatFacts.ts:146–190; chat.test.tsx:755, :786 overload arm, :812 empty-vs-transport, :848 envelope verbatim, :868 no fabricated fallback announcement, :1345 class split); the four composer verbs client-routed, no model classifies, Layer 2 never a verb (chatRuntime.ts:94–140; chat.test.tsx:199, :443 escalation-only, sweep.test.tsx:496 exemption untouched); argument gating without disabling the input (chatRuntime.ts:151–155; chat.test.tsx:501); registries served (:525); server-side sessions — create/rename/two-pressed delete (:908), honest untitled + auto-title rename via `chat.session_renamed` (:893; live.ts:140–150); file exchange — both upload gestures one verb (:947), list-as-served + delete (:977), produced-files chips as served attribution incl. the honest empty (:994; Chat.tsx:610–634); the born task + in-place intake card family (:1020, :1045, :1088 PIN-to-inbox, :1117, :1187 queue-sourced, :1206, :1221 resolved-elsewhere, :1235); typed subscription, no topic filter, frame-triggered re-read (:1144, :1261, :1278); re-snapshot honesty (:1282); hostile text (:1315); no web storage/money/progress (:1332); reload restores (:424); in-flight 409 fact (:347); assistant-cloud never wired (:228); the exchange instant with relative label (:1354). The turn-running/abandoned/empty facts (chatFacts.ts:200–231) render as landed.
- **R9 — review-surface restyle with every carried behavior spec intact, enumerated.** Restyle `Deliverable.tsx` in the approved language; the S13/S15.8 contract is untouchable. Preserved, with the landed test named (deliverable.test.tsx): doors render open-as-control/closed-as-reason (:103); request-revision both limbs, finished-limb honesty, revision preset (:123, :145, :178); **split default + unified toggle** over the one served diff source (:193, :220; proposal :50's verified `split` default), default read sends no bounds (:233), explicit pair incl. old=0 (:245); image trio + hash verdict + per-side cards + not-inline reason (:267, :299); binary cards (:311); PDF degrade + extracted-text fallback (:326, :339); served pixel-diff aid (:355); unknown surface honest (:375); **anchors/lifecycle: all five placement statuses (:393), anchored-at-placement (:420), open→consumed with [F#] + attempt (:430), finding-under-comment-schema (:447), severity meanings (:460), immutability — no update/delete affordance (:466), claimed anchor composed from the rendered hunk (:485), the server's own born status (:519)** — the ±2-line drift and orphan state are S13's served truth and the restyle may not weaken one pin; the rework door's ask/pin/guidance + stale-pin-is-a-re-read (:550, :590); **try-it dual-iframe**: backed link + multi-port picker (:629), non-backed NO iframe (:650), session list + stop + partial teardown (:670), both frame srcs compose only from the served URL + shell path with re-sync (:702, :739, :759), sandbox/no-referrer (:1104), iframe-src-only-served (:1073); **one-click accept**: card trailers byte-for-byte (:775), acceptable:false (:795), PIN same-request-then-gone (:804), collision merge card (:845), applied:false honest repeat (:901), superseded 409 (:927), stale pin once (:940), wrong PIN re-prompt (:964), authorship presentation + server authority (:979, :997); frames declared/undeclared (:1015, :1058), own-verb re-read (:1034); hostile text (:1085); every comment reachable — widget or synthetic strip (:1143, :1171); guidance-rides-answer (:1187); comments read once (:1245); parser-reject honest (:1271); door-route wall (:1290); the follow-up family (:1349–:1499). react-diff-view stays adopted-unforked with its own stylesheet (index.css:1202–1207's recorded posture).
- **R10 — Memory rides for the self-teaching layer only.** `Memory.tsx` was built in the new language at UI-3 and has no owned CSS section; its opening here is bounded to R11/R12 (SurfaceHead treatment if its header is not already the idiom, teaching empties with the D1 gate, tour/hint coverage) — every §49 behavior pin (memory.test.tsx :344–:1058 set) passes; the 375px leg (:878) survives.

## 4. Requirements C — the self-teaching layer and the background-run etiquette

- **R11 — the "what this is" line on this packet's surfaces.** Chat's header (:117–123) and Deliverable's head (:96–110 region) take the §51 `SurfaceHead` treatment (parts.tsx export); Memory per R10. Facts pinned + overclaims asserted absent per surface: chat = the three-verb contract (R7c) + conversations live on the platform; must NOT promise free-form generation or claim the assistant edits anything; deliverable = review one deliverable's revisions, compare rounds, comment, try it, accept; must NOT say *approved*/*signed off* (approval state is served per artifact) and must NOT claim acceptance is reversible. Memory's line follows its landed scope sentence (the served `visibility` sentence stays the scope authority, §49 :1604).
- **R12 — teaching empty states with the §51-D1 gate, per site.** Chat's `Empty` sites (Chat.tsx:152 no-conversation-open, :232 no-sessions, :425 nothing-said, :801 no-questions, :946 nothing-to-hand-over) and Deliverable's (:141 no-revision, :744 no-comments, :758 all-placed, :1230 no-doors + the try-it/accept absences) migrate to kit `EmptyState` (`what` + `why` that teaches what makes a row appear) **only where a teaching `why` is true and useful — a mid-form absence may honestly stay `Empty`-grade**; every migrated site is **SERVED-gated** (`data && …`) with the D1-shape negative driven through `doubles.ts`'s `pending` route (:128–133): no teaching state over a read that has not answered, `Freshness` owns the window. **Chat's session rail currently renders `sessions.data?.sessions ?? []` (Chat.tsx:126–128) — the §51-D1 class; gate it.** `Empty` itself: after this packet its consumers may reach zero — if grep proves zero, retire the export with the removal recorded in §53; if any survives, it stays with the dated note (never a dead export claimed as alive).
- **R13 — the interactive do-the-thing tour (proposal §3 :44), owned JSX, production-safe.** One owned overlay (spotlight ring + step words) mounted at the shell (App.tsx census hunk c), a per-route step registry, started ONLY by its own visible affordance (recommended: a small ghost control in the shell header) — **NEVER auto-started** (no persistence exists to remember "already shown" — an auto-start would ambush every load; see OQ1). **The verb wall (OQ2, pin it per step class):** a step's "real control" click MAY target (a) nav links and route-changing `Link`s, (b) disclosure/selection controls that fire no request or only GETs (filter-bar links, the composer verb pickers, panel-openers, `details` toggles); a step MUST NEVER target a control whose activation fires a non-GET, opens an `ActConfirm`/`Modal` whose primary act is a verb, submits any form, or sits inside a confirm dialog. **Every step's target must exist on an EMPTY database** (the tour ships to production, where the seeded world never exists — spotlight chrome, never data rows); a step whose target is absent at runtime is SKIPPED with the skip visible, never spotlighting nothing. Escape and a visible skip end the tour at any step (the kit's escape-stack discipline); spotlight motion is `motion-safe:`-gated. **Proof, both directions:** (i) a test walks EVERY registered step to completion with the full fetch log asserting **zero non-GET requests** fired by tour advancement; (ii) a structural test over the step registry asserts every step's declared target class ∈ the allowed set, with a planted verb-class step firing it; (iii) at least one route's walk advances by REAL clicks on the real controls (jsdom click on the target). No ⚙ key, no storage, no library — owned JSX only.
- **R14 — one-shot first-use hints (OQ1 decides the persistence shape).** Recommended shape: a module-scope in-memory seen-set — each hint renders once per tab session, dismissible, never covering the control it names; hints re-appear on the next full load, and that narrowing of "one-shot" is recorded as a dated deviation (the §41-B web-storage ban is absolute; no server-side per-person UI-state store exists; inventing a ⚙ key or a store is out of scope). Hint copy obeys the same truth/overclaim discipline as SurfaceHead lines; count stays small (the teaching layer is the tour + empties — hints are seasoning, not a second tour).
- **R15 — background-run etiquette on REAL landed signals only (proposal §3 :42; OQ5 pins the sets).** **Parked streams need no new mechanism** — the landed survives-navigation inventory IS the parked-stream behavior (chatFacts.ts:49–107); assert it unchanged. What this packet adds is VISIBILITY, at the shell, on the ONE existing stream: App's `useConnection` already subscribes topicless, which forces the **unfiltered relay connection** (events.ts:219–237) — every owner-scoped frame already arrives; the etiquette listens by TYPE client-side (events.ts:285), adding **no server-side change and no REST read**. **Sidebar dots** (`data-dot` on nav links, census hunk b): light when a frame of the target's OPENING set arrives while the person is NOT on that route; clear on route entry; no counts, no numbers, feed-down means no dots (the pill owns feed state). Recommended mapping (OQ5): **chat** ← `chat.turn_settled`/`chat.turn_abandoned` (live.ts:163–169); **inbox** ← the card-OPENING types only — `engine.gate_ask`, `intake.state`, `intake.delta_decision`, `watchdog.flagged`, `drift.finding`, `benchmark.alarm` (live.ts:110–135) — closing types (`decision.recorded` etc.) excluded, because a dot must not announce removals; **no memory dot** — knowledge frames never reach the addressee (live.ts:101–108), so a memory dot would be structurally incomplete: record why. **Completion toasts** through the kit's `useToast`: recommended set = the chat turn lifecycle EXACTLY — `chat.turn_settled` / `chat.turn_abandoned` arriving while not on `/chat`; copy states only the fact the TYPE name carries + a `Link` to the chat surface. `run.state_changed` gets NO toast (the frame's top-level fields are `seq/run_id/user_id/type/topics/ts` with an opaque `payload` — events.ts:16–27; the new state is not a top-level fact, frames are triggers and REST is the truth (§42), so a "finished" toast would fabricate); deliverable/artifact events get NO toast at v0 (no honest link target without reading payload); both narrowings recorded. **No client-side re-filter by user** — the relay's own scoping is the authority (a second filter is a second authority model, the §52-D1 lesson). Tour/etiquette copy that mentions parked runs POINTS at the Inbox (`hrefFor('inbox')`) where the release verb lives — §48 OQ2 (:1563): cross-link, never re-implement.
- **R16 — ToastProvider's production mount (§47 :1539; the §48/§51 seam lists both name it UI-7's).** ONE mount in App.tsx wrapping the shell tree (census hunk a) so every test mounting `<App>` inherits it; `ui/Toast.tsx` is byte-frozen — `limit={4}` with `data-limited` overflow (Toast.tsx:35–39, :76) is asserted AT THE MOUNT (drive five toasts; the fifth is hidden-not-dropped). The mount changes no landed behavior: the 668 landed tests pass; the provider's portal/viewport adds no interactive control outside the toast stack.

## 5. Requirements D — the seeded demo world (dev-only, both walls structural)

- **R17 — what the seed IS (proposal §4 :59, the row's own list).** A dev-only seed making EVERY surface walkable in the throwaway posture: the fixture-world users **through the real auth store with PINs** (apifixtures_test.go:146–160's method; the script prints who exists and the demo PIN so the walk can exercise login + the personal surfaces via the landed C-1 Sign-in affordance), tasks + runs across states, asks/approval cards, queue rows (:162–253's method), a deliverable with revisions and anchored comments (the `seedFixtureReviewSurface` producer family :634), decisions through real verbs (`driveFixtureDecisions` :1192), workforce rows (`seedFixtureOversightCards` :1043), memory entries (the §49 producers :411), chat sessions/turns where the producers reach (`driveFixtureChat` :893), and meter/budget facts **only as the real stores serve them** — declared budgets via real verbs; consumption renders as the real ledger's answer over whatever rows the producers legitimately write, else the honest absence. **No API-seam fake reaches the throwaway binary** — `fixtureMeter`/`fixturePrices`/`fixtureIntake`/`fixtureBenchmark` (apifixtures_test.go:101–119, :1510, :1657, :1937) are test-server doubles and structurally cannot be wired into `cmd/sinet`; where the fixture world used a double, the seeded world shows the real store's honest state instead, and the walk documentation says so.
- **R18 — the dev-only wall, BOTH directions, structural (the row's negative).** Recommended shape (OQ4): the seed is an **env-gated test-package entrypoint** beside the producers it derives from (the `TestWebAPIFixtures` pattern — apifixtures_test.go:20, :75: skip without the named env var; with it, open/migrate the target state dir and drive the producers). **The negative is the compile boundary:** `_test.go` files are never compiled into `go build ./cmd/sinet`, so the shipped binary cannot contain the seed AT ALL — stronger than the row's "default path", and it respects the B0-3 rule (posture is env, never a build flag — internal/shell/config.go:35–44; the compile boundary is not a posture mechanism). Asserted both ways: (+) the clickthrough script invokes it and the walk finds the world; (−) a source-scan test asserts the seed identifiers live only in `_test.go` files, and the `TestFixtureWriterIsNeverAutomated` discipline (:2312) extends to the seed's env var (CI never sets it). The positive path runs ONLY inside `P3/gates/B6-clickthrough.sh`'s own throwaway posture — the script's production-refusal preflight (:52–68: forbidden ports 8481/8482, forbidden state dirs, `unset STATE_DIRECTORY CONFIGURATION_DIRECTORY NOTIFY_SOCKET`) stays intact and the seed leg sits after it; seeding order vs first boot (who migrates the DB) is executor mechanics, but the script stays ONE command (its :17 contract) and `--clean` still removes everything.
- **R19 — fixtures posture: does ANY committed fixture change? NO.** The golden set (52 files under `web/src/fixtures/api/`) is byte-frozen; `cursor` 89; the zero-drift regeneration re-run is part of acceptance. The seed writes a THROWAWAY database at run time and no repo file. The only repo changes for the seed: the test-package entrypoint (test-only Go, the §49/§50 fixture-driving precedent), the script, and their records.

## 6. Cross-cutting requirements

- **R20 — ⚙: NONE consumed, NONE created — verify.** The tour, hints, dots, toasts and seed read no registry key and invent none; no web storage anywhere (the landed chat scan :1332 stays green; extend the same class of scan over the new tour/etiquette modules); the only new client constants are the frame-type LISTS (registered vocabulary, not numbers) and the tour's step registry (structure, not policy figures). `internal/settings/index.go` byte-unchanged at 118/33.
- **R21 — adopted components: NONE new.** Lockgate 38/574 unmoved; no tour/spotlight/toast library (the §47 :1539 posture: `sonner` and every sibling stay NOT-ADOPTED); the tour is owned JSX; FC-v1 §2/§3 picks untouched at recorded versions.
- **R22 — the CSS shed, consumer-verified rule by rule (§51/§52 method; nothing renamed), AFTER the cascade fix lands.** This packet's shed sections: **the assistant :1001–1200** (`.chat-*` rules :1007–1199 and `.chip`/`.chip-name` :1134/:1076 — Chat is `.chip`'s last consumer per §51 :1717) **except `.chat-body`**, which is a sweep phone-duty container (sweep.test.tsx:784) and KEEPS per the §51/§52 recorded-deviation precedent (sweep.test.tsx stays byte-unchanged); **the review surface :1201–1348** (`.deliverable .*`, `.diff-*`, `.comment*`, `.synthetic-strip`, `.doors`, composer/frames/accept groups) plus the content-section Deliverable-only pair `.lineage`/`.revisions` (:803/:807). Each rule sheds only with a grep-proven empty consumer list and its replacement utility **verified to compile** in the built sheet. **The shared blocks mostly STAY — this grounding's tree-wide consumer map is the evidence:** `.absent` (parts.tsx + Workforce + Chat/Deliverable) · `.error` (parts + History + Chat/Deliverable) · `.notice` (Filters + Deliverable) · `.warn-flag` (Deliverable/Filters/Fleet/Memory/MissionControl/Settings/TaskDetail) · `.surface` (nine files) · `.block`/`.table-scroll`/`.cards` (multi-consumer + sweep duty rows :778–786) · `.muted` (nineteen files incl. App) · `.owner` (parts + settingsForm) · `.money` (parts only — parts.tsx primitives' own classes stay, tension-8's stay rule). After Chat/Deliverable markup moves to utilities, re-grep every one: whatever loses its LAST consumer sheds; everything else gets the dated §53 stay-note update (§52 :1780's consumer-list discipline — a list inherited from prose is a list nobody checked). **The preflight-compatibility block (:250–285): verify its exit honestly rather than forcing it.** Its comment says it leaves with the last surface section, but frozen surfaces still render bare headings/paragraphs/lists that rely on it; if any shipped markup still needs it, it STAYS inside the new layer with a dated truth-note (+ §47 ledger cross-note) — forcing its exit would change frozen surfaces beyond the sanctioned cascade change.
- **R23 — the scans.** Tree-wide motion scan green over the new tour/etiquette markup (every `transition-`/`animate-` utility `motion-safe:`-gated); both no-ticker scans byte-unmodified — **the tour and the dots hold no timer** (advance is event-driven; a pulsing dot is CSS motion behind the gate, never a timer); `<time>` pin stays `['./parts.tsx','./ui/Timestamp.tsx']`; no view formats a date; escape-scan byte-frozen and green (the tour overlay renders TEXT; the review preview stays the one raw-HTML channel); the money/percentage/ETA scans green (no etiquette copy invents a figure).
- **R24 — zero production `.go` change; test-only Go per the §49/§50 precedent** (the seed entrypoint + the dist cascade test if Go-homed). If either genuinely cannot be test-only, STOP-and-flag with the evidence — the ledger adds nothing silently.
- **R25 — bundle, measured + itemized vs the UI-6 reference (§1).** CSS moves BOTH ways plus the layer restructure — three itemized components (R6). JS grows with the tour/hints/etiquette/card-rework/restyle — copy-scale expected; beyond copy-scale, split by controlled builds (§48 method). Fonts unchanged; SPA un-split; no new package.
- **R26 — CONVENTIONS §53** per the §48–§52 model: decisions; deviations under scrutiny (expected: the hints narrowing, the toast/dot set narrowings, the A-2 sentence scope, the preflight-block outcome, any `Empty` retirement); the cascade record — fix class, re-audit outcomes per rule, the frozen-source/rendered-look sentence, the dist-test bite proof; the shed/stay lists with counts; the seed record (shape, wall proofs, what the walk shows); measurements (bundle three-way CSS itemization, assertion tables per file); the §47 ledger + §51/§52 stay-note dated cross-notes; AGPL attestation.
- **R27 — full battery in landing order** (§52 R19's list): `cd web && npm run typecheck && npm run test && npm run build` · `gofmt -l internal/ cmd/ tools/` empty · `go vet ./...` · `go build ./...` · `go test ./... -count=1` over the REAL new dist (45 pkgs / 14 skips) · `go run ./tools/lockgate` (38/574) · zero-drift fixture regeneration (`cursor` 89) · frozen-path spot-diffs per wall 1 · the clickthrough script end-to-end with the seed (throwaway only, production untouched — the script's own §4 checks).

## 7. Acceptance rubric (the evaluation judges against this — verify each by your own run)

1. **Walls:** every wall-1 file byte-identical to `8595174` (spot-diff api.ts, controls.tsx, chatFacts.ts, chatRuntime.ts, ui/Toast.tsx, ui/Timestamp.tsx, escape-scan.test.ts, push.ts, sw.js, every frozen surface, every production `.go`, `Spec/`, all 52 fixtures explicitly); App.tsx and History.tsx diffs are exactly the censused hunks; `cursor` 89 with a zero-drift regeneration re-run; lockgate 38/574; catalog 50/50; ⚙ 118/33.
2. **The cascade fix, proven in the BUILT artifact** (hazard #1 — the proven-highest-yield class): the dist test passes and its pre-fix bite is recorded; no owned rule below the token block remains unlayered (or unscoped, per the chosen class); the §52-B re-audit table exists in §53 with a per-rule outcome; the executor's per-surface visual record covers the 11 Button-consumer surfaces AND the native-controls-unchanged check; the 668 landed tests passed the fix commit unmodified.
3. **Every carried chat behavior pin** (R8's enumeration) green — the inventory partition, the error table, sessions/titling, file exchange, born-task family, no-storage scan.
4. **Every carried review behavior pin** (R9's enumeration) green — anchors/lifecycle (±2-line drift, orphan, open→consumed), findings→retry, dual-iframe try-it, one-click accept, split-default; no pin weakened by the restyle (hazard: an anchors test re-pointed to a vacuous selector).
5. **The A-2 card rework:** grouped-by-served-category with the order/count equality test; served question/reason/choices verbatim; choice-selects-never-fires; both consumers (History panel + chat) green; the client copy passes the overclaim negatives — **no unwired-cause claim** (hazard: the A-2 sentence claiming a capability or a cause the wire does not state); the three-verb head line true, file exchange included.
6. **The tour fires no verb:** the zero-non-GET walk over every registered step; the registry structural test with its planted-step bite; every target exists on an empty database (drive at least one route's walk over empty-world doubles); no auto-start; Escape/skip work; no storage, no ⚙, no timer, no library.
7. **Hints:** the chosen persistence shape matches the OQ1 disposition; no banned storage (scan); dismissible; copy truthful.
8. **Etiquette signals are real:** every dot/toast keys on a REGISTERED type from the landed sets (live.ts cites); dots on chat+inbox opening-types only, cleared on entry, no counts; toasts on the chat lifecycle only, suppressed on-route, kit-mounted; the narrowings (no run/deliverable toast, no memory dot) recorded with their evidence; no new REST read, no topic filter change (the connection stays as App made it).
9. **ToastProvider mount:** one mount, cap-4 + `data-limited` asserted at the mount, 668 landed tests green through it.
10. **The seed's both-direction wall:** compile-boundary negative asserted (test-only symbols + env-var-never-in-CI); the script runs one-command, seeds only its own throwaway, production-refusal preflight intact, `--clean` clean; every surface walkable per R17's list with the login leg documented; committed fixtures byte-frozen.
11. **Teaching empties:** every migrated site served-gated with its `pending` negative; Chat's rail `?? []` gated; no teaching over an unanswered read.
12. **CSS shed consumer-clean** (hazard: a shed rule with a surviving consumer): every shed rule's consumer grep empty; `.chat-body` still styled (sweep byte-unchanged); the shared blocks' stay-notes updated datedly; the preflight block's outcome verified, not assumed.
13. **Assertion accounting:** per-file pre→post vs §1 baselines by the landed method; nothing down silently; spot-check ≥5 rewritten assertions per big suite for predicate honesty; new suites (tour/etiquette/seed-wall/dist-cascade) each bite-proven.
14. **Phone 375px:** landed structural properties survive; the tour overlay, dots, toasts and restyled chat/review each get a both-way-probed leg.
15. **Bundle** vs §1's reference: three-way CSS itemization (wrap/shed/compile), JS copy-scale or split, fonts unchanged, no new package.
16. **Battery green over the real dist**; counters grow-only (vitest ≥668); §53 written with every deviation recorded; AGPL attestation present (tour/spotlight/etiquette/hints = reimplemented ideas, zero Odysseus code).

**Hazards named in advance (attack these):** the jsdom-cannot-see-the-cascade class is PROVEN highest-yield (§52-B) — demand the dist-test bite and the visual record; a tour step that fires a verb (or targets a confirm's act row); a hint state persisted to banned storage; a seed reachable in the shipped binary or in production posture; a toast invented for a signal the platform never emits (or one whose copy exceeds the frame's own fact); the A-2 sentence claiming the unwired cause or any capability the degraded mode lacks; an anchors/lifecycle pin weakened by the restyle; a shed rule with a surviving consumer on a frozen surface; assertion churn hiding a vacuous predicate behind stable counts; the AnswerView rework breaking the History-panel pins (one renderer, two consumers); a dot keyed to a closing type announcing removals as arrivals.

## 8. Files expected to change

`web/src/Chat.tsx` · `Deliverable.tsx` · `Memory.tsx` (bounded per R10) · `History.tsx` (censused: AnswerView card branch only) · `App.tsx` (censused: the three hunks) · `parts.tsx` (only if `Empty` retires or a genuinely shared idiom emerges — a new export is a recorded deviation, §51 precedent) · `index.css` (the layer restructure + the two sections' shed + stay-note updates) · NEW owned modules for the tour/hints and the etiquette hook (executor names them; under `web/src/`) · tests: `chat.test.tsx` · `deliverable.test.tsx` · `memory.test.tsx` · `mission.test.tsx` (AnswerView grouped-render legs) · NEW test files for tour/etiquette/seed-wall/dist-cascade · `doubles.ts` (test wiring) · `shell.test.tsx` ONLY if composition legs demand it (then true :401–402, dated) · `internal/api/apifixtures_test.go` or a sibling `*_test.go` (the seed entrypoint, test-only) · a Go-side dist-cascade test home if chosen (test-only) · `P3/gates/B6-clickthrough.sh` (the seed leg) · `P3/CONVENTIONS.md` (§53 + dated cross-notes). Everything else: wall 1. `sweep.test.tsx` expected byte-unchanged (duty containers kept; scans already tree-wide) — widening one is allowed only by addition with both-way probes.

## 9. Open questions for coordinator disposition (pre-launch)

- **OQ1 — the hints-persistence shape (tension: "one-shot" vs the §41-B storage ban).** No server-side per-person UI-state store exists; localStorage/sessionStorage are banned; a ⚙ key or migration is out of scope and the ledger admits nothing new. **Recommendation: module-scope in-memory seen-state — once per tab session, re-appearing next visit — recorded as a dated narrowing of the proposal's "one-shot", plus the same evidence ruling out tour auto-start.** Alternatives: a served signal (none exists — verified: no UI-state read in api.ts, no store in the schema); building one (a route + migration + scoping — far beyond the freeze).
- **OQ2 — the tour's verb-wall exact rule.** **Recommendation: R13's two-class allowlist (nav/route links; request-free or GET-only disclosure and selection controls), with the three-part proof (zero-non-GET walk · registry class check with planted bite · empty-DB target existence), and confirm-openers excluded** even though they fire nothing — a tour that opens a destructive confirm invites the next click to fire it. Alternative (allow confirm-openers with the dialog dismissed by the tour) adds risk for no teaching value.
- **OQ3 — the cascade fix class.** **Recommendation: layer the owned sheet below `utilities` (`@layer base`, or a named layer inserted before `utilities`)** — one structural move, exactly the row's first arm; the scoping alternative would have to rewrite element rules that still serve FROZEN surfaces' raw forms (§52 :1778) and cannot land whole this packet. R3's re-audit expectation (most §52-B keeps stay, re-dated) rides either class.
- **OQ4 — the seed's shape.** **Recommendation: the env-gated test-package entrypoint (R18)** — compile-boundary negative (never in the shipped binary at all), direct reuse of the producers it must derive from, the `TestWebAPIFixtures`/`TestFixtureWriterIsNeverAutomated` discipline extended. Alternative (a `devPosture()`-gated subcommand in `cmd/sinet`) leaves seed code dormant in the shipped binary — admissible under the row's words, strictly weaker, and it would be the packet's only production `.go` change.
- **OQ5 — the etiquette signal sets.** **Recommendation: R15's exact sets** — dots: chat (turn lifecycle) + inbox (card-OPENING types only), no memory dot (frames never reach the addressee — live.ts:101–108); toasts: `chat.turn_settled`/`chat.turn_abandoned` only; no run/deliverable toasts (the frame's top-level fields carry no state fact; payload is opaque by the §42 frames-are-triggers discipline; no honest link target) — each exclusion recorded with its evidence. Alternative (payload-reading toasts) breaks the frames-are-triggers discipline the whole tree observes.
- **OQ6 — the A-2 degraded-mode sentence vs the approved proposal's example.** The proposal (:44) illustrates "no local model is wired, so I can't read sentences yet — pick a query"; the wire carries no cause code (api.ts:202–206) and the card has ≥7 reason classes (layer1.go) — the example cannot ship as unconditional client copy without claiming a cause the client cannot know (§38 bans prose-classification). **Recommendation: R7's shape — general-card operator words + the served reason verbatim as the sole cause statement — with a dated amendment note on the proposal (its own §5 mechanism: "deviations found mid-build return here as dated amendments"), coordinator-authored.** Alternative (a served machine code on the card) is a backend change needing its own sanction — available later, not needed for honesty now.
- **OQ7 — the A/B/C seam (pre-authorized mid-flight split precedent, batch preamble :142).** **Recommendation: A = the cascade fix + §52-B re-audit + dist test + the chat/review restyle + the A-2 card rework + their sheds (the fix lands FIRST inside A so the restyle authors utilities that win as authored); B = the self-teaching layer (tour/hints/teaching empties) + the etiquette + ToastProvider's mount; C = the seeded world + the script leg + the exit battery + §53 finalization.** Each stop leaves the tree green and coherent (A: all landed tests + the new cascade/restyle tests; B: adds only additive UI; C: repo-outside-web changes are test-only + script). A clean stop at A or B is a stated success mode.
