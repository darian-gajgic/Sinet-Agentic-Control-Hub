# P3-B6-8 brief — Review surfaces + try-it + accept UI + workforce map

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.8** (review surfaces) + **S15.10** (workforce map) owning, **S15.2/S15.3/S15.12** binding context (`Spec/drafts/S15-frontend-api.md`); **S13.1–S13.4** (deliverable/revision schema, per-type comparison, the carried N15 anchor/lifecycle contract, THE drain), **S13.6** (accept flow), **S13.8** (previews + the dual-iframe before/after) in `S13-deliverables-review-git-backup.md`; **S08.1/S08.4/S08.8/S08.9** (the worker registry the map renders; version→outcome joins; routing accountability; automations) in `S08-workers-composition-routing.md`; **FC-v1 §2** (`Spec/frontend-components-v1.md` — the react-diff-view pick + decision notes, binding). `P3/CONVENTIONS.md` binds: **§5, §7, §9, §30, §38, §39(+B), §40-B, §41 + §41-B ENTIRE, §42 + §42-B ENTIRE, §43 + §43-B** (B6-6, restate-at-launch — see counters). **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is packet 12/13 of B6** (queue `P3/STATE.md` §B6; B6-1/2A/2B/2C/3A/3B/3C/4/5 landed+validated; **B6-6 landed executor-side pending evaluation; B6-7 brief exists, its executor NOT launched — both volatile, restate at launch**). It fills the two B6-8 stub routes — `/deliverables/:id` and `/workforce` (`web/src/routes.ts:49–50`, owner `'B6-8'` → `''`) — as the first consumer of the whole B6-3B deliverables/accept/preview family. It builds web-side views over landed verbs; it re-implements NO server machinery, and the Go side changes ONLY per OQ dispositions (each a pre-raised grounded gap — never a new route family invented by the executor, never a migration, never a new event type outside a dispositioned §29 path). Push/PWA/re-nag + the S15.12 sweep (B6-9, the LAST packet) are NOT this packet. **Pre-authorized A/B stop (queue row): A = review + try-it + accept; B = workforce map.**

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-29 — do not relitigate)

**A. The deliverables family (`internal/api/deliverables.go`; routes `api.go:451–461`, follow-up spawn `api.go:397`).**

- **`GET /api/deliverables`** (deliverables.go:119–205) → `DeliverableList{deliverables[], cursor, truncated}`; filters `?project`/`?state` (closed set in-review/accepted/superseded, :54–56)/`?type`/`?task` (:150–169; `?task` is the B6-5 task-detail read). **`GET /api/deliverables/{deliverable}`** (:207–254) → `DeliverableDetail{deliverable, revisions (1..N, never compressed), lineage: TaskLineage, doors[], cursor}`. 404-before-403 via `deliverableScope` (:645–660).
- **Doors-as-data** (OQ3 of B6-3, ratified; :58–95, 256–357): five verbs — `accept` (PinFrom `GET …/accept-card`), `request-revision` (two limbs: in-review + open rework card ⇒ the landed answer route `/api/approvals/ask:<id>/answer` with `answer:"revise_with_guidance"`, the ask id and **the card's own `gates.CanonicalHash` pin** served on the door (:329–348); finished ⇒ `…/follow-up` with preset `revision` (:350–356); in-review with NO open card ⇒ **no route named**, narrative reason only :340–348), `follow-up`, `preview`, `preview-compare`. `Door` = {verb, method, route, available, reason, pin_from?, answer?, ask_id?, payload_hash?, preset?}. **A closed door still ships with why; the surface renders a control ONLY where the door is available — no dead door, no invented door** (the D9/D3 rule, §40-B:814).
- **`GET …/compare`** (:439–475) → `review.Comparison` VERBATIM. `?new=`/`?old=` both optional: **defaults new = current revision, old = new−1 — round-over-round IS the server default** (:451–463); any distinct pair on demand; **old=0 = the pre-task base** (S13.1). No HTML, no tokens, no server highlight ships (:430–437; §40-B:823) — react-diff-view parses the unified text and highlights client-side (FC-v1 §2).
- **`review.Comparison`** (internal/review/compare.go:17–49): {deliverable_id, type, old_n, new_n, **surface** ∈ {`line-diff`, `extracted-text-diff`, `image-pair`, `binary-cards`}, fallback?, **label** (the visible labeling text), unified? (host-side git diff text), old_objects[]/new_objects[] (`ObjectRef{name,size,sha256,type}`, review.go:134–139), changed? (by-hash verdict), pixel_diff? (nil seam at v0)}. Type mapping (compare.go:120–179): code/text→line-diff; **PDF→extracted-text-diff labeled** ("v0 PDF surface; pixel overlay is post-v0"), extraction failure **degrades to binary-cards, labeled, never a refusal** (:134–141); images→image-pair (the S15 trio renders; per-side objects + changed); other text-extractable→extracted-text **fallback labeled**; opaque→binary-cards (+fallback label for unknown types).
- **`GET …/comments?revision=`** (:490–547; default = current revision) → `PlacedComments{deliverable_id, revision_n, comments[], placements[], cursor}`. **Placements are recomputed server-side on EVERY render** (S13.3; FC-v1 §2) — a client's positions are never placement authority. `Placement` = {comment_id, revision_n, **status ∈ {exact, mapped, drifted, file, orphan}** (review.go:49–66, 92–100), anchor (live position; file ⇒ LineNo 0; orphan ⇒ zero-valued — the quote lives on the comment row)}.
- **`review.Comment`** (review.go:103–130): {id, owner, deliverable_id, **revision_n (the original-revision link)**, run_id?, kind, **severity** (`blocker`|`note`, :40–43), category?, criterion?, body, suggested_change?, **anchor (the original AS-CLAIMED anchor)**, origin_anchor?, **anchor_status (BornStatus — server-validated at birth)**, **status (`open`|`consumed`)**, **finding_number ([F1..Fn], stamped by THE drain at consumption, 0 while open)**, consumed_at?, consumed_by?, created_ts}. `AnchorRecord` = {file_path, side `old`|`new`, line_no, line_text} (:83–88).
- **`POST …/comments`** (:549–631): body {revision (0 = current), **body REQUIRED**, severity (closed vocabulary or empty), anchor?, file_level?, suggested_change?}; side/severity/revision validated at the boundary (§39 drain-D7); author = the AUTHENTICATED identity (:611); the claimed anchor lands with whatever status the ladder gives it. **Create + Read ONLY (B6-3 OQ2 ratified): no update verb, no delete verb exists, at any shape — seven absence probes landed** (§40-B:813). Response = the created `review.Comment`; the `review.comment` event is the store's own mint — the UI mints nothing.

**B. The accept (`internal/api/accept.go`).** **`GET …/accept-card`** (:170–221) → `AcceptCard` = {deliverable_id, revision_n, pin_kind, content_pin, project_id, protected_ref?, **trailers (byte-for-byte what the commit will carry — shown BEFORE accept, S13.6 step 3)**, provenance {minting_run_id, engine, model, lane, vendor_noreply, **absent?** (why trailers could not render)}, **signing** (the structural secret-free posture block, :98–113 — no per-call flag, key never leaves the broker), tier `"high"` + tier_statement (:159–160), **payload_hash** (= `gates.CanonicalHash` over {deliverable_id, revision_n, content_pin, trailers} — :162–168, 212–218; **the project's expect-sha is deliberately NOT in it**: HEAD moving = a collision = a merge card, §40-B:816), **acceptable + reason** (`acceptable()` :225–239 is the ONE expression of the preconditions, read by card AND verb), route}. Cards answer for ANY state — accepted/non-repo-backed/no-provenance get acceptable:false with the reason, never a refusal (:186–188; §40-B:818 — the no-attribution refusal is a RATIFIED fail-closed posture).
- **`POST …/accept`** (:355–447): body {payload_hash, pin, subject?, provenance?}. **Resolved first** (§39 order): accepted ⇒ 200 read-back `applied:false` + recorded commit/effect (:383–391, 510–525); superseded ⇒ 409 (:392–396). Then **hash pin BEFORE PIN step-up** (:413–419 — the RATIFIED B6-3B deviation, §40-B:817: a stale card costs a re-read, never an `auth.reprompt` row for an act that 409s): blank hash ⇒ 400 missing (:569–571); wrong hash ⇒ **409 `{error:"stale_payload", detail, current: <the FRESH AcceptCard>}`** (:578–592) — **NOTE: `current` here is an AcceptCard, NOT an ApprovalItem — the landed `staleCard()` helper (web/src/api.ts:742–747) does not fit; the accept surface needs its own stale reader**. acceptable:false ⇒ 400 with the card's reason (:423–426). **PIN in the SAME request** via the landed stepUp (:427–437; missing ⇒ 401 `pin_required`, wrong ⇒ 401 + `auth.reprompt_failed` — the B6-6 R3 flow shape). **Authorship, not visibility** (:370–379): the operator can READ another owner's card and can never accept it — the verb's scope drops the operator limb.
- **`AcceptOutcome`** (:337–353, 486–508): {deliverable_id, **applied**, state, revision_n, commit?, effect_id?, superseded[], routed_runs[] (the S02.8 sibling-accept freshness fan-out), **merge_card?**, detail}. **The merge card** (:50–87, 497–502): `MergeCardView{card (verbatim from internal/accept), options[3], durability}` — **all three options `answerable:false` with per-option reasons** (agent-auto-resolve: no S07 executor at v0; resolve-manually: none by design; abort-to-new-attempt: none, **but Route+Preset name the landed follow-up door reaching the same outcome**); `durability` states the OQ4 return-only posture as data (v0 stores no merge card; a failed effect stays journal-queryable). A collision answers 200-class with `applied:false`, state in-review, "nothing was pushed".

**C. Previews (`internal/api/previewapi.go`; `internal/preview`).** **Low tier throughout — NO journal, NO proposal, NO PIN on any preview route** (previewapi.go:14–20; S13.8 "launching one is a Low-tier, reversible action"). `POST …/preview` {revision (0=current)} → `preview.Session`; `POST …/preview/compare` → `preview.Comparison`; `GET /api/previews` (transport owner-scopes: member own, operator all, :130–148); `POST /api/previews/{session}/stop` (own-or-operator, 404-before-403; partial teardown keeps the session retryable, :159–188). **Every per-type disposition is a 200 ANSWER, never an error** (:22–27; §40-B:821): no-preview, self-preview, requires-container-tier, unavailable, at-capacity — states with reasons; only caller mistakes are 4xx.
- **`preview.Session`** (preview.go:135–170): {id, deliverable, revision, role, lane, **state**, reason?, **url** (the tailnet front-chain subdomain when routed; the loopback backend in dev — routing disabled), backend_addr?, pool_port?, route_id?, routed, **ports[]** (the multi-port picker data), runner?, user, host_injections?, created_ts}. **`SessionView`** (:178–193) = the lean per-side data {role, deliverable, revision, url, state, reason}.
- **`preview.Comparison`** (preview.go:205–216; comparison.go:24–55): {deliverable, **before?** (nil when no accepted revision), **after**, **single_instance**, **sync: {mode:"path", enabled}**}. The "before" is the ACCEPTED revision, launched as its own full instance; no accepted revision ⇒ the honest single-instance state, `sync.enabled:false` ("revision 1 has no before; its diff-against-base story is S13.1's, not a preview fake"). **`SyncSemantics.Mode:"path"` = mirror the URL path+query across both sides — DATA; behavior is this packet's** (preview.go:195–203).
- **HOST HAZARD (standing):** hermetic tests NEVER touch the live Caddy admin chain (8481/8482). Go-side preview fixtures compose `NewCaddyClient("","")` (routing disabled) with the AST tripwire over every `internal/api` test file (§40-B:822); web-side tests are doubles/fixtures and launch NOTHING.

**D. The escape-scan grounding — THE packet-specific finding.** `web/src/escape-scan.test.ts`: the allowlist is EMPTY (:22) and the banned-token set is **exactly seven raw-HTML/code-execution constructs** — `dangerouslySetInnerHTML`, `innerHTML`, `outerHTML`, `document.write`, `eval(`, `new Function`, `insertAdjacentHTML` (:23–31, split-written). **An `<iframe src={…}>` element trips NONE of them: an iframe with a src URL embeds a browsing context by reference — it is not a raw-HTML assignment and is outside the banned-construct set entirely.** The scan therefore does NOT need to move for the S15.8 preview surface. BUT three landed texts promise otherwise — the scan's own header ("THE ALLOWLIST IS EMPTY. It widens at B6-8…", :10–12), the assertion message ("the allowlist widens at B6-8 with the preview surface, not before", :83), and §41-B:883/§43:977 — so the resolution MUST be recorded, not silently enjoyed (OQ1). The one raw-HTML channel named server-side is `review.EscapeFirst().SanctionedRawHTML = ["sandboxed-rendered-document-view"]` (review.go:184–196), test-pinned to exactly one (compare_test.go:176–177) — S13.3's name for what S15.12 calls "S15.8's preview surface": one channel, two spec vantages.

**E. The workforce-map data reality (`internal/worker`) — grounded gaps.**

- **The registry shapes exist and are rich**: `Template` (worker.go:114–125: id, owner, name, scope `personal|household`, kind `agentic|automation`, domain, status, active_version), `Version` (:129–149: file ref + full provenance block + approval/graduation stamps), `Guardrails` (:172–187: granted tools, gated tools, permission mode, confinement class, egress + hosts, budgets, gate policy, **first_n_remaining**, schedule_attachable), `Domain` (:190–195: maturity full|degraded), `Definition.Equipment` (template.go:37–42: **tools, skills, knowledge, connectors — NO "helpers" field exists in the S08.1 schema**; helpers are per-run routing, S08.8 step 5), `Definition` (:54–69: selectors, profile duty+effort, persona, eval hooks, body).
- **NO roster/list read exists.** The store has only single-item reads — `Template(id)` store.go:150, `VersionByID` :185, `Guardrails(versionID)` :214, `Domain(name)` :251, `VersionSource` :558, `LatestRevalidation` lifecycle.go:720; the only all-rows scans are unexported internals (routing.go:406, lifecycle.go:567). **NO `internal/api` file imports `internal/worker`; no `/api/workers` route exists** (api.go grep). → OQ3.
- **NO per-version quality/cost read exists.** S08.4: "Version→outcome is a JOIN, not a system" — `routing.decided` records worker+version per run (`Decision` routing.go:237–276: `worker`, `worker_name`, `version`, model, lane, effort, plain_reason, generalist, degraded), joined to verdicts (`verdict.recorded` = verify.EventRound, verify/record.go:36; `Revision.VerdictRef` review.go:153) and receipts (money is READ via `MeterReader.RunMeter` — §42 precedent, never computed). Nothing in `internal/history` references `routing.decided` (grep), and **the Layer-1 catalog sits EXACTLY at its 30–50 band ceiling (50/50, §43-B:995) — a new catalog query breaches the band**. → OQ4.
- **NO object-bytes read exists.** The image visual compare and the binary "download to inspect" (S13.2) need the object-dir BYTES; `ObjectRef` carries only metadata+sha, `Store.ObjectPath(sha)` is exported (objects.go:45) but no route serves content (grep: no ServeContent/ServeFile in `internal/api`). → OQ2b.
- **The v0 roster is EMPTY on a fresh host** — compose-when-earned (S08.6); nothing seeds workers (shell.go:391 constructs the store only). The map's first honest render is the S08.5/14.4 no-standing-army state; fixtures must CREATE workers through the real producers (`CreateDraft`→`Approve`, lifecycle.go:67/220; kind=automation bodies are `automation.Workflow{dialect, service, steps[{id, verb, args, approval}]}`, automation.go:48–67 — **the registry's own multi-stage data**).

**F. The web machinery (landed B6-4/5/6 — consumed, never rebuilt).** Views swap into App.tsx's route switch; URLs never renamed; `useLive({key, read, types, …})` is the only feed access (live.ts:166; §42); one EventSource, per-subscriber debt, declared types with per-type reasons (§41-B/§42); golden fixtures under `web/src/fixtures/api/` byte-tied by `internal/api/apifixtures_test.go` (compare-only; fixed-clock; regeneration env-gated; **deliverable fixtures already exist and already serve doors+lineage** — deliverable-detail.json carries both), vitest doubles import the same files (doubles.ts); `parts.tsx` honesty primitives (absences with reasons, money as served, verbatim timestamps, no percent/ETA); phone-first structural responsiveness. `api.ts` already has `api.deliverable/deliverables` (:865–868) but **its `DeliverableDetail` type predates doors/lineage (:777–792) and NO compare/comments/accept/preview client verbs exist** — this packet extends them. react-diff-view 3.3.3 + gitdiff-parser probe-green at the pin (picks.test.tsx:61–98: parseDiff + `<Diff>/<Hunk>` render; insert/delete lines DOM-distinguishable — "the anchor model B6-8 needs", §41-B:885).

**Counters at grounding (2026-07-29 — VOLATILE: B6-6 is executor-landed pending evaluation, B6-7 brief exists unlaunched; RESTATE ALL at executor launch):** migrations **0001–0019** (`user_version` 19); event inventory **91/5/96**; ⚙ **118/33** (`internal/settings/index.go` byte-unchanged); `components.lock` **29**; conformance rows **12**; history catalog **50/50 (band ceiling — flagged)**; CONVENTIONS **§1–§43(+§43-B)** — this packet appends **§44 or the next free number (B6-7 may land §44 first — confirm at launch)**; web **209 vitest / 17 files** (B6-6 executor claim; drains may move it); escape-scan floor `> 35`; **43 Go test pkgs** green.

---

## 1. Numbered requirements

### A — Review surface, try-it, accept (`/deliverables/:id`) [S15.8; S13.1–S13.4, S13.6, S13.8; FC-v1 §2]

- **R1. The diff surface is react-diff-view over served unified diffs** [S15.8; FC-v1 §2]. The line-diff/extracted-text surfaces render `Comparison.unified` through `parseDiff` + `<Diff>/<Hunk>` with BOTH `split` (side-by-side) and `unified` (inline) view types, user-switchable; **highlighting is client-side via the library's own tokenizer** (or plain untokenized rendering where tokenize adds nothing — a recorded choice), NEVER a server-rendered form and never a second parser. The extracted-text and fallback surfaces render the SAME diff widget **with the served `label` visibly displayed** (the 2.1 honesty posture — a fallback never poses as a rich surface).
- **R2. Round-over-round default + revision-over-revision navigation** [S15.8; S13.1]. The default view is the server's own default read (no params: new=current, old=new−1); navigation renders the revision lineage 1..N (already served on the detail) and drives explicit `?old=&new=` pairs, including **old=0 = the pre-task base**; the current pair is always visible. Lineage is NEVER compressed client-side; every revision row shows its pin, minting run and verdict ref as served.
- **R3. Every comparison surface type renders, including its degrade lanes** [S13.2]. `image-pair` → the visual compare over the per-side objects (GitHub's trio 2-up / swipe / onion-skin per S13.2 — owned CSS/DOM, no new dependency) + the by-hash changed/unchanged verdict + the optional pixel_diff aid when served; `binary-cards` → a metadata card per side (name, size, hash, type), the hash verdict, and **download-to-inspect** per OQ2b's disposition; `extracted-text-diff` with its label (PDF; text-extractable fallback); the PDF-extraction-failure body (surface binary-cards + fallback + label) renders as served. **An unknown/future `surface` value renders the honest "no rich comparison surface for this answer" state with the served fields — never a blank or a crash** (the §42 forward-tolerance).
- **R4. Anchored comments at exact places — the gutter/widget wiring** [S15.8; S13.3; FC-v1 §2]. Comments render AT their server-computed placements via the widget/gutter hooks (keyed off the rendered hunks' change positions): exact/mapped/drifted placements render at `placement.anchor`'s (side, line_no) **with the anchor status visible** (drifted says drifted); `file` placements render on the file header region with the original quote; **`orphan` placements render on the synthetic strip**. Creating a comment: selecting a diff line composes the CLAIMED `{file_path, side, line_no, line_text}` from the rendered hunk data plus body/severity/suggested_change and POSTs; the response's `anchor_status` renders immediately (a client that named a wrong line SEES the drifted/file placement the server gave it — never the position it asserted). File-level comment entry (no anchor) is first-class. Severity is the closed two-value choice (`blocker`/`note`) rendered with the S13.4 meaning (blocker = another round; note = polish travels along); an empty severity is allowed as served.
- **R5. The synthetic render surface — a finding is never invisible** [S13.3; FC-v1 §2; P-T12-2]. A dedicated, always-visible strip renders every comment without a live anchor in the current view (file-level, orphaned, cross-cutting) with the original quote and original-revision link. **Invariant, tested: no served comment may exist without a render location** — a driven test feeds a PlacedComments body containing all five statuses (orphan included) and asserts every comment id appears in the DOM.
- **R6. Lifecycle + the drain, rendered — not operated** [S13.3–S13.4]. `open` vs `consumed` renders distinctly; a consumed comment shows its **[F#] finding_number and the batch `consumed_at` + consuming attempt ref** — "what did that rework receive" is auditable on the surface. **The UI has NO drain verb and NO update/delete affordance: immutability renders as the property it is** (the §43-B price-table posture); a person who wants to change what they said adds another comment. Verification findings (kind/category/criterion/origin_anchor fields) render under the same schema alongside human comments.
- **R7. Doors render as served — the deliverable page's action surface** [§40-B:814; B6-3 OQ3]. Every door renders; available doors are controls, closed doors render their reason verbatim (never a dead button, never an invented one). The `request-revision` door drives the landed answer verb with the door's own ask_id + payload_hash + `revise_with_guidance` (the B6-6 answer-flow shape: stale ⇒ re-read; the guidance text lands as durable requester comments through the drain — say so in the UI from the door's reason); the finished-state limb links the follow-up spawn with preset `revision`. Follow-up + lineage render both directions.
- **R8. Try-it: one-click preview + every disposition honest** [S15.8; S13.8]. The preview door launches `POST …/preview` and renders the Session: a backed session links/embeds its `url`; `ports[]` >1 renders the picker; **every non-backed state (no-preview, self-preview, requires-container-tier, unavailable, at-capacity) renders its served reason as an answer — NEVER a broken iframe** (S13.8's named failure). The owner's live sessions list renders with stop (retryable on partial teardown, as served); `preview.stopped`'s detail renders.
- **R9. The dual-iframe before/after — the honest synced shape** [S13.8; S15.8] — per OQ2's disposition. `POST …/preview/compare` renders `before`/`after` as two iframes whose `src` is EXACTLY the served `SessionView.url` (+ the shell's current path suffix). **Grounded constraint the design must honor: the preview origins are cross-origin to the SPA (tailnet subdomain or loopback port), so the shell can neither read an iframe's current location nor observe in-frame navigation — "synced navigation" is therefore SHELL-DRIVEN: one shared address/path control renders `sync.mode:"path"` by composing `<side.url><path>` and setting BOTH frames' src together; clicks inside a frame move that frame alone and the surface SAYS so, with a re-sync affordance re-asserting the shell's path on both.** `single_instance:true` renders ONE frame + the served no-before honesty (`sync.enabled:false` — no fake second pane). The comparison consumes a second concurrency slot per side (server fact); at-capacity on either leg renders as served.
- **R10. Accept: ONE action on the reviewed revision** [S15.8; S13.6; S15.6]. The accept door opens the card view rendering EVERY served field: the revision + content pin, the protected ref, **the trailers byte-for-byte with their provenance sources** (or the absent-reason when acceptable:false), the signing posture block, the High-tier statement, and the payload_hash. The act: collect the PIN (type=password, cleared after submit, never stored/logged), POST `{payload_hash, pin, subject?, provenance?}` **in one request**; render every outcome honestly — applied (commit, effect id, superseded list, routed-runs re-validation fan-out, detail verbatim); **the merge card** (the three options WITH their answerable:false reasons rendered as statements, the third linking the landed follow-up door; the durability text; "nothing was pushed"); already-accepted read-back (applied:false as the retry-safe answer, not an error); superseded 409; **stale 409 re-renders from the carried `current` AcceptCard with a visible change notice** (a NEW accept-shaped stale reader — `staleCard()` does not fit, fact B); 401 pin_required/pin_rejected re-prompt. **Presentation follows authorship**: the accept form renders only for the deliverable's owner (session identity vs served owner — client-side PRESENTATION per S15.2, the server remains the authority and the 403 path is still driven in tests); a non-owner viewer (operator included) sees the card read-only with the authorship line.

### B — Workforce map (`/workforce`) [S15.10; S08.1/S08.4/S08.8/S08.9]

- **R11. View-only roster from the S08 registry** [S15.10] — over OQ3's dispositioned read. Every worker visible to the caller renders: name, kind (agentic/automation), scope, owner, status, domain **with its maturity marking** (degraded renders as the structural fact it is, S08.7), active version + provenance summary (author kind, origin, approved-by/when, graduation), first-N remaining where not graduated. Identity rules per the disposition (personal to their owner, the shared roster to all). **The empty roster renders the honest no-standing-army state** (machinery is composed when earned — S08.6/14.4), never a blank page. **NO mutation affordance exists anywhere on the map** (S15.10: editing is parked to 15.5) — a scan/DOM assertion proves no POST fires from this surface.
- **R12. Equipment per worker** [S15.10; S08.1/S08.2] — the registry's own vocabulary: the version's requested equipment (tools, skills, knowledge selectors, connectors) AND the granted `worker_guardrails` as the permissions block (granted tools, confinement class, egress + hosts, budgets, gate policy) — **requested vs granted distinguishable** (the S08.2 split made visible). "Helpers" renders per OQ5's disposition — the schema has no helper field; nothing is invented.
- **R13. Multi-stage connections** [S15.10; S08.9] — per OQ7's disposition: kind=automation workers render their parsed workflow step chain ({id, verb, approval-node marking} in order — the registry's own multi-stage data); agentic workers render their selectors/duty/effort profile. The approval-node marking renders as the D7 fact it is.
- **R14. Per-version quality/cost views** [S15.10; S08.4] — over OQ4's dispositioned read: alongside each worker, its versions with per-version outcome figures (runs routed, verdict outcomes, cost figures READ from receipts — never computed client- or transport-side) plus validation/revalidation stamps where served. **Honest absences bind**: a version with no routed runs renders "no outcomes recorded", zero rows render as the reading nobody took — at v0 these views will usually be empty, and that empty is the truthful render.
- **R15. TBD-P3(workforce-map rendering approach) RESOLVED with a dated note** [S19.5 TBD row; queue row] — per OQ6's disposition (owned rendering expected). The resolution lands as a dated note in this packet's CONVENTIONS section; **a graph-layout dependency can enter ONLY via a coordinator-ratified S16.4 rail walk — never an executor decision** (see §5).

### C — Cross-cutting

- **R16. The escape posture** [S15.12; FC-v1 §2] — per OQ1's disposition. Expected shape: **the allowlist stays EMPTY** (the grounded finding: iframe-with-src is outside the banned-construct set); B6-8's commit updates the scan file's stale forward-promises (header + assertion message) to record the resolution with a dated note; iframe `src` values compose ONLY from served preview-session URLs + the shell's path state — never from deliverable/comment/model content; NO `srcdoc` anywhere (see OQ1's hardening limb). Everything else renders escaped: diff text, comment bodies, labels, reasons, worker definitions are text. The scan floor moves over the new views and names them.
- **R17. Live wiring + rendered honesty** [S15.3/S15.12; §41-B/§42]. Views ride `useLive` with declared event types chosen against the registry, each with its §42 per-type reason (candidates grounded: `review.comment`, `verdict.recorded`, `decision.recorded`, `preview.started`/`preview.stopped`, `routing.decided`, `run.state_changed`; the executor enumerates against `topicsForEvent`); REST is the truth, frames trigger re-reads; disconnect renders visibly; after any own verb the view re-reads. `parts.tsx` honesty primitives for absences/money/timestamps; no percent/fraction/ETA; desktop-comfortable is S15.12's word for these two surfaces — phone still renders them readably (structural assertions), full comfort is not demanded.
- **R18. api.ts + fixtures via real verbs** [§42]. Typed reads/verbs extend `web/src/api.ts` (detail type gains doors+lineage; compare, comments GET/POST, accept-card, accept, preview launch/compare/list/stop; the OQ-dispositioned workforce reads); golden fixtures for every newly consumed read shape, produced through REAL producers in `apifixtures_test.go` (comments via real `AddComment` with real placements incl. a genuine orphan across a revision hop; comparisons from real revisions of each surface type; an acceptable accept card via the hermetic accept composition (fake pusher, t.TempDir git — §40-B posture); preview shapes per OQ8's disposition; workers via real `CreateDraft`→`Approve`) — compare-only, fixed-clock, the stability re-seed test extended.
- **R19. Counters + untouched machinery.** NO new npm dependency (lock stays at its launch-restated value; react-diff-view + gitdiff-parser are installed — wiring is consumption); NO new ⚙ key (118/33, `index.go` byte-unchanged); NO migration (0001–0019); NO new event type (91/5/96) — every UI act has a landed canonical row (comment→`review.comment`, accept→the journal + `decision.recorded` machinery, preview→`preview.started/stopped`); Go-side changes ONLY per OQ dispositions, additive, with owner-scope three-way tests and the §40-B/§42 scans extended; `internal/review`/`accept`/`preview`/`worker` core semantics, `internal/settings/index.go`, `eventlog/contract.go`, BENCH-REG, `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.
- **R20. CONVENTIONS §44-or-next-free appended** (confirm at launch; B6-7 may land §44 first): the OQ dispositions as landed, the escape-scan resolution note, the dual-iframe honest-sync reading, the TBD-P3 dated resolution, per-view type declarations, new structural constants with reasons. Earlier sections never rewritten. Commit subjects per §5 + trailer.
- **R21. Battery + hermetic + $0** [§3/§5/§41]: `gofmt -l internal/ cmd/ tools/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` · `go run ./tools/lockgate` · `cd web && npm run typecheck && npm run test && npm run build` — all green. Vitest offline (doubles; fixtures as bodies); NO test dials 8481/8482, any live unit, or the network; no web test launches a preview or spawns a process; `VITE_SINET_ADDR` unset. $0.

## 2. Seams — what belongs to LATER packets / the gate (leave named, never build)

| Where | Owns | B6-8 leaves |
|---|---|---|
| **B6-7** (may land first — restate) | assistant (`/chat`) | its stub; no chat wiring here |
| **B6-9** (LAST) | Declarative Web Push + re-nag, PWA completion, the S15.12 completeness sweep + escape AUDIT ("only the preview iframe renders raw"), the final "SPA consumes every API" check, human-friendly timestamp policy | stable URLs it deep-links; the sweep re-checks this packet's surfaces |
| **B6 gate / bring-up** | the catalog 50/50 band-ceiling revisit; the `Gate.authorize` carries; benchmark readout; done-directly aggregate; fleet seat/GPU fill; anything OQ dispositions defer | named, not built |
| **v1 / 15.5** | workforce-map EDITING; docx/pdf-pixel lanes; monaco upgrade path; sidecar container tier | view-only map; labeled degrade lanes |
| **Not this packet** | new mutation verbs beyond consuming landed ones; SSE server changes; `internal/review`/`accept`/`preview` semantic changes; any npm addition | — |

## 3. ⚙ settings consumed (by exact dotted key; none invented)

- **None read client-side.** `review.anchor_drift_lines` acts inside the review store (S13.3 ladder); `preview.idle_stop`/`preview.max_concurrent` act inside the preview manager — all three speak through SERVED data (placements, at-capacity states) this packet renders. Client display constants (view-switch defaults, list caps) are structural with named reasons (§41/§42 precedent), enumerated in the new CONVENTIONS section — interim under the standing settings-tab directive.

## 4. Files expected to change

- **New/extended (web):** `web/src/` deliverable-review view module(s) (diff surface, comments, doors, try-it incl. the dual-iframe shell, accept flow) + workforce-map view module(s), replacing the two Stub renders (App.tsx switch; routes.ts owner `'B6-8'`→`''`, patterns never renamed); their vitest suites; `web/src/api.ts` typed reads/verbs; `web/src/doubles.ts` + `web/src/fixtures/api/` new fixtures; `index.css` owned additions; `escape-scan.test.ts` per OQ1's disposition (the stale-promise correction + any ratified token hardening — a deliberate, recorded touch).
- **Extended (Go, ONLY per OQ dispositions):** candidates are OQ2b (object-bytes read), OQ3 (roster read in `internal/worker` + its `internal/api` serving), OQ4 (the version→outcome read), OQ8 (fixture-world composition growth in `internal/api/apifixtures_test.go`); each additive with tests + scans extended; nothing else in `internal/`.
- **Bookkeeping:** `P3/CONVENTIONS.md` (the new section).
- **Expected NOT to change:** `web/src/routes.ts` patterns, `router.tsx`, `events.ts`/`live.ts` semantics, `Inbox.tsx`/`Settings.tsx`/B6-5 views (except a real link swap where a stub href was the placeholder), `internal/review`/`internal/accept`/`internal/preview`/`internal/worker` existing verbs and walls (additive reads only per dispositions), `internal/settings/index.go`, migrations, `internal/eventlog/contract.go`, `internal/benchmark/registered.go` + BENCH-REG, `components.lock` + `web/package.json`/`package-lock.json` (byte-unchanged), `go.mod`/`go.sum`, CI, `Spec/*`, `Docs/*`, `Research/*`, `P3/STATE.md`.

## 5. Adopted components touched

**react-diff-view 3.3.3 + gitdiff-parser (MIT) — installed at B6-4, probe-only until now; THIS packet wires it.** FC-v1 §2 decision notes bind as constraints: **the widget renders; the ported N15 behavior spec governs** (the anchor model, server-side re-validation, degrade ladder, orphan state, synthetic strip, escape-first — all already server-enforced; the UI renders them, never re-implements); **highlighting is client-side via its tokenizer — no server-side highlighter exists in the platform**; single-maintainer risk is bounded by the pin + containment to this one view; **monaco diff (@monaco-editor/react 4.7.0) is the PRE-REGISTERED upgrade path** — a dead/relicensed/deprecated finding at execution re-verification = STOP + flagged coordinator decision, never a silent substitute. **Re-verify liveness at the pin at execution** (registry latest/deprecation/license/advisories + lockfile integrity — the §43-B JSON Forms ceremony). Adopt-don't-fork: no patches, pin never bumped. **Any graph-layout library for the map is a NEW adoption through the S16.4 rail — an OQ6 coordinator/operator matter, NEVER an executor decision; no other dependency enters.**

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format + trailer, no push, never edit Spec/Docs/Research/STATE.
- **§7/§9/§30/§38** — unversioned API consumed as-is; identity from the session read only; SSE contract verbatim; served content renders as data; error handling branches on served codes/types, never message text.
- **§39(+B)/§40-B** — landed decision-plane semantics consumed VERBATIM: hash pins, the pin-before-step-up accept order, resolved-first retry-safety, doors-as-data, Create+Read-only comments, merge-card return-only, Low-tier previews, authorship-not-visibility; the UI implies no authority a verb does not grant.
- **§41 + §41-B + §42 + §42-B + §43 + §43-B ENTIRE** — npm exact-pin discipline; owned router (fill, never rename); one EventSource + per-subscriber debt + declared types; `useLive` as the only feed access; golden-fixture tie (compare-only, fixed-clock, real producers); escape scan mechanics; phone-first structural responsiveness; `VITE_SINET_ADDR` hermetic posture; rendered-honesty primitives; verbatim timestamps; ApiError code-carrying.
- **The 35 D6-ratified readings** (§29–§37): derive-from-log, no tickers, money read never computed, model output is untrusted input, honest absence over fabrication, stale never poses as live, proposals never auto-apply, structural constants carry named reasons.
- **Hermetic + $0** — nothing dials the live Caddy admin chain (8481/8482) or any live unit; the preview AST tripwire stays green; vitest offline; no paid call.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. `/deliverables/:id` renders detail + lineage + all five doors from a served body; closed doors render reasons, open doors are controls; NO door is invented and none is hidden (driven from fixtures in both availability directions).
2. Diff: split AND unified views render a real served unified diff through parseDiff/Diff/Hunk; no second diff parser, no server-markup assumption (scan: the view consumes `Comparison.unified` only); highlighting client-side.
3. Round-over-round is the no-param default; revision navigation drives explicit pairs incl. old=0; the rendered pair is always labeled (driven).
4. Every surface type walk: line-diff, extracted-text (label shown), image-pair (trio + changed verdict + per-side cards), binary-cards (metadata + hash + download per OQ2b), PDF-extraction-failure degrade body, unknown-surface forward-tolerance — each from a fixture, each honest (driven).
5. Comments render at server placements for all five statuses; drifted/file/orphan visibly distinct; the synthetic strip shows every unanchored comment; **the no-invisible-comment invariant is a driven test over a five-status fixture**.
6. Comment create: line selection composes the claimed anchor from rendered hunk data; POST body carries the closed severity vocabulary; the response's born status renders (incl. the server-corrected case: claimed line ≠ quote ⇒ drifted/file rendered, driven); file-level entry works; **no update/delete affordance exists (DOM + code scan)**.
7. Lifecycle: open vs consumed render distinctly; consumed shows [F#] + consumed_at + attempt ref; no drain verb exists client-side (scan).
8. Request-revision door: the answer flow posts the door's ask + pin + `revise_with_guidance`; stale ⇒ re-read; the finished limb links follow-up preset `revision` (driven both limbs).
9. Try-it: launch renders a backed session's URL + multi-port picker; every non-backed disposition renders its reason as an answer (fixture per state); NEVER an empty/broken iframe (driven: no iframe element renders for non-backed states); list + stop work; stop's partial-failure stays retryable as served.
10. Dual-iframe: both srcs compose EXACTLY from served URLs + the shared path; the shell-driven sync updates both on path change; the in-frame-navigation honesty text + re-sync affordance exist; single-instance renders one frame + the served no-before reason, sync disabled (driven; jsdom asserts attributes/state, loads nothing).
11. Accept card renders every served field (trailers byte-exact, signing posture, tier statement, pin); acceptable:false renders the reason with no act affordance.
12. Accept act: PIN collected and sent same-request (asserted in the POST body; never in DOM/state after submit); outcomes driven — applied (commit/effect/superseded/routed_runs), merge card (three answerable:false statements + follow-up link + durability text + nothing-was-pushed), read-back applied:false, superseded 409, stale 409 re-rendering the carried AcceptCard with a change notice (exactly one POST — no blind retry), pin_required/pin_rejected re-prompt.
13. Authorship presentation: owner sees the accept form; a non-owner (operator fixture) sees read-only + the authorship line; the server 403 path is still driven (test hits the verb as non-owner and renders the refusal).
14. Workforce roster per OQ3: every visible worker renders identity/status/domain-maturity/version+provenance/first-N; the EMPTY roster renders the no-standing-army state (driven both ways); **zero mutation affordances (DOM walk + scan: no POST from map modules except none)**.
15. Equipment: requested (tools/skills/knowledge/connectors) AND granted guardrails render, distinguishable; helpers per OQ5's disposition, nothing invented.
16. Multi-stage: an automation fixture renders its step chain in order with approval nodes marked; agentic renders selectors/profile (driven).
17. Per-version views per OQ4: figures render as served/read (no client or transport arithmetic on money — scans extended); empty outcomes render as honest absences (driven).
18. TBD-P3 resolution: the dated note exists in the new CONVENTIONS section; the map ships on owned rendering (or the ratified alternative), and no unratified dependency entered (lockgate + lock byte-check).
19. Escape: the scan passes over the grown tree with the allowlist per OQ1's disposition (expected EMPTY); the stale-promise texts are corrected with the dated note; iframe srcs compose only from served preview URLs (test); no srcdoc (per OQ1); the floor moves and names the new views.
20. Live wiring: declared types with reasons; frames trigger re-reads; debt honored; stale visible; post-verb re-reads (driven via FakeSource + doubles).
21. Fixtures: every new fixture is producer-driven (real AddComment/revisions/accept-composition/CreateDraft→Approve per OQ8's disposition); `TestWebAPIFixtures` + stability green; compare-only in CI.
22. Counters hold at their launch-restated values (lock, ⚙ 118/33 byte-unchanged, migrations 0001–0019, inventory 91/5/96, conformance, catalog 50); Go-side diff shows ONLY dispositioned additions with three-way owner-scope tests; CONVENTIONS appended, earlier sections untouched; **full battery green both sides, vitest offline, $0, no live-Caddy touch (tripwire green)**.

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — the escape-allowlist resolution (grounded finding D).** An `<iframe src>` trips none of the seven banned tokens — **the allowlist does not need to move**; but the scan file, §41-B and §43 all promise "widens at B6-8", so silence would leave a landed promise false. **RECOMMENDED: (a) allowlist stays EMPTY forever-until-needed; B6-8's commit corrects the scan header + assertion message to record the resolution with a dated note ("the sanctioned channel landed as iframe-by-src — outside the banned-construct set; the allowlist never widened"), plus one hardening limb: add `srcdoc` (split-written) to the banned tokens with a planted probe** — srcdoc IS raw-HTML-by-attribute (exactly the banned class), this packet introduces iframes, and the constraint "src only from served preview URLs" gets a structural backstop. Alternative (b): widen the allowlist for the review view file — REJECTED-leaning: nothing in the view needs a banned construct, and an unneeded allowlist entry is a standing hole. Dispose (a)/(b) + whether the srcdoc token is in scope.
- **OQ2 — the dual-iframe sync contract reading.** Grounded: cross-origin embeds; no scriptable sync; the server serves `sync.mode:"path"`. **RECOMMENDED: ratify the shell-driven reading in R9** (shared path control mirrors path+query to both srcs; in-frame navigation unobservable and stated; re-sync affordance) as the honest v0 meaning of S13.8's "synced navigation" — no postMessage protocol (would require injecting into arbitrary user apps), no proxy rewriting. Dispose the reading + whether the re-sync affordance suffices.
- **OQ2b — object bytes for images + binary download (grounded gap E).** No route serves object-dir content; the image trio and download-to-inspect cannot render without bytes. **RECOMMENDED: one additive owner-scoped read, `GET /api/deliverables/{deliverable}/objects/{sha}`** — resolves the sha against the deliverable's OWN revisions' ObjectRefs (never an arbitrary-sha oracle), streams via the store's exported `ObjectPath`, `Content-Type` from the ref's recorded type with a safe default (`application/octet-stream` + `Content-Disposition: attachment` for non-image types — served bytes must not become a second raw-HTML channel; images inline for the trio), 404-before-403. Alternatives: base64 inside Comparison (bloats every compare read) — rejected-leaning. Dispose route + the inline-vs-attachment line.
- **OQ3 — the workforce roster read (grounded gap E).** No list verb exists; equipment requires the store's own tamper-checked template walk (`activeDefinition` unexported), so transport SQL would duplicate a reader (the §40-C D3 twin hazard). **RECOMMENDED: narrow exported read(s) in `internal/worker` (the B6-3C `browse.go` precedent — a REPORTED widening, declared not silent): e.g. `Roster(ctx) → []RosterEntry{Template, active Version, Equipment (parsed via the store's walk), Guardrails, Domain maturity, latest validation/revalidation stamps}`**, served by a new `internal/api` read (`GET /api/workforce`?) — visibility: own personal + all household for members; **operator sees all** (workers are audited machinery with D10 operator gates — the runs/telemetry precedent, NOT the memory content line; flag if the coordinator reads S15.10's identity rules stricter). Dispose the read home, the route name, and the visibility line.
- **OQ4 — the per-version quality/cost read (grounded gap E).** The S08.4 join exists nowhere; the Layer-1 catalog is AT its band ceiling (50/50 — a catalog query forces the gate-flagged band revisit). **RECOMMENDED: an api-side derived read (the §42-B decisions-block precedent)** — bounded join over `run_events` `routing.decided` (worker+version per run) ⋈ `verdict.recorded` per run ⋈ receipts via the landed `MeterReader.RunMeter` (money READ, never computed), owner/visibility-scoped like OQ3, served per version with honest absences; empty at v0 is the truthful render. Alternatives: (b) catalog query — breaches the band (gate matter); (c) the join inside `internal/worker` — widens a leaf's imports into verify/metering territory (wrong home). Dispose (a) + the row shape.
- **OQ5 — "helpers" on the equipment card.** S15.10 names "(tools, knowledge, permissions, helpers)"; the S08.1 schema has NO helper field (helpers are per-run routing, S08.8 step 5). **RECOMMENDED: render the registry's own vocabulary (tools/skills/knowledge/connectors + guardrails-as-permissions) and render "helpers" as the honest structural statement** (helper selection is per-run through the same routing pipeline; no standing helper roster exists) — optionally, if OQ4's read lands, show helper-spawn routing causes among the version's outcomes. Never a fabricated helper list. Dispose the mapping.
- **OQ6 — TBD-P3(workforce-map rendering approach).** **RECOMMENDED: OWNED rendering** — semantic HTML/CSS (grouping by domain/kind, cards, the §42 view idiom) plus owned inline SVG only if an edge/connection drawing is genuinely needed. Grounds: household-scale roster (S08.8's own "~15 active candidates" line), EMPTY at v0, view-only with no interaction physics; a layout library (reactflow/dagre/elkjs class) is a NEW adoption needing the full S16.4 rail + gate ratification, indefensible for rendering an empty-to-tiny read-only list. The disposition IS the dated TBD resolution; the executor records it verbatim in the new CONVENTIONS section. Dispose (owned vs rail-walk).
- **OQ7 — "multi-stage connections" reading.** **RECOMMENDED: ratify that at v0 this grounds to the registry's own multi-stage data — kind=automation workflow step chains (automation.Workflow) — plus each worker's selector/duty facts**; the S06 task pipeline is task detail's story (B6-5), not the map's. Dispose the reading.
- **OQ8 — fixture depth for the heavy shapes.** Producer-driven golden fixtures need the accept composition (fake pusher + t.TempDir git) and possibly preview manager composition (`NewCaddyClient("","")`) inside `apifixtures_test.go`'s fixed-clock world. **RECOMMENDED: golden fixtures for the read shapes (detail/compare-per-type/placed-comments/accept-card/roster/version-views); driven-double tests (not golden fixtures) for the accept OUTCOMES and preview comparison** if composing those stacks under the fixed-clock rule proves disproportionate — each downgrade named in the new CONVENTIONS section. Dispose the line.

## 9. SCOPE VERDICT

**NOT one opus sitting — the queue row already pre-authorizes the A/B stop, and this grounding confirms it:**

- **A — review + try-it + accept** (R1–R10 + their slice of C; OQ1/OQ2/OQ2b/OQ8 land here): the diff widget wiring, per-type surfaces with degrade lanes, the comment loop, doors, previews, the dual-iframe shell, the full accept flow.
- **B — workforce map** (R11–R15 + the rest of C; OQ3–OQ7 land here): the dispositioned reads, the roster/equipment/connections/version views, the TBD resolution.

Order **A → B** (A is the packet's headline surface and exercises the fixture patterns B's reads reuse; B additionally waits on the OQ3/OQ4 Go-side dispositions). If the executor reaches the seam green with budget, same-sitting continuation is sanctioned; stopping at A-landed is equally a success mode. Within A, a further natural pause exists after R7 (review loop) before R8–R10 (try-it + accept) if the sitting demands it — a stop there is a declared success mode too, per the B5-3 precedent.
