# P3-GF13 — The server-authored jargon purge (plain words on every requester surface)

**Grounding brief, 2026-08-28.** Backend four-stage packet (small). Single-use; prior briefs are
EXPIRED. Truth is: the code (read at commit `61d7df7`), the frozen spec drafts, the operator
records (`P3/design/b6-gate-operator-findings-r4-2026-08-23.md` §F5 + Appendix A RA-5..RA-13;
r5 §C rule 4), the walk records (`P3/design/walkf1-findings-2026-08-27.md` W4/W5/W6), and the
GF9/GF13 entries in `P3/STATE.md`. Every load-bearing claim in §§1-2/4-6 was audited against a
file read or command run in this grounding session; §3's tree-wide census was produced by a
serving-path-tracing sweep whose claims were spot-audited (three re-verified verbatim) — the
executor verifies each §3 line it acts on. File:line refs are to `61d7df7`.

**THE RULE (operator-ratified across r4/r5/the walks):** requester-facing content speaks plain
words. Spec citations live in CODE COMMENTS (house style, expressly kept) and ops logs — never on
the wire to a requester surface. Telemetry/ops surfaces (control.log, panels explicitly labeled
technical detail) may keep precision; §§30/40/44's content-vs-telemetry line governs which
audience a string serves. **The register bar is `internal/verify/bootstrap.go:47`
(`BootstrapPostureNote`)** — the GF4-landed bootstrap disclosure the walk called "the strongest
surface of the walk": complete, honest, zero citations. Every rewrite is drafted BY THE EXECUTOR
at that bar; this brief never dictates replacement wording, only what must be gone and what the
sentence must still say.

**Register duties on every rewrite:** (a) the sentence keeps every FACT it carried — a citation
is dropped, never the claim it decorated; (b) honest-absence and refusal sentences stay honest and
specific; (c) where a citation was doing real work (distinguishing two facts, naming a seam), the
plain words must do that same work (R7's distinctness duty below is the sharp case); (d) the
landed plain copy (BootstrapPostureNote, the New-project door, the GF12 cards) is the style canon.

---

## 1. Numbered requirements — the enumerated floor (verify, rewrite, move pins)

Each site: the exact current string → the required register. RED tests (§5) pin most of them.

**R1 — the research-decision card + the planner's marker echo (intake).**
- `internal/intake/pipeline.go:1208` — Summary `"The task depends on facts in the world, but the
  plan still lacks required research steps for: " + joinKeys(res.MissingResearch)`.
  `MissingResearch` is raw P47 rule IDs (`internal/intake/spine.go:143` appends `h.RuleID`), so
  the card literally says "…research steps for: P47-5". The seed table
  (`internal/intake/triggers.go:140-188`) carries a plain `Class` per rule ("Prices & costs",
  "Schedules & external events", …) — the served sentence names the CLASS (or the cue/plain
  subject), keeping the rule id in `Detail` only if the executor judges `Detail` machine-shaped
  (it is `[]string` of ids today; a machine member may stay machine — the SENTENCE may not).
- `internal/intake/pipeline.go:1215` — Help What: `"Live research is required by policy for
  data-bearing tasks (1.9)."` → plain (what "data-bearing" means to a person, why live research).
- The "Research finding P47-1…" marker text the GF9 builder saw is MODEL-AUTHORED: the planner
  receives raw rule ids as `data_hits` in its input (`internal/stage/engines.go:609-613`) and
  echoes them into marker/assumption prose. Fix at the prompt: add a plain-words rule to
  `pairSchema` (`internal/stage/engines.go:383-423`) — clarifications/markers/assumptions speak
  to the requester in plain words and never quote internal rule ids or spec section numbers.
  `pairSchema` pins are Contains-based (`gf8prompt_test.go`, `gf12prompt_test.go`) — additive-safe.
- Park/resume reasons served on the task timeline (walk W4 "timeline events"):
  `internal/intake/pipeline.go:656` + `:1816` `"intake gate open…— gates wait (S06.1)"` (both also
  interpolate a raw card-kind token — name the card in plain words), and
  `internal/intake/answer.go:129` `"intake gate answered: pipeline resumes in place (4.3)"`.

**R2 — `buildApprovalCard` cost/size lines (intake).**
- `internal/intake/pipeline.go:1846` — `"~%.2f USD (API-equivalent, D5)"` → plain (what the walk's
  own receipt copy already does well: "no dollar price exists for these calls…"). FE note: the FE
  regex-repairs exactly this shape (`web/src/Inbox.tsx:1579 plainCostLine`) and FALLS BACK TO
  VERBATIM on non-match — a server-side plain rewrite is safe and renders as written; the regex
  becomes dead code (recorded FE debt, §4).
- `internal/intake/spine.go:121` — sizeDelta incomparable: `"size classification cannot be
  compared numerically (unpriced side) — surfaced, never silent (2.5)"` → plain. (:125/:127's
  over/under details name a ⚙ key — the ⚙ key is a settings identifier the operator can act on;
  executor's audience call, defensible either way, record it.)

**R3 — routing `plain_reason` (worker + intake) — the r4 RA-6 headline.**
`internal/worker/routing.go`, all served sentences (PlainReason, pin refusals, gap advice):
- `:1156` `"No specialist fits; running as generalist-with-injected-knowledge (the default for
  one-offs, S08.8)."` and `:1158` the DEGRADED-MARKED variant with `(S08.7)`.
- `:918` `"Runs on %s (%s lane) per the %s duty seat; flat-rate coverage bound (D5)."` — the duty
  NAME interpolated is also a machine token; say what the seat does in words.
- `:923` `" Research nodes route on the search-capable lane (S08.8 step 4)."`
- `:306-307` pin refusal `"…subscription coverage binds every choice (S08.8 step 3)…"`.
- `:993-995` pin-unhonorable refusal `"…(S08.8 step 3; seat rows are the lane documents' own,
  S03.6)"` — served on the wire per LN-9's honest-refusal contract; the refusal STAYS a refusal
  with its facts, minus the citations.
- `:1002-1003` honored-pin sentence `"…(S08.8 visible-and-overridable [S00.9 A13]; never
  dollars — D5)."`
- `:884` + `:908` `"Subscription gap (2.7): …"` advice sentences.
- RA-6 also flagged the leaked irrelevant candidate evaluation ("'release-notes-writer' cannot
  write it") — candidate/no-fit sentences that ride the card are part of this site: keep the
  candidates honest, drop worker-internal vocabulary where a plain phrase exists.
- `internal/intake/route.go:275` + `:284` override prose `"(override recorded, S08.8)"`.
- OUT of R3: `:906 fmt.Errorf("… (D5/G1 P7)", ErrInvalid)` is an internal structural error (ops);
  the executor's sweep adjudicates every remaining routing.go string by audience.

**R4 — `ProjectList.Visibility` (api).** `internal/api/projects.go:284-287`
`projectsVisibilityRule`: `"(S13.7)"` + `"D10 is authority over what the platform DOES"` → the
same three facts (whose projects appear, pending-onboarding visibility, why the operator bit opens
nothing) in plain words, bare `D10` gone. The memory family's sibling
(`internal/api/memory.go:188-190`, "(S4.1)"/"(S4.5)"/"D10") and the workforce roster sentence
(`internal/api/workforce.go:411`, "— D10") are the SAME class on member-readable surfaces — in
scope via the sweep (§3), same treatment.

**R5 — the onboarding card (project + stage).** The card renders
`"Danger zone: <path> (<action>) — <rule>"` (`internal/stage/onboard.go:287-295`) from the
always-on rules `internal/project/scan.go:139-140`:
- `"credentials are broker-held, never in a workspace (D2)"` → plain.
- `"never force-push a protected ref — accepts only (Spec S13.6)"` → plain.
- The raw glob `**/*credential*` is a MACHINE VALUE (DangerZone.Path, a drift-hash input via
  `SourceHash`/`scanHash` — `scan.go:111-144`): **do not change the Path.** The requester-facing
  fix is at the CARD LINE: the stage's rendering says in plain words what the pattern covers
  (e.g. names-containing-"credential", any folder) beside or instead of the bare glob — executor's
  shape, but a person who cannot read globs must understand the line (walk W4).
- `stage/onboard.go:428` park reason `"onboarding draft awaiting owner approval — gates wait
  (D10)"` → plain.
- Scan-hash notes: rule copy feeds `scanHash` for NEW scans only; project tests pin determinism,
  not golden hash values (`onboard_test.go:131`) — no pin move. Stored captures in existing worlds
  keep the old strings as immutable history; that is honest and expected.

**R6 — the commands-write detail (api).** `internal/api/projects.go:951` (`"(S07.8)"` in the
missing-body refusal), `:1010` (`"(S07.8)"`), `:1013` (`"(S07.3)"`, `"(S07.8)"`) — the three
`commandsDetail` sentences and the refusal keep their three distinct FACTS (retry-answer /
cleared-means-bootstrap / captured-means-next-round-ladder) minus citations. GF5's Go tests pin
only robust substrings ("bootstrap", "Inbox", version words) — no Go pin move; the
`project-commands.json` fixture moves (§4).

**R7 — the verdict note (verify) — with a DISTINCTNESS DUTY.**
- `internal/verify/pipeline.go:776-777` Finding.Text
  `"did-research-actually-run undecidable for %s [%s]: %s"` (StepID, RuleID, Detail) → plain words
  naming WHICH research question (the rule's plain class/subject, not `[P47-1]`) on WHICH step.
  **The P3-RW-18 D3-R2 lesson binds:** two rules on one step are two facts — the rewritten text
  must still differ per rule (the seed table's Class strings are distinct per rule id), and the
  `Anchor` (`"step:"+StepID`, `:775`) is the cross-round suppression key — BYTE-UNTOUCHED.
- `internal/verify/v1.go:587` Detail `"per-step usage counters not wired (S10 seam; B2-4) —
  recorded, not silently passed"` (and `:598`'s plain sibling is the register example) → plain.
- `internal/verify/pipeline.go:759` Detail suffix `" (re-run seam not wired; B2-4)"` → plain.
- `internal/verify/pipeline.go:788` escalation Summary `"research node(s) never invoked a research
  tool (1.9) — correctness on world-facts is never left to model memory"` → plain (the second
  clause is already the right register).

**R8 — accept tier/signing statements (api).** `internal/api/accept.go`:
- `:109-111` signing Statement `"…all-or-nothing per user (S13.6 step 5)…"` → plain.
- `:212-219` the three tier statements, each ending `"(S15.6; S01.9)"` → plain. **Negative pin
  kept:** the local-arm statement must keep NOT claiming a push
  (`gf11accept_test.go:149` asserts `!Contains("pushes a commit")`) — the rewrite preserves the
  per-arm truthfulness that test protects.
- `:437-438` `attributionAbsence`'s `"…naming nobody (S13.6 step 3)"` — GF10 put this reason on
  the card's FACE, which makes its register requester copy. All three branches stay
  branch-truthful (which fact is missing), minus the citation.
- Sweep-adjacent accept strings (executor adjudicates by audience, same file): `:328`
  `"(S13.1/S13.6)"`, `:704` `"(S13.6 step 1; S1.11)"`, `:709` `"(S13.6; S02.8)"`, `:715`
  `"(S13.6; S13.7; S02.8)"`, `:738` `"(S15.2 retry-safety)"`, `:775`/`:788` `"(S15.2)"` — these
  are served Detail/refusal sentences on the accept door; the walk read this family. Same rule.

**R9 — `UnderstoodItem` label mapping (intake) — a VALUE fix, not copy.**
`internal/intake/pipeline.go:888-908` (`understoodBlock`) and `:957-985` (`reviewQuestions`)
serve `UnderstoodItem.Value = SlotResolution.Value` — the machine option token ("graceful",
"simplest", "plain", "english"), while the taxonomy Option carries the plain Label the requester
actually clicked (`taxonomy.go`, e.g. `edge_cases`' "Carry on with a sensible default and make a
note of it" ↔ `graceful`). **Required shape: ADDITIVE.** Serve the option's Label in a new member
on `UnderstoodItem` (`internal/intake/cards.go:115-126`) resolved at composition time (match
`r.Value` against the slot's Options; own-words answers get no label — the value IS the words).
`value` stays BYTE-UNCHANGED on the wire: it is the round-trip machine key — the FE seeds the
"fix this" edit box from it (`web/src/Intake.tsx:1347`) and the answer fold matches option values
(`hasOptionValue`, `pipeline.go:989-996`); replacing it would turn a re-submitted choice into
free text. The stored `State.Resolutions` are untouched. Carried-over slots (`:905`, slot outside
the active set) have no options to label — honest absence. FE rendering of the new member is a
NAMED RIDER (§4), not this packet's.
- `understoodRecap`'s escalation items (`:929-934`) already serve the question + the requester's
  own answer — no token, no change.

**R10 — the per-AC EARS echo: DETERMINED FE-COMPOSED, out of this packet's edits.**
The wire serves `structured_kind: "ears"|"gwt"|""` — a machine value (`intake/artifact.go:41`).
The sentence the walk quoted is the FE's own translation
(`web/src/TaskDetail.tsx:397-403`: `'written as strict when-X-then-Y requirements (the EARS
form)'`), and `web/src/Inbox.tsx:2258` renders the RAW token (`({ac.structured_kind})`) on the
approval card. **No backend change.** Both FE sites go on the FE-handoff list (§4) with this
brief's determination as evidence. The prompt (`engines.go:395` "plain phrasing on every
criterion") already forbids the model explaining its format inside `plain`; if the executor finds
model-echoed format prose in walked artifacts, one added prompt sentence rides R1's prompt rule.

**R11 — raw ISO timestamps (W5): DETERMINED FE-OWNED AND ALREADY FIXED; server half SETTLED.**
Servers serve RFC3339 data fields (correct; machine values). The inline raw-nanosecond display
was the FE's D5 rendering, amended by GF9 (`web/src/ui/Timestamp.tsx:12-18` — the verbatim stamp
moved to `dateTime`/`title`, off the visible line). Grounding's tree-wide sweep (§3) verified
**no server-composed prose sentence embeds a raw RFC3339Nano timestamp anywhere** — every
`Format(time.RFC3339Nano)` in non-test `internal/` writes a dedicated struct field or SQL column,
never a `Sprintf` sentence. No change; the executor re-confirms only if it touches those files.

---

## 2. The frozen DO-NOT-TOUCH list (enumerate-and-avoid; every one verified at `61d7df7`)

The RW-19 frozen wire literals and their satellites — **byte-untouched, whatever the sweep says**:

1. `internal/run/canceldetail.go:31` — `CancelCauseHuman = "human cancel (4.5)"` (frozen wire
   literal, HANDOFF §5) and the detail keys `cause/actor/ladder_invoked/ask_id/reason`.
2. `internal/history/catalog.go:258-271` — the SQL literals `'requester cancelled'`,
   `'requester: rethink'`, `'human cancel (4.5)'`, `'no reason given'` (the
   `status.tasks_cancelled` two-leg read).
3. The two ledger cancel MINT PREFIXES: `"requester cancelled"` (`internal/stage/answer.go:117`,
   `internal/stage/ladderanswer.go:171`, `internal/intake/answer.go:616`/`:977`) and
   `"requester: rethink"` (`internal/intake/answer.go:471`) — the divergence pin
   (`internal/history/cancelrow_rw19_test.go` + `TestCancelMintPrefixesStayInTheAsciiSubset…`)
   holds them; the PREFIX bytes never change. Text AFTER a prefix (e.g. "at the X card") is
   theoretically editable but this packet keeps the whole cancel-mint family untouched —
   cancel-why is landed, walked, and not on the jargon lists.
4. `internal/api/reads.go:1136` `humanCancelCause` + `:1142` `cancelLedgerPrefixes` (the reader
   half of the same contract).
5. The refusal code `reason_too_long` (`internal/api/actions.go:86`) — codes are machine values.
6. The cancel-family "(4.5)" prose siblings (`stage/cancel.go:283` mechanical sentence,
   `stage/answer.go:118`/`:136`, `stage/ladderanswer.go:172`, `intake/answer.go:129` is R1's,
   the rest stay) — frozen-adjacent: OUT of this packet except `intake/answer.go:129` (a resume
   reason, not a cancel mint; verified no test/SQL matches it).
7. `internal/intake/artifact.go` markdown renderers (`renderSpecMD`/`renderPlanMD`, incl.
   `[P47-x]` at `:624`/`:715` and the `(kind)` sub-line at `:604`): the SPEC/PLAN markdown is the
   D9 artifact of record with a BYTE-STABLE re-render duty (S06.6 resume integrity; the
   CitedEntries precedent, `:656-661`). **No edit.** The record keeps machine ids — surfaces that
   display it may translate (FE-handoff note, §4). Same for any other renderer whose output is
   hashed or re-render-compared.
8. Machine VALUES everywhere: card kinds, `structured_kind`, option/choice `Value`s, DangerZone
   `Path` globs, `plain_reason`'s sibling STRUCTURED members, event types (`intake.state` on the
   timeline is a TYPE, not prose — FE display concern), ledger author kinds, landing vocabulary
   (`remote-push`/`local-store`/`decision-record`), refusal `Code`s. Prose changes only.
9. All engine-pin/adopt-rail strings, `Spec/`, `Docs/`, `Research/`.

## 3. The sweep — RUN AT GROUNDING (tree-wide, serving-path-traced); the executor TRIAGES

Grounding swept every NON-TEST string literal under `internal/` for the classes
`\(S[0-9]+\.` · `\(Spec S` · `\(D[0-9]+` · `P47-` · `\([0-9]+\.[0-9]+\)` · bare tokens
(`\bD10\b`, `B[0-9]-[0-9]`, `A1[0-9]`), tracing each hit's serving path. **Census at `61d7df7`:
≈372 class-(a) requester-surface hits · ≈96 class-(b) ops/log · ≈118 class-(c) error strings that
reach the wire only through the `writeSurface` fallback.** Three spot-checks re-verified by the
coordinator against the files. The four conduits that carry most of class (a):

1. `ledger.RecordDecision(…, text, reason, …)` → `internal/api/reads.go:1555` →
   `decisions[].reason`/`decision` on the task detail;
2. `run.TransitionOptions{Reason:…}` → `internal/api/projection.go:1262` → `last_activity.line`
   on run cards (park/resume/scheduler/recovery reasons);
3. intake/verify `Card` JSON served verbatim (Summary/Detail/Help/Question);
4. `SurfaceError.Msg` — and the FALLBACK `internal/api/intake_handlers.go:102`, which serves
   `Msg: err.Error()` for ANY unmapped error (the class-(c) conduit).

**The executor's duty is a TRIAGE, not a 372-string rewrite.** The partition, operator-rule
applied:
- **REWRITE (this packet):** the floor R1-R9 plus every class-(a) hit on the WALKER-FACING card
  faces and doors — intake cards (incl. `pipeline.go:491`'s fallback disclosure), verify
  escalation/ladder cards (`escalate.go:73-85/:231-232`, `pipeline.go:444/:625-626/:666/:672/
  :851`, `v2.go:324`, `bootstrap.go:132`), the accept door (R8's full list incl. `:328-353`,
  `:401`, `:704-738`, `:775/:788`, `internal/accept/accept.go:292`), the approvals inbox
  (`api/approvals.go:253-336/:601/:948/:1016/:1237-1392`), projects/commands
  (`projects.go:307/:621/:640/:877/:898/:922/:1024/:1041`), member-facing meters/memory/
  workforce refusals (`meters_verbs.go` list, `memory.go:188-190/:221/:440-446/:486-504/
  :553-555/:594/:642`, `workforce.go:411-497` + `worker/store.go:366-388`, `roster.go:232`,
  `lint.go`/`skill.go` finding messages), park/resume/`last_activity` reasons a walker reads on
  the task page (`stage/answer.go:81-220`, `resume.go:48-131`, `pause.go:42-100`,
  `scheduler/limits.go` + `scheduler.go:295/:366/:563`, `recovery/recovery.go:489-669`,
  `preview/manager.go` reasons, `run/freshness.go:87`, `metering/receipt.go:174`,
  `gates/effects.go:432`, `watchdog/suppress.go:58`/`sweep.go:201`).
- **KEEP-WITH-REASON allowed (list each in the landing report):** ledger `decisions[].reason`
  BASIS strings that name the deciding rule as audit provenance (`intake/answer.go` "why"
  arguments, `pipeline.go` stage-record reasons) — these are the platform's own audit record
  rendered on a record surface; the executor may keep citation-bearing BASIS columns while the
  DECISION sentence goes plain, or rewrite both — either way each kept string gets its one-line
  audience reason. Also legitimately keepable: `settings` registry Help/Ratified rows (operator
  settings surface quoting its own registry — the standing settings-tab directive owns that
  surface), `conformance/registry.go` Notes/Handles (operator-scoped gate cards whose PRECISION
  is the point), taxonomy/memory `Source` provenance strings, `eventlog` registry metadata,
  `triggers.go:140` Source + `TriggerHit.RuleID` (machine values).
- **OUT (recorded, not fixed here):** class-(c) wholesale — the `writeSurface` err.Error()
  fallback means every `fmt.Errorf` sentinel in intake/verify/worker/metering is one unmapped
  error from the wire. That is GF11's D-2 §38 class, STRUCTURAL, and a packet of its own:
  **flag it in the landing report as a deferred-ledger item** ("the api error-translation
  fallback serves raw internal chains — sentinel-not-text coverage owed"), rewriting only the
  (c) strings R1-R9 already touch. Class (b) untouched.

**Machine-token flags from the sweep (adjudicate, don't blanket-fix):** raw write-set globs in
plan markdown (`artifact.go:679-683` — record surface, keep), sha256 digests in
`decisions[].decision` (`answer.go:789` — audit record, keep or plain-summarize), `pin=%v` Go-bool
rendering (`answer.go:538` — rewrite with R3), served slot/step ids (`escalation-%d`, `step:`
floor-reason details — machine keys beside prose, keep the keys, plain the prose), ⚙ key names in
sentences (settings identifiers the operator can act on — keep), `conformance` `go test` handles
(operator runbook strings — keep), `local/manifest.go:143` Note (operator page — executor's call).

**The landing report proves the triage:** the method (grep/AST classes above), the rewrite set,
and the kept list with per-string one-line reasons. No NEW instance of the classes may land in
any served field.

## 4. Pin moves, fixtures, riders

**Sanctioned pin/companion-edit list (by name; nothing else moves):**
- `internal/worker/routing_test.go:219` (`Contains "generalist-with-injected-knowledge"`) — moves
  to the new plain sentence's load-bearing words.
- `internal/stage/route_e2e_test.go:205` (same literal in card JSON; `:235`'s bare "generalist"
  may survive — executor's call).
- `internal/intake/route_test.go:177` (synthetic PlainReason fixture — update for realism).
- `internal/verify/pipeline_test.go:212` (`Contains "undecidable"` on the CatResearchNotRun
  text) — moves with R7's rewrite.
- `internal/review/findingdedupe_test.go:43/65/92/96` (synthetic bodies in the old text shape —
  keep the fixture text faithful to the new copy; the dedupe keys are anchor-based, unaffected).
- The five GF13 RED test files (§5) flip to green and STAY as the standing guard.
- **Fixtures:** regenerate the golden wire fixtures whose bodies carry the old copy — grounding's
  scan: `web/src/fixtures/api/{project-commands,projects,accept-card,deliverable-detail,
  approvals,approvals-mine,memory,memory-operator,workforce,workforce-member,settings-member}.json`
  (11; the exact set follows the actual rewrites). **Vitest debt rule (gate item 12 / HANDOFF §5):
  this packet moves `web/src` fixtures, so the frontend battery (vitest + tsc) runs at landing,
  not just Go.** FE tests consuming fixture text may move with the fixtures
  (e.g. `inbox.test.tsx:2353`'s `plainCostLine` input becomes a museum shape — leave the unit test
  green as-is or update the input; the FUNCTION is FE code, do not edit it here).

**FE-handoff list (recorded for the exit-gauntlet builder, NOT this packet's edits):**
1. Render `UnderstoodItem`'s new label member (display at `web/src/Intake.tsx:1304/:1370`; edit
   seeding at `:1347` keeps reading `value`).
2. `web/src/TaskDetail.tsx:397-403` `structuredKindWords` register + `web/src/Inbox.tsx:2258`'s
   raw-token render (R10's determination).
3. `web/src/Inbox.tsx:1574-1583` `plainCostLine` becomes dead once the server speaks plainly —
   simplify or retire.
4. FE display of timeline event TYPES (`intake.state`) and card-kind tokens where server prose
   now names them plainly.

## 5. RED tests (committed with this brief; all verified RED at `61d7df7`, 9 tests / 5 files)

- `internal/intake/gf13jargon_red_internal_test.go` — sizeDelta plain-words; the four intake
  literals gone (AST literal scan, comments exempt); the understood LABEL served beside the
  unchanged machine value (fails today: no label member).
- `internal/api/gf13jargon_red_internal_test.go` — commandsDetail three branches;
  projectsVisibilityRule (class + bare-D10); the three tier statements, signing statement, and
  all three attributionAbsence branches.
- `internal/worker/gf13jargon_red_internal_test.go` — eight routing.go fragments gone
  (AST literal scan).
- `internal/verify/gf13jargon_red_internal_test.go` — four verify fragments gone.
- `internal/project/gf13jargon_red_internal_test.go` — two danger-rule fragments gone.

The reds assert ABSENCE of the old strings and the jargon classes on named surfaces — never the
new wording. The executor extends the class-assert pattern to the surfaces it rewrites where
cheap (the sweep proof may BE such a test if the executor prefers structure over prose proof).

## 6. Checklist (concretely checkable at landing)

1. All 9 GF13 reds GREEN; no red was weakened (assertions only extended).
2. R1-R9 rewrites landed; each old literal absent from the file's string literals; every rewritten
   sentence still states its original facts (spot-check against this brief's quotes).
3. R7 distinctness: two research rules on one step yield two DIFFERENT served texts; `Anchor`
   byte-untouched (existing `pipeline_test.go` dedupe coverage green).
4. R9: `UnderstoodItem` wire JSON carries label beside byte-unchanged `value`; `State.Resolutions`
   untouched; carried-over slots label-absent; existing intake tests green.
5. Frozen list §2 byte-untouched — `git diff` clean on `run/canceldetail.go`,
   `history/catalog.go`, `reads.go:1130-1145` region, the cancel mints, `intake/artifact.go`
   renderers; `cancelrow_rw19_test.go` + divergence tests green.
6. Triage proof in the landing report (§3's shape): method, the rewrite set, the kept list with
   per-string reasons — and the class-(c) `writeSurface` fallback FLAGGED to the deferred ledger
   (a sentinel-not-text coverage packet owed; not fixed here).
7. Pin moves only from §4's list; fixtures regenerated through the fixture harness (never
   hand-edited); **both batteries green: `go test -p 1 ./...` (48 pkgs) AND web vitest + tsc**
   (fixture-debt rule).
8. Negative pins hold: local-arm tier statement still never claims a push; `plainCostLine`
   untouched; no FE source edits at all (fixtures only).
9. gofmt/vet clean; no new ⚙ (118/33), no new dependency (lock 40), no migration (0001-0026).

**⚙: none. Adopted: none. Amendment: none expected** — copy is not spec mechanics. FLAG to the
coordinator if any rewrite site turns out spec-mandated VERBATIM text (grounding found none: the
S06/S13/S15 drafts mandate content contracts, not these sentences; the one verbatim family —
the RW-19 cancel literals — is §2's do-not-touch). **Batteries: single-package `-p 1`,
pgrep-guarded; serial per the standing directive; $0 — no engine calls anywhere in this packet.**
