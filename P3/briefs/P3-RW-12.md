**EXPIRED at landing 2026-08-13 (P3-RW-12 DONE+VALIDATED, eval FAIL F1–F10 → drain r1 `d3316b6` → re-check all-closed → drain r2 `a8e6870` → final re-check PASS). Post-drain: phraseMaxTokens=4000 (think-aware), T12 = the self-start live leg, §57 snapshot doctrine digest-enforced, provenance decisions row-based with unverifiable-skip posture, recap includes escalations. Never read as truth (amendment D); the two re-check reports are the authoritative record.**

# P3-RW-12 grounded brief — Deep-Plan interview depth: per-family taxonomies + the S06.5 phrase-and-summarize seat

**Packet:** P3-RW-12 · **Grounded:** 2026-08-13, against the post-RW-11 tree (RW-11 landed: red `e1dc1c9` + impl `b037ceb` + drain `e3384f0`) · **Spec:** `Spec/core-architecture-v1.md` S06.2/S06.4/S06.5/S06.6/S06.10, S09.2/S09.4/S09.10, S12.1/S12.4, S15.2 · **Directive:** operator, 2026-08-12 (P3/STATE.md log, authoritative): the generic 8-question interview is "way too thin" (~20% of the predecessor's planning experience); full per-family question sets at Deep-Plan depth for EVERY family — software includes technology/stack choice (prompted or shown for accept/change, requester assumed NOT an advanced programmer), asset/picture choices, look-and-feel; equivalents for research/content/data/chore; an understanding summary; goal = "the program knows exactly what the user wants". Harvest shape: `Docs/component-harvest-map-proposal-v1.md` N6 (Deep Plan engine) — this packet is the INTERVIEW-depth leg; critique/revise already exist (S06.8).

**Execution order note:** RW-12 executes BEFORE RW-13 (both touch `pipeline.go`; STATE 2026-08-13 orders them). §12 enumerates the touchpoints.

---

## 1. What exists (verified in code + spec this session)

- **The taxonomy rails (RW-11, landed):** six-value family vocabulary (`intake.Families()`, `ValidFamily`), `State.FamilySource` provenance (`registry|classifier|requester|default`), the `decision.family` pre-round card (`familyGate`, pipeline.go:684 — fires only on `FamilySourceDefault`, band never asked), `taxonomyFor` (pipeline.go:114 — unseeded family → generic set), the DISCLOSED fallback (`applyFamilyTaxonomy` returns the disclosure line, `recordTaxonomyFallback` ledgers it — pipeline.go:316–343), and `applyFamilyAnswer` (answer.go:207 — family swap needs no slot surgery; the next advance re-reads `taxonomyFor`).
- **`Taxonomy.Validate` structural rules** (taxonomy.go:46): id+version required, ≥1 slot, unique slot ids, positive weights, question text required, options 0 or 2–4 (free text is always available and is never an Option).
- **Seeds today:** `SeedTaxonomies()` = software (ClarifyCodeBench 10-type, v1, weights evidence-informed: failed types 12 > base 10/8 > natively-handled 6) + generic (8 slots, v1). CCB slot ids: `behavior, terminology, edge_cases, collection_semantics, comparison_rules, ordering_atomicity, indices_ranges, output_format, units, numerical_precision`.
- **The interview loop** (pipeline.go `phaseInterview`, 566–668): platform code — highest-weight unresolved slots first, ≤ `maxQuestionsPerCard` (=4, a ratified non-⚙ constant, CONVENTIONS §14), continues while Clearance < tier floor (⚙ 60/75/90), band never interviews, force-proceed converts to listed assumptions. Cards issue via `issueCard` (parks the run; ask row snapshot = the full Card JSON — the durable record). Two interview-card build sites: the re-interview card (:583–590) and the regular card (:603–610); the clarification card builds in `phaseSpine` (:810–817).
- **The Utility seam today:** `intake.Utility` has exactly `Help(ctx, pair)` (intake.go:363), consumed only in `buildApprovalCard`; nil/error → deterministic `defaultHelp`. The S06.5 phrasing/summarize leg is UNBUILT — cards render taxonomy question text verbatim (the operator's webshop saw the raw generic 8-slot set).
- **The local seat** (§26; stage/local.go): `localUtility` rides `local.AliasUtility` via `duty.Call(ctx, runID, DutyRequest{...})` — temp-0, engine-enforced ordered `json_schema` (reason-first), structural length caps (`helpMaxTokens=700` precedent), `Classification:false` for drafting (no forced abstain), ONE $0 D7 checkpoint row on the consuming run. Schema SHAPES live in `internal/local/schema.go` (`HelpSchema`, `orderedObjectSchema`); stage supplies values (import wall, AST-pinned both directions).
- **Metering does NOT bite here (verified):** `gates.ErrNotCheckpointable` admits running/draining only; `advanceLoaded` refuses to run unless the intake run is `running` (pipeline.go:526 `ErrNotRunning`), and every interview-beat and approval-card build happens inside it. The RW-11 constraint was Start-only (triage before the run exists) — that is RW-13's problem, not this packet's. A phrase/summarize call inside the interview rides `st.RunID` (post-rebind: the fork) in a checkpointable state.
- **8.3-gate machinery (S09, landed, §17):** `knowledge_entries` kind `taxonomy`; `memory.Gate` write path (`Import`, `NewVersion(actor, supersedesID, Draft)` — supersession, never in-place; S09.4 station 4); `EnsureB2SeedGovernance` (memory/seeds.go:109) already governs `seed-intake-taxonomy-software` and `seed-intake-taxonomy-generic` as house-scope file-backed entries (origin `imported`, originRef = the B2 ratification record, `taskType: "machinery:intake"` keeps them out of stage briefs), idempotent by entry-id existence, never resurrects, `ErrNoOperator` retry; wired at `internal/shell/shell.go:394`. **Note:** `b2Seeds()` marshals the LIVE `SeedTaxonomies()` — a fresh world would govern v2 content under the B2 provenance, and an existing world's governed v1 file would silently diverge from the runtime seed. R5 must close both ends.
- **S06.10 duty map (frozen text):** "Question/card phrasing, 13.5 help drafting, summaries → utility model (local, ≤8B at v0)". The S12.4 ceremony cut line's "Interviewing … NEVER local" governs interview CONDUCT (the Stage-1 planning session) — phrasing/summaries are the utility row in the same table. No tension.
- **In-code seeds remain the runtime source at v0** (§17; S06.3 posture): intake reads `SeedTaxonomies()`/`LoadTaxonomy` overrides; the governed file IS the operator-editable override input, proven equal by test. Consumer rewiring onto the store is NOT this packet's.

## 2. Requirements (numbered, each with its S-ref)

- **R1 — Software taxonomy v2 at Deep-Plan depth** [S06.5; operator directive]. Keep all ten CCB slots (ids, MustKnow rationale, evidence-informed weights, source citation intact). ADD three slots: `technology_stack` — plain-language options for a non-programmer, one option being the DEFAULT "you choose for me and show me what you picked" (canonical value `planner_chooses`; accept/change then rides the existing S06.9 approval card — restatement/steps show the choice, Re-plan contests it — no new machinery), plus options in the class of match-what-I-already-use / simplest-thing-that-works / I'll-specify (free text structural); `assets_media` — where pictures/images/media come from (I'll-supply / find-free-stock / generate / placeholders-for-now class); `look_feel` — style direction options + free text. Every requester-facing string plain language (the operator is the audience). Version `v2`; `Source` records drafting model+version, date, packet id.
- **R2 — Four NEW family taxonomies** (research, content, data, chore) at the same depth [S06.5; S09.10 row 1; directive]. Required coverage axes (the rubric; exact slots/wording/weights are the executor's drafting duty per R4):
  - *research:* the question to answer + the decision it serves; depth/rigor expected; trusted/excluded sources; recency bound; output form; balance/counterarguments wanted; what the requester already knows/believes (false-premise guard input).
  - *content:* what kind of piece + where it will live; audience; tone/voice; length/format; key messages that MUST appear; examples/references to imitate; asset/picture choices (the directive names them beyond software); review expectations.
  - *data:* the source data (where it lives, form, access); the question/transformation wanted; output form; correctness bar (spot-checked vs validated); messy/missing-data posture; privacy/sharing constraints.
  - *chore:* what done looks like each time; trigger/schedule; per-run inputs that vary; success signal + how reported; failure posture (retry/notify/stop); notification preference.
  Every set passes `Validate`; every slot has a stated MustKnow; wherever a technical choice appears, a `planner_chooses`-style plain default exists. `SeedTaxonomies()` returns all six families.
- **R3 — Generic set reviewed** against the same bar [S06.5]. It remains FamilyGeneric's own set AND the unseeded-family fallback (machinery stays; after R2 it no longer fires for any of the six vocabulary values in practice). Version bumps only on content change; report the outcome.
- **R4 — Drafting mechanics** [S06.5: "drafted at implementation time with the strongest available frontier model"]. The content IS drafted in the packet's execution session (frontier-class executor model); no copying CCB into the new families. Weights evidence-informed where evidence exists (CCB stays cited), otherwise reasoned AND stated (rationale in `Source` or slot comments). Provenance (model, date, packet) on every taxonomy.
- **R5 — 8.3-gate entry via the landed S09 machinery** [S09.10; S09.4 station 4; §17]. An idempotent governance function in `internal/memory` (the `EnsureB2SeedGovernance` discipline), shell-wired at boot beside shell.go:394: (a) creates four governed house entries `seed-intake-taxonomy-{research,content,data,chore}` (kind `taxonomy`, topicKey `intake/taxonomy/<family>`, `taskType: "machinery:intake"`, file-backed `intake-taxonomy-<family>.json`, origin `imported`, originRef = the RW-12 ratification record — OQ3); (b) SUPERSEDES the existing `seed-intake-taxonomy-software` (and generic, if R3 changed it) via `Gate.NewVersion` attributed to the operator when the governed active content differs from the current in-code seed — never in-place, old version retired (S09.8); (c) never resurrects removed/tombstoned entries; (d) `ErrNoOperator` → skip-with-log, retry next boot. Every governed active file parses via `intake.LoadTaxonomy` and deep-equals the in-code seed (the §17 proven-by-test rule: the governed file IS the operator-editable override input). What stays for the operator: ratifying the CONTENT at the packet gate — the free-text gate answer is the 8.3 approval (the B2 "operator import = the approval" precedent).
- **R6 — Per-request phrasing, invariant structural** [S06.5 sentence; S06.10 duty row; S12.4 utility row]. New optional seam (OQ1): the pipeline selects the slots FIRST (taxonomy-deterministic: highest-weight unresolved, ≤ `maxQuestionsPerCard`), THEN offers exactly those to the phrase call; the result is folded BY SLOT ID onto the already-selected questions — an id not asked is dropped, a missing id keeps verbatim text, `Options` (labels and values), slot ids, and question COUNT are byte-unchanged. Phrasing can reword, never add/drop/merge — enforced in platform code, never prompt-level (R03 §2.1 doctrine). The durable ask records BOTH: `Question.Text` stays canonical; additive `Question.Phrased` carries the reworded text (the ask-row snapshot is the record). Applied at both interview-card build sites (regular + re-interview).
- **R7 — The local adapter + metering** [S12.4; §26]. `stage/local.go` implements the seam on `local.AliasUtility`: schema shape `PhraseSchema(...)` in `internal/local/schema.go` (ordered, reason-first free text, then per-slot phrasings + summary), `Classification:false`, structural max-tokens constant (settings-tab-flagged, §26 precedent). The consuming run id is carried EXPLICITLY on the seam input (`PhraseInput.RunID = st.RunID` — the `DraftInput.RunID` precedent), so the ONE $0 D7 row rides the run the pipeline is driving now, post-rebind the fork. (Verified: interview beats only run under a running run — the RW-11 Start-metering constraint does not apply.)
- **R8 — Per-round understanding block** [directive; S06.5 "summarizes"]. Additive `Card.Understood` on interview cards (and clarification cards, OQ5): deterministic items composed from `State.Resolutions` against the active taxonomy — `{slot id, name, how ∈ registry|answered|assumption, value/assumption}` — ALWAYS present once ≥1 slot is resolved, platform-composed; plus optional utility-phrased prose `Text` ("here's what I understood so far"). Derive from `Resolutions` at card build — no new State field (the durable record already exists: resolutions ride every state event, the rendered block rides the ask snapshot).
- **R9 — Final understanding recap** [directive; S06.1's ratified 1.3 realization]. `buildApprovalCard` composes the FULL block (every resolution incl. force-proceed/band conversions, each origin-labeled, plus answered escalations) into additive `ApprovalLayer1.Understood`, beside `Restatement` — the deterministic slot-record complements the planner's prose restatement; the utility may phrase the prose line. **NO new blocking card**: S06.1 assigns restate-and-confirm to the approval card and approval is the confirmation; a pre-draft recap gate would add a click (S06.4 discipline). Band posture untouched (band builds no approval card; its auto-assumptions already list on the completion card — zero clicks stays zero).
- **R10 — Depth scales by tier through EXISTING knobs only** [S06.4]. Band: zero questions (existing guards byte-identical). Tiers: the ⚙ clearance floors (60/75/90) bound rounds — so the drafted weight distributions are a REQUIREMENT: for every seeded family, highest-weight-first cumulative clearance crosses 60 within the first 8 slots (≤2 cards) and 75 within the first 12 (≤3 cards) at the ratified defaults; high tier walks the full depth (uncapped, S06.5). No new ⚙, no per-tier question caps (G1 P8 rejected them).
- **R11 — Boundaries.** Zero `web/` edits (SPA rendering of `Phrased`/`Understood` is the builder's FRONTEND.md pass; family card answerable from the Inbox already proves the card path). NO Start/dispatch restructuring (RW-13). No new ⚙, no new dependency, no migration. Additive JSON everywhere: pre-RW-12 state events and card snapshots decode unchanged; answering a pre-RW-12 open interview card still applies.
- **R12 — Honest absence everywhere** [§26 degrade doctrine]. Nil/erroring phrase seam → verbatim canonical questions + items-only Understood + zero added clicks or cards; never a faked model output; the deterministic items are never presented as model prose. A phrase-call metering defect surfaces loudly (`local.unmetered_defect` path) — never a silently unmetered call.

## 3. Seams to respect

- `internal/intake` imports storage/eventlog/run/ledger ONLY (§14) — it never imports `memory` (the governance leg lives in `internal/memory`, wired by shell; the `CitedEntryVersions` precedent).
- `internal/local` imports storage/eventlog/gates/settings + stdlib ONLY (AST-pinned `importwall_test.go`); schema SHAPES in local, VALUES in stage (§26). The adapter lives in `stage/local.go` (the router.go bridge precedent).
- The phrase seam is OPTIONAL and the taxonomy is the coverage authority (S06.5: "phrases and summarizes but does not decide what must be asked"). The seat never decides; its output is human-gated by the card itself.
- The phrase prompt is an injection surface (request text in, requester-facing card out): containment is STRUCTURAL — fold-by-id, engine-enforced schema, length cap; quoted request material is data, never instructions (P-T05-3 posture). Never prompt-level lockouts (R03 §2.1).
- Ledger discipline (§14): no new ledger verbs; the interview stays event-sourced state + ask rows; artifacts untouched (SPEC/PLAN schema unchanged — the recap is card+state derived).
- Memory gate discipline (§17): supersession in one gate call (retire+replace, same tx inside `writeEntry`); house scope demands the operator role; tombstones never resurrected.

## 4. ⚙ settings (registry names; no new keys)

- Consumed, unchanged: `intake.clearance_floor.{low,standard,high}` (60/75/90 — R10's shape bar cites the ratified defaults), `intake.zero_interaction_cost_usd` (band, per-user), plus the §14 set (`size_recheck_factor`, `coverage_autofix_rounds`, `critique_revise_rounds`, `freshness.max_age`, `claims.default_write`) untouched.
- `local.alias` (TypeMap) resolves `utility` at call time — no write needed (code default already maps it; RW-11 precedent).
- **NO new ⚙.** The phrase/summarize max-tokens cap is a structural constant beside `helpMaxTokens` (§26 precedent) — flagged under the standing settings-tab directive. `maxQuestionsPerCard = 4` stays a ratified non-⚙ constant (§14). If the executor finds a genuinely needed new number, it IS a registered ⚙ with an S18 sweep — expected outcome: none.

## 5. Files expected to change

- `internal/intake/taxonomy.go` — six seeds (software v2, four new, generic reviewed); seed helpers per family.
- `internal/intake/cards.go` — `Question.Phrased` (additive), `Card.Understood` + `UnderstoodBlock`/`UnderstoodItem` types, `ApprovalLayer1.Understood` (all additive JSON).
- `internal/intake/intake.go` — the new optional phrase-and-summarize seam type(s) (`PhraseInput`/`PhraseResult` + interface; OQ1) with `RunID` on the input.
- `internal/intake/pipeline.go` — `phaseInterview` (weave: select slots → phrase → fold → set Understood; both card sites), `phaseSpine` (clarification card Understood, per OQ5), `buildApprovalCard` (recap), the new seam field on `Pipeline`. **Start, familyGate, applyFamilyTaxonomy, advanceLoaded bodies untouched.**
- `internal/stage/local.go` — `localUtility` gains the phrase-and-summarize adapter (AliasUtility).
- `internal/local/schema.go` — `PhraseSchema` (ordered reason-first).
- `internal/memory/seeds.go` (or sibling file) — the RW-12 governance function (four creates + supersessions).
- `internal/shell/shell.go` — boot wiring beside `EnsureB2SeedGovernance` (:394).
- Tests: `internal/intake` (taxonomy/pipeline/familygate), `internal/stage` (adapter/metering), `internal/memory` (governance).
- `P3/CONVENTIONS.md` — the packet's conventions delta (new § extending §14's seam list + seed section).
- **Untouched:** `web/`, migrations (0001–0024 stand), `Docs/`, `Spec/` (no amendment — §11), `surface.go`, artifact schema, `answer.go` (unless OQ5 rendering needs the clarification card builder — the ANSWER paths stay byte-identical).
- **Pre-existing test edits, sanctioned BY NAME:** `intake/taxonomy_test.go` `TestSeedTaxonomiesValidate` (pins software to exactly 10 slots — becomes the v2 shape assertion, CCB evidence pin preserved); `intake/familygate_test.go` `TestFamilyCardChoiceGenericExplicit`'s no-seeded-set subtest (its premise — a vocabulary family without a seed — vanishes with R2; retarget the DISCLOSED-fallback machinery onto a custom `Pipeline.Taxonomies` map missing a family). Any other pre-existing test that pins seed slot counts is enumerated in the executor report; tests that iterate `SeedTaxonomies()` dynamically (familygate_test.go:29,244,276 pattern) survive content growth.

## 6. Acceptance headline → checklist

Headline (STATE queue): *per-family Deep-Plan question sets for all five families + upgraded software set with stack/assets/look-and-feel; per-request phrasing + per-round understanding summary via the S06.5 phrase-and-summarize seat; 8.3 knowledge-gate entry.*

1. `SeedTaxonomies()` returns six validating sets; software v2 ⊇ the ten CCB ids ∪ {`technology_stack`, `assets_media`, `look_feel`}, with the `planner_chooses` default present.
2. Each of research/content/data/chore resolves to its OWN taxonomy at Start/family-swap — the disclosed generic fallback no longer fires for any six-vocabulary value (machinery retained for genuinely unseeded values).
3. Every slot honors Validate; weights carry stated rationale; every requester-facing string is plain language; every technical choice has a "planner chooses and shows you" default.
4. Interview cards carry `Phrased` beside canonical `Text` when the utility duty answers; the ask row durably records both; phrasing can neither add, drop, nor merge slots (fold-by-id proven against a hostile fake).
5. Absent/erroring seam → verbatim questions, zero added clicks, no fake — the interview is byte-equivalent to today's minus depth.
6. Every interview card carries "what I understood so far" (deterministic items + optional phrased prose); the approval card carries the full recap beside the restatement; no new blocking card anywhere.
7. Phrase/summarize calls ride `utility` on the consuming RUNNING run with ONE $0 D7 row (post-rebind: the fork); no `local.unmetered_defect` on the happy path.
8. Governance: four new governed house entries + software (and generic if changed) superseded to v2 via the gate; files parse via `LoadTaxonomy` and equal the in-code seeds; idempotent; tombstones never resurrected; skip-with-log without an operator.
9. Trivial band asks nothing (unchanged guards); every family's weight order crosses the low floor within ≤2 cards and standard within ≤3 at ratified defaults.
10. Zero `web/` edits; no new ⚙/dependency/migration; Start/dispatch untouched; pre-RW-12 state events and open cards decode and answer unchanged.

## 7. Acceptance-test specifications (SPECS ONLY — no red commits; standing red-hold)

- **T1 `TestSeedTaxonomiesCoverSixFamilies`** (intake, rewrites `TestSeedTaxonomiesValidate` — sanctioned §5): `SeedTaxonomies()` has exactly the six `Families()`; each `Validate()`s; software: `Version=="v2"`, slots ⊇ ten CCB ids + three new ids, `Source` cites 2607.00711 AND the RW-12 drafting provenance; evidence pin stays (`collection_semantics.Weight > units.Weight`); every taxonomy's `Source` non-empty.
- **T2 `TestSeedWeightsMeetTierFloorShape`** (intake): per family — sort slots weight-desc; cumulative weight% after `min(n,8)` slots ≥ 60 and after `min(n,12)` ≥ 75 (R10's bar at ratified defaults).
- **T3 `TestEveryFamilyUsesItsOwnSetNoFallbackDisclosure`** (intake): for each of research/content/data/chore: Start with a Registry seam handing that family → `st.TaxonomyID == "<family>"`, `TaxonomyVersion` = that seed's; ledger decisions contain NO "no question set is seeded" line. Companion: a custom `Taxonomies` map missing one family still yields the disclosure (machinery retained).
- **T4 `TestStackSlotOffersPlannerChoosesDefault`** (intake): software v2 `technology_stack` carries an Option with `Value=="planner_chooses"`; 2–4 options; the drafted label/question strings pinned exactly (content-ratification aid).
- **T5 `TestPhrasingFoldsOnlyAskedSlots`** (intake, hostile fake seam): fake returns phrasings for asked-id-0, asked-id-1, and an id NOT asked, omits asked-id-2, plus a summary. Card asserts: question count unchanged; ids exactly the pipeline's selection; Q0/Q1 carry `Phrased` with canonical `Text` intact; Q2 has no `Phrased`; no question exists for the foreign id; every `Options` slice deep-equals the taxonomy's; `Understood.Text` == the summary. Ask-row snapshot re-read shows both texts durable.
- **T6 `TestPhrasingAbsenceIsVerbatimZeroClicks`** (intake): nil seam and erroring seam both: card question set/text/count byte-equal the no-seam baseline; ask-row count identical; `Understood` items still present (deterministic), `Understood.Text` empty.
- **T7 `TestUnderstoodAccumulatesAcrossRounds`** (intake): registry pre-resolves one slot; answer round 1 with two answers → the NEXT interview card's `Understood` items = 3, `How` values `registry`/`answered` correct; after force-proceed the approval recap includes the converted assumptions with `How=="assumption"`.
- **T8 `TestApprovalCardCarriesUnderstandingRecap`** (intake): full walk to the approval card → `Approval.Layer1.Understood` non-nil, item count == `len(st.Resolutions)`, each value matches its recorded resolution; `Restatement` still present (complement, not replacement).
- **T9 `TestPhraseCallRidesTheConsumingRun`** (intake + stage): (a) intake: capturing fake asserts `PhraseInput.RunID == st.RunID`, and after a recovery-fork rebind equals the FORK id; (b) stage: the adapter against `local.FakeServer` writes ONE checkpoint row on the passed run with the `local` marker `duty=="utility"` — running state required, `ErrNotCheckpointable` posture surfaced loudly otherwise.
- **T10 `TestPreRW12SnapshotsDecodeAndAnswer`** (intake): fixture pre-RW-12 state-event JSON and interview-card snapshot (no new fields) unmarshal clean; answering the old open interview card applies via `applyInterviewAnswer` unchanged.
- **T11 `TestTaxonomyGovernanceCreatesAndSupersedes`** (memory): world with operator; a governed `seed-intake-taxonomy-software` row fixed at v1 content → run the RW-12 Ensure → four new active entries (kind `taxonomy`, correct topic keys/files); software superseded (old retired, new active, `supersedes_id` chained), content = marshaled in-code v2; each active file parses via `intake.LoadTaxonomy` and deep-equals `SeedTaxonomies()[family]`; second run creates 0 (idempotent); a tombstoned research entry stays dead; no operator → `ErrNoOperator`.
- **T12 (optional live leg, `SINET_LIVE_SMOKE=1`, $0)** — the §26 tier-L pattern: real stack phrases the software card for "Create a simple webshop for car parts" → non-empty `Phrased` for every asked id, schema-conformant, summary non-empty, the $0 D7 row present with model hash + engine build. Shape-only assertions (model output).

## 8. CONVENTIONS constraints that bind

§3 (test conventions; red-hold — SPECS only, executor materializes), §6 (⚙ by dotted key; no new keys), §13 (ledger fencing; state events at current generation), §14 (intake package map; `maxQuestionsPerCard` non-⚙; seeds operator-editable via strict-JSON loaders; absent seams degrade per spec, never faked), §17 (gate discipline; seed governance; engine-facing packages never import memory), §26 (import walls; $0 metering wire contract; per-duty caps structural; `Classification:false` for drafting; degradation honest), §43 (one list, two readers — any new card vocabulary follows it).

## 9. Open questions [OQ1..7] with recommendations

- **OQ1 — seam shape:** grow `intake.Utility` with a second method, or add a separate optional `Pipeline` seam? **Rec: separate optional seam** (working name `intake.Phraser`, one method `PhraseAndSummarize(ctx, PhraseInput) (PhraseResult, error)`): strictly additive (the landed `Utility` contract and every fake stay byte-stable), pair-independent input (Help is pair-bound; phrasing runs pre-pair), nil = verbatim. `stage/local.go`'s `localUtility` implements BOTH interfaces on the one `utility` alias — the S06.10/S12.4 duty ownership stays single.
- **OQ2 — call granularity:** one duty call per card carrying phrasings + summary, or two calls? **Rec: one** — one $0 row, one degrade point, the summary shares the same context; the schema is one ordered object (reason → summary → per-slot phrasings).
- **OQ3 — ratification record for R5:** **Rec:** the operator's free-text answer at the RW-12 packet gate IS the 8.3 approval (B2 precedent); the Ensure ships with originRef fields citing the RW-12 gate record (`P3/STATE.md` landing entry date + packet id); the coordinator confirms the dated record at landing before the wiring goes live.
- **OQ4 — phrase option labels too?** **Rec: NO at v0** — question text only; labels are authored plain-language content and values are canonical; the smallest invariant surface wins (scope guardrail). Free text is always available regardless.
- **OQ5 — `Understood` on clarification cards?** **Rec: YES** (same field, composed at the same `issueCard` boundary, zero extra machinery — a Stage-1 ask is a Stage-1 ask); escalation and family cards excluded (single-question mid-planning; pre-round nothing-understood).
- **OQ6 — weights for the three new software slots:** **Rec: the 10–11 band** — above the natively-handled types (6–8), at/just below the CCB empirically-failed types (12): for a non-programmer they shape the spec more than format/units, but the CCB failure evidence keeps its measured priority. Exact values executor-drafted with stated reasoning; operator ratifies at the gate.
- **OQ7 — band/trivial guards:** **Rec:** restate as explicit non-goal — the packet adds ZERO asks to the band; `familyGate`'s band guard and the interview's band skip stay byte-identical (T-coverage via existing `TestBandNeverAsksFamily*` remaining green by name).

## 10. Spec-conflict findings

**None.** Three readings recorded inside frozen text:
1. **Scope acceleration, not amendment.** S06.2 "v0 ships the software family plus a generic fallback set; further family question sets arrive with their domains" and the S06.10 Deferred row are a v0 FLOOR, not a prohibition; the operator directive (2026-08-12, STATE) ships the five sets early — operator-directed scope, additive (S15.2), no S00.9 amendment. Already bound in STATE's scope reading.
2. **Phrasing on the local seat is the spec's own assignment.** The S12.4 cut line's "Interviewing … NEVER local" governs interview CONDUCT; S06.10's duty table assigns "question/card phrasing, 13.5 help drafting, summaries" to the utility model (local ≤8B) — and S06.5 fixes the invariant this packet enforces structurally: the seat "phrases and summarizes but does not decide what must be asked".
3. **"The interview runs inside the Stage-1 planning session"** — the landed, gate-ratified B2 reading stands: the taxonomy carries coverage deterministically, platform code issues the cards, the planning session consumes the resolutions at Draft (`DraftInput`). RW-12 deepens content and adds the phrasing seat without moving the interview's home.

## 11. Watch notes / flags for the executor report

- **Governance divergence trap (found this session):** `b2Seeds()` marshals live seeds — after R1 a FRESH world's B2-Ensure would write v2 content under B2 provenance while an EXISTING world keeps a v1 governed file diverging from runtime. R5's supersession leg must close the existing-world end; for the fresh-world end either pin `b2Seeds()` to v1 fixtures or let the RW-12 Ensure own all taxonomy governance going forward (executor's choice, stated in the report).
- Content drafting is IN-SESSION work product: the report must state, per family, which slots are evidence-backed vs reasoned, so the operator's gate read is honest.
- Generic-set review (R3): avoid gratuitous change — stage tests walk the generic set dynamically but churn costs review attention; bump only on real improvement.
- The `Understood` `How` values reuse the `Resolved*` constants (machine field); plain-language rendering of them is the SPA builder's (zero `web/` here).
- Phrase-length cap sizing: one card = ≤4 questions + summary; start near the `helpMaxTokens` class (700) scaled up modestly; it is a structural constant — record it under the settings-tab flag.

## 12. RW-13 touchpoints (coordinator sequencing input)

RW-13 (Stage-0 classify moves onto the running run — evaluator-characterized shape, STATE 2026-08-12) shares `pipeline.go`. RW-12 lands FIRST (STATE order). Overlap map:
- `Start` (:134–301): RW-12 does NOT touch it; RW-13 removes the classifier leg from it.
- `advanceLoaded` (:515–562): RW-12 does NOT touch it; RW-13 inserts the run-once classify step at its top. RW-12's additions live in `phaseInterview`/`phaseSpine`/`buildApprovalCard` — disjoint functions.
- `applyFamilyTaxonomy`/`recordTaxonomyFallback`: both packets rely; RW-12 changes only the taxonomy MAP CONTENT, not these functions — RW-13 may re-run them post-classify unchanged.
- `familyGate`: untouched by RW-12; RW-13's classify-on-run makes it fire less often (classifier reachable at Start's successor point).
- The Stage-0 intake record's `taxonomy` field reads `<family>@v2` automatically; RW-13's record versioning composes on top.
- The phrase seam gives RW-13 nothing to move: it already rides the running run by construction (R7).
