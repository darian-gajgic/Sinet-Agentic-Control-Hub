# P3-RW-11 grounded brief — interview family reachability (registry capture + live classifier + ask-the-family)

**Packet class:** rework (operator-directed, 2026-08-12 directive in P3/STATE.md's newest Log entry — checkpoint 3 holds until RW-10/11/12 land). During checkpoint 3 the operator's "Create a simple webshop" task got the generic 8-question interview: the ratified software taxonomy is UNREACHABLE dead code in every current world.
**Headline:** *every task resolves its question-set family through real machinery — the registered project's captured family fires the existing `slice.Family` plumbing, the S12 local classifier classifies for real on the live user-level stack, and when neither resolves a family the interview ASKS THE USER as a first-class question — never a silent generic fallback.*
**Scope:** backend Go (`internal/project`, `internal/intake`, `internal/stage` non-surface files, `internal/shell`, `internal/api`) + migration 0024 + two small SPA hunks (create-door family select; journey-surface gate extension — OQ5/OQ6 disposition) + demo-world wiring (user-level, zero host changes). **NOT in scope:** taxonomy CONTENT for the other four families (RW-12 owns it), `internal/stage/surface.go` (RW-10 owns it — flag, never edit), production local-tier activation (D6/S19.6 bring-up ceremony), the onboard-card draft EDITOR (stated absence since RW-8).

All file/line references verified against the working tree 2026-08-12 (HEAD `419a67f`). The live-stack facts verified against `~/.sinet-b45` this session. Expired briefs were not consulted — code + spec only.

## 1. What exists (verified in code + live stack this session)

1. **The dead-code proof, first-hand.** `intake.Pipeline.Start` seeds `Family: FamilyGeneric` (pipeline.go:153). Exactly TWO family sources exist downstream: (a) the registry slice — `if slice.Family != "" { st.Family = slice.Family }` (pipeline.go:173–175) — and (b) the classifier proposal (pipeline.go:185–210). Source (a) never fires: the production `registrySeam.Match` → `toRegistrySlice` (internal/shell/project_seams.go:167–193) **never sets `Family`** (nor `ResolvedSlots`) — the field exists on `intake.RegistrySlice` (intake.go:165, "the registered project's task family") but nothing anywhere populates it; the onboarding capture has no family field at any layer (`project.Draft`/`Capture`/`CaptureInput`, migration 0008's `repo_registry_captures` columns, `stage.onboardDraftShape`, the card digest). Source (b) never fires either: the classifier seam is wired only when `local.StackFromEnv().Configured()` — i.e. `SINET_LOCAL_ENDPOINT` non-empty (internal/local/conformance.go:51–63; internal/shell/local_seams.go:97–100) — and no current world sets it (the demo-world launch, P3/gates/B6-clickthrough.sh:126, passes no `SINET_LOCAL_*`). So `taxonomyFor(FamilyGeneric)` stands for every task, and `SeedTaxonomies()` ships exactly {software, generic} (taxonomy.go:144–149) — the ClarifyCodeBench software set (ratified P3-B2-2) is unreachable.
2. **The plumbing past the family value is ALREADY REAL.** `phaseInterview` re-reads `p.taxonomyFor(st.Family)` on every advance (pipeline.go:507); `Taxonomy.Clearance` counts only slot ids present in the ACTIVE taxonomy (taxonomy.go:86–98), so resolutions keyed to another taxonomy's ids are retained in state but stop counting — a family swap between rounds recomputes clearance correctly with zero new code. `DraftInput.Family` flows to the Planner (pipeline.go:576). Six family constants exist (intake.go:64–71: software/research/content/data/chore/generic). `taxonomyFor` falls back to generic for a family with no seeded taxonomy (pipeline.go:114–120) — **silently** (R6 makes it disclosed).
3. **The classifier adapter is landed and live-capable.** `stage.localClassifier` (internal/stage/local.go:84–115) builds the S12.4 `intake-triage` duty call (temp-0, engine-enforced json_schema, abstain member, 512-token cap, $0 D7 row on `<task>.intake`, R4 workhorse re-check nil-gated on calibration). Fail-closed is exact: a duty error OR abstain returns an error → `Start` sets `TierHigh` (pipeline.go:211–213). **One defect inside it:** `parseTriage` maps an INVALID family label silently to `FamilyGeneric` (local.go:146–149) — a silent-generic path the directive forbids (R5 corrects it to unresolved).
4. **The live user-level stack, verified on disk this session.** `~/.sinet-b45/bin/llama-swap` reports **v241** (= `local.LlamaSwapPin`); `~/.sinet-b45/build/llama.cpp` is at git tag **b10085** (= `local.LlamaCppPin`), CUDA build at `build-cuda/bin/llama-server`. Models present in `~/.sinet-b45/models/`: `Qwen3.5-4B-Q4_K_M.gguf` (2.74 GB, the `fast` seat = intake-triage's alias default), `Qwen3.5-9B-Q5_K_M.gguf` (workhorse), `Arctic-Text2SQL-R1-7B.Q5_K_M.gguf`, `gemma-4-12b-it-qat-q4_0.gguf`, `granite-guardian-3.3-8b` + `-4.1-8b` Q5_K_M, `minicheck-flan-t5-large-q6_k.gguf` — matching the committed manifest names with `Pulled: true` + real sha256 (internal/local/manifest.go). A previously-generated `smoke-config.yaml` (tier-L smoke, PASS at B4-5 per CONVENTIONS §26) shows the working config shape incl. the GPU UUID. `GenerateConfig` (internal/local/config.go:66) renders config from manifest + params. ⚙ `local.alias` code-default maps `intake-triage → fast` (settings/index.go:691–707); `Registry.Resolve` reads it at call time and `seatByKey("fast")` hits the pulled 4B — **no settings write is needed to go live**. `SINET_LOCAL_ENDPOINT` is the BASE URL (client.go:183 appends `/v1/chat/completions`).
5. **The ask/card machinery the family question can ride.** Intake decision cards (`DecisionBody{Summary, Detail, Choices, Help}`, cards.go:75–80) carry an UNBOUNDED choice list + the 13.5 help block; the S06.5 "2–4 labeled options" ratified bound binds interview QUESTIONS (enforced in `Taxonomy.Validate`, taxonomy.go:64–67), not decision choices (SPEC-DOUBT's 3-choice card is the precedent). The SPA renders both families generically: the inbox derives a decision card's verbs from `decision.choices[].value` and composes `{choice}` (Inbox.tsx `answerEnvelope`/`composeAnswer` — the RW-8-proven fire path); the journey surface renders interview questions as a form with options + free text (Intake.tsx:572–669, `{answers:[{id,value}]}`), and enumerates the three decision kinds at Intake.tsx:491 — an unknown kind degrades honestly to "answer it from its inbox card" (:496–501). `Surface.Answer` special-cases ONLY approval/delta kinds for the High-tier PIN (surface.go:221) — **a new intake CardKind flows through `pipe.Answer` with zero surface.go edits.**
6. **The onboarding capture path (family's home-to-be).** `repo_registry_captures` uses DEDICATED COLUMNS per capture field (migration 0008:78–88), immutable rows, version-monotone triggers; migrations 0001–0023 exist and committed migrations are immutable (CONVENTIONS §6) → adding family to the capture = **new migration 0024**. The flow family must ride, all landed: create door `POST /api/projects` (api/projects.go:544, body {project_id, name, remote_url}; Projects.tsx `CreateProject` modal) → `onboardDoor.StartOnboarding` (shell/project_seams.go:131–145, the RW-2 precedent: door-only fields go through `project.OnboardInput`, never widened stage seams) → `project.Onboard` → `Scan`+`Capture` → pending entry → `dispatchOnboard` re-reads the draft and issues the `onboard:` ask with digest `Detail` lines (stage/onboard.go:255–310) → owner approves (`{choice:"approve"}` or the landed raw door `{approve:true, draft:{…}}` — the RW-8 F1 pin applies an EDITED draft) → `OnboardApprove` → active → `toRegistrySlice`. `Rescan` rebuilds `CaptureInput` purely from `Scan` output (project/onboard.go:205–219) — an owner-declared family would be silently dropped on re-scan unless carried forward (R3).
7. **Sequencing facts.** RW-12 (not this packet) drafts per-family taxonomies for all five families + generic at Deep-Plan depth and lands the S06.5 phrase-and-summarize seat; until it lands, only {software, generic} are seeded, so RW-11's payoff case is software (webshop → software questions) and a requester-chosen research/content/data/chore records the TRUE family (durable for RW-12/routing/drafting) while the generic set asks, disclosed (R6). RW-10 owns `internal/stage/surface.go` (the detached-context corpse cut). The demo-world script `P3/gates/B6-clickthrough.sh` currently carries the OPERATOR'S OWN uncommitted hunks (verified `git diff` this session) — the executor must not clobber them (OQ4).

## 2. Requirements (numbered, each with its S-ref)

- **R1 — registry family fires the landed plumbing (S06.2 step 2; S13.7; S1.6).** `toRegistrySlice` projects the active capture's family into `RegistrySlice.Family`; a task in a family-carrying project gets that family at Start via the EXISTING pipeline.go:173–175 lines (no pipeline change for this leg), `TaxonomyID` = that family's set, and the interview asks its questions. Empty capture family ⇒ the slice carries none — the ask leg (R4) decides.
- **R2 — onboarding captures the project family (S13.7 capture versioning; D10).** `project.OnboardInput`, `Draft`, `Capture`, `CaptureInput` gain `Family string` (JSON `family`, additive/omitempty); migration **0024** adds `family TEXT NOT NULL DEFAULT ''` to `repo_registry_captures` (additive, S15.2; old rows read back `""` = honest absence). The value is validated against the six-value vocabulary or `""` (loud `ErrBadInput` otherwise) — a project-local six-string list, because the §23 import wall bars `internal/project` → `internal/intake`; a shell-level conformance test pins the two vocabularies equal. The owner SEES it on the onboard card: a digest `Detail` line (e.g. "Task family: software — its tasks get the software question set") via `onboardDraftShape.Family` + `onboardDigest`; the full draft (with family) rides the snapshot, and the landed edited-draft envelopes (`{approve:true, draft}` and the RW-8-F1-pinned `{choice:"approve", draft}`) apply an edited family exactly as they apply edited conventions. Absent-family drafts and pre-RW-11 snapshots answer exactly as today (the RW-8 compat posture).
- **R3 — re-capture never silently drops the family (S13.7 "captured content is versioned"; honest absence).** `Rescan` carries the entry's current capture family forward into the new version (the scan cannot detect family; it is owner-declared data). Only an explicit owner edit changes it.
- **R4 — ask-the-family: a first-class question, never a silent generic (operator directive; S06.5 + 1.7 ask-don't-assume; S06.1 gates wait; D10).** When Stage 0 leaves the family UNRESOLVED (no registry family AND no valid classifier family — see R5/OQ3 for what counts), `phaseInterview` issues, BEFORE any taxonomy question card, one **`decision.family`** card (new `CardKind`; DecisionBody): summary "What kind of task is this?", six choices with plain-language non-programmer labels (recommended: "Build or change software" / "Find something out" / "Write or create content" / "Work with data" / "A routine chore" / "Something else"), 13.5 help block. The answer `{choice:<family>}` sets `st.Family` + provenance, appends the state event, records a human-authored ledger decision, and the SAME advance loop re-reads `taxonomyFor` — the right taxonomy's questions fire next, clearance recomputed over the new set (landed semantics, §1.2). The choice values are package constants validated one-list-two-readers (the §43/RW-8 discipline). **Band tasks are never asked** (S06.4: trivial tasks never require a click — the band keeps generic with its auto-listed assumptions). The card parks the run exactly as every intake gate does; only the requester answers (existing `Answer` owner check, D10).
- **R5 — no silent-generic path survives anywhere (operator directive).** `parseTriage`: an invalid family label becomes **unresolved** (`Family: ""` on the proposal — the ask fires), never a silent `FamilyGeneric`; abstain/error keep today's fail-closed error. `Start` records family provenance on the state — `FamilySource ∈ {"registry","classifier","requester","default"}` (additive JSON field, no migration; the state is an event payload) — so "generic" is always attributable: requester-chosen generic and band-default generic are recorded as such, and `"default"` + non-band is precisely the state the R4 card exists to eliminate.
- **R6 — honest fallback for not-yet-seeded families (S06.5; RW-12 boundary).** A resolved family whose taxonomy is not seeded (research/content/data/chore until RW-12) keeps `st.Family` TRUE (it drives the intake record, `DraftInput.Family`, and RW-12's future swap-in), uses the generic question set, and the fallback is RECORDED — a platform ledger decision line + the state's `TaxonomyID` (= "generic") vs `Family` divergence as the durable witness — never silent. No taxonomy content is drafted in this packet.
- **R7 — stakes posture untouched (S06.2).** Classifier absence/error/abstain still fails closed to `TierHigh`; the family card never changes tier, floors, band membership, or data-bearing state; deterministic floors and Stage-2(b) re-checks are untouched. The family ask composes WITH S06.2's rules-first/classifier-add-only order — it is Stage-1 interview machinery, not a triage change.
- **R8 — the classifier goes live on the demo world, user-level, zero host changes (S12.1 class (b); S12.2; S12.8; D6/S19.6 boundary; CONVENTIONS §26).** Demo wiring shape (OQ4): the pinned llama-swap (v241, `~/.sinet-b45/bin/llama-swap`) runs as a **user-level process** on a loopback port against a `GenerateConfig`-rendered config from the committed manifest (llama-server = the b10085 CUDA build; models = `~/.sinet-b45/models`); the world's control plane gets `SINET_LOCAL_ENDPOINT=http://127.0.0.1:<port>` (base URL — the client appends `/v1/...`), `SINET_LOCAL_LLAMA_SERVER`, `SINET_LOCAL_MODEL_CACHE`, `SINET_LOCAL_STATE=<world-state>/local`. No ⚙ write (the `local.alias` code-default already maps `intake-triage → fast` → the pulled Qwen3.5-4B), no unit install (generated-never-installed stands), no driver/kernel/system-package change (the hard gates are absolute). PRODUCTION activation stays with the D6/S19.6 bring-up ceremony — this packet proves the wiring on the demo world only.
- **R9 — $0 metering and duty discipline unchanged (S12.1; S12.4; CONVENTIONS §26).** The live classify writes its ONE $0 D7 row on the consuming run via the landed `duty.go` path; abstain members, temp-0, engine-enforced schema, and the R4 workhorse re-check (nil-gated on calibration — intake-triage is honestly `calibrated=false` at the deployed seat, amendment A7) are consumed as landed, not rebuilt.
- **R10 — additive API/schema only (S15.2).** `POST /api/projects` gains optional `family` (validated; absent = `""`); `api.OnboardSurface.StartOnboarding` threading follows the RW-2 shape (the field lands on `project.OnboardInput` at the root); no route removed or renamed; old request bodies, old ask snapshots, and old capture rows answer byte-identically to today. Card/state additions are new JSON fields only.
- **R11 — scope guardrail (operator directive: the simplest machinery that makes all three legs real).** No new ⚙ key (the 118-key tally stays green); zero new `components.lock` entries (17) and zero new Go modules; no edits to `internal/stage/surface.go` (RW-10 — if a need appears, FLAG it in the executor report instead), `internal/verify`, the watchdog/ladder, or the engine adapters; no swap-gate, calibration, or confidence machinery beyond what is landed.

## 3. Seams to respect

- **Import walls (AST-pinned):** `internal/project` imports storage/eventlog + stdlib ONLY (§23) — the family vocabulary is duplicated by value there, pinned equal by a root-level test; `internal/local` never imports intake/stage/settings (§26); stage↛memory, worker↛local as landed. The shell (`project_seams.go`, `local_seams.go`) is where translation lives.
- **The two-call onboarding is ONE onboarding:** `onboardDoor.StartOnboarding` performs the registration with `project.OnboardInput` (family goes HERE); `Skeleton.StartOnboarding`'s own `OnboardStart` call is an idempotent re-read — do NOT widen `stage.Config.OnboardStart`'s landed signature (the RW-2 precedent).
- **The ask substrate:** the family card uses `issueCard`/`closeAndResume`/`advanceLoaded` exactly as every intake gate (gates wait, parked→running bumps generation, answers resume in place); no new ask row shape, no new transport.
- **RW-10 disjointness:** `Surface.Answer`/`mapDriveErr`/`askCardMeta` live in surface.go — verified this session that a new CardKind needs no edit there (the PIN gate names only approval/delta). Any discovered need = a flag to the coordinator, never an edit.
- **Frontend:** ALL SPA hunks run per the standing FRONTEND.md workflow (single author, screenshot loop). The backend must be complete without them: the family card is answerable from the inbox with zero SPA change (verified: generic DecisionCard + choice envelope); the journey-surface form gate (Intake.tsx:491) and the create-door select are the only SPA touches (OQ5/OQ6).
- **The operator's uncommitted clickthrough hunks:** `P3/gates/B6-clickthrough.sh` has 112 uncommitted operator-authored insertions in the working tree — demo wiring must not overwrite them (OQ4 recommends a sibling script).

## 4. ⚙ settings

**New keys: NONE.** Consumed (by dotted key, all landed): `local.alias` (resolution at call time), the S06 clearance floors (`intake.clearance_floor.*` — the family card is kind-gated, not floor-gated), `intake.zero_interaction_cost_usd` (band posture unchanged). The `SINET_LOCAL_*` values are structural config (the R28/`SINET_SRT_PATH` precedent — composition-root passthrough, gate-flagged under the standing settings-tab directive), not ⚙.

## 5. Files expected to change

- `internal/storage/migrations/0024_capture_family.sql` — NEW (additive column on `repo_registry_captures`).
- `internal/project/project.go` (`Capture.Family`), `registry.go` (`CaptureInput.Family`, capture read/write, vocabulary validation), `onboard.go` (`OnboardInput.Family`, `Draft.Family`, `draftOf`, `Rescan` carry-forward).
- `internal/shell/project_seams.go` (`toRegistrySlice` family projection; `onboardDoor.StartOnboarding` threading).
- `internal/api/api.go` (`OnboardSurface` signature or param struct — additive), `internal/api/projects.go` (`projectCreateBody.Family` + validation + refusal text).
- `internal/stage/onboard.go` (`onboardDraftShape.Family`, digest line).
- `internal/stage/local.go` (`parseTriage` invalid-family → unresolved).
- `internal/intake/intake.go` (family-source constants), `state.go` (`State.FamilySource`), `cards.go` (`CardFamily` kind + choice constants + label list), `pipeline.go` (pre-round family gate in `phaseInterview`; disclosed generic fallback in/around `taxonomyFor` use), `answer.go` (`applyFamilyAnswer` case).
- `web/src/Projects.tsx` (create-door family select), `web/src/Intake.tsx` (decision.family in the :491 gate + a `kindLine` entry) — per OQ5/OQ6 disposition, FRONTEND.md workflow.
- `P3/gates/` — the demo local-stack helper (OQ4; recommended sibling script, not an edit to the operator's modified clickthrough).
- Tests in each touched package + the root-level vocabulary pin + the tier-R/L live leg (specs §7 — the executor materializes them; standing red-hold).

## 6. Acceptance headline → checklist

1. ☐ A task in a project whose capture says `software` gets the software question set — `st.Family == software`, `TaxonomyID == "software"`, first card asks software slots, `FamilySource == "registry"` (R1).
2. ☐ The create door accepts `family`; the onboard card's digest NAMES it; approve (choice envelope AND edited-draft envelope) activates an entry whose capture carries it; `registrySeam.Match` returns it (R2).
3. ☐ Re-scan preserves the owner-declared family (R3).
4. ☐ With the local stack configured, a "Create a simple webshop" request classifies `software` via the REAL 4B model — no registry, no ask — with the $0 D7 row written (R8/R9; hermetic tier-R + demo-world walk).
5. ☐ With no registry family and no classifier (today's dev default), the FIRST card is the family question with six plain-language choices; answering "software" makes the NEXT card ask software questions; clearance recomputes; the tier stays fail-closed high (R4/R7).
6. ☐ Requester-chosen "something else" proceeds generic with `FamilySource == "requester"` — attributed, not silent; a chosen family without a seeded taxonomy records the disclosed generic fallback and keeps the true family durable (R5/R6).
7. ☐ An invalid classifier family label yields the ask, not silent generic (R5).
8. ☐ Band tasks are never asked and never gain a click (R4).
9. ☐ Old worlds: pre-0024 captures read `""`; pre-RW-11 onboard snapshots answer identically; old create bodies unchanged (R10).
10. ☐ No new ⚙, no new lock entries, no surface.go edits, no host changes (R11).

## 7. Acceptance-test specifications (SPECS ONLY — do NOT commit failing tests; the executor materializes them under the standing red-hold)

- **T1 `TestStartRegistryFamilySelectsTaxonomy`** (`internal/intake`): Pipeline with a fake `Registry` whose `Match` returns `RegistrySlice{Project:"shop", Family:FamilySoftware}`, nil Classifier. `Start` + walk to running + `Advance`. Assert: `st.Family==FamilySoftware`, `st.TaxonomyID=="software"`, `st.FamilySource=="registry"`, NO `decision.family` card issued, first open card kind `interview` with every question id ∈ software slot ids, `Tier==TierHigh` (fail-closed stands).
- **T2 `TestOnboardFamilyEndToEnd`** (`internal/shell` or `internal/project` + shell seam): `project.Store.Onboard` with `OnboardInput{Family:"software"}` → assert pending entry's capture row v1 has `family="software"`; stage-issue the onboard ask → assert one digest line contains the family; answer `{choice:"approve"}` → assert active entry `Capture.Family=="software"`; `registrySeam.Match` on a request pinning the project → `slice.Family==FamilySoftware`. Variant: approve with edited draft `{choice:"approve", draft:{...,"family":"content"}}` → active capture v2 `family=="content"` (the RW-8 F1 pin applies it).
- **T3 `TestRescanCarriesFamilyForward`** (`internal/project`): entry with capture v1 family `software`; `Rescan` → capture v2 `family=="software"` (scan output carries none; the carry is explicit).
- **T4 `TestFamilyCardFiresWhenUnresolved`** (`internal/intake`): nil Registry, nil Classifier, non-band request. `Advance` → open card kind `decision.family`, six choices whose values are exactly the six family constants, non-empty Help. Answer `{choice:"software"}` → ask closed `answered`, human ledger decision recorded, `st.Family==FamilySoftware`, `st.FamilySource=="requester"`, and the SAME answer-drive's next card is `interview` over software slots with clearance computed over the software set. Assert generation bumped once on resume (the landed fence).
- **T5 `TestFamilyCardChoiceGenericExplicit`** (`internal/intake`): as T4, answer `{choice:"generic"}` → interview proceeds on the generic set, `FamilySource=="requester"`; answer `{choice:"research"}` → `st.Family==FamilyResearch`, `st.TaxonomyID=="generic"`, and the disclosed-fallback record exists (ledger decision naming the fallback). Refusal leg: `{choice:"bogus"}` → `ErrBadAnswer` naming the vocabulary, card stays open.
- **T6 `TestClassifierFamilyResolves`** (`internal/intake`): fake Classifier returning `TriageProposal{Family:FamilySoftware, Tier:TierStandard}` → no family card, `FamilySource=="classifier"`; fake returning an error → `TierHigh` AND the family card fires (`FamilySource=="default"` at Start).
- **T7 `TestParseTriageInvalidFamilyUnresolved`** (`internal/stage`): drive `parseTriage` with `{"family":"bogus","stakes":"low",...}` → proposal `Family==""` (NOT generic); `{"abstain":true}` → error (unchanged); valid `{"family":"software"}` → `FamilySoftware`.
- **T8 `TestBandNeverAsksFamily`** (`internal/intake`): fake Classifier proposing trivial+band (cost under the ⚙ cap) → band completes with NO card of any kind issued pre-approval, family generic, `FamilySource=="classifier"` (or `"default"`), zero interaction preserved.
- **T9 `TestOnboardCompatNoFamily`** (`internal/stage`): a pre-RW-11 ask snapshot (draft without `family`) answers `{choice:"approve"}` exactly as today — active entry, capture family `""`, digest omits the family line, no error.
- **T10 `TestFamilyVocabularyPinned`** (root-level, may import both packages): `project` family list == the six `intake.Family` constants, byte-equal — the cross-wall duplication cannot drift.
- **T11 tier-R live leg `TestLiveIntakeTriageClassifiesWebshop`** (`internal/stage` or `internal/local`, the §26 tier-R/L pattern: auto-runs when the installed stack exists, else `SANCTIONED SKIP (CONVENTIONS §10)`): against a real llama-swap on an ephemeral port with the generated config, run the real `localClassifier.Classify` on title "Create a simple webshop" → `Family==FamilySoftware`, and the $0 D7 checkpoint row exists on the consuming run with the model hash + engine build markers. ($0 by construction.)
- **T12 migration leg `TestMigration0024Additive`** (`internal/storage` or `internal/project`): a DB migrated through 0023 with existing capture rows migrates to 0024; old rows read `family==""`; the immutability/no-delete/monotone triggers still hold on the widened table.

## 8. CONVENTIONS constraints that bind

§3 (test conventions; the red-hold: specs here, executor materializes red-first), §5 (commit discipline), §6 (migrations immutable once committed — hence 0024; ⚙ by dotted key, never hardcoded), §10 (behavior-asserted conformance, sanctioned-skip only for absent real stacks), §14 (intake: event-sourced state, no intake table, artifact/ledger discipline, seeds operator-editable via strict-JSON loaders, `maxQuestionsPerCard` non-⚙), §23 (project import wall AST-pinned; captures versioned trigger-enforced; registry feeds; no new ⚙), §26 (local tier: import walls, $0 wire contract, alias map-family discipline, generated-unit-never-installed, tier F/R/L, pins test-coupled to the lock), §27 (GPU-broker planes untouched; the duty pre-flight admission stands), §43/RW-8 (one-list-two-readers answer vocabularies; producer declares, surfaces render).

## 9. Open questions for coordinator disposition

- **OQ1 — where the family lives in the capture schema; migration mechanics.** Recommendation: capture-level — `repo_registry_captures.family` via NEW additive migration 0024 (`TEXT NOT NULL DEFAULT ''`), because the onboard draft→digest→approve→edited-draft→re-capture machinery is all capture-shaped and S13.7's "captured content is versioned" then covers family edits for free (audit trail included). Alternative: an entry-level `repo_registry.family` column (family-as-identity; but it forks a second edit path outside the draft flow and the identity-immutability trigger family). Old rows/backups: `""` = honest absence; no data migration, no version bump beyond the schema number (`capture-v1` JSON shape gains an omitempty field — additive per S15.2; `onboardDraftShape` readers tolerate absence by construction).
- **OQ2 — ask-the-family mechanics: pre-round decision card vs interview-card slot; swap + clearance; resolved generic slots.** Recommendation: a NEW `decision.family` CardKind issued pre-round (§2 R4). Grounds: (a) the six-way choice EXCEEDS S06.5's ratified "2–4 labeled options" bound on interview questions — as a decision card (unbounded choices; SPEC-DOUBT precedent) there is NO spec tension and no amendment; (b) pre-round issuance makes the swap trivial — the landed advance loop re-reads `taxonomyFor` and `Clearance` counts only active-set ids, so no resolved-slot surgery exists at all (nothing has been asked yet); registry-supplied `ResolvedSlots` for the new family re-apply on the next advance… (executor note: `Start` applies them once against the START taxonomy — re-apply after a family swap, one loop, same helper); (c) both SPA surfaces handle it (inbox for free; journey via the two-line gate extension or the honest inbox pointer). Mid-interview family changes stay OUT of scope (no card exists that offers one; re-interview keeps its taxonomy). Alternative (rejected): a synthetic slot on the interview card — violates the option bound and needs answer-path special-casing in `applyInterviewAnswer`.
- **OQ3 — classifier-vs-asked precedence; low-confidence confirmation.** Recommendation: precedence **registry > classifier > ask > default-generic(band only)**; a VALID classifier family — including an explicit `generic` — resolves (the directive's letter: the classifier RESOLVED it; it is attributed `FamilySource=="classifier"`, visible, not silent), and absence/error/abstain/invalid-label asks. NO confidence-gated user confirmation this packet: S12.5 calibration exists as machinery, but intake-triage is honestly `calibrated=false` at the deployed 4B seat (amendment A7 — can't-separate), so there is no calibrated threshold to gate on and fabricating one would violate S12.5; the landed R4 workhorse re-check activates by itself when calibration lands. Alternative for the coordinator: treat classifier-`generic` as unresolved (ask anyway) — one extra tap, the strongest "never silent generic" reading; costs the zero-touch path for genuinely-generic tasks.
- **OQ4 — demo-world wiring shape.** Recommendation: a NEW sibling helper (e.g. `P3/gates/local-stack.sh`): generate the llama-swap config from the committed manifest (`GenerateConfig` via a tiny `go run`-able entry or the tier-R harness pattern), start `~/.sinet-b45/bin/llama-swap` user-level on an ephemeral loopback port, print the four `SINET_LOCAL_*` exports; the clickthrough gains at most an OPT-IN env passthrough once the operator's uncommitted hunks are landed by their owner. Grounds: the clickthrough script carries operator-authored uncommitted changes this packet must not clobber; llama-swap lifecycle = user-level process (the tier-R precedent), NEVER the generated `sinet-llamaswap.service` (generated-never-installed, §26; install is a bring-up/hardening act). The S12.4 registry rows need NOTHING: the ⚙ `local.alias` code-default + manifest already resolve `intake-triage → fast → Qwen3.5-4B (Pulled)`.
- **OQ5 — the create-door family face.** Recommendation: an OPTIONAL labeled select on the CreateProject modal (six plain-language labels + "decide later" = absent), additive body field; family also editable at approval via the landed edited-draft envelopes (T2 variant). Card-side family editing beyond that stays with the stated-absent draft editor (RW-8 OQ3 posture) for the future onboard-polish packet. The digest line is the "sees it" half and is pure backend.
- **OQ6 — the journey-surface form for the family card.** Recommendation: extend the Intake.tsx:491 kind gate + `kindLine` map so `decision.family` renders `DecisionForm` in the journey (two small hunks, FRONTEND.md workflow); without them the card is still answerable via the honest inbox pointer (verified fallback) — so the coordinator may defer the hunks to the builder's pass without blocking the packet.

## 10. Spec-conflict findings

**None requiring S00.9 amendment.** Three tensions examined and resolved within frozen text: (1) S06.2's fail-closed wording governs STAKES (high tier on classifier failure) — untouched by all three legs; ask-the-family is Stage-1 interview machinery under S06.5/1.7, not a triage rewrite. (2) S06.5's ratified "2–4 labeled options" bound would conflict with a six-way interview QUESTION — avoided structurally by the decision-card shape (OQ2). (3) S13.7's entry field list does not name family, but the landed, ratified `intake.RegistrySlice.Family` (built at B2-2 citing S06.2/S13.7) already anticipates it and S15.2 sanctions additive schema evolution; a cosmetic A-row catch-up (the A9 precedent) may be recorded at the gate if the coordinator wishes — nothing blocks on it.

## 11. Watch notes / flags for the executor report

- If any leg turns out to need `internal/stage/surface.go` (RW-10's file) — FLAG, do not edit (verified unnecessary for the recommended shape).
- `Start` applies registry `ResolvedSlots` once against the start taxonomy (pipeline.go:219–225) — after a family swap, re-apply against the new set (OQ2 note) so a family-carrying project's pre-answered slots are not lost.
- The smoke config generation at B4-5 included non-pulled seats' exclusion logic — regenerate from TODAY'S manifest, never reuse `smoke-config.yaml` verbatim.
- RW-12 lands per-family taxonomy content + local phrasing on this packet's rails: keep `FamilySource`, the disclosed-fallback record, and the six-value vocabulary stable for it.
