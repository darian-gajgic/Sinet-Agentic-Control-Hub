# P3-UI-1 brief — Foundation: substrate adoption + Nexus tokens + fonts/icons + aurora & shell restyle + primitive kit + density

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical behavior text is `Spec/drafts/` (frozen v1): **S15.1, S15.3–S15.4** (`Spec/drafts/S15-frontend-api.md`) and **`Spec/frontend-components-v1.md` (FC-v1)** — untouched by this packet. **The design authority for this batch is the operator-APPROVED `P3/design/design-approach-proposal.md`** (approved 2026-08-04, free text recorded in its §6; defaults stand: aurora on, glass/motion as specified, density default comfortable) — **subordinate to the spec on all behavior**. Its three inputs are the design source record: `P3/design/nexus-frontend-harvest.md` (the token values and pattern specs), `P3/design/ui-foundation-sourcing.md` (the substrate decision basis; **its pins are research-time, not binding**), `P3/design/odysseus-pattern-study.md`. `P3/CONVENTIONS.md` binds: **§1–§5, §41+§41-B, §42** (constraints extracted in §6 below). **The spec wins over this brief, over the proposal, and over existing code; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**AGPL BOUNDARY (binding, an explicit evaluation item):** Odysseus (`odysseus-dev/odysseus`) is AGPL-3.0 and is an **ideas-only** source, forever. **Zero Odysseus code, CSS, markup, or asset may appear anywhere in this packet** — the pattern study is descriptions, and everything built here is our own implementation. The only third-party code entering the tree comes from npm at the pinned versions or is TSX generated by the shadcn CLI into our tree (MIT, becomes owned code).

**This is packet 1/7 of the UI batch** (queue `P3/STATE.md` §UI batch, cut 2026-08-04). It is the foundation every later packet builds on: the styling substrate through the adoption rail, the Nexus token set poured into shadcn token names, bundled fonts/icons, the aurora backdrop and shell restyle, the primitive kit, and density classes. It builds **NO surface work beyond the shell chrome**: the fourteen D3 controls are UI-2/3/4, the C-1 login fix and applied D5 timestamps are UI-4, the D8 pass over the eleven routes (self-teaching layer, timeline rail, donut meters, approval-row anatomy, assistant-card rework) is UI-5/6/7. **This packet changes NO behavior**: API contracts, events, scoping, and routes are untouched; no route gains or loses function; `web/src/api.ts`, `events.ts`, `routes.ts`, `router.tsx` and every surface component's data consumption stay as landed.

**Model routing:** executor + finalizer **opus** (standing batch directive, STATE 2026-08-04); grounding/evaluation inherit (Fable) — UI scope is not S10/S11-dense; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-08-04)

**Baseline, re-measured this session (not carried):** `go run ./tools/lockgate` → OK, **29 entries; 240 npm packages covered or toolchain-scoped**. `cd web && npm run test` → **432 tests / 23 files, all green**. Bundle reference (B6 gate record + session-entry battery, exact match 2026-08-04): **`dist/assets/index-*.js` 976.99 kB / gzip 292.96**. Escape-scan: allowlist **EMPTY**, **8** banned tokens (incl. `srcdoc`), file floor 50. Worktree clean except four long-standing untracked operator items (`Presentation/`, `Research/Presentation/`, `Sinet-Logo.jpeg`, `tools/dbpeek/`) — **never staged by any packet**.

**Research-time pins (ui-foundation-sourcing.md, live-fetched 2026-08-04 — the decision basis, NOT the binding pins):** `tailwindcss@4.3.3` + `@tailwindcss/vite@4.3.3` (MIT, dev-time), shadcn CLI `4.16.1` (MIT, dev-time), `@base-ui/react@1.7.0` (MIT — **renamed from `@base-ui-components/react`**; stable 1.0.0 since 2025-12-11), `lucide-react@1.28.0` (ISC), `clsx@2.1.1` (MIT), `tailwind-merge@3.6.0` (MIT), `class-variance-authority@0.7.1` (Apache-2.0), `tw-animate-css@1.4.0` (MIT), `@fontsource-variable/inter@5.3.0` (OFL-1.1 per the sourcing doc — the font-file licence; the package's declared licence is read path-scoped at the installed ref, R4), `@fontsource/jetbrains-mono` (likewise OFL-1.1 fonts). Ecosystem fact: **shadcn's default primitive layer is Base UI since 2026-07-03** — new `init` projects wire to Base UI; that is the ratified track here (proposal §2). **The executor's live-verify obligation is explicit and non-optional (R3).**

**The Nexus token values (harvest, coordinator-spot-checked verbatim from `style.css:5-35`) — the design source this packet pours:**

```
--bg:#07070d  --bg-2:#0b0b15  --panel-solid:#12121d
--panel:rgba(148,148,190,.055)  --panel-2:rgba(148,148,190,.09)  --panel-3:rgba(148,148,190,.14)
--border:rgba(148,148,190,.16)  --border-l:rgba(148,148,190,.28)
--text:#eaeaf4  --text-dim:#9a9ab2  --text-faint:#5e5e78
--accent:#7c5cff  --accent-2:#5eead4  --cyan:#22d3ee  --accent-glow:rgba(124,92,255,.16)
--green:#4ade80  --yellow:#fbbf24  --red:#f87171  --blue:#60a5fa  --orange:#fb923c  --pink:#f472b6
--grad:linear-gradient(135deg,#7c5cff 0%,#22d3ee 100%)
--grad-soft:linear-gradient(135deg,rgba(124,92,255,.22),rgba(34,211,238,.12))
--sidebar-w:232px  --radius:14px  --radius-sm:9px  --shadow:0 14px 40px rgba(0,0,0,.35)
```

Chip formula: `color:X; background:rgba(X,.09); border-color:rgba(X,.25)`. Aurora: one fixed div, four radial gradients at 5–13% opacity, 26s alternate drift. Blur tiers: shell 18px, overlays 6–12px. Type: Inter (prose) / JetBrains Mono (every number, ID, timestamp, status token; `tabular-nums`); tracked-uppercase section labels; 30px mono stat values. Dark-first at v0 — no light ramp is built (the token architecture keeps one addable; `ARCHITECTURE.html`'s dual-ramp contract is the filed reference, not this packet's work).

**Code facts (read 2026-08-04):**

- `web/src/index.css` is **1,409 lines**: a 9-token flat `:root` (`--bg:#101418 --panel --fg --muted --line --accent --ok --warn --bad`), `system-ui` type, phone-first with ONE `@media (min-width: 48rem)` block, per-surface sections for every landed route. Its header comment says "No framework is adopted" — that sentence becomes false this packet and MUST be updated (R21).
- The shell (`App.tsx`) is header (`brand` + `ConnectionState` + `who`/Sign out) → wrapping `shell-nav` (7 nav routes from `routes.ts` `nav:true`) → `shell-main`. The route table (`routes.ts`) is pinned by `routes.test.ts`; `parts.tsx` carries the §42 honesty primitives (`Absent`/`Money`/`Stamp`/`ParkedUntil`/`Owner`/`Freshness`/`Section`/`Empty`) — their contracts are landed discipline, not this packet's to rework.
- **Markup/stylesheet-coupled tests that WILL need honest updates:** `responsive.test.ts` (reads `index.css?raw`; asserts `.shell*` selectors, phone-first `min-width`-only queries, wrap/overflow rules per surface), `shell.test.tsx` (queries `.shell-nav a`, `.conn`, button text "Sign out", stub/title text). `vitest.config.ts` sets `css: true` (load-bearing — RE-VERIFIED §41-B D5) and `TZ: Pacific/Auckland`.
- **Scan rules new code must clear (`scans.test.ts`, `escape-scan.test.ts`):** no `dangerouslySetInnerHTML`/`innerHTML`/`outerHTML`/`insertAdjacentHTML`/`document.write`/`eval(`/`new Function`/`srcdoc` anywhere under `web/src` (vendored TSX included — it is scanned like everything else); no `localStorage`/`sessionStorage`/`document.cookie`; no identifier matching `/percent/i`, `/\bpct/i`, `/progress_?bar/i`, `/\beta_?(s|seconds)?\b/i` (name nothing "ProgressBar"; a skeleton is not a progress figure); no client-side money arithmetic; in-app links only via `hrefFor`/`Link`; no JSON Forms theme package.
- **The npm rail mechanics are LANDED** (`internal/lockfile/npm.go`, read at grounding): any entry (any kind) with `npm_packages` claims is pin-lockstep-checked — `entry.pin` == lockfile-installed version == `package.json`'s exact declaration, **and every claimed package must be a DIRECT declaration in `package.json`**; the tree entry (`React 19 + Vite + TypeScript tree`) is exempt from per-package lockstep and instead its pin must equal `NPMTreePin(viteVersion, lockBytes)` = `<vite>+sha256:<hash of web/package-lock.json>` — **so the tree entry re-pins as a dated lock edit whenever the lockfile moves, including this packet**. Production coverage is computed by graph reachability from claimed roots; `dev`/`devOptional` packages are toolchain-scoped wholesale. Consequence: **one lock entry per differently-versioned claimed package** (a shared entry is possible only where versions are identical — the JSON Forms / tailwind-pair shape).
- **CI (`.github/workflows/ci.yml`) already runs** setup-node (SHA-pinned) → `npm ci --ignore-scripts` → web typecheck/test/build → Go steps over the real dist → lockgate. **No CI change is expected this packet.**
- Go-side expectation: **zero `.go` changes, zero `go.mod`/`go.sum` movement** — the packet is web-tree + `components.lock` + CONVENTIONS only.
- `index.html` carries `<meta name="theme-color" content="#101418">`; `web/public/manifest.webmanifest` carries `background_color`/`theme_color` `#101418`. These are visual values and move with the palette (R11).
- B6 §9 findings **A-1/A-2/C-1 are CONTEXT ONLY here** — their fixes ride UI-4 (C-1) and UI-5/6/7 (A-1/A-2). Do not fix them in this packet.

---

## 1. Numbered requirements

### A — Substrate adoption through the rail (proposal §2; S15.1 riders; CONVENTIONS §4/§41; S16.2–S16.4)

- **R1. Dev-time adoptions** [proposal §2; sourcing §1]: `tailwindcss` 4.x + `@tailwindcss/vite` 4.x enter `web/package.json` **devDependencies** at exact pins; the `@tailwindcss/vite` plugin is added to `vite.config.ts` `plugins` (the dev-proxy block, `VITE_SINET_ADDR` discipline, and build outDir stay byte-equivalent in behavior — `devproxy.test.ts` keeps passing unmodified). Nothing of Tailwind exists at runtime: it is a compiler; the binary serves only built assets (§41 posture unchanged).
- **R2. The shadcn CLI is a code generator, NEVER a runtime dependency** [proposal §2]. Components arrive as **vendored TSX under `web/src` — code we own**; upstream churn cannot touch shipped components; the adopted-engine rules apply only to the runtime primitive deps (R3). Generation runs on the **Base UI track** (the post-2026-07-03 default). The CLI's entry shape is OQ1 (recommended: exact-pinned devDependency). `web/components.json` (the CLI's config) and a `@/*` tsconfig+vite alias are sanctioned scaffold mechanics, recorded in §47. Generated TSX may be freely edited after generation — it is ours; **`node_modules` is never patched** (adopt-don't-fork binds the npm deps).
- **R3. Runtime deps, exact-pinned, LIVE-VERIFIED AT ADOPTION** [proposal §2; STATE queue row; §4 pin discipline]: `@base-ui/react`, `lucide-react`, `clsx`, `tailwind-merge`, `class-variance-authority`, `tw-animate-css`, `@fontsource-variable/inter`, `@fontsource/jetbrains-mono` enter `dependencies` at exact pins. **The sourcing doc's versions are research-time. The executor MUST re-verify every pin against the live npm registry at adoption** (current version, license unchanged, not deprecated, package name current — `@base-ui/react` was renamed once already), record the check date in each lock entry's `license.checked`/notes, and pin what the registry serves at execution. A relicensed/deprecated/vanished package = **STOP and report** — never a silent substitute. **This dependency set is CLOSED**: the proposal names exactly these; any additional package (a toast library, a portal helper, anything a generated component wants that is not already in the tree) is a STOP-and-flag, not an install. The npm registry fetch is the packet's one sanctioned network act ($0).
- **R4. `components.lock` entries** [S16.2–S16.4; §4/§41]: one entry per R3 package (`kind: frontend`; a shared entry only where claimed versions are identical) with `npm_packages` claims, path-scoped licenses read from the INSTALLED ref (the B6-4 precedent; the Fontsource entries record the OFL-1.1 font-file licensing plus whatever code-licence split the installed ref actually declares, in `license.scope`), funeral plans (see §5 table), watch rows (quarterly pass co-scheduled with ⚙ `frontend.dependency_pass_interval`), and the S16.4 ten-check walk in notes, **#10 operator ratification queued for the batch/exit gate**. Plus: a `kind: toolchain` entry for the Tailwind pair (both packages, one pin — they release in lockstep; if the live versions differ, split entries), the OQ1-shaped record for the shadcn CLI, and the **tree entry re-pinned** to the new lockfile hash as a dated lock edit. `go run ./tools/lockgate` green over the real tree is the proof; expected count 29 → 29+N with N named in the executor report.

### B — Tokens: Nexus values poured into shadcn names (proposal §1; harvest §3)

- **R5. The token pour** [proposal §1 "Mechanically, the Nexus values are poured into shadcn's token names"]. `web/src/index.css` gains, ahead of the existing rules: the Tailwind imports (`@import "tailwindcss"`, `tw-animate-css`), and a `:root` block defining **the full shadcn base token set** (`--background --foreground --card --card-foreground --popover --popover-foreground --primary --primary-foreground --secondary --secondary-foreground --muted --muted-foreground --accent --accent-foreground --destructive --border --input --ring --radius` and the current shadcn-CLI set's siblings) **from the Nexus values above** — near-black `#07070d` base, the violet-tinted translucent `rgba(148,148,190,α)` neutral family for panels/borders (one material, never plain gray), `#7c5cff` primary with the `#7c5cff → #22d3ee` gradient as identity, `#eaeaf4`/`#9a9ab2`/`#5e5e78` text ramp, `--radius` 14px/9px pair. **Sinet extension tokens** alongside, under their own names: the six status hues (green/yellow/red/blue/orange/pink), `--grad`/`--grad-soft`, `--accent-glow`, the shadow/blur tiers, `--sidebar-w`, aurora hues. Derived values via `color-mix()` where a ramp is needed (Odysseus theme-discipline idea, reimplemented). **Dark-first**: dark values sit directly on `:root` (`color-scheme: dark`); no light ramp is built; the class-switched-theme-root shape stays addable later.
- **R6. Tailwind wiring** [proposal §2]: an `@theme inline` block maps the `:root` variables to Tailwind utility names (the standard shadcn v4 wiring) so `bg-background`, `text-muted-foreground`, `border-border` etc. resolve from the ONE token set — which also themes @assistant-ui/react (it natively assumes these token names), the JSON Forms renderers, and react-diff-view chrome from one place. Utilities and bespoke CSS coexist; no landed rule is deleted this packet (R20).
- **R7. Legacy-token re-point (RECORDED READING — coordinator ratifies at landing)** [proposal §2 "tokens land first and coexist; no existing surface loses its styling"]. The nine landed tokens (`--bg --panel --fg --muted --line --accent --ok --warn --bad`) **remain defined and are re-pointed at the corresponding new values** (`--bg` → `#07070d`/`var(--background)`, `--panel` → the panel material, `--fg` → `#eaeaf4`, `--muted` → `#9a9ab2`, `--line` → the border neutral, `--accent` → `#7c5cff`, `--ok/--warn/--bad` → green/yellow/red). Ground: every landed surface re-bases onto the new palette with zero structural edits — the app is visually coherent at UI-1 exit, no surface loses its styling, and each surface's own CSS sheds in its later packet. The alternative (freezing old values) would ship a two-palette app and was rejected; the coordinator can veto at landing review.

### C — Fonts and icons, bundled — zero CDN, zero runtime fetches (proposal §1/§5)

- **R8. Fonts** [proposal §1; harvest §6-5]: Inter (variable) for prose and **JetBrains Mono for every number, ID, timestamp and status token** with `font-variant-numeric: tabular-nums`, imported from the Fontsource packages (CSS or JS import — executor's choice, recorded) so Vite bundles the woff2 files into `dist/assets/` and the binary embeds them. Import only the weights/subsets the design uses (JetBrains Mono 400–700 per the harvest). The base `font-family` moves off `system-ui`; a mono utility class/token carries the JBM rule.
- **R9. Icons** [proposal §2]: `lucide-react`, tree-shaken per-icon imports only (never the barrel of all icons). Nexus's inline-SVG icon language (24×24, 1.6–1.8 stroke, round caps) is lucide-compatible; where a Nexus icon is better the executor redraws it as an owned TSX component — never copies Odysseus's, and Nexus SVG paths may be adapted (Nexus is the operator's own prior work, an ADOPT/PORT source per the harvest).
- **R10. The zero-CDN proof** [proposal §5; batch rule]: after `npm run build`, **no `http://`/`https://` reference to any external host exists in the built `dist/` CSS/JS/HTML** (fonts resolve to same-origin hashed assets). Checked at landing by grep over `internal/webui/dist` and stated in the executor report. The escape-scan and its EMPTY allowlist are untouched.
- **R11. Chrome color values follow the palette** [shell restyle scope]: `index.html` `theme-color` and `manifest.webmanifest` `background_color`/`theme_color` move from `#101418` to the new `--background` value. Visual-only; `sw.js` and the manifest's identity/scope fields are untouched.

### D — Aurora backdrop + shell restyle (proposal §1/§4-1; harvest §6-2/§6-3, recommended-order 2 and 4)

- **R12. Aurora** [proposal §1 "Depth"]: ONE fixed, `aria-hidden`, `pointer-events:none` div in the app shell behind all content — four drifting radial gradients at 5–13% opacity over the near-black base, ~26s alternate drift. The drift animation is authored so it runs only under `prefers-reduced-motion: no-preference` (R17); under `reduce` the backdrop is a static gradient field (depth without motion). Operator default: aurora ON (proposal §6).
- **R13. Shell restyle — sidebar/topbar/nav in the Nexus language** [proposal §4-1; harvest layout skeleton]: the shell chrome (`App.tsx` + shell CSS) rebuilds as the blurred sidebar (gradient logo mark, grouped nav — groups are PRESENTATION defined in the shell component; `routes.ts` ids/patterns/titles/nav flags byte-unchanged) + blurred topbar carrying the brand/identity/`ConnectionState` (restyled as the connection pill) + scrolling content. **Every existing shell element survives: nav links to the same 7 routes, Sign out, the who-line, the connection indicator with all six states and its never-reads-as-live rule, the login/stub/not-found arms.** No element gains or loses function — sidebar micro-meters, clock, focus-context bar, and pending-count badges are LATER packets (they need data consumption this packet must not add).
- **R14. Responsive discipline holds** [S15.12 via S1.10; §41-B]: phone-first, `min-width`-only queries, the sidebar layout appears at the breakpoint and phone width gets a wrapping/collapsible nav — every surface reachable at 375px, no fixed-pixel layout width, connection LABEL never hidden, body never scrolls sideways. `responsive.test.ts`'s invariants are preserved; its selector-level assertions update honestly with the new markup (R19).

### E — The primitive kit (proposal §4-1; harvest §6-3/§6-5; B6 gate D5 answer)

- **R15. The kit** — vendored-shadcn-restyled or owned TSX, exported from one home (recommended `web/src/ui/`, with `cn()` in `web/src/lib/utils.ts`), each with tests: **Button** (variants primary = gradient + soft glow, secondary, ghost, danger; sizes; disabled/focus-visible states), **Panel** (the one glass material: translucent gradient, 1px border, radius 14, hover lift), **Chip** (the ONE chip formula over the six status hues + accent — 7 variants), **StatusDot** (8px glowing dot; `live` prop adds pulse, gated by R17), **StatTile** (2px gradient hairline via `::before`, tracked-uppercase micro-label, icon chip on `--grad-soft`, 30px mono tabular value; **NO count-up in this packet** — count-up transforms displayed figures and its admissibility against the no-fabricated-numbers invariant is a surface-packet decision), **Toast** (top-right stack, color-coded left border, blurred, max 4 visible; on `@base-ui/react`/vendored shadcn only — **no new toast dependency**, R3's closed set), **Modal** (blur-tier overlay, overshoot entry under no-preference), **Drawer** (right sheet, 520px desktop / full-width phone), **Skeleton** (shimmer under no-preference, static under reduce), **EmptyState** (centered faint glyph + a sentence slot for *what will appear here and why* — the teaching-sentence pattern; per-surface adoption is UI-5..7), and **R16's Timestamp**.
- **R16. The D5 timestamp primitive, two variants** [B6 gate §9 D5 answer; STATE queue row]: `Timestamp` renders a served UTC RFC3339 string in `<time dateTime={ts}>`; variant **`live`** = relative time ("3m ago") **beside** the verbatim UTC — the UTC is never dropped or replaced; variant **`audit`** = verbatim UTC only. Relative text derives from the served instant vs the device clock and refreshes on a coarse interval; absent/empty ts renders the honest absence (the `Absent` pattern). **This packet SHIPS the primitive with tests; call sites are NOT migrated** — applying it across live surfaces is UI-4's row ("D5 timestamps applied"), and `parts.tsx`'s `Stamp` continues to serve landed audit-style call sites unchanged.
- **R17. `prefers-reduced-motion` discipline on EVERY animation** [proposal §1 "Motion only as feedback"]: a global reduce kill-switch lands in `index.css` (under `@media (prefers-reduced-motion: reduce)`, animations/transitions neutralized — the ARCHITECTURE.html precedent), AND the packet's own motion (aurora drift, pulse, shimmer, hover lifts, modal overshoot, any tw-animate-css utility) is authored gated. A structural test asserts the kill-switch block exists and that the new keyframe animations are gated (the `responsive.test.ts` raw-source pattern).
- **R18. Density root classes** [proposal §1/§4-1; operator default from §6]: `density-comfortable` (DEFAULT, applied at the root) and `density-compact` both ship — density flows from spacing/padding tokens the two classes switch; a test toggles the class and asserts the spacing variables change. **No persistence** (web-storage is banned by the landed scan — a stored preference would need a server-side home that does not exist), **no switcher control** (a new control is later-packet scope; no density ⚙ key exists in S18 and none is invented).

### F — Tests, migration honesty, and the ledger (batch rules; §41-B; B6-9 discipline)

- **R19. Existing tests stay green; markup-coupled assertions update HONESTLY** [STATE queue row; the B6-9 discipline]: baseline **432 tests / 23 files**. Assertions coupled to restyled markup (`shell.test.tsx`, `responsive.test.ts`) are updated to assert the SAME invariants against the new structure — **assertion counts never drop silently**: the executor report states before/after test and assertion deltas per touched file with reasons; the suite total only grows (new primitive/structural tests). The scans (`escape-scan`, `scans.test.ts`) are not weakened: allowlist stays EMPTY, banned-token list stays or grows, planted probes intact.
- **R20. Progressive `index.css` migration, ledgered** [proposal §2]: tokens + imports + kill-switch + density land FIRST and coexist with the entire landed sheet; **no landed per-surface rule is deleted or renamed this packet** (surfaces shed their CSS in their own later passes); new primitive classes must not collide with landed selector names (the harvest's collision warning — `.panel` already exists and keeps its meaning; the kit's panel uses its own class/utilities). §47 opens a short migration ledger naming which packet sheds which surface section.
- **R21. CONVENTIONS §47 appended** (confirm the next free number at launch; §46 is last at grounding): the substrate record (Tailwind wiring, shadcn-CLI-as-generator posture + its OQ1 entry shape, components.json, the `@/*` alias), the token architecture (shadcn names + Sinet extensions + the R7 re-point reading, `@theme inline`, dark-first), the primitive-kit export contract (§2 below) and its home, density classes, the reduced-motion kill-switch, the D5 primitive contract, the migration ledger, and bundle numbers at landing. **Two dated cross-notes accompany it, never silent contradictions:** (a) §41's "No CSS framework…" sentence gains a dated note pointing at §47 (superseded through the rail under the operator-approved proposal — the §41-B CORRECTED precedent); (b) `index.css`'s "No framework is adopted" header comment is rewritten to state the new posture.
- **R22. Battery + bundle measurement** [batch rules; §3/§5]: in order — `cd web && npm run typecheck && npm run test && npm run build` → `gofmt -l internal/ cmd/ tools/` (empty) → `go vet ./...` → `go build ./...` → `go test ./... -count=1` (web build FIRST so `TestRealEmbeddedBuild` exercises the REAL new dist, not the sanctioned skip) → `go run ./tools/lockgate`. All green before any commit. **Bundle recorded at landing**: the full `vite build` output table (JS + CSS + font assets, raw and gzip) against reference **976.99 kB / gzip 292.96** — growth from fonts (new `.woff2` assets), @base-ui/react, lucide icons and Tailwind CSS is EXPECTED and is stated with numbers, never hidden; anything anomalous (an accidental full-icon-barrel import would be hundreds of kB of JS) is investigated before landing. The SPA stays un-split.
- **R23. Counters + untouched machinery** [§5; batch rules]: **zero `.go` file changes; `go.mod`/`go.sum` byte-unchanged; migrations, `internal/settings/index.go` (⚙ 118/33), `internal/eventlog/contract.go`, event inventory, conformance rows, CI workflow — all byte-unchanged.** `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed; commits staged explicitly (never `git add -A`; never the four operator items); subjects `P3-UI-1: … (S15.1, S15.3–S15.4, FC-v1, S16.2–S16.4)` + the standing trailer.

## 2. Seams — the interfaces later packets consume (state them, build nothing beyond them)

**UI-2/3/4 (the fourteen controls) and UI-5/6/7 (the D8 pass) import THIS packet's exports and tokens; their packets must not need to re-open the foundation.** The stable surface this packet publishes:

1. **The token contract** — shadcn base names on `:root` (themes shadcn primitives + assistant-ui + JSON Forms renderers + react-diff-view from one place), the Sinet extension names (status hues, `--grad`/`--grad-soft`, glow, shadow/blur tiers, `--sidebar-w`, radius pair), and the re-pointed legacy nine. **New code references tokens/utilities, never raw hex** (raw hex lives only in the token block and aurora — a §47 discipline).
2. **The primitive-kit export contract** (names fixed here; exact prop types are the executor's, recorded in §47): `Button` (variant: primary | secondary | ghost | danger), `Panel`, `Chip` (tone: 7), `StatusDot` (tone + live), `StatTile` (label/value/icon/tone), toast API (severity-toned, action-capable), `Modal`, `Drawer`, `Skeleton`, `EmptyState` (what + why slots), `Timestamp` (variant: live | audit), `cn()`. A rename after UI-1 is a recorded deviation, not a drift.
3. **Density classes** `density-comfortable`/`density-compact` and the spacing tokens they switch.
4. **parts.tsx honesty primitives stay authoritative** for absence/money/freshness semantics — the kit styles surfaces; it never re-opens §42's rules (no progress/percent/ETA primitive exists, and none is added).
5. **Explicitly NOT built here** (later packets own them ON this kit): the fourteen controls, C-1 fix, D5 call-site application, timeline rail, threshold-donut meter idiom, focus-context bar, approval-row anatomy, tours/hints/teaching empty-state adoption, assistant-card rework, sidebar micro-meters, count-up, seeded demo world, the two cosmetic S00.9 amendments.

## 3. ⚙ settings consumed (by registry name)

**None consumed, none created.** `internal/settings/index.go` stays byte-unchanged at 118 keys / 33 domains. Referenced-not-consumed: ⚙ `frontend.dependency_pass_interval` (the landed `frontend-dependency-pass` conformance row already covers the new deps' quarterly pass — its watch rows in R4 name it). Client-side numbers introduced here (density spacing steps, animation durations, toast cap, drawer width) are **structural constants with named reasons** in code comments — the §41-B precedent (the browser cannot read the registry pre-login); the standing settings-tab directive (deferred memory item) may revisit them later, not this packet.

## 4. Files expected to change

- **Modified:** `web/package.json` + `web/package-lock.json` (R1/R2/R3 deps); `web/vite.config.ts` (tailwind plugin, `@` alias); `web/tsconfig.app.json` (alias); `web/src/index.css` (imports, tokens, `@theme inline`, kill-switch, density, aurora CSS, shell chrome CSS; landed per-surface rules retained); `web/src/App.tsx` (aurora div, shell markup); `web/src/ConnectionState.tsx` (pill restyle, states/labels unchanged); `web/index.html` + `web/public/manifest.webmanifest` (R11 colors only); `web/src/main.tsx` (font imports, density default class — if not in `index.html`); `web/src/responsive.test.ts` + `web/src/shell.test.tsx` (honest updates); `components.lock` (R4); `P3/CONVENTIONS.md` (§47 + the §41 dated note).
- **New:** `web/components.json`; `web/src/lib/utils.ts`; `web/src/ui/*` (vendored + owned primitives) + their test files; a motion/tokens structural test (R17) if not folded into `responsive.test.ts`.
- **Expected NOT to change** (the evaluation checks this list): any `.go` file, `go.mod`/`go.sum`, `.github/workflows/ci.yml`, `web/src/{api.ts,events.ts,routes.ts,router.tsx,routes.test.ts,escape-scan.test.ts,scans.test.ts,devproxy.test.ts,doubles.ts,testing.tsx,live.ts,push.ts,kanban.ts,chatRuntime.ts,chatFacts.ts}`, every surface component (`MissionControl/Board/TaskDetail/Fleet/Filters/Inbox/Settings/settingsForm/Chat/Deliverable/History/Workforce/PushDevices/Login/Stub`), `web/src/fixtures/**`, `web/public/sw.js`, `Spec/**`, `Docs/**`, `Research/**`, `P3/STATE.md`. (`test-setup.ts`/`vitest.config.ts` only if a recorded harness need forces it — reason stated.)

## 5. Adopted components touched (every pin live-verified at execution; ratifications queued)

| Adoption | Research-time version (2026-08-04) | License | Funeral plan |
|---|---|---|---|
| tailwindcss + @tailwindcss/vite (dev) | 4.3.3 / 4.3.3 | MIT | remove plugin + imports; compiled utilities inline back into owned CSS (the sheet still ships compiled — no runtime loss); or daisyUI/owned-CSS per sourcing ranks |
| shadcn CLI (dev-time generator; OQ1 shape) | 4.16.1 | MIT | drop the generator — vendored TSX is owned code and keeps working; future components hand-written |
| @base-ui/react | 1.7.0 | MIT | vendored components re-seat on hand-rolled behavior or the Radix track (`radix-ui` 1.6.x alive); swap contained to the kit files |
| lucide-react | 1.28.0 | ISC | owned inline-SVG icon set (the Nexus 24×24 language — already the fallback style) |
| clsx / tailwind-merge / class-variance-authority | 2.1.1 / 3.6.0 / 0.7.1 | MIT / MIT / Apache-2.0 | trivial owned re-implementations behind `cn()`/variant maps (leaf utilities) |
| tw-animate-css | 1.4.0 | MIT | owned keyframes in index.css (it is CSS-only) |
| @fontsource-variable/inter · @fontsource/jetbrains-mono | 5.3.0 / live | OFL-1.1 (fonts; declared split read at installed ref) | self-vendored woff2 + owned @font-face (OFL permits); system-ui stack as the degraded fallback |
| React 19 + Vite + TS tree (landed entry) | — | tree set | **re-pinned** (lockfile hash moves) — dated lock edit, not a new adoption |

FC-v1's four picks are **untouched at their recorded pins** (18.0.1 / 3.3.3 / 0.14.27 / 3.8.0). NOT adopted, named so no session mistakes them: sonner or any toast lib, framer-motion or any animation lib, any icon set beyond lucide, any Radix-track package, next-themes, @fontsource CDN variants, Testing Library, a linter. Adopt-don't-fork binds: exact pins, no `node_modules` patching, no `patch-package`.

## 6. CONVENTIONS constraints binding this packet (by section)

- **§1/§2/§3** — one binary; `web/` stays Go-free; zero new Go modules; stdlib `testing` Go-side; green precondition for every commit; the real `components.lock` is validated inside `go test ./...`, so lock mistakes fail the battery, not just the gate.
- **§4** — strict-JSON lock, exact pins only (`^ ~ > < =` rejected — npm side included via `checkNPMExactDeclarations`), removal only by funeral plan, dated lock edits for any pin movement (the tree entry this packet).
- **§5** — explicit staging; subject format + trailer; no push; never edit `Spec/`/`Docs/`/`Research/`/`P3/STATE.md`; never read `*-api-key.txt`; never stage the four operator items.
- **§41** — npm rail mechanics verbatim (claims, computed closure, pin lockstep, tree-identity pin, `npm ci --ignore-scripts`, no install script admitted without a recorded decision naming the package); embed/serve posture untouched; the "No CSS framework" sentence superseded via R21's dated note, never silently.
- **§41-B** — escape-scan allowlist EMPTY; no web-storage; owned router untouched; responsive skeleton discipline (phone-first, min-width-only); `css: true` stays; structural constants carry named reasons.
- **§42** — the honesty primitives' rules bind the kit: absence rendered not filled, money as served, stale never poses as live, **no progress/percentage/ETA primitive**.
- **Batch rules (STATE 2026-08-04)** — proposal is design authority subordinate to spec; bundled assets only, zero CDN; **AGPL ideas-only (explicit evaluation item)**; permissive-only exact pins through the rail; honest assertion updates, counts never drop silently; bundle measured at landing, no silent growth; behavior-preserving; FC-v1 picks untouched; every ⚙ registry-routed; Layer-1 catalog band untouched (no packet need here).

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a command)

1. `web/package.json`: the R1+R3 packages present, ALL exact-pinned (grep shows no `^`/`~`/range anywhere); R3 set complete and **nothing beyond the closed set** (diff `dependencies` against R3 + the four FC-v1 picks + react/react-dom); `package-lock.json` moved in lockstep.
2. Live-verify evidence recorded: each new lock entry's `license.checked` = execution date, notes name the registry re-check (version/license/deprecation/name-current); any research-time→execution version drift is stated, never silent.
3. `components.lock`: one entry per differently-versioned claimed package, S16.2-complete (nine fields, path-scoped licenses incl. the OFL-1.1 font-files scoping, funeral plans, watch rows, S16.4 walks, #10 queued); the tree entry re-pinned to the new lockfile hash as a dated edit; `go run ./tools/lockgate` → OK with every new package covered (29 → 29+N, N named).
4. Tokens: `:root` defines the full shadcn base set from the Nexus values (spot-check `--background: #07070d`, the `rgba(148,148,190,…)` border/panel family, `--primary` violet) + the Sinet extensions + the re-pointed legacy nine; `@theme inline` maps them; dark-first (`color-scheme: dark`, no light ramp built).
5. Fonts bundled: Inter variable + JetBrains Mono imported from Fontsource; built `dist/assets/` contains the woff2 files; body type is Inter; numbers/IDs/timestamps render mono with `tabular-nums` (class/token present and used by the kit).
6. **Zero CDN**: grep of built `internal/webui/dist` shows no external `http(s)://` asset reference; no `fonts.googleapis`/`cdn.` anywhere in `web/src` or dist.
7. Aurora: one fixed `aria-hidden` `pointer-events:none` div in the shell; four radial gradients; drift gated to `no-preference`; static under `reduce`.
8. Shell restyle: sidebar (grouped nav, gradient logo mark) + blurred topbar + content in the new language; `routes.ts` **byte-unchanged**; all 7 nav routes reachable; Sign out, who-line, six-state ConnectionState (labels intact, non-live states never read as live) all survive; phone width (375px) keeps every surface reachable (wrapping/collapsible nav), min-width-only queries, no fixed-pixel layout width — `responsive.test.ts` invariants pass against the new markup.
9. Primitive kit: all eleven components + `cn()` exported from the kit home; each has a test that RENDERS it (variants included); Chip implements the one formula over 7 tones; StatTile has hairline + mono value and **no count-up**; Toast caps at 4, no new dependency; no component or identifier trips the landed scans (percent/pct/ProgressBar/localStorage bans).
10. D5 Timestamp: `live` variant renders relative time BESIDE verbatim UTC in `<time dateTime>` (UTC present in DOM — asserted), `audit` renders UTC only; honest absence on empty; tests cover both variants; **no landed call site migrated** (`parts.tsx` Stamp untouched).
11. Density: both root classes ship; comfortable applied by default; a test toggles compact and asserts spacing tokens change; no storage, no switcher, no new ⚙.
12. Reduced motion: the global kill-switch block exists in `index.css`; every new `@keyframes`/animation is authored gated; the structural test proves presence and can fail.
13. Escape/behavior scans untouched and green: allowlist EMPTY, 8+ banned tokens, planted probes intact; vendored TSX passes the scan; `scans.test.ts` unmodified and green.
14. **No behavior change**: the §4 "expected NOT to change" list holds (`git diff --name-only` against it); api.ts/events.ts/routes.ts/router.tsx byte-unchanged; no new API call, no route added/removed/renamed; fixtures untouched.
15. Test integrity: suite ≥ 432 tests, all green; per-file assertion deltas for `shell.test.tsx`/`responsive.test.ts` stated with reasons in the executor report (the B6-9 discipline); total files ≥ 23.
16. **Full battery green in R22's order** (web typecheck/test/build → gofmt/vet/build/`go test -count=1` over the REAL dist → lockgate) and **bundle recorded**: full build output table vs reference 976.99 kB / gzip 292.96, growth itemized (JS delta, CSS delta, font assets listed), no anomaly unexplained.
17. **AGPL item**: zero Odysseus-derived code/CSS/markup anywhere (evaluation attests after reviewing the new files against the pattern study's descriptions — ideas only).
18. Counters: zero Go changes (`git diff --stat` shows no `.go`, no `go.mod`/`go.sum`); ⚙ 118/33 byte-unchanged; CI workflow byte-unchanged; CONVENTIONS §47 appended + the §41 dated note present, §1–§46 otherwise untouched; nothing pushed; the four operator items unstaged.

## 8. Open questions for coordinator disposition (nothing resolved silently)

- **OQ1 — the shadcn CLI's rail shape.** RECOMMENDED: exact-pinned **devDependency** (`shadcn@<live-verified>`) + a `kind: toolchain` lock entry claiming it (the Vitest precedent) — reproducible re-runs for UI-2..7's additional components, dev-classified so the closure is toolchain-scoped wholesale. Alternative: `npx shadcn@<exact>` one-shot (the create-vite precedent) with the version recorded in the Tailwind entry's notes + §47 — leaner tree, but every later packet re-records its invocation. Dispose (a)/(b).
- **OQ2 — vitest × Tailwind plugin interplay.** `vitest.config.ts` merges `vite.config.ts`, so the Tailwind plugin runs under the harness and `css: true` must keep resolving `@import "tailwindcss"`. If the executor hits a real harness incompatibility, the sanctioned fallback is gating the plugin under the `VITEST` marker **with the reason recorded and the css-processing consequences re-verified** (the dev-proxy gating precedent) — not silent config surgery. Pre-dispose: executor latitude with recording, or stop-and-flag?
- **R7's legacy re-point** is bound in-brief as a recorded reading (rationale stated there) — flagged here for the coordinator's landing ratification alongside the §41 dated-note supersession and the R11 chrome-color move.

**Spec conflicts found at grounding: none.** The one tension — S15.1/§41's landed "no CSS framework" posture vs this adoption — is not a spec conflict: S15.1's rider is the lockfile-pinned-minimal-tree discipline and §41's sentence carried its own door ("anything more enters through the S16.4 rail"), which is exactly the path taken under the operator-approved proposal; R21 records the supersession datedly. S15 is silent on visuals (the D8 grounded finding), so the proposal governs appearance and nothing here touches an S15 behavior clause.

## 9. SCOPE VERDICT

**One opus sitting is plausible but not certain** — one ecosystem (web-only, zero Go), but substrate bring-up + a full restyle + eleven primitives with tests is wide. Pre-authorized A/B split at the natural seam (STATE batch rules; the B6-2/B6-3 precedent — a stop at the seam is a success mode):

- **A — substrate + look:** R1–R14 + R17 + R19–R23's slice (deps, lock entries, tokens, fonts/icons, aurora, shell restyle, kill-switch, honest test updates, battery + bundle). Lands as: the app re-based on the Nexus language end to end, everything green.
- **B — the kit + density:** R15–R16 + R18 + the rest (primitives + tests, D5 primitive, density classes, §47 completion).

Order A → B (B builds on A's tokens/utilities). If the executor reaches the seam green with budget, continuing is sanctioned; stopping at A-landed is equally a success.
