# P3-B6-5 brief — Oversight surfaces: mission control + live board + task detail + fleet + personal filters

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.5** owning + **S15.3** + **S15.12** (`Spec/drafts/S15-frontend-api.md`); the feature-list items S15.5 binds by reference — **S1.3** (card face), **S1.4** (filters), **S1.10** (phone), **S2.2/S2.4/S2.5** (live inspection, human decisions, cost altitudes), **3.5** (effort modes + disclosed downgrade) in `Docs/agent-platform-feature-list-v1.md`; **FC-v1 §1** (`Spec/frontend-components-v1.md` — the @hello-pangea/dnd pick and its decision notes); **G2 D2.8** via `Spec/benchmark-preregistration-v1.md` §13 (the receipt labels — frozen registered text). `P3/CONVENTIONS.md` binds: **§5, §7, §9, §30, §38 (incl. rulings (a)/(b)), §39-B (hint), §41 + §41-B ENTIRE** (constraints in §6 below). **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is packet 9/13 of B6** (queue `P3/STATE.md` §B6; B6-1/2A/2B/2C/3A/3B/3C/4 are landed). It is the **first real consumer of the app shell**: the four B6-5 stub routes (`/` mission control, `/board`, `/tasks/:id`, `/fleet`) become live views, plus the S1.4 personal filters. It builds **web-side views over landed reads** — it re-implements NO server machinery, and the Go side changes ONLY per the dispositions of OQ4/OQ5/OQ6 (additive read fields closing grounded gaps — never a new route family, never a mutation verb, never a migration). The inbox surface, settings UI, verdict forms (B6-6), assistant (B6-7), review/try-it/map (B6-8), and push/PWA + the S15.12 sweep (B6-9) are NOT this packet — their stubs stay.

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit — S15/FC-v1 frontend scope is not S10/S11 security-dense; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-29 — do not relitigate)

**The data plane this packet consumes (all routes read from `internal/api/api.go:329–470`; every non-auth route behind `requireIdentity`, owner-scoped server-side, 404-before-403):**

- **`GET /api/runs`** (reads.go:243) — `RunList{runs[], cursor, truncated}`; `RunListItem` (reads.go:220–234) = {run_id, owner, task_id, state, **waiting_on_human**, **parked_until**, wedged, stage, lane, generation, created_ts, updated_ts, last_activity_ts}. Filters: `?person` (member naming another → 403, reads.go:170–180), `?status` (closed S02.3 stored set, reads.go:63–67 — `wedged` is derived, never a status value), `?task`, `?since`/`?until` (RFC3339, **UTC-normalized** reads.go:192–208), `?limit` (default 200 / max 1000, reads.go:49–52). `cursor` = head event_seq at projection time — the S15.3 snapshot-then-tail cursor on EVERY read below too.
- **`GET /api/runs/{run}`** (reads.go:399) — `RunDetail{card: RunCard, live_activity: {cursor, topic:"run", run_id, last}, spawn_records[], routing_records[], cursor}`. `RunCard` (projection.go:99–113) = {run_id, owner, state, waiting_on_human, parked_until, stage, tool{name, args_digest}, counters{tokens, api_equiv_cost_usd, elapsed_s, steps}, last_activity{type, ts, line}, wedged, lane, generation, ceilings}. `routing_records` = the `routing.decided` rows verbatim — whose payload contract carries **effort mode** and the mandatory plain-language reason (`internal/eventlog/contract.go:357`): the only landed home of the S1.3 "effort mode + disclosed downgrade note" data.
- **`GET /api/tasks`** (reads.go:505) — `TaskList{tasks[], cursor, truncated}`; `TaskListItem` = {task_id, owner, title, **kanban_status**, project, created_ts, latest_run: {run_id, state, wedged, last_activity_ts}}. Filters: `?person`, `?status` (kanban), `?project` (the honest `(no project)` bucket is selectable, reads.go:69–72), `?since`/`?until`, `?limit`.
- **`GET /api/tasks/{task}`** (reads.go:676) — `TaskDetail` = {task_id, owner, title, kanban_status, created_ts, cursor, **spec** + **plan** (intake pair; absent → `artifacts_absent` with reason), **stage_progress[]** {run_id, seq, type, stage, kind, outcome, ts} derived from `stage.started`/`stage.finished`/`intake.state` (reads.go:828), **lineage** {project, project_choices, succeeds[], succeeded_by[]} (both directions of `task_successor_of`), **runs[]** {run_id, state, created_ts, **receipt** (receipts.usage_json VERBATIM) | receipt_absent}, pipeline}. **§38 RULING (a) binds the rendering** (CONVENTIONS:671): a pre-approval task serves its DRAFT spec/plan with serialized status fields — S15.5's "confirmed specification" is the APPROVED-view display contract **this packet renders**: the view must show the approval status honestly (draft labeled draft), never present a draft as confirmed.
- **`GET /api/runs/{run}/receipt`** (reads.go:894) — the S10.10 receipt; its `direct_use.label` is `internal/metering/receipt.go:19` verbatim.
- **`GET /api/meters`** (meters.go:89) — `Meters{lanes[], burn_rates, budgets, limit_events, per_person, per_period, cursor}`; `MeterLane` per (person, lane) = {owner, lane, weighted_consumption, cache_read_weight, **assumed**, pressure_applicable, pressure, budget_declared, budget_remaining, total/active/parked_runs, **parked_until**}. The five `MeterView`s serve Layer-0 view rows **VERBATIM at the view's own grain** (`cost_burn_rate` is per PERSON — usd per observed day; the B6-1 OQ2 disposition and the §37 money-is-read-never-computed rule are why `FleetLane.BurnRate`/per-model burn rate stay nil, projection.go:269–276). Absent surfaces carry `absent` with the reason.
- **`GET /api/events/views|catalog|views/{view}|query/{query}?slot_*=|ask?q=|search?q=|open-sql?q=`** (historyapi.go) — the routed S14.10 layers; `history.Answer` served verbatim (layer, confidence — open-sql visibly "lower-confidence", notes, disambiguation card, audit); refusals and cards are 200-class ANSWERS, never bare errors (historyapi.go:218–231). The mission-control "filterable history" instrument.
- **`GET /api/approvals`** (approvals.go:115–155) — `ApprovalItem` {id "<kind>:<native-id>", kind, owner, run_id, tier, answerable(+reason), batchable, step_up_required, payload_hash, observed_ts, expiry_at, stale(+reasons), actions[], card…}: the "what-needs-me" feed.
- **`GET /api/deliverables?state=|project=|type=`** (deliverables.go:52, 136, 154–156; states in-review/accepted/superseded) — the "review-ready" feed (state=in-review) and the task detail's revision data via `GET /api/deliverables/{deliverable}` (immutable numbered revisions + doors-as-data).
- **`POST /api/tasks/{task}/priority-hint`** (meters_verbs.go:347) — **the ONE mutation this packet's views may call.** Body `{rank: int64, reason?}`; `|rank| ≤ 1000` (structural, meters_verbs.go:87–95); **0 = "no hint"** — a re-rank strategy must never assign 0 meaning "first"; lower = claimed earlier. **STRICTLY the caller's OWN task — the operator is deliberately NOT excepted** (meters_verbs.go:366–373). Applies to the task's QUEUED runs only; a stale board answers 200 `{applied:false, detail:"nothing to reorder…"}` — honest, not an error (meters_verbs.go:407–411). Response `{task_id, rank, runs[], applied, detail}`; co-mints `decision.recorded` (card `priority_hint:<task>`). Scheduler semantics (scheduler/workload.go:164–234): the hint is a score-multiset-preserving permutation **within (user, class)** — it never outranks the class ladder, never crosses persons.

**The SSE contract (sse.go + topics.go — consumed verbatim, never renegotiated, §30/§41-B):** one endpoint `/events`; `?topics=` comma-set from the closed `{run, board, fleet, inbox}` (unknown → 400); `topics=run` requires exactly ONE `?run=` (owner-checked 404/403, sse.go:244–256). A topic connect emits one `event: snapshot` frame per topic — `data: {topic, cursor, state}`, no `id:` — then tails from the snapshot head; **snapshots supersede `Last-Event-ID`** (sse.go:136–148). Data frames: `id:` = event_seq, `event:` = the stored type, data = `wireEvent{seq, run_id?, generation?, user_id, type, schema_version, topics[], payload, ts}`. No-topics = the unfiltered relay (delta-only, still owner-scoped). **The per-topic snapshot STATES are the B5-2 projections and they are FACE-INCOMPLETE for S15.5** (measured): `BoardSnapshot.tasks[]` = {task_id, title, kanban_status, latest_run{run_id, state, wedged, last_activity_ts}} — no owner, no project, no stage, no effort, no cost, no waiting-on-human (projection.go:194–214); `FleetSnapshot` = lanes (burn_rate nil by design) + `local_seats` **empty at v0** + `gpu` **nil at v0** — declared seams whose comment names "the B6 fleet surface" as their landing place (projection.go:287–301). The rich reads are REST.

**The client machinery (web/src/events.ts + §41-B — consumed, extended only per OQ1's disposition):** `sharedStream()` holds the tab's ONE EventSource; `subscribe({topics?, runID?, types?, onSnapshot?, onEvent?, onResnapshot})` joins the fan-out; **a subscription must NAME the event types it consumes** (EventSource has no catch-all; the transport names every frame with its stored type — §41-B). `desiredQuery()` (events.ts:225–240) unions subscribers' topics; **any subscriber with no/empty `topics` forces the union to `''`** — the unfiltered relay — **and `useConnection` (web/src/App.tsx:135–162) subscribes topicless for the whole authed lifetime: as wired, view topic-subscriptions will neither narrow the connection nor receive server `snapshot` frames. This is carried finding F7 — OQ1 dispositions the wiring before the executor builds a view on it.** Per-subscriber snapshot debt (events.ts:295–315): `onResnapshot(reason, done)` — reasons `connected`/`stream-rewound`/`frame-unreadable`; `done()` clears ONLY that subscriber; the stream is `live` only when nobody owes. Statuses: connecting/catching-up/live/disconnected/logged-out/unreachable, rendered by `ConnectionState` on every route. **Every B6-5 view wires its REST re-read to `onResnapshot` and calls `done()` when the read lands — a view that ignores the signal renders stale data as live (the §41-B D3 lesson).**

**The receipt labels — G2 D2.8, VERBATIM strings (find-and-render, never re-typed into new literals):**

- Per-run heuristic label: **`"direct-use estimate (heuristic)"`** — `internal/metering/receipt.go:19` (`metering.DirectUseLabel`) and `internal/benchmark/registered.go:57` (`LabelHeuristic`; deliberately duplicated across the import wall, both test-pinned against the registration). Present on EVERY receipt's `direct_use.label` from day one.
- Aggregate measured label: **`"measured (benchmark n=%d)"`** — `internal/benchmark/registered.go:59–65` (`labelMeasuredFmt`/`MeasuredLabel(n)`), the **BENCH-REG §13.2 frozen form** (`Spec/benchmark-preregistration-v1.md:141`): once a domain accrues ≥10 measured pairs the AGGREGATE figure carries it; **per-run receipts keep the heuristic line — both labels side by side**. Served via the `done_directly` Layer-0 view / `cost.done_directly` catalog query (`DoneDirectly` shape: {measured, n, median_usd, label, heuristic_label, summary} — benchmark/feed.go:40–53).
- **S15.5's inline quotation "measured, n=…" differs from the registered/landed text** — a letter-vs-letter divergence between binding documents. OQ3 raises it; the grounded reading: the UI renders the label string **the API serves, verbatim, untranslated** — it never composes a label of its own.

**Grounded data-shape gaps (each an OQ — the coordinator dispositions; none is resolved silently):**

1. **The S1.3 card face has no single serving read.** Face = what it is ✓ (title), whose ✓ (owner — on `/api/tasks`, not on the board snapshot), current stage ✗ (on `RunListItem`/`RunCard`, NOT on `TaskListItem.latest_run`/`BoardRun`), effort mode + disclosed downgrade note ✗ (only inside per-run `routing.decided` records — N+1 per card), cost so far ✗ (only `RunCard.counters.api_equiv_cost_usd` via run-detail/run-topic; NO list read carries it), waiting-on-human ✗ (on `RunListItem`, not on the task list). → OQ4.
2. **"Every human decision" (S2.4) has no serving read.** `decision.recorded` rows are owner-attributed with **NO run_id** (approvals.go:1322–1327 — the subject travels in the payload); no Layer-0 view and no catalog query serves the Human-decision family (grep-verified; `status.waiting_on_human` is OPEN asks only). Ask-answers/cancels mint run-scoped rows; effect approvals live in `decision.recorded` + the effects journal. → OQ5.
3. **`queue.hint_rank` is write-only from the surface's viewpoint** — read ONLY by the scheduler's claim loop (scheduler/claim.go:81); no REST/SSE read serves it, and the board/task lists order by `created_ts`, so the board cannot render recorded drag order truthfully after a reload. → OQ6.
4. **The kanban vocabulary is provisional-until-B6** (stage/skeleton.go:775–777: "provisional strings until the S15/9.1 board semantics land (B6)"): values observed in producers = `intake` (intake/pipeline.go:229), `executing`, `verifying`, `done`, `attention`, `cancelled` (skeleton.go:342–751). → OQ7.
5. **Fleet seat/GPU seams are declared-empty** (projection.go:287–301) and the receipt's mode line is the static seam note (`"no mode change (S10.6 downgrade ladder lands with routing S08/local tier S12)"`, receipt.go:145–146) — honest absences to RENDER, not gaps to fake. → OQ8 (seats/GPU); the mode note renders verbatim (R14).

**Counters at open (assert before changing anything):** migrations **0001–0019** (`user_version` 19); event inventory **91 minted / 5 declare-only / 96 registered**; ⚙ **118 keys / 33 domains** (`internal/settings/index.go` byte-unchanged); `components.lock` **29**; conformance seed rows **12**; CONVENTIONS **§1–§41 (+ §41-B)** — this packet appends **§42**; `web/` 30 source files (31 tracked paths), **57 vitest tests / 8 files** green; **43 Go test packages** green.

---

## 1. Numbered requirements

### A — Mission control (`/`) [S15.5 ¶1; S3.1]

- **R1. The home surface: one live screen of the household's work** [S15.5]. Renders, live: running / queued / parked (each parked item showing its **"parked until…"** from `parked_until` where a marker exists — absent marker renders as parked-without-horizon, never a fabricated time) / **blocked-on-a-human** (waiting_on_human; the `status.waiting_on_human` catalog query and `/api/runs` rows are the instruments) / **recently finished** (terminal states within a recency window — the window is a structural display constant with a named reason, §41 precedent). Who owns what is visible on every item (owner fields — the operator sees everyone, a member their own; the server already scopes, the view filters nothing to create privacy, S15.2).
- **R2. Consumption + automation-budget meters with burn rates** [S15.5; S2.5; S10 via /api/meters]. Renders `Meters` faithfully: per-(person, lane) gauge rows including the **`assumed` cache-weight label** (G1 Def.10 — the assumption is stated, not dropped), pressure only when `pressure_applicable` (never a fabricated denominator), budget remainders with the **honest undeclared-budget absence** (`budget_declared:false` + the view's absence reason rendered as data); **burn rates at the view's own per-PERSON grain, verbatim** — the recorded reading: S15.5's "per person and model" is satisfied by per-(person,lane) consumption METERS plus per-person BURN RATES; the client never divides a person's rate across lanes (money is read, never computed — §37; the B6-1 OQ2 disposition). `MeterView.absent` renders as the absence it is.
- **R3. Filterable history** [S15.5; S2.10 via S14 layers]. A history panel over the `/api/events` family: the Layer-0 view registry + Layer-1 catalog rendered as the choice surface (`HistoryRegistry` — served registries, never a hand-copied list), slot-filling for catalog queries (`?slot_*=`), and answers rendered with their **layer + confidence + notes + audit verbatim** — a disambiguation card and a refusal-with-audit are ANSWERS to display, not errors to swallow; open-sql's lower-confidence flag is VISIBLE when that layer's answers render [G3 D3.5]. Mission-control filters (project/person/status/date) ride the landed list-read filters.
- **R4. Drill-through** [S15.5]. Every item links to its task detail (`/tasks/:id`) or run trace; links use `hrefFor` over the §41-B URL contract — no hand-built paths.

### B — Live board (`/board`) [S15.5 ¶2; 9.1; S1.3; FC-v1 §1]

- **R5. Cards move from the live feed** [S15.5; S15.3]. The board loads its REST snapshot (`/api/tasks` + the OQ4-dispositioned face data), tails per OQ1's dispositioned wiring, and re-reads on its `onResnapshot` signal (calling `done()` when the read lands). Card movement follows stored state — `kanban_status` per OQ7's disposition — with **no client-side status invention** and no percent/fraction/ETA anywhere (§30's negative extends to the web: no view computes or renders a progress percentage, completion fraction, or time estimate the API does not serve — and the API serves none).
- **R6. The card face is EXACTLY the S1.3 set** [S1.3; S15.5; 3.5; G2 D2.1]: what it is (title), whose it is (owner), current stage, effort mode **with any disclosed downgrade note** (from the routing record's effort + plain-language reason per OQ4's disposition — the note renders when the platform disclosed one, and is ABSENT when none was disclosed, never synthesized), cost so far (the read money figure per OQ4 — rendered as served, with its absence honest when no meter row exists), and waiting-on-human. Nothing more on the face (the exhaustive trace is the detail's job), nothing less.
- **R7. Project grouping + follow-up lineage visible** [S15.5; S1.1–S1.2 semantics]. Cards group by project (the `(no project)` bucket renders as its own honest group, never dropped — §37); follow-up lineage renders from `TaskLineage` edges (succeeds/succeeded-by) on the card/detail; `project_choices > 1` renders the ambiguity, never collapses it.
- **R8. Drag-drop uses @hello-pangea/dnd 18.0.1 — and stage columns are NEVER writable by drag** [S15.5; FC-v1 §1]. The installed pick (probe-green since B6-4, picks.test.tsx:25) is wired for real: lists-only kanban (its documented limit is immaterial for S1.3 — FC-v1 §1). **Structural, not policed:** a cross-column drop target must not exist for stage/status columns — stage is FSM state owned by the control plane [S02]; the ONLY droppable reorder surface is the caller's own queued cards (R9). A drag that is not that is a no-op with no verb call. Adopt-don't-fork: no patch, no fork, version pinned; a dead/relicensed finding at execution re-verification = STOP + the pre-registered fallback (pinned @dnd-kit/core 6.3.1) as a flagged coordinator decision. The @dnd-kit 0.x rewrite family stays excluded outright [FC-v1 §1].
- **R9. Drag = own-queued reorder → the landed hint verb** [S15.5; §39-B]. A completed reorder drag POSTs `/api/tasks/{task}/priority-hint` per OQ6's dispositioned rank strategy (respecting: |rank| ≤ 1000; 0 = no-hint, never a position; own tasks only — cards not owned by the caller are not drag-reorderable even for the operator, matching the verb's deliberate no-operator-exception); the response's `applied:false` stale-board answer renders honestly (the board re-reads); the `detail` line's honesty ("breaks ties among your own same-class queued work…") is surfaced, not hidden — the drag must not claim scheduling authority the hint does not have.

### C — Task detail (`/tasks/:id`) [S15.5 ¶3; 9.2; S2.2; S2.4; G2 D2.8]

- **R10. Spec + numbered AC + approved plan** [S15.5; S06 artifacts]. Renders the served spec/plan with numbered acceptance criteria; **the §38 ruling (a) display contract**: approval status rendered honestly — a draft pair is labeled as the draft it is, an approved pair as confirmed; `artifacts_absent` renders its reason (a task before drafting has none — a fact, not an error).
- **R11. Per-stage progress + live activity feed** [S15.5; S2.2]. The stage story from `stage_progress` (started/finished/outcome per stage, across the task's runs) plus the LIVE activity feed for the task's active run: seeded from `RunDetail.live_activity` / `RunCard.last_activity`, tailed per OQ1's wiring (the run-scoped delta frames), re-read on the resnapshot signal. Counters render as the monotonic values they are — never a derived percentage.
- **R12. Every human decision along the way** [S15.5; S2.4] — per OQ5's dispositioned read. Renders who approved which gate, who answered which question, who accepted, and when — from served rows only; absences honest (a task with no decisions yet shows none).
- **R13. Deliverable revisions** [S15.5; S13 via /api/deliverables]. The task's deliverables with their immutable numbered revisions (list + links into `/deliverables/:id` — the B6-8 stub; rendering diffs/comments/accept is B6-8's, NOT this packet's; the link is the honest door).
- **R14. The receipt, with the ratified labels VERBATIM** [S15.5; G2 D2.8; BENCH-REG §13]. Renders each run's receipt from the served JSON: ceremony-vs-execution itemization, unpriced honesty (`UNPRICED`/worst-tier as served), the park history, the mode line's seam note verbatim, and the done-directly figure **under the exact label string the API serves** — `"direct-use estimate (heuristic)"` per run, and the aggregate measured label (`"measured (benchmark n=…)"` form) wherever the done-directly aggregate renders — **byte-for-byte as served, never re-typed, never translated, never abbreviated** (OQ3 settles the S15.5-paraphrase question; the UI obligation is verbatim-as-served either way). `receipt_absent` renders its reason.

### D — Fleet (`/fleet`) [S15.5 ¶4; 9.3; S2.5]

- **R15. Per-person/per-model meters + budget remainders** [S15.5]. The `Meters` lanes at their (person, lane) grain + the budgets view (declared remainders; undeclared = the honest absence with its served reason); the per-person and per-period cost views rendered verbatim at their own grains.
- **R16. Limit-event status + history** [S15.5; 3.2]. Current park state per lane (`parked_runs` + `parked_until` — "parked until…") and the `limit_event_history` view rows; all filterable (`?person`/`?lane` + client-side presentation filters that narrow display only).
- **R17. Accounts always distinguished** [S15.5; S3.10; D2]. Every figure carries its owner visibly — the fleet always shows WHOSE account burns; rows are never summed across owners client-side (a cross-person total is money arithmetic the client must not do). The declared seams render honestly per OQ8: local seats/GPU as the named absences they are at v0.

### E — Personal filters [S15.5 ¶5; S1.4; S1.10]

- **R18. Four first-class views, identity-scoped** [S1.4]: **what-needs-me** (the caller's `ApprovalItem`s — approvals + questions — plus review-ready deliverables via `?state=in-review`), **mine** (own tasks/runs — a member's default scope; the operator's "mine" uses `?person=<self>`), **running**, **finished-today** (`?since=` local-midnight UTC-normalized). Each is a stable, deep-linkable presentation over the landed reads — server scope does the authorization (client filtering is presentation only, S15.2); each is live per R20 and drills through per R4.
- **R19. Phone-complete** [S1.10 via S15.12]. All four filters, board glance, fleet glance, and task status are usable at phone width: the §41-B structural discipline (phone-first CSS, `min-width` widening, no fixed-width container, no horizontal scroll at 375px) extends to every new view, asserted structurally from the stylesheet as the landed responsive tests do. Diff-review-grade density is NOT required on the phone (desktop-comfortable is S15.12's split) — but nothing in this packet's scope may be desktop-only.

### F — Cross-cutting (every view)

- **R20. Live by default, with visible disconnect state** [S15.12; S15.3]. Every view updates from the feed — manual refresh never required for currency; the shell indicator plus per-view honesty: a view whose subscriber owes a resnapshot renders its catching-up state (or visibly stale-marked data), NEVER fresh-looking stale data; `disconnected`/`unreachable` states leave the last-known data visibly marked stale. Each view's subscriber declares its event TYPES (§41-B — the enumerated trigger set, chosen against the topic map `topicsForEvent`, topics.go:51–82) and clears ONLY its own debt via `done()`.
- **R21. The wiring per OQ1's disposition** [S15.3; F7]. Whatever OQ1 ratifies, these hold: at most ONE EventSource per tab (the landed invariant, observable via `opened`); snapshot-then-tail per view (REST snapshot at its served `cursor`, then deltas); on any not-proven-continuity signal the view re-reads — it NEVER patches blind [S15.3]; no second stream client, no polling loop, no ticker.
- **R22. Escape-by-default; no client-side side-truths** [S15.12; §41-B; §38]. All rendered content rides React's default escaping; the escape-scan allowlist stays EMPTY (it widens at B6-8, in B6-8's commit); zero `dangerouslySetInnerHTML`/`innerHTML`/etc. anywhere new. Views hold no state that outlives a re-snapshot as truth (derive-from-log applied client-side: served rows + frames in, presentation out); model-derived strings (titles, activity lines, plain reasons) render as text, never as markup, never interpreted; no client-side money computation, no percent/fraction/ETA invention (the server serves none — the view renders none).
- **R23. Counters + untouched machinery** [§29–§41 precedent]. NO new npm dependency (`components.lock` stays **29**; dnd is installed — wiring it is consumption, not adoption); NO new ⚙ key (**118/33**, `index.go` byte-unchanged — client display constants are structural with named reasons, interim under the standing settings-tab directive); NO migration (0001–0019 byte-untouched); NO new event type (**91/5/96**; the hint verb's `decision.recorded` is landed); Go-side changes ONLY per OQ4/OQ5/OQ6 dispositions — additive fields/derives on EXISTING reads with their tests, no new route family, no mutation verb, `internal/history`/`internal/redact`/`internal/settings`/SSE frame contract byte-untouched; `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.
- **R24. CONVENTIONS §42 appended** (confirm the next free number at launch): the OQ dispositions as landed (wiring, face fields, decisions read, rank strategy, kanban reading, label rendering), the per-view type declarations, new structural constants with reasons, and this packet's readings. Earlier sections never rewritten. Commit subjects per §5 (`P3-B6-5: … (S15.5, S15.3, S15.12, FC-v1 §1)` + trailer).
- **R25. Battery + hermetic + $0** [§3/§5; §41]: `gofmt -l internal/ cmd/ tools/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` · `go run ./tools/lockgate` · `cd web && npm run typecheck && npm run test && npm run build` — all green. Vitest runs offline (scripted fetch/EventSource doubles — the FakeSource pattern, events.test.ts:17); NO test dials 8481/8482, the live front chain, any unit, or the network; `VITE_SINET_ADDR` stays unset in tests (§41). $0.

## 2. Seams — what belongs to LATER packets (leave named, never build)

| Later packet | Owns | B6-5 leaves |
|---|---|---|
| **B6-6** | inbox surface (`/inbox`, `/inbox/:id`): card rendering, tiers/batch/step-up UX, verdict forms, watchdog/drift/conformance card verbs' UI, the ninth (memory-conflict) kind, D4(b) eval-results surface; settings UI (`/settings`, JSON Forms) | those stubs; what-needs-me LINKS into `/inbox` items (the honest door — answering happens there, not in the filter) |
| **B6-7** | assistant (`/chat`), history-layer chat UX | the stub; R3 renders answers as data, no conversational UX |
| **B6-8** | review surfaces (`/deliverables/:id` — diffs, comments, accept, try-it; the ONE raw-HTML preview channel + escape-allowlist widening), workforce map | those stubs; R13 links only |
| **B6-9** | push/PWA completion, re-nag SLA, the S15.12 completeness SWEEP (final phone-complete audit), week-one drill | R19's structural responsiveness; the sweep re-checks it |
| **Not this packet** | new routes/route families, mutation verbs beyond consuming the landed hint verb, SSE server/frame changes, migrations, `internal/history`/`redact`/`settings`/`scheduler` internals, any npm addition, `node_modules` patches | — |

## 3. ⚙ settings consumed (by exact dotted key; none invented)

- **None newly read.** Server-side keys ride landed call sites (`obs.sse_keepalive` in sse.go; `effects.approval_expiry`/`freshness.max_age` inside the approvals list this packet reads). The browser cannot read the registry pre-login: every client-side number (recency window, refetch debounce, list caps) is a **structural constant with a named reason** (§41 backoff precedent), interim under the standing settings-tab directive — enumerated in §42.

## 4. Files expected to change

- **New/extended (web):** `web/src/` view modules for mission control, board, task detail, fleet, filters (replacing the four B6-5 `Stub` renders in App.tsx's route switch — routes/URLs NEVER change, §41-B); their vitest suites; `index.css` additions (owned CSS only); `web/src/api.ts` extended with the typed reads the views call (same-origin, credentialed, the landed request helper); `web/src/events.ts` ONLY per OQ1's disposition.
- **Extended (Go, ONLY per OQ4/OQ5/OQ6 dispositions):** `internal/api/reads.go`/`projection.go`/`meters.go` additive fields + derives, with tests (never-percent/redaction/owner-scope scans extended over changed shapes); nothing else in `internal/`.
- **Bookkeeping:** `P3/CONVENTIONS.md` (§42).
- **Expected NOT to change:** `web/src/routes.ts` patterns (fillers, not renames — `owner` fields may flip to '' for built surfaces), `router.tsx`, `Login.tsx`, escape-scan mechanics (its fixture set may grow), `internal/settings/index.go` (byte-unchanged), migrations, `internal/eventlog/contract.go`, `internal/history`, `internal/redact`, `internal/scheduler`, `internal/webui` serve rules, `components.lock`, `web/package.json`/`package-lock.json` (byte-unchanged — no dependency moves), `go.mod`/`go.sum`, `.github/workflows/ci.yml`, `Spec/*`, `Docs/*`, `Research/*`, `P3/STATE.md`.

## 5. Adopted components touched

**@hello-pangea/dnd 18.0.1 (Apache-2.0) — installed at B6-4, probe-only until now; THIS packet wires it.** FC-v1 §1 decision notes bind as constraints: purpose-built for the kanban **list-to-list** shape ("lists only, no grids" — immaterial for S1.3); re-verify liveness at execution (registry status at the pin — the B6-4 grounding observed latest still 18.0.1, repo alive 2026-07-28); a dead/relicensed/deprecated finding = STOP + pinned @dnd-kit/core 6.3.1 (frozen-but-working) as a flagged coordinator decision; the @dnd-kit 0.x rewrite family is excluded outright (re-visit only at its 1.0 with a dated note); adopt-don't-fork (no patches, no `patch-package`, pin never bumped). **No other adoption is touched; no new dependency enters** (a genuine need = an OQ through the S16.4 rail, never an executor decision).

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format + trailer, no push, never edit Spec/Docs/Research/STATE, never read `*-api-key.txt`.
- **§7/§9** — unversioned API consumed as-is; the session/identity mechanics are landed (§41-B): identity from `GET /api/auth/session` only, no localStorage identity, 401 → login with return-to.
- **§30/§38** — the SSE frame contract and owner-scope/redaction are SERVER facts the client renders, never re-implements; content-vs-projection (ruling (b)) means served content renders as data; ruling (a) binds R10; no percent/fraction/ETA anywhere.
- **§39-B** — the hint verb's landed semantics (own/queued/tie-break-only) are consumed verbatim; the drag UI must not imply more authority.
- **§41 + §41-B ENTIRE** — the web conventions: exact-pin npm discipline (no dependency moves here), owned router + URL contract (fill, never rename), one EventSource + per-subscriber debt + sparse-seq discipline + types-per-subscription, escape-scan empty allowlist, phone-first structural responsiveness, `VITE_SINET_ADDR` hermetic posture, `vitest run` offline.
- **The 35 D6-ratified readings** (§29–§37, binding precedent): derive-from-log (client state = served rows + frames, re-snapshot wins), no tickers, money read never computed, model output is untrusted input (rendered escaped, never interpreted), honest absence over fabrication, stale never poses as live, structural constants carry named reasons.
- **Hermetic + $0** — the standing host hazard: nothing dials the live Caddy admin chain (8481/8482) or any live unit; vitest offline; no paid call, no push, no install.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. Mission control renders running/queued/parked/blocked-on-human/recently-finished from served rows (driven vitest: scripted fetch doubles seed each bucket; parked shows "parked until…" when the row carries it and no time when it does not).
2. Meters render: assumed-label present, pressure only when applicable, undeclared budget = the served absence reason (probe: `budget_declared:false` fixture renders no number), burn rate per person verbatim; **no client-side division/summing of money anywhere** (scan + code read).
3. History panel: registries rendered from the served `HistoryRegistry` (not a hand list); a disambiguation card and a refusal-with-audit render as answers; the open-sql lower-confidence flag is visible on its answers (driven test).
4. Drill-through: every item's link resolves via `hrefFor` to `/tasks/:id` (or a named stub route); no hand-built path strings (grep).
5. Board: cards group by project incl. the `(no project)` group; lineage edges render; `project_choices>1` renders as ambiguity (fixture-driven).
6. Card face = EXACTLY the S1.3 set per OQ4's disposition — all six elements present when served, downgrade note ABSENT when none disclosed (both directions tested); no extra status invention; no percent/ETA (scan extended over new views).
7. dnd: board renders through DragDropContext/Droppable/Draggable at the pinned 18.0.1; **stage/status columns are not droppable reorder targets** (structural — the test asserts no cross-column drop wiring exists, and a simulated cross-column attempt calls NO verb); drag-end on own queued cards produces the OQ6-dispositioned `priority-hint` POST(s) with in-bounds nonzero ranks; `applied:false` renders the stale-board honesty and triggers a re-read (scripted double asserts the POST bodies).
8. Non-own cards are not reorder-draggable (fixture with two owners under an operator session: only own queued cards accept the drag).
9. Task detail: spec + numbered AC + plan render with the §38 ruling (a) status honesty (draft fixture labels draft; approved fixture reads confirmed); `artifacts_absent` renders its reason.
10. Stage progress + live activity: the stage story renders from `stage_progress`; the activity feed seeds from the snapshot and applies/refetches on frames per OQ1's wiring; the resnapshot signal triggers the re-read and `done()` (driven through the FakeSource pattern: connect → rewind frame → assert re-read then live).
11. Human decisions render per OQ5's disposition from served rows (fixture: an answered ask + an approved effect + a cancel — each shows actor + when); empty = honest none.
12. Receipt: itemization + park + mode-note + done-directly render; **the label strings are byte-equal to the served values** — a test pins the rendered text to `"direct-use estimate (heuristic)"` and the `"measured (benchmark n="` form from fixtures, and a scan proves no view file re-declares either string as its own literal (render-from-data, not from a client constant).
13. Fleet: lanes + budgets + limit-event history render at served grains, every row showing its owner; parked-until renders; seat/GPU seams render the OQ8-dispositioned honest absence; filterable; no cross-owner aggregation (code read + scan).
14. Filters: all four render identity-scoped over the landed reads (what-needs-me = approvals + questions + in-review deliverables — fixture-driven with all three), each deep-linkable at a stable URL, finished-today uses UTC-normalized since (test with a non-UTC local zone).
15. Live-by-default: every view subscribes with DECLARED types, wires `onResnapshot`→REST-re-read→`done()`, and renders catching-up/stale visibly (driven: a view mid-debt never shows fresh-looking data; the indicator reads live only after every mounted view settles — the per-subscriber debt test extended over real views).
16. The OQ1-dispositioned wiring is implemented as ratified; `opened` stays 1 per tab across navigation in a driven multi-view test; no polling loop, no ticker, no second EventSource (grep + test).
17. Escape/`no-side-truths` sweep: escape-scan green with allowlist EMPTY; no `dangerously*`/`innerHTML`/`eval` in any new file (the landed scan covers them — assert count grew over the new tree); no localStorage/sessionStorage state; no client-side money arithmetic (scan with probe).
18. Responsive: the §41-B structural assertions extended — new views' containers carry no fixed pixel width, phone-first rules exist for each view, nav still reaches everything at 375px.
19. Go-side (if OQ4/OQ5/OQ6 disposition additive fields): fields are additive (landed tests untouched-and-green), owner-scoped three-way per §38, never-percent + redaction-edge scans extended, money fields READ from the landed meter/view seams (no arithmetic — scan); NO new route beyond dispositions (route-table diff).
20. Counters: lock **29**, ⚙ **118/33** byte-unchanged, migrations **0001–0019** untouched, inventory **91/5/96**, `package-lock.json` byte-unchanged, CONVENTIONS §42 appended §1–§41 untouched, `Spec/`/`Docs/`/`Research/`/`P3/STATE.md` untouched, nothing pushed.
21. **Full battery green** (R25) both sides, vitest offline, **$0**.

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — the F7 wiring: how do views go live on the one connection?** As wired, `useConnection` subscribes topicless (App.tsx:135–162), pinning the tab's EventSource to the unfiltered relay — topic subs neither narrow the query nor receive server `snapshot` frames while it exists. **RECOMMENDED: (b) views ride the unfiltered relay with client-side selection + REST snapshot-then-tail.** Grounds: (i) the server's per-topic snapshot states are FACE-INCOMPLETE for S15.5 (measured above) — every B6-5 view needs its REST read regardless, and every landed read carries its tail `cursor` for exactly this; (ii) one stable connection avoids reopen-and-re-snapshot churn on every navigation (each union change re-opens and puts EVERY subscriber back in debt — events.ts:176–180); (iii) zero server and zero events.ts semantic changes; frames route in-app by their `topics` field + declared types, and views use deltas as apply-or-refetch triggers over their REST truth. Alternatives: (a) make the shell subscriber topic-scoped (e.g. board,fleet,inbox) so unions stay narrow and server snapshots flow — pays reconnect churn per navigation and still needs the REST reads; (c) change the union mechanics: distinguish "wants nothing" (`topics: []` contributes nothing to the union — the shell's true need: status only) from "wants everything" (`topics` undefined = relay) — one small honest events.ts change enabling (a) without the shell forcing `''`. **(c) is the named door if relay volume ever hurts**; dispose (a)/(b)/(c) — the executor builds nothing on this seam before the ruling.
- **OQ2 — the F9 fetch-shape tie.** Web tests script their fetch doubles; nothing mechanically ties them to the Go handlers' real shapes (field-diffed equal at B6-4 by hand; B6-5 multiplies the surface). **RECOMMENDED: committed golden fixtures with a Go assertor** — canonical response JSON per consumed read (deterministic: seeded test DB + fixed clock) committed under `web/src/fixtures/api/`; an `internal/api` test asserts each handler's serialized output equals its fixture (compare-only — tests never write outside `t.TempDir()`, §2; regeneration = an env-gated update mode or a small `tools/` command, run deliberately); the vitest doubles IMPORT the same files as their bodies. One shape, two consumers, drift fails the Go suite (the §40-C D3 one-predicate principle across the language boundary). Zero new dependencies. Alternatives: a shared schema manifest both sides validate (heavier, new machinery); generated TS types from Go structs (new tooling — rail territory); status quo + hand-diff discipline (the drift stays silent). Dispose.
- **OQ3 — the measured-label wording: two binding letters differ.** S15.5 (and the queue row) quote `"measured, n=…"`; BENCH-REG §13.2 (signed; changes only via its own §17) registers **`"measured (benchmark n=…)"`**, and the landed code renders exactly that, test-pinned against the registration (registered.go:59–65). RECOMMENDED: the UI renders label strings **verbatim as served** (which yields the BENCH-REG form); the registered text is the ratified letter — G2 D2.8's own resolution committed "formula text goes into the signed commit", making BENCH-REG the label's home and S15.5's inline quote a paraphrase. Also confirm the placement nuance as binding: the measured label belongs to the AGGREGATE figure only; **per-run receipts keep the heuristic line, both side by side** (§13.2 verbatim). If the coordinator instead reads S15.5 as controlling, that is an S00.9/§17 amendment question — flag, never a silent UI translation. Dispose.
- **OQ4 — the S1.3 card face read gap (a REPORT, pre-raised).** No list read serves stage/effort-mode/downgrade-note/cost-so-far/waiting-on-human on task cards (grounded above). Options: **(a) RECOMMENDED — additive server-side face fields** on `TaskListItem.latest_run` (and `BoardRun` if the SSE snapshot should stay usable): stage (the landed per-run derive, decorateRunRow already computes it for runs), waiting_on_human, parked_until, cost-so-far via the landed `MeterReader.RunMeter` (money READ from the S10.4 seam, the RunCard precedent — never computed), effort + disclosed-note from the latest `routing.decided` payload (redacted per the §38 D10 payload-derived rule). One derivation, served at the list grain, N+1-free. (b) client joins `/api/tasks` + `/api/runs` (covers stage/waiting/parked; CANNOT cover cost or effort — those live nowhere the client can list) — insufficient alone. (c) render a reduced face — BARRED: the queue row says EXACTLY the S1.3 set. Dispose (a) (+ scope: task list only, or board snapshot too).
- **OQ5 — the "every human decision" read gap (a REPORT, pre-raised).** No serving read exists; `decision.recorded` carries no run_id (subject in payload). Options: **(a) RECOMMENDED — an additive `decisions` block on `GET /api/tasks/{task}`**, derived server-side: the Human-decision-family rows run-scoped to the task's runs (ask-answer/cancel rows) + `decision.recorded` rows whose payload subject/card-id names the task, one of its runs, or an effect of one of its runs (effects-table join) — bounded by `runDetailRecordCap`, payload-derived strings redacted per §38 D10; misses documented in the field's comment (a decision keyed to none of those subjects — e.g. a person-level pause — is not a task decision). (b) a Layer-1 catalog query — touches `internal/history` (byte-frozen this packet) and still leaves payload-subject matching to SQL. (c) client-side assembly over `/api/events` — derivation logic in the client, against no-client-side-truths. Dispose (a) + the exact subject-matching breadth.
- **OQ6 — drag list semantics vs the hint verb's contract (part REPORT).** The verb is per-task rank (int64, ±1000, 0=no-hint); `hint_rank` is unreadable from any surface, and the board orders by `created_ts` — so recorded drag order cannot be rendered truthfully after reload. RECOMMENDED: **(i) REPORT-half:** additively serve the task's queued-run hint rank on the task list (the queue row is one JOIN away; read-only, no scheduler change), so the own-queued lane orders by (hint_rank≠0 first by rank, then the scheduler's default) — rendered order = recorded order; **(ii) write strategy:** on drop, POST **spaced ranks** (position×10, 1-based — never 0) for every own queued task whose effective rank changed, newest state re-read after; deterministic, idempotent, gap-free. Alternatives: single-POST midpoint insertion (fewer writes; degenerate when no integer gap → full re-rank anyway); no read + client-remembered order (a client-side side-truth — barred). Dispose (i)+(ii).
- **OQ7 — the board-column vocabulary.** `kanban_status` strings are "provisional … until the S15/9.1 board semantics land (B6)" (skeleton.go:775). RECOMMENDED: **ratify-as-data** — the board's columns render the landed vocabulary {intake, executing, verifying, done, attention, cancelled} with display labels, plus a forward-tolerant bucket for any unknown stored value (S14.2 rule-3 spirit — a new producer string must not vanish a card); producers byte-untouched. Alternative: renaming producer strings — touches `internal/stage`/`internal/intake` (out of scope; would be its own REPORT). Dispose.
- **OQ8 — the fleet seat/GPU seams.** `FleetSeat`/`FleetGPU` are declared-empty at v0; the projection comment names "the B6 fleet surface" as where the `internal/local` seat read + B4-6 VRAM land — but the ratified queue-row fleet acceptance names neither. RECOMMENDED: render the **named honest absence** at B6-5 (the seams stay declared; the surface says local-seat/GPU readings are not wired at v0) and carry the seat/GPU fill as a named item for the B6 gate list / bring-up — wiring `internal/local` reads is Go work beyond this row's acceptance. Alternative: wire them now (scope growth; dispose explicitly if wanted). Dispose.

## 9. SCOPE VERDICT

**NOT one opus sitting.** Five surfaces, the OQ1 wiring decision landing in live code, dnd wiring with its structural negatives, the receipt rendering, four filters, plus the dispositioned Go-side additive reads (OQ4/OQ5/OQ6) with their scan/test lockstep — on top of a view-test discipline (driven FakeSource + fetch doubles) that B6-4 only exercised at shell scale. **The queue row's pre-authorized A/B stop applies — RECOMMENDED:**

- **A — mission control + board** (R1–R9 + their slice of F; OQ1/OQ2/OQ4/OQ6/OQ7 land here): the wiring decision implemented and tested, the face fields, dnd + hint, the history panel.
- **B — task detail + fleet + filters** (R10–R19 + the rest of F; OQ3/OQ5/OQ8 land here): the receipt labels, decisions block, fleet honesty, the four filters.

Order **A → B** (B reuses A's view/live-wiring patterns and the OQ1 outcome). If the executor reaches the seam green with budget, same-sitting continuation is sanctioned; a stop at A-landed is equally a success mode.
