# P3-B4-7 brief — S12.5 + S12.9 + S12.10: confidence/calibration + T15 battery + consolidated B4 measurements + D3 riders

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets; predecessors `P3/briefs/P3-B4-5.md`, `P3-B4-6.md`). The executor and the evaluation agent work from THIS file plus the cited sections (`Spec/drafts/` canonical: **S12.5, S12.9, S12.10 owning**; S12.3–S12.4 model set + duty rules; S12.7 tuple/calibration split; S12.8 power posture; S09.7 contradiction screen; S07.4/S07.9/S07.10 entailment + P-T06-5/-3; S08.10 revalidation triggers; S17 rows P-T15-1/-2; S18 registry rows; `Spec/benchmark-preregistration-v1.md` (BENCH-REG); gate records `Research/decisions/GATE-3-spec-readiness.md` Def.4/Def.8, `P3/gates/B3-report.md` §7 D3; deferral record `P3/measurements/2026-07-20-verification-seeds-deferral.md`) — never from chat history. §7 is the evaluation rubric. The spec wins over this brief; a conflict found while executing is reported, never papered over. **This is the FINAL B4 packet — the B4 gate presentation follows it.**

**BINDING carried readings (`P3/STATE.md` phase status — do not relitigate):**
- **Class-(a) has NO v0 consumer** (no second engine adapter; every S12.4 duty is class-(b)). Nothing here changes that; the sandboxed GPU-broker plane stays a hermetic seam (§27).
- **F7 ratified reading:** the abstain/`unclear` schema member binds **classification-shaped duties only**; free-text drafting (utility Help, distill) is exempt — S12.4's own rationale. The same boundary governs which duties get margin calibration (R2).
- **B4-6 F5 carry:** the VRAM footprint key widens to the full S12.7 **(model, quant, context, slots, engine-build)** tuple at calibration (R21).
- **B4-5 residual carry:** the tier-L order-preservation check (`internal/local/conformance_test.go:273` — currently `t.Logf`) HARDENS to an **assert** inside the S12.10 bump procedure (R14).

**The spine of this packet is a THREE-WAY split (honest at grounding, re-checked at evaluation):**
1. **BUILD (machinery, hermetic, $0):** the S12.5 margin→isotonic→threshold calibration machinery; the T15 battery harness versioned in-repo; the S12.10 swap gate (promotion rule + P-T15-1 hard block + 7.3 flag firing); the `memory.ContradictionScreen` and `verify.EntailmentChecker` implementations; the F5 key widening. All fake-tested tier F; walls hold.
2. **EXECUTE (measurements ON THIS MACHINE, AC power, G3 Def.8 result files in `P3/measurements/`):** the six S12.9 measurements + the three carried B2 rows (entailment TPR/TNR, judge calibration, length bias — the deferral record) + both D3 riders. Def.8 file discipline per the established template (PROBES.md): pre-registered expectation → exact observation → pass/fail verdict → dated file. The queue row (ratified contract) names **result FILES** as the record; durable platform-data recording is OQ1.
3. **SEAM (respected, never faked):** the S14 pinned eval runner + golden-set governance (B5) — the harness runs standalone now, consumable by the runner later; watchdog/watchlist/intent-filling margin consumers (B5); the entailment GATE stays idle (software launch domain; activation needs web-research v0.1 — S07.4); the scheduled re-eval cadence duty (v1 schedule surface); B6 surfaces.

**PAID-LEGS DISCIPLINE (absolute):** the two D3 riders (§G) are the packet's **ONLY paid legs** — PRE-RATIFIED at the B3 gate (`P3/gates/B3-report.md` §7 row 3: "Both ratification riders are pre-registered in the B4-7 queue row"). Spend is scoped to the pre-registered design: golden set = the 26-case `golden-software` v1 seed, both axes, on the ratified **opus-4-8** judge; E3 leg = the committed B1-4 harness at its established scale (E1 ×1, parallel ×6, round-trip ×1) on the ratified **sonnet-5** executor. Everything else in this packet is $0 local. Any further paid call (e.g. a live S12.5 paid-escalation smoke) is NOT sanctioned — flag, never assume (OQ2).

**HARD HOST GATE (unchanged, absolute):** user-level only. NEVER any driver/kernel/system-CUDA/system-package change; NEVER `nvidia-persistenced`/`-lgc`; no systemd unit installed or started; the LIVE front chain (serve→Caddy 8481→unit 8482) and production units untouched — tests hermetic. A genuine host-package need = a FLAGGED OPERATOR DECISION, never performed. **Sanctioned network legs (this packet):** (a) the **Gemma-4-12B-QAT / Granite-Guardian / Arctic re-pulls** (HF per-IP rate limit at B4-5; disk check first; **partial files exist in the cache** — resume or restart, then sha256 + manifest re-hash on arrival); (b) **Granite Guardian 4.1** pull for the S12.9 #5 "3.3 vs 4.1" comparison (card-license verified per S12.3 — "evaluate 4.1 and verify its card license at adoption"); (c) FP16/BF16 KL baselines **only per OQ3's disposition**. **Bespoke-MiniCheck stays an OPERATOR act** (HF 401 license-gate + CC-BY-NC acceptance) — the entailment comparison states honestly what ran without it. No other network legs.

---

## 1. Numbered requirements

### A — S12.5 confidence machinery: margin → isotonic → threshold

- **R1.** **The supported cheap signal is the label-token logprob margin (top1−top2) under greedy decoding, post-hoc calibrated per duty** (S12.5). Verbalized confidence from 4–8B models is **never** a routing signal — assert structurally (no code path reads a self-reported confidence field). The margin computes from the B4-5 client's top-2 logprobs (`client.go` already requests `TopLogprobs=2` — its comment names B4-7 as the consumer; `DutyResult.Logprobs []TokenLogprob`). New machinery EXTENDS `internal/local` (file names executor §2 discretion; suggested `margin.go`/`calibrate.go`/`swap.go` + the battery under a package subdir) within the §26 import wall.
- **R2.** **The four-step calibration recipe, verbatim from S12.5:** (1) a labeled set (~30–50 items bootstrap per duty, grown from production under golden-set governance [XREF:S14 — B5 seam]); (2) margins via `/v1` logprobs; (3) an **isotonic map margin→P(error)** fit on a calibration split; (4) the **escalation threshold picked on a validation split — minimize local-only cost subject to the duty's acceptance bar**. Recipe shape adopted; **every number re-derived locally** — no imported constants. Implementation is stdlib-only (isotonic regression = pool-adjacent-violators; no Go module). Scope: **classification-shaped duties** (the F7 boundary): margins presuppose a label token — `intake-triage`, `watchdog-disambiguator`, `watchlist-triage`, `intent-filling`, `entailment` (binary + P(yes) from logprobs, S12.4 row), `contradiction-screen` (confirm stage), `sql-open` flagged-by-construction. Generation-shaped duties (`utility`, `distill-summarize`) have no label margin — their quality control is the S12.4 rows themselves (human-gated downstream; entailment-screened sampling); record that boundary honestly, never invent a pseudo-margin.
- **R3.** **Calibrated values are platform data keyed by (duty, model hash, engine build)** (S12.5) — model hash = `SeatRecord.ModelHash()` (the manifest sha256, `manifest.go`), engine build = `local.LlamaCppPin` (b10085). This key is **exactly what the S12.10 swap gate keeps valid** (P-T15-1). All thresholds are `TBD-BRINGUP(per-duty confidence calibration)` until the EXECUTE half fills them (R25); the machinery ships with an honest uncalibrated posture (no threshold belief ⇒ the duty's ungated v0 behavior continues — the `calibrated=false` VRAM-ledger precedent). **Durable storage shape = OQ1** (coordinator dispositions before the executor picks).
- **R4.** **Live margin consumers wired ONLY where a consumer exists** (CONVENTIONS §19 — absent duties degrade, never faked): the S12.4 low-margin behaviors for duties with live callers — `intake-triage` "fast; **workhorse re-check on low margin**" (the classifier seam, `internal/stage/local.go`) and the spot-check riding the same alias. When a calibrated threshold exists for the (duty, model hash, engine build) key, a below-threshold fast-tier call re-checks on the workhorse ($0, local→local); uncalibrated ⇒ no re-check gate (posture recorded). The B5 consumers (`watchdog-disambiguator` → verdict IS `unclear` on low margin; `watchlist-triage` re-check; `intent-filling` "which did you mean?" card) are **seams** — their margin plumbing may be built but no consumer is faked. `watchdog-disambiguator` **never escalates beyond `unclear` and never gains a cloud fallback** (S2.7/S12.5) — recorded as data/doc posture with its B5 consumer.
- **R5.** **Escalation-target posture recorded, not over-built** (S12.5): registry duties escalate to the requester's paid ceremony/verification seat *when a window is open* — billed to the requester, itemized (1.10, 3.4); with every paid window empty, everything keeps running at local quality and escalations queue as parked work ("3.9's 'whatever remains feasible' boundary, made concrete"). The entailment row's escalation ("below threshold ⇒ flagged into the paid verification pass — never a silent pass") is DATA on the verdict, consumed by the existing V2 machinery — not a new paid call path. **Whether any further paid-escalation wiring lands here is OQ2** — the queue-row headline names calibration + swap gate + measurements, not escalation wiring.

### B — T15 battery harness (S12.9), versioned in-repo

- **R6.** **The T15 battery is S12's acceptance instrument: built once at bring-up, versioned in the platform repo, run against the *exact deployed quant and engine build*** (S12.9). Contents: **~30–50 labeled cases per registry duty (synthetic seeds** + real traces as they accumulate) **plus the quant sanity check** (R8). Duty coverage, honestly: suites for every S12.4 duty with a servable seat and a defined case shape — the classification duties of R2 plus `utility`/`distill-summarize` with output-contract-shaped cases (schema/length/grounding checks, not margins); `embedder` is post-gate (G2 Def.8 — no suite, recorded); `sql-open` cases run against the pulled Arctic seat (its guardrail-stack consumer stays S14/B5 — the suite measures the alias, not the query surface). Case data is strict-JSON in-repo (the `Load*` seed precedent, §15/§17); the runner is executor-shaped (package + optional `sinet` tool verb — a tool, never a mode, §7) and must be runnable standalone NOW and consumable by the S14 pinned eval runner at B5 (seam note, no S14 machinery built).
- **R7.** **Battery runs are batch passes: AC-only** — consume ⚙ `local.batch.ac_only` (S12.8; declared `index.go` S12 block) by dotted key: the runner refuses a batch execution on battery when the ⚙ is true (host power-supply observation, the B4-6 `type=Mains online` read — never a periodic wake). This is the key's FIRST consumer (B4-6 deliberately left it to B4-7).
- **R8.** **The quant sanity check: `llama-perplexity --kl-divergence` on ~250K tokens of Sinet-domain text against the FP16 baseline** (S12.9). Facts the executor builds on: `llama-perplexity` is NOT in the B4-5 CUDA build (only `llama-server` + libs — verified at grounding) → build the additional target(s) (`llama-perplexity`, optionally `llama-bench` for R23) from the SAME pinned b10085 tree with the same user-level toolchain (micromamba gcc-14 + user CUDA 12.9; §26 drain record) — same lock entry, no new adoption. The ~250K-token Sinet-domain corpus assembles from the repo's own text (Spec/Research/Docs — platform-owned, no licensing issue), versioned with the harness. **Which quants get the KL check at B4-7 and whether their FP16/BF16 baseline pulls are sanctioned = OQ3.** KV cache stays fp16/q8_0 — the check validates weights quant only (S12.3 quant policy).
- **R9.** **Results recording:** every EXECUTE item lands a G3 Def.8 result file in `P3/measurements/` (pre-registered expectation, exact observation, pass/fail, dated — the PROBES.md template; the queue-row contract). S12.9's "results recorded in `platform.db`" + S12.5's "calibrated values are platform data" need a durable, queryable home for the swap gate's calibration check — **shape = OQ1**, never silently invented.

### C — The swap gate (S12.10) — machinery live, exercised by any bakeoff flip

- **R10.** **The promotion rule, verbatim** (S12.10): a challenger replaces an incumbent **only if it wins ≥2 duty suites with non-overlapping 95% Wilson intervals at equal-or-better tokens/s**. Implemented as code over battery results (Wilson interval math = stdlib); the rule is the ONLY path by which the bakeoff (R20) may flip the workhorse default (G3 Def.8: "Qwen3.5-9B ships as default alias target unless the bakeoff flips it").
- **R11.** **Swap ⇒ recalibrate is a HARD gate (P-T15-1, S12.10/S17):** every model, quant, or engine-build swap silently invalidates per-duty confidence thresholds and calibration maps. An alias retarget (a ⚙ `local.alias.<duty>` write) is REFUSED unless (a) fresh S12.5 calibration exists for every affected (duty, NEW model hash, engine build) key AND (b) the battery's affected duty suites re-ran on the new target — **before the new target goes live behind its alias**. "No alias retarget goes live uncalibrated" (S17 row). The ⚙ write itself goes through `settings.Registry.Set` (one tx: override row + `settings_events` + `settings.changed` — S01.10/S18.1); the S18 row's "changes only via the S12.10 swap gate" means the gate is the sole calling surface for that key — asserted by test.
- **R12.** **Every swap fires the 7.3 revalidation trigger** (S12.10 × S08.10a): worker template versions whose validation record or model pin references the swapped alias/model are **flagged and MUST be revalidated before further unsupervised use** — `worker.Store.FlagByModel(ctx, model, reason)` already exists (S08.10a, `lifecycle.go:525`). **Import wall:** `internal/local` NEVER imports `worker` (§26) → the flag firing is a func seam on the swap gate, satisfied by `FlagByModel` at the composition root (the `memory.Committer` precedent). At v0 the production worker set is empty — the firing is proven by test, honestly documented as flagging nothing yet.
- **R13.** **Serving-engine bumps ride the S03.3 deliberate-bump procedure** (S12.10): the local lane's conformance entries assert **`/v1` logprobs, `json_schema` enforcement, and the llama-swap YAML/endpoint contract BEHAVIORALLY on every bump — documentation is never the check (P-T15-2)**. B4-5/B4-6 built the tier-R/L asserts; this packet consolidates them as the named bump-procedure entry set (doc + test naming so a future bump session runs them as one battery). No engine bump happens in this packet (b10085 stays pinned; a bump would itself fire P-T14-1 mass revalidation — S08.10b, not exercised).
- **R14.** **The Logf→assert carry lands INSIDE that bump-procedure set:** the reason-first property-order check (free-text-precedes-constrained-region is S12.4 ratified text; b10085 preserves schema property order — confirmed live at B4-5) hardens from `t.Logf` to a hard assert in the tier-L smoke + the bump-procedure entry, so an engine bump that breaks ordered grammars FAILS loudly (the two-call fallback becomes the flagged remedy, never a silent regression).
- **R15.** **Aliases make every swap invisible to workers, templates, and platform callers** (S12.10/S12.4/S12.6) — no caller-side change anywhere in a swap; the re-eval cadence ⚙ `local.reeval.cadence_months` (6) is REFERENCED as lifecycle data (its scheduled duty consumer is the v1 schedule surface + S14 drift events — B5/v1; posture documented, no timer built).

### D — Seams going LIVE: contradiction screen (S09.7) + entailment checker (S07.4)

- **R16.** **`memory.ContradictionScreen` goes LIVE** (S09.7; the B3-1 named seam, `gate.go:35`): implement the advisory screen on the S12.4 `contradiction-screen` duty — v0 target per OQ4's disposition (the S12.4 row names "DeBERTa CPU pre-screen → workhorse confirm on hits"; the DeBERTa seat is NON-SERVABLE at the pin — the manifest carries the coordinator-flagged pick for this packet to ratify). The screen: `json_schema`-enforced, abstain-carrying (classification-shaped, F7), temp-0; **high-precision advisory: question cards only, never silent resolution** (S12.4/S09.7). Wall-clean wiring: `memory` never imports `local` → the adapter lives at the composition bridge (`internal/stage/local.go` precedent or shell), wired into `Gate.Screen`; **nil-safe when the local stack is unconfigured** (dev default); **the deterministic same-`topic_key` detection runs regardless and is NEVER suppressed by the screen** (§17 — the screen only refines question text on hits).
- **R17.** **`verify.EntailmentChecker` goes LIVE-wired** (S07.4; `entailment.go` — "wiring lands with the local tier (B4)"): the S12.4 `entailment` duty (Guardian seat; "binary + P(yes) from logprobs") implements the checker; adapter at the bridge/shell (`verify` already imports adapters/intake — it never imports `local`; keep it that way, seam-injected). **Wiring ≠ activation:** the `EntailmentGate` stays idle — `Active()` requires the web-research domain (v0.1) AND `Calibrated` (set only when the pre-registered TPR/TNR bar has been measured and recorded — this packet's R24 records it; how `Calibrated` persists rides OQ1). Below-threshold ⇒ flagged into the paid verification pass, never a silent pass (S12.4 row — verdict data, R5).

### E — B4-6 F5 carry: the footprint-key widening (S12.7)

- **R18.** **Widen the VRAM-ledger footprint key from the model string to the full S12.7 tuple: (model, quant, context, slots, engine build)** (`vram.go` `Ledger.footprints map[string]int64` → tuple-keyed; S12.7 step 5 names the tuple verbatim). The calibration run (R21) fills it (`calibrated=true` per measured seat); an unmeasured tuple stays honest-uncalibrated; admission continues to rest on the LIVE reading regardless (S12.7 admission math — unchanged). Re-run triggers (driver/engine/model-quant/display-mode change) stay documented data (`LedgerReRunTriggers`, B4-6).

### F — The EXECUTE half: the consolidated measurement battery (Def.8 files; AC; this machine)

Order matters: **re-pulls first** (R19), then $0 measurements (R20–R25), riders last or parallel (§G). Every file: pre-registered expectation → observation → pass/fail → dated.

- **R19.** **Model re-pull leg (sanctioned):** complete the three rate-limited seats — verified at grounding, PARTIAL files sit in `~/.sinet-b45/models/` (Gemma ~4.4 of ~7 GB; Granite 3.3 ~1.3 of ~5.7 GB; Arctic ~3.9 of ~5.4 GB — leftovers of the B4-5 killed curls): disk check first (117 GB free at grounding), resume-or-restart each, **sha256 on arrival → manifest re-hash** (`manifest.go` sha256 fields fill; `Pulled:true`). Plus **Granite Guardian 4.1** (R24's 3.3-vs-4.1 comparison; card license verified per S12.3; a manifest measurement-alternate row, platform data). If HF throttles again mid-packet: record the honest blocker per the B4-5 discipline (none of these is the v0 default path) and state per-measurement what ran. **Bespoke = operator act** — never attempted unauthenticated beyond confirming the 401.
- **R20.** **S12.9 #1 — workhorse bakeoff:** Qwen3.5-9B vs **Gemma-4-12B-QAT** on the battery's duty suites over Sinet-domain cases (synthetic seeds at bring-up — S12.9's "real Sinet-domain traces" accumulate later; state the basis in the file). **The 9B ships as default alias target unless the bakeoff flips it** (G3 Def.8). A flip applies ONLY through the swap gate (R10/R11) — and the Gemma alias flip is ADDITIONALLY BLOCKED pending operator ratification of the flagged card-vs-spec license conflict (`manifest.go` Gemma note: "must be operator-ratified before this alternate goes live (B4-7 bakeoff)"). Running the measurement is sanctioned (the queue row names the contender); applying a flip without the ratification is not.
- **R21.** **S12.9 #2 — VRAM-ledger calibration** (the S12.7 protocol on the real machine): per-model footprints **measured through a warm-up generation** (KV + CUDA graphs only fully materialize then) + guard band ⚙ `local.vram.guard_band_mb`, keyed by the R18 tuple; **both compositor-headroom figures (hybrid + MUX separately** — hybrid is the recommended VRAM-maximizing mode; if the MUX leg needs a display-mode switch/reboot it is an **operator-assisted leg — flag, record the hybrid figure now, never fake the other**); **unload/kill→free latencies** (through the B4-6 preempt path: TTL/eager unload AND SIGTERM→grace→SIGKILL, recovery verified). Sleep-gate discipline binds every read (§27 — no periodic polling; `runtime_status` first). Fills the widened ledger + the Def.8 file.
- **R22.** **S12.9 #3 — CPU-tier throughput floor:** 3–4B Q4 tokens/s on the host CPU (the ~20–35 t/s figure is a class estimate with no citable laptop source — this measurement replaces it). Tool: `llama-bench` or `llama-server -ngl 0` timings from the pinned tree (R8). The verdict **decides how much of the S12.8 battery-mode policy is real and whether a 2B CPU downshift is needed** (the parked re-entry) — state the decision in the file.
- **R23.** **S12.9 #4 — contradiction-screen P/R:** measured operating point of the screen AS SHIPPED per OQ4 (two-stage DeBERTa→workhorse if a pre-screen path ratifies; else the one-stage workhorse screen) on realistic lesson pairs (synthetic set versioned with the battery; label balance stated). Literature predicts **high precision / weak recall with temporal/numeric blind spots** (S12.9) — pre-register that expectation; record P/R + the operating threshold; this is `TBD-BRINGUP(contradiction-screen P/R)` (S09.7) closing for the shipped shape.
- **R24.** **S12.9 #5 — entailment thresholds + mandatory-coverage bar** (closes the B2 TPR/TNR deferral): **Guardian 3.3 vs 4.1 vs MiniCheck** (if the operator unblocks Bespoke — else honestly two-way) on **~200 Sinet-domain claim–citation pairs** (grown from the 10-pair `entailment-calibration` v1 seed; versioned in-repo; judged against fetched/excerpted source content, never citation text — S07.4). Sets: the entailment **thresholds**, the **mandatory-coverage (load-bearing) bar**, and the **Flan-T5-0.8B CPU-floor verdict** — which FIRST requires validating its serving path at the pin (`Servable:false` today; T5 seq2seq with a specialized fact-check shape — "B4-7's to validate", manifest note; if unservable at b10085, record the honest verdict and the sampled-checks fallback posture). **The TPR/TNR bar is pre-registered in the result file BEFORE entailment ever gates unsupervised** (deferral record; G3 Def.4). **The same run SETS ⚙ `verification.entailment_sample_rate`** (declared default 0, TBD-BRINGUP — `index.go:347`): the S18/S01.10 mechanics are an **operator-owned `Registry.Set` write with its audit row** (`settings_events` + `settings.changed`), NEVER an edit of the declared code default; the measurement file records the derived value and the write's execution (dev-posture: the write lands wherever the coordinator's OQ1/bring-up disposition puts durable platform state; if no durable operator store exists yet, the derived value + write procedure are recorded and flagged to the gate — honestly, never silently skipped).
- **R25.** **S12.9 #6 — per-duty confidence calibration:** execute the R2 fit for every measurable registry duty (classification-shaped, servable seat, labeled set from R6) at the deployed quant + engine build; record per-duty (margin distribution, isotonic map, chosen threshold, achieved error at threshold) keyed per R3. Duties without a live seat or with generation shape: recorded honestly as not-calibrated-with-reason. Closes `TBD-BRINGUP(per-duty confidence calibration)` for covered duties (spec-text bookkeeping = coordinator/gate S00.9, never a packet edit).

### G — D3 riders (PAID, operator-pre-ratified at the B3 gate — the packet's only paid legs)

- **R26.** **Rider 1 — P-T06-5 golden-set run on the ratified opus-4-8 judge (+ the B2-3 length-bias check).** The D3 advisor split (applied `e06f0a4`: judge=opus-4-8) is a judge-model change → the P-T06-5 gate fires: **golden-set re-run (TPR/TNR re-measured) before unsupervised judging resumes** (S07.9). Design (pre-registered): the `golden-software` v1 seed — **26 cases (20 planted defects + 6 clean controls)** — through the REAL verify judge seam (`Compliance` + `Sanity`, the S07.5 input slice, clean context) on the opus-4-8 seat; judge-as-classifier TPR/TNR computed vs labels; **judge-reported rates statistically corrected, never taken raw** (S07.11/deferral record); rates land in the rubric bundle's `GoldenSetRates` (`Measured:true` — an in-code seed **v2 bump**, gate-ratified like the v1 seeds) and forward on `verify.round` rows. **Same run measures the per-judge-model length bias** (P-T06-3): score correlation vs response length over the golden set → `RubricBundle.LengthBiasNote` measured (re-measured on every judge change). Def.8 file with both results. Spend scope: 26 × 2 axes ≈ 52 opus-4-8 calls on short planted artifacts; **pre-register a projection + stop line in the file BEFORE running** (the B3-3 discipline — projection $0.14/stop $0.50 precedent; suggested stop line here $5.00, coordinator may adjust at OQ disposition).
- **R27.** **Rider 2 — serialize-by-deny E3 leg on the ratified sonnet-5 executor.** **Exactly the committed B1-4 harness** (`internal/adapters/claudecli/spike_test.go`, `SINET_B1_4=1`, real `sinet engine-hook`, `SINET_B1_4_MODEL=<the DefaultDutyMap execution seat — sonnet-5>`) at the **established B1-4/B3-3 scale: E1 ×1, parallel E3 ×6, defer→resume round-trip ×1**. Pre-registered expectations verbatim from the two predecessor files (2026-07-20 + 2026-07-21): E1 clean defer-park; E3 6/6 faithful single-call parks at ~+1 turn under ⚙ `adapter.parallel_gate_fallback = serialize-by-deny`; E2-context = does first-defer-honored-on-parallel persist on sonnet-5 (the favorable drift is model/engine-dependent — a fallback-to-completion on sonnet-5 is a FINDING for the per-pin canary, not a failure of the rider). Stop line pre-registered (B3-3 measured $0.1304 on haiku; sonnet-5 ≈ 3× weight per the B3 D3 record — projection ≈ $0.40; suggested stop line $1.50). Def.8 file.
- **R28.** **BENCH-REG discipline (binds both riders):** the riders **RUN measurements; they never edit registered numbers** — any change to `Spec/benchmark-preregistration-v1.md` happens ONLY via its own §17 (dated signed amendment; not this packet's act). The golden-set run is the S07.9 P-T06-5 gate, NOT a benchmark-gate evaluation: BENCH-REG §10.1(d)'s regression floors register per rubric/eval-set version **at 8.3 knowledge-gate entry** (a gate/operator act), and §7's win-rate math is not invoked anywhere here.

### H — Tests, walls, no-new-things

- **R29.** **Hermetic tier-F coverage** for every BUILD piece: margin math on fixture logprobs; isotonic fit + threshold pick on synthetic labeled sets (deterministic); the swap gate's P-T15-1 refusal (retarget without fresh calibration REFUSED; with calibration + suites, admitted), promotion-rule math (Wilson intervals — non-overlap and overlap cases), 7.3 flag-seam firing (fake seam records the call; plus a composition-root test proving `FlagByModel` satisfies it); battery runner on fixture cases against the `local.FakeServer`; the ContradictionScreen impl (hits create edges + question cards; deterministic detection never suppressed; nil-screen unchanged); the EntailmentChecker adapter (verdict mapping incl. `unverifiable`; gate stays inactive for software domain). Zero network, zero paid, `t.TempDir()`.
- **R30.** **Conformance tiers §10:** F always; R real llama-swap+llama.cpp when installed ($0 — this host qualifies; discovery via `SINET_LOCAL_LLAMA_SWAP`/`SINET_LOCAL_LLAMA_SERVER`/`SINET_LOCAL_MODEL_CACHE`; binaries live durable at `~/.sinet-b45/bin/llama-swap` [v241] + `~/.sinet-b45/build/llama.cpp/build-cuda/bin/llama-server` [b10085 sm_120]); L under `SINET_LIVE_SMOKE=1` ($0) — incl. the hardened order assert (R14). Absence-skips print `SANCTIONED SKIP (CONVENTIONS §10)`; pins test-coupled to the lock entries; loud installed-vs-pin delta, never retarget. The EXECUTE half is **measurements, not Go tests** (§3): probe runs recording to `P3/measurements/`.
- **R31.** **Import walls hold (AST-pinned where new):** `internal/local` stays storage/eventlog/gates/settings + stdlib ONLY (§26 — never worker/verify/memory/scheduler/sandbox/adapters/metering...); the 7.3 flag firing, the ContradictionScreen impl, and the EntailmentChecker impl are **seams satisfied at the composition root/bridge** (`internal/shell`, `internal/stage/local.go` — stage already imports intake/worker/verify); `memory` and `verify` gain NO `local` import; `worker` untouched. Any cross-edge a seam seems to demand = STOP-and-flag.
- **R32.** **No-new-things ledger:** **ZERO new `components.lock` entries expected** (stays 17): `llama-perplexity`/`llama-bench` are additional build targets of the EXISTING pinned llama.cpp entry; Guardian 4.1/re-pulled weights are platform data (manifest), never lock entries; isotonic/Wilson math is stdlib. **The ONE path to a new entry is OQ4(b)** (a pre-screen runtime) — flagged, never assumed. **NO new ⚙ key** (118-tally test stays green — proof); thresholds/calibration values are platform DATA (S12.5), not ⚙. **Migration posture rides OQ1** (0001–0009 stand unless OQ1(a) dispositions 0010 — a coordinator call, never a silent add). `P3/CONVENTIONS.md` gains **§28** (append-only, section-cited): the calibration keying + storage disposition, the swap-gate discipline (P-T15-1 + promotion rule + 7.3 seam), the battery instrument + AC gate, the screen/checker wiring shape, the measurement record set, ⚙ consumed/set, walls.

## OPEN QUESTIONS (genuine spec/scope ambiguities — recorded, NOT resolved here; coordinator dispositions before the executor launches — the B4-6 precedent)

- **OQ1 — durable home for calibration values + battery results.** S12.5: "calibrated values are platform data keyed by (duty, model hash, engine build)". S12.9: battery "results recorded in `platform.db`" — but in the same sentence as the S14-owned pinned eval runner (B5). The queue row (ratified contract) names **Def.8 result FILES**. The swap gate (R11) and `EntailmentGate.Calibrated` (R17) need a QUERYABLE calibration record. Options, each with spec support: **(a)** migration `0010` (calibration + battery-result tables; satisfies "recorded in platform.db" now; breaks the B4 no-migration streak — which is precedent, not law); **(b)** event-sourced platform-scope events with latest-wins reads (the §13 `ledger_update` pattern; event rows ARE in platform.db; no migration); **(c)** a committed/state-dir platform-data file (the manifest/structural-config precedent) + platform.db recording deferred to B5's runner. Grounding does not pick.
- **OQ2 — S12.5 paid-escalation wiring: in or out of this packet?** S12.5's escalation-targets paragraph (escalate to the requester's paid ceremony/verification seat when a window is open; billed + itemized; queue-as-parked when windows are empty) is owning-section text, but the ratified queue-row headline names only calibration machinery + swap gate + measurements + riders, and most escalation consumers don't exist until B5/B6. R4/R5 ground the narrow reading (local→local re-check live; escalation posture as data). Does the coordinator also want any paid-escalation path wired (and if so, is a paid smoke sanctioned — the STATE routing line anticipated "battery escalation smokes" as possible B4-7 paid legs, but the queue row does not name them)?
- **OQ3 — KL-check scope + FP16 baseline sanction.** The KL check binds "deployed quants" (S12.3) and S12.9 names it once in the battery contents. FP16/BF16 baselines are extra multi-GB pulls (9B ≈ 18 GB, 4B ≈ 8 GB; Guardian/Arctic more; disk 117 GB free, cache currently 19.7 GB + ~12 GB of re-pull completions). Options: **(a)** default-path quants only (workhorse 9B Q5_K_M + fast 4B Q4_K_M) — two baselines, ~26 GB, clearly affordable; **(b)** every servable deployed quant (adds Guardian, Arctic, bakeoff winner); **(c)** harness support now, KL execution deferred to a disk-checked follow-up. The baseline pulls are network legs beyond the three named re-pulls — sanction is the coordinator's.
- **OQ4 — the DeBERTa pick revisit (queue row: "the pick revisit lands here").** The manifest carries the coordinator-flagged concrete pick (`cross-encoder/nli-deberta-v3-base`, Apache-2.0, ~100 MB) but it is **NON-SERVABLE at the pin** (encoder classification head — no GGUF/llama-server path; "its consumer is B4-7"). Options: **(a)** v0 ships the contradiction screen **one-stage** (workhorse confirm via the `contradiction-screen` alias; the S12.4 row's pre-screen stage re-enters when a serving path exists; S09.7's deterministic same-`topic_key` detection runs regardless; R23 measures what ships — honest degradation per §19); **(b)** adopt a CPU inference runtime for the encoder (ONNX Runtime class — a NEW lock entry + operator #10; heavy); **(c)** substitute a servable GGUF NLI model of the same class (executor live-verifies existence — risky; "DeBERTa-class" is a class, not a card, S12.3). Grounding leans (a) as the only zero-adoption path but does not pick.
- **OQ5 — dead-man canary V2-leg upgrade: here or B5?** The deferral record flags: "the dead-man canary upgrades from the zero-model RESEARCH-NOT-RUN plant to the full V2 leg when the local tier lands (B4)" — but canary scheduling/silence-alert wiring is S14 (B5), and the B4-7 queue row does not name the upgrade. Build the upgraded canary task shape now (still unscheduled) or carry the flag to B5?

## 2. Seams to respect (named; stub/absence shape only)

1. **S14 (B5):** the pinned eval runner + golden-set governance (the battery's future driver); watchdog/watchlist/intent-filling duty consumers (margin plumbing may exist, consumers absent); the logprob **drift canary** (consumes this packet's margin/logprob surface at B5); canary/drill scheduling + silence alerts; the conformance registry that will own the R13 bump entries; `benchmark.sampling_rate` and all BENCH-REG machinery. Event names stay provisional pending the S14 contract.
2. **B6 (S15):** calibration/battery/swap-gate status surfaces — DATA + composition only (the `SurfaceCard` precedent); the settings-tab directive covers exposing calibration data later.
3. **Class-(a) local execution lane:** still no consumer (BINDING) — nothing here dispatches a run onto lane `local`; the GPU-broker sandboxed plane stays hermetic (§27).
4. **`embedder`:** post-gate (G2 Def.8) — no suite, no calibration, refusal posture unchanged.
5. **v1 activations:** L1 writers/stations (S09.4), the schedule surface consuming `local.reeval.cadence_months` timing, worker-overlay scope.
6. **The operator/gate:** Bespoke credential act; Gemma license ratification (blocks only the alias flip); MUX display-mode leg if a mode switch is needed; migration 0010 per OQ1; seed v2 (rubric bundle) ratification; `local.alias` flip ratification if the bakeoff flips; TBD-BRINGUP spec-text bookkeeping (S00.9, coordinator).
7. **S10:** pressure/budget/admission POLICY — untouched; the free tier stays the D5 floor ($0 rows, B4-5 wire contract).

## 3. ⚙ settings

- **Consumed** (dotted key, never constants; all declared since B0-2): **`local.alias`** (TypeMap — the swap gate is now its ONLY write surface, R11; readers unchanged), **`local.batch.ac_only`** (R7 — first consumer), **`local.vram.guard_band_mb`** (R21 calibration protocol), **`local.unload.term_grace_s`** (R21 kill→free latency leg, via the B4-6 preempt path), **`adapter.parallel_gate_fallback`** (R27 — the harness consumes the registry default), verification SLA/pipeline keys only as already consumed by `internal/verify`.
- **SET at bring-up (value writes with audit — never code-default edits):** **`verification.entailment_sample_rate`** (R24 — declared default 0 TBD-BRINGUP; operator-owned `Registry.Set`, one tx: override row + `settings_events` + `settings.changed`; the measurement file records the derived value + the write). **`local.alias.workhorse`** ONLY IF the bakeoff flips AND the swap gate + Gemma ratification admit it (R10/R11/R20).
- **Referenced, not consumed:** `local.reeval.cadence_months` (lifecycle data; scheduled consumer v1/B5 — R15), `local.ttl.*`/`local.battery.gpu_admission`/`local.broker.sandbox_logprobs`/`local.gamemode_hook` (B4-5/B4-6 consumers, untouched).
- **No new ⚙ key** — the 118-tally test stays green (proof). Calibration thresholds/maps are platform DATA keyed (duty, model hash, engine build) (S12.5), never ⚙; battery case counts, stop lines, and corpus paths are structural/pre-registered data (the `sseBatchSize` precedent, settings-tab directive).

## 4. Files expected to change

- `internal/local/`: NEW margin/calibration/swap-gate/battery files (executor names, §2 discretion); `vram.go` (R18 tuple key); `manifest.go` (re-hash fills, Guardian 4.1 row, OQ4 disposition note); `conformance_test.go` (R14 assert); `fake.go` extensions (margin fixtures); colocated tests + `importwall_test.go` (unchanged wall).
- `internal/stage/local.go` (+ shell): the intake-triage low-margin re-check consumer (R4); the ContradictionScreen + EntailmentChecker bridge adapters (R16/R17); `internal/shell/local_seams.go`: swap-gate composition (calibration store + `FlagByModel` seam + gate surface).
- `internal/verify/seeds.go`: rubric-bundle **v2** (GoldenSetRates measured + LengthBiasNote) after R26 — content change flagged for gate ratification; `entailment.go` untouched structurally (gate stays idle).
- `internal/memory/`: NO structural change (`Gate.Screen` is already the seam) — wiring is composition-root.
- `internal/adapters/claudecli/spike_test.go`: expected UNCHANGED (`SINET_B1_4_MODEL` exists since B3-3).
- `P3/measurements/`: ~9 dated Def.8 result files (re-pull record may fold into the files that consume the models; bakeoff; VRAM calibration; CPU floor; contradiction P/R; entailment thresholds + ⚙ set; per-duty calibration; rider 1; rider 2).
- `P3/CONVENTIONS.md`: append **§28** (R32). `internal/storage/migrations/`: ONLY per OQ1(a).
- Expected UNCHANGED: `internal/{worker,metering,gates,scheduler,sandbox,broker,intake,review,project,accept,backup,preview,portpool}` code, `internal/settings/index.go` (no key add), `components.lock` (**17**), `go.mod`.

## 5. Adopted components touched

- **NONE new expected.** llama-swap v241 + llama.cpp b10085 (+ Ollama standby) are CONSUMED at their pins (§10 test-coupling); `llama-perplexity`/`llama-bench` build from the SAME pinned llama.cpp tree (same lock entry — a build-target addition, not an adoption); models/weights are platform data (S16.1 — never lock entries; weights never committed). Engine seats for the riders (opus-4-8 judge, sonnet-5 executor) ride the existing claude engine entry + DutyMap data.
- **The one flagged path to a new entry:** OQ4(b) — a CPU encoder runtime for the contradiction pre-screen would be a full S16.4 onboarding + operator #10. Never assumed; STOP-and-flag.

## 6. Dev-mode vs LIVE (host gate; operator hands-on FLAGGED)

- **Executor performs (sanctioned):** the R19 re-pulls (+ Guardian 4.1; resume partials; sha256 + manifest re-hash); the extra llama.cpp build targets (user-level, pinned tree, micromamba gcc-14 + user CUDA 12.9); ALL $0 measurements on the durable user-level stack (AC verified — on AC at grounding; `runtime_status` active); the ⚙ set per R24 mechanics; the TWO paid riders within their pre-registered scope + stop lines. Weights/scratch stay in `~/.sinet-b45` (outside repo; cache-location ratification stays a gate item).
- **Executor NEVER:** installs/starts units; touches driver/kernel/system-CUDA/8481/8482/tailscale/live Caddy; runs `nvidia-persistenced`/`-lgc`; pulls Bespoke past confirming the 401 (operator credential + CC-BY-NC acceptance); switches display mode if that needs reboot/BIOS (operator-assisted MUX leg); edits `Spec/` (incl. BENCH-REG — §17 territory) or `P3/STATE.md`; makes any paid call beyond the two riders; adds a migration/⚙ key/lock entry without the flagged path.
- **Operator hands-on (flag each in the packet report, due B4 gate):** (a) Bespoke-MiniCheck HF credential + license acceptance (else the entailment comparison stands two-way, honestly); (b) Gemma license-conflict ratification (blocks only an alias flip); (c) the MUX-mode headroom leg if a mode switch is required; (d) rubric-bundle v2 + any manifest-row ratifications; (e) OQ1–OQ5 dispositions (coordinator/operator per the pipeline); (f) unchanged standing items (hardening session, `/tmp/llamaswap-test` rm, cache-location ratification).

## 7. Acceptance checklist (the evaluation rubric)

Green battery required: `gofmt` clean, `go vet` clean, `go build ./...`, `go test ./...` (`-count=1`; `-race` on touched packages), `go run ./tools/lockgate`. Sanctioned skips only per §10/§12, printed as such.

1. [ ] Margin signal = label-token logprob top1−top2 under greedy, computed from the B4-5 top-2 logprobs; verbalized confidence structurally absent as a routing input (R1).
2. [ ] The four-step S12.5 recipe implemented (labeled set → margins → isotonic margin→P(error) → threshold minimizing local-only cost s.t. the duty's bar); stdlib-only; classification-shaped duties only (F7), generation duties honestly out (R2).
3. [ ] Calibration keyed (duty, model hash = manifest sha256, engine build = LlamaCppPin); uncalibrated posture honest; storage per OQ1's disposition — never silently invented (R3).
4. [ ] Low-margin consumers wired ONLY where live (intake-triage fast→workhorse re-check + spot-check); B5 consumers stay seams; watchdog never-beyond-`unclear`/never-cloud recorded (R4/R5; OQ2 honored).
5. [ ] T15 battery versioned in-repo: ~30–50 labeled cases per covered registry duty, synthetic seeds, strict-JSON; duty coverage honesty (embedder post-gate; sql-open measured, consumer B5); runnable standalone; S14-runner seam noted (R6).
6. [ ] Battery batch runs consume ⚙ `local.batch.ac_only` (first consumer) via a sleep-safe host power observation (R7).
7. [ ] KL machinery: `llama-perplexity` built from the pinned b10085 tree user-level; ~250K-token in-repo Sinet-domain corpus; scope + baselines per OQ3's disposition (R8).
8. [ ] Swap gate: promotion rule = ≥2 duty suites won with non-overlapping 95% Wilson intervals at equal-or-better tokens/s (R10).
9. [ ] **P-T15-1 hard block proven by test:** alias retarget without fresh calibration for the new key + affected suites REFUSED; ⚙ `local.alias` writes flow ONLY through the gate (`Registry.Set` + audit) (R11).
10. [ ] Every swap fires 7.3 flags through a wall-clean seam satisfied by `worker.Store.FlagByModel` at the root; proven by test; empty production set stated honestly (R12).
11. [ ] The S12.10 bump-procedure conformance set named + consolidated (P-T15-2: `/v1` logprobs, `json_schema`, llama-swap contract — behavioral); **the order-preservation check is an ASSERT, not Logf** (R13/R14).
12. [ ] VRAM footprint key = the full (model, quant, context, slots, engine-build) tuple; calibration fills it; live-reading-authoritative admission unchanged (R18).
13. [ ] `memory.ContradictionScreen` LIVE per OQ4's disposition: duty-alias-backed, json_schema + abstain, advisory-only, question cards; deterministic same-`topic_key` detection never suppressed; nil-safe unconfigured; wall-clean wiring (R16).
14. [ ] `verify.EntailmentChecker` LIVE-wired (Guardian seat, binary + P(yes)); the GATE stays idle (domain + Calibrated discipline intact); wall-clean (R17).
15. [ ] Re-pull leg: partials resumed/restarted, sha256 → manifest re-hash; Guardian 4.1 card-verified; genuine blockers recorded honestly; Bespoke never pulled unauthenticated (R19).
16. [ ] Def.8 files for ALL measurements: pre-registered expectation, observation, pass/fail, dated, in `P3/measurements/`; AC power held (R9, R20–R25).
17. [ ] Bakeoff run 9B vs Gemma-4-12B-QAT; default flips ONLY via the swap gate AND behind the Gemma license ratification flag (R20).
18. [ ] VRAM calibration: warm-up-generation footprints per tuple, hybrid headroom measured (MUX = flagged operator leg if a switch is needed), unload/kill→free latencies; sleep-gate discipline held (R21).
19. [ ] CPU floor measured with the decision stated (battery-policy realism; 2B downshift re-entry) (R22).
20. [ ] Contradiction-screen P/R measured for the SHIPPED shape with the pre-registered high-precision/weak-recall expectation (R23).
21. [ ] Entailment thresholds + coverage bar + Flan-T5 verdict (serving path validated or honestly failed) on ~200 in-repo pairs; Guardian 3.3 vs 4.1 (vs MiniCheck if unblocked — else stated); **TPR/TNR bar pre-registered before any unsupervised gating; ⚙ `verification.entailment_sample_rate` SET via the audited registry write (never a code edit)** — the B2 deferral CLOSED (R24).
22. [ ] Per-duty confidence calibration executed + recorded per key for covered duties; uncovered duties stated with reasons (R25).
23. [ ] Rider 1: 26-case golden set × both axes on opus-4-8 through the real judge seam; TPR/TNR statistically-corrected into rubric bundle v2 (`Measured:true`) + length-bias measured; projection + stop line pre-registered; Def.8 file (R26).
24. [ ] Rider 2: the committed B1-4 harness at established scale on sonnet-5 (`SINET_B1_4_MODEL`); expectations from the predecessor files; projection + stop line pre-registered; Def.8 file (R27).
25. [ ] BENCH-REG untouched (no registered-number edit; no §7 math; floors register at 8.3 entry — not here) (R28).
26. [ ] Hermetic tier-F coverage per R29; tiers F/R/L with `SANCTIONED SKIP` printing; pins test-coupled, loud delta (R30).
27. [ ] Import walls hold AST-pinned: `local` unchanged wall; `memory`/`verify`/`worker` gain no `local` import; seams at root/bridge (R31).
28. [ ] ZERO new lock entries (17; lockgate green); NO new ⚙ key (118 tally green); migrations per OQ1 disposition only; CONVENTIONS §28 appended, section-cited, no earlier section rewritten (R32).
29. [ ] Paid spend = the two riders ONLY, within pre-registered scope + stop lines; zero paid calls anywhere else; host gate clean (production ports/units/driver untouched); every §6 operator item + OQ1–OQ5 surfaced in the packet report, never silently decided.

## 8. Boundary readings this brief rests on (each section-cited; evaluation re-checks)

1. **The queue row's EXECUTE half is the consolidated S19.6 B4 battery.** S19.6: "At B4 (local tier): the workhorse bakeoff, the VRAM-ledger calibration, the CPU-tier throughput floor, the per-duty confidence calibration, and the contradiction-screen precision/recall — the consolidated G3 Def.8 battery [S12, S09]" + the STATE measurement-rows line adds entailment thresholds (S12.9 #5), the carried B2 rows, and both D3 riders — all at B4-7.
2. **Def.8 result files are the ratified record; platform.db recording is OQ1.** The queue row names "G3 Def.8 result files in `P3/measurements/`"; S12.9's "results recorded in platform.db" sits in the battery-instrument sentence whose runner is S14's (B5). The swap gate still needs a queryable calibration record — hence OQ1, not silence.
3. **Margin calibration binds classification-shaped duties.** S12.5's signal is the label-token margin; S12.4's cross-cutting rules scope abstain to classification-shaped duties (the F7 RATIFIED reading); generation-shaped duties are governed by their own S12.4 rows. Measurement #6's "every registry duty" is read through that boundary — calibrate where a label margin exists, record the rest honestly.
4. **The riders are P-T06-5/7.3-analog gates, not benchmark work.** S07.9 P-T06-5: any judge-model change gates on a golden-set re-run before unsupervised judging resumes; the B3 gate pre-ratified exactly these two runs on the ratified seats (B3-report §7 row 3). BENCH-REG governs the self-benchmark (platform-vs-direct pairs) — untouched here; its registered numbers change only via §17.
5. **The swap gate is the sole write surface for `local.alias`.** S18 row: "changes only via the S12.10 swap gate"; S12.10: swap ⇒ recalibrate + revalidate BEFORE the target goes live; S01.10/S18.1: every settings write = override row + audit in one tx. A bakeoff flip is therefore gate-mediated data, and the Gemma flip is additionally license-ratification-blocked (manifest R8 flag).
6. **7.3 flags fire through a seam because the wall is absolute.** S12.10 names the trigger; S08.10a owns the verb (`FlagByModel`); CONVENTIONS §26 bars `local`→`worker` — the `memory.Committer` composition-root precedent is the sanctioned shape.
7. **The contradiction screen's LIVE shape depends on OQ4 because the spec names a class, not a card.** S12.3 "DeBERTa-class NLI cross-encoder"; the manifest's concrete pick is honest `Servable:false` at the pin ("its consumer is B4-7... to ratify"); S09.7's deterministic detection is the never-suppressed floor either way; question cards only.
8. **Entailment wiring ≠ activation.** S07.4: v0 launch domain is software — machinery ships idle, activates with web-research at v0.1 AND only calibrated ("pre-registered TPR/TNR bar before entailment gates unsupervised", G3 Def.4/deferral record). This packet wires the checker, measures, sets the ⚙ — and leaves the gate honestly idle.
9. **The measurement corpus grows from the B2-3 seeds under the same governance.** The 10-pair calibration seed and 26-case golden set are versioned, operator-editable seed objects (§15/§17); S12.9 #5's ~200 pairs and the per-duty case sets extend them in-repo as versioned data; content ratification rides the gate (the B2 seed precedent).
10. **Zero new adoptions is the expected posture, with one flagged exception path.** Build targets of a pinned tree are not adoptions (S16.1 — same component, same pin); models are platform data (§26 reading 6); only OQ4(b) could demand a lock entry, and only through S16.4 + #10.

## 9. CONVENTIONS constraints binding this packet (`P3/CONVENTIONS.md`)

- **§2/§3** stdlib-first (isotonic/Wilson in-repo; zero new Go modules); measurements are probe files, NOT Go tests; fixtures under `t.TempDir()`; never commit red.
- **§5** commit subject `P3-B4-7: <summary> (<sections>)`; explicit-path staging; never push; never touch `Docs/`/`Spec/`/`Research/`/`P3/STATE.md`; never read/stage `*-api-key.txt`.
- **§6** settings discipline: dotted-key reads; writes ONLY via the registry tx (override + audit + `settings.changed`); map-family `local.alias` one TypeMap key.
- **§10** conformance tiers F/R/L + `SANCTIONED SKIP`; pins exported + lock-coupled; assert behavior, never docs (P-T15-2); loud pin-delta.
- **§11** metering: $0 local rows ride the B4-5 wire contract; UNPRICED honesty for paid lanes untouched; riders' spend reads from the harness' reported `total_cost_usd` (the B1-4/B3-3 shape).
- **§15/§16** verify walls (never `internal/gates`; judge slice fixed); the golden-set run drives the REAL judge seam, clean context, no transcript.
- **§17** memory walls (engine-facing never import `memory`; the screen never suppresses deterministic hits; question cards ride `knowledge_conflicts`).
- **§18/§19** worker store: flags via the existing verbs; FLAGGED ⇒ supervised-only until revalidate+Repoint; routing degradation honesty.
- **§26** the `internal/local` package map + import wall (the binding predecessor); duty shaping (temp-0, schema in prompt AND engine, abstain per F7); manifest = platform data; structural-config posture for paths/env.
- **§27** GPU broker/VRAM: sleep-gate every read, no periodic polling, kill-not-freeze, live-reading-authoritative admission — the calibration run consumes this machinery, never re-invents it.
- Packet-specific (STATE queue row): S12.5/S12.9/S12.10 own; both B4-6 carries land; the hard host gate + AC/Def.8 measurement discipline; the riders are the only paid legs.

## 10. Executor live-verify at build time (never from memory; all checked at grounding 2026-07-22)

- **Stack (durable, user-level):** `~/.sinet-b45/bin/llama-swap` (v241, sha-matched at B4-6 landing) + `~/.sinet-b45/build/llama.cpp/build-cuda/bin/llama-server` (b10085 sm_120, shared-lib build — small binary + `libggml-cuda.so` siblings, expected); discovery env `SINET_LOCAL_LLAMA_SWAP`/`SINET_LOCAL_LLAMA_SERVER`/`SINET_LOCAL_MODEL_CACHE`; **no `llama-perplexity`/`llama-bench` built yet** (R8).
- **Model cache (`~/.sinet-b45/models/`):** COMPLETE + hashed: Qwen3.5-9B Q5_K_M (6.58 GB), Qwen3.5-4B Q4_K_M (2.74 GB), MiniCheck-Flan-T5 Q6_K (644 MB). **PARTIAL (re-pull targets):** gemma-4-12b-it-qat-q4_0.gguf 4.71 GB of ~7, granite-guardian-3.3-8b Q5_K_M 1.40 GB of ~5.7, Arctic-Text2SQL-R1-7B Q5_K_M 4.16 GB of ~5.4 — verify sizes/hashes yourself before serving anything; a partial GGUF must never be configured.
- **Host:** RTX 5070 Ti Laptop 12227 MiB (11733 free at grounding; 2 graphics `process_info` clients — per-process attribution must see them); GPU `runtime_status` **active**; **on AC** (`power_supply` online=1); disk **117 GB free** (re-check before pulls + baselines); production chain LIVE on 8481/8482 — hermetic tests only.
- **Engine seats for the riders:** `worker.DefaultDutyMap()` — judge `opus-4-8`, execution `sonnet-5` (D3 applied `e06f0a4`); the harness takes `SINET_B1_4_MODEL`.
- **After the edits:** lockgate green at **17**; settings tally **118**; migrations per OQ1 disposition (0001–0009 unless 0010 dispositioned); full battery + `-race` on touched packages; the tier-L smoke green with the R14 assert.
