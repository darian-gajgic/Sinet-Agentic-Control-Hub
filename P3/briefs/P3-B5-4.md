# P3-B5-4 brief — S14.5 conformance registry: the scheduling home for every "proven, not assumed" obligation

**Packet:** the S14.5 conformance registry — the control-plane table of standing conformance suites and drills "(id, owning section, fixtures, trigger set, schedule, last run, last result), the single scheduling home for every 'proven, not assumed' obligation the spec declares" (S14 term coinage, `Spec/drafts/S14-observability-evals-watch.md` line 7; body S14.5). The queue-row acceptance headline this brief decomposes: `conformance_registry` table; REGISTER the existing suites (kill-9+suspend, adapter per-lane incl. leak/native-spawn/no-SSE-replay, D6 violation attempts, compaction canary, forced-escalation e2e, verified-restore drill) with their triggers/schedules; results land as `eval.score_recorded`; any red → card (flag-now when lane/storage-affecting); suites assert behavior never docs; the S03.3 bump-gating tie recorded as data.

**Grounding sources (all read this session):** S14 draft in full (S14.5 home; S14.2 event contract; S14.4 watchdog; S14.6 canary layer; S14.8 sweep; ⚙ table); S02 in full (S02.1 migrations, S02.2 schema/attribution, S02.9 kill-9+suspend+durable set); S03 in full (S03.1 verbs, S03.3 bump gate, S03.5 lowering/leak obligation); S04.4–S04.6; S05.7; S06.7–S06.9; S07.7; S13.10; `P3/CONVENTIONS.md` §1–§31 in full; `internal/settings/index.go` (key declarations verified); the in-tree suites and seams cited throughout §2.

**Model routing (standing pipeline):** S14.5 is observability registration — not S10/S11-dense. Grounding/evaluation inherit the coordinator's model; executor and finalizer are ALWAYS opus (`P3/STATE.md` B5 routing note; the Fable-safeguard memory does not trigger on this packet's vocabulary).

**Spend:** this packet is $0 and fully hermetic. The executor REGISTERS suites; it never executes tier-L/paid suites, never spawns an engine, never dials anything.

## BINDING carried facts (verified in the tree at grounding — do not relitigate)

- **CF1 — the watchdog TODO.** `internal/watchdog/sweep.go:166–171` (B5-3): "TODO(S14.5/B5-4): register these four check ids AND the dead-man canary (deadman.go, R28-DMC — a fifth registry-row candidate: the watchdog's own liveness probe) into the conformance_registry when it lands". The four ids (`internal/watchdog/watchdog.go:77–80`): `watchdog.resume_reconcile`, `watchdog.listener`, `watchdog.organ_absence`, `watchdog.event_log_size`; the canary rule is `watchdog.dead_man` (:81). S14.5's OWN six-row table lists none of them — S14.4 calls them "recurring watchdog entries declared by siblings, scheduled here [S14.4]". Reconciliation is OQ5.
- **CF2 — the B5-2 TODO.** `internal/api/sse_test.go:680–687`: "TODO(S14.5/B5-4): register this standing assertion as a row" — the named seam is `TestConformance_NoEngineSSEReplay_ResumeFromLogAlone` (sse_test.go:688), the S14.3-¶3/S14.5 standing assertion (CONVENTIONS §30).
- **CF3 — kill-9 crash-atomicity scenario.** The B5-3 evaluation's F17(c) drop note carried "park+flag same-WriteTx under crash" as an OPTIONAL registry candidate. S14.5's text does not pull it in, and adding a scenario is suite CONTENT, which "stays with the declaring section" (S14.5) — i.e. an S02.9-suite addition in `internal/recovery`, not registry work. Disposition is coordinator discretion; NOT a requirement of this packet; the kill-9 registry row is unaffected either way.
- **CF4 — migration posture.** Migrations `0001`–`0010` exist (`internal/storage/migrations/`); no migration freeze binds B5-4 (unlike B5-3). The registry table is **migration 0011**, under S02.1/CONVENTIONS §6 rules (numbered one-transaction file, immutable once committed, `user_version` sole authority).
- **CF5 — BENCH-REG is read-only.** Registered numbers change only via its own §17. S14.5 does not touch benchmark scheduling (S14.7's "(14.5 hold)" is a feature-list item ref, not this section); the `benchmark.pair_recorded` producer and all BENCH-REG machinery are B5-7's.
- **CF6 — B5 sequencing.** B5-5 (promptfoo regression evals, S14.8) and B5-6 (watchlist/canary executor, S14.6) are separate packets. S14.6 explicitly assigns the weekly RUN of "the S14.5 adapter suites" to the canary layer ("Conformance canaries: the S14.5 adapter suites on their weekly schedule"). B5-4 is the scheduling HOME — rows, dueness, recording, red→card — never those executors.
- **CF7 — the event-type flip precedent.** `eval.score_recorded` is declare-only in `internal/eventlog/contract.go:531` with provenance "producer: B5-7 (S14.7/S14.8)" under a family-14 header comment "DECLARE, producers B5-7" (:529). S14.5's text makes B5-4 a producer ("Results land as `eval.score_recorded` events"); the B5-1 prediction is a code comment, not spec — corrected at the flip (R10). `producerTypes()` currently asserts 79 minted (`internal/eventlog/contract_test.go:148`), 14 declare-only.

## 1. Numbered requirements (each with its S-ref)

### A — Migration 0011 + the table (S14.5; S02.1/S02.2; CONVENTIONS §6)

- **R1.** New migration `internal/storage/migrations/0011_conformance_registry.sql`: numbered, one transaction, contiguous after 0010, immutable once committed (S02.1 "Migrations"; CONVENTIONS §6). History is never rewritten.
- **R2.** Table `conformance_registry` carries the S14 term-definition field set recognizably: **id, owning section, fixtures, trigger set, schedule, last run, last result** (S14 line 7; the queue row repeats it verbatim). Exact DDL is the P3 schema workshop's (S02.2 "exact DDL is the P3 schema workshop's job"); additional columns (severity class per R14, run-handle inside fixtures, notes) are P3 discretion.
- **R3.** Rows are platform-scope, owner-attributed: `user_id` = `'platform'` per 15.6 (S02.2 "every row carries its owning user_id") — the `snapshot_ledger` shape (`0009_snapshot_ledger_followups.sql`: `user_id TEXT NOT NULL DEFAULT 'platform' CHECK (user_id = 'platform')`).
- **R4.** Durable-set membership is automatic: the table lives in `platform.db`, therefore inside the S02.9 snapshot payload and every S13.10 archive (the 0009 header precedent). No extra work; do not exclude it.
- **R5.** `last run`/`last result` are MUTABLE registry state (the term definition names them registry fields — this table is control-plane state like `runs`/`asks`, not an event log). Row identity/owning-section/declared-by must never be rewritten by a result update; whether that is trigger-enforced (the §22/§23 raw-SQL-battery pattern) is executor discretion.

### B — Row registration: register the EXISTING suites (S14.5 table, verbatim)

- **R6.** Seed rows for the S14.5 table's six row-groups, each carrying its declaring section and its triggers/schedule from the spec table verbatim (S14.5): kill-9+suspend [S02.9 — "before any storage-touching component bump; quarterly sweep"]; adapter per-lane suites [S03.1/S03.5 — "on any engine/CLI version change + weekly"]; D6 violation attempts [S04.5 — "quarterly + on engine bump"]; compaction canary [S05.7 — "on engine bump + quarterly"]; forced-escalation e2e [S06/S04/S07 — "quarterly, with the full escalation drill (G1 Def.3 superseded set)"]; verified-restore drill [S02.9/S13 — "per S13's schedule"].
- **R7.** "Adapter per-lane" registers the lanes whose suites EXIST: **anthropic** (the claudecli battery, §2) and **local** (the S12.10 bump battery, §2). The zai lane has NO adapter package and NO suite (`internal/adapters/` contains only `claudecli`; S12.1 class (a) has no v0 consumer — the B4 coordinator reading in `P3/STATE.md`): its row lands with its adapter, and the omission is recorded in the seed's notes — the acceptance headline says "REGISTER the **existing** suites", and a row claiming a nonexistent suite would fake (the §26/§27 honest-dormancy discipline). This is a clearly-implied reading, not an open question.
- **R8.** Every seeded row's `fixtures` field names a REAL in-tree suite: package path + test-name pattern + env gates as DATA — the §28-F17 one-named-run-handle precedent (e.g. `go test ./internal/local/ -run TestBumpProcedure`, `SINET_LIVE_SMOKE=1` for tier L, `SINET_B3_4=1` for the per-pin canary). The verified inventory is §2 of this brief; the compaction row's fixture is OQ1.
- **R9.** "Suites assert behavior, never docs" (S03.3 rule 4; the headline) is recorded as a registry-level invariant (doc comment + row notes where relevant); the registered suites already conform (CONVENTIONS §10). The new package contains no doc/help-text parsing of any kind.

### C — Results land as `eval.score_recorded` (S14.5; S14.2 family 14; CONVENTIONS §29)

- **R10.** Flip `eval.score_recorded` declare→minted in `internal/eventlog/contract.go` with real provenance (the B5-4 recording surface; note B5-5/B5-7 as future co-producers — S14.7/S14.8 remain producers of this family) + a `producerTypes()` entry; counts move 79→80 minted / 14→13 declare-only; the family-14 header comment (:529 "DECLARE, producers B5-7") is corrected (§29 standing rule; the B5-3 flip precedent §31). `benchmark.pair_recorded` stays declare-only (CF5).
- **R11.** The recorded payload satisfies the family's contract minimum verbatim: "suite id + version, asset id + version, metrics, runner + runner version" (S14.2 family "Benchmark & eval"; `contract.go:394`). Mapping (P3 discretion, stated for convergence): suite id = registry row id; suite version = the row's fixture/suite version; asset = the component under test (e.g. engine pin, storage); runner + version = the run handle and its toolchain/engine identity. A representative golden joins the §29 required-field conformance set.
- **R12.** Recording a result appends `eval.score_recorded` AND updates the row's `last run`/`last result` in ONE WriteTx (the §6/§8 same-tx discipline: state change + event together). The recording surface is a control-plane verb usable by CI/operator ingestion now and by B5-5/B5-6 executors later (seam, §3).
- **R13.** The restore-drill row's result path follows OQ2's disposition — `internal/backup/drill.go` already records `platform.restore_drill` either way (+ `platform.restore_drill_flag` High on ledger mismatch; drill.go:113–129), and the S14.5 row is "registered here for visibility; pipeline and drill content owned by [XREF:S13]".

### D — Any red → card, flag-now when lane- or storage-affecting (S14.5)

- **R14.** A red result raises a CARD in the approval inbox; the flag-now alert class applies exactly when the row is lane- or storage-affecting (S14.5). The predicate is row DATA, seeded from each row's declaring section: kill-9+suspend and verified-restore = storage-affecting; adapter per-lane, D6 attempts, compaction canary = lane-affecting; forced-escalation e2e = neither (ordinary approval-class card). Flag-now vs digest are alert-routing classes (S14.4 "Alert discipline"), distinct from the inbox risk tiers.
- **R15.** Mechanism constraint: `asks.run_id` is `NOT NULL REFERENCES runs` (`0001_core_schema.sql:123`), and a suite run has no platform run — the card is therefore NEVER a bare asks row. The sanctioned shape is the B5-3 run-less-flag precedent: durable platform-scope state (here: the red registry row + its `eval.score_recorded` event) surfaced by derivation in the inbox projection (`internal/api/projection.go` `InboxSnapshot`, which already carries `WatchdogFlags` and a `DriftCards []any` placeholder at :407–418) — a view/subscriber over the log and control-plane state, per S14.1. A green result raises nothing.
- **R16.** Owner-scoping per S01.9 as implemented in B5-2 (§30): platform-scope conformance cards are operator-visible; members never see them.

### E — Scheduling-home semantics: dueness, triggers, the bump-gating tie (S14.5; S03.3)

- **R17.** The registry is the scheduling HOME, not an executor: it computes DUE-ness per row from `schedule` + `last run` and exposes due/overdue rows as a read surface. It does NOT execute Go-test batteries in-process at v0 (they are `go test` runs; execution stays with owners — CI/operator per §10 tiers, the B4-3 restore-drill timer, B5-6's weekly canary executor per CF6, B5-5's probes per S14.8). Dueness derivation must survive restart/suspend — derived from stored state, never an in-memory ticker (the B5-3 dead-man dueness precedent, §31).
- **R18.** Trigger kinds are DATA in the `trigger set` column, from the spec rows: `engine_bump` (S03.3), `storage_component_bump` (S02.9), `weekly`, `quarterly`, `per_s13_schedule` (⚙ `backup.drill_interval`), `with_escalation_drill` (G1 Def.3 superseded set; ⚙ `verification.drill_interval_days`). Naming is P3 discretion; the SET must cover the six spec rows' trigger text.
- **R19.** The S03.3 bump-gating tie is recorded as DATA (the headline): an engine bump lands only after (a) the candidate passes its per-lane conformance suite AND (b) the S14.8 before/after quality probe shows no regression (S14.5 closing ¶; S03.3 item 3; P-T02-5). The registry makes the bump-gating set queryable (rows whose trigger set contains `engine_bump`, plus the recorded (b)-limb reference to S14.8/B5-5); it builds NO bump executor and NO quality-probe machinery — the deliberate bump stays the operator's S03.3 procedure (the B4-gate D2 precedent), and a landed bump's mass-revalidation trigger (P-T14-1) is executed by the S14.8 runbook [XREF:S08], B5-5's.
- **R20.** Both `TODO(S14.5/B5-4)` comments are discharged this packet: sse_test.go per R6/R8 (the assertion registered; comment updated to name its row id), sweep.go per OQ5's disposition (rows or a recorded disposition comment). No stale `TODO(S14.5/B5-4)` remains in-tree afterward. The watchdog's importwall forward-allowlist (`{eventlog,local,metering,run,storage}`, §31) must NOT gain an edge — discharge is comment/data-level (and inbox-side derivation reads events, never imports `watchdog`).

### F — Package home + walls (S01.3; CONVENTIONS §2 precedents)

- **R21.** Proposed home: NEW package `internal/conformance` (the registry types, migration-consuming store verbs, seed rows, dueness, the recording surface). Justification: no existing seam owns it — `eventlog` is the log (§29: contract data only), `verify` is judging logic (S07 boundary: "this section records and detects, it never judges" — S14 scope note), `watchdog` is detection with a pinned import wall (§31). Imports: `storage` + `eventlog` + `settings` (per OQ3/OQ4 key consumption) + stdlib ONLY; AST/import pins per the §15/§17/§26 precedent. `internal/api` surfaces via its own projection queries (the §31 pattern: api reads state without importing the producer); `conformance` never imports `api`/`adapters`/`stage`/`verify`/`watchdog`/`backup`.
- **R22.** Shell/composition wiring only as far as the dispositions require (dueness read into any surfacing; no goroutine unless OQ3 mandates an announcement path). The package must be reachable from the composed binary (`go list -deps` includes it) if any runtime surface lands — the `buildAcceptSurface` precedent (§24).

### G — Tests (CONVENTIONS §3; stdlib only)

- **R23.** Hermetic test obligations: migration applies + contiguity (0001–0011); seed-row conformance (every S14.5 spec row-group present with declared-by + schedule matching the spec table; per-lane per R7); fixtures resolve (each named test pattern matches a real in-tree test — checkable by source scan, no execution); the R10 flip's contract tests (declare-once, counts, golden required-fields); recording verb one-tx behavior (event + row update together, atomically visible); red→card projection (red row surfaces, flag-now class per R14 seeding, green silent, member-scoped invisible); dueness math incl. the restart-safety property; import walls. `go build ./... && go test ./...` green; `-race` on touched packages; gofmt/vet clean; lockgate green.

## 2. The suite inventory (verified in-tree this session — the fixtures column's ground truth)

| S14.5 row | What exists (cite) | Run handle / gates |
|---|---|---|
| kill-9 harness + suspend-cycle [S02.9] | `internal/recovery/crash_test.go` — self-re-exec via `SINET_CRASH_SCENARIO` (:31), `TestKill9MidTransitionInvisible` (:210), `TestKill9EffectCrashWindow` (:290); companion `TestSuspendCycleWakeGrace` (`recovery_test.go:206`) — CONVENTIONS §8 | `go test ./internal/recovery/` |
| Adapter per-lane — anthropic [S03.1/S03.5] | `internal/adapters/claudecli/conformance_test.go` — tier F fixtures (`TestParse*`, `TestLowering*`, `TestHook*`, `TestOutcome*`), leak/lowering (`TestLoweringKnobs` :85, `TestLoweringEnvScrub` :176, `TestLoweringRefusesNativeSpawnTools` :165), pin coupling (`TestPinMatchesLock` :621), tier R gated w/ `SANCTIONED SKIP` (:649; `TestRealEngine*` :654–686); `e2e_test.go` (defer-park/resume/cancel-ladder e2e :172–371); `../crash_boundary_test.go` (§20 C3); tier L `live_smoke_test.go:25` (`SINET_LIVE_SMOKE=1`); per-pin canary `recite_live_test.go:46` (`SINET_B3_4=1`); memory-containment per-pin `containment_e2e_test.go:19` (P-T10-1, §17) — CONVENTIONS §10 F/R/L | `go test ./internal/adapters/...`; live gates as data |
| Adapter per-lane — local [S03.2/S12.10] | the S12.10 deliberate-bump battery, §28 F17: pin↔lock coupling + real llama-swap `/v1` logprobs/`json_schema`/unload + reason-first order assert | `go test ./internal/local/ -run TestBumpProcedure` (+`SINET_LIVE_SMOKE=1`) |
| No-engine-SSE-replay standing assertion [S14.3 ¶3; S14.5] | `internal/api/sse_test.go:688` `TestConformance_NoEngineSSEReplay_ResumeFromLogAlone` (B5-2, §30; CF2) — a named bullet of the spec's adapter row; own row vs adapter-row component is executor discretion (the test lives in `internal/api`) | `go test ./internal/api/ -run TestConformance_NoEngineSSEReplay` |
| D6 violation attempts [S04.5] | `internal/stage/helpers_e2e_test.go` — `TestHelperDepth3RecursionRefused` (:202), native-or-lateral-tool refusal (:169 in `TestHelperRefusalsAreLoggedEvidence` :155), `TestHelperSpawnBudgetExhaustionRefuses` (:239), `TestHelperKillSalvagesAndSiblingsContained` (:336), `TestHelperEscalationOpensDurableAsk` (:307) — the §19 standing battery; per-lane native-spawn limb also `TestLoweringRefusesNativeSpawnTools` (claudecli) | `go test ./internal/stage/ -run TestHelper` |
| Compaction canary [S05.7] | **NO standing canary-constraint→compact→adherence suite exists in-tree** (grep: no adherence/fidelity machinery). What exists: the B1-4 spike record `P3/measurements/2026-07-20-precompact-injection-mechanics.md` (PreCompact veto + `SessionStart source:"compact"` re-injection PASS, closed A4); the re-injection machinery (`claudecli/sessionstart.go:31` matcher `startup\|resume\|compact` + fires log; `ledger` reinjection manifest); the `SINET_B3_4`-gated per-pin live test (recite_live_test.go). → **OQ1** | per OQ1 |
| Forced-escalation e2e [S06.7/S06.8/S04.5/S07.7] | `internal/intake/pipeline_test.go` — `TestCoverageEscalationForced` (:635, S06.7 "covered by an end-to-end test that forces it"), `TestSpecDoubtForced` (:716, S06.8); `internal/stage/helpers_e2e_test.go:307` (helper ESCALATED → durable ask); `internal/verify/escalate_test.go` — the planted-defect class, one per route category (S07.7 proof 1): `TestPlantedACBlockerEscalates` :76, `TestPlantedSanityBlockerEscalates` :99, `TestPlantedReopenSpecReachesInbox` :116, `TestPlantedJudgeCheckDisagreementCardsAndQuarantines` :133, `TestPlantedExecutorChallengeCards` :174, `TestPlantedAuditFailureQuarantines` :188, `TestPlantedResearchNotRunReachesInbox` :202, `TestPlantedCapHitReachesInbox` :233, `TestPlantedWorkerFlawFilesTicket` :252, `TestPlantedSafetyAlertsOperator` :271, + `TestRouteTableTotalAndSinkTypesOnly` :20 | `go test ./internal/intake/ ./internal/stage/ ./internal/verify/` (patterns as data) |
| Verified-restore drill [S02.9/S13.10] | PRODUCTION machinery, not a test: `internal/backup/drill.go:45` `RestoreDrill` (remote fetch → fail-closed ledger verify → escrow decrypt → scratch rebuild → S02.9 invariants; records `platform.restore_drill` either way, `platform.restore_drill_flag` High on mismatch — `backup.go:63–68`); `sinet restore-drill` mode + generated `Persistent=true` calendar timer from ⚙ `backup.drill_interval` (§24) | timer-driven; row derives per OQ2 |

Unwired S07.7 seams adjacent to the forced-escalation row (verify comments assign them to "Spec S14's (B5)"): `CanaryPlan` (escalate.go:347, the always-escalating synthetic task shape), `CanaryWatch.Silent` (:390, silence vs ⚙ `verification.canary_interval_hours` — "the operator alert it feeds is S14 wiring (B5)" :381), `DrillCard` (:417) + `DrillDue` (:428, ⚙ `verification.drill_interval_days`). Referenced nowhere outside `internal/verify` (grep-verified). → **OQ4**.

## 3. Seams to respect (stub shapes for phases not yet come)

- **B5-5 (promptfoo, S14.8):** records regression-eval scores through the SAME `eval.score_recorded` family; the S03.3 bump tie's (b) limb references S14.8 as data (R19). Stub shape: the R12 recording verb accepts an arbitrary runner identity; no promptfoo code, pin, or adoption here.
- **B5-6 (watchlist/canary, S14.6):** runs the weekly adapter-suite cadence (CF6) and the behavioral/auth canaries; `canary.result`/`drift.finding` stay declare-only (contract.go:473–474, "producer: B5-6"). Stub shape: the R17 due-rows read surface is consumable by a future executor; nothing polls, nothing fetches.
- **B5-8 (queryable history):** derive queries over registry rows/events may later gain indexes — B5-4 adds none beyond what its own queries need (the B5-3 F16 carry assigned indexing to B5-8).
- **B6 (S15 surfaces):** card rendering and a registry browsing surface are React/HTTP work — B5-4 exposes projections/read verbs only (the §31 "HTTP endpoint + React button are B6/S15" posture).
- **verify (S07.7):** consume `DrillDue`/`CanaryWatch` only as OQ4 disposes; never re-implement their logic in `conformance` (content stays with the declaring section, S14.5).
- **watchdog (S14.4):** no import edge either direction (R20); reconciliation is data/comments per OQ5.

## 4. OPEN QUESTIONS — coordinator dispositions required BEFORE the executor launches (the B5-1/2/3 precedent)

- **OQ1 — the compaction-canary row's fixture.** The S14.5 row exists; no standing suite does (§2). Options: (a) register the row with fixtures = the spike-method probe (the B1-4 measurement record as the re-runnable procedure, re-run per engine bump, result ingested via R12) plus the existing per-pin gated tests (`SINET_B3_4` canary, sessionstart fixtures) as the nearest standing behavior checks, notes recording the gap honestly; (b) additionally commission the canary-constraint→compact→adherence suite now — but that is suite CONTENT owned by S05.7/the adapter, paid/live by nature, and outside a registration packet's scope. **Recommendation: (a);** the missing suite is flagged to the B5 gate as an owning-section follow-up, not silently absorbed.
- **OQ2 — restore-drill result path.** (a) The row is visibility-registered: `last run`/`last result` DERIVE from the existing `platform.restore_drill` events (S13.10 owns content and already records either way; no `backup` change); or (b) uniform recording: the drill ALSO emits `eval.score_recorded` (S14.5's blanket "results land as `eval.score_recorded`"), touching `internal/backup`. **Recommendation: (a)** — "registered here for visibility; pipeline and drill content owned by [XREF:S13]" (S14.5 row) reads as mirror-not-re-emit, and double-recording the same outcome serves nothing.
- **OQ3 — overdue posture.** S14.5 mandates a card only for RED. Does an overdue/never-run row raise anything at v0 — (a) nothing active: dueness is a passive read surface (B6 renders it; the S14.6 "a dead watch MUST announce itself" clause is the watchlist's, not this section's); (b) a digest-tier announcement when a row passes its due date. **Recommendation: (a)** at v0, with the read surface making overdue-ness trivially visible; (b) is one derivation away if the coordinator wants it.
- **OQ4 — the S07.7 canary/drill wiring split.** The verify seams (§2 end) defer "scheduling and the watchdog alert wiring" to "Spec S14's (B5)" without naming the packet. Options: (a) B5-4 registers the escalation-drill cadence as row data on the forced-escalation row (⚙ `verification.drill_interval_days` read for dueness) AND records the daily escalation-canary as a registry-visible schedule (⚙ `verification.canary_interval_hours`), while the canary's RUNNER (spawning the synthetic pipeline task) and the silence→operator-alert wiring go to B5-6/B6; (b) B5-4 also wires `CanaryWatch.Silent` into an alert path now; (c) leave both wholly to B5-6. **Recommendation: (a)** — the registry is the scheduling home (dueness/visibility), executors are not this packet (CF6), and the silence ALERT is alert-discipline machinery (S14.4/B5-3 severities) whose runner does not exist yet.
- **OQ5 — the watchdog TODO reconciliation (CF1).** S14.5's table does not list the four platform-health checks or the dead-man canary; S14.4 schedules them itself (continuously-running detection on the sweep, not fixture-backed proof suites — the registry's term is "standing conformance suites and drills"). Options: (a) register all five as visibility rows (schedule = "continuous (watchdog sweep)" / "daily (dead-man)", last-run derived from `watchdog.*` events); (b) register none — discharge the TODO with a recorded disposition comment citing S14.4 ownership; (c) register only the dead-man canary (a daily drill-like probe, the same G1-Def.3-superseded set the S14.5 escalation row cites). **Recommendation: (c)** — the dead-man is genuinely a scheduled drill with a pass/fail outcome; the four checks are detection, and puffing the registry with non-suites dilutes "proven, not assumed". Any of the three is spec-consistent; the executor implements the disposition verbatim.
- **OQ6 — the quarterly/weekly cadence source.** No ⚙ exists for S14.5's bare "quarterly"/"weekly" literals; the B5 phase preamble's key list includes `eval.sweep_interval`, but its S18 declaration (verified: S14, months, default 3, clamp 1–6) reads "Months between full regression-eval sweeps" — S14.8's revalidation sweep, ratified R12 §4.7. Options: (a) the literals are structural row DATA seeded from the S14.5 table verbatim (quarterly=3 months, weekly=7 days as structural constants — the `sseBatchSize`/§31 precedent, gate-flagged under the standing settings-tab directive); `eval.sweep_interval` stays referenced-not-consumed (B5-5's); (b) consume `eval.sweep_interval` as the shared quarterly knob for registry rows (one operator knob for all quarterly obligations — stretches its ratified S14.8 meaning); dedicated new keys are FORBIDDEN either way (118-key tally frozen; B5 rule: never re-declare). **Recommendation: (a).**

**No S00.9 candidate found.** No spec conflict, gap, or impossibility surfaced: the CF1 reconciliation is code-comment-vs-spec-table (dispositionable, OQ5); the missing compaction suite is an implementation gap flagged via OQ1; the `asks.run_id` constraint is satisfied by the sanctioned derive pattern (R15); the contract.go "producer: B5-7" line is a code comment (CF7).

## 5. ⚙ settings (confirmed against `internal/settings/index.go` this session; NONE re-declared — tally stays 118 keys / 33 domains)

| Key | Declared (verified) | This packet |
|---|---|---|
| `backup.drill_interval` | S13, months, default 3, clamp 1–12 (:739) | READ for restore-drill row dueness (per OQ2(a): derive + display); the timer that RUNS the drill already consumes it (B4-3, §24) |
| `verification.drill_interval_days` | S07, days, default 90 (:375) — "distinct from the backup restore drill, S18.4 R6" | READ for the forced-escalation row's drill cadence per OQ4 |
| `verification.canary_interval_hours` | S07, hours, default 24 (:368) — G1 Def.3 superseded "canary daily" | READ per OQ4(a) if the canary schedule registers |
| `eval.sweep_interval` | S14, months, default 3, clamp 1–6 (:853) — "full regression-eval sweeps" (S14.8) | referenced-not-consumed per OQ6(a); consumed only under OQ6(b) |
| `canary.auth_interval` / `canary.behavioral_interval` / `watchlist.fetch_fail_streak` | S14 (:832/:839/:825) | B5-6's — untouched |
| `watchdog.*` (8 keys) | S14 | B5-3's — untouched |

Consumption is by dotted key through a narrow `conformance.Settings` interface (§6 discipline); no key is ever a hardcoded constant; the structural cadence constants of OQ6(a) are code data flagged to the B5 gate (settings-tab directive).

## 6. Files expected to change

- NEW `internal/storage/migrations/0011_conformance_registry.sql` (R1–R4).
- NEW `internal/conformance/` — registry store/verbs, seed rows, dueness, recording surface, red→card derivation input, tests (R6–R23; name per R21).
- `internal/eventlog/contract.go` + `contract_test.go` — the R10 flip (provenance, `producerTypes()`, 79→80 count assert, family-14 header).
- `internal/api/projection.go` + tests — the R15 inbox surfacing derivation (the `InboxSnapshot` extension point; owner-scoped).
- `internal/api/sse_test.go` — CF2 TODO discharge (comment names the registered row; test body untouched).
- `internal/watchdog/sweep.go` — CF1 TODO discharge per OQ5 (comment/data only; no import change, no behavior change).
- `internal/shell/` — composition wiring only as far as R22 requires.
- `internal/verify/escalate.go` — comment-level updates only if OQ4(a) lands (the "S14 wiring (B5)" notes point at their registry rows); logic untouched.
- `internal/backup/` — ONLY under OQ2(b); untouched under the recommendation.
- `P3/CONVENTIONS.md` — §32 added (this packet's conventions; append-only, citing S14.5/S02.1/S03.3).
- NO other package; migrations 0001–0010 untouched (immutable).

## 7. Adopted components touched

None. No engine is invoked; no new Go module (stdlib + existing internals); `components.lock` stays at 17 entries; no pin moves. The registered suites REFERENCE adopted engines (claude CLI, llama-swap/llama.cpp) as row data only.

## 8. CONVENTIONS constraints binding this packet

- §2 (stdlib-first; package naming; ⚙ discipline — no hardcoded ⚙ value, dotted-key reads; doc comments cite spec sections); §3 (stdlib `testing` only; `t.TempDir()`; green precondition; never commit red); §5 (subject `P3-B5-4: <summary> (S14.5, …)`; trailer `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`; stage explicitly, never `git add -A`; NEVER push; never edit `Docs/`/`Spec/`/`Research/`/`P3/STATE.md`); §6 (migration rules; settings conventions; one-tx event+state writes; tally test 118/33 stays green — `internal/settings/settings_test.go:44/:79`); §8 (same-tx transition discipline the R12 verb mirrors); §10 (tier vocabulary + the one sanctioned skip class — reused in row data, not re-invented); §22/§23 (trigger-battery + no-new-⚙ precedents); §26/§27 (structural-config precedent; honest dormancy — nothing faked); §29 (contract-flip mechanics + declare-once); §30 (CF2 seam); §31 (importwall inviolate; structural-constant posture; derive-from-log precedent).
- Real-world facts: none needed live — this packet registers in-repo suites (promptfoo adoption is B5-5's; no library/API/pricing claim is load-bearing here).
- Deviations, if any, are DECLARED in the packet report for coordinator triage — never silent.

## 9. Acceptance checklist (the evaluation rubric — each item concretely checkable)

1. Migration `0011_conformance_registry.sql` exists, applies in one tx after 0010, `user_version` = 11; files 0001–0010 byte-untouched (`git diff --stat` clean on them).
2. `conformance_registry` DDL carries id / owning section / fixtures / trigger set / schedule / last run / last result recognizably + `user_id` platform attribution (R2/R3).
3. Seed rows cover every S14.5 table row-group per R6, with declared-by + triggers/schedules matching the spec table verbatim; adapter rows per-lane per R7 (anthropic + local; zai omitted with recorded reason).
4. Every seeded fixture reference resolves to a real in-tree test/procedure (§2 inventory; compaction row per the OQ1 disposition); no row claims a suite that does not exist.
5. `eval.score_recorded` is minted: contract entry flipped with corrected provenance, `producerTypes()` lists it, counts 80/13, family-14 header corrected; `benchmark.pair_recorded` and B5-6's types remain declare-only.
6. A recorded result's payload carries suite id+version, asset id+version, metrics, runner+runner version (representative golden in the §29 conformance set) — R11.
7. Recording = ONE WriteTx: `eval.score_recorded` append + row `last run`/`last result` update land atomically (test proves both-or-neither) — R12.
8. RED → an inbox-visible card via derivation (no asks row; `asks.run_id NOT NULL` respected); flag-now class exactly on lane/storage-affecting rows per the R14 seeding; GREEN raises nothing; member view excludes platform-scope cards (R15/R16).
9. Dueness derives from schedule + last-run, restart/suspend-safe (no ticker as source of truth); ⚙-backed cadences read by dotted key at evaluation time (R17; §5 keys per OQ dispositions).
10. The bump-gating set is queryable data (engine-bump rows + the S14.8 (b)-limb reference); no bump executor, no quality-probe machinery, no promptfoo, no canary executor in the diff (R19; CF5/CF6 boundaries held).
11. Both `TODO(S14.5/B5-4)` markers gone from the tree, each discharged per its disposition (R20); watchdog imports unchanged (importwall test green).
12. OQ1–OQ6 dispositions implemented exactly as ratified (verbatim check against the coordinator's log entry).
13. Package walls hold: `internal/conformance` imports ⊆ {storage, eventlog, settings, stdlib}; nothing imports it except shell/api-side composition per R21/R22; AST/import pin test present.
14. No new ⚙ key (settings tally test green at 118/33); no new `components.lock` entry (17); no new Go module (`go.mod` untouched); BENCH-REG untouched; `Spec/`/`Docs/`/`Research/`/`P3/STATE.md` untouched.
15. Battery green: `gofmt` clean, `go vet` clean, `go build ./...`, `go test ./...`, `-race` on touched packages, `go run ./tools/lockgate` — all green; suites-assert-behavior invariant recorded (R9) and no doc-parsing anywhere in the new package.
16. CONVENTIONS §32 appended (spec-cited, append-only); commit subject/trailer per §5; nothing pushed.
