# P3-B6-7 brief — Conversational assistant: backend + widget

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.7** owning + **S15.2/S15.3/S15.4/S15.12/S15.13** binding context (`Spec/drafts/S15-frontend-api.md`); **FC-v1 §3** (`Spec/frontend-components-v1.md` — the @assistant-ui/react pick, its decision notes, and the carried Jarvis behavior specs); **S14.10** (the three query layers; `S14-observability-evals-watch.md`); **S12.4/S12.5/S12.1** (duty aliases, calibration, $0 metering; `S12-local-models-tier.md`); **S06.1/S06.2/S06.5/S06.9** (the intake pipeline the assistant hands to; `S06-intake-pipeline.md`); **S11.3/S11.6** (file confinement; `S11-sandboxing-confinement.md`); feature items **9.5 / S3.6** (the assistant) and **15.6** (owner attribution on every record) in `Docs/agent-platform-feature-list-v1.md:231/376/305`. `P3/CONVENTIONS.md` binds: **§5, §7, §9, §30, §38, §39, §40, §41 + §41-B ENTIRE, §42 + §42-B ENTIRE, and §43 once B6-6 lands** (constraints in §6 below). **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is packet 11/13 of B6** (queue `P3/STATE.md` §B6; B6-1/2A/2B/2C/3A/3B/3C/4/5 are landed; **B6-6 is EXECUTING as this brief is written — the coordinator restates deltas at launch**). It builds the S15.7 conversational assistant: **A — the backend** (server-side per-user chat sessions, the file exchange, first-message auto-titling as a local-tier duty, turn routing over the LANDED S14.10 layers, the S06 intake handoff) and **B — the widget** (`/chat` goes live on @assistant-ui/react with a Sinet adapter; the carried Jarvis behavior specs). **The A/B stop is PRE-AUTHORIZED in the queue row** (§9). It re-implements NO history/intake/local machinery — `internal/history`'s layers, `IntakeSurface.Submit`, and `local.Duty` are consumed as landed. Review surfaces + the escape-allowlist widening (B6-8), push/re-nag + the S15.12 sweep (B6-9) are NOT this packet — their stubs stay.

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit — S15/FC-v1 frontend scope is not S10/S11 security-dense; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-29 — do not relitigate)

**A. What S15.7 actually grants the assistant (S15:96–105).** Three verbs, exactly: (1) **platform-state questions** — "platform-state answers ride the S14 query layers" and S14.10 ¶1 sharpens it to "the conversational assistant consumes these layers **and nothing else**" (S14:136); (2) **start a task by chatting** — "hands the conversation to the S06 intake pipeline **in place**"; (3) the sessions/file-exchange machinery around them. **No free-generation chat duty exists anywhere in the spec** — S12.4's registry has no conversational seat, and the S06.10 cut line keeps open-ended interaction off local aliases. A turn that is none of the three verbs has no sanctioned answer path (OQ3 dispositions the honest behavior). S15.13: the JARVIS file exchange is explicitly NOT parked — it ships at v0 (S15:148); conversation mode/STT/TTS/avatar ARE parked — nothing here builds toward them.

**B. The landed query ladder (consumed, never rebuilt).** `internal/history` is the S14.10 surface; B6-1 routed it (`internal/api/historyapi.go`): **GET** `/api/events/views` + `/api/events/catalog` (registries, :60–84), `/api/events/views/{view}` (Layer 0, :86–106), `/api/events/query/{query}?slot_<name>=` (Layer 1 direct, :108–125), `/api/events/ask?q=` (Layer 1 intent-filled, :127–141), `/api/events/search?q=` (:146–157), `/api/events/open-sql?q=` (Layer 2, :159–178). **Layer 2 has its OWN route and NOTHING falls through to it — "reaching open SQL is an act"** (historyapi.go:25–28). Registries at grounding: **11 Layer-0 views, 49 catalog queries** (layer0.go / catalog.go; **B6-6-volatile — its OQ5 adds `eval.scores` → expect 50; restate at launch**). The one answer currency is `history.Answer` (history.go:202–217): `{layer, query, confidence, question?, columns, rows, row_count, truncated, notes?, card?, audit?}` — `newAnswer` is the ONLY constructor and derives `confidence` from the layer (`deterministic`/`canned`/`lower-confidence`, :44–61, :222–233); a disambiguation card (`Disambiguation{question, reason, choices[{query,category,description}]}`, :185–196) and a Layer-2 refusal are **200-class answers served WITH their audit** (`writeAnswer`, historyapi.go:206–231), never bare HTTP errors. `OpenSQLAudit` (layer2.go:110–126) carries outcome ∈ {executed, refused, no_sql, unavailable, failed} (:86–92); every attempt writes `history.query_audited` (:79). `Ask` NEVER guesses: two grammar-constrained duty calls on `local.AliasIntentFilling` (intent then slots, layer1.go:150–288), calibrated margin gate when `CalStore` has the key (intent-filling@4B IS calibrated, S12.5-A7), and every unresolved path — absent stack, abstain, unknown name, low margin, unfilled slot — returns the card (layer1.go:103–137). `Answer.Notes` already announce the classifying model ("intent classified by <model> on duty alias intent-filling", :131) — the in-feed fallback announcement has a served source.

**C. The duty machinery (the $0 discipline).** `local.Duty.Call(ctx, runID, DutyRequest{Alias, System, User, Schema, Name, MaxTokens, Classification})` (duty.go:38–51, :146–160): **every call needs a consuming run** — a run-less human act rides a short-lived platform advisory run via `stage.AdvisoryMeter`/`BeginAdvisory` (advisory.go:30–49; fallback id `platform.local-advisory`), exactly how history's intent legs are wired at the shell root (`shell/history_seams.go:86–98`) — and writes ONE $0 D7 checkpoint (SessionSubstrate `"local"`, duty.go:225–239). Nil-safe: an absent stack is `ErrStackAbsent` and the CALLER degrades per its S12.4 row — never a fake. Classification calls are greedy+logprobs+abstain-belt (`assertAbstain`, :274–291); free-text drafting sets `Classification:false`. The registry resolves over ⚙ `local.alias` READ AT CALL TIME (alias.go:32–49); the landed map (settings/index.go:691–703) carries **exactly the ten S12.4 rows — there is NO auto-titling alias and NO row may be invented** (OQ2). Hermetic tests ride `local.FakeServer` (fake.go:49–67) — an httptest llama-swap double with per-model responses and request-shape capture (:184–241) — the fake-engine rig every duty leg's tests MUST use ($0, zero network).

**D. The landed intake entry (the handoff target).** `api.IntakeSurface` (intake_handlers.go:25–48): `Submit(ctx, userID, body)` → Stage-0 triage + task/run birth + `Enqueue(ClassInteractive)` → task view (stage/surface.go:87–107); the route is `POST /api/intake/requests` (api.go:384). **The submit body is `{title, text}` and nothing else** (`submitBody`, surface.go:79–82; `intake.Request{TaskID?, UserID, Title, Text}`, intake/intake.go:73–78) — **no file/inputs field exists** (OQ8). The interview then flows as batched option cards (S06.5) answered via the landed ask/approvals verbs, and "answering a card resumes the pipeline in place" (S06.1). `maxIntakeBody` = 1 MiB is the transport-bound structural-constant precedent (intake_handlers.go:67). Errors are `*SurfaceError{Status, Code, Msg}` → `{"error": code, "detail": msg}` (:52–58, :85–92).

**E. The event plane.** ONE SSE endpoint `/events` (sse.go:77–201): unfiltered relay owner-scoped (`visible`, :58–75), four closed topics {run, board, fleet, inbox} (topics.go:17–31) — **a type in no topic still flows on the unfiltered relay** (topics.go:11–14, forward-tolerant), and the landed client listens by event TYPE, so new `chat.*` frames need ZERO topics.go change. `wireEvent` = {seq, run_id?, generation?, user_id, type, schema_version, topics, payload (redacted at edge), ts} (:26–38); `id:` = event_seq. Inventory at grounding: **91 minted / 5 declare-only / 96 registered** (contract_test.go:218); **no chat type exists** (grep) — any new type enters through `internal/eventlog/contract.go` with its pins moved in lockstep (the B6-1/B6-3A precedent). Every mutation lands on the event log (S15.2 rule 3); no direct outward-effect verb may exist anywhere on the API (S15.2 rule 1).

**F. The landed web machinery (consumed, never rebuilt; line refs as read at grounding — B6-6 touches App.tsx/routes.ts/api.ts/doubles/fixtures, so re-locate by symbol at launch).** `/chat` exists as a stub: `routes.ts:48` `{id:'chat', pattern:'/chat', title:'Assistant', owner:'B6-7', nav:true}`; going live = flip `owner` to `''` AND add `'chat'` to the `built` array (routes.test.ts:27) — **patterns are never renamed** (§41-B). The tab holds ONE module-scope EventSource (`events.ts:413–420`); **subscriptions declare event TYPES, never topics** — the shell subscriber pins the connection to the unfiltered relay for the whole authed lifetime (App.tsx:153–180; live.ts:9–17; §42 OQ1(b)), and `LiveRead` has no topics field at all (live.ts:106–119). Six-state connection Status (events.ts:36–43); resnapshot reasons {connected, stream-rewound, frame-unreadable} (:48–61); backoff ladder 1/2/5/10/30s (:112). `api.ts`: `request<T>` + `ApiError{status}` + `Unreachable` (:48–85), `errorMessage` prefers the served `{error, detail}` (:89–101); **no chat method exists** (:3–4 defers the family here). **The assistant-ui probe** (picks.test.tsx:208–265): `useLocalRuntime(adapter)` with a `ChatModelAdapter` whose `run` is an async generator (:215–219, :237), a whole turn appended and rendered (`runtime.thread.append`, :239; transcript asserted :256–257), and **`fetch` stubbed-to-reject with `expect(fetchSpy).not.toHaveBeenCalled()`** (:209–210, :262) — the first instalment of the assistant-cloud negative; §41's recorded reading: "never used" means **never WIRED and never CALLED — not absent from node_modules** (assistant-cloud IS a hard dependency of the pin). `test-setup.ts:21–28` stubs `ResizeObserver` no-op BECAUSE @assistant-ui/react constructs one on mount (:10–20). Golden fixtures: `web/src/fixtures/api/*.json` (18 files at grounding) produced compare-only by `internal/api/apifixtures_test.go` (dir :55, env `SINET_WRITE_API_FIXTURES` :59, fixed clock :63–69), consumed via `?raw` by `doubles.ts` (:5–22); `scriptedFetch` THROWS on any unscripted request (:103–127); `FakeSource` is the EventSource double (:38–82). Scans that bind the new surface: escape scan with EMPTY allowlist — **widens at B6-8, never here** (escape-scan.test.ts:22–31); **no localStorage/sessionStorage/document.cookie** (scans.test.ts:105–111) — chat state MUST be platform-owned; no money arithmetic (:63–67); no percent/fraction/ETA (:87–103). vitest: jsdom, `css:true`, **TZ `Pacific/Auckland`** (vitest.config.ts:17/25). `web/package.json` pins are exact; `@assistant-ui/react` **0.14.27** (package.json:13); no markdown package, no attachment add-on package exists in the tree (OQ10).

**G. Grounded gaps this packet must not paper over.**
1. **The S15.2 family table has NO chat/assistant row** (S15:25–34): the assistant is named an equal CLIENT (S15:21), but the "server-side per-user session registry" (S15:105) has no route root and no data-owner column anywhere in the frozen table. A new resource family is additive-first evolution (S15.2 rule 4) — but WHERE it roots and WHO owns the rows is a coordinator disposition (OQ1), never an executor invention.
2. **Auto-titling has no S12.4 registry row** (S12:56–67) and no `local.alias` key (index.go:691–703, byte-frozen). S12.4's own rule — "which duties exist is owned by their sections" — puts the duty's EXISTENCE in S15.7; its serving seat is OQ2.
3. **No exchange-folder concept exists anywhere** — not in S11 (which governs run sandboxes: allowlist-only mounts, S11.3; resolve-then-deny on config paths, S11.7), not in storage, not in the API. Its disk home, manifest, bounds and diff mechanics are OQ4/OQ7.
4. **`submitBody` has no inputs field** (D above) — "files handed to a launched task enter as task inputs through intake" (S15:104) needs an additive shape (OQ8).
5. **The B6-1 evaluation carry (N13) lands here**: open-sql (and ask) ride GET with `?q=` — the wire-semantics/POST question was explicitly deferred to this grounding (OQ9).

**Counters at open (B6-6 is executing — the coordinator RESTATES these at launch; assert before changing anything):** migrations **0001–0019** (`user_version` 19); event inventory **91/5/96**; ⚙ **118 keys / 33 domains** (`internal/settings/index.go` byte-unchanged through all of B5+B6); `components.lock` **29**; conformance rows **12**; CONVENTIONS **§1–§42(+B)** with **§43 expected from B6-6** — this packet appends **§44**; `web/` **147 vitest / 15 files** at B6-5's landing (B6-6 grows it); **43 Go test packages** green. **B6-6-volatile facts in this brief:** the §43 number, catalog 49→50 (`eval.scores` + the `internal/history` importwall/registry-size pins it moves), the approvals ninth-kind fold, App.tsx/routes.ts/api.ts/fixture line positions, vitest counts.

---

## 1. Numbered requirements

### A — The backend: sessions, titling, exchange, turn routing, intake handoff

- **R1. Server-side per-user chat session registry, fail-closed** [S15.7 (S15:105); 15.6; S01.9]. Sessions and their messages are platform-owned server-side state, every record carrying its owner (15.6); reads and writes are owner-scoped server-side with the landed three-way discipline (operator sees per OQ1's disposition; a member exactly their own; unknown identity matches nothing — the `history.Scope` zero-value direction, history.go:93–109). Storage shape, route root, and package home per **OQ1** — reported as gap G.1, never silently resolved. Session CRUD is minimal: create, list (newest-first, bounded), rename (the human override of R2's title), delete/archive per OQ1; no client-side session state anywhere (scans.test.ts:105–111 already enforces it).
- **R2. First-message auto-titling as a local-tier duty** [S15.7; S12.4/S12.1]. A session's title derives from its first user message via ONE `local.Duty` call on the seat OQ2 dispositions — free-text drafting shape (`Classification:false`, length-capped, no schema fabrication), metered on a consuming run per the AdvisoryMeter precedent (C above). Degradation is honest and total: `ErrStackAbsent`/refusal/admissions-stopped ⇒ the session stays untitled with a deterministic fallback (e.g. the timestamp or a truncation of the first message — a MECHANICAL derivation, never a fake model output), and nothing blocks the turn. The title is data a human may edit (R1 rename); the duty output never overwrites a human-set title. Every leg driven against `FakeServer` — $0, zero network.
- **R3. Platform-state answers ride the landed ladder — and only it** [S15.7; S14.10; §37/§38]. The assistant's platform-state path calls the LANDED layers (B above) and re-implements nothing: no SQL anywhere in new transport code, no second guardrail, no confidence re-derivation, no fall-through to Layer 2 (its reach stays a deliberate act — OQ3 binds who acts). Answers, cards, and refusals-with-audit arrive as the store made them and are served/rendered VERBATIM: the query name always visible, `canned` answers named, **Layer-2 answers visibly carrying `lower-confidence`** (G3 D3.5 — the flag renders wherever the answer renders, never stripped, never restyled into invisibility), disambiguation cards rendered as pickable answers (choices are real catalog queries), audit blocks rendered when present.
- **R4. Turn routing per OQ3's disposition** [S15.7; A above]. The three verbs are the whole surface: a platform-state question routes to the ladder; an explicit task-start hands to intake (R5); anything else gets the honest refusal OQ3 dispositions — **never a free-generation fallback, never a silent guess between verbs**. Whatever routes (client or server per OQ3), the routing decision is visible in the turn's rendered outcome (which verb answered, which query, which layer).
- **R5. Start-a-task hands to S06 intake in place** [S15.7; S06.1/S06.5; D above]. The handoff calls the LANDED `IntakeSurface.Submit` (the one ingress — S16.6 Enqueue; never a second pipeline entry) with the requester's identity; the born task is announced in-feed with its id/title and deep link; the conversation then continues **in place** per OQ8's disposition (the intake interview cards surfaced in the chat flow or deep-linked — dispositioned, not invented). Handed files enter as task inputs through the OQ8 shape, recorded as requester-supplied inputs (S06.3/S06.6 letter). Cancel of the born task stays the landed cancel verb's business — the chat surface adds no lifecycle authority.
- **R6. The file exchange: upload, list, delete — owner-attributed, fail-closed** [S15.7; 15.6; S11]. Per-user exchange storage per OQ4's disposition: drag-drop + click-browse upload (widget side, R13), a list read, and delete — every object owner-attributed at write time and owner-scoped at read/delete (fail-closed on ownership: an object with no resolvable owner is unreadable, not orphan-shared). Transport hygiene: upload size/count bounds as structural constants with named reasons (the `maxIntakeBody` precedent — no ⚙ key exists and none may be invented); filenames sanitized and paths confined to the exchange home with **symlink-resolve-then-deny** (the P-T09-1 shape, S11.7) — a driven traversal probe (`../`, symlink, absolute path) must fail closed. Files reach RUNS only as intake inputs through R5's handoff — the exchange folder is control-plane territory; no sandbox ever mounts it wholesale (S11.3 allowlist-only).
- **R7. Produced-files chips from the exchange-folder diff around the turn** [S15.7; FC-v1 §3]. Post-turn chips are computed as a diff of the exchange manifest around the turn per OQ7's disposition (window, attribution of late arrivals, and the at-v0 producer set are OQ7); the diff is DERIVED from stored state — no ticker, no filesystem watcher (§32 no-tickers). If at v0 no platform producer writes into the folder, the mechanism still ships and is driven by test through the sanctioned write path — the chips render honestly empty otherwise (never fabricated).
- **R8. Every mutation lands on the event log; the inventory moves in lockstep** [S15.2 rule 3; E above]. Session/exchange/turn mutations mint their events per OQ1's disposition (new `chat.*` types enter `contract.go` with provenance, the registered count + any pinned tests moving in the same commit — the B6-3A `price.row_added` precedent); payloads are refs-not-blobs (message CONTENT lives in the store, not the event payload) and redact at every serving edge (store-raw/serve-redacted, §30). No new topic; the relay carries the types (E above).
- **R9. S15.2 contract rules hold on every new verb** [S15.2; §39]. No direct outward-effect verb (nothing in this family sends/publishes/pushes — file HANDOFF to intake is an internal state act; the born task's own outward effects stay D7-gated downstream); writes are retry-safe (a repeated delete/create/turn submit is idempotent or answers the already-resolved state — the landed `applied:false` shape); JSON error bodies via `*SurfaceError` with typed mapping, never message-text matching; unversioned additive-first paths; every read bounded (the history clamp precedent).
- **R10. Owner-scope + redaction + scans extended over the new shapes** [§30/§38/§42]. Three-way owner tests (operator/owner/other-member) on every new read AND mutation; `writeReadRedacted`-class serving on payload-bearing responses; the never-percent scan and money-scan cover the new response shapes; no new observable beyond register row 2 (S15.11 duty — nothing here phones anywhere).

### B — The widget: `/chat` live on the pick

- **R11. The route flip, exactly** [§41-B]. `owner:'chat'`→`''` + `'chat'` into `built` (F above); the pattern, title, and nav placement stay; deep links ride `hrefFor`; the stub component stops rendering for `/chat` and every OTHER stub stays (B6-8/B6-9 surfaces untouched).
- **R12. @assistant-ui/react on LocalRuntime/ExternalStoreRuntime with a Sinet adapter — state platform-owned, cloud never** [S15.4/S15.7; FC-v1 §3]. The runtime choice per OQ5's disposition; either way the adapter speaks ONLY to the platform API (R1's reads/verbs + the ladder + intake), message/session state round-trips through the platform (reload restores the transcript from served state — closing the Jarvis no-restore gap BY ownership, not by client persistence), and **assistant-cloud is never wired and never called**: no assistant-cloud import, no cloud config object, no network call the scripted-fetch double didn't script — the probe's zero-network discipline extended to the real surface (a driven whole-turn test with `scriptedFetch` proves every request is a scripted platform call; any unscripted request already throws, F above).
- **R13. Transcript + composer render escaped; the file sidebar is the pick's attachment surface** [S15.12; FC-v1 §3; §41-B]. All content renders as text (empty escape allowlist binds — no raw-HTML markdown path; OQ10 dispositions the markdown posture); the upload sidebar = drag-drop + click-browse driving R6's verbs, the uploaded list with delete, produced-files chips (R7) rendered post-turn; attachment UI uses the pick's own primitives where they fit (FC-v1 §3 names them) — no new npm dependency without OQ10's rail disposition.
- **R14. The stream-survives-navigation inventory, explicit and tested** [S15.7; FC-v1 §3]. An EXPLICIT inventory (in code, as the §41-B URL-contract shape: a table a test pins) of what a view switch may shed vs what MUST survive: **the in-flight turn, its stream/progress, and its produced-files accumulation survive navigation** (leave `/chat`, return, the turn is still going or its outcome is there — platform-owned state + OQ5's transport make survival = re-read/re-attach, and the tab's ONE EventSource never re-opens on navigation, F above); **hard-stop is a separate explicit user action** with OQ6's dispositioned server semantics — never a navigation side effect, never an unload handler. Driven both ways: navigate-away-and-back mid-turn keeps the turn; the stop verb ends it visibly.
- **R15. The error-humanization table** [S15.7; FC-v1 §3; §38]. One table, over the vocabulary that EXISTS (enumerated from landed classes, never invented): `Unreachable` vs `ApiError{status}` with the served `{error, detail}` (transport vs server, distinguished); **empty-stream vs transport-error distinguished** (a turn that yielded nothing vs a connection that failed — two different rendered facts); 429/overload-class statuses humanized to plain language WITH the served detail preserved; the six stream states already rendered by the shell never duplicated or contradicted; **ladder degradations render as the answers they are** (a disambiguation card is an answer, `unavailable` audit outcomes render their reason) — and the **model-fallback announcement rides the SERVED notes in-feed** (B above: the intent-model note; never a client-fabricated "switched model" line). The client NEVER re-classifies by message text (§38) — mapping keys on status/code/class only.
- **R16. Session UI** [S15.7]. Session list (titles from R2, honest untitled state), switch, create, rename, delete per OQ1's vocabulary; the active session is URL-addressable per OQ1 (deep-linkable — S15.11 stable-URL duty) and phone-complete (S1.10 names chat).
- **R17. Live wiring per the landed rules** [§41-B/§42]. Types-only subscription on the shared stream (never topics, never a second EventSource, never polling); REST is the truth and frames are the trigger (`useLive` or the same contract via the events API where the hook's shape doesn't fit chat — deviation recorded, not silent); resnapshot debt honored; stale renders visibly.
- **R18. Golden fixtures via REAL VERBS** [§42 + drain-r1 root cause]. Every new read the widget consumes gets its fixture produced by the Go assertor through the REAL handlers with rows minted by REAL verbs (a real session created, a real upload, a real turn, a real Submit — the B6-5 lesson: imagined payload keys pass their own tests and serve nothing); vitest doubles import the same files; fixed-clock; compare-only in CI.

### C — Cross-cutting

- **R19. Counters + untouched machinery** [§29–§43 precedent]. NO new npm dependency unless OQ10 disposes one through the rail (then: exact pin + lock entry + liveness verification in the same commit; otherwise `package.json`/`package-lock.json` byte-unchanged and lock stays **29**); NO new ⚙ key (**118/33**, `index.go` byte-unchanged — structural constants with named reasons instead, enumerated in §44); migrations 0001–0019 immutable (**0020 only per OQ1's disposition**); event-inventory movement only per R8; `internal/history` untouched (B6-6 owns its one sanctioned addition); `internal/intake`/`internal/local`/`internal/stage` semantics untouched except the OQ8-dispositioned additive submit shape; `internal/eventlog/contract.go` only per R8; BENCH-REG, `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.
- **R20. CONVENTIONS §44 appended** (confirm the free number at launch — §43 is B6-6's): the OQ dispositions as landed, the session/exchange data model, the turn-routing shape, the survives-navigation inventory, the error table, new structural constants with reasons, this packet's readings. Earlier sections never rewritten. Commit subjects per §5 (`P3-B6-7: … (S15.7, FC-v1 §3, S12.4, S14.10)` + trailer).
- **R21. Battery + hermetic + $0** [§3/§5/§41]: `gofmt -l internal/ cmd/ tools/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` · `go run ./tools/lockgate` · `cd web && npm run typecheck && npm run test && npm run build` — all green. Every duty leg on `FakeServer`; vitest offline (`scriptedFetch`/`FakeSource`; `VITE_SINET_ADDR` unset); NO test dials 8481/8482, any live unit, or the network. **$0.**

## 2. Seams — what belongs to LATER packets (leave named, never build)

| Later packet | Owns | B6-7 leaves |
|---|---|---|
| **B6-6 (concurrent)** | inbox + settings UI; the ninth approvals fold; `eval.scores` in `internal/history` | its files alone — same-path writes barred until it lands; the coordinator sequences |
| **B6-8** | review surfaces (`/deliverables/:id`), try-it dual-iframe — **the ONE raw-HTML channel; the escape allowlist widens THERE**; workforce map | those stubs; chat deep-links INTO deliverables/tasks, never renders diffs |
| **B6-9** | Declarative Web Push + re-nag SLA, PWA completion, the S15.12 sweep + real-device drill | the stable chat URLs push will target; real-drag/real-stream device verification |
| **B6 gate / bring-up** | Layer-2 open-SQL acceptance measurement (TBD-BRINGUP); benchmark readout; local-tier live wiring | the ladder consumed as landed; no live-stack test here |
| **v0.1 / 15.5** | multi-channel ingress (a bot is another API client at THIS seam); C3 web-reading | the API seam clean — nothing chat-specific that another client couldn't call |
| **Not this packet** | routes beyond OQ dispositions; any second SSE endpoint; STT/TTS/avatar/conversation mode (parked, S15.13); free-generation chat; engine sessions for chat turns | — |

## 3. ⚙ settings consumed (by exact dotted key; none invented)

- **None newly read.** S15.7 names NO ⚙ key and none exists for chat/session/exchange bounds (grep over the 118 — G above); the S15 table introduces only `frontend.dependency_pass_interval` (S15:158, consumed by B6-4's conformance row). Server-side keys already speaking through consumed surfaces: `obs.sse_keepalive` (the relay), `effects.approval_expiry`/`freshness.max_age` (inside intake cards the handoff surfaces). The titling duty resolves its seat through ⚙ `local.alias` **at call time** via the landed registry (C above) — a read through landed machinery, not a new consumption site. Every new bound (session list cap, message length, upload size/count, title length, diff-window) is a **structural constant with a named reason** (§35/§41/§42 precedent), enumerated in §44 — interim under the standing settings-tab directive.

## 4. Files expected to change

- **New (Go, homes per OQ1/OQ4 dispositions):** the chat-session/exchange store as a LEAF package over storage+eventlog (the §39 pattern — held directly in `api.Config`, nil ⇒ 503), its api transport file(s) + tests, migration **0020** if OQ1 disposes tables, `contract.go` additions per R8, shell composition (the AdvisoryMeter wiring for R2, the F-seam precedent), `apifixtures_test.go` growth (R18).
- **New/extended (web):** the Chat view + Sinet adapter module(s) + their vitest suites; `api.ts` chat family; `routes.ts` owner flip + `built` array; `web/src/fixtures/api/` chat fixtures; `index.css` additions; the survives-navigation inventory + error-table modules with their tests.
- **Bookkeeping:** `P3/CONVENTIONS.md` (§44).
- **Expected NOT to change:** `internal/history` (byte-level; B6-6 owns its sanctioned touch), `internal/local` (consumed), `internal/intake` semantics (the OQ8 additive shape is the sole sanctioned touch, in `stage/surface.go`'s submitBody + `intake.Request` per disposition), `internal/settings/index.go` (byte-unchanged), landed migrations 0001–0019, `events.ts`/`live.ts` semantics, `router.tsx`, route PATTERNS, `Login.tsx`, escape-scan mechanics + allowlist, `components.lock` (unless OQ10), `web/package.json`/`package-lock.json` (byte-unchanged unless OQ10), `go.mod`/`go.sum`, `.github/workflows/ci.yml`, `Spec/*`, `Docs/*`, `Research/*`, `P3/STATE.md`.

## 5. Adopted components touched

**@assistant-ui/react 0.14.27 (MIT) — installed + probe-proven at B6-4; THIS packet wires it.** FC-v1 §3 decision notes bind as constraints: **LocalRuntime/ExternalStoreRuntime only — assistant-cloud NEVER** (the checkable negative, two limbs: never WIRED — no import/config; never CALLED — zero unscripted network in driven turns; §41's recorded reading + the probe's fetch-spy discipline extended to the real surface); the **ExternalStoreRuntime boundary keeps message/session state platform-owned so a widget swap loses no data** — the state contract in R1 is what makes the pre-registered fallback ("owned chat primitives on the same SSE cursor") real; the carried Jarvis behaviors are **specs OVER the widget, never modifications OF it** (adopt-don't-fork: no patches, no `patch-package`, pin never bumped — 0.x churn is absorbed by the pin + quarterly pass; upstream 0.15.1 existed at B6-4 and the pin deliberately stands). **Re-verify liveness at the pin at execution** (registry dist-tags + repo alive + license unchanged — the B6-4/B6-5 discipline); a dead/relicensed/hostile finding = STOP + coordinator flag, never a silent substitute or bump. Attachment primitives are IN the pick (FC-v1 §3 names them for the file-exchange pattern); any package BEYOND the pick (markdown, attachment add-ons) is OQ10's rail decision, never assumed. No other adoption is touched.

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format + trailer, no push, never edit Spec/Docs/Research/STATE, never read `*-api-key.txt`.
- **§7/§9** — unversioned API consumed as-is; identity from `GET /api/auth/session` only; the dev identity never administers; 401 → login with rooted same-origin return-to.
- **§30/§38** — SSE frame contract + owner-scope/redaction are SERVER facts; served content renders as data; no percent/fraction/ETA; error mapping by status/code/type, never message text; store-raw/serve-redacted.
- **§39/§40** — verb semantics consumed verbatim (hash pins, applied:false, typed errors); leaf-package + held-directly Config seams; 404-before-403 where the family established it.
- **§41 + §41-B + §42 + §42-B ENTIRE** — npm exact-pin rail; URL contract fill-never-rename; one EventSource + types-only subscriptions + per-subscriber debt; `useLive`/events contract as the only feed access; golden-fixture tie via real verbs; escape-scan EMPTY allowlist; no web storage; phone-first structural responsiveness; `VITE_SINET_ADDR` hermetic posture; rendered honesty (absences with reasons, money as served, timestamps verbatim); structural constants with named reasons; TZ-pinned suite.
- **§43 (B6-6's, once landed)** — read at launch; its inbox/settings patterns are precedent, not targets.
- **The 35 D6-ratified readings (§29–§37)** — derive-from-log, no tickers, money read never computed, model output is untrusted input, refusal never repair, every refusal audited, honest absence over fabrication, stale never poses as live.
- **Hermetic + $0** — nothing dials the live Caddy admin chain (8481/8482) or any live unit; vitest offline; every duty leg fake-engine; no paid call, no push, no install.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. Session registry: create/list/rename/delete per OQ1's disposition, owner-scoped three ways (operator per disposition / owner / other-member — driven); fail-closed on absent ownership; sessions + messages platform-owned (no web-storage token anywhere — scan); the S15.2-gap resolution matches OQ1's disposition exactly (route root + data owner as dispositioned).
2. Auto-titling: first user message triggers ONE duty call on OQ2's seat (FakeServer captures alias/model/shape — free-text drafting, length-capped); the $0 D7 checkpoint lands on the dispositioned consuming run (asserted); `ErrStackAbsent` ⇒ deterministic untitled fallback, turn unblocked (driven); a human rename is never overwritten by the duty (driven); no `local.alias` key added (index.go byte-diff).
3. Ladder: the assistant path calls the LANDED routes/store only — no SQL, no guardrail re-implementation, no fall-through to open-sql (code read + a probe that an unresolved Layer-1 turn yields the card, never a Layer-2 call); Layer-2 reached only via the OQ3-dispositioned explicit act, its answer rendering the `lower-confidence` flag VISIBLY (driven from a served fixture; flag-absence probe fails).
4. Answer rendering: query name always visible; disambiguation cards render as pickable answers whose choices are served catalog entries (picking one runs it — driven); refusals render WITH their audit block at 200 (fixture with `outcome:refused`); notes (incl. the intent-model line) render in-feed verbatim.
5. Turn routing per OQ3: each verb driven end-to-end; a neither-verb turn gets the dispositioned honest refusal (driven; no free-generation path exists — grep for any un-dispositioned duty alias/model call in the new code).
6. Intake handoff: a start-a-task turn calls the LANDED Submit (userID = the authenticated caller; asserted); the born task announces in-feed with a working deep link; the in-place continuation matches OQ8's disposition (driven); handed files land as requester-supplied task inputs in the dispositioned shape (driven end-to-end: upload → hand → Submit → the input visible on the task's served artifacts/view).
7. Exchange: upload (both gestures wired to one verb), list, delete — owner-attributed at write, owner-scoped three ways, fail-closed (driven); size/count bounds enforced with named-reason constants; traversal probes (`../`, symlink, absolute) fail closed (driven — the resolve-then-deny shape); no sandbox-mount of the exchange home anywhere (grep).
8. Produced-files chips: the diff computed per OQ7's window from stored state (no ticker/watcher — grep); driven through the sanctioned producer path with files appearing/attributing per disposition; honest empty state otherwise.
9. Events: every new mutation minted per R8; `contract.go` counts + pinned tests moved in lockstep (the registered total matches the landed delta); payloads refs-not-blobs; redaction at every new serving edge probe-verified (plant-and-serve).
10. Retry-safety: repeated session-create/delete/turn-submit/upload answer idempotently or with the already-resolved state (driven); no outward-effect verb in the new family (route-table read).
11. Widget flip: `/chat` live, `built` array + owner flip exact, every other stub intact (shell test still walks them); deep-linkable session URLs per OQ1 round-trip through hrefFor/router.
12. Runtime: the dispositioned runtime mounts with the Sinet adapter; a whole driven turn round-trips against `scriptedFetch` with EVERY request scripted (zero unscripted — the double throws); **no assistant-cloud import/config anywhere in `web/src`** (scan); reload restores the transcript from served state (driven).
13. Survives-navigation: the inventory exists as a pinned artifact (test-asserted table); navigate-away-mid-turn + return keeps the in-flight turn AND its produced-files accumulation (driven); the tab's EventSource count stays 1 across the round trip (the `opened` counter); hard-stop is a rendered explicit action calling OQ6's dispositioned verb, and navigation calls nothing (driven negative).
14. Error table: one mapping module keyed on status/code/class (no message-text matching — grep); empty-stream vs transport driven as two distinct renders; 429-class humanized with served detail preserved; ladder degradations render as answers (driven); the fallback announcement renders from SERVED notes only (grep: no client-fabricated model-switch string).
15. Escape/side-truth sweep: allowlist EMPTY over the grown tree; no web storage; no money arithmetic; no percent/ETA; PIN/session discipline untouched; all content escaped (plant a hostile message fixture — renders as text).
16. Live wiring: types-only subscription (no topics field usage — grep), no second EventSource, no polling loop (schedule-spy where applicable); REST-truth-frames-trigger honored; stale visible.
17. Fixtures: every new read's fixture produced through REAL verbs in the Go assertor (read the seeding code — no raw-SQL imagined payloads), fixed-clock, compare-only; vitest doubles import the same files; `TestWebAPIFixtures` + stability green.
18. Counters hold (launch-restated values): lock 29 (or +OQ10's dispositioned entry), ⚙ 118/33 byte-unchanged, migrations 0001–0019 (+0020 only per OQ1), inventory moved exactly per R8, conformance 12, `package.json`/`package-lock.json` byte-unchanged (unless OQ10); CONVENTIONS §44 appended, earlier sections untouched; full battery green both sides; vitest offline; **$0**.
19. A/B seam (if stopped): part A landed alone leaves the tree green with `/chat` still the stub — the backend serves; nothing half-wired renders.
20. Negative sweep: `internal/history`/`internal/local` byte-unchanged; intake changes confined to the OQ8 shape; no new ⚙ read sites; no route outside the dispositioned family; `Spec/`/`Docs/`/`Research/`/`P3/STATE.md` untouched.

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — the session registry's storage + home (gap G.1).** The S15.2 table has no chat row; S15.7 demands a server-side per-user registry, fail-closed. Options: **(a) RECOMMENDED — durable tables via migration 0020** (`chat_sessions` + `chat_messages` + the exchange manifest if OQ4 co-locates it), owner column CHECK'd non-empty (the 0001 discipline), in a NEW leaf package (suggest `internal/assistant` or `internal/chat`) over storage+eventlog, held directly in `api.Config` (§39 pattern), route root `/api/chat` (additive-first) — messages are CONTENT like asks/memory/deliverables (all table-backed precedents: `asks.snapshot`, S09 stores), and a transcript must survive restart + serve reloads cheaply; events mint lifecycle facts (refs-not-blobs) per R8. (b) pure derive-from-log (`chat.*` events carry content; sessions derived) — honors §29's derive-from-log but puts message BODIES in event payloads (against refs-not-blobs) and makes reload-reads log scans. Dispose (a)/(b) + the package name + route root + the operator-visibility question (does the operator see members' chat sessions? The S01.9 see-all read precedent vs chat's privacy shape — the B6-3C conflict-visibility deviation is precedent that a deliberate narrower line is expressible; dispose explicitly) + the delete semantics (hard delete vs archived; messages immutable?).
- **OQ2 — the auto-titling seat + calibration posture (gap G.2).** No S12.4 row exists; the registry map is byte-frozen ⚙ data. Options: **(a) RECOMMENDED — ride `utility`** (S12:58: "summaries … drafting" serves; per-person per 1.10; "outputs are human-gated downstream; this seat never decides" — a title is an editable label, R2 keeps the human override); free-text drafting shape, NO margin gate (utility is uncalibrated by design — the honest-uncalibrated posture; no logprobs needed). (b) a new alias row — BARRED short of an S00.9 amendment + S18 sweep (the map default is registry data). Also dispose the metering run: (i) RECOMMENDED — a `BeginAdvisory`-shaped platform advisory run labeled for chat titling (the history-intent/contradiction-screen/run-summary precedent, C above); (ii) a per-session run concept (heavier; nothing else needs it).
- **OQ3 — ladder routing authority: who decides deterministic-vs-canned-vs-Layer-2, and what a neither-verb turn gets.** Grounded: the server never falls through (B); `Ask` never guesses; Layer 2 is an act; the landed B6-5 History panel is a client-routed precedent (registry-driven pickers + Ask + explicit open-sql). Options: **(a) RECOMMENDED — client routes**: the chat surface sends NL turns to `/api/events/ask` (the floor); registry-driven affordances (a view/query picker fed by the served registries) cover Layer 0/Layer-1-direct; **open-sql is reached ONLY by an explicit per-turn user act** (a rendered "escalate this question" control on a card/answer — preserving act-not-fallback), task-start per OQ8's trigger. (b) a server-side turn router (one new duty-classified verb deciding ladder-vs-intake-vs-refuse) — one more grammar-constrained classification (the intent-fill shape exists as precedent) but a NEW duty consumer with no S12.4 row (the OQ2 problem squared). Dispose (a)/(b) + the task-start trigger shape (explicit UI act vs classified intent — an explicit act needs no model and cannot misfire a task) + the neither-verb refusal text posture (a card-shaped honest refusal listing the three verbs, catalog choices attached).
- **OQ4 — the exchange folder's confinement home + manifest.** Nothing exists (gap G.3). Options for the object home: **(a) RECOMMENDED — a per-user subtree under the platform data root** (co-located with the storage home the shell already owns; never inside any repo/workspace lowerdir), with a **DB manifest as the truth** (owner, name, size, sha256, uploaded_ts, origin turn/session; the filesystem is storage, the manifest is the record — 15.6 on the ROW) and filesystem access only through the store's own confined path-builder (resolve-then-deny, R6). (b) manifest-less directory listing — bars owner attribution (a scan cannot say WHO) → fails 15.6 fail-closed; reject unless re-argued. Dispose (a) + the bounds (size/count constants + reasons) + retention posture (v0: keep until deleted; no sweeper — named for the gate list?).
- **OQ5 — the stream transport + the runtime choice (they interlock).** Grounded: ladder answers arrive WHOLE (request/response — no token stream exists anywhere in the platform; no engine session runs for chat, S12.1(b)); the tab has ONE EventSource, types-only; new event types ride the relay free (E); the adapter's `run` is an async generator (F). Options: **(a) RECOMMENDED — synchronous turn verbs + relay-driven currency**: a turn POST returns the turn's outcome (answer/card/handoff record) when ready; minted `chat.*` events make OTHER tabs/views and post-navigation re-reads current via the relay; the adapter resolves its generator from the POST (yielding once — an honest non-stream, matching what the backend truly does); "in-flight" = the POST is outstanding, server-side turn state makes it re-attachable (R14). (b) a per-turn SSE/chunked response stream — real streaming transport for answers that arrive whole (nothing to stream at v0), a SECOND stream shape beside the one-EventSource rule; reject unless a real need is argued. Runtime: **(i) RECOMMENDED — ExternalStoreRuntime** (the store IS platform state — messages/sessions served; the probe proved the package mounts and FC-v1 names both; the swap-loses-no-data boundary is exactly this); (ii) LocalRuntime with the adapter reading/writing platform state (probe-proven shape; state ownership then lives in the adapter's discipline rather than the runtime's contract). Dispose (a)/(b) + (i)/(ii).
- **OQ6 — hard-stop semantics.** "Hard-stop is a separate explicit user action" (S15.7) — but a v0 turn is a server-side act (a ladder read, a duty call, a Submit), not a run. Dispose what stop DOES: (a) RECOMMENDED — abandon-the-turn: the verb marks the turn abandoned (event minted; an in-flight duty call's context cancelled server-side where cancellable; the record survives — §37-C WithoutCancel precedent for the RECORD of the ending); a completed-anyway result renders late with its honest state; a handed-off task is NOT cancelled by chat stop (task cancel stays the landed verb — chat claims no lifecycle authority). (b) client-only forget — bars R14's "stop is an action" letter (nothing observable happened); reject.
- **OQ7 — the produced-files diff window + at-v0 producers.** The chips = "an exchange-folder diff around the turn" (S15.7). At v0, WHAT writes into a user's exchange folder? Grounded candidates: nothing does today; the natural v0 producer is the intake/deliverable side handing artifacts BACK (unbuilt; B6-8 territory) — or R6 uploads themselves (user-origin). Dispose: (a) RECOMMENDED — manifest-diff between turn-start and turn-settle (rows added during the window, origin-attributed; late arrivals attribute to the turn that CAUSED them via origin refs, else to no turn — honest), with the platform-side producer socket named-not-built if none lands in A; (b) defer chips entirely — bars the queue row's letter; reject unless the coordinator re-scopes.
- **OQ8 — the intake handoff shape (gap G.4).** `submitBody` is `{title, text}`. Dispose: **(a) RECOMMENDED — additive `inputs` on the submit body** (refs to exchange-manifest entries owned by the SAME requester — cross-owner refs refused; recorded on the intake record/SPEC as requester-supplied inputs per S06.3/S06.6; `intake.Request` gains the additive field; the walking-skeleton route keeps its path per S15.2 additive-first). (b) a post-submit attach verb (a second write to a born task's intake — resume/replan semantics complications). ALSO dispose the "in place" rendering: (i) RECOMMENDED — the chat feed renders the born task's OPEN intake card inline (served card snapshot, answered through the LANDED ask/approvals verbs from the chat surface — same verbs, same vocabulary, no new answer path) with a deep link out; (ii) link-only (hands the conversation OFF rather than continuing in place — weaker against S15.7's letter). Dispose (a)/(b) + (i)/(ii).
- **OQ9 — open-sql/ask wire semantics (the B6-1 N13 CARRY).** All seven history routes are GET with `?q=` — questions land in access-log/history lines and URL length bounds. Dispose: (a) RECOMMENDED — keep GET (landed, cached-never [no-store], loopback-only behind the front chain; the question is not a secret-class field; the audit row already stores it server-side) and record the reading; (b) add POST siblings for ask/open-sql (additive-first allows it; costs route duplication + fixture churn mid-phase). This packet consumes whichever shape is dispositioned; nothing else changes.
- **OQ10 — markdown + any dependency beyond the pick.** FC-v1 §3 lists Jarvis's "no markdown rendering" as a gap the ADOPTION answers, but markdown in assistant-ui rides a separate package (not in the tree — F); the empty escape allowlist bars any raw-HTML path regardless. Dispose: (a) RECOMMENDED — **plain-text transcript at v0** (answers are tables/cards/labels — the ladder's content is structured, not prose; escaped text renders it faithfully; zero new dependency, zero allowlist motion); (b) admit a markdown package through the S16.4 rail (exact pin + lock entry + license + funeral plan + escape-safe rendering proof — and the allowlist still never widens). Dispose; (b) only with named evidence plain text fails a real surface.

## 9. SCOPE VERDICT

**NOT one opus sitting — and the queue row PRE-AUTHORIZES the A/B stop; take it.** Part A is a full backend family (a storage decision + migration, session/exchange/turn verbs with three-way scoping, the titling duty leg, event minting with inventory lockstep, the intake-handoff shape, real-verb fixtures); part B is the heaviest widget of the phase (runtime + adapter, transcript, session UI, the survives-navigation inventory, the error table, the file sidebar + chips, intake-in-place rendering, the cloud-negative discipline). Assignment:

- **A — backend** (R1–R10 + their slice of C; OQ1, OQ2, OQ4, OQ7-server-half, OQ8, OQ9 land here): sessions/exchange/titling/turn-routing-server-side/intake handoff/events/fixtures.
- **B — widget** (R11–R18 + the rest of C; OQ3, OQ5, OQ6, OQ7-render-half, OQ10 land here): the `/chat` surface end to end.

Order **A → B** (B renders A's served shapes; A's fixtures are B's doubles). A landed alone is a success mode (rubric 19); same-sitting continuation is sanctioned if A lands green with budget. **Launch gate: the B6-6 executor must have LANDED (or the coordinator must explicitly sequence the overlap) before A touches `internal/api`/`contract.go`/`web/src` — same-path writers are barred by standing rule.**
