# P3-B6-4 brief — SPA scaffold: Vite+React 19+TS PWA tree + npm rail + four picks + embed + app shell

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.1, S15.3–S15.4, S15.12** owning (`Spec/drafts/S15-frontend-api.md`; S15.2/S15.5–S15.11 for context — their surfaces are B6-5..9's); **S01.4 + S01.5 + S01.9 + S01.11** (`S01-process-architecture-shell.md`); **S16.2–S16.4** (`S16-adoption-manifest.md`); **`Spec/frontend-components-v1.md` (FC-v1) ENTIRE** — the four binding picks, their pre-registered fallbacks, the carried behavior specs. `P3/CONVENTIONS.md` binds: **§1–§5, §7, §9, §29, §30, §32, §38–§40** (constraints extracted in §6 below). **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is packet 6/13 of B6** (queue `P3/STATE.md` §B6; B6-1/2A/2B/2C/3A are landed; B6-3B/3C land before this executor launches). It is where the **frontend tree first appears**: the `web/` Vite + React 19 + TypeScript scaffold resolving TBD-P3(SPA toolchain tree), the four FC-v1 picks installed at their binding versions, the npm-side adoption rail (the `modules` equivalent CONVENTIONS §4 defers to B6 — **defined by this packet**), the lockgate + CI extension, `go:embed` serving through the S01.4 front chain, and the **app shell**: router with stable URLs, the one-EventSource snapshot-then-tail client, login over S01.9, always-visible connection state, escape-by-default, responsive skeleton. It builds **NO surface beyond the skeleton**: mission control/board/task detail/fleet/filters are B6-5, inbox + settings UI B6-6, assistant B6-7, review/try-it/map B6-8, push/PWA-completion + the S15.12 sweep B6-9 — every surface page ships as a named STUB. Nothing landed is rebuilt: the API routes, SSE contract, auth stack and lockfile/lockgate machinery are consumed/extended, never re-implemented.

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit — S15/FC-v1 frontend scope is not S10/S11 security-dense; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-29 — do not relitigate)

**Live verification of the four picks (registry.npmjs.org metadata + api.github.com, fetched 2026-07-29; $0, no install):**

- **@hello-pangea/dnd 18.0.1** — published (2025-02-09), license Apache-2.0 unchanged, not deprecated; **latest is still 18.0.1** (no release in ~17 months) but the repo is ALIVE (pushed 2026-07-28, archived=false) — matches FC-v1's "active stewardship" ground; peers `react ^18.0.0 || ^19.0.0` ✓. Runtime deps: `@babel/runtime`, `css-box-model`, `raf-schd`, `react-redux ^9`, `redux ^5`.
- **react-diff-view 3.3.3** — published (2026-03-30), MIT unchanged, latest = pin; repo alive (pushed 2026-03-30 — quiet, the FC-v1 single-maintainer-slow risk, bounded by the pin). Runtime deps: **`gitdiff-parser ^0.3.1`** (MIT; latest 0.3.1 — FC-v1's "+ gitdiff-parser" arrives inside this closure), `classnames`, `diff-match-patch`, `lodash`, `shallow-equal`, `warning`.
- **@assistant-ui/react 0.14.27** — published (2026-07-16), MIT unchanged, not deprecated; **latest moved to 0.15.0 on 2026-07-28** — the pin 0.14.27 STANDS (FC-v1: pinned; 0.x churn absorbed by lockfile + quarterly pass; never silently bumped). Repo very active (pushed 2026-07-28). Peers `react ^18 || ^19` ✓. Runtime deps (~19) include `assistant-cloud ^0.1.35` (MIT), `assistant-stream`, `@assistant-ui/{core,store,tap}`, `zod ^4`, `zustand ^5`, `nanoid ^6`, `radix-ui` + `@radix-ui/*`, `react-textarea-autosize`. **Recorded reading: "assistant-cloud is never used" (FC-v1/S15.4) means never wired/called — a checkable negative at B6-7 — not absent from node_modules; it is a hard dependency of the package and will sit in the pinned tree.**
- **JSON Forms 3.8.0** (`@jsonforms/core` + `@jsonforms/react`) — both published (2026-06-16), MIT unchanged, latest = pin; repo alive (pushed 2026-07-27). `@jsonforms/react` peers `@jsonforms/core 3.8.0` (exact) + `react …^19.0.0` ✓. Core deps: `ajv ^8`, `ajv-formats`, `lodash`.
- **Verdict: no pick is dead, relicensed, or deprecated — no fallback activates.** (A dead/relicensed finding at EXECUTION re-verification = STOP + the FC-v1 pre-registered fallback as a flagged coordinator decision, never a silent substitute.)

**Toolchain reality (live, 2026-07-29):** React **19.2.8**; Vite **8.1.5** (major 8 since 2026-03-12 — ~4.5 months old, NOT day-one; Vite 8 is rolldown-based: deps `rolldown ~1.1.5` (MIT, Rust native binaries via optionalDependencies), `lightningcss` (**MPL-2.0**), `postcss`; engines `node ^20.19.0 || >=22.12.0`); TypeScript: `create-vite` main react-ts template pins **`~6.0.2`** (Apache-2.0, 2026-03-23) while `latest` is **7.0.2** (the native-compiler line, 2026-07-08 — 3 weeks old; the template itself does not day-one it); `@vitejs/plugin-react` **6.0.4**; published `create-vite` is **9.1.1** (2026-06-30); the react-ts template at vitejs/vite main (fetched 2026-07-29 — ahead of 9.1.1 by current patch pins) ships react/react-dom ^19.2.8, vite ^8.1.5, TS ~6.0.2, plugin-react ^6.0.4, @types/{node,react,react-dom}, **`oxlint ^1.75`** (a linter), scripts `dev` / `build: tsc -b && vite build` / `preview` — and **NO test harness** (the react-ts template ships none; the executor scaffolds with create-vite@latest at execution and pins what it gets, exact). **Vitest 4.1.10** (MIT) is the Vite-native harness (peers `vite ^6||^7||^8` ✓, node `^20||^22||>=24` ✓); DOM envs: jsdom 30.0.0 (MIT) / happy-dom 20.11.1 (MIT). react-router: v8.0.0 is 2026-06-17 (6 weeks old); the v7 line is still maintained (7.18.2, 2026-07-28). `vite-plugin-pwa` 1.3.0 (MIT) exists — **not needed** for Declarative Web Push (S15.11: no service-worker requirement). **FC-v1/S15 name NO test harness and NO router** — both are P3 choices through the rail (OQ7/OQ6).

**Host + code facts (read 2026-07-29):**

- Host node **v22.22.1** (`/usr/bin/node`) satisfies Vite 8's engines; npm **9.2.0** (package-lock `lockfileVersion: 3`). `web/` does not exist; `.gitignore` already carries `node_modules/`.
- **Serve path:** `internal/shell/shell.go:772` mounts `api.Server.Handler()` as the WHOLE `http.Server` handler; the api mux (api.go `Handler()`) has **no `GET /` route and serves no static asset anywhere** — the embed+serve layer is entirely new. Go 1.22 ServeMux precedence means a `GET /` fallback coexists with the longer `/api/...`/`/events` patterns without shadowing them.
- **Route surface the app shell consumes** (as of B6-1..3A landed + B6-3B in eval; B6-3B/3C routes exist by execution — the coordinator restates deltas at launch): pre-session `GET /api/health` (readiness + `event_head` cursor bootstrap), `GET /api/auth/session` (identity + device-hint prefill block), `GET /api/auth/users` (picker), `POST /api/auth/login`, `POST /api/auth/users` (bootstrap-window first-operator create); session-required `POST /api/auth/logout`, `GET /events` (the ONE SSE endpoint — §30 contract: `id:` = `event_seq`, `?topics=` with per-topic `snapshot` frames that SUPERSEDE `Last-Event-ID`, raw resume `Last-Event-ID` then `?after_seq`, keepalive comments). The full S15.2 families (`/api/runs|tasks|meters|approvals|benchmark|settings|deliverables|previews|memory` + `/api/events/*` query layers + `/api/auth/verify-pin` step-up) exist server-side for B6-5..8 — the SHELL consumes only health + auth + `/events`; stubs render nothing from the API.
- **Auth mechanics the shell must honor (§9):** cookie `sinet_session` HttpOnly (JS never reads it; identity comes from `GET /api/auth/session`), SameSite=Lax, Secure outside dev posture; 401 fail-closed on everything session-required including `/events`; while `users` is empty ONE anonymous create is allowed and must be an operator (the first-run path the operator click-through will hit).
- **Lockgate today** (`tools/lockgate` + `internal/lockfile`, read in full): validates the S16.2 fields, asserts every `go.mod` require covered by an entry's `modules`, the `go` directive matches the Go-toolchain pin, and every workflow `uses:` SHA-pinned + entry-covered. **It knows nothing of npm.** `internal/lockfile` rejects unknown JSON fields — a new entry field is a deliberate schema extension in that package. The real manifest is ALSO validated inside `go test` (`internal/lockfile`).
- **CI today** (`.github/workflows/ci.yml`): ONE job on `ubuntu-24.04` — checkout + setup-go (both SHA-pinned with lock entries) → gofmt → vet → build → test → lock-gate. No node anywhere.
- **`components.lock`: 21 entries.** The four frontend picks + the "React 19 + Vite + TypeScript tree" toolchain row are S16.3-bound but **not yet materialized** (§4: a row's entry materializes when first consumed — that is THIS packet). CI actions are `kind: toolchain`, pinned to 40-hex SHAs, runner label exact (`ubuntu-24.04`, never `-latest`).
- **⚙ `frontend.dependency_pass_interval`** is declared (`index.go:869`): TypeInt, Unit `days`, default 90, clamp (30, 365), help "co-scheduled with the components.lock review (S18.4 R7)". S18.4 R7: two deliberate sibling keys — this one = SPA dependency bumps; `adoption.dependency_pass_months` = the S16.7 manifest review. **No conformance row exists for either pass.** `internal/conformance` has the `setting:<dotted-key>:<unit>` cadence token (registry.go:53) and the `Fixture{Handle, Pkg, Run}` shape whose Pkg/Run must resolve to a REAL in-tree test (the verified-restore-drill row is the procedure-row precedent: procedure story in Handle, real validating test in Pkg/Run); dueness is passive, derived from stored state, no ticker (§32).
- **Host hazard (standing):** the production front chain is LIVE on this machine — Caddy admin on 127.0.0.1:8481, the production `sinet-control` unit on **127.0.0.1:8482, which is also `shell.DefaultHTTPAddr`**. No test — Go or vitest — may dial 8481/8482 or any live unit; the Vite dev-proxy target must be explicitly configurable (OQ5) because the dev default port is OWNED by production on this host.
- **Counters AS OF THIS WRITING** (B6-3B executor landed pending eval; B6-3C not started — the coordinator restates deltas at executor launch): migrations **0001–0019** (`user_version` 19); event inventory **91 minted / 5 declare-only / 96 registered**; ⚙ **118/33** (`index.go` byte-unchanged through all of B5+B6); `components.lock` **21**; CONVENTIONS **§1–§40** with §40-B in flight (B6-3C expected to append §40-C — this packet appends the next free top-level section, expected **§41**).

---

## 1. Numbered requirements

### A — The tree: `web/` scaffold + the four picks (S15.1; FC-v1; S16.3 toolchain row)

- **R1. `web/` — a plain Vite + React 19 + TypeScript SPA, no meta-framework, no RSC, no SSR** [S15.1; G3 D3.3]. The Vite scaffold shape (react-ts template) under `web/`: `package.json`, committed **`package-lock.json`** (lockfileVersion 3 — this file IS the resolved TBD-P3(SPA toolchain tree)), `vite.config.ts`, `tsconfig*.json`, `index.html`, `src/`. **Exact versions in `package.json` for every direct dependency** (no `^`/`~` — the §4 pin discipline applied to the npm side; the lockfile pins the transitive closure). Scripts: `dev`, `build` (typecheck + `vite build`), `typecheck`, `test`. Installs run **`npm ci`** wherever the lockfile exists (never bare `npm install` in CI) and the install posture is **`--ignore-scripts`** unless the executor verifies a script is load-bearing (then: which package, why — recorded; the S16.6 registry-supply-chain posture). The npm registry fetch at execution is the packet's one sanctioned network act ($0).
- **R2. The four FC-v1 picks installed at their binding versions, exactly** [S15.4; FC-v1; S16.3]: `@hello-pangea/dnd@18.0.1`, `react-diff-view@3.3.3`, `@assistant-ui/react@0.14.27`, `@jsonforms/core@3.8.0` + `@jsonforms/react@3.8.0` — as `dependencies` (runtime). **Live-verify at adoption** (S16.4 #3/#5, the execution-time re-check): registry status (published/deprecated/license at the exact pin) + a behavioral probe per pick in the test harness (R20). A dead/relicensed/deprecated finding = **STOP, report, pre-registered fallback as a flagged coordinator decision** (fallbacks: pinned @dnd-kit/core 6.3.1 / monaco diff via @monaco-editor/react 4.7.0 / owned chat primitives / RJSF v6) — never a silent substitute, never a silent version bump (0.15.0 exists for assistant-ui; the pin stays 0.14.27).
- **R3. Toolchain pins resolve to the grounded majors** [S15.1 riders; S16.3 row]: Vite **8.1.x** (the current major, 4.5 months old — first adoption of the current line is not a day-one major bump; the lag rider governs FUTURE majors from here), TypeScript **6.0.x** (what the template itself pins; **TS 7.0.x is NOT adopted** — a 3-week-old compiler-line replacement is the exact day-one adoption the discipline forbids; note it for a future pass), `@vitejs/plugin-react` 6.0.x, react/react-dom **19.2.x**, `@types/react`/`@types/react-dom`/`@types/node` per template. Exact patch versions = whatever the executor resolves at execution and pins (recorded in the lock entries). **Dropped from the template: `oxlint`** (a linter is a dependency; none is ratified — the §2 no-additional-linters posture applied to npm; OQ9 confirms). **No CSS framework, no state library, no data-fetching library** — owned CSS (plain/CSS-modules) and native fetch; anything more enters only through the rail.
- **R4. PWA posture per OQ6's disposition** [S15.1 "installed as a PWA"; S15.11]. Recommended shape: a hand-written `manifest.webmanifest` (name, start_url `/`, display standalone, theme/background colors, one placeholder icon set) linked from `index.html` — installability with ZERO dependencies; **no service worker, no vite-plugin-pwa** (Declarative Web Push needs no SW; offline-shell/final icons/iOS-path documentation are B6-9's, live-verified then).

### B — The adoption rail: lock entries + the npm `modules` equivalent + lockgate + CI (S16.2–S16.4; S01.11; §4)

- **R5. `components.lock` entries materialize** [S16.2; S16.3; §4]: the four `kind: frontend` rows at the binding pins (license `{spdx, scope, checked: <execution date>}` path-scoped at the pinned ref — the S16.3-recorded SPDX ids re-verified; funeral plans verbatim from S16.3; watch rows referencing the S16.8 adds); the **"React 19 + Vite + TypeScript tree"** `kind: toolchain` row resolved — pin = the tree identity (recommended: `<vite-version>+lock:sha256:<hash of package-lock.json>`, content-hash discipline for a tree pin; executor's exact form recorded), license = the tree license SET observed at the sweep (expect MIT/ISC/Apache-2.0/BSD + **MPL-2.0 via lightningcss** — the sops precedent; record set + date), notes recording **host Node v22.22.1 + npm 9.2.0** (dev/build runtime, dev-time only) and the react/TS/plugin majors; plus entries for the harness (OQ7) and any OQ6 router adoption. Every new entry: the S16.4 checklist walked, **operator ratification queued for the B6 gate** (checklist #10 — the gate's phase decision batch already names "S16.4 #10 ratifications for the SPA tree + any new deps").
- **R6. The npm-side `modules` equivalent — DEFINED HERE per OQ1's disposition** [§4: "an npm-side equivalent of `modules` is defined at B6, not before"]. Recommended: an additive `internal/lockfile` extension field (e.g. `npm_packages`) claiming package names from `web/package-lock.json`; each frontend entry claims its ROOT package(s); coverage of the transitive closure is computed, not enumerated (OQ2). The definition lands in `internal/lockfile` (schema + validation: no duplicate claims, claimed name must exist in the lockfile) and is recorded in CONVENTIONS §41.
- **R7. lockgate extension** [S01.11; §4; OQ2's disposition]. `tools/lockgate` + `internal/lockfile` gain an npm gate over `web/package-lock.json` (stdlib JSON — the §4 serialization ground repeats): **every installed package is covered or explicitly toolchain-scoped** (the ratified acceptance phrase) — recommended mechanics: non-dev packages must be reachable from a frontend entry's claimed roots through the lockfile's own dependency graph; `dev: true` packages fall to the toolchain-tree entry wholesale; anything unclaimed/unreachable FAILS with the offending package named. Plus lockstep pins: each frontend entry's lock pin == the lockfile's installed version for its root == `package.json`'s exact declaration; absence of `web/` fails (the manifest claims a tree that must exist). The gate stays runnable as `go run ./tools/lockgate` and keeps its `internal/lockfile` test-suite twin (§3: plain `go test ./...` trips on manifest breakage — extend the real-manifest validation to the npm side).
- **R8. CI job: typecheck + build + test for the frontend, per OQ3/OQ4's dispositions** [S01.11; §4/§5]. `ci.yml` gains node: **`actions/setup-node` SHA-pinned + lock entry** (a new CI-action adoption on the rail), `npm ci` in `web/`, `npm run typecheck`, `npm run test`, `npm run build` — ordered so the **Go build/test steps run AFTER the web build** (the embed integration is exercised on every CI run; OQ4). The release pipeline (on tag) stays a later packet — do not bolt release steps on (the ci.yml header comment binds).
- **R9. The frontend test harness per OQ7's disposition** [queue row: "frontend test harness"]. Recommended: **Vitest** at an exact pin (+ one DOM environment package for the pick probes) as devDependencies, config in `vite.config.ts`/`vitest.config.ts`, `npm run test` = `vitest run` (CI-shaped, no watch). Harness constituents are adoptions: lock entries + ratification items like everything else. No Testing Library at B6-4 (surfaces may propose it at B6-5+ if earned).

### C — `go:embed` + serving through the front chain (S01.4/S01.5; S15.1; §1/§7)

- **R10. The built SPA embeds into the one binary and serves through the S01.4 chain** [S01.5 "one static Go binary with the built SPA assets embedded"; S01.4 "Caddy's role is routing, not asset hosting"]. Embed home per OQ3's disposition (recommended: `internal/webui` with Vite `build.outDir` targeting its `dist/` — `web/` stays a pure npm tree and the §1 "everything beyond cmd/ and tools/ is internal/" letter holds). The api mux gains the static surface: hashed assets served with correct MIME + long-lived immutable cache headers; `index.html` served no-store (deploys take effect on reload); **SPA fallback** — a GET that matches no asset and no API route serves `index.html` (client-side routes are hard-navigable: S15.11 stable URLs/deep links/bookmarks) — but **`/api/*` and `/events` NEVER fall through to HTML** (an unknown API path stays a JSON-shaped 404; the fallback is for app routes only).
- **R11. Assets are pre-session surface** [S01.9 layer 1; §9]. The login page IS the SPA, so the asset+fallback routes serve without a session (reaching them already proves tailnet membership — exactly the §9 pre-session rationale; the API surface behind them stays fail-closed 401). State this in the §9-adjacent doc comment; the §9 pre-session route CLASS is extended, its five API members unchanged.
- **R12. Honest absence + the committed-tree posture per OQ3's disposition** [the honest-absence discipline; §3 "packets never commit red"]. `go build ./... && go test ./...` MUST stay green on a tree with no node build run (fresh clone, CI's Go-only consumers, the next packet's executor). Recommended: commit only a `.gitkeep` in the embed dir (built output git-ignored), embed `all:dist`, and serve a built-in minimal "UI not built into this binary" page when `index.html` is absent from the embedded FS — an honest absence, never a broken 404 shell. The with-assets serving leg asserts fully when assets exist (CI always exercises it per R8's ordering) and skips honestly when absent — **the skip-class sanction is OQ3's to extend** (§10's text currently admits exactly one sanctioned skip class).
- **R13. Dev posture = Vite dev proxy, dev-time only** [queue row; S16.3 row "dev-time only, no runtime service exposure"]. `vite.config.ts` proxies `/api` and `/events` (SSE-safe: no buffering/compression on the proxy leg) to a DEV control plane; target per OQ5's disposition (**never implicitly the live production unit** — 127.0.0.1:8482 is production's port on this host; the default must be overridable via env and documented). The dev server binds localhost only. **Nothing of Vite exists at runtime**: no vite/rolldown/plugin code is reachable from the binary; production serving is R10's embed alone.

### D — The app shell (S15.1; S15.3; S15.12; S01.9; §30)

- **R14. Router with stable URLs; every B6-5..9 surface a named stub** [S15.11 "Every approval card and task has a stable URL"; queue row]. Router per OQ6's disposition (recommended: owned, typed route table over the History API — small, no dependency; react-router 7.18.x exact-pinned is the sanctioned alternative). Routes are REAL paths from day one — they are the deep-link contract the S15.11 `navigate` field will target; suggested set (executor-final, recorded in §41 as the URL contract): `/` (mission control, B6-5), `/board` (B6-5), `/tasks/:id` (B6-5), `/fleet` (B6-5), `/inbox` + `/inbox/:id` (B6-6 — the push deep-link target), `/settings` (B6-6), `/chat` (B6-7), `/deliverables/:id` (B6-8), `/workforce` (B6-8), `/login`. Each stub page names its surface, its owning packet, and renders inside the shell chrome — honest "not built yet", never a blank or a fake.
- **R15. Login/session over S01.9 — the full §9 shape, nothing invented** [S01.9; §9]. Login page: user picker from `GET /api/auth/users`, device-hint prefill from the `GET /api/auth/session` hint block, PIN/password post to `POST /api/auth/login`; logout verb; **the bootstrap window** — when the picker is empty, the create-first-operator form (`POST /api/auth/users`; the first real-host click-through hits this). Identity state comes ONLY from `GET /api/auth/session` (the cookie is HttpOnly — the client never reads or stores credentials/tokens; no localStorage identity). Any 401 anywhere fail-closes to the login route (with return-to). PIN input is `type=password`, never logged, never echoed into state beyond the submit.
- **R16. ONE EventSource per tab + in-app fan-out + the snapshot-then-tail client** [S15.3; §30; G2 D2.1]. A typed client module (the shell's core deliverable): holds AT MOST ONE `EventSource` per tab (S15.3 — multi-tab rides HTTP/2 at the front door; no SharedWorker), fans frames out in-app by the frame's `topics` field + type; **resume** — native `Last-Event-ID` on auto-reconnect, explicit `?after_seq=<last seq>` on manual re-open, cursorless first connect tails from head (`/api/health`'s `event_head` is the REST-snapshot cursor bootstrap for views); **topic streams re-snapshot by contract** — on (re)connect with `?topics=`, `snapshot` frames arrive and REPLACE state (never merged into a stale copy; §30: the server supersedes `Last-Event-ID` on topic subscribes); **on cursor gap or eviction the client re-snapshots — never patches blind** [S15.3]: the machinery tracks `event_seq` continuity per subscription and surfaces an explicit "resnapshot required" signal consumers must obey (B6-5+ views wire their REST re-loads to it; at B6-4 the signal + its tests are the deliverable). Reconnect backoff is a small owned policy (structural constants with named reasons — the client-side analog of the settings-tab interim discipline).
- **R17. Connection state always visible** [S15.12; S15.3]. A shell-chrome indicator driven by the client machinery: live / connecting / catching-up (between snapshot and tail) / disconnected / logged-out / **unreachable** (fetch to `/api/health` failing — the honest tailnet-down/host-asleep state, S15.12). Stale never poses as live; the indicator is on every route including stubs and login.
- **R18. Escape-by-default** [S15.12; FC-v1 §2]. React's default JSX escaping is the mechanism; the DISCIPLINE is structural: **no `dangerouslySetInnerHTML`, no `innerHTML`/`outerHTML`/`document.write`, no `eval`/`new Function` anywhere in `web/src`** — asserted by a source-scan test in the harness **with its own planted probe** (the §38 scan pattern). The S15.8 preview iframe (B6-8) is the ONE sanctioned raw channel and does not exist yet; the scan's allowlist is EMPTY at B6-4.
- **R19. Responsive skeleton, phone-complete nav** [S1.10 via S15.12]. One workspace: the shell chrome + nav work at phone width (every stub reachable) and desktop width; owned CSS (no framework, R3); no fixed-width layout that scrolls horizontally on phone. This is skeleton responsiveness — surface-level phone-completeness (inbox usability etc.) is B6-5..9's, swept at B6-9.
- **R20. Pick smoke probes — the behavioral half of live-verified at adoption** [S16.4 #5]. One harness test per pick at the pinned version: @hello-pangea/dnd renders a minimal DragDropContext/Droppable/Draggable list; react-diff-view parses a real unified-diff fixture via gitdiff-parser and renders hunks; JSON Forms renders a small fixture schema+uischema (Categorization + Control — the S15.9 vocabulary shape the B6-3A emitter produces; fixture-fed, NOT API-fed — hermetic); @assistant-ui/react mounts a LocalRuntime provider + thread primitive with **no cloud config and no network** (the never-wired negative starts true and testable here). Probes prove the pinned versions actually work under React 19.2 + the harness DOM — import-compiles is not the bar.

### E — The scheduled dependency-pass obligation (S15.1 rider; S16.7; §32)

- **R21. Register the frontend dependency pass in the conformance registry, reading ⚙ `frontend.dependency_pass_interval`** [S15.1: "Scheduled dependency pass every ⚙ `frontend.dependency_pass_interval` = 90 d; SHOULD be co-scheduled with S16's manifest review"; §32]. A new seed row (e.g. `frontend-dependency-pass`) in `internal/conformance` `SeedRows()`: cadence `setting:frontend.dependency_pass_interval:days` (the ⚙ read live at evaluation — no ticker, dueness passive from stored state, restart/suspend-safe; §32 mechanics verbatim), owning section S15.1/S16.7, notes naming the co-scheduling with the S16.7 manifest pass and the pass content (upstream releases/advisories vs pins, license drift, archive/abandonment, peer-range/toolchain health — proposals only, never silent bumps), Fixture per the verified-restore-drill precedent: the pass procedure story in `Handle`, a REAL validating test in `Pkg`/`Run` (the lockfile npm-gate test is the natural instrument). Registration only — no runner, no executor, no alert wiring (the pass is an operator/coordinator act recorded via `RecordResult` when it runs; never-run raises nothing at v0, §32 OQ3). Seed-count/fixture-resolution tests move in lockstep.

### F — Cross-cutting (every part)

- **R22. Hermetic + $0 beyond the sanctioned npm fetch** [§25 hazard; §3]: NO test — Go or vitest — dials 127.0.0.1:8481/8482, the live front chain, any live unit, or the npm registry; vitest tests run offline post-install (fake EventSource/fetch — scripted doubles, the packet adds no network test); Go tests stay tier-F. Nothing installs globally, nothing touches the host beyond `web/node_modules` inside the repo (git-ignored).
- **R23. Counters + untouched machinery** [§29–§40 precedent]: NO new ⚙ key (`index.go` byte-unchanged, **118/33** — `frontend.dependency_pass_interval` is consumed, not created); NO new Go module (`go.mod`/`go.sum` unchanged — the embed/serve/lockgate work is stdlib); NO migration (0001–0019 byte-untouched at this writing; restate at launch); NO new event type (inventory 91/5/96 at this writing — registering a conformance row mints nothing; `RecordResult` on a future pass rides the landed `eval.score_recorded`); `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.
- **R24. CONVENTIONS §41 appended** (expected number — confirm the next free section at launch): the repo-layout extension (`web/` + the embed home), the npm rail definition (OQ1/OQ2 as dispositioned), the URL contract (R14's chosen paths), the frontend conventions (exact-pin package.json, `npm ci`, ignore-scripts posture, harness, escape-scan, no-CSS-framework), the dev-proxy posture, and this packet's readings. Earlier sections never rewritten. Commit subjects per §5 (`P3-B6-4: … (S15.1, S15.3–S15.4, FC-v1, S16.2–S16.4, S01.4–S01.5, S01.11)` + trailer).
- **R25. Battery** [§3/§5 + this packet's extension]: `gofmt -l internal/ cmd/ tools/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` · `go run ./tools/lockgate` · **`cd web && npm run typecheck && npm run test && npm run build`** — all green, run by the executor; CI runs the same set per R8. A red anywhere is never committed.

## 2. Seams — what belongs to LATER packets (leave named, never build)

| Later packet | Owns | B6-4 leaves |
|---|---|---|
| **B6-5** | mission control, board (dnd wiring, card faces, drag→hint verb), task detail, fleet, filters | stub routes; the SSE client + REST-snapshot signal they consume; @hello-pangea/dnd installed, probe-only |
| **B6-6** | approval inbox UI (tiers, step-up prompts, verdict forms), settings UI (JSON Forms renderer SET — Sinet-owned renderers are B6-6 work), ninth inbox kind | stub routes; JSON Forms installed, probe-only; `/api/auth/verify-pin` unconsumed |
| **B6-7** | assistant (LocalRuntime/ExternalStoreRuntime adapter, sessions, file exchange, error-humanization, stream-survives-navigation) | stub route; @assistant-ui/react installed, probe-only, cloud never wired |
| **B6-8** | review surfaces (react-diff-view wiring, anchors, synthetic strip), try-it dual-iframe (**the ONE raw-HTML channel — the R18 scan allowlist widens THERE, not here**), accept UI, workforce map + its TBD-P3 | stub routes; react-diff-view installed, probe-only |
| **B6-9** | Declarative Web Push, PWA completion (final icons, iOS ≥18.4 path, install docs, offline/unreachable polish, any SW decision), re-nag SLA, S15.12 completeness sweep, push-drill runbook | R4's minimal manifest; the honest unreachable state at shell level (R17) |
| **Not this packet** | any change to `internal/api` route semantics, SSE server contract, `internal/auth`, `internal/settings`, migrations, `internal/history`, the four picks' code (adopt-don't-fork: `node_modules` is never patched, no `patch-package`, no vendored forks) | — |

## 3. ⚙ settings consumed (by exact dotted key; none invented)

- **`frontend.dependency_pass_interval`** — consumed by the R21 conformance row's cadence token (`setting:frontend.dependency_pass_interval:days`), read live at dueness evaluation. The first ⚙ consumption of this key (declared-unconsumed until now).
- **Referenced-not-consumed:** `adoption.dependency_pass_months` (the S16.7 sibling — named in the row's notes, not read), `obs.sse_keepalive` (server-side, landed). Client-side numbers (reconnect backoff, etc.) are structural constants with named reasons — the browser cannot read the registry pre-login; interim under the standing settings-tab directive.

## 4. Files expected to change

- **New:** `web/` (the whole npm tree: package.json, package-lock.json, vite/tsconfig configs, index.html, manifest, `src/` shell + stubs + client machinery + tests); the embed home (recommended `internal/webui/`: embed.go + serve handler + tests + `dist/.gitkeep`); `.gitignore` additions (built dist, vite caches).
- **Extended:** `internal/lockfile/` (npm-side schema field + gate + tests over the real manifest+lockfile); `tools/lockgate/main.go` (wire the npm gate); `.github/workflows/ci.yml` (setup-node + web steps + ordering); `components.lock` (R5 entries); `internal/api/` (the static/fallback serve surface — routes only, no landed handler touched) or `internal/shell` per OQ3's home; `internal/conformance/registry.go` (+ its seed-count/fixture tests) for R21; `P3/CONVENTIONS.md` (§41).
- **Expected NOT to change:** `internal/settings/index.go` (byte-unchanged), migrations, `internal/eventlog/contract.go`, `internal/auth`, `internal/history`, `internal/redact`, `go.mod`/`go.sum`, `Spec/*`, `Docs/*`, `Research/*`, `P3/STATE.md`, the landed api handler files' semantics (additive route registration only).

## 5. Adopted components touched (every npm adoption rides the S16.4 rail; ratification queued for the B6 gate)

| Adoption | Version (grounded 2026-07-29; executor pins exact at execution) | License | Rail status |
|---|---|---|---|
| @hello-pangea/dnd | **18.0.1** (binding) | Apache-2.0 | S16.3-bound; entry materializes now |
| react-diff-view (+ gitdiff-parser in closure) | **3.3.3** (binding; gitdiff-parser 0.3.1 transitive) | MIT | S16.3-bound; entry materializes now |
| @assistant-ui/react | **0.14.27** (binding; latest is 0.15.0 — pin stands) | MIT | S16.3-bound; entry materializes now |
| JSON Forms (@jsonforms/core + @jsonforms/react) | **3.8.0** (binding) | MIT | S16.3-bound; entry materializes now |
| React 19 + Vite + TS tree (toolchain) | react 19.2.x · vite 8.1.x · TS 6.0.x · plugin-react 6.0.x · @types/* (TBD-P3 **resolves here**; lockfile = the tree) | tree SET incl. MPL-2.0 (lightningcss); recorded at sweep | S16.3-bound; entry materializes now; node v22.22.1 + npm 9.2.0 recorded in notes |
| Vitest (+ one DOM env pkg) | 4.1.x (+ jsdom 30.x or happy-dom 20.x) | MIT | **NEW** — OQ7; S16.4 walk + entry + #10 item |
| actions/setup-node | current stable, SHA-pinned at execution | MIT | **NEW** — CI-action toolchain entry (§4); #10 item |
| Router (only if OQ6 = adopt) | react-router 7.18.x exact | MIT | **NEW** — #10 item; NOT v8 (6 weeks old) |

Adopt-don't-fork binds: exact pins, no `node_modules` patching, no forks, no `patch-package`. NOT adopted (named so no future session mistakes them): TS 7.0.x (3 weeks old), react-router 8 (6 weeks old), vite-plugin-pwa (no SW need at v0), oxlint (linter unratified), Tailwind/any CSS framework, Testing Library (unearned at scaffold), AG-UI runtime (PATTERN-only, settled).

## 6. CONVENTIONS constraints binding this packet (by section)

- **§1** — one binary; the release artifact embeds the SPA (`go:embed` at B6 — this packet); `tools/` never ships in the artifact; layout extension recorded in §41, not silently.
- **§2/§3** — stdlib-first Go (the serve/embed/lockgate work adds NO Go module); stdlib `testing` only Go-side; tests never write outside `t.TempDir()`; green precondition for every commit.
- **§4** — the lock discipline verbatim: strict JSON, unknown fields rejected (a new field is a deliberate `internal/lockfile` schema extension), exact pins (`^ ~ > < =` rejected), CI actions SHA-pinned + entry-covered, runner labels exact, removal only by funeral plan. **This packet DEFINES the npm-side `modules` equivalent** — the definition itself is an OQ disposition, then §41 text.
- **§5** — staged commits (never `git add -A`), subject format + trailer, no push, never edit `Spec/`/`Docs/`/`Research/`/`P3/STATE.md`, never read `*-api-key.txt`.
- **§7/§9** — unversioned API; pre-session surface extension per R11; fail-closed 401 elsewhere; dev posture = absence of systemd env (the Go side is landed; the web dev posture rides the Vite proxy, OQ5).
- **§29** — no new event type without same-packet contract mechanics (none expected here).
- **§30/§38** — the SSE frame contract is LANDED (`id:` = seq, topic snapshots, `?after_seq`): the client CONSUMES it verbatim, never renegotiates it; owner-scope/redaction are server-side facts the client renders, never re-implements.
- **§32** — conformance registration mechanics for R21: seed idempotent, fixtures resolve to real tests, dueness passive, no ticker.
- **§38–§40 / the 35 D6-ratified readings** — binding precedent: derive-from-log (client state derives from frames + REST reads, no client-side side-truths that outlive a re-snapshot); no tickers (dueness from stored state — R21); escape-by-default (R18); owner-scope server-side (the client filters nothing to CREATE privacy — presentation only, S15.2); honest absence over fabrication (R12 placeholder, R14 stubs, R17 states); structural constants with named reasons interim under the settings-tab directive; model output is untrusted input (nothing in the shell renders unescaped content).
- **Hermetic + $0** — R22; the standing Caddy/live-unit hazard extends to vitest and the dev proxy's defaults.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. `web/` exists: Vite 8.1.x + React 19.2.x + TS 6.0.x react-ts shape; `package.json` direct deps ALL exact-pinned (no range specifiers — grep); `package-lock.json` committed, lockfileVersion 3; scripts dev/build/typecheck/test present; no oxlint, no CSS framework, no meta-framework/RSC/SSR anywhere.
2. The four picks in `dependencies` at EXACTLY 18.0.1 / 3.3.3 / 0.14.27 / 3.8.0+3.8.0; execution-time registry re-verification recorded (dates + status in the lock entries' license.checked/notes); no silent bump (0.14.27 NOT 0.15.0), no substitute.
3. `components.lock`: the R5 entries present and S16.2-complete (nine fields; path-scoped licenses with dates; funeral plans = the FC-v1 fallbacks verbatim; watch refs; toolchain row resolves TBD-P3 with the tree pin + license set incl. MPL-2.0 + node/npm noted); every new entry's S16.4 walk written; ratification items listed for the gate. Lock count moves 21 → 21+N with N named.
4. The npm `modules` equivalent exists per OQ1's disposition: schema field validated in `internal/lockfile` (duplicate claims rejected; claims must exist in the lockfile), definition recorded in §41.
5. lockgate npm gate per OQ2: `go run ./tools/lockgate` fails on (a) an installed non-dev package no entry covers [probe: add a fake package to a lockfile copy], (b) a pin↔lockfile↔package.json mismatch [probe], (c) missing `web/` when entries claim it; passes on the real tree; the `internal/lockfile` test twin covers the same over the REAL files so `go test ./...` trips on npm-manifest breakage.
6. CI: `ci.yml` has setup-node SHA-pinned + entry-covered (lockgate green proves it); web steps run `npm ci` (not install) + typecheck + test + build; Go build/test AFTER web build (order visible in the file); runner label still exact; no release steps.
7. Embed + serve: with assets built, `GET /` serves the SPA index (no-store), hashed assets serve with immutable cache + correct MIME, an app route (e.g. `/inbox/x`) serves index.html (SPA fallback), and `/api/nonexistent` + `/events`-adjacent paths stay JSON-shaped 404/handler — NEVER html (test both directions); with assets absent (fresh tree), `go test ./...` still green and the serve path yields the honest placeholder (asserted); the with-assets leg's skip/branch mechanics match OQ3's disposition.
8. Assets are pre-session (no 401 on `GET /`/assets/fallback — test), while `/events` and the session-required API remain 401 fail-closed (unchanged tests still green).
9. Dev proxy: `vite.config.ts` proxies exactly `/api` + `/events`; target per OQ5 (overridable; the config never forces the live production port silently); dev server localhost-only; NO vite/rolldown import reachable from any Go file (trivially true — assert no Go change references it) and the binary serves without node present.
10. App shell routes: every B6-5..9 surface has a stub at a stable path; the chosen URL set recorded in §41; deep-linkable (hard navigation to each stub serves the shell via R10); stubs name surface + owning packet; `/login` + bootstrap-create flow works against a real dev control plane shape (Go-side httptest or the real api handler in-process — hermetic).
11. Login: picker from `/api/auth/users`; hint prefill consumed from the session read's hint block; PIN posts to login; logout works; empty-users → create-first-operator form posting `POST /api/auth/users`; identity ONLY from `GET /api/auth/session`; no cookie/token read from JS, no localStorage identity (scan + test); 401 → login redirect with return-to.
12. SSE client (vitest, scripted fake EventSource — offline): exactly ONE EventSource per tab enforced (second subscriber joins the fan-out, no second connection — test); fan-out routes by `topics` field; auto-reconnect relies on native Last-Event-ID; manual re-open carries `?after_seq=<last>`; topic (re)connect REPLACES state on `snapshot` frames (never merges — test with a poisoned stale copy); seq-gap surfaces the "re-snapshot required" signal and the machinery NEVER patches blind past a gap (test); backoff constants carry named reasons.
13. Connection state: the indicator renders all states (live/connecting/catching-up/disconnected/logged-out/unreachable) from the client machinery's state — driven tests flip through them; present in the shell chrome on every route.
14. Escape scan: zero `dangerouslySetInnerHTML`/`innerHTML`/`outerHTML`/`document.write`/`eval`/`new Function` in `web/src` — scan test with planted probe proving it can fail; allowlist empty.
15. Pick probes (R20) pass under the harness: dnd renders, diff parses+renders a real unified fixture, JSON Forms renders Categorization+Control fixture, assistant-ui mounts LocalRuntime with no network (fetch spy asserts zero calls).
16. Responsive: phone-width nav reaches every stub (viewport-driven test or a checkable CSS structure — no horizontal scroll at 375px on the shell); desktop intact.
17. R21 row: seeded idempotently, cadence token `setting:frontend.dependency_pass_interval:days` resolves through the landed setting-cadence path (test: dueness moves when the ⚙ value changes), fixtures resolve to a real test, notes name S16.7 co-scheduling; seed-count + fixture-resolution tests updated; no ticker anywhere (grep the new code for time.Ticker).
18. Counters: ⚙ 118/33 (`index.go` byte-unchanged); `go.mod`/`go.sum` unchanged; migrations byte-untouched at the launch-restated numbers; event inventory unchanged; CONVENTIONS §41 appended, §1–§40(+B/C) untouched; `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.
19. Hermetic sweep: no test (Go or vitest) dials 8481/8482/live units/network (grep + the vitest config runs offline post-install); `web/node_modules` and built dist git-ignored; nothing global installed.
20. **Full battery green** (R25 — Go battery + lockgate + `npm run typecheck && npm run test && npm run build`) and **$0 beyond the sanctioned registry fetch**.

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — the npm-side `modules` equivalent (the §4 definition this packet owes).** RECOMMENDED: additive `internal/lockfile` field `npm_packages` — each frontend entry claims its ROOT package name(s) (e.g. `@assistant-ui/react`); transitive coverage is computed by lockgate from the lockfile's own graph (OQ2), NOT enumerated per-entry. Alternatives: (b) full-closure enumeration per entry (the Go `modules` letter — but the four picks' closures are hundreds of names of regenerated review noise); (c) claim-direct-only with no closure check (weaker: an uncovered transitive addition slips in). Dispose (a)/(b)/(c).
- **OQ2 — the lockgate extension shape.** RECOMMENDED: parse `web/package-lock.json` (v3 `packages` map; stdlib JSON), classify `dev: true` packages as **toolchain-scoped** (covered wholesale by the "React 19 + Vite + TypeScript tree" entry — the acceptance phrase's "explicitly toolchain-scoped" made mechanical), require every non-dev package REACHABLE from a claimed root through the lockfile graph, fail otherwise naming the package; plus the R7 pin-lockstep checks. `optional`/platform-binary packages follow their parent's classification. Alternative: name-set coverage without graph walk (then every transitive name must be claimed — collapses into OQ1(b)). Dispose the mechanics.
- **OQ3 — embed home + absence posture + the skip-class sanction.** RECOMMENDED: (i) embed lives in `internal/webui` (package + `//go:embed all:dist`; Vite `build.outDir` → `../internal/webui/dist`, `emptyOutDir`), keeping `web/` Go-free and §1's letter intact; (ii) committed content = `dist/.gitkeep` only, built output git-ignored, runtime serves the honest built-in placeholder when `index.html` is absent (R12); (iii) the with-assets serve test auto-runs when assets exist and prints a sanctioned-skip line otherwise — **this extends §10's "one skip class" text with a second, web-assets analog of tier-R** — ratify the extension (recorded in §41) or direct an env-gated branch instead. Alternatives: (a) `web/embed.go` package at repo root (simpler path, needs the §1 layout amendment stated); (b) committing built assets (rejected-recommended: generated artifacts in review, drift risk). Dispose (i)/(ii)/(iii).
- **OQ4 — CI placement + build ordering.** RECOMMENDED: ONE job, sequenced setup-go+setup-node → web ci/typecheck/test/build → Go vet/build/test (embeds the real dist every CI run — the embed integration is continuously exercised) → lockgate. Alternative: a second parallel `web` job (faster wall-clock, but the Go job then tests the placeholder path only, and artifact handoff for the embed costs more than it saves at this repo's size). Dispose; also confirm setup-node's cache option usage (built-in, no extra action) or none.
- **OQ5 — the dev-proxy posture.** The Vite proxy target: `shell.DefaultHTTPAddr` (127.0.0.1:8482) is the natural dev default **but is the LIVE production unit's port on this host**. RECOMMENDED: target read from env (`VITE_SINET_ADDR` or similar) with the DOCUMENTED default `http://127.0.0.1:8482` and a §41-recorded warning that on hosts running the production unit the developer MUST override (dev control plane on a free port via `--http-addr`); vite dev binds localhost only; no test ever runs the dev server. Alternative: no default (env required — safer, one more step every dev session). Dispose default-with-warning vs required-env.
- **OQ6 — the router.** FC-v1/S15 bind NO router pick; the S15 needs are pathname routes + deep links + bookmarks (no loaders/nested-data machinery). RECOMMENDED: **owned typed route table over the History API** (small, zero-dependency, no major-version churn; consistent with build-the-control-plane/adopt-the-organs — a router this size is not an organ). Alternative: adopt react-router **7.18.x exact** through the rail (industry standard, but v7→v8 majors 13 months apart is live churn, and v8 is 6 weeks old — day-one-barred either way). Dispose owned vs adopted-v7.
- **OQ7 — the test harness constituents.** RECOMMENDED: Vitest 4.1.x exact + ONE DOM env (jsdom 30.x — broadest compatibility for the radix-based assistant-ui probe; happy-dom if the executor verifies the probes pass — behavioral verification decides, recorded) as devDependencies with lock entries; NO @testing-library at B6-4 (probes use react-dom/client + `act`); `npm run test` = `vitest run`. Alternative: node:test + no DOM (rejected-recommended: R20's render probes need a DOM, and the harness must serve B6-5..9 component tests). Dispose harness + DOM-env choice authority.
- **OQ8 — PWA manifest scope at B6-4 vs B6-9.** RECOMMENDED: R4's minimal manifest + placeholder icon NOW (the row says "PWA scaffold"; installability is scaffold-shaped and dependency-free), everything else (final icons, iOS ≥18.4 documentation, push, any SW/offline decision) B6-9 with live verification then. Alternative: no manifest until B6-9 (then "PWA scaffold" reduces to "SPA scaffold" — say so if intended).
- **OQ9 — scaffold deviations from the template, confirmed.** The template ships `oxlint` (dropped per R3 — no linter is ratified; the §2 posture) and `tsc -b && vite build` as `build` (kept — typecheck-before-build IS the CI typecheck leg; R1 also exposes a bare `typecheck` script). Confirm both, and confirm the node/npm host-pin recording home (recommended: the toolchain entry's notes, since the platform cannot pin the host's node — the OS-mechanism-row precedent).

## 9. SCOPE VERDICT

**NOT confidently one opus sitting.** Two ecosystems (Go embed/serve/lockgate/CI/conformance + a from-zero TS/React tree with its own harness), a toolchain bring-up (npm install reality, template deviations), and the app-shell client machinery with real test depth. The B6-2/B6-3 precedent applies — RECOMMENDED pre-authorized split at the rail/shell seam (one brief serves both; a stop at the seam is a success mode):

- **A — tree + rail + embed** (R1–R13, R21 + their slice of F; OQ1–OQ5, OQ8–OQ9): `web/` scaffolded with the four picks pinned, lock entries + npm gate + lockgate extension, CI job, embed+serve with the placeholder posture, conformance row. Lands with the battery green and CI exercising the real embed — the platform serves its own (stub) UI through the front chain.
- **B — app shell** (R14–R20 + the rest of F; OQ6–OQ7): router + stubs + login + SSE client + connection state + escape scan + responsive skeleton + pick probes.

Order A → B (B needs A's tree and harness). If the executor reaches the seam with budget and full green, continuing into B in the same sitting is sanctioned; stopping at A-landed is equally a success.
