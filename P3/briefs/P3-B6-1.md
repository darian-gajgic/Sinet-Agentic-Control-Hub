# P3-B6-1 brief — S15.2 read-side API completion + S14 query layers routed + stage-event production

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.2–S15.3, S15.5** owning (`Spec/drafts/S15-frontend-api.md`); **S14.2 + S14.10** (`Spec/drafts/S14-observability-evals-watch.md`); **S02.3** run FSM (`Spec/drafts/S02-durable-state-recovery.md`); **S06.6** SPEC/PLAN artifact contract (`Spec/drafts/S06-intake-pipeline.md`); **S10.1/S10.4/S10.10** meter semantics (`Spec/drafts/S10-metering-scheduling.md`); **S01.9** identity/AuthZ (`Spec/drafts/S01-process-architecture-shell.md`). `P3/CONVENTIONS.md` binds: **§5, §7, §9, §11, §29, §30, §36, §37** (constraints extracted in §6 below). **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is the FIRST packet of phase B6** (queue cut 2026-07-28, `P3/STATE.md` §B6). It completes the **read side** of the S15.2 REST resource families over machinery that ALL already exists, routes the landed B5-8B History store as `/api/events`, and flips the two `stage.*` declare-only event types to minted at real stage boundaries. It is **NOT** the mutation/decision plane (approvals, cancel, budget edits, board-drag hints — B6-2), **NOT** the content families (settings/deliverables/preview/memory — B6-3), **NOT** the SPA or any rendering (B6-4+), **NOT** a rebuild of anything B5 landed — the History guardrails, the SSE surface, the projections, and the redaction primitive are consumed, never re-implemented.

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit — S15 read-plumbing is not S10/S11 security-dense (HANDOFF §5 calibration note); lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-28 — do not relitigate)

- **The API surface today** (`internal/api/api.go`, read in full): pre-session surface (health + auth), protected `/events` SSE + `/api/auth/*`, and the B2-4 walking-skeleton routes — `POST /api/intake/requests`, `GET /api/tasks/{task}`, `POST /api/tasks/{task}/advance`, `POST /api/asks/{ask}/answer`, `GET /api/runs/{run}/receipt` — explicitly **"PROVISIONAL pending the S15.2 endpoint families (B6)"** (intake_handlers.go:14). `api.Config` already HOLDS unrouted: `History *history.Store` ("wired but NOT yet routed — the transport is B6's", api.go:124), `Accept`, `FollowUp`, `Preview` (those three are B6-2/B6-3's, NOT this packet's). `Config.DB` + `Config.Meter` (the B5-2 `MeterReader`: `RunMeter(runID)`, `LaneMeter(userID, lane)`) are wired from the shell (shell.go:650–665).
- **The walking-skeleton READS are NOT owner-scoped.** `Surface.Task` (internal/stage/surface.go:176) and `Surface.Receipt` (surface.go:203) return any task/receipt to any authenticated identity; only `Advance`/`Answer` check ownership. This is a pre-multi-owner placeholder exactly like the B0-3 unscoped SSE relay was (§30 OQ1 called that out and fixed the stream side at B5-2). The read side is B6-1's to close.
- **Owner-scope machinery is landed**: `ownerScope{UserID, Operator}` (projection.go:35), `isOperatorRead` (sse.go:264 — one role bit via `auth.Store.User`; dev identity operator-equivalent for READ), `authorizeRun` 404/403 semantics (sse.go:244), `OwnerScopeClause` / `OwnerOrPlatformScopeClause` exported verbatim (projection.go:1143). The two-owner leak-test pattern exists across `projection_*_test.go`/`sse_test.go`.
- **Projections landed at B5-2** (projection.go, 1273 lines, read in full): `RunCard` (FSM state, `waiting_on_human`, `parked_until`, stage, tool+args-digest, monotonic counters, last-activity, DERIVED `wedged` — and the checkable NEGATIVE: no percent/fraction/ratio/ETA field, asserted by conformance test), `BoardSnapshot`, `FleetSnapshot` (with `FleetLane.BurnRate *float64` an explicit **nil seam**: "a real burn_rate needs a windowed calc B4-5/B6 own — nil here, never faked", projection.go:270), `InboxSnapshot`. Run_events derive queries are **exported verbatim constants** with EXPLAIN-plan index tests (the §36 D7 discipline; served by 0015's `run_events_type_seq_idx` / 0001's `run_events_run_idx`).
- **The History store surface** (`internal/history`, B5-8B, read in full): `Views() []View` / `ViewByName` / `CostQuestions()` (Layer-0 registry over the eleven 0016 views), `Catalog() []Query` / `QueryByName` / `CatalogNames()` (49 canned queries, 6 categories, all 8 S14.10 filter axes reachable — §37), and the five Store verbs: `SelectView(ctx, name, scope, limit)`, `RunQuery(ctx, name, slots, scope, limit)`, `Ask(ctx, question, scope, limit)`, `Search(ctx, question, scope, limit)`, `AskOpenSQL(ctx, question, scope, limit)`. One `Answer` shape from ONE constructor: `Layer`, `Query`, `Confidence` (derived from layer — `lower-confidence` for Layer 2, inexpressible otherwise), `Columns/Rows/RowCount/Truncated/Notes`, `Card *Disambiguation`, `Audit *OpenSQLAudit` (carried on Layer-2 answers). `history.Scope{Operator, UserID}` is the S01.9 scope input; limits clamp via `DefaultRowLimit` 200 / `MaxRowLimit` 1000 (structural, reasons recorded in §37 OQ6). Layer 2 executes on `storage.ReadOnly`; **every attempt (incl. every refusal) writes a `history.query_audited` platform-scope row scoped to the asker**; escalation is the ABSENCE of a fall-through path. **All guardrails, audits, and the redact-before-match search are LANDED and battle-drained (67-case × 2-scope battery). This packet routes; it re-implements nothing.**
- **The declare rows** (`internal/eventlog/contract.go:430–431`): `declare("stage.started", FamilyRunLifecycle, "producer: B6 (S15/9.1 board); stages today only setKanban")` and `declare("stage.finished", FamilyRunLifecycle, "producer: B6 (S15/9.1 board)")`. Family-1 RequiredFields (verbatim, byte-pinned by `TestFifteenFamiliesWithRequiredFields`): *"from→to + cause (every FSM transition appends [S02.3]); stage id/kind, stage-brief hash, outcome"* — the stage part binds `stage.*` payloads: **stage id/kind, stage-brief hash, outcome**. Current inventory **88 minted / 7 declare-only / 95 registered**, pinned by `contract_test.go:185–204` and `TestDeclareOnlyFutureTypes` (the 7: `stage.started`, `stage.finished`, `run.parked`, `run.resumed`, `ask.observed`, `ask.answered`, `tool.called`). Topics need NO change: FamilyRunLifecycle already tags `board`, run-scope already tags `run` (topics.go:56,78 — §30 mapped the declare-only producers in advance).
- **Stage boundaries today** (`internal/stage`, read in full): `Skeleton.Session(ctx, SessionInput)` (runner.go:129) is the S05.3 fresh-context stage-session boundary — `SessionInput.Stage` names the stage (plan-step id for execute; role names for ceremony/verify; `helper-<id>` for S04 helpers; split successors ride split.go). Assembled briefs come from `ledger.Assemble`; `ledger.PlacePinned(workDir, brief)` already returns a **sha256hex** (assemble.go:602) and the S04.4 `spawnRecord.BriefHash` (helpers.go:272) is the in-repo brief-hash precedent. Artifact-only sessions (`Assemble=false` — critique S06.8, judge/rework S07) have a zero-value Brief. Role legs flip kanban via `setKanban` (skeleton.go:733 — "provisional strings"); the intake pipeline FSM appends `intake.state` rows (family 1, minted). RunCard's stage read already PREFERS the richer marker: `latestPayloadType(… "stage.started", "stage.finished", "intake.state")` (projection.go:152).
- **Meter data sources (all landed)**: `metering.PressureGauge.Read(ctx, userID, lane, budget) → Gauge` (pressure.go:80 — `WeightedConsumption`, `CacheReadWeight`, `Assumed`, `Pressure`/`Applicable` — honest when no budget), **`Budget` is a caller-supplied value object; `UndeclaredBudget()` is the v0 posture — NO operator budget is persisted anywhere at v0** (pressure.go:24–39; restated by 0016's `cost_budget_remainder` view: `remainder_status='UNAVAILABLE'` with its `absence_reason`, one row per person). Money figures live ONLY in `receipts.usage_json` (the marshaled `metering.Receipt`, priced at write) and the 0016 Layer-0 views over it: `cost_per_run/task/project/person/period`, **`cost_burn_rate`** (usd-per-observed-day, a division of already-priced totals — "No token is multiplied by any rate here"), `cost_budget_remainder`, `cost_done_directly` (CITES the receipt's own `direct_use` block; BENCH-REG §17 numbers read-only), **`limit_event_history`** (event_seq, ts, user, run, limit_class, provider_signal, resets_at, lane; carries the `engine.rate_limit` legacy alias), `routing_quality`, `task_project`. S10.1: "Aggregations per person × model × task × day/week/month are **queries over the ledger, not new state**."
- **Task-detail sources (all landed)**: `intake.Spec` (numbered contiguous `ACs []AC` with `Plain` always + `Structured` where checkable, assumptions, out-of-scope, supplied facts — artifact.go:36–114), `intake.Plan` (numbered `Steps` with `DoneWhen`, the AC `Coverage` map, research nodes, cited entries — artifact.go:152+), reachable via the intake pipeline stores (`pipe.ApprovedPair`/`LoadState`, surface.go precedent). Lineage: **project** via `artifact_claims.project` — the ONLY populated relational edge, isolated in the 0016 `task_project` view (`''` excluded, one row per task, `'(no project)'` bucket; §37 recorded reading: there is NO run→project column); **follow-ups** via `task_successor_of` (0009: task → (deliverable, revision), immutable, indexed both directions). Receipts: `receipts.usage_json` served verbatim (the existing `Surface.Receipt` shape, surface.go:206).
- **Redaction edge today**: `redact.RedactJSON` fires exactly in `internal/api/sse.go` (frames + snapshot states); `TestRedactionObservabilityEdgeOnly` (sse_test.go:497) **pins `redact.` to sse.go only** within internal/api, asserts review/preview/accept/intake never import the primitive, and asserts retention (write side) + history (read side) still do. Research/18 §7-C2·1 scopes C2 to "run-event payloads on **SSE/REST**, the S14 query layers, and any search" — the REST half arrives with this packet (OQ6).
- **Hermetic harness**: `newTestServer` (api_test.go) builds `api.New` over a `t.TempDir()` `storage.Open` DB + `httptest`. **Tests NEVER touch the live Caddy admin API/config or the production units on 8481/8482 — standing live-host hazard.**
- **Counters at open** (assert before changing): ⚙ **118 keys / 33 domains** (`internal/settings/index.go` byte-unchanged through all of B5); `components.lock` **21**; migrations **0001–0016** (`user_version` 16); event inventory **88/7/95**; CONVENTIONS §1–§37.

---

## 1. Numbered requirements

### A — `/api/runs`: list + detail (S15.2 runs row; S02.3; S14.3)

- **R1. `GET /api/runs` — owner-scoped run list** [S15.2 table "runs" row; S15.5 mission control]. Each row summarizes one run from the stored S02.3 FSM row + the landed derive machinery (state incl. the 11 stored values, DERIVED `wedged` — never a column, §30 R11 — `waiting_on_human`, `parked_until`, lane, task_id, owner, created/updated). Filterable by **status (FSM state), person (operator cross-owner only), date range, task** via query params (exact names are P3's within S15.2). Bounded reads (the `DefaultRowLimit`/`MaxRowLimit` clamp precedent — reuse or mirror with named reasons; no unbounded materialization on the shared handle).
- **R2. `GET /api/runs/{run}` — run detail** [S15.2: "run FSM state, live-activity refs, spawn records, routing records (S2.6)"]. Exposes: the full `RunCard` projection (reused, not rebuilt — projection.go); **live-activity refs** = the last-activity line + the cursor refs a client needs to tail `/events` (S15.3 snapshot-then-tail: the response carries the head `event_seq` captured at projection time — R18); **spawn records** = the run's `helper.spawned` / `spawn.refused` / `orchestration.helper` rows (S04.4 spawn-record payloads served); **routing records** = the run's `routing.decided` rows (S14.2 family 7 payloads: cause, score, worker/model/lane, effort, plain_reason). 404 unknown / 403 other-owner / operator-any — the `authorizeRun` precedent verbatim.
- **R3. New run_events derive shapes follow the §36-D7 discipline**: production query texts exported as constants beside the existing set (projection.go:1117+), owner-scope clauses appended per S01.9, and the EXPLAIN-plan test extended to assert index service (0015's `run_events_type_seq_idx` / 0001's `run_events_run_idx`) in BOTH operator and member forms. No new index without measurement; if one is genuinely needed it is migration 0017 + justification (expected: none — the type+run shapes are already served).

### B — `/api/tasks`: list + detail enrichment (S15.2 tasks row; S06.6; S13.9; S15.5 task detail)

- **R4. `GET /api/tasks` — owner-scoped task list**: board-card summaries (task_id, title, kanban_status, latest-run summary incl. state/wedged/stage — the `BoardSnapshot` machinery reused), filterable by **person (operator only), status, date, project** (project via `task_project`; a task with no resolved project is the honest `'(no project)'` bucket, never dropped — §37).
- **R5. `GET /api/tasks/{task}` — detail enrichment** [S15.2: "task objects: spec + numbered AC, plan, stage progress, lineage (project, follow-ups), receipt view"; S15.5 task detail]: (a) the SPEC artifact — restatement + **numbered ACs verbatim** (both phrasings where present, G1 P10), assumptions, out-of-scope, supplied facts; (b) the PLAN — numbered steps with Done-when, the AC coverage map, research nodes; (c) **stage progress** — the per-stage story derived from the run(s)' `stage.started/finished` + `intake.state` rows (derive-from-log; no side store); (d) **lineage** — project (via `task_project`/`artifact_claims`) and follow-ups (via `task_successor_of`, BOTH directions: what this task succeeded, what succeeded it); (e) **receipt view** — the per-run materialized receipts served from `receipts.usage_json` VERBATIM (ceremony-vs-execution itemization and the `direct_use` block with its `formula_ref` are already inside the stored Receipt; label RENDERING under G2 D2.8 is B6-5's). Absent artifacts (pre-approval task, no receipt yet) render as honest absences, never errors that hide the task.
- **R6. Owner-scoping closes over the walking-skeleton reads** [S01.9; S15.2 contract rules; §30 precedent]. `GET /api/tasks/{task}` and `GET /api/runs/{run}/receipt` become owner-scoped server-side (member = own only, 404/403 semantics; operator = all). The mutation routes (`submit`/`advance`/`answer`) are NOT this packet's — they stay as-is for B6-2. Disposition of route shape is OQ5.

### C — `/api/meters`: the read side (S15.2 meters row; S10.1/S10.4; 0016 views)

- **R7. `GET /api/meters` — consumption, pressure, budgets, burn rates, limit-event status per (person, lane, period)** [S15.2 meters row]. Sources, all landed: **consumption + pressure** from `metering.PressureGauge.Read` per (person, lane) — `WeightedConsumption` always real, `Assumed` label carried, `Pressure` only when `Applicable` (v0: `UndeclaredBudget()` ⇒ not applicable — honest, never a fabricated denominator, D4); **budgets** = the v0 honest absence (OQ2): `budget_declared=false` + the absence reason, the `cost_budget_remainder` posture served, never a zero; **burn rates** from the `cost_burn_rate` view rows (money READ, never computed — S14.10 ¶1, §37); **limit-event status** = the `limit_event_history` view rows (both type aliases, per §37) plus the current park state per (person, lane) derived from the latest `limit.event`/`run.state_changed` markers ("parked until…", S15.5 fleet). **Period** axes ride `cost_per_period`/`cost_per_person` rows; if a true (person × lane × period) aggregation is needed beyond the landed shapes it is added as a **query in `internal/metering`** (S10.1: aggregations are queries over the ledger, not new state) — never priced arithmetic in `internal/api`.
- **R8. NO money is computed in `internal/api` — a checkable negative** [S14.10 ¶1; §37 `TestNoMoneyByGeneration` precedent]. Every dollar figure served is read from `receipts`/the 0016 views/`metering` reads. internal/api gains no token-times-rate expression and no import of the price table. A test asserts it (source scan or equivalent, with a non-tautology probe).
- **R9. The SSE `FleetLane.BurnRate` seam** (projection.go:270) is filled from the SAME `cost_burn_rate` source or explicitly left nil with the seam note updated to name `/api/meters` as the surface — per the OQ2 disposition. It is never computed a second way (one source for one figure).

### D — `/api/events`: routing the S14.10 layers (S15.2 events row; S14.10; §37)

- **R10. Route the History store — routing ONLY** [S15.2: "filterable history (S2.10) **through the S14 query layers**"; §37 "Held, not routed"]. New endpoints under `/api/events` (exact paths/methods are P3's within S15.2) expose: the **registries** (Layer-0 views list + the 49-query catalog with categories/slots — `Views()`, `CostQuestions()`, `Catalog()`/`CatalogNames()`, so a client can render choices), **`SelectView`** (Layer 0), **`RunQuery`** (Layer 1 direct, typed slots), **`Ask`** (Layer 1 intent-filled), **`Search`** (redact-before-match), and **`AskOpenSQL`** (Layer 2). The transport builds `history.Scope` from the request identity (operator bit via the `isOperatorRead` path), passes the caller's limit through the package's own clamps, and serializes the `Answer` shape. **`internal/api` builds NO SQL text, interpolates NOTHING into a query, and re-implements NO guardrail, audit, redaction, or confidence logic** — the package boundary is the whole point (§37; G3 D3.5).
- **R11. The Layer-2 lower-confidence flag and audit are on the wire verbatim** [S14.10 ¶3; G3 D3.5; S15.7 data need]. The routed answer carries `confidence` exactly as the store set it (`lower-confidence` for every Layer-2 answer — the unflagged form is inexpressible, §37 R26) and the `audit` block (incl. refusals, which return as answers carrying their audit — the transport must not convert a refusal or a disambiguation card into a bare HTTP error; a card is a 200-class answer, the honest non-answer).
- **R12. Mission-control filters answerable** [S15.5: "filterable history by project, person, status, and date [query layers XREF:S14]"; acceptance headline]. Each of **project / person / status / date** is demonstrably answerable through the shipped read surface: the routed catalog (all 8 axes reachable per §37 — the endpoint binds slots, nothing new) and/or the R1/R4 list filters. The evaluator exercises each axis on a two-owner fixture.
- **R13. Layer-2 exposure is deliberate, not default** [S14.10 ¶3 "escalation, never default"]. Reaching `AskOpenSQL` over HTTP is its own explicit route/parameter — never a fallback the transport takes when `Ask` returns a card. (The store already enforces no-fall-through internally; the transport must not rebuild one above it.)

### E — Stage-event production (S14.2 family 1; §29; the queue row's "board data complete")

- **R14. `stage.started` / `stage.finished` are minted at real stage boundaries** [S14.2 Run-lifecycle row; contract.go:430 "stages today only setKanban"]. Producer lands in `internal/stage` at the boundary set the OQ3 disposition fixes (recommended: the `Session()` stage-session boundary — every fresh engine stage session across roles, incl. helper sessions and split-successor sub-stages; intake pipeline phases keep `intake.state`). Payloads carry the S14.2 contract minimum: **stage id/kind, stage-brief hash** (OQ4 fixes the hash definition), and **outcome on `stage.finished`** (completed / split / error / park vocabulary per the disposition — the record is honest on non-happy paths). Events are run-scoped appends at the run's generation via `eventlog` (envelope validation, payload cap, fencing — the B5-1 contract).
- **R15. §29 flip mechanics, exactly** [CONVENTIONS §29]: both types flip declare→minted in `contract.go` with real provenance (`file:line`), gain `producerTypes()` entries, and join the required-field conformance goldens. Inventory moves **88/7/95 → 90 minted / 5 declare-only / 95 registered**; `contract_test.go`'s count pins and `TestDeclareOnlyFutureTypes`'s list update accordingly (the other 5 declare-only rows are untouched — `run.parked/resumed`, `ask.observed/answered`, `tool.called` are NOT this packet's to mint). NO new type, NO new family, NO S00.9 amendment. Topic map untouched (already correct for family 1 + run scope).
- **R16. Board data complete** [queue row; S15.5]: with the producer live, `RunCard.Stage` (which already prefers `stage.started/finished`, projection.go:152) and the R5 stage-progress derivation render real per-stage data on a driven fixture (the fake-engine walking-skeleton path). The §36 run-summary aggregate picks the new rows up forward-tolerantly — assert no retention test breaks; retention needs NO change (family 1 is not keep-forever; stage rows are strippable trace, correctly).

### F — Cross-cutting contract rules (every new endpoint)

- **R17. Owner-scoped server-side, fail-closed** [S15.2; S01.9]: every new route runs behind `requireIdentity`; scope resolves via the one role bit; member sees own rows only, operator sees all; internal resolution failure is 500, never anonymous downgrade. **Cross-owner leak tests per endpoint** (two-owner fixture; three-way assertion: operator both, owner theirs, other member ZERO/403-404) — the §37 axis-test pattern.
- **R18. REST snapshots carry their cursor** [S15.3: "a view loads its REST snapshot, then tails `/events` from the snapshot's `event_seq` cursor"]. List/detail responses that feed live views carry the head `event_seq` captured at projection time (the `Health.EventHead` / snapshot-frame `cursor` precedent).
- **R19. Read-side only; unversioned lockstep** [S15.2]: NO new mutating verb of any kind in this packet (the only writes anywhere are the stage-event appends inside `internal/stage` and the landed Layer-2 audit inside `internal/history`). No `/v1` prefix; additive-first evolution; no new external observable (S01.8 register untouched).
- **R20. Redaction at the REST observability edge** [§30; Research/18 §7-C2·1; OQ6]: new responses that embed **run_events payload content** (R2's spawn/routing/activity records; any routed answer already redacted upstream stays as the store returns it) pass through `redact.RedactJSON` at serialization, the `writeSnapshotFrame` precedent — store-raw / serve-redacted (stored rows never mutated). `TestRedactionObservabilityEdgeOnly`'s file pin widens HONESTLY (sse.go + the new read-handler file(s)); its negative limbs (review/preview/accept/intake never import; retention+history still do) are UNCHANGED. Redaction is NOT blanket middleware: SPEC/PLAN artifacts, receipts, and view/catalog rows are not payload-bearing frames and are not wrapped (§30 "NOT a blanket response middleware"; §7-C2·2).
- **R21. Structural constants carry named reasons; NO new ⚙ key** [§29–§37 precedent; ratified settings-tab directive]. Any new bound (list caps, filter windows) is a named structural constant with its reason, interim under the standing settings-tab directive. `internal/settings/index.go` stays byte-unchanged (118/33). The only ⚙ this packet's code touches is the already-consumed `obs.sse_keepalive` (unchanged).
- **R22. CONVENTIONS §38 is appended** documenting this packet's readings/decisions (earlier sections never rewritten); commit subjects per §5 (`P3-B6-1: … (S15.2, S14.10, S14.2)` + trailer); `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.

---

## 2. Seams — what belongs to LATER packets (leave named, never build)

| Later packet | Owns | B6-1 leaves |
|---|---|---|
| **B6-2** | ALL mutation verbs: approvals family (list is inbox topic today; verbs/inbox-list endpoint per S15.6), task/run **cancel**, follow-up spawn, board-drag priority hint, **meters budget edits + pause-my-automation** (budget persistence arrives WITH its edit verb — OQ2), watchdog/drift/conformance verbs, benchmark driver + verdict backend | read endpoints whose shapes B6-2 verbs will act on; the honest budget absence |
| **B6-3** | `/api/settings`, `/api/deliverables` (+ preview sessions), `/api/memory` | `Config.Accept/FollowUp/Preview` stay held-not-routed |
| **B6-4+** | the SPA, all rendering, receipt-label rendering (G2 D2.8 verbatim strings), EventSource client, connection-state UX | data only; no HTML, no React |
| **B6-7** | the conversational assistant consuming these routed layers | the `/api/events` transport it will call |
| **Not this packet** | minting `run.parked/resumed`, `ask.observed/answered`, `tool.called` (stay declare-only); any watchlist/canary/benchmark change; BENCH-REG numbers (read-only) | — |

## 3. ⚙ settings consumed (by exact dotted key)

- `obs.sse_keepalive` — already consumed by the SSE transport; unchanged.
- **No other ⚙ is consumed and NONE may be invented.** New number-shaped choices are structural constants with named reasons (R21). The history/metering packages read their own ⚙ internally — not this packet's concern.

## 4. Files expected to change

- `internal/api/` — new read-handler file(s) (runs/tasks/meters/events-history transport); `api.go` (routes; possibly narrow Config seams in the existing pattern); small projection extensions (list shapes, exported query texts); `sse_test.go` (R20 pin widening); new endpoint tests incl. cross-owner batteries.
- `internal/stage/` — the stage-event producer at the disposed boundary set (runner.go / skeleton.go / helpers-split paths as the boundary set requires) + tests.
- `internal/eventlog/contract.go` + `contract_test.go` — the two declare→minted flips, provenance, `producerTypes()`, goldens, count pins 90/5/95.
- `internal/metering/` — ONLY if the (person, lane, period) read needs a new aggregation query (R7); no schema, no pricing change.
- `internal/shell/` — wiring only if a new narrow seam is added (History/DB/Meter are already wired).
- `P3/CONVENTIONS.md` — §38 appended.
- Expected NOT to change: migrations 0001–0016 (0017 only under R3's measured-need rule), `internal/settings/index.go`, `internal/history/*` (routing consumes it as-is; any change there is a referral, not a fix), `internal/redact`, `components.lock`, `go.mod`.

## 5. Adopted components

**Expected: none touched, none added.** Stdlib + landed packages only. A new dependency would be an OPEN QUESTION for the coordinator (S16.4 rail + operator ratification), never an executor decision.

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format, no push, never edit Spec/STATE/Docs/Research, never read `*-api-key.txt`.
- **§7** — API unversioned; SSE frame contract untouched; identity middleware seam; structural-constant precedent (`sseBatchSize`).
- **§9** — route classes (everything new is session-required, fail-closed 401); step-up machinery exists but NO new verb here needs it (read-side).
- **§11** — metering is a READ MODEL; aggregations are queries not state; UNPRICED honesty; no dollar-ranked anything.
- **§29** — declare-once registry mechanics; flip rules; family fields byte-pinned; forward-tolerant consumers; NO new family without S00.9.
- **§30** — the SSE surface stays extended-never-rebuilt; owner-scope per S01.9 (operator/member/dev-read); progress-semantics NEGATIVE (no percent field — new list/detail shapes included); store-raw/serve-redacted (R19); redaction constant-on, no ⚙.
- **§36** — derive queries as exported verbatim texts + EXPLAIN tests (D7); run-summary forward-tolerance (R16); migration immutability (0001–0016 byte-untouched).
- **§37** — the three layers' semantics; routing quality/axes; guardrails/audit/flag NEVER re-implemented or weakened; `Answer`/`newAnswer` shape authority; structural-constant reasons; "Held, not routed" is the seam THIS packet discharges.
- Cross-phase (D6 ratified, HANDOFF §6): derive-from-log/no side stores; no tickers; cards/projections derive bounded; money read never computed; severity computed never judged; refusal never repair; every refusal audited; model output is untrusted input.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. `GET /api/runs` exists, owner-scoped, filterable (status/person/date/task), bounded; rows carry FSM state + derived wedged/waiting/parked-until; **cross-owner three-way test passes** (operator all / owner theirs / other member zero).
2. `GET /api/runs/{run}` serves RunCard + live-activity refs + spawn records + routing records; 404/403 per the authorizeRun semantics; **its cross-owner test passes**; run_events-payload content is redacted on the wire while the stored rows stay verbatim (planted-secret test).
3. `GET /api/tasks` list: owner-scoped, filterable incl. project via `task_project` with the `'(no project)'` bucket intact; cross-owner test.
4. `GET /api/tasks/{task}`: numbered ACs verbatim (plain + structured), plan steps + coverage map, stage progress from the log, lineage both ways (project + `task_successor_of`), per-run receipt view served from stored `usage_json` verbatim; honest absences for missing artifacts; cross-owner test; the walking-skeleton read-scoping (R6) proven by a test that previously would have leaked.
5. `GET /api/meters`: gauge consumption + pressure with `Assumed`/`Applicable` honesty; budgets render the v0 absence with reason (no fabricated zero/denominator); burn rates from `cost_burn_rate`; limit-event status incl. both type aliases; per-axis (person, lane, period) answerable; cross-owner test; **no money computed in api** (the R8 negative test exists and a probe proves it can fail).
6. `/api/events` routes ALL of: registries, SelectView, RunQuery, Ask, Search, AskOpenSQL — verified by tests driving each through HTTP against a two-owner fixture; member/operator scope threading proven (member reads own rows only, through the layers).
7. Layer-2 answers on the wire carry `confidence: "lower-confidence"` + the audit block; a REFUSED Layer-2 attempt returns as an answer (not a transport error) and its `history.query_audited` row exists; a Layer-1 unresolved question returns the disambiguation card intact.
8. **Routing-only proven**: no SQL string construction for history data in `internal/api` (source assertion), `internal/history` is byte-unchanged, and grep/AST shows no second implementation of guard/redact/audit/confidence logic.
9. Mission-control axes: project, person, status, date each exercised end-to-end through the shipped surface on the two-owner fixture.
10. `stage.started`/`stage.finished` minted at the disposed boundary set; payloads carry stage id/kind + brief hash + outcome; run-scoped at the correct generation; pairing/outcome behavior on happy, split, and error paths tested; `RunCard.Stage` + R5 stage progress show real data on a driven fixture.
11. `contract.go`: both flips with real provenance; `producerTypes()` entries; goldens added; counts **90 minted / 5 declare-only / 95 registered** asserted; `TestDeclareOnlyFutureTypes` updated; declare-once + reverse (minted-has-producer) tests green.
12. No percent/fraction/ratio/ETA field in ANY new response shape (the §30 conformance negative extended to the new endpoints).
13. R18 cursors present on live-view-feeding responses; R19 no new mutating route (route-table review); no `/v1`.
14. `TestRedactionObservabilityEdgeOnly` widened per R20 and still asserts all original negative limbs.
15. Counters: `internal/settings/index.go` byte-unchanged (118/33); `components.lock` 21; migrations 0001–0016 byte-untouched (0017 only with the R3 justification); `go.mod` unchanged; new structural constants each carry a written reason.
16. CONVENTIONS §38 appended; §1–§37 untouched; STATE not edited by the executor.
17. **Full battery green**: `gofmt -l internal/ cmd/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` (all packages, hermetic — no live Caddy/units/network) · `go run ./tools/lockgate`.
18. $0 spent: every test is tier-F/hermetic; no paid model call, no live seat.

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — the queue row's "S02.3/S02.10" reading.** Spec-draft S02.10 is the *workspace storage seam* (worktree+overlayfs), which no read endpoint consumes; feature-item **S2.10** is *queryable history*, exactly this packet's `/api/events` (S15.2 events row cites "filterable history (S2.10)"). RECOMMENDED: read it as feature-item S2.10 (already covered via S14.10); no workspace exposure in run detail. If the coordinator intended workspace refs on run detail, say so and it becomes a field, not a rebuild.
- **OQ2 — budgets and burn rate at v0.** No operator budget is persisted anywhere (pressure.go; 0016 `cost_budget_remainder`). RECOMMENDED: `/api/meters` serves the honest absence (declared=false + reason + consumption still real); persistence arrives WITH B6-2's budget-edit verb, not here. Burn rates serve `cost_burn_rate`; the SSE `FleetLane.BurnRate` nil seam is filled from that SAME view (one source) — or, if the coordinator prefers zero SSE-shape change in this packet, left nil with the seam note pointing at `/api/meters`. Pick one.
- **OQ3 — the stage-boundary set.** RECOMMENDED: mint at the `Skeleton.Session()` boundary — one `stage.started` before the engine session, one `stage.finished` with outcome after (outcomes: `completed` / `split` / `error`; a park inside a session already rides ask/limit events and the session outcome records what ended it) — covering ceremony, execute steps, verify, compose, helpers, and split successors; intake pipeline phases stay `intake.state` (already family 1); `setKanban` flips stay as-is. Alternative (coarser): role-leg boundaries only (intake/execute/verify legs). The disposition fixes the set; the executor implements exactly it.
- **OQ4 — the stage-brief hash for artifact-only sessions.** `Assemble=true` sessions have `ledger.PlacePinned`'s sha256; `Assemble=false` sessions (critique, judge, rework) have a zero-value Brief. RECOMMENDED: hash = sha256 over the exact prompt text the session received (assembled brief text + instructions, or instructions alone when `!Assemble`) — a uniform "what did this stage read" fingerprint, never empty; the S04.4 `spawnRecord.BriefHash` precedent.
- **OQ5 — walking-skeleton read-route disposition.** RECOMMENDED: keep the paths (`GET /api/tasks/{task}` becomes the enriched R5 detail; `GET /api/runs/{run}/receipt` stays as the per-run receipt read), scope both per R6, and update the B2-4-era tests to the scoped behavior (their unscoped expectations were placeholders, per the §30 precedent). Alternative: new canonical paths + legacy kept temporarily — rejected by default (additive-first does not require keeping an unscoped read alive).
- **OQ6 — REST redaction-edge widening.** Research/18 §7-C2·1 names "SSE/REST" as the observability projection scope; the landed test pins `redact.` to sse.go because REST payload-serving didn't exist yet. RECOMMENDED: confirm R20's reading — payload-bearing REST responses redact at the serving edge, the file-pin widens to the named read-handler file(s), all negative limbs stay. This is scope arriving, not a guardrail change.

## 9. SCOPE VERDICT

**Feasible as one (opus) sitting, at the upper edge.** Everything routes or derives from landed, tested machinery — no schema, no adoption, no new ⚙, no guardrail logic — but it is four endpoint families + a producer + contract flips + a large test surface (~8 cross-owner batteries). If the coordinator prefers the pre-authorized seam (the B5-6/B5-8 precedent), the clean split is:
**A = stage-event production + `/api/runs` + `/api/tasks`** (the board/task story: E + A + B groups, R1–R6, R14–R16) — self-contained, proves "board data complete";
**B = `/api/meters` + `/api/events` routing** (C + D groups, R7–R13) — self-contained, proves the query layers + money honesty.
Cross-cutting F (R17–R22) binds both halves. A stop at the A|B seam is a success mode, not a failure.
