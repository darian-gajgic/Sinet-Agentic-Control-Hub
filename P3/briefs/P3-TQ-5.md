> **EXPIRED 2026-09-17 — landed on main (grounding `b1279e0`, reds `0594bec`, commit 1 `7a21c73`, commit 2 `d9dee2d`, drain r1 `c88c84c`; evaluation PASS, re-check PASS; landing battery 48 pkgs `-p 1` 0 FAIL 0 SKIP; CONVENTIONS §73). Single-use artifact: never read as truth; code + spec are the truth. Superseded at drain: a degraded (unknown) duty rides the execution seat's alternates AND lane order; the receipt judge line takes the last verify.round row WITH a judge; §6 mis-listed `internal/evals/runbook_test.go` as leave-alone (it reads the rubric's MeasuredOn — the date literals moved with the data, adjudicated forced).**

# P3-TQ-5 — the model rerouting (TQ-F9): grounded brief

**Packet:** P3-TQ-5 · **Phase:** post-B6 task-quality rework · **Grounded:** 2026-09-17 on `cc8fd85` · **Status:** EXPIRED (landed 2026-09-17).

**Ruling this packet implements (authoritative, verbatim in `P3/gates/rework-sitting-gate.md` item B7 + §Answers, operator 2026-09-17: "First Kimi K3 CLI"; finding `P3/design/taskquality-webshop-findings-2026-09-16.md` TQ-F9):** retire `claude-sonnet-5` and `claude-opus-4-8` from the task pipeline's shipped duty map. Planning seat → Opus 5 on `anthropic`. Judge seat → Opus 5 on `anthropic`. Execution seat → K3 on `kimi-cli` as the configured FIRST choice, K3 on `kimi` second, Opus 5 on `anthropic` third. Until a Kimi key is placed, execution resolves to Opus 5 on `anthropic` with the routing reason saying why — never an error, never a park; in that interim the judge model equals the executor model and the self-family-judging flag rides every receipt (S07.5 / G1 Def.1). P-T06-5 is a hard gate: the judge change re-runs the 26-case golden set on the Opus 5 judge → `rubric-software` v3, `JudgePin` = Opus 5, rates/floors/length bias re-measured, before unsupervised judging resumes. Judge ≥ executor across vendors (Opus 5 judging K3) is RECORDED as a judgment call, never claimed measured. The B3 gate D3 record (`P3/gates/B3-report.md` §7 decision 3) and the code's provenance comments were the ratification objects for the old mix; the 2026-09-17 gate record supersedes them. No S00.9 changelog row is owed (the spec names duty CLASSES — S06.10 "planning model", S07.5 "planning-model class on a paid flat-rate lane" — never models). The `AlternateSeatsFor` "EXECUTION ONLY" ratification (planning/judge anthropic-only) is untouched.

The spec wins over every report and over this brief. Code + spec were the only inputs; no prior brief was read.

---

## 0. Live-verified facts (source + date; never from memory)

| Fact | Value | Source | Date |
|---|---|---|---|
| Opus 5 Claude API model id | **`claude-opus-5`** — "Claude API ID: `claude-opus-5`", "Claude API alias: `claude-opus-5`"; "Every Claude model ID is a pinned snapshot, including the dateless IDs used from the 4.6 generation on" | https://platform.claude.com/docs/en/models/overview (the docs.anthropic.com URL 301-redirects there) | 2026-09-17 |
| Opus 5 pricing / window / output | $5 / input MTok, $25 / output MTok; context window 1M tokens; max output 128K; default effort `high`; adaptive thinking | same page | 2026-09-17 |
| Opus 4.8 status | listed under "Legacy models (still available)" | same page | 2026-09-17 |
| The harness note `claude-opus-5` | CONFIRMED (the id in `internal/intake/taxonomy.go:287` and the gate record is the live id) | same page | 2026-09-17 |
| K3 on `kimi-cli` | seed id `k3` = `default_model` of `internal/adapters/opencode/lanedata/kimi-cli.json:103` ("K3, matching lane kimi's seat exactly"), and of `kimi.json` (`default_model: "k3"`). Observation grade: **documented at primary source** (https://www.kimi.com/code/docs/en/kimi-code/models.html, re-read 2026-08-26 per the document's own note) AND present verbatim in the pinned opencode 1.18.3 engine record (`kimi.json` `installed_catalogue_record.models_verbatim`); **NOT observed on this account** — no key is placed, so per P-T17-3 the account's observed model list settles it at the LN key ceremony. The kimi-cli adapter sends the seat's model per invocation as `KIMI_MODEL_NAME=<req.Model>` (`internal/adapters/kimicli/lower.go:509`), never a constant | lane documents; `lower.go` | 2026-08-26 (documents), read 2026-09-17 |
| Kimi lanes commissioned on this host | **none** — `git status` shows no placed key; `logCommissioned` (`internal/shell/engineadapters.go:272-295`) reports `lanes=""` with nothing placed (CONVENTIONS §65: coverage grows only when a lane is COMMISSIONED, startup-bound) | code | 2026-09-17 |
| S18 tally | 118 keys / 33 domains, asserted by `internal/settings/settings_test.go:45` and `internal/conformance/kimicli_ln7_test.go:123`; no key covers the duty map (`grep -rn duty internal/settings` → only `local.alias.<duty>`, `index.go:705`) | code | 2026-09-17 |

---

## 1. Requirements (numbered, each with its S-ref)

**R1 — The shipped duty map rides `claude-opus-5` on every seat it carries [S06.10 "uniform recommended default"; S07.5 "planning-model class on a paid flat-rate lane"; B7].** `worker.DefaultDutyMap()` (`internal/worker/routing.go:166-172`) returns execution / planning / judge = `Seat{Model: "claude-opus-5", Lane: "anthropic", WindowTokens: DefaultWindowTokens}`; the utility seat stays ABSENT. The execution row is the seat of LAST RESORT (the always-configured lane's seat — the third choice); planning and judge have no other seat. `DefaultWindowTokens` stays 200k (Opus 5 is 1M-class per §0; understating only splits stages earlier — the standing reasoning at `routing.go:129-140`, whose comment moves from "(opus-4-8, sonnet-5 …)" to name `claude-opus-5` 1M live-verified 2026-09-17). The provenance comment at `routing.go:142-165` is rewritten: ratification object = `P3/gates/rework-sitting-gate.md` B7 + §Answers 2026-09-17 (supersedes B3 D3); the judge is deliberately the SAME model as the interim executor and the flag on the receipt says so (R4); judge ≥ executor across vendors (Opus 5 judging K3) is a JUDGMENT CALL recorded here, never measured; no S18 key covers the map (the standing settings-tab directive is the resolution path).

**R2 — The execution duty carries an ORDERED lane preference as worker DATA; the first covered seat in that order wins when the gauge cannot separate the lanes; skipped preferred lanes are NAMED in the plain reason; never a gap, never an error [S08.8 step 3: "resolves against the requester's duty maps", "subscription coverage binds every choice", "among flat-rate lanes, selection uses consumption pressure, never dollars"; S08.8 accountability: plain_reason; B7 "configured FIRST choice"].** Exact surface (the RED tests bind to these names):
- `type LaneOrder map[string][]string` keyed by duty class; `func DefaultLaneOrder() LaneOrder { return LaneOrder{DutyExecution: {"kimi-cli", "kimi", "anthropic"}} }` — lane NAMES only. Planning and judge have NO entry (the anthropic-only ratification stays; `AlternateSeatsFor` still populates `DutyExecution` only, `routing.go:209-226`).
- `Router.LaneOrder LaneOrder` — **nil = the pre-TQ-5 order exactly** (duty-map seat first, then alternates in the handed-in order; `TestLN4PlacedCredentialIsSelectedByRouting` and `TestLN4NothingPlacedIsNotSelectable` in `internal/worker/lanecommission_ln4_test.go` build Routers without it and MUST stay byte-identical in expectation).
- `chooseFlatLane` (`routing.go:1089-1162`): when `r.LaneOrder[duty]` exists, `covered` is ordered by it (listed lanes in order; unlisted lanes after, in their existing relative order — `zai` is not named by B7 and keeps LN-4's place after the configured lane); the duty-map seat is no longer automatically first. The `len(alts) == 0` early return (`:1091`) must NOT swallow the skipped-lane sentence: with an order present the sentence is composed whenever a listed lane AHEAD of the chosen seat's lane is not covered — including the nothing-commissioned world where `alts` is nil. The gauge semantics are UNTOUCHED: ≥2 covered + comparable readings → the gauge decides and says so (`"how much of each is left"`); not comparable / nil → the configured (ordered) first wins with the standing `"configured order"` phrase (`lanecommission_ln4_test.go:153` asserts it).
- Plain-words sentence (requester surface, §38/P3-GF13: no spec citations, no lane-name jargon beyond the lane names themselves), e.g.: *"The first choice for doing the work is the kimi-cli lane and the second the kimi lane; neither is set up on this platform yet, so it runs on the third choice, claude-opus-5 on the anthropic lane."* The tests assert substrings only: each skipped lane name, `first choice`, no `Not covered by a subscription`, no `S08`/`S07`/`S06`/`A13`/`D5`. It must not contain `flat-rate lanes cover this duty` or `consumption pressure` (the LN-4 nothing-placed guard's banned phrases).
- The composition root sets it: `stage.New` (`internal/stage/skeleton.go:134-146`) constructs the Router with `LaneOrder: worker.DefaultLaneOrder()` unconditionally — platform DATA like the DutyMap default (`skeleton.go:106-109`); no `stage.Config` field, no ⚙ key.
- The K3 model id never appears in `internal/worker`, `internal/shell`, `internal/stage` non-test sources (`TestNoLaneValueIsAConstantInRoutingOrShell`, `internal/worker/zai_ln2b_test.go:177-209`, bans `"k3"` with quotes): the K3 seat reaches the router only as `AlternateSeatsFor(LaneSeat{...})` from `opencode.CommissionedSeats` (`lane.go:868-881`, the documents' `default_model`) once the lane is commissioned (`internal/shell/engineadapters.go:456-472`).
- A per-task lane pin [S00.9 A13] bypasses the order entirely (`resolveLanePin`, `routing.go:990-1087`, untouched): it is the person's tool to force `kimi-cli` regardless of the gauge.

**R3 — The judge names the JUDGE seat and computes the self-family flag from the executor that ran [S07.5 "Self-family judging is always flagged on the receipt [G1 Def.1]"; S07.5 "Default judge = the requester's designated verification-review engine"; S07.11 "judge model" on every verdict row].** Today `EngineJudge.Meta()` (`internal/stage/engines.go:801-806`) answers `Model: j.s.modelFor("")` — the PLANNING seat via `runner.go:403-411` — and `SelfFamily: true` hardcoded since B2-4. Exact surface (the RED tests bind to these names):
- `func newEngineJudge(s *Skeleton, executorModel string) *EngineJudge` — the one construction seam. `newVerifier` (`skeleton.go:950-968`) calls it with the task's recorded execution model read exactly as `engineRevise` reads it (`engines.go:831-834`: `s.pipe.LoadState(ctx, taskID)` → `st.Routing.Model`, "" when absent).
- `func sameFamily(exec, judge string) bool` — pure: both non-empty and the leading token before the first `-` equal (`claude-*` ↔ `claude-*` true; `k3`, `kimi-*`, `glm-*` vs `claude-*` false). A recorded judgment call about what "model family" means for the four ids in play; no vendor table.
- `Meta()` = `{Model: judgeModel, SelfFamily: sameFamily(executor, judgeModel)}` where `judgeModel` = `cfg.Model` when set (the dev/test override `modelFor` already honours — `direct_internal_test.go:172` pins no seat name by design) else `s.seat(worker.DutyJudge).Model`; an empty executor falls back to `s.seat(worker.DutyExecution).Model` (what would have run).
- The judge SESSION runs on the seat `Meta()` names: `EngineJudge.session` (`engines.go:781-799`) passes `Model: judgeModel` and the judge seat's `Lane`/`WindowTokens` on `SessionInput` (fields exist, `runner.go:19-27`), so the row's `judge_model` and the engine's model cannot drift. Equal by data on the shipped map (planning = judge); the change is that it can no longer lie when they differ.
- `UnsupervisedJudgingGate(rubric, judge.Meta().Model)` at `skeleton.go:961` now checks the JUDGE seat — which is the seat the rubric pin must name (R5).

**R4 — The self-family flag rides the receipt as ONE additive member, composed at the serving side [S07.11 "the self-family-judge flag rides the same receipt [G1 Def.1]"; S10.10; S15.2 additive].** The flag exists today only on the verdict row (`internal/verify/record.go:67-69` `self_family_judge`, `history/catalog.go:399-404` `verdicts.self_family_judge`) — the receipt (`internal/metering/receipt.go:27-79`) has no member for it. Exact surface (RED test binds to it): `type JudgeLine struct { Model string `json:"model"`; SelfFamily bool `json:"self_family"`; Note string `json:"note,omitempty"` }` and `Receipt.Judge *JudgeLine `json:"judge,omitempty"``. Composed in `stage.Surface.Receipt` (`internal/stage/surface.go:462-489`) exactly the way `Verification` is (P3-GF4 OQ5: metering never imports verify): read the run's `verify.round` rows (the `verificationPosture` read shape, `surface.go:491+`; LAST row wins), take `judge_model` + `self_family_judge`, serve `judge` only when a verdict row exists — a run with none is served byte for byte. Plain-words note (requester copy, no citations): self-family → *"The work and the check of the work were both done by the same model family (claude-opus-5 did both), so this check is less independent than usual."*; cross-family → *"The check was done by claude-opus-5, a different model family from the one that did the work."* The SPA rendering of `judge` is NOT this packet (FRONTEND.md lane); the member is additive so the receipts drawer tolerates it — named follow-up for the frontend lane, not an omission.

**R5 — rubric-software v3: judge pin `claude-opus-5`, rates MEASURED on it [S07.10 "Each rubric version pins its judge model; changes follow P-T06-5"; S07.9 P-T06-5 "judge model pinned per rubric version; any judge-model change gates on a golden-set re-run (TPR/TNR re-measured) before unsupervised judging resumes"; P-T06-3 "per-judge-model length-bias … re-measured on judge change"].** `verify.SeedSoftwareRubric()` (`internal/verify/seeds.go:114-158`): `Version: 3`; `VerifiedOn` = the rider date; `JudgePin` names `claude-opus-5` at a token boundary (`pinNamesSeat`, `judgegate.go:40-56`) and cites the gate record + measurement file, never `opus-4-8`; `GoldenSet{TPR, TNR, Measured: true, MeasuredOn: <rider date ≥ 2026-09-17>}` from the rider; `LengthBiasNote` "MEASURED on claude-opus-5 <date> … r = <value>"; the four `Items` VERBATIM v2 (the content did not change, only the pin and its measurement). **v3 lands ONLY together with the measured numbers** (see §10 sequencing): `UnsupervisedJudgingGate` (`judgegate.go:21-36`) refuses an unmeasured or mismatched pin and `newVerifier` turns that into a P-T06-5 decision card on EVERY verification (`skeleton.go:961-966` → `verify.NewPreambleRefusal` → the S07.7 infrastructure card at `skeleton.go:921-940`) — the code has no advisory posture for an unvalidated judge, and inventing one is out of scope.

**R6 — The seeded floor tracks v3 and its measurement [S14.8 ¶2/¶5 floors per (asset, VERSION), measurement-derived; BENCH-REG §10.1(d)].** `internal/evals/floors.go:29-45`: `rubricSoftwareCatchFloor` = the Wilson 95% lower bound of the rider's observed TPR at n = 20; `rubricSoftwareTNR` = the Wilson lower bound of the observed TNR, carried REPORT-ONLY; `floorBasis` names `claude-opus-5`, the rider date, `P3/measurements/2026-09-17-rider1-golden-set-opus5.md`, "Wilson", and never `opus-4-8`. `SeedFloors()` derives the version from the rubric (already `r.Version`) → v3; `Ratified: false` (the gate's act). The v2 row already registered in live databases is untouched (`EnsureFloorsRegistered` is insert-once, `floors.go:71-80`). BENCH-REG's registered numbers do not move.

**R7 — The golden-set harness retargets to the Opus 5 seat and both riders are PRE-REGISTERED before any paid call [S07.9 P-T06-5; P-T06-3; CONVENTIONS §3 "bring-up measurements are not Go tests"; §28 "measurements are Def.8 probe files"].** `internal/adapters/claudecli/rider1_golden_test.go:36` `rider1Model = "claude-opus-5"` (+ the head comment and the skip text at `:21-33`, `:103`); the test name `TestRider1GoldenSetOpus`, the simplified two-axis prompts, the 26-case `SeedGoldenSet()`, Wilson CIs and the point-biserial length bias stay EXACTLY as 2026-07-22 (`P3/measurements/2026-07-22-rider1-golden-set-opus.md` is the design being repeated). The executor writes the two pre-registration files (§8) with their Observation sections marked PENDING; the COORDINATOR runs both riders (§10). No code change for rider 2: `spike_test.go:53-58` takes `SINET_B1_4_MODEL`.

**R8 — Every seat-coupled assertion site moves; nothing else does [runbook: executor may not modify pre-existing tests except edits the brief sanctions BY NAME].** Sanctioned edits, exhaustively (§6). Fixture/data sites that merely CONTAIN the retired ids are NOT coupled and are left alone (§6 second list).

**R9 — Truth about the seat actually used, at every recording point [S08.8 accountability `routing.decided`; S10.1; P3-LN-9 R9 (`run.SetDecidedLane`, `internal/run/run.go:556-596`, called at `skeleton.go:686`)].** In the interim: `routing.decided` on `<task>.execute` carries `model: claude-opus-5`, `lane: anthropic`, a `plain_reason` naming `kimi-cli` and `kimi` as skipped, no `gap_advice`; `runs.lane`/`runs.substrate` = `anthropic`/`claude-cli` (the stamp is a no-op because the decided lane IS the configured one — `run.go:584-596`); the verify.round row's `judge_model` = `claude-opus-5` and `self_family_judge` = true; the startup line `lanes: commissioned from the credentials placed …` says `lanes=""` (unchanged code, `engineadapters.go:272-295`) — the honest statement that nothing is commissioned. With a placed Kimi key (not this host today) the same three records name `k3`/`kimi-cli`/`kimi-cli` and `self_family_judge` = false; no code path invents a K3 dispatch without a commissioned lane (`ErrLaneNotCommissioned`, `opencode.go:101`; the kimi-cli dispatch guard `internal/stage/kimicli_ln7_test.go:47` is the existing proof that a `Decision{Model:"k3", Lane:"kimi-cli"}` reaches the kimicli adapter).

**R10 — No ⚙ default or clamp moves; no ⚙ consumed or added [S18; S00.9].** The duty map and the lane order are code DATA under the standing settings-tab directive (memory `settings-tab-see-change-everything`; CONVENTIONS §19 reading (4)) — they are already on the deferred ledger as future operator-editable settings. Tally stays 118 / 33 (asserted). No S18 sweep. No new dependency, `components.lock` untouched (lockgate 40), no adopted code modified, `lanedata/*.json` untouched.

---

## 2. The seven questions, settled from code + spec

**(1) How a DutyMap first-choice seat whose lane is NOT commissioned resolves today, and the smallest change.** Today: `resolveSeat` (`routing.go:832-988`) → `chooseFlatLane` (`:1089`) with `alts = r.Alternates[duty]`; with nothing commissioned `Alternates` is nil (`laneAlternateSeats` → `CommissionedSeats` → none, `engineadapters.go:456-472`; pinned by `TestLN4NothingPlacedIsNotSelectable`) so `chooseFlatLane` returns the duty-map seat unchanged at `:1091-1093`; then `:919-928`: `!r.Coverage.laneCovered(seat.Lane)` → the 2.7 gap-advice string `"Not covered by a subscription: work of this kind (…) is assigned to kimi-cli, and the household holds no plan for it"` → `Route` falls to `noFit` with `GapAdvice` (`:651-661`) → the approval card offers subscription-gap advice and the seat is zeroed (`:1179-1183`). **That is a park, exactly what B7 forbids.** So a DutyMap execution seat of `k3/kimi-cli` cannot be the shape; and it could not be anyway — `"k3"` is a banned constant in `internal/worker` (§63 D5 scan). The smallest change that yields "ordered preference per duty, first commissioned seat wins, skipped seats named": R2 — the DutyMap keeps ONE seat per duty (every consumer, `s.seat(duty)` at `runner.go:415-420`, `modelFor`, `pinnedExecutionSeat`, `noFit`, is untouched), the anthropic seat is that row, and a `LaneOrder` orders the `covered` slice `chooseFlatLane` already builds. That is S08.8 step 3's own "configured order" made explicit data — no new resolution path, no new seat type; `AlternateSeats` keeps carrying the K3 seats from the lane documents.

**(2) Whether the self-family-judging receipt flag exists.** On the VERDICT ROW yes: `verify.JudgeMeta.SelfFamily` (`v2.go:93-98`) → `roundPayload.SelfFamilyJudge` `self_family_judge` (`record.go:67-69`, written at `record.go:170`) → history catalog `verdicts.self_family_judge`. On the RECEIPT no: `metering.Receipt` (`receipt.go:27-79`) has `Verification string` (posture) and nothing for the judge; `handleRunReceipt` (`api/reads.go:1667-1683`) serves `Surface.Receipt` verbatim. And the value is a constant: `engines.go:805` `SelfFamily: true` with `Model: j.s.modelFor("")` = the PLANNING seat. Wire shape and copy: R4.

**(3) What the pipeline does with a rubric whose GoldenSet is unmeasured.** `newVerifier` (`skeleton.go:950-968`) gates EVERY production judge construction: `UnsupervisedJudgingGate` fails on `!Measured || TPR == nil || MeasuredOn == ""` or on a pin that does not name the seat (`judgegate.go:28-34`) → `verify.NewPreambleRefusal(err)` → the verify leg raises the S07.7 verification-infrastructure decision card (`skeleton.go:921-940`, P3-RW-14 R4) — no verdict, an operator card per task, never a silent PASS, never a crash. There is NO bring-up-grade/advisory posture for an unvalidated judge in the code (`RoundRecord.Posture` is the A14 bootstrap posture of the CHECK PACK, `rework.go:99-111`, a different thing). Honest posture for the window: **there is no window.** The measurement precedes the seat flip inside the packet (§10), and `TestTQ5ShippedJudgeSeatIsTheRubricsMeasuredPin` (green at grounding) trips on any tree that flips the seat before the re-measured v3 lands beside it. A tree that flipped early would park every verification on the P-T06-5 card AND fail every e2e that reaches a verdict — the battery itself enforces the sequencing.

**(4) The golden-set re-run.** A coordinator-run measurement rider after the mechanism lands (commit 1) and BEFORE the seat flip (commit 2): `SINET_RIDER1=1 go test -p 1 -count=1 -run TestRider1GoldenSetOpus -v ./internal/adapters/claudecli/` on the operator's subscription `claude` login (setup-token; $0 cash, API-equivalent tracked from the harness's `total_cost_usd`). The executor only retargets the harness (R7) and writes the pre-registration `P3/measurements/2026-09-17-rider1-golden-set-opus5.md` (§8). What lands in `floors.go` afterwards: R6.

**(5) The serialize-by-deny E3 leg (rider 2).** Harness: `internal/adapters/claudecli/spike_test.go` — `TestSpikeSingleGatedDefer` (E1 ×1), `TestSpikeParallelSerializeByDeny` (E3 ×6), `TestSpikeDeferResumeRoundTrip` (×1); gates `SINET_B1_4=1`, `SINET_HOOK_BIN=<a built sinet>`, model via `SINET_B1_4_MODEL` (`:50-58`); the 2026-07-22 record is `P3/measurements/2026-07-22-rider2-serialize-by-deny-sonnet.md`. Second post-landing coordinator rider on the NEW executor seat that actually runs work on this host = `claude-opus-5` on `anthropic` (the interim and third-choice seat). Stated honestly: the E3 leg is a claude-CLI PreToolUse-hook mechanism; the K3/`kimi-cli` executor has no such leg (its print-mode gating was spiked at LN-7, `P3/measurements/2026-08-26-kimi-cli-print-mode-spike.md`), so rider 2 covers the claude seat only and says so. Pre-registration: `P3/measurements/2026-09-17-rider2-serialize-by-deny-opus5.md` (§8).

**(6) `runs.lane`/`substrate` and the startup commissioning line tell the truth.** Assertions: R9, committed as `TestTQ5InterimExecuteRunsOnOpus5AndNamesTheSkippedFirstChoice` (routing.decided seat + reason + `runs.lane`/`substrate` on the execute run). The startup line needs no change and is already tested (`internal/shell/lanedrain_r1_test.go:81`); with nothing placed it prints `lanes=""`, which is the truth about the seat: the execute run's seat is on `routing.decided`, not on the startup line.

**(7) ⚙ default or clamp moves.** None. No key covers the duty map or a lane order; nothing in S18 moves; no STOP condition.

---

## 3. Seams to respect

- `internal/worker` never imports `metering`/`intake`/`stage`/`adapters` (§19); the lane order is string data in `worker`, and the executor may pin `worker.DefaultLaneOrder()[worker.DutyExecution]` == `{adapters.LaneKimiCLI, adapters.LaneKimi, adapters.LaneAnthropic}` from a test package that imports both.
- `internal/metering` never imports `internal/verify` (P3-GF4 OQ5): `JudgeLine` is strings + bool, composed in `stage.Surface.Receipt`.
- `internal/stage` cannot import `internal/shell`; the router is composed in `stage.New` (`skeleton.go:134-146`).
- `internal/adapters/opencode` lane documents are the ONLY home of model ids and endpoints for the kimi lanes (§62/§63/§65); commissioning (`opencode.Commission`, `CommissionedSeats`) is untouched.
- `AlternateSeatsFor` stays EXECUTION ONLY (`routing.go:195-226`); `resolveLanePin`, `LanePinRefusal`, `PinnableLanes` untouched.
- `UnsupervisedJudgingGate` stays the pure function it is; the enforcement point stays `newVerifier`.
- The frontend (`web/src`) is untouched: the `judge` receipt member's rendering is a named follow-up for the FRONTEND.md lane.

## 4. ⚙ settings by registry name

Consumed: none new. Referenced-not-consumed: none. Added: none. `local.alias.<duty>` (`settings/index.go:705`) is the only duty-keyed ⚙ and it governs the local tier, not this map. Tally 118 / 33 unchanged.

## 5. Files expected to change

Mechanism (commit 1): `internal/worker/routing.go` (R1 comments, R2), `internal/stage/skeleton.go` (`LaneOrder` at the Router literal; `newVerifier` → `newEngineJudge`), `internal/stage/engines.go` (R3), `internal/stage/runner.go` (only if a `judgeModel` helper sits beside `modelFor`), `internal/metering/receipt.go` (R4 type + member), `internal/stage/surface.go` (R4 composition), `internal/adapters/claudecli/rider1_golden_test.go` (R7), `P3/measurements/2026-09-17-rider1-golden-set-opus5.md` + `P3/measurements/2026-09-17-rider2-serialize-by-deny-opus5.md` (pre-registrations). Data (commit 2, after the rider): `internal/worker/routing.go` (`DefaultDutyMap` values), `internal/verify/seeds.go` (v3), `internal/evals/floors.go` (R6), the sanctioned test sites (§6), the two measurement files' Observation sections (coordinator). `P3/CONVENTIONS.md` gains a §-entry at landing (coordinator).

## 6. Seat-coupled assertion sites that MUST move (sanctioned edits, by name) — and the sites that must NOT

Move (commit 2 unless noted): `internal/worker/routing_test.go:62` (`claude-sonnet-5` → `claude-opus-5`), `:332` (same), `:391-393` (`TestDefaultDutyMapShape` want-map → all `claude-opus-5`; its comment now cites the 2026-09-17 gate record); `internal/stage/route_e2e_test.go:232` (`claude-sonnet-5` → `claude-opus-5`); `internal/stage/split_e2e_test.go:66` (ceremony seat `claude-opus-4-8` → `claude-opus-5`, per its own comment "the ceremony seat carries the ratified judge model … P-T06-5"); `internal/stage/compose_e2e_test.go:211` (`ver.Composer` = the planning seat → `claude-opus-5`); `internal/verify/seeds_test.go:22-33` (v2 → v3; pin names `claude-opus-5`; comment); `internal/verify/judgegate_test.go:19`, `:41`, `:47` (the measured seat `claude-opus-4-8` → `claude-opus-5`; the blocked list at `:24` may gain `claude-opus-4-8`); `internal/verify/pipeline_test.go:61` (message text only); `internal/evals/evals_test.go:79-80`, `:120`, `:149`, `:156` (the `0.84` literal → the v3 measured bound ONLY if the rider's Wilson bound differs; the probe values `0.90`/`0.70` move with it so green/red stay on the right sides); `internal/adapters/claudecli/rider1_golden_test.go:21-36`, `:103` (commit 1). Comment-only sites: `routing.go:135`, `:142-165`; `seeds.go:116-123`, `:154-156`; `floors.go:20-45`; `engines.go:730-736`, `:801-804`.

Leave alone (data/fixtures/recorded events, not coupled to the duty map): `internal/api/gf10accept_test.go`, `internal/api/deliverables_test.go:242`, `internal/api/acceptapi_test.go`, `internal/api/projection_drift_test.go:217`, `internal/api/gf13jargon_red_internal_test.go:58`, `internal/api/apifixtures_test.go:1679`, `internal/accept/*_test.go`, `internal/shell/accept_seams_test.go:113`, `internal/shell/zai_ln2b_test.go:152`, `internal/metering/lanepin_ln9_test.go:77`, `internal/metering/metering_test.go`, `internal/history/fixture_test.go`, `internal/history/routing_test.go`, `internal/eventlog/contract_test.go:784`, `internal/evals/runbook_test.go`, `internal/evals/runner_test.go:100`, `internal/watchlist/canary_test.go`, `internal/worker/lanepin_ln9_test.go:277-310` (a template `ModelPin` literal), `internal/intake/taxonomy_test.go:78`, the `internal/memory/*` and `internal/intake/taxonomy.go` provenance strings (drafting facts), `web/src/**` (no fixture carries either id).

## 7. Adopted components touched

None. No `components.lock` change; the claude CLI pin and the opencode/kimi-cli pins are untouched; no adopted code modified.

## 8. The two pre-registrations the executor writes (Observation sections PENDING, coordinator fills)

**`P3/measurements/2026-09-17-rider1-golden-set-opus5.md`** — Title "Rider 1 — P-T06-5 golden-set run on the claude-opus-5 judge + length bias (S07.9, P-T06-3) — 2026-09-17". Why: B7 moved the judge seat to `claude-opus-5` — a judge-model change; P-T06-5 gates unsupervised judging on a re-run; this is the S07.9 gate, not a BENCH-REG evaluation (no registered number moves). Design (pre-registered, identical to 2026-07-22): `SeedGoldenSet()` 26 cases (20 planted + 6 clean) through `TestRider1GoldenSetOpus` on `claude-opus-5` via the committed claudecli adapter, simplified two-axis prompts, clean context; judge-as-classifier; TPR/TNR vs the human labels with Wilson 95% intervals (the S07.11 correction); point-biserial length bias. Command: `SINET_RIDER1=1 go test -p 1 -count=1 -run TestRider1GoldenSetOpus -v ./internal/adapters/claudecli/` (subscription lane, $0 cash; API-equivalent from `total_cost_usd`; serial per the operator's serial-tests directive). Spend projection: Opus 5 $5 / $25 per MTok (live-verified 2026-09-17, the same rate card as Opus 4.8) → 52 calls ≈ 104K in / 62K out ≈ **$2.10 API-equivalent**; **STOP LINE $5.00** (the 2026-07-22 line, unchanged): at cumulative ≥ $5.00 the harness halts and the partial is recorded; **a partial run does NOT clear the gate** — v3 stays unlanded. Pre-registered readings: the v3 catch floor = Wilson lower bound of the observed TPR (n = 20); TNR lower bound carried report-only; length bias |r| ≥ 0.10 is a P-T06-3 FINDING for the gate, not a rider failure; a TPR lower bound below the registered v2 floor 0.84 is a FINDING presented at the gate before v3 lands. What lands afterwards: R5 + R6 + the `evals_test.go` literal if it moves; optionally the S14.8 record `evals.JudgeRemeasurement(obj, "claude-opus-5", evals.Measured{…}, green)` (`internal/evals/runbook.go:273-290`).

**`P3/measurements/2026-09-17-rider2-serialize-by-deny-opus5.md`** — Title "Rider 2 — serialize-by-deny E3 leg on the claude-opus-5 executor (S02.8 carry-forward; B7) — 2026-09-17". Why: B7 moved the seat that executes on this host to `claude-opus-5`; the per-pin canary re-confirms the parallel-gate fallback on it (haiku 2026-07-20/21, sonnet-5 2026-07-22). Honest scope: the claude-CLI hook leg only; K3/`kimi-cli` has no E3 leg (LN-7 spike record). Design (exactly the committed B1-4 harness): `SINET_B1_4=1 SINET_HOOK_BIN=<built sinet> SINET_B1_4_MODEL=claude-opus-5 go test -p 1 -count=1 -run 'TestSpikeSingleGatedDefer|TestSpikeParallelSerializeByDeny|TestSpikeDeferResumeRoundTrip' -v ./internal/adapters/claudecli/`; scale E1 ×1, E3 ×6, round-trip ×1; expectations: E1 clean defer-park; E3 6/6 faithful single-call parks at ~+1 turn under ⚙ `adapter.parallel_gate_fallback = serialize-by-deny`; E2-context (does first-defer-honored-on-parallel persist on Opus 5?) is a per-pin canary FINDING either way. Spend projection: B3-3 haiku $0.13 × 5 (Opus 5 is 5× haiku on both rates) ≈ **$0.65 API-equivalent**; **STOP LINE $2.50** (the 2026-07-22 ratio ≈ 3.8× projection); installed `claude` version reported per §10, never retargeted.

## 9. Acceptance checklist (concretely checkable)

1. `worker.DefaultDutyMap()` = `claude-opus-5`/`anthropic`/200k on execution, planning, judge; utility absent; neither retired id on any seat. (`TestTQ5DefaultDutyMapIsOpus5OnEverySeat`)
2. `worker.DefaultLaneOrder()[DutyExecution]` = `[kimi-cli kimi anthropic]`; no planning/judge entry. (`TestTQ5DefaultLaneOrderIsKimiCLIThenKimiThenAnthropic`)
3. Nothing commissioned → execution resolves to `claude-opus-5`/`anthropic`, no error, no gap, reason names `kimi-cli` and `kimi`, says "first choice", no gap sentence, no citations. (`TestTQ5NothingCommissionedResolvesToOpus5AndNamesTheSkippedLanes`)
4. `kimi-cli` commissioned → `k3`/`kimi-cli` wins regardless of the alternates' handed-in order; only `kimi` → `k3`/`kimi`, `kimi-cli` named as skipped; coverage still binds. (`TestTQ5CommissionedKimiLanesWinInTheConfiguredOrder`)
5. With comparable gauge readings the gauge still decides and the reason says so; a nil `LaneOrder` keeps the duty-map seat first. (`TestTQ5LaneOrderIsTheTieBreakNotAGaugeOverride`, `TestTQ5NilLaneOrderKeepsTheDutyMapSeatFirst`)
6. `stage.New` composes `LaneOrder: worker.DefaultLaneOrder()`; at the real call site the interim execute run records `routing.decided` `claude-opus-5`/`anthropic`, the skipped lanes in `plain_reason`, no `gap_advice`; the card's routing block already names `kimi-cli`; `runs.lane`/`substrate` = `anthropic`/`claude-cli`. (`TestTQ5InterimExecuteRunsOnOpus5AndNamesTheSkippedFirstChoice`)
7. `EngineJudge.Meta().Model` = the JUDGE seat (not planning). (`TestTQ5JudgeMetaNamesTheJudgeSeatNotThePlanningSeat`)
8. `newEngineJudge(sk, executor).Meta().SelfFamily` computed: `claude-opus-5` → true, `claude-sonnet-5` → true, `k3` → false, `glm-5.3` → false, "" → the configured execution seat's answer; `sameFamily` table. (`TestTQ5SelfFamilyIsComputedFromTheExecutorThatRan`, `TestTQ5SameFamilyRule`)
9. The judge session's `SessionInput.Model` equals the seat `Meta()` names when `cfg.Model` is empty and planning ≠ judge. (executor-materialized `TestTQ5JudgeSessionRunsOnTheJudgeSeat`, internal, recording-adapter pattern of `internal/stage/lanecommission_ln4_test.go:100-180`)
10. `metering.Receipt.Judge *JudgeLine` additive (`judge.model`, `judge.self_family`, `judge.note`), absent = byte-identical. (`TestTQ5ReceiptJudgeLineIsAdditive`)
11. `Surface.Receipt(ctx, verifyRun)` on a drained task serves `judge.model == "claude-opus-5"`, `judge.self_family == true`, a non-empty citation-free note; the verify.round row's `judge_model`/`self_family_judge` agree. (executor-materialized `TestTQ5ReceiptCarriesTheJudgeLine`, template `internal/stage/bootstrap_gf4_test.go:100-140` `h.sur.Receipt(ctx, verifyRun)`)
12. Rubric v3: pin `claude-opus-5`, gate passes under it, `MeasuredOn ≥ 2026-09-17`, TNR present, length bias measured on `claude-opus-5`, items verbatim; retired seats refused. (`TestTQ5RubricV3PinsOpus5AndIsMeasuredOnIt`, `TestTQ5RetiredJudgeSeatsAreRefusedByTheGate`)
13. Seed floor keyed v3, basis names `claude-opus-5` + the measurement file + Wilson, TNR companion present, unratified. (`TestTQ5SeedFloorTracksTheOpus5Measurement`)
14. At every commit the shipped judge seat passes the gate under the shipped rubric. (`TestTQ5ShippedJudgeSeatIsTheRubricsMeasuredPin`, green tripwire)
15. Property (executor-materialized `TestTQ5LaneOrderProperty`, 200 fixed-seed draws, the §65 precedent): for any coverage subset of `{anthropic, kimi, kimi-cli, zai}` containing `anthropic`, any handed-in alternates order, `Pressure` nil — the chosen lane is the first covered lane in `[kimi-cli kimi anthropic]`, every listed lane ahead of it that is uncovered is named in the reason, `gap == ""`, `err == nil`.
16. Both pre-registration files exist with the §8 content; `rider1Model == "claude-opus-5"`.
17. `TestNoLaneValueIsAConstantInRoutingOrShell` and `TestNoKimiLaneValueIsAConstantInTheAdapter` stay green; no `"k3"` in non-test worker/shell/stage sources.
18. All §6 sanctioned sites moved; no other pre-existing test changed; `go test ./...` green at commit 2; `gofmt`/`vet`/lockgate green; tally 118/33.
19. Both riders executed by the coordinator with Observation sections filled; rider 1 PASS (full 26 cases, under the stop line) is a precondition of commit 2.

## 10. Landing sequence (binding — the P-T06-5 gate forces it)

- **Commit R (this grounding, `P3-TQ-5: grounding …`):** brief + red tests. The `internal/worker`, `internal/stage` and `internal/metering` TEST binaries do not compile (red-by-compile on the R2/R3/R4 identifiers — §65 D10 precedent; a bisect landing here should `git bisect skip` it). `go build ./...` is green; `internal/verify` and `internal/evals` compile and are red on values.
- **Commit 1 (executor, the MECHANISM; red window declared in the message):** R2, R3, R4, R7, the two pre-registrations. Green: every `TestTQ5*` except the four DATA tests (`TestTQ5DefaultDutyMapIsOpus5OnEverySeat`, `TestTQ5RubricV3PinsOpus5AndIsMeasuredOnIt`, `TestTQ5RetiredJudgeSeatsAreRefusedByTheGate`, `TestTQ5SeedFloorTracksTheOpus5Measurement`) and the e2e `TestTQ5InterimExecute…` (red on the model until the seat flips). The old seats still ship, so production behaviour is byte-identical except that the execution reason now names the skipped first choices (honest either way). Battery otherwise green.
- **Coordinator riders (between commit 1 and 2):** rider 1, then rider 2 (§8), serial, subscription lane.
- **Commit 2 (executor, the DATA; closes the red window):** R1, R5, R6, §6 sites, measurement values. Whole battery green; the tripwire (checklist 14) stays green because the seat and the pin move together.
- If the coordinator cannot run rider 1 promptly, commit 2 does NOT land and the packet stays open at commit 1 — never an unmeasured v3, never a judge seat the rubric was not measured on.

## 11. CONVENTIONS constraints that bind

§2 (no ⚙ as constants; stdlib-first; doc comments cite sections, no research narration); §3 (tests-first carve-out: red window declared, `go build ./...` green, packet paths only; measurements are not Go tests); §5 (commit subject `P3-TQ-5: … (S06.10, S07.5, S07.9–S07.11, S08.8)`; stage by pathspec; never edit `Spec/`, `Docs/`, `Research/`, `P3/STATE.md`); §11 (receipts materialize per run-end; the ledger never prices flat lanes); §19 (selection deterministic, dollar-free; `routing.decided` per run; the duty map is code DATA, gate-flagged; readings (2) and (4)); §28 (Def.8 probe files pre-registered; BENCH-REG read-only); §38 (requester copy in plain words, no citations); §63 (call-site lesson: prove at `stage.New`/dispatch, not only at the resolver; no model id constants); §65 (commissioning startup-bound; the fill's tests must not change expectation); §67 (kimi-cli model per invocation from the decision; one pool); §68 (lane pin bypasses the gauge; `SetDecidedLane` no-op when nothing moved); memory `serial-tests-gpu-only` (`-p 1`, one package at a time, paid legs opt-in only); memory `fable5-safeguard-false-positive` (executor on Opus).

## 12. STOP conditions found

None. No ⚙ default or clamp moves; no S00.9 row; no adopted component modified; no amendment owed.
