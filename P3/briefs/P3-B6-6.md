# P3-B6-6 brief — Decision surfaces: approval inbox + settings UI

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.6** (approval inbox) + **S15.9** (settings UI) owning, **S15.2/S15.3/S15.12** binding context (`Spec/drafts/S15-frontend-api.md`); **S06.9** (plan-card contract, staleness, resume-in-place; `S06-intake-pipeline.md`); **S14.4–S14.7** (watchdog/conformance/drift/benchmark card semantics; `S14-observability-evals-watch.md`); **S01.9/S01.10** (step-up; the one-artifact-set registry rule; `S01-process-architecture-shell.md`); **BENCH-REG §3/§12/§13/§14/§15** (`Spec/benchmark-preregistration-v1.md` — signed; registered numbers are read-only data); **FC-v1 §4** (`Spec/frontend-components-v1.md` — the JSON Forms pick and its decision notes). `P3/CONVENTIONS.md` binds: **§5, §7, §9, §30, §38, §39 (+B/C), §40, §41 + §41-B ENTIRE, §42 + §42-B ENTIRE**. **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is packet 10/13 of B6** (queue `P3/STATE.md` §B6; B6-1/2A/2B/2C/3A/3B/3C/4/5 are landed). It fills the three B6-6 routes — `/inbox`, `/inbox/:id`, `/settings` (`web/src/routes.ts:45–47`, owner `'B6-6'` → `''`) — as the **first consumer of the whole B6-2 decision plane and the B6-3A settings family**. It builds web-side views over landed verbs and reads; it re-implements NO server machinery, and the Go side changes ONLY per OQ dispositions (each a pre-raised, grounded read gap — never a new route family invented by the executor, never a migration, never a new event type). The assistant (B6-7), review surfaces + the escape-allowlist widening (B6-8), push/re-nag SLA + the S15.12 sweep (B6-9) are NOT this packet — their stubs stay.

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit — S15/FC-v1 frontend scope is not S10/S11 security-dense; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-29 — do not relitigate)

**A. The approvals data plane (`internal/api/approvals.go`; routes `api.go:380–385`).**

- **`GET /api/approvals`** → `ApprovalList{items[], cursor, truncated}`; **`ApprovalItem`** (approvals.go:115–158) = {id `"<kind>:<native-id>"`, kind, owner, run_id?, tier, **answerable** (+`not_answerable_reason` naming where the verb lives), **batchable**, **step_up_required**, payload_hash?, observed_ts, **expiry_at** (= observed + ⚙ `effects.approval_expiry`), engine_expiry_ts?, **stale** (+`stale_reasons`), **actions[]** (the card's OWN verb vocabulary), approvals? (`EffectApprovals`), card (raw JSON)}. No percent/fraction/ETA field exists and none may be added (§30).
- **Kinds** (approvals.go:72–80): `ask`, `effect`, `watchdog_flag`, `conformance_card`, `drift_card`, `benchmark_verdict`, `benchmark_alarm` — plus the **eighth**: a failed benchmark pair rides the `benchmark_verdict` id space at tier Low with `actions:["decline"]` only (approvals.go:341–352). **Ranking** = tier (High→Medium→Low, tierRank :93) then oldest-first then id (:196–205); list cap structural (:53–59).
- **Tier mechanics in ONE place** (`applyTierRules` :447–452): `batchable = answerable && tier==low`; `step_up_required = answerable && tier==high`. Effects are ALWAYS High (:266–273). The three oversight verbs' cards deliberately skip tier rules — their tier is a severity RANKING (`severityTier` :387–395: flag-now→high, else medium), not a batching/step-up gate (:290–302).
- **One authority family, read by card AND verb** (D9: the inbox never renders a control whose use would be refused): `mayAnswerAsk` = owner only, operator included in the refusal (:417); `mayAnswerEffect` = owner, + operator iff platform-level (:421–423); `mayAnswerWatchdogFlag` = owner or operator (:429–431); `mayAnswerPlatformCard` (drift/conformance) = operator (:436); `mayAnswerVerdict` = requester ONLY, operator refused (benchmark.go:407); `mayDisposeAlarm` = operator (benchmark.go:413). Every non-answerable card carries its served reason — render it, never a dead control.
- **Ask cards**: snapshot served VERBATIM as `card` — including the **13.5 help block** (`{what, wrong, recommend}`, intake/intake.go:282–286; drafted intake/cards.go:245–255) and the S06.9 Layer-1/Layer-2 bodies; actions derive from the snapshot's own `approval.actions` / top-level **`choices`** (the Go field is `Verbs`, the wire key is `choices` — the B6-3B drain fix, approvals.go:496–520) / `decision.choices[].id`; an untiered card defaults Medium (:561–583). Stored staleness OR pending-age > ⚙ `freshness.max_age` sets `stale` at read; the served reason text names one-click re-plan (:454–473).
- **Answer verbs**: `POST /api/approvals/{id}/answer`, body `{payload_hash, pin?, answer}` (:764–768) → `ApprovalAnswerResult{id, applied, state, result?, approvals?, detail}` (:771–784). Retry-safety verbatim: stale hash → **409 `stale_payload` carrying `current: <the fresh card>`** (:1354–1368) — the UI re-reads and re-renders, never guesses; repeat-same-answer → 200 `applied:false` + recorded state; conflicting answer → 409 `already_answered`; deny-then-approve on an effect → 409, denial is terminal (:1217–1238). **Batch**: `POST /api/approvals/answer-batch`, `{items:[{id, payload_hash, answer}]}` ≤100 (:67, :859–866) → per-item `ApprovalBatchOutcome{id, status, code?, detail?, result?}` + cursor — a batch is transport convenience, each item individually validated/applied/logged, one refusal leaves the rest untouched (:820–889); a non-Low item in a batch → 409 `not_batchable` (:1159–1165).
- **Step-up** (:1171–1190): High tier demands the actor's PIN **in the same request** (`pin` field) → `sessions.VerifyPIN`; missing PIN → **401 `pin_required`** (`ErrPINRequired`, intake_handlers.go:60–63) — the UI prompts (card already says `step_up_required`) and re-posts; wrong PIN → 401 `pin_rejected`; the dev identity can never step up. `POST /api/auth/verify-pin` (api.go:341) exists as a naked 204 step-up verb — the landed decision flows do NOT use it (same-request `pin` is the contract; accept.go:327–331 is the same shape).
- **Effect co-approval**: `EffectApprovals{platform_level, owner_approved, operator_approved, owner_approved_by?, operator_approved_by?}` (:99–107) — DERIVED from `decision.recorded` rows, **cycle-scoped** (hash + expiry window, :677–725); platform-level = run-less effect (:606–617). A signature that completes nothing returns 200 `state:"proposed"` + the updated approvals block + a detail saying both signatures are needed (:1093–1107) — render it as recorded-and-waiting, both states visible (S15.6).
- **Answer body vocabulary**: effects take `{action:"approve"|"deny", reason?}` (:1005–1017); intake asks take the S06 `Answer` members — approval/delta cards `{action, contest?}` (the S06.9 structured Re-plan entry rides `contest`), interview `{answers[], assume[], force_proceed}`, escalation `{text}`, coverage/research `{choice, criteria[], facts[]}` (intake/cards.go:186–202). The UI composes the answer in the card's own served vocabulary; the pipeline's refusal is the validator.
- **Audit**: effect approve/deny co-mints `decision.recorded` (family 5, :1269–1347); every other act mints its own canonical row — the UI mints NOTHING and renders decisions from served state.

**B. The oversight verbs (`internal/api/oversight.go`; routes `api.go:392–398`).** `POST /api/watchdog/flags/suppress` body `{run_id?, anomaly_class, reason?}` → `FlagSuppressed{run_id?, anomaly_class, suppressed, detail}` — the detail names the ⚙ suppress→retune mechanic; visibility IS authority, invisible flag → 404 (:80–155). `POST /api/runs/{run}/resume` — path-only, owner-scoped; the "resume — I was wrong" verb; an open-ask park refuses with a pointer (B6-2B, render the refusal verbatim) (:159–186). **The watchdog card's served `actions` is `["suppress"]` only (approvals.go:306) — the resume door is NOT in it; OQ9 dispositions how the card renders it.** `POST /api/approvals/{id}/dismiss` (id `drift_card:<fingerprint>`) body `{reason?}` → `DriftDismissed{card_id, fingerprint, window_start_seq, dismissed, detail}` — incident-window-scoped, findings stay queryable (:188–262). `POST /api/approvals/{id}/acknowledge` (id `conformance_card:<row id>`) body `{reason?}` → `ConformanceAcknowledged{card_id, row_id, last_run_ts, acknowledged, **still_red: true**, detail}` — an acknowledgement is not a pass; a later red is a new obligation (:271–339). `POST /api/meters/pause` (api.go:393) is the S15.2 pause-my-automation switch — **no landed surface consumes it; NOT this packet's scope** (seams §2).

**C. The benchmark verdict backend (`internal/api/benchmark.go`; routes `api.go:405–409`).** `GET /api/benchmark/verdicts` → `BenchmarkVerdictForms{pairs, guess_required: true, detail}`; `pairs` = `[]benchmark.PendingPair` verbatim: {pair_id, user_id, domain, task_id, sampled_ts, render_a, render_b, length_a, length_b} — **no arm, no position, no model, structurally** (store.go:248–272); the two renders are product CONTENT, deliberately un-redacted (benchmark.go:126–139). `POST /api/approvals/{id}/verdict` (id `benchmark_verdict:<pair>`) body `{verdict, guess}` → **the §14 record commits FIRST; the reveal is a read of the committed record**, returned as `VerdictRecorded{pair_id, recorded, reveal?, detail}` — a failed reveal read-back still returns `recorded:true` with a detail; render that branch honestly (:161–194). `POST .../decline` → `VerdictDeclined` — a §14 record with no verdict/guess/artifacts; declining is visible, never silence (:196–218). `POST .../dispose` (id `benchmark_alarm:<domain>#<epoch>`) body `{disposition, reason?}` → `AlarmDispositioned{card_id, domain, disposition, cleared, detail}`; operator-only (:261–335). `POST /api/benchmark/opt-in` `{person?, enabled}` — SELF-only, operator refused (:337–391). **Vocabulary**: verdict ∈ {"A","B","tie","both-bad"}, guess ∈ {"A","B"} mandatory for every choice — frozen §3.3 text living ONLY in `internal/benchmark` (benchmark.go pkg:105–163; `NewAnswer` makes a guess-less verdict inexpressible); `internal/api` deliberately restates none of it (benchmark.go:17–26) — **OQ4 dispositions where the form's button vocabulary comes from.** The §12 disposition vocabulary is likewise package-validated registered text.

**D. The NINTH inbox kind — memory conflicts (B6-3 OQ6(a) + the B6-3C landing; `internal/api/memory.go`, `internal/memory/browse.go`).** `memory.Conflict` = {conflict_id, **affected_owner**, entry_id, other_entry_id, topic_key?, question, status (`open`|`resolved`), detected_ts, resolved_by?, resolved_ts?} (browse.go:135–146). **`mayAnswerConflict(c, caller) = c.Affected == caller` is the ONE expression of visibility AND authority** (memory.go:602–607): edges show on entry detail only to the addressee (B6-3C drain D2 — third parties and the non-affected operator see the entry WITHOUT the edge block), and the resolve verb accepts only the addressee — **the operator has NO limb**, deliberately. `POST /api/memory/conflicts/{conflict}/resolve` (path id = the int64 row number, no body) → `MemoryConflictResolved{conflict, detail}`; repeat → 200 already-closed (memory.go:619–661); 404-before-403. **Resolve mints NO event — the ratified F7 reading: the row IS the record** (resolved_by/resolved_ts). **No read lists open conflicts BY ADDRESSEE**: the browse store has only `EntryConflicts(entryID)` and `Conflict(id)` (browse.go:72–184) — a grounded gap, OQ1. **No reliable live frame exists for the addressee on either edge**: the Knowledge family routes to NO SSE topic (topics.go:55–75) and relay frames are owner-scoped to the WRITER, whose conflicting write is another person's event — the kind derives from stored state via REST re-reads (the derive-from-log/no-tickers discipline), and the idempotent already-closed answer makes a stale card harmless.

**E. The settings data plane (`internal/api/settings.go`; routes `api.go:422–427`; `internal/settings/uischema.go`, `read.go`, `schema.go`).**

- **`GET /api/settings`** → `SettingsView{schema, uischema, values[], domains[], registered?/registered_absent, editable, editable_reason}` (settings.go:70–117). Schema + UISchema are the registry's OWN emissions served verbatim (S01.10(b): one artifact set — UI, validation, docs cannot drift). `values` = `[]settings.Value` (read.go:26–67): {key, section, domain, title, **help**, type, unit?, ratified_by, dormant?, default, **effective**, **overridden**, user_values?, numeric, **floor?/ceiling?** (EFFECTIVE bounds), min_exclusive?, enum?, auto, per_user, **restart_required**} — a member's `user_values` is narrowed to their own (:124–140). `registered` = the S18.4 R9 frozen block through the benchmark seam, each value carrying its own read-only marker; absent with reason when unwired (:150–159).
- **The UISchema emitter** (uischema.go): root `Categorization` → one `Category` per S18 domain in declared order (label `"<domain> (<section>)"`), one `Control` per key with `scope: "#/properties/<seg>/properties/<seg>…"` (**nested JSON-Pointer hops** — controlScope :117–119), label = Title, `options` = the badge facts {unit, auto, perUser, restartRequired, dormant} (:131–152); `Group` only where ≥2 keys share a parent path (:154–172). **v0 EMITS NO RULE — a recorded decision** (:24–35, `TestUISchemaEmitsNoFabricatedRule`): badges are facts, not visibility conditions; the rule capability is structurally present for a later packet. The emitted JSON Schema carries `description` = the help text per leaf (schema.go:62) and `x-sinet` metadata (:122).
- **THE NESTED-DATA BRIDGE (the B6-4 carry, §41-B:887, re-verified here):** JSON Forms composes data paths by splitting scopes on DOTS — the nested `#/properties/a/properties/b` scopes bind against a NESTED data object `{a:{b:…}}`. The API serves `values[]` as a FLAT list keyed by dotted key. **A flat dotted-key data object renders every control empty.** The UI must build nested data from `values[]` (and map edits back to per-key writes) — OQ2 dispositions the exact bridge.
- **Writes are operator-only, structurally** (the registry's actor model admits no member kind — B6-3 OQ8; settings.go:26–39): `editable`/`editable_reason` on the read states per-caller whether controls may render at all (:88–91, :452–461); the dev identity reads and never administers. `POST /api/settings/{key}` body `{value, for_user?, reason?}` — null/absent value = reset-to-default (deletes the override row) → `SettingsWritten{key, reset, value, for_user?, detail}`; the restart-required case says so in the served detail (:203–265). `POST /api/settings/{key}/bounds` `{floor, ceiling, reason?}`, both-null = reset to the ratified clamp — **the G1 rider 1 split: automation moves value only within bounds, only the operator moves bounds** (:267–307). Errors map on TYPE (`ErrInvalidRequest` et al., :503–519), never message text.
- **`GET /api/settings/{key}/history`** → `SettingsHistory{key, entries[], limit}`; `AuditEntry` = {id, actor, key, user_id?, old, new, ts, reason?} newest-first from `settings_events`, values verbatim (read.go:122–180).
- **The price table** (S18.3 over append-only migration 0019): `GET /api/settings/prices` → `StoredPricesView{rows, version, posture?, deferred[], editable}` — rows are the metering package's OWN marshaled shape, opaque to the transport (`StoredPriceRow`: {id, model, lane, unit_prices{input_usd, output_usd, cache_read_usd,…}, effective_from, verified_on, source, created_by, created_ts, reason?}, pricestore.go:62–107); the EMPTY table ships a posture sentence (UNPRICED is the shipped stance, never silent $0) and `deferred[]` names the two S18.3 surfaces not built, with reasons (settings.go:317–372). `POST /api/settings/prices` `{row, reason?}` → `PriceRowAdded{row, version, detail}` — an effective-dated APPEND; past rows are immutable by trigger; **no edit or delete verb exists and none may be faked** (:374–422).

**F. The web machinery (landed B6-4/B6-5 — consumed, never rebuilt).** Views are components in `web/src/` swapped into App.tsx's route switch (App.tsx:83–101); URLs never change; deep links ride `hrefFor` (routes.ts:100–107) — **card ids contain `:`, `\x1f` (watchdog: `run\x1f class`) and `#` (alarm: `domain#epoch`), so `/inbox/:id` paths MUST be built via hrefFor's `encodeURIComponent` and read back via the router's decode, never string-concatenated.** Live wiring is `useLive({key, read, types, applies?, stream?})` (live.ts:90–198; §42): REST is the truth, frames are the trigger, per-subscriber debt cleared only by a read started after the signal, bursts coalesce without a timer; each view DECLARES its trigger types (§41-B). Golden fixtures per consumed read under `web/src/fixtures/api/` with the compare-only Go assertor (`apifixtures_test.go`) and vitest doubles importing the same files (§42 OQ2 mechanics — fixed-clock, regeneration env-gated; `approvals.json` already exists and grows). Typed reads/verbs extend `web/src/api.ts` (`api.approvals()` :518 exists). Escape scan allowlist EMPTY (§41-B:883; widens at B6-8, in B6-8's commit). Rendered honesty via `parts.tsx` primitives (§42): absences with reasons, money as served, timestamps verbatim, no progress/percent/ETA primitive anywhere.

**G. Grounded read gaps + an independent observation for the coordinator.**

1. **`eval.score_recorded` has NO deterministic serving read** (the D4(b) carry): grep of `internal/history` finds no Layer-0 view and no Layer-1 catalog query over it (the `score` column at catalog.go:345 is `routing_quality`); the rows (minted by `internal/conformance.RecordResult`, conformance.go:46/140, co-produced by S14.8 evals) are reachable at v0 only via Layer-2 open-SQL or redacted search. The queue row demands them visible — OQ5.
2. **The B6-5 done-directly dispute — independently confirmed on this session's own reads** (grounded fact, not relied on the in-flight evaluation): the `cost.done_directly` catalog query SELECTs per-RUN columns ({run_id, …, label, heuristic_usd, …}) from the `cost_done_directly` Layer-0 view (catalog.go:507–512; layer0.go:33) — per-run heuristic lines only; the §13.2 AGGREGATE (`DirectUseAggregate`, `benchmark.Practice.DoneDirectly`, feed.go:64) is reachable ONLY through the in-process readout (readout.go:297/347) — **no REST route serves the aggregate**, and the api `BenchmarkSurface` (benchmark.go:56–92) has no readout method. §42-B:918 records the same. Nothing in B6-6 depends on the aggregate; the benchmark READOUT surface (win rates + the BENCH-REG §15 honest-claims table, a frozen obligation WHEREVER a win rate renders) is an unserved, unqueued surface — named for the B6 gate list, not built here.
3. **The alarm card's `dispose` flow and the verdict form need registered vocabularies the API deliberately does not restate** — OQ4.

**Counters at open (restated at launch; assert before changing anything):** migrations **0001–0019** (`user_version` 19; 0019 last); event inventory **91 minted / 5 declare-only / 96**; ⚙ **118 keys / 33 domains** (`internal/settings/index.go` byte-unchanged); `components.lock` **29**; conformance seed rows **12** (test-pinned); CONVENTIONS **§1–§42 (+§42-B)** — this packet appends **§43**; `web/` **134 vitest tests / 15 files**; **43 Go test packages** green.

---

## 1. Numbered requirements

### A — The approval inbox (`/inbox`, `/inbox/:id`) [S15.6; S3.2; 13.5]

- **R1. The one ranked queue** [S15.6]. `/inbox` renders the caller's `GET /api/approvals` list in SERVED order (risk-ranked server-side — the client never re-ranks); every kind present including the eighth (failed-pair) and — per OQ1's disposition — the ninth (memory conflicts). Display classes (proposals / questions / sign-offs / escalations) are presentation grouping over served kinds, never a filter that hides a card; tier, owner, and answerability are visible per card. Cards the caller cannot answer render read-only WITH the served `not_answerable_reason` — never a dead control (the D9 principle, now on screen).
- **R2. Cards explain themselves** [S15.6; 13.5; S06.9]. The ask card renders its stored snapshot: the Layer-1 phone screen (restatement, what-you-get, numbered steps, what-I-will-NOT-do, the assumptions list as centerpiece, cost/time/Clearance, the 13.5 help block {what, wrong, recommend}) with Layer-2 expandable below; for settings-class approvals the help text is registry-sourced (it arrives IN the snapshot — render as served, never re-drafted client-side). All card content escapes by default; model-derived strings are text, never markup.
- **R3. Tier mechanics, rendered from served flags** [S15.6]. Low: `batchable` cards are selectable; **one action answers a selected set** via the batch verb, each item individually listed BEFORE the act and its per-item outcome individually rendered AFTER (partial failure is per-item — a refused item shows its own code/detail beside applied siblings). Medium: individual, no batch affordance. High: never batchable; **the PIN re-prompt flow** — the UI collects the PIN (type=password, cleared after submit, never stored/logged — §41-B session discipline) and sends it in the SAME answer request; a 401 `pin_required`/`pin_rejected` re-prompts honestly. Under co-approval, **both approver states render from the served `approvals` block** (owner ✓/✗, operator ✓/✗, by whom) and a completes-nothing signature renders as recorded-and-waiting from the served state + detail (OQ8).
- **R4. Plan cards ALWAYS carry Approve + Re-plan** [S15.6; S06.9]. The approval/delta card's actions render from its served `actions` vocabulary (Approve · Re-plan · Re-interview as served); **Re-plan is the structured entry** — contest rides the answer's `contest` member referencing the tapped AC/assumption/step (S06.9's bounded-delta shape, composed in the card's own vocabulary). Delta cards render their ADDED/MODIFIED/REMOVED presentation from the snapshot.
- **R5. Staleness + expiry, from served fields only** [S06.9; S15.6; G2 Def.2]. `stale:true` renders the "assumptions may be stale" flag with its served reasons and **one-click re-plan** (the re-plan action pre-selected — one tap, then the structured entry); the flag never blocks approving. `expiry_at` renders as a countdown (display derivation of a served instant — allowed; inventing an expiry when the field is absent is not); `engine_expiry_ts` renders verbatim beside it when present.
- **R6. Answering resumes-in-place, and the UI says so honestly** [S15.6; S15.2]. After any answer: render the verb's response (`applied`, `state`, `detail` — the resumed/recorded/already-resolved stories are SERVED text) and re-read the inbox; a 409 `stale_payload` re-renders from the carried `current` card with a visible "this card changed" notice — never a silent swap, never a blind retry.
- **R7. Watchdog flag cards** [S14.4]. Trigger evidence + Tier-1 annotation render from the card; **"resume — I was wrong" is first-class** per OQ9's disposition (the run-scoped resume verb; its open-ask-park refusal renders verbatim with the pointer); **suppress** posts the card's own `{run_id?, anomaly_class}` and renders the served suppress→retune detail (the ⚙-driven threshold-raise proposal story).
- **R8. Drift + conformance cards** [S14.6; S14.5]. Drift: dismiss (operator-only per served answerability) with optional reason; the response's incident-window scope renders (findings stay queryable; a new window opens a new card). Conformance red: acknowledge; **`still_red: true` renders as exactly that** — an acknowledgement is never displayed as a pass, and the card stays listed until a real green run delists it server-side.
- **R9. Blind-pair verdict forms** [S14.7; BENCH-REG §3/§14]. The verdict card + `GET /api/benchmark/verdicts` render the two responses through the served side-keyed fields (render_a/render_b + the length figures as served); **the arm-guess is MANDATORY in the same form** (`guess_required` is served; the form cannot submit without both verdict and guess — and the backend's `NewAnswer` refuses it regardless); **reveal renders ONLY from the verdict response's `reveal`** (or a post-record read) — the pre-record surface carries no arm/position/model field to leak, and the view must not fabricate one (a test asserts the form renders only served PendingPair fields); the reveal-absent branch (recorded, read-back failed) renders the served detail as success-with-late-reveal. **Decline** is a first-class button beside the verdict (§4.2.5: declining must be visible, not look like ignoring); the failed-pair card offers decline only. **Alarm cards** (High, operator): the §12 disposition flow per OQ4's vocabulary disposition, reason attached, response detail rendered.
- **R10. The ninth kind: memory-conflict question cards** [B6-3 OQ6(a); B6-3C F7] — per OQ1's disposition. The addressee (and ONLY the addressee — no operator limb) sees each open conflict as an inbox card: the question, both entry refs, detected_ts; **resolve** posts the landed verb; the already-closed repeat renders the served detail. Currency is honest: the kind derives from stored state on the inbox's re-reads (no SSE frame exists for it — a note in the view, not a ticker to compensate).
- **R11. Stable deep-link URL per card** [S15.11; §41-B]. `/inbox/:id` renders one card (the push `navigate` target); ids round-trip through hrefFor/router-decode including `:`/`\x1f`/`#` natives; an id not in the caller's list renders the honest not-found/not-visible state. `/inbox` and every card state is phone-complete (S15.12: the inbox and every decision).
- **R12. Live inbox** [S15.12; §42]. The inbox rides `useLive` with declared types chosen against the topic map (the Gate-ask / Human-decision / Watchdog / Drift / Benchmark families' minted types — enumerate in code with the §42 per-type reasons); re-reads on frames and resnapshot signals; stale renders visibly; after any own verb the view re-reads. No polling loop, no ticker, no second stream.

### B — The settings UI (`/settings`) [S15.9; S01.10; FC-v1 §4; 13.4/13.5]

- **R13. Generated, never hand-built** [S15.9]. JSON Forms (`@jsonforms/core` + `@jsonforms/react` 3.8.0, installed at B6-4, probe-green) consumes the SERVED schema + UISchema verbatim — the client hand-builds no form tree, hard-codes no key list, and renders Categorization tabs (33 domains) + Groups + Controls exactly as emitted. **The Sinet-owned renderer set is THIS packet's web work** (FC-v1 §4; S15.4: no theme-package dependency) — renderers + cells for the emitted vocabulary (Categorization/Category/Group/Control over string/number/int/bool/duration/enum leaves), owned CSS, tested per renderer.
- **R14. The nested-data bridge** [§41-B:887] — per OQ2's disposition. Values bind through a NESTED data object built from the served flat `values[]` (dotted keys split into object paths, binding `effective`); edits map back to **per-key** `POST /api/settings/{key}` writes. A driven test proves the bridge against the REAL served shapes (fixture): every control renders its value (the flat-object empty-render defect is pinned as a regression probe).
- **R15. EVERY setting present + editable, including the price table** [S15.9; 13.4]. All 118 keys render (a completeness test derives the expectation from the served values[]/schema, never a hand count); the price table renders as its own surface (R21); the registered block renders read-only with its per-value markers; `dormant` badges render. Editability follows the SERVED `editable` flag: for the operator every value control is live; for a member the WHOLE write surface renders read-only with the served `editable_reason` — the UI never renders a control that will 403 (OQ3).
- **R16. Clamp bounds shown; the value-vs-bounds split enforced** [S15.9; G1 rider 1]. Each numeric key shows its EFFECTIVE floor/ceiling (served — never re-derived) beside the control; the value editor clamps client-side for UX but the registry's validation is the authority (server refusals render verbatim); **bounds editing is a separate, operator-only affordance** posting the bounds verb — never conflated with value edits; `auto` keys badge that automation may move the value within bounds.
- **R17. Restart-required badged; write outcomes honest** [S15.9; S01.10]. `restart_required` badges from the served flag; a write's response `detail` renders verbatim (the running-process-keeps-the-old-value story); `overridden` distinguishes explicitly-set-to-default from following-the-default, and **reset-to-default** is a first-class act (null value) whose meaning the UI states from the served detail.
- **R18. Per-setting audit history** [S15.9]. Each key opens its `GET /api/settings/{key}/history` rows: actor, old → new (verbatim JSON values), ts, reason, user_id where per-user — newest first, as served.
- **R19. Inline help** [S15.9; 13.5]. The registry help renders inline per control (the schema's `description` / the Value's `help` — one source, rendered where the person decides); unit renders from options/Value.
- **R20. Per-user overrides, honestly scoped** [S15.9; S01.9]. `user_values` render per key (the operator sees all users'; a member their own — as served, no client filtering to create privacy); the operator's `for_user` write path is an explicit target-user affordance on PerUser keys (OQ3).
- **R21. The price-table surface over the append-only store** [S15.9; S10.3; S18.3] — per OQ6's disposition. Rows render verbatim (model, lane, unit prices, effective_from, verified_on, source, attribution); the EMPTY table renders the served posture sentence prominently (UNPRICED is a stance, not a fault) and `deferred[]` renders as the stated decisions they are; **add-row is the ONLY mutation** (operator-only, effective-dated, reason-carrying) and the UI offers no edit/delete affordance — immutability is rendered as a property, not hidden as a limitation; the append response's version + re-validation story renders.
- **R22. The eval-results surface** [D4(b); S14.5/S14.8; P-T11-4] — per OQ5's disposition. `eval.score_recorded` results readable in the settings area: suite/asset, score vs floor, outcome, when — from the dispositioned read, rendered as served (scores are data, never client-judged). This is the surface whose existence the B6 gate records as the D4(b) paid-sweep unlock.

### C — Cross-cutting

- **R23. Escape-by-default; no client-side side-truths** [S15.12; §41-B; §42]. Allowlist stays EMPTY; zero `dangerously*`/`innerHTML`/`eval` anywhere new (the scan grows over the new tree); card content, questions, help text, audit values render as text; no localStorage/sessionStorage state; **the PIN exists only in the in-flight request**; no client-side money arithmetic; no percent/fraction/ETA anywhere (scans extended).
- **R24. Counters + untouched machinery** [§29–§42 precedent]. NO new npm dependency (lock stays **29**; JSON Forms is installed — wiring is consumption); NO new ⚙ key (**118/33**, `index.go` byte-unchanged); NO migration (0001–0019 untouched); NO new event type (**91/5/96**); `package.json`/`package-lock.json` byte-unchanged; Go-side changes ONLY per OQ dispositions with tests and scans extended over changed shapes; `internal/settings/index.go`, `internal/eventlog/contract.go`, `internal/benchmark/registered.go`, BENCH-REG, `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed.
- **R25. CONVENTIONS §43 appended** (confirm the next free number at launch): the OQ dispositions as landed, the renderer-set inventory, the nested-data bridge, the per-view type declarations, new structural constants with reasons, this packet's readings. Earlier sections never rewritten. Commit subjects per §5 (`P3-B6-6: … (S15.6, S15.9, FC-v1 §4, S14.7, S01.10)` + trailer).
- **R26. Battery + hermetic + $0** [§3/§5; §41]: `gofmt -l internal/ cmd/ tools/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` · `go run ./tools/lockgate` · `cd web && npm run typecheck && npm run test && npm run build` — all green. Vitest offline (scripted doubles; fixtures as bodies); NO test dials 8481/8482, any live unit, or the network; `VITE_SINET_ADDR` unset in tests. $0.

## 2. Seams — what belongs to LATER packets (leave named, never build)

| Later packet | Owns | B6-6 leaves |
|---|---|---|
| **B6-7** | assistant (`/chat`); conversational answering over history | the stub; inbox cards render data, no chat UX |
| **B6-8** | review surfaces (`/deliverables/:id`: diffs, comments, accept UI + its High-tier step-up screen, try-it dual-iframe — **the ONE raw-HTML channel; the escape-allowlist widens THERE, in B6-8's commit**), workforce map, merge-card derived listing (B6-3 OQ4) | those stubs; inbox/what-needs-me LINK into deliverables; the accept-card/accept verbs unconsumed here |
| **B6-9** | Declarative Web Push + **re-nag SLA** (S15.6's remind/re-push cadence keys — delivery is the notifier's, G1 Def.3), PWA completion, the S15.12 completeness sweep + the final "SPA consumes every API" check (where `POST /api/meters/pause`, `POST /api/auth/pin`, grants, opt-in placement land if unconsumed sooner) | the deep-link URLs (R11) push will target; unconsumed verbs stay unconsumed, named here |
| **B6 gate / bring-up** | benchmark READOUT surface (win rates + BENCH-REG §15 honest-claims table — a frozen obligation wherever a win rate renders; NO win rate renders at v0 because none is served), the done-directly aggregate serving read, Gate authority-narrowing carries | named, not built |
| **Not this packet** | new routes beyond OQ dispositions, mutation verbs beyond consuming landed ones, SSE server/frame changes, migrations, `internal/benchmark` vocabulary moves, renderer THEME packages, any npm addition | — |

## 3. ⚙ settings consumed (by exact dotted key; none invented)

- **None newly read.** `effects.approval_expiry` + `freshness.max_age` are consumed SERVER-side inside the approvals list this packet reads (approvals.go:45–50); `watchdog.suppress_retune_count` speaks through the served suppress detail. The settings UI DISPLAYS all 118 keys — display of served data, not consumption. Client-side numbers (re-read debounce if any, list presentation caps) are **structural constants with named reasons** (§41/§42 precedent), enumerated in §43 — interim under the standing settings-tab directive.

## 4. Files expected to change

- **New/extended (web):** `web/src/` Inbox + InboxItem + Settings view modules (replacing the three `Stub` renders in App.tsx's switch; routes.ts `owner` flips `'B6-6'`→`''` — patterns never renamed); the Sinet-owned JSON Forms renderer set (own module(s)); their vitest suites; `web/src/api.ts` typed reads + verbs; `web/src/fixtures/api/` new fixtures (settings, settings-history, prices, benchmark-verdicts, per-kind approval cards, conflict card) + `internal/api/apifixtures_test.go` grown to serve them; `index.css` owned additions.
- **Extended (Go, ONLY per OQ dispositions — each pre-raised):** candidates are OQ1 (addressee-scoped conflict read + the ninth-kind fold), OQ4 (served verdict/disposition vocabulary), OQ5 (the eval-results read), OQ9(b) if taken (watchdog card resume action) — additive, with tests, redaction/owner-scope/never-percent scans extended; nothing else in `internal/`.
- **Bookkeeping:** `P3/CONVENTIONS.md` (§43).
- **Expected NOT to change:** `web/src/routes.ts` patterns, `router.tsx`, `Login.tsx`, `events.ts`, `live.ts` semantics, escape-scan mechanics (fixture set may grow), `internal/settings/index.go` (byte-unchanged), `internal/settings/uischema.go`/`schema.go` emissions, migrations, `internal/eventlog/contract.go`, `internal/benchmark/registered.go` + BENCH-REG, `internal/memory` gate/store semantics (a read addition per OQ1 is the sole sanctioned touch), `components.lock`, `web/package.json`/`package-lock.json` (byte-unchanged), `go.mod`/`go.sum`, `.github/workflows/ci.yml`, `Spec/*`, `Docs/*`, `Research/*`, `P3/STATE.md`.

## 5. Adopted components touched

**JSON Forms 3.8.0 (`@jsonforms/core` + `@jsonforms/react`, MIT) — installed at B6-4, probe-only until now; THIS packet wires it.** FC-v1 §4 decision notes bind as constraints: **the renderer set is Sinet-owned** — no theme package (`@jsonforms/material-renderers` et al. NEVER enter), core theme-independent UISchema vocabulary only (Group/Categorization tabs + the SHOW/HIDE/ENABLE/DISABLE rule vocabulary, which core evaluates — OQ7); Eclipse-backed, steady-cadence pick chosen over RJSF for exactly this generated-registry shape; **RJSF v6 is the pre-registered fallback** — a dead/relicensed/deprecated finding at execution re-verification = STOP + flagged coordinator decision, never a silent substitute; the RJSF re-entry condition (JSON Forms stalls >18 months or a registry need exceeds its vocabulary) is recorded, not this packet's concern. Re-verify liveness at the pin at execution (B6-4 verified 3.8.0 = latest, repo alive 2026-07-27). Adopt-don't-fork: no patches, no `patch-package`, pin never bumped. **No other adoption is touched; no new dependency enters** (a genuine need = an OQ through the S16.4 rail).

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format + trailer, no push, never edit Spec/Docs/Research/STATE, never read `*-api-key.txt`.
- **§7/§9** — unversioned API consumed as-is; identity from `GET /api/auth/session` only; 401 → login with return-to; the dev identity never administers.
- **§30/§38** — SSE frame contract + owner-scope/redaction are SERVER facts; served content renders as data; no percent/fraction/ETA; error mapping is the server's — the client renders codes/details, never re-classifies by message text.
- **§39 (+B/C)** — the approvals/oversight/benchmark verbs' landed semantics are consumed VERBATIM (hash pins, tier gates, one-authority-rule, record-before-reveal, incident-window dismiss, still-red acknowledge); the inbox UI must not imply authority a verb does not grant. The inbox answers ride the S15.6 verbs — never the walking-skeleton `POST /api/asks/{ask}/answer` (unpinned; B2-4 legacy).
- **§40 (+B/C)** — the settings family's ownership lines (registry computes, transport narrows, UI renders); memory reads/writes stay gate-routed; typed-error mapping.
- **§41 + §41-B + §42 + §42-B ENTIRE** — npm exact-pin discipline (no dependency moves), owned router + URL contract (fill, never rename), one EventSource + per-subscriber debt + declared types, `useLive` as the only feed access, golden-fixture tie, escape-scan EMPTY allowlist, phone-first structural responsiveness, `VITE_SINET_ADDR` hermetic posture, rendered-honesty primitives, verbatim timestamps.
- **The 35 D6-ratified readings** (§29–§37): derive-from-log (client state = served rows + frames), no tickers, money read never computed, model output is untrusted input, honest absence over fabrication, stale never poses as live, severity computed never judged, proposals never auto-apply, structural constants carry named reasons.
- **Hermetic + $0** — nothing dials the live Caddy admin chain (8481/8482) or any live unit; vitest offline; no paid call, no push, no install.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. Inbox renders every served kind in served order (fixture with all kinds incl. the eighth failed-pair and the OQ1 ninth); no client re-ranking (code read + a shuffled-fixture probe); non-answerable cards render the served reason and expose NO acting control (driven).
2. Ask card: Layer-1 content + 13.5 help {what/wrong/recommend} + Layer-2 expansion render from the snapshot; actions render from the served vocabulary (`approval.actions` and the `choices` wire key both covered by fixtures); nothing re-drafted client-side (scan: no hard-coded help/action strings).
3. Tier mechanics: only `batchable` cards are selectable; the batch POST composes per-item `{id, payload_hash, answer}`; per-item outcomes render individually with a partial-failure fixture (one 409 beside applied siblings); Medium cards expose no batch path; High cards demand the PIN modal before POST and send `pin` in the same request (driven: the POST body carries it; `pin_required`/`pin_rejected` re-prompt; the PIN never lands in state/DOM after submit — asserted).
4. Co-approval: both approver states render from `approvals` (fixture: platform-level effect with owner-signed-only); the completes-nothing response renders recorded-and-waiting from served state; a member's view of an operator-only card is read-only with reason.
5. Plan card: Approve + Re-plan both present whenever served; Re-plan drives the structured contest entry (tap→`contest` ref in the POSTed answer, driven); Re-interview renders when served.
6. Staleness: `stale` fixture renders the flag + served reasons + one-click re-plan; approving stays possible on a stale card (driven both ways). Expiry: countdown renders from `expiry_at` (fake clock); absent expiry renders no countdown; `engine_expiry_ts` verbatim beside it.
7. Answer flow: 409 `stale_payload` re-renders the carried `current` card with a visible change notice (driven); repeat answer renders `applied:false` + state as already-resolved; after every verb the view re-reads (POST asserted followed by GET).
8. Watchdog card: suppress POSTs the card's own identifiers; the retune detail renders; the resume door per OQ9's disposition works and the open-ask-park refusal renders verbatim (fixture).
9. Drift dismiss + conformance acknowledge: verbs fire with reason; `still_red:true` renders as not-a-pass and the card's continued presence is honest (fixture where the row stays red).
10. Verdict form: renders ONLY served PendingPair fields (test enumerates rendered fields against the fixture — no arm/position/model anywhere pre-record); submit is impossible without BOTH verdict and guess (driven); the reveal renders only from the response `reveal`; the reveal-absent branch renders recorded-with-late-reveal; decline is first-class; failed-pair card offers decline only; alarm dispose (operator) posts disposition+reason per OQ4's vocabulary disposition.
11. Ninth kind per OQ1: the addressee sees their open-conflict card and can resolve; a non-addressee (operator included) sees no card (three-viewer fixture); already-closed renders the served detail; no ticker/poll for its currency (grep).
12. Deep links: every card id round-trips `/inbox/:id` through hrefFor + router decode — driven with `:`/`\x1f`/`#` natives (watchdog + alarm ids); unknown id renders honest not-found; inbox + one of each card kind pass the structural phone-width assertions.
13. Inbox liveness: declared types enumerated with reasons; frames trigger re-reads; resnapshot debt honored (`done()` after the read lands); stale renders visibly (driven via the FakeSource pattern).
14. Settings: JSON Forms renders the SERVED schema+uischema through the Sinet-owned renderer set — 33 Category tabs, Groups where emitted, one Control per key; NO theme package imported (dependency scan); no hand-built key list (completeness derived from served data, both directions: every served key renders, nothing renders that is not served).
15. The nested-data bridge: every control shows its value from the REAL fixture (the flat-object regression probe renders empty and FAILS — pinned); an edit maps back to `POST /api/settings/{key}` with the right key + body (driven per type: number, bool, enum, duration, per-user).
16. Bounds + split: floor/ceiling render beside numeric keys from served values; the bounds editor is a separate operator-only affordance posting the bounds verb; a member sees the whole write surface read-only with the served `editable_reason` (driven both roles); server refusals render verbatim.
17. Badges + outcomes: restart-required and dormant badge from served flags; the restart write detail renders verbatim; reset-to-default is expressible and distinguishable via `overridden` (driven).
18. History + help: per-key audit rows render (actor, old→new verbatim, ts, reason); inline help renders per control from the served text (fixture spot-checks).
19. Price table: rows render verbatim; empty-table posture + `deferred[]` render as served; add-row (operator) posts `{row, reason}` and renders the version + re-validation detail; NO edit/delete affordance exists (code read + DOM assertion); member sees read-only.
20. Eval results per OQ5: recorded scores render (suite/asset, score vs floor, when) from the dispositioned read, as served; the surface exists where the disposition placed it.
21. Escape/side-truth sweep: scan green with allowlist EMPTY over the grown tree; no storage-API state; no money arithmetic; no percent/ETA primitive; PIN handling asserted clean; fixtures byte-tied via the Go assertor (grown fixture set passes `TestWebAPIFixtures` + stability).
22. Go-side (per dispositions only): additive shapes with owner-scope three-way tests, redaction + never-percent scans extended, route-table diff shows ONLY dispositioned additions; counters hold (lock **29**, ⚙ **118/33** byte-unchanged, migrations **0001–0019**, inventory **91/5/96**, conformance **12**, `package-lock.json` byte-unchanged); CONVENTIONS §43 appended, §1–§42 untouched; **full battery green both sides, vitest offline, $0**.

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — the ninth kind: derivation + ranking.** No addressee-scoped conflict list read exists (browse.go has only `EntryConflicts(entryID)`/`Conflict(id)`), and no SSE frame reaches the addressee on either edge (Knowledge family unrouted, topics.go:55–75; relay frames owner-scoped to the WRITER). **RECOMMENDED: (a) a server-side ninth derivation in `GET /api/approvals`** — one additive browse read (`OpenConflictsFor(affected)` — SELECT over the landed columns WHERE affected + status='open') + a fold in `approvalItems` as kind `memory_conflict`: owner = the addressee, `answerable` via the SAME `mayAnswerConflict` line (addressee-only — the operator sees NO other person's conflict card, the deliberate B6-3C D2 visibility line, a stated deviation from the other kinds' operator-sees-all), actions `["resolve"]`, card = the Conflict row, **tier Medium** (individual, no step-up, releases nothing outward — the landed unclassified-card default, approvals.go:561–568), never batchable. Currency inherits the inbox view's re-read triggers; the idempotent already-closed answer keeps a stale card harmless; the honest no-live-frame note is rendered, not compensated. Alternatives: (b) a client-side derive over a new `/api/memory` conflicts read (splits ranking/interleaving into a second client path); (c) mint an event — **BARRED** (F7 ratified: the row IS the record; a new type is §29 machinery nothing pre-authorized). Dispose (a)/(b) + the tier.
- **OQ2 — the nested-data bridge shape.** RECOMMENDED: a pure `settingsData(values) → nested object` binding each key's **`effective`** (platform scope) by splitting dotted keys; JSON Forms `onChange` diffs against the served values and the UI issues **per-key** `POST /api/settings/{key}` writes (the landed write grain — no bulk write exists) with an explicit save act per key (never auto-fire on keystroke: a settings write is an audited act); per-user overrides render OUTSIDE the form data model (R20 panel) because the form binds one value per key. Dispose the bridge + the save-act shape.
- **OQ3 — the settings-write UX vs the operator-only wall.** The registry admits no member actor (structural, B6-3 OQ8); the read serves `editable`/`editable_reason` per caller. RECOMMENDED: members (and the dev identity) get the full VIEW — values, bounds, badges, help, history — with every write affordance absent and the served reason shown once, prominently; the operator additionally gets value editors, bounds editors, the `for_user` target-user affordance on PerUser keys, and price add. The member-self-service question stays where B6-3 OQ8 left it: the settings-tab amendment pass, NOT this packet. Dispose the member-view shape.
- **OQ4 — the verdict form's vocabulary + the reveal flow vs BENCH-REG §14.** The four verdict values, the guess sides and the §12 disposition set are frozen registered text living ONLY in `internal/benchmark`; `internal/api` deliberately restates none (benchmark.go:17–26), and a UI hard-coding them would be a THIRD home for registered text (the §35/§17 drift shape; B6-5's label precedent barred exactly this client-side). **RECOMMENDED: additive served vocabulary** — `BenchmarkVerdictForms` gains `choices[]` + `guess_sides[]`, and the alarm flow a served `dispositions[]`, marshaled from the package's own constants through the existing `BenchmarkSurface` seam (the `RegisteredValues` precedent: the owner speaks, displays render) — render-from-served, scan-pinned that no view file declares the strings. Alternatives: UI constants + a test pinning them to the package (still a copy); free-text entry (server-validated but hostile). **Reveal flow:** the backend already enforces record-before-reveal in one transaction — the UI obligations are R9's (render only served pre-record fields; reveal only from the response/committed read; honest late-reveal branch); confirm this reading needs NO further mechanics. Dispose.
- **OQ5 — the eval-results surface's minimal shape.** Grounded: no Layer-0 view / catalog query serves `eval.score_recorded` (G.1). **RECOMMENDED: (a) one additive Layer-1 catalog query in `internal/history`** (e.g. `eval.scores`: run_events WHERE type='eval.score_recorded', json_extract fields — suite/asset/score/floor/pass/ts, owner-scoped, newest-first) — rides the LANDED audited `GET /api/events/query/{query}` route + the B6-5 history-panel rendering patterns; `internal/history` is frozen-by-precedent, so this is exactly the pre-raised sanctioned-narrow shape (importwall/registry-size pins move in lockstep, the B6-3A precedent); the settings area renders it as R22's results panel. Alternatives: (b) an internal/api projection read (a new read outside the history layers for history-shaped data); (c) point the surface at open-sql (drags the lower-confidence flag onto a deterministic question — dishonest). "Visible" minimally = the recorded scores readable with suite/asset/score/floor/when; the gate records the unlock. Dispose (a) + placement.
- **OQ6 — the price-table editor over the append-only 0019 model.** The row shape is S10.3's (`StoredPriceRow`, opaque to the transport). RECOMMENDED: a structured add-row form for the KNOWN fields (model, lane, unit_prices.{input,output,cache_read}_usd, effective_from, verified_on, source) composing the S10.3 JSON — **pinned by a golden fixture marshaled from the real `StoredPriceRow`** (the §42-B receipt-fixture precedent) so the form cannot drift from the owner's shape; the store's validation remains the authority and its refusals render verbatim. Alternative: raw-JSON row entry (drift-proof, operator-hostile). Dispose form-with-fixture-pin vs raw-JSON (or form + a raw fallback).
- **OQ7 — SHOW/HIDE rules when the emitter emits none.** v0 emits NO rule, deliberately (uischema.go:24–35). RECOMMENDED: **ratify render-as-emitted** — the renderer set relies on JSON Forms CORE's native rule evaluation, so the S15.9 rule vocabulary is supported structurally the day the emitter has a ratified condition to emit, and the UI fabricates no rule client-side (the emitter's own no-fabrication reason applied to the renderer); record the reading in §43. Alternative: demonstrate a rule (BARRED by the same reasoning the emitter recorded). Dispose.
- **OQ8 — co-approval both-states from derive-from-log.** The served `approvals` block is cycle-scoped derive-from-log (approvals.go:677–725) — the UI renders it verbatim. RECOMMENDED reading to confirm: render the block on every effect card as served (`platform_level:false` shows the owner-only path; `platform_level:true` shows both limbs with by-whom); the completes-nothing answer renders recorded-and-waiting from response state + detail; **no client-side inference of who-signed from event frames** (the served derivation is the one truth). Dispose.
- **OQ9 — the watchdog resume door.** S14.4 makes "resume — I was wrong" first-class ON the card; the landed card serves `actions:["suppress"]` only (approvals.go:306) while the resume verb is run-scoped (`POST /api/runs/{run}/resume`, owner-scoped ≈ the card's answerability rule). Options: **(a) RECOMMENDED — render the resume door from the card's `run_id`** (no Go change; the drill-through-link shape; authority enforced server-side and equivalent to `mayAnswerWatchdogFlag` for run-scoped flags); (b) one-line additive `"resume"` in the run-scoped watchdog card's actions (keeps D9's actions-are-the-vocabulary letter exact, costs a Go touch + tests). Dispose (a)/(b).
- **OQ10 — the Low-batch selection semantics.** "One action answers a selected set" (S15.6) across cards whose vocabularies may differ. RECOMMENDED: selection enables across batchable cards; the ONE action is per the selection's common action id (selection constrained to cards sharing the chosen action — e.g. a select-all-then-approve over Low asks whose vocabulary admits it), each POSTed item carrying ITS OWN payload_hash + answer in its card's vocabulary; mixed-vocabulary selections either constrain (recommended) or split visibly. Dispose the constraint shape.

## 9. SCOPE VERDICT

**NOT one opus sitting.** Two decision surfaces that are each a packet-worth of UI: the inbox alone is nine card kinds with per-kind verbs, three tier flows (batch, PIN step-up, co-approval), the verdict form's blindness obligations, and deep-link mechanics; the settings side is a from-scratch JSON Forms renderer set, the nested-data bridge, the value/bounds/audit/price surfaces, plus up to three dispositioned Go-side reads (OQ1/OQ4/OQ5) with their scan/test lockstep. The queue row pre-authorizes no split — **this grounding RECOMMENDS the coordinator pre-authorize A/B at the natural seam (one brief serves both; a stop at the seam is a success mode):**

- **A — the approval inbox** (R1–R12 + their slice of C; OQ1, OQ4, OQ8, OQ9, OQ10 land here): the ranked queue, all card kinds + verbs, tiers/batch/step-up/co-approval, verdict forms, the ninth kind, deep links.
- **B — the settings UI** (R13–R22 + the rest of C; OQ2, OQ3, OQ5, OQ6, OQ7 land here): the renderer set, the bridge, every-setting completeness, bounds/badges/history/help, the price table, the eval-results surface.

Order **A → B** (A exercises the verb/fixture patterns B's write paths reuse; the D4(b) unlock lands with B). If the executor reaches the seam green with budget, same-sitting continuation is sanctioned; stopping at A-landed is equally a success mode.
