# P3-UI-2 brief — Controls I: cancel (runs + tasks) + pause-my-automation + budget editor

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical behavior text is `Spec/drafts/` (frozen v1): **S15.5–S15.6** (oversight + decision surfaces, `S15-frontend-api.md`), **S10.4/S10.8** (`S10-metering-scheduling.md` — pause semantics, P-T08-4, budgets, "resume, I was wrong"), **S02.3** (`S02-durable-state-recovery.md` — the run FSM the cancel mapping is expressed in), **S15.2** (the tasks/runs/meters family rows). **The design authority for this batch is the operator-APPROVED `P3/design/design-approach-proposal.md`** (§3 pattern vocabulary, §5 binding constraints) — **subordinate to the spec on all behavior**. The landed-verb record binds as consumed contract: `P3/CONVENTIONS.md` **§39 (B6-2A cancel core) + §39-B (B6-2B meters/oversight verbs)**, plus **§41-B, §42+§42-B, §46, §47** (constraints extracted in §6). **The spec wins over this brief, over the proposal, and over existing code; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**AGPL BOUNDARY (binding, an explicit evaluation item):** Odysseus is AGPL-3.0 and is an **ideas-only** source, forever. Zero Odysseus code, CSS, markup, or asset may appear anywhere in this packet.

**This is packet 2/7 of the UI batch** (queue row verbatim, `P3/STATE.md` line 147): *"First three D3 control groups live in the new language on the new primitives: cancel surfaced with the ratified state mapping and honest terminal states; pause/resume with P-T08-4 preservation visible; budget editor binding the landed gauge (honest pre/post-declaration states); owner-scoped; every act lands in inbox/history views; battery + bundle."* It is the FIRST packet to mount kit primitives in production, so **the deferred `@base-ui/react` JS cost lands now — bundle growth is expected, measured, and itemized** (§47's recorded expectation: "the `@base-ui/react` JS cost lands the day UI-2 first mounts a primitive, stated here so that growth is expected rather than discovered").

**HARD WALL — this packet consumes EXISTING landed verbs only: zero API additions, zero event changes, zero scoping changes, zero routes, zero migrations, zero production `.go` changes.** If any control turns out to need a backend change, that is a **STOP-and-flag**, not an implementation (one candidate is already flagged as OQ1 — do not resolve it silently). **No new dependency**: the §47 closed set stands; a needed portal/primitive beyond `@base-ui/react` (already in the tree, reached THROUGH the kit) = STOP-and-flag. shadcn generation is barred as landed: at CLI 4.16.1 every addressable style declares `radix-ui` (outside the closed set) — **hand-write on the kit/`@base-ui` or stop** (§47).

**Model routing:** executor + finalizer **opus** (standing batch directive); grounding/evaluation inherit (Fable); lossless opus fallback on any classifier trip.

---

## BINDING carried facts (every one verified against this session's tool reads, 2026-08-04/05)

**Baseline, re-measured this session (not carried):** `cd web && npm run test` → **469 tests / 26 files, all green**. `npm run build` → **JS 981.17 kB / gzip 294.55 · CSS 53.78 kB / gzip 11.52 · 15 font assets (417.30 kB total, unchanged)**. `go run ./tools/lockgate` → OK, **38 entries; 574 npm packages covered or toolchain-scoped**. Go side at UI-1 landing: 45 packages green / 14 sanctioned env-gated skips. Worktree carries the four long-standing untracked operator items (`Presentation/`, `Research/Presentation/`, `Sinet-Logo.jpeg`, `tools/dbpeek/`) — **never staged**.

### The endpoint-per-control map (extracted from the landed routes + handlers — the whole surface this packet consumes)

| Control | Endpoint (registered `internal/api/api.go`) | Handler | Response type | Client shape |
|---|---|---|---|---|
| Cancel one run | `POST /api/runs/{run}/cancel` (api.go:506) | `handleRunCancel` (actions.go:53) | `stage.CancelOutcome` | `/api/runs/{}/cancel` |
| Cancel a task | `POST /api/tasks/{task}/cancel` (api.go:507) | `handleTaskCancel` (actions.go:76) | `stage.TaskCancelOutcome` | `/api/tasks/{}/cancel` |
| Budget editor | `POST /api/meters/budget` (api.go:515) | `handleBudgetDeclare` (meters_verbs.go:118) | `BudgetDeclared` | `/api/meters/budget` |
| Pause / resume my automation | `POST /api/meters/pause` (api.go:516) | `handlePauseSet` (meters_verbs.go:251) | `AutomationPause` | `/api/meters/pause` |
| (read side the editor binds) | `GET /api/meters` (api.go:480) | `handleMeters` (meters.go:89) | `Meters` | already consumed (`api.meters`, api.ts:1647) |

These are four of the B6-9 sweep's **fourteen registered gap routes over thirteen shapes** (`web/src/sweep.test.tsx:519–542`, pins at :623–626). "Resume — I was wrong" (`POST /api/runs/{run}/resume`, api.go:519) is **NOT** in the fourteen and is **already consumed** — `api.resumeRun` (api.ts:1723) called from the Inbox watchdog card (`Inbox.tsx:1066–1074`). See OQ2.

### The ratified cancel mapping, as landed (`internal/stage/cancel.go` — S02.3 has NO cancelled state; CONVENTIONS §14 reading 9 / §16 / §39)

- **running / draining** → S03.1 ladder on the live session, then `completed` with the cancel reason (`"cancelled by <actor> (4.5)"`) — `applied:true`, `ladder_invoked` reported honestly (cancel.go:267–284).
- **parked** → `finalized` (finalize-with-card; generation unchanged, **no resume edge**; every open ask closes in the SAME tx) — `applied:true` (cancel.go:286–306).
- **queued** → `finalized`, queue row settled in the SAME tx; with no scheduler bound the verb refuses fail-closed (cancel.go:308–331).
- **claimed / new** → `ErrCancelInFlight` → **409 `claim_in_flight`** ("being dispatched right now — retry in a moment").
- **already terminal** (`completed/finalized/tombstoned/died_at_gate`) → **200 with `applied:false`**, detail `"the run had already ended; nothing was cancelled"` — the honest idempotent repeat (cancel.go:254–256).
- **crashed** → **200 with `applied:false`**, detail `"the run already crashed; the recovery ladder owns its disposition (S02.5)"` (cancel.go:257–263).
- Kanban → `cancelled` **only when something actually cancelled** (`applied:true`) — a no-op cancel never rewrites the record (cancel.go:234–241, §39 drain D4).
- **Task cancel** = every non-terminal run under the same mapping; one refusing run (the claimed window) fails the WHOLE call so the caller retries the task as a unit — never a partial-success report (cancel.go:197–242).
- Error contract (`internal/stage/surface.go:257–274` `mapCancelErr`): 409 `claim_in_flight` · 409 `cancel_raced` ("re-read it and decide again") · 503 `not_wired` (surface nil OR queue-unsettleable) · 404 `not_found` · plus transport-level 403 `forbidden` / 404 via `authorizeOwner` (reads.go:444–452 — 404-before-403; member own, operator any).
- Wire shapes: `CancelOutcome {run_id, from, to, applied, ladder_invoked, detail}`; `TaskCancelOutcome {task_id, kanban_status, runs: CancelOutcome[], applied}` (cancel.go:125–148). Errors serialize `{error: <code>, detail: <msg>}` (intake_handlers.go:85–92).
- **NO AUTO-KILL**: cancel is a human act; the HTTP verbs are the only entry points (`TestCancelIsReachableFromTheHTTPVerbsOnly`, tree-wide AST wall, §39). Nothing in this packet may add an automated caller — a frontend control is a human's button.

### The pause verb, as landed (meters_verbs.go:230–324; S10.4; §39-B)

- Body `{person?, paused: bool (REQUIRED — no default; "a request that did not say which way it wanted the switch is not a request this verb can guess at"), reason?}`. `person` defaults to the caller. **Resume = the same endpoint with `paused:false`** — there is no separate resume route for the automation switch.
- Authority (§39-B OQ4): **own + operator-any** — member flips their own, operator anybody's; 403 for member-on-other; **404 `not_found` for an unknown person** (requirePerson, meters_verbs.go:457–469); 503 `not_wired` when the store is absent; 400 on missing `paused`.
- Response `AutomationPause {owner, paused, changed, detail}` — `changed:false` is the honest already-in-that-position repeat. The served details ARE the P-T08-4 story: pause → `"paused: no background work is admitted for this person, and an in-flight run parks at its next stage-session boundary. Nothing queued or parked is discarded, and interactive work is not gated at all (S10.4)"`; resume → `"resumed: background admission is open again and the preserved queue proceeds. A run that parked while paused is released by its own resume (S14.4)"` (meters_verbs.go:293–298).
- Spec ground: S10.4 — one-switch pause stops background admission + parks in-flight at the next checkpoint and **MUST preserve everything queued and parked** (P-T08-4 mandatory test, S17 row P-T08-4); interactive is NEVER gated. §39-B: admission consult is background-candidates-only; parking at stage-session boundaries; the per-paid-call gate is a NAMED later seam.
- **No landed GET serves the current pause state.** `PauseStore.Paused` exists (meters_verbs.go:65) and the scheduler consults it, but no handler calls it; `GET /api/meters` (Meters/MeterLane), the SSE fleet snapshot (projection.go FleetSnapshot) and the session read all carry no pause field — grep-verified this session. **This is OQ1 — do not invent a read and do not silently degrade; build per OQ1's disposition.**
- Audit: each flip mints `decision.recorded` (cardType `automation_pause`, decision `pause`/`resume`, old→new snapshots) via `recordDecision` (meters_verbs.go:299–308).

### The budget verb + the landed gauge it binds (meters_verbs.go:97–228; meters.go; S10.4; §39-B)

- Body `{person?, lane (required), period_tokens (>0 int), period_days (>0), period_start? (RFC3339, defaults now), reason?}`. Refusal texts are served verbatim and load-bearing — `period_tokens <= 0` answers *"a budget is a positive figure in weighted-consumption units (S10.4). To stop automation entirely, pause it (POST /api/meters/pause)"* (meters_verbs.go:157–161). Authority = own + operator-any; 404 unknown person; 403; 503.
- **The unit is the gauge's own — `"weighted-consumption units (S10.4)"` — NEVER dollars** (D5; the file's own header: "NO MONEY IS COMPUTED... Nothing in this file multiplies, prices, or converts anything"). The unit rides the wire as a label (`BudgetRecord.Unit`, `budgetUnit` const) so a surface cannot render it as currency by accident.
- Response `BudgetDeclared {budget: BudgetRecord, prior?: BudgetRecord, detail}` — `prior` present only when a budget existed (old→new honesty; a FIRST declaration has no `prior` member, and the audit row's old side is the explicit `{"declared":false}`).
- **The read side already exists and this packet binds it, changes nothing:** `GET /api/meters` serves `MeterLane {owner, lane, weighted_consumption, cache_read_weight, assumed, pressure_applicable, pressure (nil unless applicable), budget_declared, budget_remaining, total_runs, active_runs, parked_runs, parked_until}` + the `budgets` MeterView (`cost_budget_remainder` rows: `budget_declared, budget_lanes, budget_period_tokens_total, budget_unit, …, absence_reason`) (meters.go:33–84). §39-B drain D2 proved the two halves of one response AGREE after a declare (the gauge reports the declared budget + real pressure) — **the pre/post-declaration honest states are therefore a SERVED fact, not a client construction**.
- Audit: `decision.recorded` (cardType `budget`, decision `declare`, old→new) (meters_verbs.go:195–204).

### Where the acts land, exactly (the queue row's "every act lands in inbox/history views")

- **Cancel mints NO `decision.recorded`** — it rides `run.state_changed` (the transition rows; §39: "cancels ride run.state_changed"; §42 drain D3: correctly visible where state transitions render). Its visible landing: the task detail's run list + LiveActivity re-read (state flips to `completed`/`finalized` verbatim), the board card leaving its column for `cancelled` (kanban vocabulary is data, §42), mission-control buckets. `run.state_changed` is already in `boardEventTypes` (live.ts:42–61) — board, mission, task detail and inbox all re-read on it with **zero live.ts change**.
- **Budget + pause mint `decision.recorded`** (family 5). `decision.recorded` is already in `boardEventTypes` and `inboxEventTypes` (live.ts:53, 118) — fleet + mission (via `missionEventTypes`) and the inbox re-read on it with **zero live.ts change**. The budget act's visible landing is the re-read gauge itself: `budget_declared` flips true, `pressure` becomes real, the `budgets` view rows go declared.
- **Documented, deliberate miss — do not paper over:** person-level pause/budget decisions do NOT appear in the task-detail decisions block (§42-B: "a person-level automation pause and a per-person budget edit are the live examples" of non-task decisions), and no landed surface renders a person-level decision feed. The acts are audited (events minted, asserted in tests) and their STATE lands visibly per the above; **building a decision-feed surface is NOT this packet's scope** and inventing one would exceed the freeze. The History panel (a query instrument, §42) can ask about them only insofar as landed registry queries cover them — the Layer-1 catalog is AT its 50/50 band ceiling: **any new canned entry = STOP-and-flag the band decision** (batch rule, STATE line 142).

### The host surfaces, as read this session (mount ground; full survey in the grounding record)

- **TaskDetail** (`web/src/TaskDetail.tsx`, 516 lines): renders owner (`data.owner`), kanban, six sections; run list in `ReceiptsBlock` — every run as `div.run-receipt[data-run]` with `run_id` + `state` **verbatim** (`TaskRunView {run_id, state, created_ts, receipt?, receipt_absent?}`); `LiveActivity` renders the active run (`span.run-state`); `terminalStates = ['completed','crashed','finalized','tombstoned','died-at-gate']` (TaskDetail.tsx:177). **Zero buttons exist on the surface today.** Its live subscription is deliberately unnarrowed (§42-B).
- **Board** (`Board.tsx`): `CardFace` is pinned "EXACTLY the S1.3 set … nothing more" (Board.tsx:256–264) and board.test.tsx asserts the exact face — **the board face is NOT a cancel mount; leave `Board.tsx` untouched** (the drag stays its only verb). Runs are reachable from every card via the task link.
- **Fleet** (`Fleet.tsx`, 138 lines): THE meters surface of record (sweep.test.tsx:736 maps family `meters` → surface `fleet`). Renders `.fleet-lanes` rows carrying `data-owner`/`data-lane`, then the shared `MetersPanel` (defined in MissionControl.tsx:166–235), cost views, `LocalTierSeams`. **Only two filter `<select>`s exist — no button today.** The landed undeclared texts (must survive verbatim for undeclared rows): `"no budget declared"` (MissionControl.tsx:212) and `"no declared budget, so there is no denominator"` (MissionControl.tsx:205).
- **MissionControl**: six disjoint buckets + `MetersPanel` + `HistoryPanel`; `RunLine` rows carry `run_id` + `owner` in scope. **Not a mount this packet** (OQ3) — its D8 restyle is UI-5's.
- **Inbox** (`Inbox.tsx`): the landed verb-driving pattern this packet generalizes — `fireAction` dispatch, `ActState` outcome rendered as `<p className={failed?'error':'notice'} data-outcome=…>{note}{detail}</p>` (Inbox.tsx:921–926), re-read after every act (`onAnswered()`). The resume branch (1066–1074) is the worked example of a run-scoped POST with honest 409 rendering. **Inbox.tsx is untouched this packet.**
- **History** (`History.tsx`): a query instrument over served registries; no client change needed for acts to be askable; **no dedicated test file** (mission.test.tsx covers it).
- **Test harness** (`doubles.ts`/`testing.tsx`): `scriptedFetch(table)` global fetch stub — unscripted requests THROW; `log.set()` swaps routes mid-test; `FetchLog.calls` records `{method, path, body}`. **The default session `signedIn` is alice with role OPERATOR** (doubles.ts:175–177) — member-leg tests MUST override `GET /api/auth/session` (the carol precedent, board.test.tsx:344–347; bob, shell.test.tsx:235). Verb responses are hand-scripted per test (the board `priority-hint` + inbox `resume` precedents).
- **Markup-coupled assertions that bear on this packet's mounts:** taskdetail.test.tsx (21 tests) — banned page-text tokens `%`/`percent`/`eta` (:98–100), `.decisions li` card types exactly `['effect','deliverable','intake_delta']` (:127–132), money-line guard (:265–271), liveness one-re-read assertions (:295–328); board.test.tsx (19) — banned tokens incl. `complete:`/`estimated time` (:109–110), exact card-face content (:60–74); fleet.test.tsx (8) — owner set `{'alice','bob','op'}`, `parked, no horizon given`, filter narrowing, 503 leg (:137–140); mission.test.tsx (14) — cross-fixture arithmetic (lane counts recomputed from the runs fixture, :140–165); inbox.test.tsx (41); sweep.test.tsx — the gap pins (:623–626), the stale-exception test (:600–613), `declaredButUncalled() === ['health']` (:704–707 region), the family walk (meters→fleet), the phone-complete walk (:766+).
- **The kit contract consumed** (UI-1 brief §2; §47; `web/src/ui/index.ts` read this session): `Button` (primary|secondary|ghost|danger × sm|md; `type` defaults `"button"`), `Panel`, `Chip`, `StatusDot`, `StatTile`, `Modal {open, onOpenChange, title, description?, children?, footer?}` (focus trap/escape/portal/ARIA = `@base-ui`), `Drawer`, `Skeleton`, `EmptyState {what, why}`, `Timestamp` (live|audit), `ToastProvider`/`useToast`, `cn()`, `Tone`/`tones`/`toneStyle`. Kit scans that new kit-consuming code must keep green: `ui/kit.test.tsx:317` "every motion utility in the kit is authored behind motion-safe" and `:345` "no kit component carries a raw colour value" — both scan `web/src/ui`. `ApiError {status, code, body}` (api.ts:58) carries the server's machine code + body — controls read codes, never parse prose.

---

## 1. Numbered requirements

### A — The client seam and the sweep's bookkeeping

- **R1. Four new `api` members, types mirroring the wire exactly** [S15.2 rows; the landed handlers above]: `cancelRun(run)` → `POST /api/runs/{run}/cancel`; `cancelTask(task)` → `POST /api/tasks/{task}/cancel`; `declareBudget(body)` → `POST /api/meters/budget`; `setAutomationPause(body)` → `POST /api/meters/pause` (exact member names are the executor's, recorded in §48; the `resumeRun` naming style is the precedent). TypeScript types transcribe the Go wire shapes verbatim (`CancelOutcome`, `TaskCancelOutcome`, `BudgetDeclared`+`BudgetRecord`, `AutomationPause` — field-for-field, snake_case as served). No other api.ts member changes; `api.meters` and its `Meters` types are consumed as landed.
- **R2. The sweep's pins move deliberately, exactly as the sweep instructs** [sweep.test.tsx:615–630; §46]: the four exception entries (`/api/runs/{}/cancel`, `/api/tasks/{}/cancel`, `/api/meters/budget`, `/api/meters/pause`) are REMOVED from the exception list (the stale-entry test forces this the moment they are consumed); the gap pins move **14→10 routes / 13→9 shapes** (`gaps.length`, `Set(gaps).size`, `gapRoutes.length`), with the comment updated to say which packet closed which four; the CONVENTIONS gap record gets its dated pointer (in §48, cross-noted at §46 — earlier sections are corrected datedly, never rewritten). `declaredButUncalled()` stays exactly `['health']` — **every new member gains a real caller in the same change** (a declared-but-uncalled member fails the pin).

### B — Cancel, surfaced with the ratified mapping and honest terminal states (task detail)

- **R3. Mount: the task detail's state-computed action bar + per-run cancel** [proposal §3 "state-computed action bars (verbs render only in states that allow them)"; S15.5 ¶3]. A task-level Cancel affordance in the task header region (near title/kanban), rendered only while the task has ≥1 non-terminal run; a per-run Cancel affordance beside each non-terminal run where runs render (the run list; optionally the LiveActivity active-run block). **`Board.tsx` and `MissionControl.tsx` are byte-untouched** (the CardFace pin; OQ3 records the narrowing). Controls are built FROM the kit (Button `danger` for cancel verbs) in the new language; the host page's legacy CSS coexists (the §47 migration ledger keeps task detail's own restyle at UI-5 — this packet adds controls, it does not restyle the host).
- **R4. State-computed rendering, from SERVED state only** [the mapping; queue row "honest terminal states"]: the cancel verb renders for served states `running`, `draining`, `parked`, `queued`, `claimed`, `new`; it does NOT render for `completed`, `crashed`, `finalized`, `tombstoned`, `died-at-gate` (reuse/extend the landed `terminalStates` list — one list, not two). No client-side state is ever invented: rendering derives from the last read/re-read, and the raced case (state moved between render and click) is handled by R6, not by prediction.
- **R5. Confirm-before-fire, self-explaining (the D8 narrow mandate)** [proposal §3; B6 §9 D8]: the cancel verb opens the kit `Modal` as a confirm dialog stating in plain words WHAT will happen for the run's current served state, faithful to the mapping — running/draining: *ends now; the run completes carrying the cancel record*; parked: *closes, and its open question card closes with it; it will not resume*; queued: *withdrawn before it starts*; claimed/new: *it is being dispatched right now; the cancel may ask you to retry in a moment*. Task-level confirm says it cancels every run of the task that has not ended. Wording is the executor's but must assert nothing the mapping does not do (no "deleted", no "rolled back", no invented consequences). The confirm's act row: Button `danger` "Cancel <run|task>" + Button `ghost` to close. **No tour, no hints, no spotlight — the self-teaching layer is UI-5..7's.**
- **R6. Outcomes rendered from the wire, honestly, then re-read** [the error contract; the Inbox `[data-outcome]` precedent]: `applied:true` → outcome line with `from → to` + the served `detail` verbatim, then re-read (`useLive` reload — the landed act-then-re-read pattern; the SSE `run.state_changed` frame also triggers it). `applied:false` (already-ended / crashed) → the served `detail` verbatim as the honest no-op — **never an error styling, never a client-invented state**. 409 `claim_in_flight` / 409 `cancel_raced` → the served detail + a re-read, presented as "re-read and decide again", not as failure-red. 403/404/503 → `describeError`-class honest rendering (the board's failure-note precedent). The UI never flips a run's rendered state itself — the re-read is the only truth (§42 "REST is the truth; frames are the trigger").
- **R7. Task-level outcome is itemized and never overclaims** [TaskCancelOutcome]: the response's `runs[]` dispositions render individually (run id, from→to or the no-op detail); `kanban_status` renders only when served (`applied:true`) — a no-op task cancel renders the honest nothing-was-cancelled outcome and must NOT restyle the task as cancelled.
- **R8. Page-text scans keep passing over the new copy** [taskdetail.test.tsx:98–100; board.test.tsx:109–110]: no `%`, `percent`, `eta` (and on any surface the board scan covers: no `complete:`, `estimated time`, `remaining time`) in any new rendered copy. The served detail strings are clean of these (verified at grounding); client copy must stay clean too.

### C — Pause-my-automation, with P-T08-4 preservation visible (fleet)

- **R9. Mount: a "My automation" control section on Fleet** [S15.5 ¶4 fleet = the per-person meters surface; sweep maps `meters`→`fleet`]: for the signed-in member, their OWN pause control; for the operator, the control plus the administer-anybody path (person chosen from the owners already served in the meters read — never a free-text guess; unknown-person 404 still rendered honestly if raced). Built from the kit; Fleet's own restyle stays UI-5's.
- **R10. The control is TWO explicit position verbs, never a state-guessing toggle** [the verb's own no-default design, meters_verbs.go:232–238; OQ1]: "Pause my automation" posts `{paused:true}`, "Resume my automation" posts `{paused:false}` (+ `person` when operator-administered; optional `reason` field). Pending OQ1's disposition, the control's current-position display is the HONEST ABSENCE: it states that the platform does not serve the switch's current position here, and renders the last verb response's `{owner, paused, changed}` as post-act state (session-local, labeled as "as of your last change here" — never presented as a live read). It must NOT infer position from anything else (no history query — the catalog is at its band ceiling; no fabricated default of "active").
- **R11. P-T08-4 preservation VISIBLE, in both layers** [S10.4; queue row]: (a) pre-act, the control's plain-words explainer states what pause does: stops background admission; running work parks at its next stage boundary; **nothing queued or parked is lost**; interactive use is never gated. (b) post-act, the served `detail` sentence renders VERBATIM — it carries the preservation statement ("Nothing queued or parked is discarded…") and the resume detail carries the release story ("A run that parked while paused is released by its own resume (S14.4)" — which is the already-surfaced Inbox resume card, OQ2; the copy may point the reader at the inbox, via `hrefFor`/`Link` only). `changed:false` renders as the honest already-in-that-position repeat, not as success-of-a-change.
- **R12. Pause refusals render honestly**: 400 missing-`paused` cannot be produced by the control (both buttons always state the position — asserted); 403/404/503 render the served `{error, detail}` via the honest-error pattern; a member is never OFFERED another person's switch (no dead control — the member UI simply renders self only).

### D — The budget editor, binding the landed gauge (fleet)

- **R13. Mount: per-(owner, lane) "Declare budget" affordance on Fleet** [sweep gap text: "the fleet surface renders the meters and offers no editor"]: reachable from the meters area for each served (owner, lane) row — the member sees only own rows (server-scoped read), the operator every row and may declare for any owner (`person` = the row's owner). The editor opens as kit `Modal` (or `Drawer` — executor's pick, recorded) with fields: lane (prefilled from the row; ALWAYS shown), `period_tokens` (positive integer), `period_days` (positive integer), `period_start` (optional; omitted = server-now), `reason` (optional). A plain-words explainer (D8): what a budget does — the gauge measures this person's lane consumption against it; background admission stops at the pressure threshold; it never gates interactive use; it is measured in the gauge's own weighted-consumption units, not money.
- **R14. Units honesty — D5 is absolute**: the figure is in **weighted-consumption units**; the input's unit label sources from the SERVED unit strings (`budget_unit` in the budgets view rows / `unit` in the verb response; the explainer prose may name the unit as words). **No `$`/`USD`/currency anywhere near the budget figure; no client arithmetic ever** (no remaining = budget − consumption; `budget_remaining`/`pressure` are served or absent). The §42 money/percent scans and the no-progress-primitive rule bind every new element; JetBrains Mono + `tabular-nums` for the figures per the token contract.
- **R15. Honest pre/post-declaration states, exactly as served** [meters.go; §39-B drain D2]: pre-declaration the landed absences render UNCHANGED (`"no budget declared"`, `"no declared budget, so there is no denominator"`, `pressure` absent — never 0); post-declare the control renders the response (`budget` in force + `prior` old→new when present + `detail` verbatim; a FIRST declaration renders the honest "there was no budget before" from the ABSENCE of `prior`), then re-reads `/api/meters` — after which the gauge row itself shows `budget_declared:true`, real `pressure`, real `budget_remaining` (the served agreement §39-B D2 proved). The editor never previews a pressure figure the server has not served.
- **R16. Budget refusals render the served texts** [meters_verbs.go:152–165]: non-positive `period_tokens` renders the server's own refusal — including its pointer "To stop automation entirely, pause it" (which the control may turn into a link to the R9 control; the TEXT stays the server's); missing lane / bad `period_days` / bad `period_start` / 403 / 404 unknown person / 503 all render the served `{error, detail}`. Client-side validation may prevent obviously-refused submissions (empty lane, non-positive numbers) but must never REPLACE the server's answer as the rendered truth when a request does fire.

### E — Cross-cutting: scoping, audit, discipline

- **R17. Owner-scoping asserted with the existing doubles, in the non-tautological direction** [S01.9; §38 discipline; doubles.ts]: per control — (a) member-self acts SUCCEED (session override to a member identity, e.g. the carol/bob precedent; the fetch log proves the POST fired with the right subject); (b) the operator acts on ANOTHER owner's subject successfully (cancel via `authorizeOwner` operator limb; budget/pause via `person`); (c) a member is never RENDERED a cross-owner control (structural assertion over a served multi-owner body with a member session — zero cancel/editor/pause affordances on other-owner subjects); (d) the 403/404 legs render honestly when scripted. Remember the default `signedIn` is alice-as-OPERATOR — a scoping test that forgets the session override proves nothing about members.
- **R18. The audit chain is asserted, not assumed** [the "acts land" facts above]: tests assert (via the scripted-fetch log + liveness patterns) that each act is followed by the surface's re-read; the SSE-driven cross-surface path is asserted with the landed frame-pattern (a `decision.recorded` frame re-reads fleet/mission meters; a `run.state_changed` frame re-reads the task detail — the taskdetail.test.tsx:295–328 and inbox.test.tsx:892–930 precedents). **Zero `live.ts` changes** — the subscriptions already carry both types. The §42-B documented miss (person-level decisions absent from task-detail decisions) is left exactly as documented — asserted UNCHANGED, not "fixed".
- **R19. Built on the kit, styled by tokens, motion gated** [§47; UI-1 brief §2]: every new control uses kit primitives + Tailwind utilities over the token set — **zero raw hex, zero new global class names colliding with landed selectors**; any new component that belongs to the reusable kit layer lands in `web/src/ui/` (covered by the kit motion/colour scans); motion utilities in surface files carry `motion-safe:` by the same authored-gate discipline (the global reduce kill-switch stays the backstop). Controls import the KIT, not `@base-ui/react` directly — if a control genuinely needs a primitive the kit lacks, extend the kit in its home (recorded), and if it needs a new PACKAGE, STOP-and-flag.
- **R20. Behavior freeze, verified** [batch rule; this packet's hard wall]: zero production `.go` changes; `go.mod`/`go.sum`, `components.lock`, `web/package.json`/`package-lock.json`, migrations, ⚙ registry (118/33), event inventory (102/5/107), `routes.ts`, `router.tsx`, `events.ts`, `live.ts`, `index.css` (see §4 note), `Board.tsx`, `MissionControl.tsx`, `Inbox.tsx`, `History.tsx`, `App.tsx`, fixtures under `web/src/fixtures/` (except per OQ4's disposition), `sw.js` — all byte-unchanged. No route added/removed/renamed; no new event type; no scoping change; no ⚙ key invented or consumed client-side (§3). The C-1 login fix and the assistant-card rework are OTHER packets' sanctioned exceptions — not licenses here.
- **R21. No ToastProvider mount, no background-run etiquette** [proposal §3 — UI-7's row]: act outcomes render in place (the `[data-outcome]` idiom). The toast stack stays out of the production tree this packet (its first mount belongs to the packet that owns completion etiquette). This keeps `App.tsx` untouched.
- **R22. Tests: suite only grows; markup-coupled updates honest** [B6-9 discipline; batch rule]: baseline **469 tests / 26 files**. New control tests cover: render + state-computed presence/absence per served state; the confirm flow (a confirm that never fires without the act button; the fetch log proves one POST); every outcome leg (applied, no-op applied:false, 409 both codes, 403/404/503) rendered from scripted responses **whose shapes are transcribed from the Go types with the file:line cited in the test** (the inert-probe hazard: a hand-scripted body that drifts from the handler is a test that proves nothing — cite the source of truth, and prefer OQ4's fixture extension where disposed); scoping per R17; units honesty (a probe asserting no currency token renders in the budget editor); the R2 sweep moves. Any touched assertion in taskdetail/fleet/board/mission tests is updated to assert the same invariant against the new markup — **per-file before/after test and assertion deltas stated with reasons in the executor report; counts never drop silently**.
- **R23. Battery + bundle, in the UI-1 landing order** [UI-1 brief R22]: `cd web && npm run typecheck && npm run test && npm run build` → `gofmt -l internal/ cmd/ tools/` (empty) → `go vet ./...` → `go build ./...` → `go test ./... -count=1` (over the REAL new dist) → `go run ./tools/lockgate`. All green before any commit. **Bundle measured against this brief's session-measured reference (JS 981.17 kB / gzip 294.55 · CSS 53.78 / 11.52 · fonts 15 / 417.30) with the `@base-ui/react` step ITEMIZED**: state the JS delta and attribute it (the first production import of Modal/Drawer machinery is EXPECTED growth per §47 — measure it, name it, and flag anything anomalous beyond the primitives' own size); CSS delta from newly-compiled utilities stated; fonts unchanged. The SPA stays un-split.
- **R24. Record + commit discipline**: CONVENTIONS **§48** appended (mount decisions, member names, the sweep-pin move with the §46 cross-note, the confirm/outcome idiom contract for UI-3/4, the OQ dispositions as executed, bundle numbers); commit subjects `P3-UI-2: … (S15.5–S15.6, S10.4/S10.8, S02.3, proposal §3)` + the standing trailer; explicit staging only — never `git add -A`, never the four operator items; nothing pushed; `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched by the executor.

## 2. Seams — what UI-3/4 (and the D8 pass) reuse from this packet (state them, build nothing beyond them)

1. **The confirm-dialog idiom**: kit `Modal` + a plain-words what-this-does sentence + act row (danger/primary act, ghost dismiss) + outcome line in place + re-read after act. UI-3's memory retire/owner-delete and UI-4's history acts reuse it verbatim.
2. **The verb-button pattern**: kit `Button` variant per act class (danger = destructive verbs, primary = declarations, secondary = position flips), state-computed presence from served state only.
3. **The act-outcome contract**: `[data-outcome="applied"|"failed"]` + served `detail` verbatim + the no-op honest arm (`applied:false` is not an error) + 409-as-re-read. Generalized from the Inbox to any surface.
4. **The editor-form idiom**: labeled fields, wire-sourced unit labels, server refusal texts rendered verbatim, client pre-validation that never replaces the server's answer. UI-3's memory create/edit builds on it.
5. **The api.ts discipline**: member-per-verb, types transcribed field-for-field from the Go wire shapes, every member called (the `declaredButUncalled` pin).
6. **Explicitly NOT built here** (later rows own them ON these seams): benchmark opt-in + memory family (UI-3), history ask/search + C-1 + D5 call-site application + the two S00.9 amendments (UI-4), host-surface restyles/timeline rail/donut meters/teaching empty states/tours/hints (UI-5..7), ToastProvider + background-run etiquette (UI-7), any decision-feed surface (unlanded, unratified).

## 3. ⚙ settings consumed (by registry name)

**None consumed, none created — say so is the requirement.** The verbs consume theirs server-side inside landed packages (`pressure.cache_read_weight`, `budget.background_window_fraction` inside the gauge; `pressure.bg_admit_stop` in scheduler policy) — referenced here for the explainers' truthfulness only; the client reads no registry key (the browser-cannot-read-registry-pre-login posture, §41-B). `internal/settings/index.go` stays byte-unchanged at 118/33. Any new client number (none is expected beyond copy) is a structural constant with a named reason.

## 4. Files expected to change

- **Modified:** `web/src/api.ts` (R1 members + types); `web/src/TaskDetail.tsx` (R3–R8); `web/src/Fleet.tsx` (R9–R16); `web/src/sweep.test.tsx` (R2 pins + exception-list removal); `web/src/taskdetail.test.tsx` + `web/src/fleet.test.tsx` (new control tests + honest markup-coupled updates); `P3/CONVENTIONS.md` (§48 + the dated §46 cross-note).
- **Possibly modified, each with a stated reason:** `web/src/ui/*` (only if a control needs a kit-layer addition — covered by the kit scans; recorded); `web/src/doubles.ts` (only if a shared scripted route default earns its place; per-test scripting is the precedent); `internal/api/apifixtures_test.go` + `web/src/fixtures/api/*` **only per OQ4's disposition** (test-only Go; the shared `cursor` moves and the move is stated); `web/src/mission.test.tsx` (only if the shared `MetersPanel` markup is touched — prefer NOT touching it; the fleet-local mount avoids it).
- **Expected NOT to change** (the evaluation checks this list): any production `.go` file; `go.mod`/`go.sum`; `components.lock`; `web/package.json`/`package-lock.json`; `.github/workflows/ci.yml`; `web/src/{routes.ts,router.tsx,events.ts,live.ts,index.css,App.tsx,Board.tsx,MissionControl.tsx,Inbox.tsx,History.tsx,Chat.tsx,Deliverable.tsx,Settings.tsx,Workforce.tsx,PushDevices.tsx,Login.tsx,parts.tsx,kanban.ts,push.ts,chatRuntime.ts,chatFacts.ts,escape-scan.test.ts,scans.test.ts,design.test.ts,devproxy.test.ts,routes.test.ts,responsive.test.ts,shell.test.tsx}`; `web/public/**`; `Spec/**`, `Docs/**`, `Research/**`, `P3/STATE.md`. (`index.css`: the packet should need zero new stylesheet rules — tokens + utilities suffice; if a bespoke rule is genuinely needed it lands with a §48-recorded reason and no landed rule is touched.)

## 5. Adopted components

**None added, none touched — the closed set stands** (§47; batch rule). `components.lock` 38 entries and the npm tree are byte-unchanged; lockgate green is the proof. The first production IMPORT of `@base-ui/react` (via the kit) is a bundle event, not an adoption event — the adoption happened at UI-1 through the rail. A needed new package = STOP-and-flag (S16.4 rail + operator ratification), never an executor install.

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format, no push; never edit Spec/Docs/Research/STATE; never read `*-api-key.txt`; never stage the four operator items.
- **§39/§39-B** — the landed verb contracts consumed AS LANDED: the cancel mapping + error codes; own+operator-any authority; the pause/budget audit rows; P-T08-4's server-side guarantees. The frontend renders these; it re-litigates none of them.
- **§41-B** — escape-scan allowlist EMPTY (8 banned tokens incl. `srcdoc`); no web storage; in-app links only via `hrefFor`/`Link`; owned router untouched; phone-first min-width-only responsive discipline; structural constants carry named reasons.
- **§42/§42-B** — REST is the truth, frames are the trigger; `useLive` is the only feed access; absences render with reasons and are never filled; money/figures print as served; no cross-owner sums; **no progress/percent/fraction/ETA primitive**; the never-percent scans bind all new markup; verbatim-as-served for registered strings.
- **§46** — the sweep is the completeness instrument: consumption = a CALL; the exception list must stay honest in both directions; the gap counts move only deliberately.
- **§47** — the kit export contract + `cn()`; tokens/utilities never raw hex; kit motion behind `motion-safe:`; the shadcn/radix bar; the D4/D6 scan-scope facts (stylesheet motion = `design.test.ts`, kit motion = kit scan — complementary, neither covers surface-file utilities: discipline there is authored convention, stated in §48); the bundle-growth expectation this packet realizes; the migration ledger (hosts restyle later — controls land new-language inside legacy-styled hosts, coexisting).
- **Batch rules (STATE line 142)** — proposal is design authority subordinate to spec; bundled assets only; **AGPL ideas-only (explicit evaluation item)**; honest assertion updates, counts never drop silently; bundle measured, no silent growth; behavior-preserving; FC-v1 picks untouched; catalog band ceiling = STOP-and-flag.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a command)

1. **Endpoint fidelity**: the four api.ts members hit exactly the four registered routes (methods + paths against api.go:506/507/515/516); types transcribe the Go shapes field-for-field (diff against cancel.go:125–148, meters_verbs.go:34–45/111–116/241–249); no fifth route consumed, none of the four called anywhere but through the members.
2. **Sweep bookkeeping**: the four exception entries gone; pins moved 14→10 / 13→9 / gapRoutes 10 with the updated comment; the stale-entry, phantom-route and reason-length tests green; `declaredButUncalled() === ['health']`; the family walk still maps meters→fleet.
3. **Cancel renders state-computed**: for a served body with runs in every S02.3 state, cancel affordances appear on exactly the six non-terminal states and on NO terminal/crashed run; the task-level control appears iff ≥1 non-terminal run; `Board.tsx`/`MissionControl.tsx` byte-unchanged.
4. **Cancel confirm + fire**: the confirm modal renders the state-appropriate plain-words consequence; no POST fires without the confirm's act button (fetch-log zero until click, exactly one after); the act button is kit `danger`.
5. **Cancel outcomes honest**: scripted legs for applied running→completed (detail verbatim + re-read), applied parked→finalized, applied queued→finalized, `applied:false` already-ended (rendered as honest no-op, NOT error-styled), `applied:false` crashed (recovery-ladder detail verbatim), 409 `claim_in_flight`, 409 `cancel_raced` (both rendered with re-read, not failure-red), 503 `not_wired`, 403/404 — every rendered string traceable to the scripted body; zero client-side state flips (the run's rendered state changes only via re-read).
6. **Task cancel itemized**: `runs[]` dispositions render individually; `kanban_status` rendered only when `applied:true`; the no-op arm asserts no "cancelled" styling/label appears.
7. **Pause control**: two explicit position verbs (never a toggle); both post the stated `paused` boolean (fetch-log body assertion); operator-administer picks a served owner and posts `person`; member renders self-only; the current-position area renders the honest not-served state pre-act (OQ1 disposition honored as disposed) and the response `{owner, paused, changed}` post-act, labeled as last-change-here.
8. **P-T08-4 visible**: the pre-act explainer states admission-stop + park-at-boundary + **nothing queued or parked is lost** + interactive-never-gated; the post-act served `detail` renders VERBATIM (both directions); `changed:false` renders as the honest repeat; the resume detail's pointer toward the run-level release is a `Link` at most, with the server's text intact.
9. **Budget editor binds the landed gauge**: pre-declaration the two landed absence texts render byte-identically for undeclared rows; a scripted declare → response rendered (`budget` + `prior` old→new when present; first-declaration arm renders the no-prior honesty) → `/api/meters` re-read asserted → the row renders served `budget_declared:true` + `pressure` + `budget_remaining`; the editor never renders a pressure/remaining figure absent from a served body.
10. **D5/units honesty**: no `$`/`USD`/currency token anywhere in the editor/explainer markup (probe test); the unit label sources from served strings; zero client arithmetic over budget/consumption figures (the §42 scan class extended over the new markup); figures render mono/tabular per the token contract.
11. **Refusals verbatim**: the server's 400 texts (incl. the pause pointer in the period_tokens refusal), 403, 404 no-such-person, 503 all render from the scripted `{error, detail}` bodies; client pre-validation never suppresses the server's answer once a request fires.
12. **Owner-scoping (R17) with the doubles**: member-self success legs (session override — NOT the default operator session), operator-on-other success legs, member-sees-no-cross-owner-control structural leg, refusal legs; the fetch log proves subjects and bodies.
13. **Audit chain**: act → re-read asserted per control (fetch-log sequence); `decision.recorded` frame → meters re-read and `run.state_changed` frame → task-detail re-read asserted via the landed frame patterns; `live.ts` byte-unchanged; the §42-B person-level-decisions miss asserted still-documented-still-absent (no invented feed).
14. **Kit + scans**: controls import the kit (no direct `@base-ui/react` import outside `web/src/ui/`); kit motion scan (kit.test.tsx:317) and raw-colour scan (:345) green over any kit additions; surface-file motion utilities carry `motion-safe:`; zero raw hex in new code; escape-scan allowlist EMPTY and green; the percent/eta/complete: page-text scans green over every touched surface.
15. **375px + desktop**: every new control reachable and operable at 375px (no fixed-pixel container, controls inside the existing phone-first flow; modal/drawer at their landed phone behaviors) and at desktop — asserted structurally (the responsive.test.ts invariants untouched and green) plus a DOM-level presence assertion per control in the phone-complete spirit of the sweep's walk.
16. **Behavior freeze verified**: `git diff --name-only` against §4's expected-NOT-to-change list; zero production `.go` diffs; frozen files byte-identical; no new event subscription, route, or ⚙ read; fixtures untouched except per OQ4's disposition (then: regenerated through the REAL producers with the cursor move stated and zero drift on re-run).
17. **Tests only grow, honestly**: suite ≥ 469/26 with the delta itemized per file (before/after test AND assertion counts, reasons per touched file); no assertion silently dropped; scripted verb bodies cite their Go source of truth file:line.
18. **Full battery green in R23's order** and **bundle recorded**: the build table against JS 981.17 kB / gzip 294.55 · CSS 53.78 / 11.52 · fonts 15/417.30, with the `@base-ui/react` first-mount JS delta itemized and attributed, CSS utility delta stated, anything anomalous investigated before landing; SPA un-split.
19. **AGPL attestation**: zero Odysseus-derived code/CSS/markup in the new files (evaluation attests against the pattern-study-descriptions-only rule).
20. **Record + counters**: CONVENTIONS §48 appended (+ dated §46 cross-note; §1–§47 otherwise untouched); ⚙ 118/33 byte-unchanged; lockgate 38/574 unchanged; commits per §5 with nothing pushed and the four operator items unstaged.

## 8. Open questions for coordinator disposition (nothing resolved silently)

- **OQ1 — the pause switch's current position is not served by any landed read** (grep-verified: `PauseStore.Paused` is interface-declared and handler-uncalled; `GET /api/meters`, the SSE fleet snapshot, and the session read carry no pause field). The packet's wall says a needed backend change is a STOP-and-flag — this is that flag, raised at grounding so the executor never hits it mid-build. **RECOMMENDED (a): ship read-less this packet** — the two-position control per R10 with the honest not-served state (the platform's own honest-absence discipline applied to itself), and queue the one-line read (e.g. an `automation_paused` member on `GET /api/meters`, mirroring §39-B's own seams) as a coordinator-sanctioned narrow backend addition for a LATER packet if the operator wants live position display. **Alternative (b): sanction the read NOW as a recorded exception** (like C-1's sanctioned class) — one additive member on an existing owner-scoped read, server-side test, fixture regeneration; the control then renders true position. (a) keeps the freeze absolute; (b) is the better control room at the cost of one sanctioned additive read. Dispose before executor launch.
- **OQ2 — "resume — I was wrong" placement (the coordinator asked for this read).** Grounded answer: it is `POST /api/runs/{run}/resume` (S14.4, the ratified S02.3 parked→running edge, §39-B), it is NOT one of the fourteen gap routes, and it is **already surfaced** — the Inbox watchdog-flag card calls `api.resumeRun` (Inbox.tsx:1066–1074) with tests (inbox.test.tsx:556–600). It is also a DIFFERENT verb from this packet's pause group: the meters switch's "resume" is `paused:false` on the same pause endpoint. **RECOMMENDED: excluded from this packet's build scope** — the pause control's post-pause copy may LINK to the inbox where parked runs release (R11), and nothing else. Confirm the exclusion.
- **OQ3 — cancel mount breadth.** RECOMMENDED: task detail only (task-level + per-run) this packet — the board CardFace is pinned "nothing more" by its own comment and tests, mission-control rows are UI-5's D8 restyle target, and every run is one click from its task detail. Alternative: also mount per-run cancel on mission-control `RunLine` rows (run_id + owner already in scope there) — more reach, more markup-coupled test churn on a surface UI-5 restyles anyway. Dispose (a) task-detail-only / (b) also mission control.
- **OQ4 — verb-response test bodies: hand-scripted vs fixture-locked.** The landed precedent for POST responses is hand-scripted bodies (board priority-hint, inbox resume). The §42 root-cause lesson (fixtures with imagined keys pass while production returns nothing) pulls the other way. **RECOMMENDED: hybrid** — (i) verb RESPONSES stay hand-scripted per test with the Go type's file:line cited beside each scripted body (R22); (ii) the post-declare READ state (a declared-budget `GET /api/meters` body) enters the GOLDEN fixture world driven by the REAL verb through `internal/api/apifixtures_test.go` (the §42 OQ2 mechanism; test-only Go change; the shared `cursor` moves and the move is stated + regeneration-stable), because a pre/post-declaration test against an imagined declared-state read body is exactly the inert-probe class. Alternative: all-hand-scripted (zero fixture churn, weaker tie). Dispose (a) hybrid / (b) all-hand-scripted.

**Spec conflicts found at grounding: none.** S15.5/S15.6 bind the surfaces' honesty and scoping posture and are silent on these controls' visuals (the D8 grounded finding); the approved proposal governs appearance; every behavior this packet renders is a landed, spec-cited verb contract. The one tension found (no pause-state read) is a completeness gap in the landed surface, not a spec conflict — carried as OQ1, not papered over.

## 9. SCOPE VERDICT

**One opus sitting is plausible**: one ecosystem (web-only), zero migrations, zero backend, two host files, four client members, and the test surface is the bulk. Pre-authorized A/B split at the natural seam if the sitting overruns (the standing precedent — a stop at the seam is a success mode):

- **A — cancel**: R1 (cancel members) + R2 (the two cancel exception entries + pins to 12/11) + R3–R8 + their slice of R17–R24. Lands as: cancel live on task detail, sweep honest, battery green, bundle measured (the @base-ui first-mount lands HERE — Modal ships with the confirm).
- **B — the fleet pair**: R1's meters members + R2's remaining two entries (pins to 10/9) + R9–R16 + the rest. Lands as: pause + budget live on fleet, the pre/post-declaration proof, battery green, bundle re-measured.

Order A → B. OQ1 and OQ4 need disposition before EITHER part launches; OQ2/OQ3 before A.
