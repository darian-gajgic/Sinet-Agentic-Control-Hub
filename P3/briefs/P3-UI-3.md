# P3-UI-3 brief — Controls II: standing benchmark opt-in + the memory family (browse / read / create / edit / retire / owner-delete)

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical behavior text is `Spec/drafts/` (frozen v1): **S09** (`S09-memory-knowledge.md` — layers/writers §S09.1, scopes/schema/owner rights §S09.2, gated pipeline + v0 activation §S09.4, provenance/removal/true-deletion §S09.5, sharing v1 boundary §S09.6, precedence/conflicts §S09.7), **S15.2** (the memory family row + the contract rules), **S15.6** (the inbox — the ninth kind's home), **S15.9** (why the settings surface is NOT this family's home), **S15.12** (cross-cutting duties); plus **`Spec/benchmark-preregistration-v1.md` (BENCH-REG)** — §2 (definitions), §3 (protocol), **§4.1/§4.2 (sampling + the standing opt-in eligibility limb, the feature-list 11.2 consent)**, §15 (honest-claims table — the §5 determination below says where it binds), §17 (no-retuning — why no registered text is restated client-side). **The design authority for this batch is the operator-APPROVED `P3/design/design-approach-proposal.md`** (§3 pattern vocabulary, §5 binding constraints) — **subordinate to the spec on all behavior**. The landed-verb record binds as consumed contract: `P3/CONVENTIONS.md` **§40-C (B6-3C memory family) + §39-C (B6-2C opt-in) + §43 (the ninth inbox kind)**, plus **§41-B, §42+§42-B, §46, §47, §48** (constraints extracted in §6). **The spec wins over this brief, over the proposal, and over existing code; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**AGPL BOUNDARY (binding, an explicit evaluation item):** Odysseus is AGPL-3.0 and is an **ideas-only** source, forever. Zero Odysseus code, CSS, markup, or asset may appear anywhere in this packet.

**This is packet 3/7 of the UI batch** (queue row verbatim, `P3/STATE.md` line 148): *"Standing benchmark opt-in (self-only) with a plain-language explanation of what opting in means; memory family: scoped browse/read, gate-routed create/edit, retire, owner-delete — write-gate status and conflict edges surfaced, content-vs-telemetry visibility line held exactly as landed; battery + bundle."*

**HARD WALL — this packet consumes EXISTING landed verbs at their landed contracts: zero API additions, zero event changes, zero scoping changes, zero migrations, zero production `.go` changes — EXCEPT what OQ1's disposition sanctions (the UI-2 OQ1 precedent: a control that cannot display honest current state may earn a coordinator-sanctioned narrowest additive read; do not resolve it silently).** The route-TABLE addition in OQ2 is a client URL-contract change, named and disposed, not assumed. **No new dependency**: the §47 closed set stands; shadcn generation is barred as landed (every addressable style declares `radix-ui`, outside the closed set) — **hand-write on the kit/`@base-ui` or STOP-and-flag** (§47). A needed portal/primitive beyond the kit = STOP-and-flag.

**Model routing:** executor + finalizer **opus** (standing batch directive); grounding/evaluation inherit (Fable); lossless opus fallback on any classifier trip.

---

## BINDING carried facts (every one verified against this session's tool reads, 2026-08-05)

**Baseline, re-measured this session (not carried):** `cd web && npm run test` → **511 tests / 26 files, all green**. `npm run build` → **JS 1,076.30 kB / gzip 326.26 · CSS 53.84 kB / gzip 11.53 · index.html 1.80 kB · 15 font assets (417.30 kB, unchanged since UI-1)**. `go run ./tools/lockgate` → OK, **38 entries; 574 npm packages covered or toolchain-scoped**. HEAD `aac77e9`. Go side at UI-2 landing: 45 packages green / 14 sanctioned env-gated skips. Worktree carries the four long-standing untracked operator items (`Presentation/`, `Research/Presentation/`, `Sinet-Logo.jpeg`, `tools/dbpeek/`) — **never staged**.

### The endpoint-per-control map (the whole surface this packet consumes; registrations read from `internal/api/api.go` this session)

| Control | Endpoint | Handler | Response type (Go, wire) |
|---|---|---|---|
| Standing opt-in (set) | `POST /api/benchmark/opt-in` (api.go:529) | `handleBenchmarkOptIn` (benchmark.go:391) | `OptInSet {person, enabled, detail}` (benchmark.go:385–389) |
| Opt-in current state (read) | **NOT SERVED by any landed read — OQ1** | — | `GET /api/benchmark/verdicts` (api.go:528, already consumed: `api.benchmarkVerdicts` api.ts:1835, Inbox.tsx:184) serves pairs + vocabulary + `guess_required` and **no opt-in flag**; `Store.OptedIn` (internal/benchmark/store.go:467–478) exists and is **handler-uncalled** |
| Memory browse | `GET /api/memory` (api.go:636) | `handleMemoryList` (memory.go:192) | `MemoryList {entries[], visibility, projects[], cursor, truncated}` (memory.go:175–186) |
| Memory read one | `GET /api/memory/{entry}` (api.go:639) | `handleMemoryDetail` (memory.go:281) | `MemoryEntryDetail {entry, conflicts[], cursor}` (memory.go:273–279) |
| Create (gate-routed) | `POST /api/memory` (api.go:637) | `handleMemoryCreate` (memory.go:360) | `MemoryEntryDetail` (the write's own read-back, conflicts addressed to the writer included) |
| Edit = new version (gate-routed) | `POST /api/memory/{entry}/new-version` (api.go:640) | `handleMemoryNewVersion` (memory.go:397) | `MemoryEntryDetail` |
| Retire (remove) | `POST /api/memory/{entry}/remove` (api.go:641) | `handleMemoryRemove` (memory.go:511) | `MemoryRemoved {entry_id, status, influence[], detail}` (memory.go:494–502) |
| Owner-delete (true deletion) | `POST /api/memory/{entry}/delete` (api.go:642) | `handleMemoryDelete` (memory.go:562) | `MemoryDeleted {entry_id, tombstone, tombstone_note, detail, limits[]}` (memory.go:538–546) |
| Conflict resolve — **NOT this packet's to build** | `POST /api/memory/conflicts/{conflict}/resolve` (api.go:638) | already consumed: `api.resolveMemoryConflict` (api.ts:1847), Inbox.tsx:1125 | the ninth inbox kind's verb stays the inbox's (§40-C OQ6, §43); this packet **links** to it |

These are **seven of the sweep's ten remaining gap routes over six of its nine shapes** (`web/src/sweep.test.tsx:499–518` memory ×6, :533–537 opt-in; pins at :612–615). After this packet: **3 routes / 3 shapes remain** (`POST /api/deliverables/{}/follow-up`, `GET /api/events/ask`, `GET /api/events/search`).

### The landed memory contract, as read this session (`internal/api/memory.go`; §40-C — consume, never re-litigate)

- **OQ5 — visibility is the CONTENT line and it is SERVED as data.** A caller sees: own entries in every scope they own (S4.1) + house-scope entries (S4.5) + project-scope entries of projects they own or are invited to (S09.6). **The operator role bit opens NOTHING of another member's user-scope store** — memory is personal content; D10 is house authority; §30's operator-sees-all is about runs and telemetry and does not reach here. Both directions are pinned server-side, structurally (`Store.ListVisible` has no role parameter). The browse response's `visibility` field carries the rule as a served sentence (`memoryVisibilityRule`, memory.go:188–190) and `projects` names the membership the answer rests on.
- **The gate is the only write path; every refusal carries the GATE'S OWN message**, mapped on error TYPE (memory.go:795–825): `ErrNotHuman`/`ErrOperatorRequired`/`ErrNotOwner` → 403 · `ErrScopeDormant`/`ErrLayerForbidden`/`ErrInvalidEntry` → 400 · `ErrNotActive` → 409 `conflict` · unmarked → 500. **`ErrNotHuman` means the dev-posture identity can never write memory** — no writeability flag is served anywhere, so write controls render and refusals render verbatim when fired (the §48 `failed` arm; the settings absent-not-disabled pattern does not apply because nothing here declares `editable:false`).
- **Per-verb authority, the client predicate must mirror the door exactly (B6-6 D9: rendered ⇔ allowed):** create user-scope = any real user · create project-scope = member of the named project (the transport wall, memory.go:468–489; options come from the SERVED `projects` list, never free text) · create house-scope = **operator only** (D10) · new-version = **owner or operator** (§40-C drain D6: a co-member superseding a project entry they can read is refused `ErrNotOwner`; the operator limb succeeds and keeps the original owner) · remove = **owner or operator** (§17 reading 4) · true-delete = **strictly the owner — the operator is refused** (S4.1; G2 Def.9; §40-C: "authorship, not visibility") · conflict resolve = **affected owner only** (`mayAnswerConflict`, memory.go:607 — the inbox's verb).
- **Origins:** the create body's `origin` is omitted/`human_direct` for the workspace entry; `imported` is the operator's N20 ceremony (403 for a member, sentence at memory.go:440–441); `adopted_from`/`proposed_from` are refused BY NAME (400, memory.go:445–447). **This packet's form always omits `origin`** (= human_direct) — the import ceremony gets no UI (a named narrowing, §R14).
- **Browse filters** `?scope=`/`?kind=`/`?status=` are the CLOSED S09.2 vocabularies (memory.go:68–84): scopes `user|project|house|worker_overlay`, kinds `lesson|preference|playbook|rubric|style|exemplar|convention|observation|taxonomy|trigger_rules`, statuses `proposed|active|retired|removed`; unknown value = 400 "the S09.2 … vocabulary is closed". List cap = `readPageDefault` **200** (memory.go:57; reads.go:51); `truncated` is observed, not guessed.
- **Retire** (`Gate.Remove`, body `{reason?}` optional): response detail (memory.go:504–506) states removal semantics + names the re-verification OFFER as "the workspace UI's act over this data"; `influence[]` is the S2.9 forward trace served verbatim — `InfluenceRef {run_id, event_seq}` (internal/memory/store.go:406–409), **no task ref, so no link target exists in the route table** (see OQ5). Retire on a non-active entry → 409 `ErrNotActive` (single-subject verb — the `nothingFired` default note is true).
- **True deletion**: read-back of the tombstone stub (id, dates, note — **no content, no title on the wire**); `limits[]` is the S09.5 honest-limits list SERVED AS DATA (memory.go:551–556: git history until an S13 purge · bytes released at next `VACUUM INTO` · accepted artifacts stay — history is not rewritten (D9) · no weights to unlearn, no fine-tuning exists). Idempotent: a repeat reads the tombstone back with exactly one `knowledge.delete` row (§40-C D6).
- **Conflict edges on detail are addressee-filtered server-side** (drain D2): `conflicts[]` carries only OPEN edges the CALLER is the affected owner of; a writer whose write raised a question addressed to someone else does not see that edge — **an honest absence, not a bug; assert it, don't compensate**. The wire shape is the client's existing `MemoryConflict` type (api.ts:706–717) field-for-field (internal/memory/browse.go:135–145) — **REUSE it**.
- **Wire shapes to transcribe** (snake_case as served): `MemoryEntry` (memory.go:106–129: entry_id, owner, scope, scope_ref?, layer, kind, title, content?, file_ref?, file_commit?, topic_key?, selectors, status, tombstone, tombstone_note?, provenance, verification, last_injected_ts?, created_ts, updated_ts) · `MemoryProvenance` (:133–141: origin, origin_ref?, proposer_model?, approved_by?, approved_ts?, version, supersedes?) · `MemoryVerification` (:145–150; a zero interval is a real answer — "no re-verify interval unless a human flags one") · `Selectors {domain?, project?, task_type?, triggers?[]}` (internal/memory/memory.go:94–99) · write body (memory.go:328–342: scope, scope_ref?, kind, title, content, topic_key?, selectors?, file_backed?, file_name?, origin?, origin_ref?).
- **Tier: an own-store write is Medium — NO PIN, NEVER batched** (§40-C; S15.6; zero `auth.reprompt` is a measured server-side absence). The manual human-direct write IS the individual owner approval — the person at the surface deciding.
- **Not routed, and the surface must not compensate:** `lesson_proposals` (read into nothing), `Adopt` (the v1 browse-and-adopt sharing surface, S09.4 activation table — **browse here is the S4.1 own-scope right, not the sharing UI**), `knowledge_search` (run-scoped engine tool), any L1 write (the gate refuses `observation` — that refusal IS the v0 dormancy).

### The landed opt-in contract (`internal/api/benchmark.go`; §39-C; BENCH-REG)

- `POST /api/benchmark/opt-in` body `{person?, enabled}` — **`person` exists only so a delegation ATTEMPT is refusable; this packet's control NEVER sends it** (subject defaults to the caller). Self-only at transport AND verb: 403 *"the benchmark opt-in is the requester's own consent and is not delegable — nobody sets it for anybody else, the operator included (BENCH-REG §4.2.1)"* (benchmark.go:415–418). Success detail (render verbatim): *"your standing benchmark opt-in is recorded as a human decision with its actor and its timestamp. It defaults OFF and no boot step, migration or administrator turns it on (BENCH-REG §4.2.1)"* (:426–427). 503 `not_wired`: *"the S14.7 benchmark practice is not wired in this process"*. The flip mints its own family-5 `decision.recorded` inside the verb's transaction (nothing minted at the transport; a repeat same-position flip still records — **the response has no changed/no-op arm; do not invent one**).
- **What opting in MEANS (the plain-language explanation's ground — BENCH-REG §2/§3/§4.2, restated in operator words, NO registered numbers):** with the standing opt-in ON, an accepted task of yours in a registered launch domain may be sampled; a sampled task is run ONCE more, single-shot, against your own frontier surface with the same frozen task statement and attachments; **the duplicate run's consumption comes from YOUR automation budget** (§4.2.1); you then get a blind A/B card in the inbox — you pick which is better AND must guess which was the platform's (§3.3); **you may decline any specific sampled pair — declines are logged and the decline rate is reported beside every win rate** (§4.2.5); the record is keep-forever (§14); the flip itself is logged as your decision. **Registered numbers (rates, thresholds, n) are §17-frozen text with exactly two homes (the registration + `internal/benchmark/registered.go`) — the client must not become a third**: the explanation describes mechanism, quotes no figure, and every served `detail` renders verbatim.
- **No landed read serves the caller's current opt-in state** (grep-verified: `Store.OptedIn` handler-uncalled; the verdicts read, session read, and settings read carry no flag; `RegisteredValues` is registered-settings display, not per-user consent). **This is OQ1 — the UI-2 OQ1 class, flagged at grounding so the executor never hits it mid-build.**

### Where the acts land + liveness (events read this session)

- Memory verbs ride the four LANDED knowledge events minted by the store inside the gate's transactions: `knowledge.write`, `knowledge.remove`, `knowledge.delete`, `knowledge.conflict` (contract.go:468–471, VerdictAdmit — admitted to the feed) — **zero `decision.recorded`** (§40-C). The SSE feed is owner-scoped per event `user_id` (sse.go:51–58): **your own writes reach your tab; the operator's unfiltered relay sees all; a conflict card's ADDRESSEE gets no frame** (the event carries the WRITER's id — the B6-6 OQ1 ground for the inbox's `NoFrameNote`, Inbox.tsx:196–208). The shell's one stream is the no-topic relay, so client-side `types:` filtering suffices — **zero `live.ts`/`events.ts` changes**.
- The opt-in flip mints `decision.recorded` under the subject's id — `inboxEventTypes` already carries that type, so an inbox-mounted control re-reads on the landed subscription with zero `live.ts` change.
- The golden fixture world wires the **REAL memory store + gate** (apifixtures_test.go:131, 1803–1804) and already drives the ninth kind through `POST /api/memory` (`driveFixtureMemoryConflict`, :1016–1075 — two alice user-scope lessons on `topic_key` `release-notes-style` with one open conflict; entry ids are pinned surrogates after the real write, the recorded concession). The benchmark seam there is a FAKE (`fixtureBenchmark`, :1815+; `SetOptIn` answers not-wired). No committed memory fixture body exists yet; `benchmark-verdicts.json` exists.

### The mount/route ground (read this session)

- `web/src/routes.ts` is the §41-B URL contract, **test-pinned as an exact list** (routes.test.ts:8–22) — and the pin's own comment anticipates growth: *"a later packet adds its route to the contract before it builds the surface"* (routes.test.ts:24–29). The table's ban is on RENAMES, not additions (routes.ts:1–12).
- The sweep's own record: the memory family is **"not an omission inside a built view"** — S15's surface list (S15.5–S15.11) names no memory browser (sweep.test.tsx:509); the family walk carries `{ family: 'memory', root: '/api/memory', surface: null }` and pins `orphans === ['memory']` (:730, :747–751). S09.4's v0 activation row requires **"Manual L2 — human-direct entries via workspace UI [XREF:S15]"** live at v0, and S15's closing note names the seam ("the `/api/memory` resource family (S15.2 ↔ S09's manual-L2 workspace-UI seam)"). S15.9 rules the settings surface out as host (it is "generated, never hand-built" from the registry; memory is content, not configuration; the benchmark opt-in is a users-table consent column, **not a ⚙ registry key**, so S15.9's every-setting mandate does not capture it either).
- `App.tsx` owns the session read (`useSession`) and hands `me` to surfaces that act (Board/MissionControl/Deliverable precedent); the route switch is a ternary chain ending in `Stub`; nav grouping derives from the table (`navSections`/`navGroupOf` — §47: a route with no label renders in its own unlabelled run; shell.test.tsx derives expected hrefs from the table, no literal pin).
- The inbox renders the ninth kind with deep link `hrefFor('inbox-item', { id: 'memory_conflict:<conflict_id>' })` (Inbox.tsx:701) and resolve via `api.resolveMemoryConflict` — resolution's home. Its `NoFrameNote` is the honest-currency precedent.
- Kit + idioms consumed (§47/§48; `web/src/ui/index.ts`, `web/src/controls.tsx` read this session): `Button/Panel/Chip/StatusDot/StatTile/Modal/Drawer/Skeleton/EmptyState/Timestamp(live|audit)/cn()/tones`; `useAct` (busy + unconditional re-read incl. after refusal), `refusalOf` (classifies on machine CODE; 409 → `retry`), `nothingFired` (single-subject 409 note — TRUE for every verb here), `outcomeOf`, `OutcomeLine` (`data-outcome` = applied|noop|retry|failed), `ActConfirm` (confirm-before-fire; danger for destructive, primary for declarations, ghost dismiss). **ToastProvider stays out of the production tree (UI-7's).** Test harness: `scriptedFetch` doubles (default session = **alice-as-OPERATOR**, doubles.ts:174–177 — member legs MUST override the session), `mount/typeInto/choose/click` (testing.tsx).
- **Carried note N7 (UI-2 evaluation, STATE line 170) binds THIS packet's editor idiom:** the budget editor conflated busy-with-invalid on its confirm. Here, **invalid and busy are separate signals**: an invalid form states its reason visibly and fires nothing; busy means exactly one thing — a request in flight. Asserted in both directions.

---

## 1. Numbered requirements

### A — Routes, client seam, and the sweep's bookkeeping

- **R1. The route-table addition, per OQ2's disposition** [routes.test.ts:24–29; S09.4 v0 activation row; sweep.test.tsx:509]: `routes.ts` gains `{ id: 'memory', pattern: '/memory', title: 'Memory', owner: '', nav: true }` and `{ id: 'memory-entry', pattern: '/memory/:id', title: 'Memory entry', owner: '', nav: false }` (RouteID union +2); `routes.test.ts`'s pinned list and `built` array updated honestly (+2 each; the two-sided owner assertion and deeper-path/not-found tests stay green). **No landed route is renamed or reordered.** `App.tsx` changes are exactly: the two switch branches mounting the new surface(s) with `me`/`operator` passed from the existing session (the Board/MissionControl precedent — no second session read; Fleet's recorded deviation is not repeated), plus a `navGroupOf` entry (or none — an unlabelled run is the landed fallback). Everything else in `App.tsx` (session machinery, ConnectionState, redirect rules) is byte-untouched.
- **R2. New `api` members, one per verb, types transcribed field-for-field** [§48 seam 5; the wire shapes above]: `setBenchmarkOptIn({enabled})` (never `person`), `memoryList(filters?)`, `memoryEntry(id)`, `memoryCreate(body)`, `memoryNewVersion(id, body)`, `memoryRemove(id, {reason?})`, `memoryDelete(id)` (names are the executor's, recorded in §49; the landed naming style is the precedent). Client types transcribe the Go wire shapes verbatim, snake_case as served; the existing `MemoryConflict` type is REUSED for `conflicts[]`. Every member gains a real caller in the same change — `declaredButUncalled()` stays exactly `['health']`.
- **R3. The sweep's pins move deliberately, exactly as the sweep instructs** [sweep.test.tsx:598–619; §46]: the SEVEN exception entries (memory ×6, opt-in ×1) are REMOVED (the stale-entry test forces this the moment they are consumed); pins move **10→3 routes / 9→3 shapes / gapRoutes 10→3**, comment updated to say which packet closed which seven; the family walk's memory row becomes `surface: 'memory'` and **`orphans` moves `['memory']` → `[]`** with its message updated (:747–751 — the "one family with NO surface" test is renamed/reworded honestly to assert the emptied set); the dated cross-note lands in CONVENTIONS §46's gap record (append-dated, never rewritten).

### B — The standing benchmark opt-in (mount per OQ3)

- **R4. The control renders REGARDLESS of pending pairs** [sweep gap text: "The verdict form renders for a participant; nothing offers the opt-in that makes somebody one"]: a standing consent section on the OQ3-disposed surface, visible with zero pairs pending — it is what MAKES somebody a participant. It is the caller's own switch only; no person picker exists anywhere (self-only is the verb's law, benchmark.go:409–418).
- **R5. Two explicit position acts, never a state-guessing toggle** [the UI-2 pause idiom; optInBody]: "Opt in" posts `{enabled:true}`, "Opt out" posts `{enabled:false}` — both always available (a repeat records again; the server has no no-op arm and the control invents none). Current position renders per OQ1's disposition: with the sanctioned read, the served flag is the position; without it, the honest not-served state (never a fabricated "off"). Confirm-before-fire via `ActConfirm` (variant `primary` — a declaration, not a destruction), the what-sentence faithful to the explanation in R6.
- **R6. The plain-language explanation, faithful and figure-free** [BENCH-REG §2/§3.1–§3.3/§4.2.1/§4.2.5/§14; §17]: pre-act, beside the control, in operator words: sampled-when-opted-in; one extra single-shot run of the same frozen task statement on YOUR OWN surface; **paid from YOUR automation budget**; the blind A/B card + mandatory platform-guess arrives in the inbox; **any specific pair can be declined, and declines are logged and reported beside win rates**; the standing flip is itself recorded as your decision; it defaults OFF and nobody else — the operator included — can set it. **Zero registered numbers quoted** (no rates, no n, no thresholds — §17's two-homes rule; a probe asserts no `%`/`n=`/numeric-threshold token renders in the explanation). Post-act, the served `detail` renders VERBATIM; refusals (403 delegation — unreachable by this control but scripted anyway; 503 not_wired; 400 `ErrBadInput` no-user, the dev-posture arm) render the served `{error, detail}` via `refusalOf`.
- **R7. The act lands where decisions render** [§39-C; live.ts]: the flip's `decision.recorded` frame re-reads the mount's landed subscription (inbox-mounted: `inboxEventTypes` already carries it — zero `live.ts` change); `useAct`'s unconditional re-read covers the frameless fallback.

### C — The memory surface: browse + read (the new route)

- **R8. Browse renders the SERVED answer and nothing else** [MemoryList; §42]: entry rows (title; kind + scope + status as kit `Chip`s; owner; version; `updated_ts` via the `Timestamp` primitive — never an inline `<time>`, the sweep pin); the served **`visibility` sentence renders VERBATIM** as the surface's own scope statement (the content-vs-telemetry line said in the server's words — never paraphrased); `projects` renders as the membership the answer rests on; `truncated:true` renders as the honest more-than-this-page statement (no fabricated count). Filter controls offer EXACTLY the closed S09.2 vocabularies (memory.go:68–84) as `<select>`s sending `?scope=`/`?kind=`/`?status=` — **server parameters, not client sieves** (S15.2 presentation-only rule; no owner-comparing `.filter` joins the sweep's exact-partition list). The empty state teaches via kit `EmptyState` (what will appear here and why — the proposal's sentence pattern), distinct from the not-wired 503 arm (served message verbatim).
- **R9. Entry detail renders the whole served record honestly** [MemoryEntryDetail; S09.5]: title/kind/scope(+scope_ref)/layer/status; content as escaped text (React text nodes; escape allowlist stays EMPTY); a file-backed entry renders `file_ref` + `file_commit` as text (the row is the index, the file is the content, git is the history — S09.2; **no bytes are fetched, no filesystem link invented**); provenance block (origin, origin_ref, proposer_model when present, approved_by/approved_ts, version, supersedes) — ids/versions/topic_key in mono per the token contract; verification block with the zero-interval honest arm ("no re-verify interval unless a human flags one" — the wire's own semantics, memory.go:145–150); `last_injected_ts` absent = honestly never-injected, never "0"; the tombstone arm renders `tombstone_note` alone (the wire carries no content/title for a stub — render the stub as what it is). Timestamps via `Timestamp` (audit variant for provenance/audit fields; the live-variant choice on the list is the executor's D5 taste, recorded).
- **R10. Conflict edges surfaced exactly as landed** [S09.7; drain D2; §43]: the served `conflicts[]` render as question cards — the question text verbatim, the other entry id (mono, linked to `hrefFor('memory-entry')`), `detected_ts` — **with resolution as a `Link` to the inbox card** (`hrefFor('inbox-item', { id: 'memory_conflict:<conflict_id>' })`, the Inbox.tsx:701 shape). **This surface does NOT call `resolveMemoryConflict`** — the ninth kind's verb keeps its one home (§40-C OQ6); one door, one place. The addressee filter is asserted, not compensated: a scripted detail whose write raised a question addressed to someone else serves an empty `conflicts[]` and the surface renders no edge (the honest absence).
- **R11. Liveness rides the landed frames, honestly noted** [sse.go:51–58; contract.go:468–471; the NoFrameNote precedent]: the surface subscribes (client-side `types:`) to `knowledge.write|knowledge.remove|knowledge.delete|knowledge.conflict` on the shell's one stream — own acts push own re-reads (and everything pushes for the operator's relay). A short honest-currency note in the `NoFrameNote` vein states what does NOT push for a member (changes by other people; conflict questions raised against your entries by someone else's write) and that the page re-reads on visit and after every act. **Zero `live.ts`/`events.ts` changes**; no poll, no ticker (§32).

### D — Create + edit (gate-routed writes)

- **R12. The create form maps the landed write body and nothing beyond it** [memoryWriteBody; S09.2]: fields — scope (options mirror the doors: `user` always; `project` only when the served `projects` list is non-empty, `scope_ref` chosen FROM that list, never free text; `house` only for the operator role — D10), kind (the NINE L2 kinds — `observation` is the L1 layer with no v0 writer and is not offered; the gate's refusal is the ground, S09.1/S09.4), title, content, `topic_key` (optional — explained as the conflict-lookup key, S09.7), selectors (optional: domain / project / task_type / triggers — rule inputs, never embeddings). **`origin` is never sent** (human_direct is the omission); **`file_backed` is not offered** — the workspace entry is text-first; the N20 import and file-backed authoring have no UI at v0 (named narrowing, recorded in §49).
- **R13. Edit is a NEW VERSION and says so** [S09.4 station 4; handleMemoryNewVersion]: the edit form opens prefilled from the served entry and posts the FULL draft to `new-version`; the copy states the S4.1 correction-right truth — a new version supersedes the old, the old is retired in the same transaction, nothing is mutated in place. The response renders as the new entry (version chain visible: `version`, `supersedes` linked to the old entry's detail route).
- **R14. Write-gate status surfaced honestly** [queue row; §40-C]: the create/edit acts are **tier Medium — no PIN field, no step-up UI, no batch affordance anywhere** (a PIN prompt here would invent a ceremony the platform does not perform); the served entry's `status` chip IS the gate status (`active` on landing — the human-direct write is the station-3 approval, the person deciding at the surface); a write's response `conflicts[]` (addressed to the WRITER) render immediately per R10 — a write that raised a question SHOWS the question in the same breath.
- **R15. N7 — busy and invalid are SEPARATE signals, both directions** [carried note, STATE line 170]: an invalid form (empty title, empty content, project scope with no project chosen) states its reason visibly at the field or form level and **fires nothing** — the reason is the client's own pre-validation sentence, never a fake server answer; busy renders ONLY while a request is in flight. Asserted: (a) invalid → reason rendered + fetch-log empty + the act button not busy-styled; (b) valid + in-flight → busy + no invalid-reason present; (c) pre-validation never suppresses a server answer once a request fires (the gate's own message renders verbatim — `ErrInvalidEntry`, `ErrScopeDormant`, the project-membership 403, the `ErrNotHuman` dev-posture arm).
- **R16. Authority rendered per verb, mirrored from the doors** [the per-verb map in BINDING facts]: create renders for every signed-in identity (the gate refuses non-users with its own sentence when fired — no writeability flag exists to pre-read); the house option renders only for the operator; edit renders for owner-or-operator; a co-member viewing a project entry gets NO edit control (the D6 ground). Both directions asserted per R26.

### E — Retire + owner-delete (the destructive pair)

- **R17. Retire: confirm-before-fire faithful to the landed semantics** [removedDetail; S09.5]: `ActConfirm` (danger) whose what-sentence asserts exactly: the entry leaves every future assembly immediately; its content stays in git history and its row stays for audit; true deletion is a separate right. Optional `reason` field rides the body. Outcome: `status` + served `detail` verbatim; **`influence[]` renders as served** — run ids (mono) + event_seq, no invented links and no invented one-click re-verify act (OQ5's determination; the served sentence already names the offer in its own words and renders verbatim). 409 `ErrNotActive` (already retired/removed) renders as the `retry` arm with `nothingFired` (true for this single-subject verb); then the unconditional re-read shows the entry's real status.
- **R18. Owner-delete: strictly the owner's control, honest about its limits** [S4.1; G2 Def.9; deletionLimits]: the delete control renders ONLY when the signed-in identity IS the entry's owner — **the operator viewing another owner's house/project entry gets retire (per R16's map) and never delete** (the door refuses authorship, not visibility; §40-C). `ActConfirm` (danger) states: content is purged; what remains is a tombstone stub; **the four served honest limits render VERBATIM as the served `limits[]` data** — in the confirm (from the brief's transcription, cited) or in the outcome (from the wire), the outcome rendering mandatory. After the act: the tombstone read-back renders (`tombstone`, `tombstone_note`), the re-read shows the detail's tombstone arm, and the browse row's status chip follows the read.
- **R19. Destructive-pair copy discipline** [§48 R5-class]: no arm invents a deletion the platform did not perform (retire is not delete; the retire confirm must not say "gone"), no arm promises git-history erasure (limit #1 says the opposite), and the two verbs are visually distinct acts with distinct confirms — never one button with a mode.

### F — Cross-cutting: scoping, honesty, discipline

- **R20. The content-vs-telemetry line held EXACTLY as landed, asserted in the operator direction** [OQ5; §40-C]: over a scripted OPERATOR session and bodies, the browse renders no member's user-scope entry (the served body is the authority — the test asserts the UI adds no synthesis, and the served `visibility` sentence renders verbatim); over a MEMBER session, no cross-owner control renders anywhere (browse rows, detail verbs, the opt-in switch). The sweep's owner-filter exact-partition list gains NO new site (this surface narrows display only via server parameters).
- **R21. Escape + honesty scans hold over every new surface** [§41-B; §42]: entry content/questions/titles render as escaped text (allowlist stays EMPTY, 8 banned tokens); no `%`/`percent`/`eta`/progress primitive; no money token anywhere (memory and consent carry no figures; the budget-adjacent probes extend over the new markup); no client arithmetic over served figures; ids/versions/seqs in mono `tabular-nums`; no web storage; in-app links only via `hrefFor`/`Link`.
- **R22. Built on the kit, styled by tokens, motion gated** [§47]: kit primitives + Tailwind utilities over the token set — zero raw hex, zero new `index.css` rules (a bespoke rule needs a §49-recorded reason and touches no landed rule), zero global class-name collisions; motion utilities in surface files carry `motion-safe:`; anything reusable lands in `web/src/ui/` under the kit scans; **no direct `@base-ui/react` import outside `web/src/ui/`** (the §48 wall); `controls.tsx` is consumed as landed (an extension there needs a recorded two-consumer reason). The new surface is the first built new-language-only — no legacy CSS is added for it.
- **R23. 375px + desktop** [S15.12; §41-B]: the memory surface and the opt-in section are reachable and operable at 375px (min-width-only queries; no fixed-pixel container; modal/drawer at their landed phone behaviors) and comfortable at desktop. Memory is NOT in the S15.12 phone-complete named list — the sweep's phone-complete walk is untouched; the responsive discipline still binds.
- **R24. Behavior freeze, verified** [batch rule; the hard wall]: zero production `.go` changes EXCEPT OQ1's sanctioned shape if granted (then: exactly the named files, the wall asserted otherwise); `internal/api/memory.go` byte-unchanged REGARDLESS; `go.mod`/`go.sum`, `components.lock`, `web/package.json`/`package-lock.json`, migrations (0001–0021), ⚙ registry (118/33), event inventory (102/5/107), `live.ts`, `events.ts`, `router.tsx`, `index.css`, `parts.tsx`, `controls.tsx` (unless recorded), `Board.tsx`, `MissionControl.tsx`, `TaskDetail.tsx`, `Fleet.tsx`, `Chat.tsx`, `Deliverable.tsx`, `Workforce.tsx`, `PushDevices.tsx`, `Login.tsx`, `History.tsx`, `kanban.ts`, `push.ts`, `sw.js` — all byte-unchanged. `Settings.tsx`/`Inbox.tsx`: exactly ONE of them changes, per OQ3's disposition, by the opt-in section alone. No new event type, no scoping change, no ⚙ key invented or read client-side.
- **R25. The Layer-1 catalog is untouched** [batch rule — AT its 50/50 band ceiling]: nothing in this packet asks a history question or adds a canned query; influence, conflicts, and status all render from THIS family's served reads.
- **R26. Tests: the suite only grows; every authority direction FIRED, not merely rendered** [B6-9 discipline; §48 drain D4]: baseline **511 / 26 files**; a new `memory.test.tsx` (and the opt-in tests in the OQ3 host's file) cover: route resolution + nav presence; browse render incl. the verbatim visibility sentence, filters as server params, truncation, empty + 503 arms; detail render incl. provenance/verification/tombstone/file-backed arms; conflict render + inbox link + the addressee-absence assertion; create/edit/retire/delete flows — confirm-before-fire (fetch-log zero until the act button, exactly one after), every outcome arm (applied / 409-retry / 403 / 404 / 400 / 503) from scripted bodies **citing the Go source file:line beside each scripted body** (the inert-probe defense), N7 both directions, the R16/R18 authority renders AND the fired legs: member-self create/edit/retire/delete succeed (session override — the default double is alice-as-OPERATOR), operator-on-other retire succeeds, operator-on-other delete REFUSED with the gate's sentence, co-member edit absent; opt-in both positions fired with `{enabled}` bodies proven in the fetch log and no `person` key ever sent. Frame-driven re-reads asserted with the landed frame patterns (a `knowledge.write` frame re-reads the browse; `decision.recorded` re-reads the opt-in host). Per-file before/after test AND assertion counts stated with reasons; **counts never drop silently**.
- **R27. Battery + bundle, in the landing order** [UI-1 R22 / UI-2 R23]: `cd web && npm run typecheck && npm run test && npm run build` → `gofmt -l internal/ cmd/ tools/` (empty) → `go vet ./...` → `go build ./...` → `go test ./... -count=1` (over the REAL new dist) → `go run ./tools/lockgate`. All green before any commit. Bundle measured against **JS 1,076.30 kB / gzip 326.26 · CSS 53.84 / 11.53 · fonts 15 / 417.30 · index.html 1.80**: no new package, `@base-ui` already mounted — the delta is THIS packet's source and newly-compiled utilities only; state raw+gzip for JS and CSS, investigate anything anomalous. SPA stays un-split.
- **R28. Record + commit discipline**: CONVENTIONS **§49** appended (the route additions + mount decisions as disposed, member names, the sweep/family-walk pin moves with the dated §46 cross-note, the per-verb authority map as rendered, the N7 execution, OQ dispositions as executed, the honest-claims §5 determination, bundle numbers); commit subjects `P3-UI-3: … (S09, S15.2/S15.6, BENCH-REG §4, proposal §3)` + the standing trailer; explicit staging only — never `git add -A`, never the four operator items; nothing pushed; `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched by the executor.

## 2. Seams — what UI-4+ and the D8 pass reuse from this packet (state them, build nothing beyond them)

1. **The route-addition precedent**: table row + pinned-test update + App switch branch + nav group — the recorded shape for any future surface addition (none is currently queued; UI-4's controls mount on existing surfaces).
2. **The per-verb authority map idiom**: predicates mirrored from the DOORS per verb (not one blanket own+operator rule) — UI-4's history controls and the D8 pass reuse the discipline wherever verbs differ.
3. **The verbatim-served-sentence idiom**: a served rule/limits/detail sentence rendered as data (`visibility`, `limits[]`, opt-in `detail`) — the D8 self-teaching layer builds ON this (a surface that already speaks the server's own words).
4. **The honest-currency note** (NoFrameNote generalized): stating exactly what does not push, scoped to the truth of the feed's owner-scoping.
5. **The destructive-pair pattern**: retire-vs-delete as distinct confirmed acts with served-limits honesty — reusable for any future soft/hard pair.
6. **Everything inherited from §48 stands unchanged** (confirm idiom, `useAct`/`refusalOf`/`nothingFired`, `[data-outcome]`, editor-form idiom now N7-hardened, api.ts discipline).
7. **Explicitly NOT built here**: history ask/search + C-1 + D5 call-site application + the two S00.9 amendments (UI-4); host-surface restyles, tours/hints/teaching layer beyond the served-sentence renders (UI-5..7); ToastProvider (UI-7); the v1 browse-and-adopt sharing surface, lesson-proposal queue, L1 anything, knowledge-search UI (v1/other owners); the benchmark READOUT views (§5 determination); any re-verify-queue verb (unlanded).

## 3. ⚙ settings consumed (by registry name)

**None consumed, none created — say so is the requirement.** Determination made at grounding against `S09`'s ⚙ table (S09 lines 130–145): all twelve `memory.*` keys (`l1_ttl_days`, `reverify_lessons_days`, `reverify_house_days`, `proposals_per_task_max`, `digest_interval_days`, `distill_threshold_lessons`, the four `injection_budget_tokens.*`, the two `vector_gate.*`) govern **backend lifecycle/injection/gate-trigger machinery — none binds a surface behavior this packet renders**; the verification INTERVALS this packet shows arrive per-entry on the wire (`reverify_interval_days`), never from the registry. BENCH-REG's sampling **rate** is tunable ⚙ on the backend and is deliberately NOT quoted by the opt-in explanation (R6). The client reads no registry key (§41-B posture); `internal/settings/index.go` stays byte-unchanged at 118/33. Server structural constants touched by nothing here: `memoryListCap` = 200 (served truncation renders honestly).

## 4. Files expected to change

- **Modified:** `web/src/routes.ts` + `web/src/routes.test.ts` (R1); `web/src/App.tsx` (R1's narrow diff only); `web/src/api.ts` (R2); `web/src/sweep.test.tsx` (R3 pins + exception removals + family walk); **one of** `web/src/Inbox.tsx` + `web/src/inbox.test.tsx` **or** `web/src/Settings.tsx` + `web/src/settings.test.tsx` (OQ3; the opt-in section + its tests); `web/src/shell.test.tsx` only if a derived assertion needs an honest count bump (it derives from the table — expected to pass unmodified, §47); `P3/CONVENTIONS.md` (§49 + dated §46 cross-note).
- **New:** `web/src/Memory.tsx` (+ optionally `web/src/MemoryEntry.tsx` — executor's structure, recorded); `web/src/memory.test.tsx` (file count 26 → 27).
- **Per OQ4's disposition (test-only Go + fixtures):** `internal/api/apifixtures_test.go` (drive real memory bodies through the landed store+gate; the shared `cursor` moves — stated, regeneration-stable) + new `web/src/fixtures/api/memory*.json`; `web/src/doubles.ts` only if a shared scripted default earns its place (per-test scripting is the precedent).
- **Per OQ1's disposition (sanctioned production Go, the ONLY exception):** `internal/api/benchmark.go` (`BenchmarkSurface` + the served block), the `internal/shell` seam adapter, `internal/api/benchmark_test.go` + the fixture seam + `web/src/fixtures/api/benchmark-verdicts.json` (+ regeneration). **`internal/api/memory.go` is byte-unchanged regardless.**
- **Expected NOT to change** (the evaluation checks this list): every production `.go` file outside the OQ1 sanction; `go.mod`/`go.sum`; `components.lock`; `web/package.json`/`package-lock.json`; `.github/workflows/ci.yml`; `web/src/{router.tsx,events.ts,live.ts,index.css,parts.tsx,controls.tsx,Board.tsx,MissionControl.tsx,TaskDetail.tsx,Fleet.tsx,Filters.tsx,Chat.tsx,Deliverable.tsx,Workforce.tsx,PushDevices.tsx,Login.tsx,History.tsx,Stub.tsx,kanban.ts,push.ts,chatRuntime.ts,chatFacts.ts,settingsForm.tsx,escape-scan.test.ts,scans.test.ts,design.test.ts,devproxy.test.ts,responsive.test.ts}`; `web/public/**`; `Spec/**`, `Docs/**`, `Research/**`, `P3/STATE.md`. (The un-picked OQ3 host is on this list too.)

## 5. Determination — where the BENCH-REG §15 honest-claims obligation lands (coordinator confirms)

§15's own text: *"ships in the benchmark UI, frozen obligation … **wherever a win rate is shown**, the UI also shows what the current n could even detect."* This packet's opt-in surface shows **no win rate** — it renders consent state, a mechanism explanation, and served detail sentences; no landed read even serves a rate (`PendingVerdicts` carries pairs without arms or rates; `RegisteredValues` is the read-only registered-settings block). **Therefore §15 does not bind this packet's surface; it binds the benchmark READOUT surface (§16's reporting views) — unbuilt, not among the fourteen D3 controls, arriving with accrual at/after bring-up.** The obligation is recorded here so that packet inherits it: the first surface to render a win rate MUST ship the §15 table beside it (n, G, tie/both-bad, decline, guess accuracy — §5/§8/§4.2.5). Conversely §4.2.5's decline-visibility duty IS partially this packet's: the explanation names it (R6).

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format, no push; never edit Spec/Docs/Research/STATE; never stage the four operator items.
- **§39-C/§40-C/§43** — the landed contracts consumed AS LANDED: self-only consent; the gate's walls and messages; OQ5 both directions; OQ6 (the ninth kind stays the inbox's); the tier-Medium no-PIN reading; the D2 addressee filter; the D6 authority shapes. The frontend renders these; it re-litigates none. The `Gate.authorize` project-narrowing REPORT (§40-C, D7-ratified, contained) is NOT this packet's to fix.
- **§41-B** — escape allowlist EMPTY (8 banned tokens); no web storage; `hrefFor`/`Link` only; owned router (the table this packet extends per OQ2); phone-first min-width discipline; structural constants carry named reasons; identity from `GET /api/auth/session` and nowhere else.
- **§42/§42-B** — REST is the truth, frames are the trigger; `useLive` only; absences render with reasons, never filled; figures verbatim; no progress/percent/ETA; the never-percent/money scans bind all new markup; client filtering narrows display only.
- **§46** — the sweep is the completeness instrument; consumption = a CALL; exception list honest both directions; pins move only deliberately; the family walk and orphan pin move with their reasons.
- **§47** — kit contract + `cn()`; tokens/utilities never raw hex; kit motion `motion-safe:`; the shadcn/radix bar (hand-write on `@base-ui` or stop); the migration ledger (hosts restyle later — the opt-in section lands new-language inside a legacy-styled host; the memory surface is new-language throughout).
- **§48** — the act layer (`useAct`/`refusalOf`/`nothingFired`/`ActConfirm`/`OutcomeLine`), the four honest outcome arms, refusal classification on machine code, the editor idiom + N7, the `@base-ui` import wall, the api.ts discipline.
- **Batch rules (STATE line 142)** — proposal is design authority subordinate to spec; bundled assets only; **AGPL ideas-only (explicit evaluation item)**; honest assertion updates, counts never drop; bundle measured, no silent growth; behavior-preserving with named sanctioned exceptions; FC-v1 picks untouched; catalog band ceiling = STOP-and-flag.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a command)

1. **Endpoint fidelity**: the new api.ts members hit exactly the seven routes (methods + paths against api.go:529/636/637/639/640/641/642); types field-for-field against memory.go:106–150/175–186/273–279/328–342/494–502/538–546 + benchmark.go:376–389; `MemoryConflict` reused not duplicated; no other route consumed, none of the seven reachable except through its member.
2. **Sweep bookkeeping**: seven exception entries gone; pins 10→3 / 9→3 / gapRoutes 3 with the which-packet comment; family walk memory→`'memory'`; orphans `[]` with honest message; stale-entry/phantom/reason-length tests green; `declaredButUncalled() === ['health']`.
3. **Route contract**: routes.ts +2 rows exactly as disposed; routes.test.ts pinned list + built updated; deeper-path (`/memory/x/y` → not-found) and round-trip tests green; App.tsx diff confined to switch branches + navGroupOf + props; nav renders the new surface in the grouped shell nav.
4. **Control-per-route completeness for this packet's groups**: every one of the seven routes has a working control (browse list, detail view, create form, edit form, retire confirm, delete confirm, opt-in section) — the D3 mandate's slice delivered whole.
5. **Opt-in self-only + explanation**: no person field anywhere; both position acts fire `{enabled:…}` (fetch-log bodies); the explanation covers sampling/duplicate-run/your-budget/blind-verdict-with-guess/per-pair-decline-logged/standing-recorded/default-OFF; **zero registered numbers** in the copy (probe); served detail verbatim post-act; 503/400 arms rendered from scripted bodies; current-state per OQ1's disposition exactly.
6. **Browse honesty**: visibility sentence rendered VERBATIM from the wire; projects list rendered; truncation honest; filters are server params over the closed vocabularies; empty state teaches; 503 not-wired verbatim; no owner-comparing client filter (the sweep's exact-partition list unchanged).
7. **Detail honesty**: provenance/verification/tombstone/file-backed arms all rendered as served (scripted bodies per arm, Go file:line cited); zero-interval and never-injected absences honest; content escaped; mono on ids/versions/keys; timestamps via the primitive only (the sweep `<time>` pin green).
8. **Conflict edges as landed**: served edges render with question verbatim + inbox `Link` (the `memory_conflict:<id>` shape); NO resolve call from this surface (grep: `resolveMemoryConflict` has exactly its landed Inbox call site); the addressee-absence leg asserted (someone-else's-question serves empty and renders none).
9. **Create/edit gate-routing**: create posts the transcribed body with origin omitted; project scope_ref only from the served projects list; house option operator-only; edit posts the full draft to new-version and renders the new version with supersedes linked; response conflicts render immediately; no PIN UI, no batch UI anywhere (grep + render assertions).
10. **N7 separated**: invalid-with-reason-fires-nothing (fetch-log empty) AND busy-only-in-flight legs both asserted; pre-validation never replaces a fired server answer.
11. **Retire semantics**: confirm copy faithful (leaves-assembly / git-history-stays / delete-is-separate; no "gone", no rollback); reason field optional; influence renders verbatim (run ids mono, no invented links/acts); 409 → retry arm with `nothingFired`; re-read follows every arm.
12. **Delete strictly-owner**: control absent for operator-on-other (asserted over a served body where the operator can read AND retire); the fired refusal leg renders the gate's own sentence; limits[] rendered verbatim as data; tombstone read-back + re-read to the tombstone arm; the two destructive verbs are distinct controls with distinct confirms.
13. **Authority directions FIRED per verb** (not merely rendered): member-self create/edit/retire/delete success legs under a member session override; operator-on-other retire success; operator-on-other delete refusal; co-member edit absent; every fetch-log body/subject proven.
14. **Content-vs-telemetry held**: operator-session browse body carries no member user-scope entry and the UI adds none; member-session renders zero cross-owner controls; the visibility sentence is the served one byte-for-byte.
15. **Liveness**: the four knowledge.* types subscribed on the shell stream; a scripted `knowledge.write` frame re-reads the browse (landed frame pattern); the opt-in host re-reads on `decision.recorded`; the honest-currency note states exactly what does not push; **zero `live.ts`/`events.ts` diffs**; no setInterval (tree scan green).
16. **Kit + scans**: kit-motion and kit-colour scans green; `motion-safe:` on any surface-file motion utility; zero raw hex; no `@base-ui/react` import outside `web/src/ui/`; escape scan green (allowlist EMPTY); percent/money/progress probes green over all new markup.
17. **375px + desktop**: every new control reachable and operable at 375px (structural assertions; no fixed-pixel container; min-width-only) and at desktop; the phone-complete walk untouched.
18. **Behavior freeze verified**: `git diff --name-only` against §4's lists; `internal/api/memory.go` byte-identical; production `.go` diffs empty except exactly OQ1's sanctioned files (if granted), with the wall proven otherwise; frozen web files byte-identical; fixtures untouched except per OQ4 (then regenerated through REAL producers, cursor move stated, zero drift on re-run).
19. **Tests only grow, honestly**: suite ≥ 511, files 27; per-file before/after test AND assertion deltas with reasons; scripted bodies cite Go file:line; no assertion dropped, no scan narrowed, no floor lowered.
20. **Full battery green in R27's order** over the real dist; **bundle recorded** against JS 1,076.30/326.26 · CSS 53.84/11.53 · fonts 15/417.30 with the delta attributed (packet source + compiled utilities only — no new package); SPA un-split.
21. **AGPL attestation**: zero Odysseus-derived code/CSS/markup/assets; pattern-study-descriptions-only.
22. **Record + counters**: CONVENTIONS §49 appended + dated §46 cross-note (§1–§48 otherwise untouched, corrections dated); ⚙ 118/33 and event inventory 102/5/107 byte-unchanged; lockgate 38/574; commits per §5, nothing pushed, the four operator items unstaged; the §5 honest-claims determination recorded in §49.

## 8. Open questions for coordinator disposition (nothing resolved silently)

- **OQ1 — the opt-in control cannot read its own current state** (the UI-2 OQ1 class, checked as directed): no landed read serves the caller's standing opt-in; `Store.OptedIn(ctx, userID)` exists handler-uncalled (store.go:467–478 — the exact `Paused()` shape). **RECOMMENDED: sanction the narrowest additive read** — `BenchmarkSurface` gains an opted-in read wired to the EXISTING `Store.OptedIn` through the shell adapter (the import wall bars a direct store read in `internal/api`, so the seam is the necessary crossing), served as an `opt_in {enabled}` block **for the CALLER only** on the EXISTING `GET /api/benchmark/verdicts` (both postures: member and operator get their OWN flag — consent is self-only in both directions); +server tests, fixture seam + `benchmark-verdicts.json` regenerated. No new route, no event change, no scoping change, no migration, no ⚙ key. Note the mechanical difference from UI-2's sanction (an interface method + adapter, not just a projection read) — same class, one seam wider; the evaluation verifies the wall holds otherwise. **Alternative (b): read-less honest-absence control** — rejected by the pause precedent's own reasoning (a two-position consent control that cannot display current state fails the D8 intelligibility mandate and §42's honesty ethos). Dispose before executor launch. **The memory family needs NO such sanction — grounded: every surfaced fact (entries, visibility, projects, conflicts, influence, tombstone, limits) is served by the landed reads.**
- **OQ2 — the route-table addition (the mount answer, treated as a named question as directed).** The memory family needs a home; the record says no built view owns it (sweep.test.tsx:509 "S15's own surface list names no memory browser"; the family-walk orphan pin), the spec REQUIRES a v0 workspace UI for manual L2 (S09.4 activation row; the S15 closing-note seam), and the pinned route test's own comment anticipates additions (routes.test.ts:24–29 — renames are what §41-B bars). **RECOMMENDED: add `/memory` (nav) + `/memory/:id` (detail, nav:false)** — the task/deliverable list-detail precedent; a stable entry URL makes conflict edges navigable both ways (the inbox card names entry ids) in the S15.11 deep-links-always-land ethos. Alternatives rejected with reasons: a Settings section (S15.9 is the generated registry surface; memory is content, not configuration — and the family's six verbs outgrow a section), Workforce/Chat (wrong families), no-route drawer-over-existing-surface (leaves the family walk orphaned and the S09.4 workspace-UI row unhoused). Route additions are a URL-contract change — ratify explicitly.
- **OQ3 — the opt-in mount.** **RECOMMENDED: the Inbox** — it is the benchmark family's surface of record (its one read `api.benchmarkVerdicts` already lands there; the verdict forms the opt-in FEEDS render there; the sweep's gap sentence contrasts the two on that surface; the flip's `decision.recorded` re-read rides `inboxEventTypes` with zero `live.ts` change), as a standing consent section rendered regardless of pending pairs. **Alternative: Settings beside PushDevices** (the personal-device-management precedent) — defensible, but the opt-in is not a ⚙ key (S15.9's mandate does not reach it) and Settings would need a second consumer of the benchmark read. Dispose (a) Inbox / (b) Settings.
- **OQ4 — memory test bodies: fixture-world extension vs hand-scripted.** The fixture world already wires the REAL store+gate and drives a real conflict pair (alice, `release-notes-style`, surrogate-id concession recorded). **RECOMMENDED (the UI-2 OQ4-hybrid precedent, executed scoped):** drive real memory bodies through the landed producers — a committed operator-view browse (`memory.json`), a member view (`memory-member.json`), an entry detail with its conflict — reusing the existing driven pair; the shared `cursor` moves in lockstep and the move is stated; refusal/outcome arms (403/404/409/limits/tombstone) stay hand-scripted per test with Go file:line cited (producer-unreachable error contracts). **Alternative: all-hand-scripted** (zero fixture churn, weaker tie — the inert-probe class this batch exists to avoid). Dispose (a) hybrid / (b) all-hand-scripted.
- **OQ5 — the influence list and the "offer to queue re-verification" (S09.5).** The landed response serves `influence[]` as `{run_id, event_seq}` only — no task/deliverable ref, no landed re-verify verb, and the route table serves no run URL. The served detail sentence names the offer as "the workspace UI's act over this data". **RECOMMENDED READING: render the influence list verbatim (mono ids) + the served sentence — the offer at v0 IS the visible influence trace; no invented verb, no fabricated links, no History query (catalog at ceiling).** A one-click re-verification queue would need an unlanded verb → out of scope, carried to the record. Confirm this reading (it is also the §40-C line: "the re-verification OFFER named as the workspace UI's act … rather than faked here").
- **The §5 honest-claims determination** (BENCH-REG §15 binds the future readout surface, not this packet's opt-in) — stated with its § text above; confirm at disposition.

**Spec conflicts found at grounding: none.** S09/S15.2 bind storage, scoping and gate behavior and are silent on these surfaces' visuals (the D8 grounded finding); the approved proposal governs appearance; every behavior this packet renders is a landed, spec-cited contract. Tensions found are completeness gaps or open mounts, carried as OQ1–OQ5 — not papered over. The §40-C `Gate.authorize` project-narrowing REPORT stays a recorded, D7-ratified, contained gap — not this packet's to fix.

## 9. SCOPE VERDICT

**One opus sitting is plausible but this is the batch's widest packet** (a new surface + seven controls + a route addition + two OQ-conditioned side changes). Pre-authorized A/B split at the natural seam if the sitting overruns (a stop at the seam is a success mode):

- **A — the memory family**: R1 (routes) + R2's memory members + R3's six memory exception removals (pins 10→4 / 9→4; family walk + orphans move here) + R8–R19 + their slice of R20–R28. Lands as: the memory surface live end-to-end, sweep honest, battery green, bundle measured.
- **B — the opt-in**: R2's opt-in member + R3's last removal (pins 4→3 / 4→3) + R4–R7 + OQ1's sanctioned read if granted + the rest. Lands as: the standing consent live on the OQ3 host, battery green, bundle re-measured.

Order A → B (A carries the new-route machinery B never touches). **OQ1–OQ4 need disposition before ANY launch; OQ5 before A.**
