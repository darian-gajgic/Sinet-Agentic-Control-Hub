# P3-B6-2 brief — S15.6 decision-plane API: approvals family + action verbs + benchmark driver/verdict backend

Grounding-stage handoff (pipeline stage 1, `.claude/skills/p3-implementation/SKILL.md` §Work packets). The executor and the evaluation agent work from **THIS file plus the cited sections** — never from chat history. Canonical text is `Spec/drafts/` (frozen v1): **S15.6 + S15.2** owning (`Spec/drafts/S15-frontend-api.md`; S15.5 for the drag hint); **S02.3, S02.7–S02.8** (`S02-durable-state-recovery.md`); **S06.9** (`S06-intake-pipeline.md`); **S13.9** (`S13-deliverables-review-git-backup.md`); **S14.4–S14.7** (`S14-observability-evals-watch.md`); **S10.4** (`S10-metering-scheduling.md`); **S01.9** (`S01-process-architecture-shell.md`); **`Spec/benchmark-preregistration-v1.md` (BENCH-REG) §2/§3/§4/§12/§14 — a SIGNED binding sibling whose registered numbers are read-only data (§17)**. `P3/CONVENTIONS.md` binds: **§5, §7, §8, §9, §11, §14 (reading 9), §16, §24, §29, §30, §31, §32, §34, §35, §38** (constraints extracted in §6 below). **The spec wins over this brief, over existing code, and over memory; D1–D10 are fixed; a conflict found while executing is REPORTED, never papered over.** §7 is the evaluation rubric.

**This is packet 2/9 of B6** (queue cut 2026-07-28, `P3/STATE.md` §B6). It builds the **mutation/decision plane**: the `/api/approvals` family with payload-hash-pinned retry-safe answer verbs, task/run cancel + follow-up spawn, the board-drag scheduler hint, budget edits + pause-my-automation, the watchdog/drift/conformance HTTP verbs over LANDED internals, and the benchmark dispatch→render driver + verdict backend. It is **NOT** the content families (settings/deliverables/preview/memory — B6-3; **deliverable accept + preview session verbs belong to B6-3**, not here), **NOT** the SPA or any rendering (B6-4+; inbox/verdict-form UI is B6-6), **NOT** the assistant (B6-7), **NOT** a rebuild of anything landed — `Suppress`, the drift/conformance/benchmark projections, `DispatchDirectArm`/`RenderBlind`, the B2-5 answer path, and `VerifyPIN` are consumed/extended, never re-implemented.

**Model routing:** executor + finalizer opus (standing pipeline directive); grounding/evaluation inherit — B6 queue calibration: S15 scope is not S10/S11 security-dense; lossless opus fallback on any classifier trip.

---

## BINDING carried facts (verified at grounding, 2026-07-28 — do not relitigate)

- **The mutation surface today** (`internal/api/api.go`, read in full): `POST /api/intake/requests`, `POST /api/tasks/{task}/advance`, `POST /api/asks/{ask}/answer` (B2-4 walking skeleton, "PROVISIONAL pending the S15.2 endpoint families"); `POST /api/auth/verify-pin` + the auth family. `Config` HOLDS unrouted: `Accept *accept.Accepter` (B6-3's), `FollowUp *intake.FollowUp` (**THIS packet's to route**, api.go:127–128), `Preview` (B6-3's). B6-1 landed the read families (reads.go, meters.go, historyapi.go) with `readScope`/`authorizeOwner` (404-before-403), `writeReadRedacted`, `redactionEdgeFiles` = enumerated `{sse.go, reads.go}`, structural bounds `readPageDefault` 200 / `readPageMax` 1000.
- **The answer path** (`internal/api/intake_handlers.go` + `internal/stage/surface.go`, read in full): `POST /api/asks/{ask}/answer` takes `{pin, answer}`; a present PIN verifies via `auth.Store.VerifyPIN` (purpose-tagged `auth.reprompt` events, §9 verify-at-act — NO stored elevation state; the dev identity can never step up); `pinVerified` rides to `Surface.Answer`, which routes by ask-id prefix — onboarding → `AnswerOnboarding`; `ask-verify-`/`canary-` → `AnswerVerifyAsk` (S07.7 three verbs `accept_best_effort`/`revise_with_guidance`/`cancel`, §16); else intake → `askCardMeta` reads `asks.snapshot` (kind + tier) and a High-tier approval/delta card without `pinVerified` returns `ErrPINRequired`. Intake card actions (cards.go:134–144): `approve`/`replan`/`reinterview`/`cancel`(+`compose` on no-fit). The stored `intake.Card` carries `Tier`, the S06.9 Layer-1/Layer-2 bodies **including the 13.5 help block**, `Actions`, `StaleFlag`/`StaleReasons`, `Routing`.
- **Asks + effects substrate**: `asks` (0001:121–133) = ask_id, run_id (NOT NULL), user_id, **snapshot (the full card)**, status, observed_ts, answered_ts, answer, engine_expiry_ts — **NO payload-hash column**. Hash mechanics live in `internal/gates`: ONE canonicalization function (UseNumber → sorted-key compact, gates.go:31 region) and sha256 `payload_hash` pinned on effects at Approve; re-verified at BeginExecute; expiry/drift commits back to `proposed` (`ErrApprovalExpired`/`ErrPayloadDrift`). Effects states CHECK (0001, immutable): `proposed/approved/executing/succeeded/failed/unknown` — **no `denied` state exists** (OQ2). Journal verbs: `Propose(Tx)/Approve(effectID, approver)/BeginExecute/Succeed/Fail/MarkUnknown`. `InboxApproval` projects `state='proposed'` effects.
- **Ask-expiry reading**: the G2 Def.2 ratified set's "approval expiry 7 d" is ⚙ `effects.approval_expiry` (S02 draft line 85 + S18 row), and the S18 cross-check "cleanup_period_days must exceed effects.approval_expiry … **so an answerable ask never outlives its session**" (index.go:951–955) ties the same horizon to asks. Expiry-countdown DATA = `observed_ts + ⚙ effects.approval_expiry` (plus `engine_expiry_ts` verbatim where present). Pending-staleness threshold = ⚙ `freshness.max_age` (the ratified fold of `intake.approval_stale_hours`, S18.4 R2); the stored `StaleFlag` covers fingerprint-drift/sibling-accept — the TIME-based flag derives at read (no tickers).
- **Cancel, the ratified mapping** (§14 reading 9, restated §16 — S02.3 has NO cancelled state): running→`completed` with the cancel reason; parked→`finalized` (finalize-with-card, no resume edge, generation unchanged); kanban `cancelled`. B2-5 implemented the parked case (`verify.VerbCancel`). `run.transitions`: **queued's only edge is queued→claimed** (run.go:59) — a queued cancel needs a dispositioned edge (OQ1). `adapters.Session.Cancel` (the S03 boundary→abort→TERM→KILL ladder) EXISTS (adapters.go:539–541, `OutcomeCanceled`), but **no registry maps a live run id to its session handle** — the scheduler tracks in-flight run IDs only (claim.go:165). Resume edge parked→running bumps generation (§8); a watchdog park does NOT bump (§31).
- **Inbox projections ALL LANDED** (`internal/api/projection.go`, B5-2..B5-7): `InboxSnapshot` = Asks + Approvals + WatchdogFlags + ConformanceCards + DriftCards + BenchmarkVerdicts + BenchmarkAlarms, each owner-scoped per S01.9 (watchdog: member sees own; conformance/drift/alarms: operator-only; **benchmark verdicts: REQUESTER-owned** — a member-owned card the operator does not see). Open watchdog flag = latest `watchdog.flagged` per (run, class) not superseded by a `watchdog.suppressed` (full anomaly_class match) or a resume (`run.state_changed` to=running — "resume, I was wrong"). Drift cards: fingerprint {source, lane, change class}, one card per 24h incident window, **30-day horizon + 100 cap — structural constants that exist precisely because "dismiss is B6's" (§34 D10)**. Conformance cards: RED recorded rows only; `internal/conformance` has **no card verb** (store.go/registry.go read in full: EnsureSeeded/List/Due/BumpGatingRows/RecordResult).
- **Watchdog suppress is COMPLETE internally** (`internal/watchdog/suppress.go:40`, read in full): `Suppress(ctx, actor, runID, rule)` — `rule` is the flag's FULL anomaly_class (suffixed for run-less flags); emits `watchdog.suppressed` (owner-attributed when run-scoped; actor in payload); every ⚙ `watchdog.suppress_retune_count`-th suppression proposes the threshold-raise admin card (never an auto-move). "The operator-facing HTTP endpoint + React button are B6/S15" — the file's own words.
- **Benchmark machinery** (`internal/benchmark/pair.go` read in full; §35): pair states `sampled→dispatched→rendered→recorded|declined`. `DispatchDirectArm(pairID)`: wants `sampled`; reads the §2 frozen statement via the `Briefs` seam (S06 artifact of record, hash-verified — shell/benchmark_seams.go); creates run `DirectRunID(pairID)` = `bp-<seq>.direct` under the REQUESTER (**the runs PK makes re-dispatch structurally single-shot**), captures `statement_source` on the birth event, `Enqueue(ClassBackground)` — zero special admission; pair→`dispatched`. `RenderBlind(pairID)`: wants `dispatched`; needs `Platform` + `Direct` text seams; ONE uniform template, length never truncated; position randomized + STORED (arm-identity data — `PendingPair`/`InboxBenchmarkVerdict` structurally carry no arm/position/run/model); pair→`rendered`. `RecordVerdict(pairID, Answer)`: **the guess is STRUCTURAL** (`NewAnswer` requires it; zero `Answer` refused); §14 record append + row→`recorded` + epoch assignment + §12 alarm evaluation in ONE WriteTx; `Reveal` reads the COMMITTED record only. `Decline` records the §14 record with the decline flag from any pre-record state. `DisposeAlarm` (operator-only) and `SetOptIn` (self-only, refuses even the operator) exist as package verbs. **`benchmarkDirectTextSeam()` is HONESTLY ABSENT** ("single-shot execution and output capture land with the dispatcher"); `Skeleton.Dispatch` routes by run-id suffix (intake/execute/verify/compose/onboard) — **a `.direct` run today errors "no skeleton role"** (skeleton.go:239–246). The shell `benchmarkLoop` (1-min tick, durable cursor) only samples; §35 D4: the dispatch→render DRIVER is named follow-on scope — this packet's.
- **Budgets do NOT exist as state**: `metering.Budget` {PeriodTokens (weighted-consumption units), PeriodStart, Declared} is a caller-supplied value object; `UndeclaredBudget()` is the wired posture; "the 13.4 surface + persistence is a later phase" (pressure.go:26–30) — **B6-1 OQ2 disposition: persistence arrives WITH this packet's edit verb**. It CANNOT be a ⚙ key (S18 frozen at 118/33; per-user `PerUser` keys exist but a (person, lane, period) map was never ratified) — it is DATA rows, the `lanes`-table pattern (0001:177). **The 0016 view `cost_budget_remainder` (0016:163–172) hardcodes `budget_declared=0`, `'UNAVAILABLE'`, and a reason text saying "no operator budget is persisted at v0" — FALSE the moment a budget persists** (OQ3). Pause-my-automation (S10.4): admission stop + park-at-next-checkpoint + **MUST preserve everything queued and parked (P-T08-4 mandatory test)**; no pause flag, no per-checkpoint budget gate exists anywhere yet (grep-verified) (OQ5).
- **Scheduler**: `queue` (0001:163) = queue_id, run_id, user_id, status, priority_lane (the workload class), enqueued_ts; ordering = in-memory `priorityLess` over structural `classBias`/`agingGap` (S10.7 ladder — interactive never contends via score). S15.5: drag = "reordering one's *own queued* tasks as a scheduler priority hint" — a HINT within (user, class), never a class or cross-user override.
- **Event contract** (`internal/eventlog/contract.go`, declare rows read): inventory **90 minted / 5 declare-only / 95 registered**. `run.parked`/`run.resumed` = "no distinct producer — manifested as run.state_changed rows with cause=park/resume"; `ask.observed`/`ask.answered` = "projected from asks" (NO minted row — the asks table is the record); `tool.called` = not ours. **`decision.recorded` (family 5, Human decision) is minted with the note "B6 joins as a co-producer when the generic approval-inbox verbs land (S15)"** — payload contract minimum `{actor, card_id + card_type, decision, presented_at → decided_at + latency_s}` (contract.go:451–453). §29: a new minted type = contract entry + `producerTypes()` entry in the same packet; a new FAMILY = S00.9 amendment (STOP, report). ⚙ `state.event_payload_cap` default 65536 — bulky text is refs-not-blobs, never an event body.
- **Follow-ups LANDED** (`internal/intake/followup.go` + §24): `FollowUp.Spawn(FollowUpInput{Owner, DeliverableID, Revision, ProjectID, Preset (revision/extension/counterpart), PresetDetail, Objective, Title, …})` creates successor task + `task_successor_of` link in ONE action (composite FK validates the (deliverable, revision)) and enters normal intake via the root-wired `Start` seam; presets are framings over the SAME link. Held on `api.Config.FollowUp` — routing only.
- **Counters at open** (assert before changing): ⚙ **118 keys / 33 domains** (`internal/settings/index.go` byte-unchanged); `components.lock` **21**; migrations **0001–0016** (`user_version` 16); event inventory **90/5/95**; CONVENTIONS **§1–§38**.

---

## 1. Numbered requirements

### A — `/api/approvals`: the inbox list (S15.6; S15.2 approvals row)

- **R1. `GET /api/approvals` — the owner-scoped risk-ranked inbox list** [S15.6; S15.2]. One endpoint serving ALL seven landed card kinds by REUSING the `projector.inbox` derivations (extended, never rebuilt): open asks (now with the FULL stored `snapshot` card — the S06.9 Layer-1/Layer-2 content incl. **risk tier and the 13.5 help fields already inside it**), proposed effects, watchdog flags, conformance cards, drift cards, benchmark verdict cards, benchmark alarms. Per-kind owner scope stays EXACTLY as landed (member: own asks/flags/verdicts; operator: all + platform-scope kinds). Ranked by risk tier (High > Medium > Low; non-tiered kinds by their severity/alert class), bounded (structural caps with named reasons; the `readPageDefault` precedent).
- **R2. Every answerable item carries its retry-safety pin + expiry data** [S15.2 contract rules; G2 D2.1]: `payload_hash` = sha256 over the gates-canonicalized card content (asks: the stored `snapshot`; effects: the journal row's own pinned `payload_hash` served verbatim — ONE canonicalization function, the gates.go precedent, never a second implementation), plus `expiry_at` (= observed_ts + ⚙ `effects.approval_expiry`; `engine_expiry_ts` verbatim where present) and the TIME-based staleness flag derived at read (pending age > ⚙ `freshness.max_age`, OR'd with the stored `StaleFlag`; "assumptions may be stale" + one-click re-plan is the card's existing `replan` action, S06.9 — the flag never blocks approving).
- **R3. Payload-derived inbox content redacts at the serving edge** [§30; §38 R20]: watchdog/drift/benchmark-alarm card strings are lifted from `run_events` payloads — the new handler file joins the ENUMERATED `redactionEdgeFiles` set and serves through `writeReadRedacted`. Ask snapshots and effect payloads are S06/S02 product/decision content (the §38 ruling-(b) CONTENT-vs-PROJECTION line): served unwrapped, with any payload-DERIVED member redacted per value — mirror the task-detail treatment exactly.

### B — Answer verbs (S15.6 tiers; S15.2 retry-safe rules; S02.7; S06.9; S01.9)

- **R4. `POST /api/approvals/{id}/answer` — the one answer verb, hash-pinned** [S15.2: "pinned to the payload hash they were shown for"]. Body `{payload_hash, pin?, answer}`. Routing preserves the LANDED spine (asks route through `Surface.Answer` — onboard/verify/intake unchanged in their verb semantics; effects route to the journal). Retry-safety, exactly [G2 D2.1]: (a) **stale**: an OPEN item whose current canonical hash ≠ the given hash → rejected (409-class, machine code, fresh card in the response) — nothing fires; (b) **repeat**: an already-RESOLVED item → 200 returning the already-resolved state (the stored answer/effect state) — **a phone retry can never double-fire**, proven per verb by a double-submit test; (c) a resolved item with a DIFFERENT answer → conflict, never a second application. The hash check happens BEFORE any verb dispatch, in one place.
- **R5. Tier semantics, exactly** [S15.6 ratified rules; S01.9; D10]: **Low** (read-only/reversible-in-workspace) batchable via **R6**; **Medium** (own-store writes) individual, owner answers; **High** (outward/irreversible — every S02.7 effect approval is High by definition) **never batched** + **PIN re-prompt via the existing `VerifyPIN`** in the same request (the landed `ErrPINRequired` demand extends from intake approval/delta cards to ALL High-tier answers incl. effect approvals) + **operator co-approval where platform-level** [D10]: a platform-scope effect requires BOTH the owner's and the operator's approval before `gates.Approve` fires — each recorded as its own `decision.recorded` row, both approver states served on the card (derive-from-log; the single `approved_by` column records the final approver). Effect verbs: `approve` → `Journal.Approve`; `deny` → per OQ2's disposition. Ask verbs: the card's OWN action vocabulary verbatim (`approve/replan/reinterview/cancel`, verify's three, onboarding) — S15.2's "approve / deny / answer / re-plan" is the family-level vocabulary those map into; no new ask verb is invented.
- **R6. `POST /api/approvals/answer-batch` — Low-tier batch** [S15.6]: a list of `{id, payload_hash, answer}` items; EVERY item individually validated (scope, tier, hash), individually applied, individually logged (one `decision.recorded` per item), with a per-item outcome list in the response; any non-Low item in the batch is refused per-item (the rest proceed). A batch is a transport convenience, never a semantic merge; bounded by a structural cap with its reason.
- **R7. Every answer lands on the event log** [S15.2; S14.2 family 5]: each answer/deny/dismiss/acknowledge act appends `decision.recorded` (the contract row's pre-authorized B6 co-production) carrying the family minimum `{actor, card_id, card_type, decision, presented_at, decided_at, latency_s}` — presented_at from the card's observed/issue time, so the P-T05-2 latency datum rides every decision. Acts that already mint their own canonical rows (intake `intake.state`/`intake.delta_decision`, verify events, `watchdog.suppressed`, benchmark types, `run.state_changed`) are NOT double-minted — `decision.recorded` covers exactly the acts with no landed audit row. The provenance note on the contract row updates to name the new co-producer (real file:line).

### C — Cancel verbs + follow-up spawn (S02.3 ratified mapping; 4.5; S13.9)

- **R8. `POST /api/tasks/{task}/cancel` + `POST /api/runs/{run}/cancel`** [S15.2 tasks/runs rows: "cancel (4.5)"]. Owner-scoped (member: own; operator: any). The RATIFIED mapping, exactly [§14 reading 9 / §16]: **running** → adapter cancel ladder on the LIVE session (a new in-process registry in `internal/stage` maps run id → the live session's cancel handle, registered at dispatch, deregistered at session end; `Session.Cancel` → `OutcomeCanceled`) then `completed` with the cancel reason on the transition's `run.state_changed` row; **parked** → `finalized` (finalize-with-card; generation unchanged; the B2-5 `cancel` choreography reused where an ask is open); **queued** → per OQ1's dispositioned edge + queue-row settle in the same tx; **claimed** (the transient CAS window) → 409-retry, never a race with dispatch. Kanban `cancelled`. Task cancel = cancel every non-terminal run of the task under the same mapping. **A cancel is a human act — the NO-AUTO-KILL invariant is untouched: no automated path gains any cancel call** (the §31 watchdog importwall and §35 benchmark no-kill wall stay byte-meaningful; nothing new references those idents from an automated context).
- **R9. `POST /api/deliverables/{deliverable}/follow-up` — routing `FollowUp.Spawn`** [S13.9; S15.2 tasks row "follow-up spawn (S1.2)"]. Body: revision + preset (`revision`/`extension`/`counterpart`) + objective/title/detail. Owner = the authenticated requester; owner-scoped against the deliverable's owner (member: own deliverables only; 404-before-403). The successor enters normal intake through the root-wired `Start` seam (already composed); presets are the landed framings — no schema variant. Response = the new task view. (Deliverable ACCEPT stays B6-3's — this route must not touch accept/preview.)

### D — Board-drag priority hint (S15.5; S10.7)

- **R10. `POST /api/tasks/{task}/priority-hint`** [S15.5 "the sanctioned v0 drag interaction is reordering one's own queued tasks as a scheduler priority hint"]. Scope: the acting user's OWN tasks with a QUEUED run — anything else refused (operator included: the hint reorders *one's own* queue). The hint is a rank persisted on the queue row (OQ3 migration) consumed by `priorityLess` as a tie-break WITHIN the same (user, class) — it never outranks `classBias`, never crosses users, never touches another person's slots, and stage columns stay FSM-owned (no state change of any kind). Logged per R7 (`card_type` names the drag). A hint on a non-queued run is a stale-board no-op answered honestly (the run moved on), never an error that implies authority.

### E — Meters verbs: budgets + pause (S10.4; S15.2 meters row; B6-1 OQ2 disposition)

- **R11. Budget persistence + `POST /api/meters/budget`** [S10.4; S15.2 "budget edits (own)"]. A `budgets` table (OQ3 migration): (user_id, lane) → period_tokens (weighted-consumption units — the gauge's own unit; NEVER dollars on a flat lane, D5), period definition, declared_ts/by. Store verbs live in `internal/metering` (the S10 owner); the shell's `MeterReader` adapter reads the declared budget so the LANDED gauge computes real `Pressure`/`Applicable`/`BackgroundBudget` — the gauge itself is UNCHANGED (one denominator rule, D4: never a provider window). Edit authority per OQ4. Every edit audited (R7) with old→new. Undeclared stays the honest absence exactly as B6-1 serves it; a declared budget binds immediately (S10.4).
- **R12. `POST /api/meters/pause` + resume — pause-my-automation** [S10.4; P-T08-4]. A durable per-person flag (OQ3 migration); paused ⇒ the scheduler admits NO background-class work for that person (claim-loop consult, live-read); in-flight parking depth per OQ5. **The mandatory P-T08-4 test: pausing PRESERVES everything queued and parked** — nothing is dequeued, destroyed, or state-changed except the dispositioned parking; resume restores admission and preserved work proceeds. Both flips audited (R7). Interactive work is NEVER gated by this switch (S10.4 headroom rule — it stops *automation*).
- **R13. The 0016 budget view stays TRUE** [S10.1 honesty; OQ3]: once budgets persist, `cost_budget_remainder`'s hardcoded "no operator budget is persisted at v0" text is false — the migration recreates the view per OQ3's disposition so `budget_declared` is real and every remainder figure is READ, never computed in `internal/api` (the §38 R8 negative test extends over the new surface; `internal/history`'s view-registry row updates only as OQ3 disposes).

### F — Watchdog / drift / conformance verbs (S14.4; S14.6; S14.5; over B5-3/B5-6A/B5-4 internals)

- **R14. `POST /api/watchdog/flags/suppress`** [S14.4]: routes the EXISTING `watchdog.Suppress(actor, runID, rule)` — `rule` is the flag's FULL anomaly_class exactly as the projection serves it (suffixed for run-less flags); actor = the authenticated identity. Scope: the flag owner or the operator; platform-scope flags operator-only (the landed visibility rule — you can only suppress what you can see). The retune-proposal mechanics fire unchanged inside the package; the transport adds NOTHING to them.
- **R15. `POST /api/runs/{run}/resume` — "resume — I was wrong"** [S14.4 first-class action; S02.3]: the ratified resume edge parked→running (generation bump, §8) via `run.Store`, permitted for a HUMAN actor on a parked run they own (operator: any); the `run.state_changed` cause names the human resume, which is EXACTLY the supersession the landed open-flag derivation already honors — no projection change. A resume of a run parked on an OPEN ask/gate is refused with the pointer to answering the ask (the ask path is the resume for that class, §16); this verb is the ask-less park's (watchdog/operator/maintenance) release. `run.parked`/`run.resumed` STAY declare-only (manifested as `run.state_changed` — the contract's own words).
- **R16. `POST /api/approvals/{id}/dismiss` — drift-card dismiss** [S14.6; §34 D10 "dismiss is B6's"]: operator-only (drift cards are operator-only). Per OQ7's disposition: per-fingerprint, audited (R7), and the drift-card derivation excludes a dismissed card — a genuinely NEW finding after dismissal opens a new card (dedup horizon/cap constants unchanged). Dismiss closes a CARD, never deletes a finding: `drift.finding` rows stay queryable forever.
- **R17. Conformance-card verbs** [S14.5; per OQ6's disposition]: operator-only, over the derived RED cards; whatever verb set OQ6 ratifies is audited (R7) and **NO verb can make a red row green** — `last_result` moves ONLY through `RecordResult` (a real suite run); the derivation change is exactly the OQ6 semantics, nothing more. `internal/conformance`'s registry semantics (immutable structure, no-delete, mirror-not-re-emit) are untouched.

### G — Benchmark: the dispatch→render driver + verdict backend (S14.7; BENCH-REG §2/§3/§4/§12/§14)

- **R18. The driver** [§35 D4 named scope; BENCH-REG §3 order]: production wiring that moves pairs `sampled → dispatched → rendered` with NO protocol authority of its own. The shell loop (extending the landed 1-min benchmark tick — dueness from STORED pair state, the tick is promptness only) calls `DispatchDirectArm` on `sampled` pairs and `RenderBlind` on `dispatched` pairs whose direct run reached a terminal state. A needed pairs-by-state listing is a small `benchmark.Store` read verb (the package's import wall, no-kill/set-nothing wall, and `registered.go` stay byte-meaningful — `registered.go` byte-unchanged). Dispatch/render failures are logged and retried on the next pass by STATE re-read (the verbs' own state checks make re-entry safe; the run-row PK keeps dispatch single-shot) — never a tight loop, never a synthetic advance.
- **R19. Single-shot direct-arm capture — the engine-dispatch wiring** [BENCH-REG §2 frozen; §6]: `Skeleton.Dispatch` learns the `.direct` run leg: ONE engine session on the run's own substrate/lane whose prompt is the §2 frozen statement ALONE — **no worker template, no knowledge injection, no ledger assembly, no recitation, no stage-split successor, no retry, no follow-up turn; take what comes** (mechanism per OQ9; the adapter Driver stays wired so checkpoint-per-paid-call + metering measure the arm — the §14 record needs its consumption). The session's final text persists durably keyed by run id (home per OQ9; refs-not-blobs discipline — never an event body, cap 64 KiB) and the run completes under the ordinary FSM; a truncated/failed arm is an honest outcome the landed `parityNote` records at verdict time — never re-run. `benchmarkDirectTextSeam` becomes the REAL read of that capture (absent capture keeps returning the honest error). **Tests drive the FAKE engine only — $0; a real direct-arm run is a post-B6 bring-up act, opt-in default OFF** (`benchmark_opt_in` DEFAULT 0 stands; no test flips anyone's opt-in except through `SetOptIn`'s self-only rule on fixture users).
- **R20. Verdict backend** [S15.6; BENCH-REG §3.3–§3.4/§14; §35]: `GET` of pending verdict cards (requester-scoped — `PendingVerdicts`; pre-record shapes carry NO arm/position/model, the landed structural property); `POST` verdict `{verdict, guess}` → `NewAnswer` (**the MANDATORY arm-guess is structural — a guess-less submission cannot construct the argument**) → `RecordVerdict` — **the §14 record commits BEFORE any reveal is readable, by the landed one-WriteTx order**; the response may then include `Reveal`'s read of the COMMITTED record (arm identity + position), which is the §3.4 sequence made an API shape; `POST` decline → `Decline` (the §14 decline record; declines are logged, the rate reports); `POST` alarm disposition → `DisposeAlarm` (operator-only, logged as `decision.recorded` — already inside the verb); `SetOptIn` routing per OQ10. **No registered number, label, or threshold is restated anywhere in `internal/api`** — served values come from the package's `RegisteredValues()`/readout shapes only.

### H — Cross-cutting contract rules (every new verb)

- **R21. Owner-scoped server-side, fail-closed** [S15.2; S01.9]: every route behind `requireIdentity`; per-verb scope as specified above; 404-before-403 on other-owner subjects; internal resolution failure is 500, never a downgrade. **Per-verb three-way cross-owner tests** (operator / owner / other-member) in the non-tautological direction (§38: the member's own act must SUCCEED so an all-refusing handler cannot pass).
- **R22. No direct outward-effect verb** [S15.2; D7]: nothing this packet adds performs a non-idempotent outward act directly — outward effects still exit ONLY through the S02.7 journal behind approval (route-table review is a rubric item). The cancel ladder, scheduler hint, budget/pause, suppress/resume/dismiss are control-plane-internal state acts; the benchmark driver's engine dispatch is model-egress via the ordinary adapter path, not an outward effect.
- **R23. Unversioned additive evolution; no new external observable** [S15.2; S01.8]: no `/v1`; existing routes keep their paths; S01.8's register untouched.
- **R24. NO new ⚙ key; structural constants carry named reasons** [§29–§38 precedent; standing settings-tab directive]: `internal/settings/index.go` byte-unchanged (118/33). New bounds (inbox caps, batch cap, driver-pass batch) are named structural constants with reasons.
- **R25. CONVENTIONS §39 appended** (readings/decisions; earlier sections never rewritten); commit subjects per §5 (`P3-B6-2: … (S15.6, S02.7, S14.4–S14.7, BENCH-REG)` + trailer); `Spec/`, `Docs/`, `Research/`, `P3/STATE.md` untouched; nothing pushed; `*-api-key.txt` never read.

## 2. Seams — what belongs to LATER packets (leave named, never build)

| Later packet | Owns | B6-2 leaves |
|---|---|---|
| **B6-3** | `/api/settings` (+ price-table edits), `/api/deliverables` content family incl. **accept** (High-tier re-prompt → S13.6 flow) + **preview sessions**, `/api/memory` | `Config.Accept`/`Config.Preview` stay held-not-routed; R9 spawns follow-ups WITHOUT touching accept |
| **B6-4+** | the SPA, ALL rendering | data + verbs only; no HTML |
| **B6-6** | the inbox UI + blind-pair verdict FORM + settings UI | R1/R20 serve the data + verbs it renders |
| **B6-7** | the conversational assistant (+ the carried N13 open-SQL wire-semantics question) | — |
| **B6-9** | push/notifier: the re-nag SLA delivery (⚙ `verification.card_remind_hours`/`card_push_hours` stay consumed at card issue, B2-3) | R2 serves expiry/staleness DATA only |
| **Post-B6 bring-up** | real direct-arm accrual (BENCH-REG §4.1 schedule, operator opt-in), the S10.8 per-paid-call budget/pause gate beyond OQ5's scope | honest absences named in code |
| **Not this packet** | minting `ask.observed/answered`, `run.parked/resumed`, `tool.called` (stay declare-only/projections); any change to `internal/history`, `internal/redact`, the SSE frame contract, BENCH-REG numbers | — |

## 3. ⚙ settings consumed (by exact dotted key; none invented)

- **Newly consumed in `internal/api`**: `effects.approval_expiry` (R2 expiry data), `freshness.max_age` (R2 staleness derive) — both via the narrow `api.Settings` seam (extend it additively if a non-Duration read is needed).
- **Consumed through landed packages (unchanged)**: `watchdog.suppress_retune_count` (inside `Suppress`), `pressure.cache_read_weight` + `budget.background_window_fraction` (inside the gauge), `benchmark.sampling_rate` (inside the sampler), `obs.sse_keepalive` (SSE).
- **Referenced-not-consumed**: `verification.card_remind_hours`/`card_push_hours` (B6-9 notifier), `state.event_payload_cap` (eventlog's own gate — R19 respects it by NOT using events for capture).

## 4. Files expected to change

- `internal/api/` — new decision-plane handler file(s) (approvals list/answer/batch/dismiss, cancel, follow-up, hint, meters verbs, watchdog/benchmark verbs); `api.go` (routes + narrow Config seams in the existing pattern); `projection.go` (inbox enrichment + dismissal/ack exclusions); `redactionEdgeFiles` widened by enumeration; per-verb test batteries.
- `internal/stage/` — cancel choreography (live-session cancel registry + the queued/running paths beside the B2-5 parked path) + the `.direct` dispatch leg + capture seam; `surface.go` additions.
- `internal/metering/` — the budgets store (+ gauge wiring stays in shell adapters).
- `internal/scheduler/` — pause consult in admission; hint tie-break in `priorityLess`; queued-cancel queue-row settle.
- `internal/benchmark/` — pairs-by-state read verb (+ capture read per OQ9); `registered.go` BYTE-UNCHANGED.
- `internal/run/` — the OQ1 edge (if dispositioned) with its citation.
- `internal/gates/` — the OQ2 deny disposition only.
- `internal/shell/` — driver loop; budget/pause/capture seam wiring; `benchmarkDirectTextSeam` made real.
- `internal/eventlog/contract.go` — `decision.recorded` provenance/co-producer note (+ any OQ8-dispositioned addition with §29 mechanics).
- `internal/storage/migrations/0017…` (+ `0018…` if the split lands) — per OQ3, each table/column/view-recreate individually justified; **0001–0016 byte-untouched**.
- `P3/CONVENTIONS.md` — §39 appended.
- Expected NOT to change: `internal/history/*`, `internal/redact/*`, `internal/watchdog/suppress.go` internals, `internal/conformance` registry semantics, `internal/watchlist/*`, `internal/settings/index.go`, `components.lock`, `go.mod`, `Spec/*`, BENCH-REG.

## 5. Adopted components

**Expected: none touched, none added.** Stdlib + landed packages only. A new dependency is an OPEN QUESTION for the coordinator (S16.4 rail + operator ratification), never an executor decision.

## 6. CONVENTIONS constraints binding this packet (by section)

- **§5** — staged commits, subject format, no push, never edit Spec/STATE/Docs/Research, never read `*-api-key.txt`.
- **§7/§9** — unversioned API; route classes (everything new session-required, fail-closed); step-up = verify-at-act via `VerifyPIN` (purpose-tagged), NO stored elevation; dev identity never steps up; auth constants stay constants.
- **§8** — FSM transitions through `run.Store` in ONE WriteTx with their event append; resume bumps generation; effect-journal canonicalization/hash/expiry semantics untouched.
- **§11** — metering is a READ MODEL; budgets are DATA with the gauge's one-denominator rule (D4); no dollar-ranked anything (D5); no money computed in api (§38 R8 extends).
- **§14 (reading 9) / §16** — the ratified cancel mapping; ask close + transition in ONE WriteTx; D10 the ask owner answers; `ErrUnsupportedAnswer` stays loud for verbless categories.
- **§24** — follow-up spawn semantics (presets over the one link; claims machinery untouched).
- **§29** — declare-once mechanics; new type = same-packet contract+producer entries; new family = amendment (STOP); count pins updated in lockstep.
- **§30/§38** — owner-scope one shape (`readScope`/`authorizeOwner`); store-raw/serve-redacted at the ENUMERATED edge; content-vs-projection redaction line; no percent/fraction/ETA field in any new shape; cursors on live-view-feeding responses.
- **§31** — NO AUTO-KILL: watchdog verbs pause/flag/annotate; suppress-with-logging→retune card; the importwall stays intact (no automated caller of any cancel).
- **§32** — conformance red→card; mirror-not-re-emit; a red clears only by a real green result.
- **§34** — drift fingerprint dedup + 30-day horizon; severity derived never judged; cards derive bounded.
- **§35** — BENCH-REG discipline: registered numbers read-only (`registered_test.go` is the anti-drift instrument); guess structural; record-before-reveal; single-shot; no-kill/set-nothing + import walls; consent not delegable; no bare rate.
- Cross-phase (35 D6-ratified readings): derive-from-log/no side stores; NO tickers — dueness from stored state; proposals never auto-apply; refusal never repair; every refusal audited; model output is untrusted input (no model-authored string names a record or a verb subject).
- **Tests hermetic**: never the live Caddy admin API/config or production units; $0 — tier-F fakes only; no paid call, no live seat, no real push.

## 7. Acceptance checklist (the evaluation rubric — each item checkable by reading code or running a test)

1. `GET /api/approvals`: all seven card kinds served with landed per-kind scoping intact (three-way cross-owner test incl. the member-owned benchmark verdict and operator-only drift/conformance/alarm kinds); risk-ranked; bounded; ask cards carry tier + the 13.5 help content from the stored snapshot.
2. Every answerable item carries `payload_hash` + expiry data; the time-based staleness flag flips on an aged fixture WITHOUT any sweep/ticker (derive-at-read proven).
3. Retry-safety per verb: stale-hash rejected with nothing fired (side-effect probe); double-submit returns the resolved state with NO second application (double-fire probe on: an intake approve, an effect approve, a cancel, a verdict); conflicting answer on resolved → conflict.
4. Tier semantics: Low batch answers N items individually listed+logged (N `decision.recorded` rows) and refuses an embedded Medium/High item per-item; Medium individual; High without PIN → `pin_required`, with wrong PIN → rejected (+ `auth.reprompt_failed` audit), with PIN → applied; a platform-level effect requires BOTH approvals before `Journal.Approve` fires, both states visible (co-approval test).
5. Effect deny per OQ2's disposition: audited, terminal-or-dispositioned, never executed, and the inbox no longer lists it.
6. Cancel mapping: running → live-session cancel ladder invoked (fake adapter asserts `Cancel` called) → `completed` with reason; parked → `finalized` finalize-with-card; queued → the OQ1 disposition + queue row settled; kanban `cancelled`; cross-owner refusal; **the no-auto-kill negative: no non-HTTP/automated call path reaches the new cancel machinery (source assertion), and the §31/§35 walls still pass**.
7. Follow-up spawn: creates task + `task_successor_of` link + enters intake (Start seam driven); preset framings verbatim; cross-owner test; lineage visible via the B6-1 read (both directions).
8. Priority hint: reorders the owner's own queued candidates within (user, class) — a claim-order test proves the tie-break moves a row; NEVER crosses class (interactive still wins) or user (two-owner probe); non-queued run → honest no-op; logged.
9. Budgets: edit persists; the gauge then reports real `Pressure`/`Applicable` (a pre/post-declaration test); undeclared users keep the honest absence; the recreated `cost_budget_remainder` posture is TRUE in both cases; no money computed in api (§38 negative extended); edit authority per OQ4 tested both ways.
10. Pause: background admission stops for the paused person (claim-loop test); interactive unaffected; **P-T08-4: everything queued+parked survives pause+resume byte-intact**; both flips audited; parking depth per OQ5 tested.
11. Suppress verb: routes the landed `Suppress` with the FULL anomaly_class (a suffixed run-less flag clears — the §34 D5 regression); scope enforced; the retune proposal still fires at the ⚙ count through the HTTP path.
12. Resume verb: parked(watchdog)→running bumps generation, supersedes the open flag in the projection (the landed derivation, no projection edit); an open-ask park is refused with the ask pointer; cross-owner test.
13. Drift dismiss: operator-only; dismissed card leaves the open list; a NEW finding post-dismissal opens a new card; `drift.finding` rows untouched in `run_events`; audited.
14. Conformance verbs: per OQ6; **no verb path writes `last_result`** (source assertion + probe); operator-only.
15. Driver: a sampled pair with a terminal direct run reaches `rendered` across driver passes (fake engine, $0); dispatch is single-shot under re-entry (duplicate-dispatch probe hits the PK/state guard); a failed render retries next pass without state corruption.
16. Direct-arm leg: the `.direct` run dispatches ONE fake-engine session whose prompt is the frozen statement alone (prompt-capture assert: no worker/knowledge/ledger/marker content); Driver checkpoints/usage recorded; capture persisted + `DirectText` returns it; no split/retry/follow-up code path reachable (probe); a fixture ceiling-truncated arm yields the parity note at record time.
17. Verdict backend: pending list carries no arm/position/model (structural assert stands); a guess-less POST cannot express a verdict (400, nothing recorded); a verdict POST writes the §14 record and ONLY THEN a reveal is readable — the order probed by asserting reveal-before-record fails and the record row + `benchmark.pair_recorded` event exist before the reveal read; decline logged; alarm disposition operator-only; **`registered.go` + BENCH-REG byte-unchanged; no registered number restated in api (source scan)**.
18. Every mutation → event log: an inventory test walks every new route and asserts its audit row (`decision.recorded` or the named canonical type) with the family-minimum fields incl. latency; route-table review: no direct outward-effect verb, no `/v1`.
19. Redaction: the new payload-bearing inbox responses redact (planted-secret test); `redactionEdgeFiles` enumeration widened honestly; all original negative limbs unchanged; no percent/fraction/ETA in any new shape (scan extended).
20. Counters: `internal/settings/index.go` byte-unchanged (118/33); `components.lock` 21; migrations 0001–0016 byte-untouched + 0017(+0018) per OQ3 only; event inventory 90/5/95 unless OQ8 dispositions an addition (then pins updated in lockstep); `go.mod` unchanged; structural constants each carry a written reason; CONVENTIONS §39 appended, §1–§38 untouched.
21. **Full battery green**: `gofmt -l internal/ cmd/` (empty) · `go vet ./...` · `go build ./...` · `go test ./... -count=1` (hermetic — no live Caddy/units/network) · `go run ./tools/lockgate`.
22. $0 spent: tier-F fakes only; no paid model call, no live seat, no real engine dispatch.

## 8. Open questions for coordinator disposition (nothing here is resolved silently)

- **OQ1 — queued-run cancel edge.** `run.transitions` admits only queued→claimed (run.go:59); feature 4.5 says "any run can be cancelled cleanly at any time". RECOMMENDED: extend `run.transitions` with **queued→`finalized`** (per-edge citation: 4.5 + the §14-reading-9 mapping extended; finalize-with-card = the never-ran story is on the card), queue-row settled in the same tx. Alternative: refuse cancel while queued/claimed (rejected: it contradicts 4.5's letter).
- **OQ2 — effect `deny`.** The effects state CHECK (immutable 0001) has no `denied`. RECOMMENDED: deny = the journal's finish path to **`failed`** with an explicit machine-readable denial result `{denied: true, actor, reason}` + the R7 decision row — terminal, never executed, honest in the record. Alternative (leave `proposed` + hide from inbox) rejected: an unbounded zombie contradicting the journal's lifecycle.
- **OQ3 — migration 0017 (the explicit justification this packet needs).** Contents, each with its reason: (a) **`budgets` table** — per-(person, lane) durable operator data; no existing home (lanes carries only concurrency_cap; a ⚙ key is barred by the frozen 118 tally; the metering doc itself defers persistence to "a later phase" that B6-1 OQ2 assigned HERE); (b) **`users.automation_paused` DEFAULT 0** — the 0014 `benchmark_opt_in` precedent; (c) **`queue.hint_rank`** — the R10 hint is claim-loop working state like `status`; (d) **recreate `cost_budget_remainder`** — its hardcoded absence text goes false once (a) exists; recreated over `budgets` with `budget_declared` real, remainder expressed in the budget's own weighted-consumption unit via ADDITIVE columns, USD columns kept NULL with a D5-honest reason (a flat-lane token budget has no dollar remainder), and the matching `internal/history` view-registry row updated (a Sinet-owned registry row, not a guardrail change); (e) **the direct-arm capture column** IF OQ9 picks the in-table home. If the SCOPE VERDICT's split lands, (e) becomes its packet's own migration (0018) — migration-per-packet, each justified.
- **OQ4 — budget-edit authority.** S15.2 writes "budget edits (own)"; S10.4 calls the denominator "the operator-declared automation budget". RECOMMENDED: **own + operator-any** (the one role bit, D10) — satisfies both texts (a member declares their own; the operator can declare for anyone), every edit audited with the actor. Alternative: strictly own-only (the S15.2 letter alone) — then the operator cannot administer a member's budget and S10.4's framing is narrowed; say so if preferred.
- **OQ5 — pause parking depth.** "Parks in-flight runs at their next checkpoint" — no per-paid-call budget gate exists yet (grep-verified). RECOMMENDED: (i) admission stop + P-T08-4 preservation are MANDATORY now; (ii) in-flight runs park at the next **stage-session boundary** (the skeleton's leg loop checks the flag between sessions — a stage boundary IS a D7 checkpoint boundary, §14 reading 8), recorded as the v0 approximation; (iii) the per-paid-call gate is NAMED as the remaining S10.8 seam (post-B6/hardening), in code. Alternative: admission-only now with (ii) deferred — weaker than S10.4's sentence; needs an explicit ruling if chosen.
- **OQ6 — the conformance-card verb set.** S14.5 names no card verb; the queue row demands verbs. RECOMMENDED: exactly ONE — **`acknowledge`** (operator; audited via R7; the acknowledged red stops counting as flag-now attention but STAYS LISTED as red until a real green `RecordResult` clears it — no verb fakes green, no dismiss that hides a standing red). Alternative: no verbs (rejected: contradicts the ratified queue row) or full dismiss (rejected: hides a red obligation).
- **OQ7 — drift-dismiss semantics.** RECOMMENDED: operator-only; subject = the card's **fingerprint + window-start seq** (dismissing THIS incident); audited via R7 (`card_type: drift_card`); derivation excludes dismissed incidents; a finding in a NEW window opens a new card; horizon/cap constants unchanged. Alternative: fingerprint-forever mute — rejected (a standing mute is a watch deletion by another name, S16.2).
- **OQ8 — event-type dispositions for the new verbs.** RECOMMENDED: **no new event type** — `decision.recorded` co-production (the contract row's own pre-authorization) for answer/deny/dismiss/acknowledge/budget/pause/hint; `run.state_changed` for cancel/resume/park; `watchdog.suppressed` via the landed verb; benchmark types via the landed verbs; `ask.observed/answered` and `run.parked/resumed` STAY declare-only projections/manifestations. If the executor finds the family-5 minimum genuinely unfit for a non-card act, that is a REPORT to the coordinator (a possible new type under §29 same-packet mechanics, 95→96 with pins updated) — never a silent stretch or a silent invention.
- **OQ9 — direct-arm leg mechanism + capture home.** RECOMMENDED: the leg drives the ADAPTER path directly (StartRequest + Driver — checkpoints/metering intact) rather than full `Session()` (whose budget-watcher/stage-split machinery would grow follow-up turns the frozen §2 forbids); capture = a `benchmark_pairs` column (the `render_a/render_b` in-table precedent) written through a func seam composed at the shell root (`internal/stage` gains no `internal/benchmark` import; the §34/§35 seam posture), read back by the real `DirectText`. Alternative: artifact-store file + ref. Executor may choose within these walls; the choice lands in §39.
- **OQ10 — `SetOptIn` routing.** The standing benchmark opt-in (§4.2.1) is not a ⚙ key. RECOMMENDED: route it HERE (self-only, refusing even the operator — the landed §35 rule; audited by the verb itself as `decision.recorded`) so bring-up accrual has its switch when the verdict form ships (B6-6). Alternative: defer to B6-3's settings family — but it is not a setting, and the package verb + its rule already exist.

## 9. SCOPE VERDICT

**NOT one opus sitting.** This packet is three coherent verb planes plus a migration plus an engine-dispatch leg — materially larger than B6-1, which was already "upper edge" as pure reads. RECOMMENDED split (clean seams, each independently landable and testable):

- **A — the approvals core** (groups A+B+C: R1–R9 + their slice of H): inbox list, hash-pinned retry-safe answers, tiers/step-up/co-approval, batch, cancel verbs, follow-up spawn. No migration. The S15.6/S02 heart — B6-6's form has everything it needs after A.
- **B — meters + oversight verbs + hint** (groups D+E+F: R10–R17 + slice of H): migration 0017 (budgets/pause/hint/view — OQ3 a–d), budget/pause verbs, suppress/resume/dismiss/acknowledge, drag hint.
- **C — the benchmark driver + verdict backend** (group G: R18–R20 + slice of H): the shell driver, the `.direct` leg + capture (its migration column as 0018 if OQ9 picks in-table), verdict/decline/reveal/alarm/opt-in routes.

Order A → B → C (C touches only its own seam and may run before B if the coordinator prefers; migration numbering follows landing order). If the coordinator wants exactly two sittings: **A alone, then B+C**. Cross-cutting H binds every part; a stop at any seam is a success mode, not a failure.
