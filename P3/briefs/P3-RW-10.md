# P3-RW-10 grounded brief — the request-abort strand fix (the intake drive's crash posture must survive request-context cancellation)

**Packet class:** rework (coordinator-diagnosed live at the checkpoint-3 walkthrough, 2026-08-12; reproduction PARKED in the live world as task `t-7c22f74e0d0d6c17` — event trail re-verified this session from a DB copy, never the live file; **leave the wedged run untouched — it is the executor's test material and the landing's heal subject**).
**Headline:** *a beat that took a run to `running` ALWAYS leaves either a live driver or a classifiable corpse, regardless of the caller's context state — the crash posture (state read + crash write) is detached from the request context, on the answer beat, both continuation legs, the Advance nudge, and the S07.7 verify beats.*
**Scope:** backend Go only — `internal/stage` (+ `internal/watchdog` iff OQ1 is dispositioned YES) + tests + CONVENTIONS §56. No migration, no web, no new dependency, no new ⚙ number.

All file/line references verified against the working tree 2026-08-12 (HEAD `0b0248e`). DB evidence from a scratchpad COPY of `~/.sinet-b6-clickthrough/platform.db{,-wal,-shm}` (queried read-only via `tools/dbq`); log evidence from `~/.sinet-b6-clickthrough/control.log`.

## 1. What exists (verified in code + live evidence this session)

1. **RW-9's posture landed and works — until the failure cause is the caller's own death.** `Surface.mapDriveErr` (surface.go:105–122) consults the run's state FIRST and mints the corpse for any drive error on a run the beat took to `running` (RW-9 R4), with the cut line at the resume commit (R5) and the classifiable `advance_crashed_recovering` 500 (R6). All four intake beats route through it: answer (surface.go:228), answer continuation (:236), advance (:305), advance continuation (:308).
2. **The hole: the posture path itself rides the request context.** `mapDriveErr` reads run state via `u.sk.cfg.Runs.Get(ctx, …)` (surface.go:109) — on a canceled ctx the read fails and the function FALLS THROUGH to `mapIntakeErr` (:110–114): no corpse, raw 500. Even when the read survives, `u.sk.crash(ctx, …)` (:120) → `Skeleton.crash` (skeleton.go:384–402) runs its suppression read (:390) and its `crashed` transition (:396) on the same ctx — both die with it, the failure logged and swallowed (:400).
3. **The live wedge (event trail, DB copy).** Task `t-7c22f74e0d0d6c17`, run `t-7c22f74e0d0d6c17.intake`: both interview rounds answered (resume edges at gen 1 and 2, events 388/392); plan-draft started INSIDE the answer POST (event 395, 15:06:09Z); ~40 s later `stage.finished outcome=error detail="context canceled"` (event 396, 15:06:49Z — the append itself survived only because `appendStageEvent` already rides `context.WithoutCancel`, stageevents.go:165). control.log 17:06:49 CEST has the smoking gun verbatim: `"stage: intake answer failed and its run could not be read" … err="planner session: context canceled" read_err="run: scan: context canceled"`. No corpse. The abort was client-side (page refresh/navigation): the server has no timeout that short — no `WriteTimeout`, `ReadHeaderTimeout` 10 s, `IdleTimeout` 2 m (shell.go:882–884).
4. **Why nothing healed it.** The run sat `running`, driver-less, corpse-less, lease beaten once at hold-take (deadline 15:11:09Z = hold + `recovery.dead_after`; the keeper goroutine died with the ctx — its beats ride WithoutCancel, run/lease.go:131/:156, but a dead goroutine beats nothing). The ladder scans {claimed, running, draining} + crashed (recovery.go:197/:212) but its DEAD conjunction (§54) honors the live lease then the fresh cursor; the watchdog's silence rule (budget seeded from `recovery.dead_after` = 300 s) fired first at 15:12:00Z and PARKED it (events 397/398, `watchdog:watchdog.silence`) — and parked is never ladder-scanned (§54 "Parked stays unscanned"). Both asks answered → nothing answerable; `pipe.Advance` demands `running` (pipeline.go:466–468) → the nudge 409s on a parked run; `POST /api/tasks/{task}/advance` has ZERO SPA callers (web/src grep: only the fixture route inventory names it); `ResumeRun` un-parks + ONE fenced beat and explicitly hands the run to nobody (resume.go:93–101). The run is still there: `parked`, generation 2, no events past 398.
5. **Resume does not heal this shape — the watchdog wins the re-park race.** After a bare resume the run is `running` with one `dead_after` of lease and a fresh event; both the watchdog's silence budget and the ladder's cursor/lease bounds mature ~simultaneously at +300 s, and the watchdog sweeps every 30 s (structural `SweepInterval`, watchdog.go:131) vs the ladder's ⚙ `recovery.sweep_interval` 300 s — the RW-9 grounding measured the same race (~9-in-10 to the watchdog) and live-proved one full resume→re-park loop on `t-59f2a72…`. The silence rule consults NO lease evidence at all (sweep.go:66–104 — state, open asks, newest event ts only): the one §54 observer that still reads only half the evidence.
6. **The heal machinery on the other side of the crash edge is real and live-proven.** Crashed runs get their own scan every pass; fork ≤ one sweep; the fork's dispatch rebinds and re-drives (`AdvanceDispatched` → `rebind`, pipeline.go:347–429; RW-6). This very world carries the proof: `t-b495032cd85eb127.intake` crashed → `t-b495032cd85eb127.intake.g2` completed (gen 3), execute + verify completed behind it. The machine-only crash-fork-heal was live-proven end-to-end at checkpoint 3 (5 m 22 s).
7. **Same-class holes beside the diagnosed one (swept this session).** (a) `Skeleton.crash` is called from every dispatch leg and both verify-answer crash sites (skeleton.go:362/:431/:540/:551/:569/:595/:605/:615/:619/:628/:636/:732/:737/:742; answer.go:184/:193) — every caller is exposed wherever its ctx can die. (b) `verifyTerminal`'s error returns UNCORPSED at both call sites (`answerRevise` answer.go:196 — request ctx; `dispatchVerify` skeleton.go:745): a drain that finished its paid work but whose terminal record fails on a canceled ctx strands the run with no corpse and loses a finished outcome. (c) `answerCancel`/`answerAccept` are safe by construction: ask close + transition are one tx — an abort leaves the ask open and re-answerable, no strand. (d) `context.WithoutCancel` exists (Go 1.26.5, go.mod `go 1.26.5`) and is established in-repo doctrine: stageevents.go:165 (drain D11), api/chatapi.go:348 ("THE RECORD OF THE ENDING OUTLIVES THE REQUEST"), scheduler.go:603/:609, run/lease.go:131/:156, watchdog/deadman.go:227.

## 2. Requirements (numbered, each with its S-ref)

- **R1 — the posture path is context-independent.** `mapDriveErr`'s run-state read (surface.go:109) rides `context.WithoutCancel(ctx)` (values — identity, log attribution — kept; cancellation dropped). A beat that took a run to `running` leaves a live driver or a classifiable corpse WHATEVER the caller's context did. S-refs: S02.3 (every transition is a `runs.state` update + `run_events` append in one tx — the record of the ending must be writable at the moment the ending happens); S02.5 step 2 (crashed is the ladder's input; a corpse-less strand is invisible to it); S06.1 (answering resumes in place — the resume commit stays the cut line, RW-9 R4/R5 unchanged).
- **R2 — `Skeleton.crash` detaches at its own boundary.** `WithoutCancel` applied INSIDE `crash` (skeleton.go:384–402), covering every caller in one move — the dispatch legs, the intake beats via `mapDriveErr`, and the verify-answer crash sites. The cancel-suppression consult (`consumeCancel` at the run's current generation, :390–391) still runs, on the detached ctx, and still suppresses: a leg unwound by a human cancel stays corpse-free (drain D1). S-refs: S02.3 crashed edge; CONVENTIONS §16 dispatch-leg posture; S14.4 / G1 D1.3 (the corpse is recovery classification of a driver-less run, never a kill of live work).
- **R3 — RW-9's cut line and refusal mapping unchanged.** State consulted first; parked/terminal → the existing 4xx via `mapIntakeErr` (gates wait, S06.1); `running` → corpse + 500 `advance_crashed_recovering` (RW-9 R6). Sharpened by this packet: the refusal mapping too must survive a canceled ctx (the state read that produces the 409 now rides the detached ctx). S-refs: S06.1; S01.3 (honest surface statuses at the API seam).
- **R4 — the drive stays inline; abort-mid-drive = crash, healed by the ladder.** Weighed from spec text, decided: NO background/detached drive, no early return from the answer POST. Grounds: S02.5's fork-from-last-checkpoint is THE ratified recovery for a dead driver, and RW-6's rebind makes the fork re-drive (live-proven, §1.6); D7 bounds the re-spend structurally (S02.4), and S02.5's harvest step covers a paid call that completed server-side after the abort (engine-store reconcile before re-spend); S06.1's answering-resumes-in-place is codified as the INLINE drive (CONVENTIONS §16 "re-enters the drain INLINE in the answer request") and §54's LeaseKeeper is built on in-process driving holders. No ratified sentence demands a drive survive its caller; a detached drive would additionally need the SPA "composing" face — a separate frontend-pipeline leg, out of this packet either way. Rejected alternative recorded in §8-OQ-note.
- **R5 — verify parity (S07.7): finished work lands; residue crashes.** (a) `verifyTerminal`'s transitions (skeleton.go:850) ride `WithoutCancel` — the drain's paid outcome is an ending record and outlives the request (D7 "no paid work is lost"; the §37-C/chatapi doctrine). (b) A residual `verifyTerminal` error still takes the crash posture at both call sites (answer.go:196, skeleton.go:745) — the last uncorpsed strand path in the stage layer. The `answerRevise`/`newVerifier` crash sites (:184/:193) are covered by R2. S-refs: S07.7 (no finding dies silently), S02.3, CONVENTIONS §16.
- **R6 — (conditional on OQ1 = YES) the silence rule reads the lease and leaves dead-holder runs to the ladder.** For a `running`/`draining` silence candidate whose lease is dead-or-absent past ⚙ `recovery.wake_grace` (suspend-aware, the §54 conjunction's own grace), the watchdog raises the flag card but does NOT park — the ladder classifies and heals it (crash → fork → rebind ≤ one sweep). A candidate with a LIVE lease keeps today's park+flag exactly (S14.4 WEDGED containment, §54's assignment). The watchdog still never kills and never crashes anything — it stops HIDING dead-holder runs from the one component ratified to classify them. `run.Run` already carries `LeaseHolder`/`LeaseDeadline` (run.go:183–184) — zero new plumbing; the grace read is the existing registry key. S-refs: S14.4 (park semantics + NO auto-kill), S02.5 (the ladder owns DEAD), §54 ("the two liveness observers now read the SAME evidence, so they can no longer disagree fatally" — the silence rule is the remaining half-blind observer).
- **R7 — no new numbers; every number by registry name.** Keys consulted or reasoned about, all pre-existing: `recovery.dead_after`, `recovery.heartbeat`, `recovery.wake_grace`, `recovery.sweep_interval`, `watchdog.silence_budget` (TypeMap, per-`<run_type>`). Any new number this packet is tempted to mint is refused — it would be an S18-registered ⚙ key first (S01.10). The watchdog `SweepInterval` 30 s structural constant is read, not moved.
- **R8 — CONVENTIONS §56.** The packet lands its doctrine as a new §56 in the §54/§55 house shape: the defect one-liner, the detached-posture rule ("the record of the ending outlives the request" as the stage layer's law), the R4 inline-drive decision, and (iff taken) the R6 silence-lease reading.

## 3. Seams to respect

- `internal/intake` stays transport-free and crash-free: the pipeline errors up as designed (CONVENTIONS §14 — it owns no run FSM); the corpse is the STAGE layer's act (§16 layering). No pipeline.go behavior change.
- `internal/recovery` untouched: no cadence, scan-set, classification, or fork-policy change — the ladder already does its job the moment a corpse exists (§54/§55 landed machinery).
- `internal/run` untouched: `LeaseKeeper`/`RenewLeaseTx` semantics stay §54's; `resume.go`'s one-beat, drives-nobody semantics stay S14.4's designed release (RW-9 OQ2 disposition stands — no resume-side re-drive).
- The §31 watchdog importwall holds; iff R6 is taken, the change is inside `internal/watchdog` reading columns it already receives on `run.Run` — no new imports, no ladder calls from the watchdog, park remains the only transition the suite makes (tier2.go:18–19), now simply not made for dead-holder candidates.
- API seam (S01.3): no route changes, no handler changes; the 500/`advance_crashed_recovering` contract is unchanged; the SPA face for any of this stays B6/frontend-pipeline territory.
- Backend-only; adopt-don't-fork untouched (no engine/adapter change anywhere in this packet).

## 4. ⚙ settings

**None new, none changed, none moved.** Consumed-by-name context keys as listed in R7. S18 untouched; `components.lock` untouched.

## 5. Files expected to change

- `internal/stage/surface.go` — R1/R3 (`mapDriveErr` detached state read).
- `internal/stage/skeleton.go` — R2 (`crash` detaches inside), R5 (`verifyTerminal` detach + `dispatchVerify` residual-error posture).
- `internal/stage/answer.go` — R5 (`answerRevise` residual `verifyTerminal`-error posture).
- `internal/watchdog/sweep.go` (± `tier2.go`) — R6, ONLY iff OQ1 = YES.
- Tests: `internal/stage/abort_e2e_test.go` (new, T1–T3 beside the RW-9 `strand_e2e_test.go` battery), a stage internal test for T4, verify-battery extension for T5, `internal/watchdog/sweep_test.go` for T6 (iff R6).
- `P3/CONVENTIONS.md` — §56 (R8).
- NOT changed: `Spec/`, `Docs/`, `internal/intake`, `internal/recovery`, `internal/run`, `internal/stage/resume.go` semantics, `internal/api`, `internal/adapters`, web, migrations, `components.lock`.

## 6. Adopted components

None touched.

## 7. Acceptance-test SPECIFICATIONS (standing red-hold: SPECS ONLY here — the executor materializes them; no failing test is committed by grounding)

- **T1 `TestAnswerAbortMidDriveStillLeavesCorpse`** (e2e, `forkHarness`): `walkToOpenCard`; arm the harness planner to CANCEL the request's own ctx and return `ctx.Err()` (the browser-abort shape — the seam dies because the caller died). Call `Surface.Answer` with that cancelable ctx. Assert: the call errors 500/`advance_crashed_recovering`; the intake run is `crashed` with exactly ONE crash event whose cause is non-empty and contains `context canceled`; the answered ask is CLOSED; then one `h.tick` heals — the fork dispatches, rebinds, and re-parks on a fresh gate whose `asks.run_id` is the fork (the RW-9 T5 tail, byte-for-byte expectations). The distinguishing pin vs RW-9 T5: the ctx is ALREADY CANCELED when the posture runs.
- **T2 `TestAdvanceAbortMidDriveStillLeavesCorpse`** (e2e): the Advance-nudge mirror of T1 (RW-9 T7 shape + the canceled-ctx planner) — same corpse + heal assertions. Second leg: `Surface.Advance` on a PARKED run with an ALREADY-canceled ctx still answers 409/`conflict` (never a raw 500) — pins R3's refusal mapping surviving cancellation.
- **T3 `TestContinuationAbortStillLeavesCorpse`** (e2e): the RW-9 continuation-leg injection point (`TestContinuationLegDeathLeavesACorpseToo`'s shape), with the injected failure = cancel-then-`ctx.Err()`. Assert: corpse on the run the continuation named (`st.RunID`), 500/`advance_crashed_recovering`, ask closed, heal on one tick.
- **T4 `TestCrashHelperSurvivesCanceledContext`** (internal, package `stage`): a run driven to `running`; `s.crash(canceledCtx, runID, cause)` → run `crashed`, cause recorded. Subtest: a consumed cancel-mark at the current generation + `crash(canceledCtx, …)` → SUPPRESSED, no corpse (R2's suppression consult works detached).
- **T5 `TestVerifyAbortParity`** (verify battery): (a) a parked verify run answered `revise_with_guidance` whose drain seam cancels the ctx and errors → run `crashed`, ask closed (not stranded `running`); (b) drain SUCCEEDS but the ctx is canceled before the terminal write → the outcome still LANDS (run `completed` on SHIP / re-parked with its card on a recurrent stop), NO crash, no strand — R5(a)'s ending-record detach.
- **T6 `TestSilenceRuleLeavesDeadHolderToTheLadder`** (watchdog, ONLY iff OQ1 = YES): a `running` run, newest event past its silence budget, lease absent (and a variant: expired past `recovery.wake_grace`) → sweep raises the flag card but does NOT park; the existing live-lease silence tests stay green byte-identical (park+flag). Optional tail: one ladder pass then crashes+forks the same run — the handoff is real.

## 8. Acceptance checklist (the headline decomposed — the evaluation rubric)

1. An answer whose drive dies BECAUSE the request ctx was canceled leaves a `crashed` run with its cause — never a driver-less `running` run and never a fall-through to a corpse-less raw 500 (T1).
2. The same holds for the Advance nudge and both continuation legs (T2, T3).
3. Refusals before the resume commit keep their 4xx — including under an already-canceled ctx — and mint no corpse (T2 leg 2; RW-9 T6 stays green).
4. `Skeleton.crash` lands its corpse under a canceled ctx from EVERY caller, and cancel-suppression still suppresses (T4).
5. Verify parity: an aborted resumed drain crashes; a finished drain's outcome lands despite the abort; `verifyTerminal` residue can no longer strand (T5).
6. The heal stays machine-only and bounded: corpse → ladder fork ≤ one sweep → rebind → re-drive; no watchdog-silence window on this class, no human resume anywhere (T1/T2/T3 tails).
7. Iff OQ1 = YES: the silence rule never parks a dead-holder run; live-lease containment is byte-identical; the watchdog still parks/kills nothing else (T6).
8. No new ⚙, no new dependency, no migration, no API/route change; `internal/intake`, `internal/recovery`, `internal/run`, `resume.go` behaviorally untouched (their test suites run unmodified).
9. RW-9's whole `strand_e2e_test.go` battery stays green unmodified — this packet strictly extends the doctrine.
10. CONVENTIONS §56 records the doctrine; battery green end to end.

## 9. Binding CONVENTIONS constraints

- §3 tests-first (Amendment A): the executor materializes T1–T6 red-first on the packet's own paths; build green in the red window.
- §5 commit/process: subject format, explicit staging, no push without milestone, never edit Spec/Docs/Research.
- §8 run-FSM: `crashed` via the ratified edge only, reason+actor+detail on every transition, one WriteTx per transition, fencing untouched.
- §14 intake: pipeline imports/behavior untouched; stage work requires `running`; gates park; answers resume in place.
- §16 resume-in-place: the crash-for-ladder posture is this section's own rule — RW-10 makes it hold under cancellation; ask-close/resume tx shapes not re-invented.
- §31 watchdog: importwall; park is the suite's only transition; no auto-kill anywhere.
- §54/§55: lease semantics, DEAD conjunction, fork identity/rebind — all read-only foundations here; no re-derivation.

## 10. Open questions — coordinator dispositions each; grounding recommends

- **OQ1 — should the watchdog stop containing dead-holder running runs so the ladder can heal them?** Recommendation: **YES, as R6's abstain-and-flag** (not as a watchdog-minted crash). The silence-park of a driver-less run contains nothing (there is no next paid call to stop admitting), hides the run from the only component ratified to classify it (S02.5), and is this wedge's second half: with R1–R5 the request-abort class never strands, but process-death mid-drive (kill-9 between resume commit and posture) still creates dead-holder running runs, and the 30 s watchdog sweep beats the 300 s ladder sweep to them ~9-in-10 (RW-9 grounding measurement; live-proven resume→re-park loop). Weighed alternatives: the watchdog takes the crash edge itself (rejected — duplicates S02.5's DEAD conjunction in a second component, exactly the two-classifiers-disagreeing shape §54 closed; and it would make the watchdog a corpse-minter, against S14.4's letter); do nothing (rejected — leaves the S14.4-card resume→re-park loop as the only exit for the kill-9 shape, i.e. an unbounded human loop where S02.5 promises a bounded machine heal). Blast radius: gate parks, limit parks, operator/maintenance parks are outside the silence rule's positive allowlist already (sweep.go silenceWatched); the affected set is exactly {running/draining, silent past budget, lease dead-or-absent past grace} — today parked forever, after: healed ≤ one sweep. NO-auto-kill fully preserved: nothing live has a dead lease past grace under §54's renewal duty.
- **OQ2 — the sanctioned heal for the EXISTING wedged run (operational note, executed by the coordinator at landing — never by the executor mid-packet).** Recommendation: **resume + immediate advance, via the public API, after the fix lands.** (1) Resume: the Inbox watchdog card's "resume — I was wrong" for `t-7c22f74e0d0d6c17.intake`, or `POST /api/runs/t-7c22f74e0d0d6c17.intake/resume` (route verified api.go:531) as `op` — parked→running, gen 3, one fenced beat. (2) Within the beat's `recovery.dead_after` window: `POST /api/tasks/t-7c22f74e0d0d6c17/advance` (route verified :508; owner-only) — `advanceLoaded` drives the clearance-100 state into plan-draft and parks on the next legitimate card, both interview rounds preserved. Derive the exact curl (base URL, `op` session cookie) from the running :8483 world at heal time per the hand-steps rule — nothing pre-baked here can be pasted blind. Fallbacks, in order: with OQ1 landed, resume ALONE also heals (lease lapses at +5 m → ladder crashes+forks → rebind re-drives; ~10 m, slower but zero-touch); `POST /api/tasks/t-7c22f74e0d0d6c17/cancel` (:519) + resubmit LOSES the two answered rounds — last resort. NEVER a direct DB write (S02.1 sole-writer; the live-file etiquette that produced this brief's evidence discipline).

**No spec conflict found.** S14.4's "pause-and-flag, always" was checked against R6/OQ1: its tiers govern containment of live runaway work; a dead-holder run is S02.5's DEAD candidate, not S14.4's WEDGED (§54 drew exactly this line) — an interpretive choice surfaced as OQ1, not a contradiction between ratified sentences.
