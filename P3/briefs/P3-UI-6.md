# P3-UI-6 — D8 pass II: the decision + system surfaces — the approval inbox (the full row anatomy), settings (the honest A-2 read-only explanation), login, push/PWA

**Grounded 2026-08-05.** Sixth packet of the UI batch, second of the three D8 passes. Contract: `Spec/core-architecture-v1.md` (drafts in `Spec/drafts/` canonical) — §S15.6 (drafts/S15-frontend-api.md:86–96), §S15.9 (:121–123), §S15.11 (:129–135), plus §S15.2's approvals/settings/push family rows (:29, :31, :36) and §S15.12 (:137–142). Design authority: the operator-APPROVED `P3/design/design-approach-proposal.md` — §3's approval-row anatomy sentence (:40: "what's being approved, mono provenance line, **a plain-language 'what to check first'**, jump-to-deliverable, then the verbs") and §5's binding constraints (:63–65), subordinate to the spec on all behavior. Mandate: the B6 gate record `P3/gates/B6-report.md` §9 **A-2, SETTINGS half only** (:137 — the assistant half is UI-7's) + A-1's teaching duty. Scope source: the `P3/STATE.md` UI-batch queue row for P3-UI-6 (:151). Idiom inheritance: CONVENTIONS §47 (kit/tokens/motion/D5), §48–§50 (controls idioms; §49's opt-in panel; §50's C-1 and D5 map), **§51 (the D8 idioms: SurfaceHead/StallBanner/EmptyState discipline, the consumer-verified CSS shed, the widened motion scan, drain D1's pending-read lesson)**. This brief is self-contained: the executor and the evaluation see no chat history. The CONVENTIONS record this packet writes is **§52**.

**THE FRAME: restyle and make self-explaining WITHOUT changing behavior.** API contracts, events, scoping, routes, verbs all FROZEN — no new route, no new event type, no scoping change, no new read, **zero production `.go` change and zero `api.ts` change expected** (everything this packet renders is already served; an honest render found impossible without a read is a STOP-and-flag under §48 OQ1, never an improvisation). **Tier mechanics untouched:** the inbox's approval verbs, tiers, batching, expiry and PIN machinery are frozen behavior — the anatomy rework is presentation over the served card fields. The tour/one-shot hints, `ToastProvider`'s production mount and the assistant-card rework are **UI-7's**; what rides here per the §51 split: each surface's one-line "what this is" header (`SurfaceHead`) and teaching empty states (kit `EmptyState`, **only over SERVED reads** — §51 drain D1 is binding, and `doubles.ts`'s `pending` route (:128–178) exists for exactly that test shape).

**THE THREE WALLS — state them back before building, police them at evaluation** (byte-verify against the pre-packet commit, HEAD `0255668` at grounding):

1. **Byte-unchanged:** `web/src/App.tsx` (**fully frozen this packet — VERIFIED: no sanctioned change exists for it; the inbox/settings/login arms pass only `stream`/`session`/`id` (:187–188, :213–222) and the C-1 arms live at :104–116 and :165–179**) · `MissionControl.tsx` · `Filters.tsx` · `Board.tsx` · `TaskDetail.tsx` · `Fleet.tsx` · `Workforce.tsx` · `History.tsx` (the five UI-5-restyled surfaces) · `Chat.tsx` · `Deliverable.tsx` · `Memory.tsx` · `ConnectionState.tsx` · `Stub.tsx` · `routes.ts` · `router.tsx` · `live.ts` · `events.ts` · `api.ts` · `controls.tsx` · `kanban.ts` · `chatFacts.ts` · `chatRuntime.ts` · **`push.ts` and `web/public/sw.js`** (machinery, not a surface — carried constraint; if any S15.11 sentence genuinely demands more, that is an OQ, not an improvisation) · `parts.tsx` (**expected byte-unchanged** — SurfaceHead/StallBanner/EmptyState/`Empty` all exist; a genuinely shared new idiom is a recorded deviation, the §51 precedent) · every `web/src/ui/*` production component (**`ui/Timestamp.tsx` included — the UI-4 carried cross-note rides ONLY if another named requirement legitimately opens the file; none here does**) · every production `.go` file · `Spec/` entirely · `web/src/fixtures/api/*.json` (**no fixture regeneration; `cursor` stays 89 — verified in the committed `approvals.json`/`approvals-mine.json` this grounding — with zero drift on a regeneration re-run; the one test body this packet hand-scripts is the dev-posture settings view, citing `internal/api/settings.go:455`, which is test wiring and not a committed fixture**) · `package.json`/`package-lock.json`/`components.lock`/`go.mod`/`go.sum` (**no new dependency; §47's runtime set is CLOSED**) · **`shell.test.tsx`** (the C-1 pins :385/:407/:421/:446–450/:454 AND the landed login-behavior pins :212/:224/:253/:281/:324/:354/:500 pass **unmodified**; see OQ3 for the one stale comment) · `sw.test.ts` (its subject is frozen).
2. **Diff-censused: NONE.** No file needs hunk-level opening this packet. Discovering one mid-build is a STOP-and-flag, not a silent widening.
3. **Behavior spine of the inbox:** every control renders from the card's OWN served vocabulary and nothing else (`ApprovalItem.actions`/`answerable`, api.ts:512–538); `canFire` (Inbox.tsx:1156–1165), `answerEnvelope` (:1177–1185), `fireAction`'s dispatch (:1204–1293), `composeAnswer` (:1298–1319), the hash-pinned answer + 409 stale-payload fresh-card swap (:499–521, api.ts:573, :978–982), the PIN lives only in the request (:1041–1054, cleared :978–982), the batch machinery (:1478–1581) — all byte-preserved in behavior. **`api.resumeRun`'s one call site tree-wide is still `Inbox.tsx` (:1232)** — the UI-5 assertion keeps holding.

Also binding, carried: bundled assets only, no CDN, no runtime fetch · escape-scan allowlist EMPTY · **AGPL: Odysseus is ideas-only, forever — an explicit §52 attestation item** · every ⚙ registry-routed (this packet consumes NONE and creates NONE — R21 states it precisely) · assertion counts never drop silently · bundle measured at landing vs the UI-5 reference · Layer-1 catalog stays **50/50** (nothing here needs a canned entry; needing one = stop-and-flag) · no-ticker, `<time>` two-file pin, phone-first 375px with both-way probes.

---

## 1. Where the packet starts (verified ground, 2026-08-05)

UI batch 5/7. Counters at the UI-5 landing, re-verified this grounding where marked: vitest **642/27 (re-run green this grounding)** · Go **45 pkgs / 14 sanctioned skips** · lockgate **38 entries / 574 npm (re-run this grounding)** · bundle **JS 1129.39 kB / gzip 340.25 · CSS 56.84 / 11.92 · index.html 1.80 · 15 fonts / 417.30** (§51 drain-r1 measurement — the entry battery must reproduce it) · golden `cursor` **89 (verified in the committed bodies)** · ⚙ **118/33** · catalog 50/50 · escape allowlist EMPTY. Assertion baselines, measured this grounding by the landed method (`grep -c 'expect('`): **inbox 199 · settings 95 · push 77 · sw 25 · shell 68 · sweep 88 · responsive 57 · design 53 · scans 29 · ui/kit 61 · ui/timestamp 18**. `index.css` is 1767 lines; this packet's sections: **the approval inbox :812–970 · the settings tab :971–1078 · the S15.11 "This device" panel :1738–1767**; Login has no section of its own — it rides the content block's `.panel` (:596–601) and the global form/label/input rules (:603–631). The committed approvals world serves 18 cards across every kind and all three tiers, most with `run_id`, six answerable, one step-up, one batchable set of zero (batchable:false throughout — the BatchBar's driven tests use scripted bodies).

## 2. Requirements A — the D8 idioms, cross-surface

- **R1 — the one-line "what this is" header, all four surfaces.** Each gains the §51 `SurfaceHead` (parts.tsx:164–176) treatment, facts pinned, overclaims asserted absent: **inbox** = everything waiting on a person, ranked by risk by the control plane, never re-ordered here; answering resumes the paused work in place (S15.6 :96; the landed :70–72 sentence is the model and its facts must survive); **inbox-item** (:113–152) = one decision with its stable deep-link framing (the push `navigate` target, S15.11 :131); **settings** = every setting the platform has, with bounds, badges, help and per-key audit history; generated from the registry, never hand-built (S15.9 :123); **login** = who is signing in on a shared-house device (S01.9 framing; keep it to facts the page shows); **push panel** keeps panel grain (it is a panel inside settings, not a route — `SurfaceHead` is for routes; a panel-grain what-line is fine). **Truth constraints:** the inbox line must NOT say the client ranks, groups, or filters (Inbox.tsx:26–38's four rules); the settings line must NOT promise editing (authority is served, R13); no line claims a tier meaning beyond S15.6's own (:90–94).
- **R2 — teaching empty states, SERVED-gated (the §51 drain D1 lesson, binding).** The three landed `Empty` sites migrate to kit `EmptyState` (`what` + `why`): inbox empty queue (Inbox.tsx:75–76, gated `live.data && items.length === 0` — the gate stays), settings eval results (Settings.tsx:595–596, gated on `answer &&`), push enrolled-devices (PushDevices.tsx:187–188, gated on `live.data &&` at :39–40). The `why` teaches what causes rows to appear (e.g. inbox: what kinds of cards arrive and that answering clears them; devices: enrolling this or another device adds a row). **Each surface gets the D1-shape negative: with the surface's own read `pending` (doubles.ts:128–178), NO teaching state renders** — `Freshness` owns the window — and once the read answers empty, the teaching state is exactly what appears. `Empty` itself STAYS exported: its remaining consumers are Chat (6 sites) and Deliverable (6 sites), both UI-7's (parts.tsx:146–154's own doc).
- **R3 — restyle in the approved language, kit primitives only.** Panels/chips/buttons from `web/src/ui/` (§47 :1535 export contract); tone is a TOKEN NAME through the one chip formula; no raw hex outside `index.css`'s token block; **JetBrains Mono `tabular-nums` on every number, id, timestamp and status token** — which on the inbox is exactly the mono provenance line's material (card id, owner, run id, tier, instants) and on settings the values, bounds, defaults and audit rows. The kit is not modified; a missing variant is a STOP-and-flag.
- **R4 — the CSS shed, consumer-verified rule by rule (§51's method; nothing renamed).** This packet's sections and their verified consumer map, from this grounding's grep: **shed-eligible once the markup moves** — inbox-only rules (`.card-row` + its `[data-tier='high']` arm, `.card-head`, `.card-id`, `.card-body`, `.restatement`, `.assumptions li`, `.delta-items li`, `.questions li` — Chat's is `chat-intake-questions`, a different class — `.help-block` + `dt`/`dd`, `.acts` + `.acts .buttons`/`label`/`input` — Chat's are `chat-session-acts`/`chat-intake-acts`, settingsForm's is `setting-acts` — `.criteria`, `.verdict-form`, `.disposition-form`, `.batch-candidates`, `.batch-outcomes`, `.co-approval`, `.pair` — Deliverable's is `image-pair` — `.pair-side`, `.render`, `.delta-kind`, `.layer2 summary`, `.numbered`); settings-only rules (`.settings-cats`, `.cat-tabs`, `.cat-panel`, `.setting`, `.setting-label`, `.setting-help`, `.setting-key`, `.setting-edit`, `.badges`, `.setting-acts`, `.bounds-editor`, `.per-user-editor`, `.add-price-row`, `.setting-group`, `.audit-rows`, `.per-user-values`, `.deferred`, `.registered`, `.unit-price`); login (`.panel` :596–601 — **exact-class consumers are Login.tsx's three sections only**; the `.panel h1` HALF of the `:579` selector list sheds while `.stub h1` stays — Stub.tsx is frozen). **NOT shed, named consumers elsewhere:** `.cards` (**Board.tsx:263 still carries it** — frozen surface), `.items` (**TaskDetail.tsx** — frozen; also Settings/Push tables), `.surface`/`.block`/`.table-scroll`/`.absent`/`.owner`/`.money`/`.muted`/`.error`/`.notice`/`.warn-flag` (Chat/Deliverable/Memory + `parts.tsx` primitives — **the last consumer goes at UI-7**, §51 :1717's stay-note stands, update its consumer list datedly as inbox/settings legs drop off), the global `form`/`label`/`input,select,button` element rules (:603–631, every surface's raw forms). **The sweep duty-table containers are the phone-first LAYOUT contract and follow the §51 recorded-deviation precedent: KEEP `.cards`, `.card-row`, `.batch-bar`, `.push-device`, `.push-devices` styled in the sheet** (sweep.test.tsx:778–785 pins them per route with the no-fixed-width walk) rather than re-pointing the duty table — the §51 reasoning (landed properties survive as facts about the shipped page) applies unchanged. Record the shed list with counts in §52.
- **R5 — the scans, already in place, must stay green with the new work.** The motion scan is ALREADY tree-wide (§51 :1716; sweep.test.tsx:1025–1046) — every new `transition-`/`animate-` utility on these surfaces is `motion-safe:`-authored; no scan is widened or narrowed this packet. `design.test.ts` gates any stylesheet-declared motion. The `<time>` file pin stays exactly `['./parts.tsx','./ui/Timestamp.tsx']`; no view formats a date itself; both no-ticker scans pass byte-unmodified — the expiry countdown keeps deriving at RENDER (Inbox.tsx:439–457's `describeSpan` over the served instant; no timer enters). Escape floor: card bodies are model-derived text and render as text (Inbox.tsx:525–527) — nothing in the anatomy rework introduces raw HTML.
- **R6 — phone-first at 375px.** Every landed structural property survives: the duty containers (R4), no `@media (max-width:)`, wide content scrolls inside its own box (the blind-pair `.render`, the price/eval tables' `.table-scroll`), no pinned pixel width. New renders (the anatomy row, the A-2 explanation block, the restyled login card) each get their own 375px leg with a both-way detector probe (the shell.test.tsx:446–450 model).
- **R7 — honest assertion updates (the batch's highest-yield hazard).** Markup-coupled assertions churn heavily on these suites. Per-file pre→post `expect(` counts measured by the landed method and recorded in §52 (baselines §1); nothing drops silently; **counts alone are not the bar** — each rewritten assertion must still assert the same FACT about served data, verbs, scoping, frames; a selector updated with the predicate gone vacuous is a finding.

## 3. Requirements B — the inbox: the full approval-row anatomy (the centerpiece)

- **R8 — the anatomy is presentation over SERVED fields, and the wire is the whole inventory.** `ApprovalItem` serves: `id`, `kind`, `owner`, `run_id?`, `tier`, `answerable`(+reason), `batchable`, `step_up_required`, `payload_hash?`, `observed_ts`, `expiry_at?`, `engine_expiry_ts?`, `stale?`+reasons, `actions?`, `approvals?`, `card?` (api.ts:512–538). The five anatomy legs map so: **(1) what's being approved** — the display class (Inbox.tsx:412–430, forward-tolerant, byte-kept as the vocabulary) + the card body's own lead (restatement/summary/question/row fields); **(2) the mono provenance line** — id, kind, owner, tier, `observed_ts` (the "seen" stamp, D5 `live` variant kept, :394–396), run ref — all served, all mono/tabular; **(3) the plain-language "what to check first"** — R9; **(4) jump-to-deliverable** — OQ2: **only the served `run_id` exists as a work ref** (no card serves a deliverable id — api.ts:512–538; the conflict card explicitly carries neither task nor deliverable, api.ts:848) — the landed `run_id → hrefFor('task')` link (:387) IS the jump leg, restyled and labelled for what it opens (the task page, where deliverable revisions and receipts live); rendered only when served, absent invents nothing; **(5) then the verbs** — the landed served-actions row (:1056–1079), state-computed exactly as landed. **No anatomy slot may render a fact that is not on the wire** — the evaluation attacks this first.
- **R9 — "what to check first" is CLASS-grain teaching copy, never card-grain invention (OQ1).** One plain-language line per display class, client-authored under the D8 mandate (the §51 SurfaceHead-copy precedent — facts pinned, overclaims asserted absent): a proposal → read the assumptions and what-I-will-NOT-do before the verbs (the S06.9 card's own centerpiece, :644–656); a question → the question and its listed options are the whole decision; a sign-off → the projection row's own fields say what happened, and acknowledging does not undo it (the conformance STILL-RED render :602 in inbox.test.tsx stays); a judgement → read both responses blind, pick, and guess which was the platform's — the guess is mandatory (BENCH-REG §3.3, `canFire` :1156–1159); an effect → check both approver states and what fires on approve (:819–850). **Truth constraints, asserted in both directions:** the line must NOT contradict tier semantics (Low = read-only or reversible-inside-the-workspace, Medium = own-store writes, High = outward/irreversible + PIN re-prompt — S15.6 :90–94 verbatim grain); must NOT tell a High-tier reader it is batchable; must NOT claim approve fires an outward effect where `approvals.platform_level` says co-approval is still missing; must NOT re-draft the 13.5 help — **where the card carries its served help block it renders in full, byte-true (:769–789), and the class line must not disagree with it.** The 13.5 help stays the card-specific authority; the class line is orientation.
- **R10 — behavior preserved, enumerated (assert each, file:line).** The one `useLive` read on `/api/approvals` with `inboxEventTypes` (:50–55) and the deep-link surface's same read (:113–119); served order never re-ranked (:26–38; the shuffled-body test); every served card renders exactly once, unknown kinds included (`displayClass` default arm + `RowCard`); badge reconciled from the served count on both surfaces (:62–64, :125–127; push.test.tsx:529); `NoFrameNote` (:364–372); answerable=false → served reason, zero acting controls (:988–994); zero actions → the honest nothing-to-press (:995–1001); the stale-payload swap with its visible notice (:499–521); expiry = served instant + render-derived span + `(past)` warn + engine deadline (:439–457) — **the §50-recorded duplicate (primitive label beside the countdown) is landed and stays; dropping either drops a fact and an assertion**; staleness flag with served reasons, never blocking approve (:475–484); the co-approval block with both signer states (:819–850); the blind-pair render side-keyed with lengths "reported, never corrected" (:902–916) and the no-pair decline-only arm (:882–897); the Reveal's five facts with `recorded_ts` on audit `Stamp` (:1109–1137; inbox.test.tsx:1224's negative); PIN type=password, autoComplete off, sent in the same request, cleared on every path (:1041–1054, :978–982); the batch bar's action-first constraint, per-item hash, per-item outcomes (:1478–1581); every fireAction arm incl. the honest not-sent arms (:1204–1293); the D5 live sites exactly as mapped (§50: :395/:444/:452/:864). The **automation-step generic card** (B6 §7's "ugly enough to name" record): the RowCard restyle (:561–577) is where that presentation debt is paid — inbox.test.tsx:939's pinned facts (verb + step reach the screen) survive, honestly re-pointed if the markup moves.
- **R11 — the benchmark opt-in panel survives byte-true (carried constraint 2).** The §49 panel (:222–286) keeps: rendered with zero pairs pending, its own read, no person picker, no `person` key ever sent, both position acts always offered, the absence arms, and **the explanation's BENCH-REG-faithful figure-free copy byte-identical** — inbox.test.tsx:984–1153's pins incl. the no-digit/no-%/no-rate-token probe (:999) pass. Restyle may re-skin the panel chrome only; every sentence of :244–266 and :330–335 is frozen text.
- **R12 — the deep-link surface (:113–152)** keeps the whole-queue link, the served-absent arm (:136–139), and expanded card render; the anatomy applies identically (one component serves both, as landed).

## 4. Requirements C — settings: the A-2 explanation, and the restyle boundary

- **R13 — the honest read-only explanation, pinned to the gate record's own sentences (A-2, B6-report:137).** The record's facts: the dev-posture identity "**reads but never administers**"; the write surface is "**absent-not-disabled**" per the no-403-controls rule; "**the sole explanation is one banner line a person has no reason to connect to the missing editors**"; "**Editing is real behind a real operator session**". The server serves exactly three authority sentences (internal/api/settings.go:452–461): dev :455, member :458, operator :460 — **the client renders the served reason VERBATIM and can distinguish postures only through it**, so the fix is: keep `Authority` (Settings.tsx:102–108) rendering `editable_reason` as the sole textContent of the one `[data-editable]` element (**settings.test.tsx:323–326 pins textContent equality and exactly ONE `[data-editable]` — the teaching copy must be SIBLING markup with its own data hook, or that pin breaks**), and beside it, when `editable === false`, an explanation block in operator words making the connection the record says is missing: (a) this page is read-only for you right now — the platform's own reason is the line above; (b) **that is why no Save, Reset, bounds or price-append control renders anywhere below: the platform never shows a control it would refuse** (absent-not-disabled, taught); (c) everything is still readable — every value, its bounds, its badges, its help, its per-key History, the price table and the recorded results; (d) **editing is real behind a real operator session** — the record's sentence, posture-neutral, true for the dev reader (sign in as the operator) and the member (values are set for them by the operator, which the served member reason :458 and the landed per-person prose :76–80 already say). **No link to /login in this block** — for a signed-in member that door bounces (shell.test.tsx:407), which would be a C-1-class control that cannot do what it says for half its readers; the header's own Sign-in affordance (dev posture) is the landed door. **Overclaim negatives asserted:** the copy never says history or values are unavailable (they are not — settingsForm.tsx:288–301, History outside the write gate), never promises a member their own login unlocks editing, never names dev posture in client-authored text (the served reason carries the posture). Test arms: the member arm over the committed `settings-member.json` (editable:false, real handler's reason); the dev arm over a hand-scripted body carrying settings.go:455's sentence byte-exact with the cite; the operator arm (committed `settings.json`, editable:true) renders NO explanation block and all editors.
- **R14 — behavior preserved, enumerated.** The one read on `settings.changed` (:57–65, :98); the form is the registry emission consumed verbatim — no key list, `validationMode="NoValidation"` with its recorded reason (:190–223); per-key explicit Save, never on keystroke, dirty-list warning (:180–189); reset = the null write (:157–159); bounds by their own verb (:160–162); `for_user` with `coerceLike` (:163–167, :237–245); refusals verbatim (:129–145); History for everyone (settingsForm.tsx:288–301); `PerUserValues` outside the model (:569–590); `RegisteredBlock` + absent arm (Settings.tsx:249–269); the price table: posture line, append-only prose, deferred list, **its OWN served `editable` gate** on AddPriceRow (:297–362), the five-unit every-unit-answered machinery (:420–570); EvalResults on its one event type (:582–644); `PushDevices` mounted beside the form (:87 — stays mounted here; its restyle is R17). All settings.test.tsx pins (:73–:638) survive honest re-pointing.
- **R15 — the restyle boundary is FC-v1 §4's own line.** The renderer set is **Sinet-owned** (settingsForm.tsx — Categorization tabs :142–183, Group :187–204, SinetControl :222–309, Editor :314–394, Badges/Bounds/editors) — restyle freely there, onto kit primitives and utilities. **`@jsonforms/core`/`@jsonforms/react` are adopted engines: no modification, no theme package, no new renderer vocabulary** (FC-v1 §4; S15.4 :65–72). The read-only `<output data-readonly="true">` render (:329–334) stays the value's read-only form.

## 5. Requirements D — login (opens for restyle for the first time)

- **R16 — restyle only; the flow is frozen.** Preserved, each asserted: the users read + device-hint prefill (Login.tsx:25–43); PIN cleared in `finally` on every path (:45–57); the bootstrap window's one-operator create landing on the picker with the honest notice (:67–74, :87–126); **the picker stays a native `<select>` with option values = user ids and the PIN a `type=password` input** — the C-1 pins and login pins select exactly those shapes (shell.test.tsx:385–405, :224–353) and pass **unmodified**; the autoLogin device button (:128, :169–182); the collapsed-failure texts byte-true (:192–199 — "That did not sign you in." / the unreachable sentence); the login page stays PRE-SESSION with no EventSource (sweep.test.tsx:840). The restyle moves `.panel` markup onto the kit; R4's `.panel` shed applies. A "what this is" line per R1 teaches the shared-house picker + PIN contract without promising anything the page cannot do (no self-service account creation outside the bootstrap window).

## 6. Requirements E — push/PWA panel

- **R17 — the rendered panel restyles; the machinery is walled.** `push.ts` and `web/public/sw.js` byte-frozen (wall 1). Preserved in PushDevices.tsx, each asserted: the one read on the two family events (:29–43, :51); mount reads, never asks permission (:73–79); the four availability facts each with its served/authored reason (unsupported+installHint / denied / enrolled / not-enrolled) plus the **unknown-subscription disagreement state's honest copy** (:81–133); enrol only from the click, label input, stop-notifications, failure "Nothing changed: …" (:135–160); `data-push-state` names (:165–170); **the scope and observable sentences render VERBATIM as served** (:118, :184–186 — the S15.11/S01.8 register row 2 surface); the enrolled list is metadata-only — label, owner, origin, device_login, created `Stamp` (audit, stays), truncated `endpoint_hash`, **never an endpoint** (:190–204; push.test.tsx:256); the badge-agrees-with-queue leg (push.test.tsx:529). push.test.tsx panel pins (:323+) survive honest re-pointing; sw.test.ts untouched.

## 7. Cross-cutting requirements

- **R18 — CONVENTIONS §52** per the §48–§51 model: decisions, deviations under scrutiny, the shed/stay lists with counts, the A-2 copy record with the three served sentences cited, measurements (bundle itemized both ways, assertion tables), the §51 :1717 stay-note's consumer-list dated update, AGPL attestation.
- **R19 — full battery in landing order:** `cd web && npm run typecheck && npm run test && npm run build` · `gofmt -l internal/ cmd/ tools/` empty · `go vet ./...` · `go build ./...` · `go test ./... -count=1` over the REAL new dist (45 pkgs / 14 skips) · `go run ./tools/lockgate` (38/574). Frozen-path spot-diffs per wall 1.
- **R20 — bundle, measured + itemized vs the UI-5 reference** (§1). CSS moves BOTH ways (sections :812–1078 and :1738–1767 shed while Tailwind compiles the new utilities) — itemize the two directions per §51's method; JS growth is anatomy/teaching-copy scale — if beyond copy-scale, split by controlled builds (§48 method). No new package; SPA stays un-split; fonts unchanged.
- **R21 — ⚙: NONE consumed, NONE created — stated precisely.** The settings surface renders the registry's SERVED schema/values/bounds/help off `/api/settings`; **the server consumes the registry and the client consumes the wire — that is not the client reading a ⚙ key**, and no client file reads one (the §49/§50 precedent sentence). No new client-side constant is expected at all this packet (no threshold, no band). `internal/settings/index.go` byte-unchanged at 118/33.
- **R22 — adopted components: NONE new.** Lockgate 38/574 unmoved; the anatomy and explanation are owned JSX; JSON Forms stays at its pin, unforked.
- **R23 — seams left, named:** tour + hints, ToastProvider mount, assistant-card rework, Chat/Deliverable/Memory restyle + their `Empty`/shared-CSS sheds, seeded demo world → UI-7 (STATE :152); the WithForms/opt-in double read stays recorded, not restructured (§49 :1610); the interview-card no-verbs state stays the honest landed render (B6 §7).

## 8. Acceptance checklist (the evaluation rubric — verify each by your own run)

1. **Walls:** every wall-1 file byte-identical to the pre-packet commit (spot-diff; App.tsx, api.ts, controls.tsx, push.ts, sw.js, parts.tsx, shell.test.tsx, sw.test.ts explicitly); zero production `.go` diff; `Spec/` untouched; fixtures untouched, `cursor` 89, zero-drift regeneration re-run; lockgate 38/574; escape allowlist EMPTY; catalog 50/50.
2. **C-1 + login pins pass unmodified** (shell.test.tsx :385/:407/:421/:446–450/:454 and the :212–:354/:500 set); the picker is still a native select; the PIN still a password input; login still pre-session.
3. **Tier mechanics untouched:** the wall-3 spine asserted — served-vocabulary controls only, hash-pinned answers, stale-swap, PIN discipline, batch per-item outcomes; `api.resumeRun` one-call-site assertion green.
4. **The anatomy maps onto served fields only** (hazard #2): every slot traces to api.ts:512–538; the jump leg renders only from served `run_id`; no deliverable ref invented; provenance line mono/tabular; unknown kinds still render under their served name.
5. **"What to check first" truth** (hazard #3): class-grain, both-direction pins — facts present AND the named overclaims absent (batchable-High, co-approval elision, tier re-definition, help-block contradiction); the 13.5 help renders byte-true where served.
6. **The A-2 explanation** (hazard #4): served reason verbatim, alone, in the ONE `[data-editable]` element (settings.test.tsx:323–326 green); the explanation is sibling markup, keyed on `editable === false` only, all three posture arms driven (member fixture / hand-scripted dev body citing settings.go:455 / operator negative); no /login link; overclaim negatives pass; every write affordance still ABSENT not disabled.
7. **EmptyState discipline** (hazard #5): the three migrated sites render teaching copy ONLY over served-empty; the `pending`-route negative per surface; `Freshness` owns the window.
8. **The opt-in panel byte-true** (hazard #7): explanation text unchanged, figure-free probe green, both positions fire, no subject ever sent.
9. **CSS shed consumer-clean** (hazard #6): every shed rule's consumer list empty by grep (Board's `.cards`, TaskDetail's `.items`, Stub's `.stub h1`, Chat/Deliverable/Memory's shared blocks all still styled); duty-table containers still styled with no fixed width; nothing renamed.
10. **Assertion accounting** (hazard #1): per-file pre→post vs §1 baselines, landed method; nothing down silently; spot-check ≥5 rewritten assertions per big suite for predicate honesty; no scan touched.
11. **Phone 375px:** every landed structural property survives; new renders each have a both-way-probed leg.
12. **No ticker, no new `<time>` home, no date formatting** — all three scans byte-unmodified and green; the expiry countdown still render-derived.
13. **Bundle** vs the UI-5 reference, both-way CSS itemization, JS attribution copy-scale or split; fonts unchanged; no new package.
14. **Battery green in landing order over the real dist**; counters: vitest ≥642 (grow-only), 45 Go pkgs/14 skips, ⚙ 118/33, migrations 0001–0021.
15. **AGPL attestation in §52**: zero Odysseus code/CSS/markup/assets — the anatomy is the platform's own idiom over its own wire.
16. §52 written; every deviation RECORDED with its reason — a refusal-with-evidence beats silent compliance.

**Hazards named in advance (attack these):** the batch's highest-yield class — passes-in-jsdom/false-in-production and vacuous predicates behind stable counts — plus this packet's own: (1) assertion churn hiding weakened predicates; (2) an anatomy field no card serves (a deliverable link, a fabricated proposer, a derived risk figure); (3) check-first copy contradicting the tier/card semantics it teaches; (4) the A-2 explanation overclaiming what dev posture allows, or breaking the reason-once textContent pin; (5) an EmptyState over a pending read; (6) a shed rule with a live consumer on a frozen surface; (7) the opt-in panel's BENCH-REG wording drifting; (8) a /login door in the settings explanation that bounces for members; (9) the picker/PIN losing their pinned shapes; (10) a countdown timer or inline `<time>` entering with the restyle.

## 9. Files expected to change

`web/src/Inbox.tsx` · `Settings.tsx` · `settingsForm.tsx` · `Login.tsx` · `PushDevices.tsx` · `index.css` (the three sections' shed + the dated stay-note updates) · tests: `inbox.test.tsx`, `settings.test.tsx`, `push.test.tsx`, `responsive.test.ts` (only if a kept rule genuinely moves — prefer none), `doubles.ts` (test wiring only) · `P3/CONVENTIONS.md` (§52). Everything else: wall 1. `sweep.test.tsx` is expected UNTOUCHED (duty containers kept per R4; the motion scan already covers these surfaces).

## 10. Open questions for coordinator disposition (pre-launch)

- **OQ1 — the "what to check first" grain.** No served field carries per-card guidance beyond the 13.5 help block (api.ts:512–538; the help renders already, Inbox.tsx:769–789). **Recommendation: class-grain client-authored teaching copy per R9**, both-direction pinned, with the served help block remaining the card-specific authority — the §51 SurfaceHead-copy precedent, and the only form that invents no card fact. Alternative (per-card generation) would fabricate guidance the platform never wrote — barred by the no-fabrication invariant.
- **OQ2 — the jump leg.** The proposal says "jump-to-deliverable"; **no approval card serves a deliverable id** (api.ts:512–538, :848). **Recommendation: the served `run_id → task-page` link (Inbox.tsx:387) IS the jump leg** — restyled, labelled for what it opens, absent when unserved; a deliverable-resolving read would be a new read beyond the freeze. If the operator wants a direct deliverable jump later, it enters as its own sanctioned-read decision post-batch.
- **OQ3 — shell.test.tsx's stale comment.** The :385 test carries a UI-4-era comment "Login.tsx is byte-unchanged: it always could render here" — true when written, stale once this packet restyles Login. **Recommendation: leave `shell.test.tsx` byte-unchanged** (the pins-pass-unmodified wall outranks a comment; §52 records the comment as UI-4-scoped so the next packet that legitimately opens the file trues it). Alternative — a comment-only edit now — trades a clean wall for a tidy, and the §50 D5 comment-truth precedent required a sanction; none exists here.
