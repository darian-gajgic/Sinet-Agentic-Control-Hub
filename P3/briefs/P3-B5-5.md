# P3-B5-5 grounded brief — S14.8 regression evals + the revalidation runbook + promptfoo adoption

**Packet:** P3-B5-5 (ratified B5 queue row, `P3/STATE.md`). **Spec home:** S14.8; adjacents S14.2/S14.5 (recording), S16 (rail), S03.3 (bump gate), S07.9–S07.10 (judge/rubric governance), S08.1/S08.10 (worker eval hooks + revalidation triggers), S12.9–S12.10 (battery + swap gate). **This brief is the executor's sole handoff artifact AND the evaluation rubric** (§8). Spec wins over this brief; a discovered conflict/gap is an S00.9 candidate flagged to the coordinator, never silently resolved. BENCH-REG (`Spec/benchmark-preregistration-v1.md`) is READ-ONLY. No step-lists: requirements bind outcomes, sequencing is the executor's.

## BINDING carried facts (verified in the tree at grounding, 2026-07-23 — do not relitigate)

- **The B5-4 recording surface is open for this packet:** `conformance.RecordResult` (`internal/conformance/conformance.go:138–197`) appends one `eval.score_recorded` event AND updates the row's `last_run`/`last_result` in ONE WriteTx; contract-minimum fields enforced (`ErrIncompleteResult`: RowID, SuiteVersion, AssetID, AssetVersion, Runner, RunnerVersion all required; `scorePayload` at `conformance.go:120–129` = the S14.2 family-14 minimum verbatim — suite id+version, asset id+version, metrics, runner+runner version — plus `result` green|red). Unknown rows refused (`ErrUnknownRow`); DERIVE rows refused (`ErrDeriveRow`). `eval.score_recorded` is MINTED 80/13 (`internal/eventlog/contract.go:535`; `contract_test.go:149–153` pins 80 minted + 13 declare-only = 93) with B5-5 named as a future co-producer.
- **The S03.3 two-limb tie is registry DATA; the (b)-limb is THIS packet's to build:** `bumpGateNote` (`internal/conformance/registry.go:10–17`) on every engine_bump row: a bump lands only after (a) per-lane conformance suite green AND (b) the S14.8 before/after quality probe shows no regression (P-T02-5) — "limb (b) is B5-5's, referenced here as data". `Store.BumpGatingRows` surfaces the 5 engine_bump rows. B5-4 built no probe machinery.
- **`eval.sweep_interval` is THIS packet's key** (B5-4 OQ6(a) ratified): declared `internal/settings/index.go:853–858` — Section S14, TypeInt, Unit months, Default 3, clamp 1–6, ratified R12 §4.7, help "Months between full regression-eval sweeps". B5-4 named it referenced-not-consumed (`registry.go:74–78`); B5-4's landing verified it correctly absent from consumption. The settings-backed cadence mechanism `setting:<dotted-key>:<unit>` exists (`registry.go:51–53, 83–85`) and is read live at evaluation time.
- **The B4-7 rider-1 harness is the P-T06-5 instrument, committed and re-runnable:** `internal/adapters/claudecli/rider1_golden_test.go` (gated `SINET_RIDER1=1`; `rider1Model = "claude-opus-4-8"`, `rider1StopLine = 5.00`). Measured 2026-07-22 on opus-4-8: **TPR 1.000, 95% Wilson [0.84, 1.00] (20/20); TNR 0.500, Wilson [0.19, 0.81] (3/6)**; length bias point-biserial r = −0.167 (P-T06-3). Record: `P3/measurements/2026-07-22-rider1-golden-set-opus.md`. Rates live in **rubric v2** (`internal/verify/seeds.go:115–159`: `SeedSoftwareRubric` Version 2, `GoldenSetRates{Measured:true, MeasuredOn:"2026-07-22"}`, measured `LengthBiasNote`, `JudgePin` = opus-4-8/e06f0a4) — **RATIFIED at the B4 gate 2026-07-23 (D4)**; the over-strict TNR 0.50 is on record as safe-direction; a byte-identical-schema re-run stays at gate discretion (the rider prompts are S07.5-shaped replicas).
- **Seeds and their knowledge-gate homes:** `verify.SeedGoldenSet` = `golden-software` v1, 26 cases (20 planted across AC-BLOCKER/SANITY-BLOCKER/CHECK-INTEGRITY/RESEARCH-NOT-RUN/REOPEN-SPEC/V0-MALFORMED + 6 clean controls); `GoldenSet.Validate` enforces the ratified 25–50 size AND both defective+clean classes (`seeds.go:204–241`). `SeedCalibrationSet` = 10 entailment pairs. Strict-JSON `Load*` + `WriteSeed`. Governance-homed at B3 via `memory.Gate.EnsureB2SeedGovernance` (`internal/memory/seeds.go:83–98`: `seed-verify-rubric-software` topicKey `verify/rubric/software` KindRubric; `seed-verify-golden-software` topicKey `verify/golden-set/software` KindExemplar) — the 8.3 knowledge-gate entry point exists. Deferral record: `P3/measurements/2026-07-20-verification-seeds-deferral.md` (its B4-due golden-set measurement row is now CLOSED by rider 1).
- **Worker surfaces exist; the green-stamp verb does NOT:** template eval hooks `GoldenSetRef`/`PlantedDefectRef` (`internal/worker/template.go:46–47`; S08.1). `Store.FlagByModel` (`lifecycle.go:525`) and `Store.FlagByEnginePin` (`lifecycle.go:553` — P-T14-1/S08.10(b), matches on `validation_records.engine_pin`) both exist. **No unflag/reinstate/revalidation-stamp verb exists** — S14.8's "green: release with a dated revalidation stamp" has no surface yet.
- **P-T15-1 is BUILT (B4-7) — consume, never rebuild:** `local.SwapGate.Retarget` (`internal/local/swap.go:224`) refuses uncalibrated targets (`ErrUncalibratedTarget`/`ErrBatterySuiteNotRun`) BEFORE any alias goes live, and fires the 7.3 trigger through `FlagByModelFunc` (`swap.go:166–196`) satisfied by `worker.Store.FlagByModel` at the shell root (`internal/local` never imports `worker` — the wall precedent).
- **The T15 battery pre-registered this packet's seam:** `internal/local/battery` (Version `t15-v1`) "runs standalone NOW, consumable by the S14 pinned eval runner at B5 (the seam is noted, no S14 machinery is built here)" (`battery.go` package doc). It produces `local.SuiteResult`/`local.LabeledItem`; the swap gate + calibration store consume them.
- **`verdict.recorded` carries judge golden-set rates on every verdict** (`internal/verify/record.go:44–55` `GoldenSetRates`; S07.11) — renamed from verify.round at B5-1.
- **The pre-B6 Node-CLI adoption precedent is in-tree:** the `sandbox-runtime` lock entry (components.lock, srt notes): external Node CLI consumed as an unmodified subprocess, "Not a Go module → NO modules claim", npm -g exact-version host install executed as a GATE act, PATH + `SINET_SRT_PATH` discovery, stub-binary-under-`t.TempDir()` hermetic tests, S16.4 walk in the entry notes with #10 as the gate item. The claude CLI entry is the same class. Host facts: node v22.22.1, npx, npm, docker all present (`/usr/bin/*`).
- **Rail state:** `components.lock` = 17 entries, strict JSON, `Kinds` includes `standby` (`internal/lockfile/lockfile.go:19–31`), `License{spdx,scope,checked}`, `Role{summary,section}`, floating pins rejected, `Modules` is the Go-only extension field. No promptfoo/DeepEval entry exists. Migrations run 0001–0011 → this packet's migration is **0012**.
- **BENCH-REG §10.1(d) verbatim:** "the domain's rubric + worker golden sets and planted-defect suites … are green at their per-version floors. Floors are registered **per rubric/eval-set version at 8.3 knowledge-gate entry**; the gate cannot be evaluated — and therefore cannot open — while the domain lacks registered suites or any suite is red." Gate-limb evaluation/display is B5-7's; this packet builds the floor registry + the green/red check.
- **B5 phase preamble (ratified):** S14 ⚙ keys already DECLARED — consume by dotted key, NEVER re-declare (tally stays 118/33). "Any paid legs (behavioral-canary mini-evals, promptfoo golden runs) are pre-registered with stop lines at their packet, like the B4-7 riders." Phase paid spend so far: $0. Judge seat = opus-4-8; engine pin now 2.1.218 (B4 gate D2).

## LIVE verification (fetched this session, 2026-07-23 — S16.4 #3/#4/#5 inputs; #10 operator ratification queued for the B5 gate)

- **promptfoo — candidate pin `0.121.19` (npm), license MIT.** npm `dist-tags.latest = 0.121.19`, published **2026-07-14** (registry.npmjs.org/promptfoo/latest; `npm view promptfoo time`: steady cadence 0.121.13 2026-05-28 → 0.121.19 2026-07-14). License MIT on both npm and GitHub (spdx MIT). Node engines `^20.20.0 || >=22.22.0` — host node v22.22.1 satisfies; official docs note post-Node-20-EOL releases require ≥22.22.0 (promptfoo.dev/docs/installation, fetched 2026-07-23). Repo `promptfoo/promptfoo`: **not archived, pushed 2026-07-23**, 23,527 stars, 450 open issues (api.github.com/repos/promptfoo/promptfoo). npm publishes carry SLSA v1 provenance (GitHub Actions Trusted Publisher/OIDC). The candidate pin equals the S16.3 observed value (`v0.121.19 observed`); the executor re-verifies npm latest at execution time, records the checked date, and pins the version it behaviorally probes (S16.4 #5).
- **The OpenAI acquisition is REAL and announced:** 2026-03-09 (openai.com/index/openai-to-acquire-promptfoo/; promptfoo.dev/blog/promptfoo-joining-openai/; techcrunch.com/2026/03/09/openai-acquires-promptfoo-to-secure-its-ai-agents/). OpenAI's public statement: Promptfoo "will remain open source under the current license"; closing subject to customary conditions. S14.1's "promptfoo → OpenAI within six months" prediction has landed. **The DeepEval swap trigger has NOT fired** — the trigger is governance DRIFT manifesting (S14 Deferred list), and as of 2026-07-23 the license is intact (MIT), release cadence healthy (four releases since the announcement), repo unarchived. The S16.3 watch column ("OpenAI-acquisition drift watch") now watches a live integration, not a rumor — the lock entry's watch/abandonment fields must say so.
- **Distribution forms (official installation docs):** npm global (`npm install -g promptfoo`), npx (`npx promptfoo@latest`), Homebrew, local npm dep. **No official Docker image is documented** on the installation page.
- **DeepEval — funeral plan VIABLE:** PyPI `deepeval` **4.1.3, uploaded 2026-07-22, Apache-2.0**, requires-python ≥3.9 (pypi.org/pypi/deepeval/4.1.3/json); repo `confident-ai/deepeval` not archived, pushed 2026-07-22, 17,069 stars (api.github.com). Note honestly in the standby entry: DeepEval is a **Python** tool — the pre-registered swap crosses ecosystems, which is survivable precisely because tools are runners-never-stores (P-T11-4): a swap replaces the runner adapter; every record already lives in `platform.db`.

## 1. Numbered requirements (each with its S-ref)

### A — promptfoo adopted via the rail (S16.1–S16.4; S14.8 ¶1; P-T11-4)

1. **Lock entry materialized from the S16.3 promptfoo row** (S16.3; CONVENTIONS §4): name promptfoo, kind `library`, pin exact (candidate 0.121.19 — live-verified above; never `latest`), `license {MIT, path-scoped at the exact pinned ref with the carve-out scan (enterprise/, ee/, dual-license trailers — P-T16-2), checked date}`, `role {regression-eval runner; runner-never-store — all scores land in platform.db, → S14.8}`, `replacement` = the pre-registered DeepEval swap, `abandonment`, `watch` = the OpenAI-acquisition drift watch row (S16.3 watch column), `last_review`. Entry notes carry the full S16.4 walk #1–#9 with the live facts above (the srt-entry precedent); **#10 operator ratification queued for the B5 gate** (the B0-5/B1-3 standing pattern).
2. **DeepEval standby entry** (S16.2: a standby entry MUST exist for every replacement path naming a concrete third-party component; the Ollama-row precedent "observed; pin on activation"): kind `standby`, observed 4.1.3 / Apache-2.0 / PyPI recorded, pin-on-activation posture, re-entry condition = promptfoo governance drift manifests (S14 Deferred). Lock count 17 → **19**.
3. **Adopt-don't-fork, runner-only posture** (S16.1; S14.8 ¶1): promptfoo runs unmodified, consumed as an external Node CLI subprocess, integration by config/invocation only. **NOT a Go module — no `modules` claim** (the claude/srt precedent); **no npm-side `modules` mechanics invented** (CONVENTIONS §4: the npm-side equivalent is defined at B6, not before — consumption per OQ1 disposition).
4. **Runners-never-stores enforced structurally** (P-T11-4; S14.1 adoption criteria; S16.4 #7): promptfoo invoked self-hosted only — no share/remote flags, telemetry disabled at invocation (env/config); its output is parsed and every score lands in `platform.db` as `eval.score_recorded`; promptfoo's own on-disk eval state is scratch under the run's temp dir, never read back as a record.
5. **Runner seam** (S01.3 adoption seam; S14.6 "promptfoo-runnable" reuse): the promptfoo invocation sits behind a narrow Runner interface in the new package — discovery and install posture per OQ1; hermetic CI runs against a fake runner binary (the srt stub precedent); a real-binary leg auto-runs when promptfoo is installed and prints `SANCTIONED SKIP (CONVENTIONS §10)` otherwise. B5-6's behavioral canaries later reuse this seam (note in the package doc; build nothing for B5-6).

### B — scores land as `eval.score_recorded` through the B5-4 surface (S14.2 family 14; S14.5; S14.8 ¶1)

6. **Every regression/eval result recorded with the contract-minimum payload** (S14.2 family 14) through `conformance.RecordResult` per OQ2 disposition — suite id+version, asset id+version, metrics, runner+runner version, all non-empty. Runner identity is honest and version-pinned: `promptfoo`+pinned version for promptfoo-run objects; `internal/local/battery`+`t15-v1` for battery-run local suites; the rider harness identity for judge golden runs.
7. **The regression sweep earns its conformance-registry row** (per OQ2(a) if ratified; S14.5 "the single scheduling home for every proven-not-assumed obligation"): one new seed row, cadence `setting:eval.sweep_interval:months` (the existing settingCadence mechanism), trigger set covering engine_bump + swap + drift + the sweep cadence; red → the existing B5-4 red→card derivation; row notes record that this row IS the S14.8 (b)-limb's recording home (closing the loop the `bumpGateNote` opened). New seed rows reach existing DBs via the insert-once `INSERT OR IGNORE` seed (new row ids don't collide; the B5-4 note about note-evolution-needs-migration applies to EDITS of existing rows, not additions).
8. **No new event type** (S14.2 rule 2; B5-1 contract): the 80/13 inventory stands — B5-5 is a co-producer of the minted `eval.score_recorded`, nothing else. The `contract.go` provenance note for the type may gain the co-producer citation (comment-level; the verbatim per-family lock pins S14.2 text, not provenance strings).

### C — per-asset eval objects + registered floors (S14.8 ¶2; S07.10; S08.1; BENCH-REG §10.1(d))

9. **Per-asset eval-object binding** (S14.8 ¶2; S08.1 eval hooks): every worker template version and every rubric version resolves to its eval set = golden cases (25–50, grown from real outcomes) + a planted-defect suite the judge MUST fail. v0 software content is the EXISTING artifacts — `golden-software` (its 20 planted cases ARE the planted-defect suite; its 6 clean cases are the TNR controls) + `rubric-software` v2 — no new case content invented; growth paths stay the ratified ones (S07.10 incident refresh; S08.6 dry-run pair seeding). The binding surface must resolve a worker version's `GoldenSetRef`/`PlantedDefectRef` and a rubric version to concrete (set id, version) pairs.
10. **Floors registered per (rubric/eval-set id, version)** (S14.8 ¶2; BENCH-REG §10.1(d) verbatim above): a floor store as platform data (migration 0012), keyed by asset id + version, holding the registered planted-defect catch floor (and companion metrics), with the registration hook at 8.3 knowledge-gate entry (the `memory.Gate` governance home — the B2 seeds are already homed there; floor registration attaches to that entry point, not a second gate). Seed floor VALUES per OQ5 disposition, operator-ratified at the B5 gate. Gate-limb (d) EVALUATION/display is B5-7's — this packet builds the store + the per-suite green/red check only.
11. **Rubric falsifiability check** (S14.8 ¶5; 5.7): a rubric version "works" iff its planted-defect suite catches at least its registered floor, measured per rubric version. Below-floor → the rubric version is flagged (red flag-card to its owner), never silently passed. The benchmark practice as outer falsification loop is B5-7's — reference only.
12. **Planted-defect content classes stay v0-scoped** (S14.8 ¶2): code = test-adequacy defects + known-bad artifacts (the existing 20 planted cases cover this class); research planted-unsupported-citations is v0.1 — NOT built (the entailment calibration set stays idle per S07.4/A8).

### D — the revalidation runbook (S14.8 ¶3; S08.10; S12.10; P-T14-1; P-T06-5; P-T15-1)

13. **Trigger surface, exactly the spec's three classes** (S14.8 ¶3): (i) a watchlist drift finding — accept-shape only, the emitter is B5-6's (`drift.finding` is minted by B5-3's watch machinery only where it exists; the runbook exposes a trigger verb, fakes no emitter); (ii) an engine bump — S03.3 step 5: Sinet's deliberate-bump procedure is the announcement clock (P-T14-1), `FlagByEnginePin` is the existing flag surface (S08.10(b)); (iii) a model/duty-alias swap — the B4-7 swap gate already fires `FlagByModelFunc` (S12.10); the runbook CONSUMES that flag set, it does not re-implement the swap gate.
14. **The runbook pipeline as spec'd** (S14.8 ¶3): flag dependent workers/judges/rubrics (7.3) → run affected golden sets + planted-defect suites → compare TPR/TNR and task metrics against the **pinned baseline** → green: release with a **dated revalidation stamp**; red: asset stays flagged, card to its owner. The pinned baseline is the asset's last measured rates (for `rubric-software` v2: TPR 1.000 / TNR 0.500, measured 2026-07-22 on opus-4-8 — the rider-1 record).
15. **The green-stamp verb** (S14.8 ¶3; S08.10(a) "dated revalidation stamp"): the missing worker-lifecycle surface — a flagged template version returns to active ONLY through a dated revalidation record (who/when/which suites/vs which baseline), evented on the existing worker family (no new event type). Red keeps status=flagged; flagged workers may run supervised, never unsupervised (S08.10(a)).
16. **P-T06-5 wired structurally** (S14.8 ¶3; S07.9): a judge-model change blocks unsupervised judging until golden-set re-measurement passes. The block consumes the rubric bundle's own data (`JudgePin` + `GoldenSetRates.Measured/MeasuredOn` vs the current judge seat) at the enforcement point per OQ3 disposition; the committed rider-1 harness is the re-measurement instrument (re-runnable per judge change; byte-identical-schema re-run at gate discretion per the D4 ratification note).
17. **P-T15-1 consumed, not duplicated** (S14.8 ¶3; S12.10): every local-tier seat swap already mandates recalibration via the swap gate's hard block (`ErrUncalibratedTarget`/`ErrBatterySuiteNotRun`); the runbook records swap-triggered revalidations through the same flag→run→compare→stamp pipeline and invents NO second gate.
18. **Cadence: on-trigger + the full sweep at ⚙ `eval.sweep_interval`** (S14.8 ¶3): consumed by dotted key through a narrow per-package Settings interface (CONVENTIONS §2/§6); sweep dueness derives from stored state, restart/suspend-safe (the B5-4 passive-dueness posture — never an in-memory ticker).
19. **Aggregate-green is insufficient** (S08.10(a); R15 §2.4): the comparison is per-slice — the planted-defect suite is the slice guard; a red slice reds the asset even when averages improve.

### E — the engine-bump quality probe: the S03.3 (b)-limb (S03.3 ¶3 item 3; S14.8 ¶4; P-T02-5)

20. **Before/after small-task-battery machinery built here** (S14.8 ¶4 "Engine-bump quality probes run here"): a probe suite definition (small task battery, versioned like the other eval objects), run before/after against the candidate engine, compared — no regression = (b)-limb green. The S03.3 gate POLICY stays normative at S03.3 (a bump lands only after (a) AND (b)); this packet builds the (b)-limb machinery the `bumpGateNote` references, recording per OQ2 disposition. Schema tests catch schema drift; only quality probes catch effort/context-handling drift (P-T02-5) — the probe content must be quality-shaped (small real tasks), not schema-shaped.
21. **Probe execution posture is hermetic-first** (B5 preamble): the machinery + fake-runner tests land now; a REAL before/after run needs a live candidate engine and (on paid lanes) paid calls — execution is NOT packet-mandatory, pre-registered per OQ4.

### F — package home, walls, composition (S01.3; CONVENTIONS §2)

22. **New package** (name P3 discretion, short lowercase — `internal/evals` suggested): the S14.8 home — eval-object binding, floor store, runbook, sweep dueness, quality-probe machinery, the promptfoo Runner adapter. Imports: storage/eventlog/settings/conformance + stdlib (+`verify` only if OQ3 lands the gate function there and the edge is needed); **NEVER imports `worker`, `local`, or `stage`** — flag/stamp/swap surfaces ride func seams injected at the shell root (the `FlagByModelFunc` precedent). AST import-wall test (the §26/§31/§32 pattern).
23. **Shell composition:** the runbook wires at the root — `worker.Store.FlagByModel`/`FlagByEnginePin` + the green-stamp verb, the swap-gate hook, `conformance.Store` recording, the Runner. Wiring is composition only; no package gains a forbidden edge.
24. **`internal/verify` stays wall-clean** (CONVENTIONS §15): any P-T06-5 gate check added there is a pure function over existing verify types (no new verify imports); the judge-construction site consults it (OQ3).
25. **CONVENTIONS §33 appended** (append-mostly; cites S14.8/S16 and the OQ dispositions).

### G — tests (CONVENTIONS §3; stdlib only)

26. **Non-tautological proofs, each failing without its mechanism:** the P-T06-5 block refuses unsupervised judging on a judge/pin mismatch or unmeasured rates; the floor check reds a below-floor rubric version and the red raises the owner card; a recorded result lands the exact contract-minimum payload (golden fixture, the §29 conformance-set pattern); sweep dueness survives a simulated restart (stored state, no ticker); the green stamp returns flagged→active ONLY with a dated record and red does not; the bump/swap/drift triggers each produce the flag set from seeded workers (`FlagByEnginePin`/`FlagByModel` orchestration exercised); the fake-runner harness proves output-parse→record without promptfoo installed; the real-promptfoo leg version-asserts the pin (pin↔lock coupling, the `TestPinMatchesLock` pattern) under sanctioned skip.
27. **Battery green precondition** (CONVENTIONS §3/§5): gofmt/vet/build/test green; `lockgate` passes at **19** entries; settings tally stays **118/33**; migrations 0001–0012 with 0001–0011 byte-untouched; `-race` on touched packages.

## 2. Seams to respect (stub shapes for phases not yet come)

- **B5-6 (watchlist/canary executors):** `drift.finding` production, behavioral-canary scheduling/running, canary alert wiring — OUT. The runbook exposes its drift-trigger verb and the Runner seam; nothing calls them yet.
- **B5-7 (benchmark machinery):** BENCH-REG sampling/pairs/epochs/gate-limb evaluation — OUT. Floors are limb-(d) INPUTS built here; `benchmark.pair_recorded` stays declare-only.
- **B5-8 (retention/history):** run summaries, compaction, query layers — OUT.
- **B6:** SPA rendering of stamps/flags/dueness/floors; the npm-side `modules` equivalent — OUT (CONVENTIONS §4).
- **v0.1:** research-domain planted-citation suites; entailment activation (stays idle per A8) — OUT.

## 3. OPEN QUESTIONS — coordinator dispositions required BEFORE the executor launches (the B5-1..4 precedent)

- **OQ1 — pre-B6 consumption mechanics for a Node-ecosystem runner.** S16.3 kinds promptfoo "(library, runner)"; CONVENTIONS §4 defines npm-side `modules` only at B6; S16 text nowhere fixes HOW a Node tool installs pre-B6. Options: (a) **external Node CLI, the srt precedent verbatim** — lock entry with no modules claim; exact-version `npm install -g promptfoo@<pin>` executed as a B5-GATE host act (host changes are gate acts); discovery via PATH + a `SINET_PROMPTFOO_PATH`-style override; hermetic CI on a fake runner; real leg sanctioned-skip; (b) `npx -y promptfoo@<pin>` per invocation — exact-pinned but network-touching and cache-dependent at run time (a floating supply-chain surface on every cold run); (c) a container — no OFFICIAL image is documented (live check), so the rail would have to pin an unofficial or self-built image. **Recommendation: (a).**
- **OQ2 — recording shape for per-asset results, the sweep, and the (b)-limb probe.** `RecordResult` requires a registry RowID and refuses unknown rows; per-asset eval runs are asset-scoped, and B5-4 seeded no S14.8 row. Options: (a) **one new regression-sweep seed row** (cadence `setting:eval.sweep_interval:months`); every per-asset result records against it (the payload's AssetID/AssetVersion carries the asset; each record updates the row) with one aggregate sweep-level result recorded LAST (red if any red) so the row's resting state is the sweep verdict; the (b)-limb probe records against the SAME row with AssetID = the engine, AssetVersion = the candidate pin — keeping the adapter lanes' rows purely (a)-limb; (b) per-asset registry rows — unbounded growth; the registry is for standing obligations, asset state already lives in the worker/rubric stores; (c) direct eventlog appends bypassing `RecordResult` — duplicates the one-WriteTx discipline and loses the row mirror + red→card derivation. **Recommendation: (a).**
- **OQ3 — the P-T06-5 enforcement point.** "Blocks unsupervised judging" must be structural, but S07.9 owns judge policy and `internal/verify` logic has been deliberately untouched since B2-3. Options: (a) **a pure gate function in `verify`** over the rubric bundle's own fields (JudgePin + GoldenSetRates vs the current judge seat), consulted at the stage judge-construction site (stage already imports verify — no new edge); the runbook owns re-measurement + the version bump that unblocks; (b) the check lives in the new evals package and stage imports it — a new import edge for one predicate; (c) flag-the-rubric-asset only (no structural block — fails the headline's "blocks"). **Recommendation: (a).**
- **OQ4 — paid legs: which, and at what stop lines.** Hermetic ($0): runner adoption, eval objects, floors, runbook machinery, sweep scheduling, fake-runner tests, local-tier suites (battery/local lane). Paid: any REAL golden-set run on the paid judge (26 cases × 2 axes on opus-4-8 ≈ **$2.10 projection**, rider-1 measured $1.87). One honest observation forces the question: the rubric-v2 baseline was measured at engine 2.1.216/2.1.217-era and the engine bumped to 2.1.218 at the B4 gate — under the very runbook this packet builds, that bump would flag the rubric baseline for re-measurement (the first real runbook execution is genuinely WARRANTED, not make-work). Options: (a) packet stays fully hermetic; **pre-register** the first-real-sweep leg (golden 26×2 on opus-4-8, projection $2.10, **stop line $5.00** — the rider-1 ratified number) for coordinator/gate-discretion execution; (b) mandate the paid leg in-packet. **Recommendation: (a)** — pre-registered at the packet per the phase preamble, executed only on coordinator say-so.
- **OQ5 — seed floor VALUES for `rubric-software` v2.** S14.8 registers floors per version but fixes no numbers. Options: (a) **floor = the measured Wilson 95% lower bound** — planted-defect catch (TPR) floor **0.84**; TNR carried report-only at its 0.19 lower bound with the ratified over-strict-is-safe-direction note (a TNR floor would flag the judge for being conservative — the direction the gate accepted); (b) floor = the point estimates (TPR 1.00 — any single miss reds the rubric; brittle at n=20); (c) defer floors entirely to bring-up (leaves limb (d) unevaluable longer, BENCH-REG §10.1(d)). **Recommendation: (a), operator-ratified at the B5 gate** (floors are Sinet-registered data at 8.3 entry, not BENCH-REG text — no §17 event).
- **OQ6 — S12.9's "run via the pinned eval runner" vs the Go-native T15 battery.** S12.9 says the battery runs "via the pinned eval runner"; B4-7 built it Go-native (gate-validated) with the seam explicitly pre-registered ("consumable by the S14 pinned eval runner at B5"). Options: (a) **record-through, no re-plumb**: the battery stays the local-duty executor (itself pinned by repo version `t15-v1`), its SuiteResults land as `eval.score_recorded` through the recording surface; promptfoo is the runner for asset-facing golden/planted objects and (later, B5-6) behavioral canaries — S12.9's normative core (exact deployed quant+build, results in platform.db, golden-set governance) is fully met; (b) wrap the battery suites as promptfoo configs against the local `/v1` endpoint — duplicates a validated instrument and severs the Go-native calibration plumbing (`LabeledItem` margins feed the swap gate). **Recommendation: (a)**, and if the coordinator reads S12.9's letter as requiring (b), the clean fix is a small S00.9 annotation on S12.9 ("pinned eval runner" = the S14.8 runner family including the in-repo battery) — **flagged here as the packet's one potential S00.9 candidate; never silently resolved.**

## 4. ⚙ settings (confirmed against `internal/settings/index.go` this session; NONE re-declared — tally stays 118 keys / 33 domains)

| Key | Status in this packet | Verified declaration |
|---|---|---|
| `eval.sweep_interval` | **CONSUMED** (the sweep cadence; via a narrow per-package Settings interface, read live) | index.go:853 — S14, int, months, default 3, clamp 1–6, R12 §4.7 |
| `verification.check_audit_interval_days` | referenced-not-consumed (P-T06-1 suite-audit staleness — already consumed by `internal/verify`; a distinct cadence from the sweep) | verify.go:197 (existing consumer) |
| `canary.behavioral_interval` | referenced-not-consumed (B5-6's) | index.go:839 |
| `local.reeval.cadence_months` | referenced-not-consumed (S12.10 6-monthly re-eval; v1 schedule surface) | index.go:684 |
| `local.alias.<duty>` | untouched (the swap gate stays its sole write surface — B4-7) | CONVENTIONS §28 |

No new ⚙ key. Any probe/floor constant that S18 does not ratify is a named code constant flagged to the gate (the `sseBatchSize` precedent, under the standing settings-tab directive).

## 5. Files expected to change

- **NEW** `internal/evals/` (or the disposition's name): eval-object binding, floor store, runbook, sweep dueness, quality-probe machinery, Runner seam + promptfoo adapter, fake-runner test fixtures, import-wall test.
- **NEW** `internal/storage/migrations/0012_*.sql` — floor registry + revalidation-stamp/runbook state (0001–0011 byte-untouched).
- `components.lock` — +2 entries (promptfoo, DeepEval standby) → 19; dated edits only.
- `internal/conformance/registry.go` — +1 seed row (per OQ2(a)); nothing else in the package changes.
- `internal/worker/lifecycle.go` — the green-stamp verb (flagged→active with dated record).
- `internal/verify/` — the pure P-T06-5 gate function (per OQ3(a)) + comment-level pointer updates only.
- `internal/stage/` — the judge-construction site consults the gate (small, per OQ3).
- `internal/shell/` — root composition of the runbook seams.
- `internal/eventlog/contract.go` — provenance-note co-producer citation only (no count change: 80/13).
- `P3/CONVENTIONS.md` — §33.
- `P3/measurements/` — only if a sanctioned leg executes (per OQ4; dated file per the Def.8 pattern).

## 6. Adopted components touched

- **promptfoo — NEW adoption** (S16.3 row materialized; S16.4 walk in-entry; #10 = B5 gate). Never modified; external process only.
- **DeepEval — NEW standby** (funeral-plan entry; live-verified viable 2026-07-23).
- No existing entry changes (claude CLI pin 2.1.218, llama-swap/llama.cpp/Ollama, srt — all untouched).

## 7. CONVENTIONS constraints binding this packet

§2 (stdlib-first; dotted-key ⚙ via narrow interfaces; package naming; doc comments cite spec) · §3 (stdlib testing only; hermetic; green precondition) · §4 (rail: strict JSON, exact pins, no npm-side modules pre-B6, removal discipline, lockgate) · §5 (subject `P3-B5-5: … (S14.8, S16)`; stage explicitly; NEVER push; never touch `P3/STATE.md`/`Docs/`/`Spec/`/`Research/`) · §6 (migrations immutable; one-tx state+event; append-only) · §15 (verify walls: never `internal/gates`; quality gate ≠ effects gate) · §28 (battery/swap-gate posture: swap gate is the sole `local.alias` writer; calibration keys (duty, model hash, engine build)) · §32 (the registry is a scheduling HOME, never an executor — B5-5 is the executor side; RecordResult one-WriteTx; suites assert behavior never docs). BENCH-REG read-only. Adopt-don't-fork. Spec wins; conflicts → S00.9 candidates flagged.

## 8. Acceptance checklist (the evaluation rubric — each item concretely checkable)

1. `components.lock` has a promptfoo entry: kind library, exact pin, MIT path-scoped with checked date, DeepEval replacement, acquisition-drift watch, S16.4 #1–#9 walked in notes, #10 marked gate-pending; lockgate green at 19. (S16.2–S16.4)
2. DeepEval standby entry present with observed 4.1.3/Apache-2.0/pin-on-activation + the cross-ecosystem note. (S16.2)
3. No Go module added for promptfoo; no npm `modules` mechanics; grep-clean of any promptfoo import path. (CONVENTIONS §2/§4)
4. The Runner seam invokes promptfoo as an external process with telemetry disabled; output parsed → `eval.score_recorded`; promptfoo state is scratch-only. (P-T11-4; S14.8 ¶1)
5. Fake-runner hermetic tests pass with no promptfoo installed; the real leg sanctioned-skips absent the binary and version-asserts the pin when present. (CONVENTIONS §10 pattern)
6. Every recorded result carries the S14.2 contract minimum; a representative golden fixture asserts the exact payload; produced through `conformance.RecordResult` (or the OQ2-ratified shape) — proven by the event row + registry-row mirror in one tx. (S14.2; S14.5)
7. The regression-sweep registry row exists per OQ2 disposition, cadence settings-backed on `eval.sweep_interval`, dueness passive/restart-safe. (S14.5; S14.8 ¶3)
8. Per-asset eval-object binding resolves worker versions (GoldenSetRef/PlantedDefectRef) and rubric versions to (set, version) pairs; the existing golden-software/rubric-software v2 resolve without new content. (S14.8 ¶2; S08.1)
9. The floor store exists (migration 0012), keyed (asset id, version), registered at the 8.3 knowledge-gate entry point; seed values per OQ5 disposition, marked gate-pending. (S14.8 ¶2; BENCH-REG §10.1(d))
10. The falsifiability check reds a rubric version whose planted-defect catch < floor, raising the owner card — proven non-tautologically. (S14.8 ¶5)
11. The runbook accepts exactly the three trigger classes; bump and swap triggers drive the EXISTING flag surfaces (`FlagByEnginePin`, `FlagByModel`/swap-gate output); no re-implementation of the swap gate. (S14.8 ¶3; S08.10; S12.10)
12. The pipeline compares TPR/TNR + metrics vs the pinned baseline per-slice (planted suite = slice guard; aggregate-green insufficient). (S14.8 ¶3; S08.10(a))
13. Green stamp: flagged→active only with a dated revalidation record; red keeps flagged + owner card; both proven by tests. (S14.8 ¶3; S08.10(a))
14. P-T06-5: unsupervised judging structurally refused on judge/pin mismatch or unmeasured rates; unblocked only by a passing re-measurement (the rider-harness path); proven by a failing-without-gate test. (S07.9; S14.8 ¶3)
15. The (b)-limb quality-probe machinery exists (before/after comparison, quality-shaped content) and records per OQ2; no bump executor built; the S03.3 policy text untouched. (S03.3; S14.8 ¶4; P-T02-5)
16. Import walls hold (AST test): the new package never imports worker/local/stage; verify gains no new imports; composition at the shell root only. (S01.3; CONVENTIONS §15/§28/§32)
17. ⚙ discipline: `eval.sweep_interval` consumed by dotted key; no new key; tally 118/33 green. (CONVENTIONS §2/§6)
18. Event inventory stays 80 minted/13 declare-only; no new type. (S14.2; B5-1 contract)
19. Migrations 0001–0011 byte-untouched; 0012 single new file, one transaction, user_version bump. (CONVENTIONS §6)
20. Paid posture: $0 spent unless the OQ4-ratified leg is explicitly triggered by the coordinator; any executed leg has its pre-registered projection + stop line + dated measurement file. (B5 preamble; B4-7 rider precedent)
21. CONVENTIONS §33 appended; commit subject/trailer per §5; no push; STATE/Spec/Docs/Research untouched. (CONVENTIONS §5)
22. Full battery green: gofmt/vet/build/test, -race on touched packages, lockgate 19. (CONVENTIONS §3)
