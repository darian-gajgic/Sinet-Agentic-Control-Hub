# P3-RW-19 — the cancel's why becomes human (backend half)

Grounded 2026-08-18 against `Spec/drafts/` (canonical, spec-v1) and the tree at `c79786b`; acceptance tests committed RED at `63deabc`. Single-use: this brief expires when the packet lands.

## 0. Origin and scope wall

The 2026-08-18 final gate walk (spouse persona, kept world `~/.sinet-b6-final`, cancelled task `t-fe5ff6c325967c3e`) answered WHAT and WHO in under two minutes by both routes and blocked on the WHY alone. Two walker-named residues:

- **(a)** History's `status.tasks_cancelled` renders `cancelled_by` as "—" on pre-parity rows: its SQL (`internal/history/catalog.go`, the `status.tasks_cancelled` entry) derives the actor only from the structured leg (`$.detail.actor`, NULLIF-platform fallback on `$.actor`), while the task page's derive (`internal/api/reads.go` `cancelDecisions` → `structuredCancels` + `ledgerCancels`) has a second leg reading the `ledger.decide` record. The task banner says "cancelled by op"; the History row for the same task says "—". If owner ≠ canceller this actively deceives.
- **(b)** No human reason exists at cancel time: every "why" surface renders rule citations ("finalize-with-card (4.5)") where a motive should be.

**SCOPE WALL — backend only: capture, record, serve.** The reason input UI and all rendering run separately under the frontend pipeline (FRONTEND.md). This brief PINS the wire contract that pipeline will consume (§3). Nothing under `web/` changes in this packet. No new route; no route for `intake.Pipeline.Cancel` (unrouted today, stays unrouted — recorded reading R13).

## 1. Requirements

Every requirement carries its S-ref (Spec/drafts) or CONVENTIONS §-ref. "The one constructor" = `internal/run/canceldetail.go` `CancelDetail`.

**R1 — History serves the who by the same two-leg reading the task page uses** [S14.10 ¶2 (the canned catalog is the reliability floor); S02.2 (derive from durable records, never double-mint); 15.6 attribution via S02.2].
`status.tasks_cancelled`'s `cancelled_by` becomes `COALESCE(structured leg, ledger leg, NULLIF($.actor,'platform'))`:
- structured leg: `json_extract(e.payload,'$.detail.actor')` — unchanged, and it WINS;
- ledger leg (new): a correlated subquery on the SAME run — the newest `run_events` row of type `ledger_update` whose `$.change.verb` = `ledger.decide` and whose LAST `$.ledger.decisions` entry (`[#-1]`, SQLite JSON path) has `author` = `human` and text opening (case-insensitive) with `requester cancelled` or `requester: rethink` — serving that event's `$.change.actor`. This is byte-for-byte the qualification `internal/api/reads.go` `ledgerCancels` + `cancelLedgerPrefixes` applies (last entry, human author, the two mint prefixes);
- the NULLIF-platform fallback stays LAST. A row where all three legs are empty stays NULL — honest residual absence, not an error.
The subquery adds **no bind placeholder** and no statement separator, so the §37 whole-table structural rules (placeholders = slots+3, ends `LIMIT ?`, owner predicate in exact exported text, single statement) stay green with `catalog_test.go` unmodified.

**R2 — the History row's `reason` becomes the human motive with a frozen honest absence** [S14.10 (walk residue (b)); §37 honest-absence posture — 'UNPRICED' / '(no project)' precedent: absence is stated in the served row because no renderer sits between this row and the reader — the assistant "consumes these layers and nothing else" (S15.2 events row; S14.10 preamble)].
`reason` = `COALESCE(json_extract(e.payload,'$.detail.reason'), 'no reason given')`. The literal `no reason given` is a frozen wire literal from this packet on. The mechanical `$.reason` is no longer served on this row (recorded reading R12); pre-parity rows serve the honest absence — the ledger text's embedded "— note: …" suffix is prose, never scraped into a field (the reads.go leg-B posture, mirrored).

**R3 — the History row gains a `cause` column: the mechanical ending in plain words** [walk residue (b); §57 drafting rule 1 — requester-facing strings are written for someone who is not a programmer].
A SQL `CASE` over the transition's `$.to`: the running-stop shape (`completed`) and the parked/card-withdrawal shape (`finalized`) serve DIFFERENT plain-words sentences, with a generic plain fallback for any other value. No rule citation tokens — no "(4.5)", no "finalize-with-card", no "§", no S-refs — in any served sentence (pinned by the red test as a property, not a prose golden; exact sentences are the executor's, drafted under §57 rule 1). The mapping lives ONCE, in this SQL (see OQ1 for why not also on the task page).

**R4 — the one constructor gains the reason, additively** [S02.2 (durable record); §58 D2a (one cancel mapping, one implementation); RW-18 D1-R1 (one producer of the detail)].
`run.CancelDetail` carries a fourth input, the human reason, marshalled as `"reason"` inside the detail (`$.detail.reason`), `omitempty` — absent when empty, the `ask_id` precedent. `CancelCauseHuman` = `"human cancel (4.5)"` stays byte-frozen; the existing keys `cause`/`actor`/`ladder_invoked`/`ask_id` stay byte-frozen; the new field is ADDITIVE-ONLY. No call site may hand-build the detail JSON; the constructor stays the only producer. Mark: **property-based test** (stdlib `testing/quick`, the `truncation_rw16_prop_test.go` precedent) — for all inputs, the marshalled keys are a subset of the five frozen names, `cause` is byte-exact, and `ask_id`/`reason` are present iff non-empty.

**R5 — capture at the HTTP cancel verbs** [S15.2 (tasks/runs rows "cancel (4.5)"; evolution is additive-first, so a body field is the sanctioned shape); 4.5 via S06.9 ("Cancel is always available")].
`POST /api/runs/{run}/cancel` and `POST /api/tasks/{task}/cancel` accept an OPTIONAL JSON body `{"reason": "<one line>"}`. Absent body, empty body, `{}`, and empty `reason` all mean "no reason" and stay accepted (the landed clients post `{}` or nothing — §48 pinned the cancels path-only; this change is strictly additive). The reason threads through `api.CancelSurface` → `stage.Surface` → `Skeleton.CancelRun`/`CancelTask` → `cancelOne` → the one constructor, landing on EVERY transition the verb mints (a task cancel that ends three runs writes the same reason on all three — each transition is its own record, the reads.go per-run reading).

**R6 — capture at the card verbs: the existing `note` field IS the reason channel** [S15.6/S06.9 (cancel affordances live on cards); §58 (the {retry,cancel} card contract)].
- verify/ladder cards: `verify.Answer.Note` (landed, `json:"note,omitempty"`) — `answerCancel` (internal/stage/answer.go) passes `ans.Note` into the constructor beside `askID`; `ladderCancel` (internal/stage/ladderanswer.go) passes `ans.Note` through `CancelTaskAtCard` so every sibling transition carries it. The note keeps ALSO riding the ledger decision text via `noteSuffix` — that is the landed record, untouched. `retry` answers keep their note in the ledger text only: a retry mints no cancel detail, and this packet touches nothing else about it.
- intake cards: `intake.Answer` gains additive `Note string \`json:"note,omitempty"\``, honored on the two cancel-shaped answers — `ActionCancel` (approval card) and `ChoiceRethink` (SPEC-DOUBT card) — reaching the constructor at the `closeAndResume` cancel leg (internal/intake/answer.go:130). Elsewhere it is ignored at v0 with a comment saying so (OQ2).
- intake verb: `Pipeline.Cancel` gains the reason parameter for mint parity (all four paths accept one) — no route is added (R13).
NO second field is minted anywhere: one channel per surface, one constructor for the record.

**R7 — serve on the task page: `TaskDecision` gains `human_reason`, additively** [S15.5 task detail — "every human decision along the way (S2.4)"; S02.2 reconstruction; S15.2 additive-first].
`HumanReason string \`json:"human_reason,omitempty"\``, lifted by leg A (`structuredCancels`) from `$.detail.reason`. Absence is STRUCTURAL — key absent when no reason was given, never present-and-blank (the `ask_id` omission precedent); the frontend renders the honest "no reason given" line from the absent key (OQ1). Leg B (`ledgerCancels`) serves none — pre-parity records hold no motive, and scraping the "— note: " suffix out of ledger prose would be inventing structure. The mechanical `Reason` field keeps its landed semantics BYTE-UNTOUCHED, so `canceldecision_test.go` passes unmodified.

**R8 — the new served field passes the redaction edge** [§38 D10 / codor C2 — store-raw, serve-redacted; the `redactTaskDecisions` precedent].
`redactTaskDecisions` gains `HumanReason`. The stored `run_events` payload stays raw.

**R9 — one act, one row, still** [S02.2 (never double-mint); RW-18 "ONE ACT, ONE ROW"].
A post-parity card cancel writes BOTH records; the task page keeps serving exactly one row per run (leg order unchanged) and the History view exactly one row per task (its `GROUP BY t.task_id` + `MAX(e.event_seq)` unchanged; the ledger leg is a correlated scalar, never a join widening). Mark: **property-based/table-driven over shapes** — for k runs each carrying both records, the task page serves exactly k cancel rows and History exactly 1 per task, with the structured actor winning on every one.

**R10 — the reason is bounded at the NEW field only, and over-cap refuses, never truncates** [§60 (content is never edited — "delimiters may be completed, content never", applied as: refuse loudly rather than silently alter what the person wrote); §37 OQ6 (number-shaped bounds are structural constants)].
`cancelReasonMaxRunes = 280`, a structural constant in `internal/api` beside the handlers (transport-owned validation: the constructor never fails). Over-cap → HTTP 400, `SurfaceError` code `reason_too_long`, message naming the bound; NOTHING is minted. 280 because the affordance is a one-line motive, the record rides every transition of a task cancel, and a paragraph is a note for the card channel, which already exists. The landed card `note` field gains NO new bound (bounding a landed contract is a behavior change this packet was not given — the asymmetry is stated here on purpose; the ledger text already carries the note unbounded). **NO new ⚙ key** — flagged per the standing settings-tab directive ([[settings-tab-see-change-everything]]): the constant is interim and surfaces there later.

**R11 — literal pins, both ends** [§37 ("legacy aliases are carried in SQL, and the literals are PINNED"); RW-18 `cancelcatalog_rw18_test.go` / `cancelliterals_rw18_test.go` precedent].
The catalog SQL's new literals — the two ledger prefixes, `ledger_update`, `ledger.decide`, `human`, `no reason given` — are SQL data; a history-side test pins the two prefixes and the ledger discriminators against `internal/ledger`'s exported constants (`EventLedgerUpdate`, `VerbDecide`, `AuthorHuman` — test-package import, the wall binds production only) and against the producer-pinned prefix strings (the same two spellings `internal/stage`/`internal/intake` `cancelmint_rw18_test.go` pin at the mints). The api side extends its constructor pin (NEW test function, new file or `cancelwhy` file): `run.CancelDetail` with a reason marshals `"reason":"…"`, and without one marshals no `reason` key.

**R12 — recorded reading: the mechanical `$.reason` leaves the History row.** The row's question is "who cancelled each one, why, and when" (its own description); the mechanical sentence remains served verbatim on `status.run_state_history` and on the task page's `Reason`. Keeping three why-shaped columns (`reason` human, `cause` plain, plus the raw mechanical) would make the row answer the same question three ways.

**R13 — recorded reading: `intake.Pipeline.Cancel` is unrouted in production** (grep-proven this session: no production caller). It still gains the reason parameter (R6) because it is a mint path and mint parity is the point of the one constructor; routing it is a different packet's decision.

**R14 — non-goals, held.** No new event type (`run.state_changed`'s pinned payload note names `detail` as an opaque member — additive fields inside it touch nothing in `internal/eventlog/contract.go`); no migration (asks/tasks/runs/run_events all carry what is needed; migration set byte-untouched); no new ⚙ (`internal/settings/index.go` byte-unchanged); no new dependency (`components.lock`/`go.mod`/`go.sum` byte-unchanged); no new catalog entry (count stays 51; the band test is untouched); no ledger schema change (the typed `kind` on ledger decisions stays the future packet RW-18 OQ1 named); `web/` untouched; `internal/local` untouched; the NO-AUTO-KILL wall untouched (the reason is data on existing human-only entry points — it adds no caller, so `TestCancelIsReachableFromTheHTTPVerbsOnly`'s target/sanction sets do not move).

## 2. Seams to respect

- **Package walls:** `internal/intake` cannot import `internal/stage`; both already import `internal/run`, which is why the constructor lives there — it stays there. `internal/history` is a LEAF (allowlist storage/eventlog/settings/local/redact, `importwall_test.go`); its new discriminators are SQL literals + test pins, never a production import. `internal/api` imports no producer package; its literals stay pinned by `cancelliterals_rw18_test.go` + the R11 extension.
- **One producer:** `$.detail.*` on a cancel transition is written by `run.CancelDetail` and nothing else. A second spelling of any literal is the drift class RW-18 existed to kill.
- **No second mint; serve = derive:** the reason is captured INTO the existing transition record. Nothing writes a new decision row, event type, or table for the reader's benefit (S02.2; the approvals-plane never-double-mint rule restated in canceldetail.go).
- **Owner scoping (S01.9):** the catalog keeps `scoped("t.user_id")` in the exact exported text (`OwnerScopeOpen`/`OwnerScopeClose`); the ledger-leg subquery correlates on the joined run (`le.run_id = r.run_id`) and can only widen WITHIN a task the scope already admitted. The API paths keep `authorizeOwner` untouched.
- **One cancel mapping (§58 D2a):** the reason threads THROUGH `CancelTask`/`CancelTaskAtCard`/`cancelOne`; no path grows its own ending. Signature threading, not a parallel entry: a `CancelRunWithReason` twin would be the two-spellings drift §58 forbids.
- **Drafting rule (§57):** every new requester-facing sentence (the `cause` values, the 400 refusal message) is written for someone who is not a programmer.

## 3. The wire contract (frozen for the frontend pipeline)

Request (capture):
- `POST /api/runs/{run}/cancel`, `POST /api/tasks/{task}/cancel` — optional body `{"reason": string}`; absent/empty = no reason; > 280 runes → 400 `{code:"reason_too_long"}`, nothing cancelled. Machine code, not prose, is the classification key (§48).
- Card answers (`POST /api/asks/{ask}/answer`) — the existing `answer.note` string is the reason channel on every cancel-shaped choice: verify/ladder `{"choice":"cancel","note":…}`, intake approval `{"action":"cancel","note":…}`, SPEC-DOUBT `{"choice":"rethink","note":…}`.

Served:
- Task page (`GET /api/tasks/{task}` → `decisions[]`): cancel rows gain `human_reason` (omitempty; absent = none given — the view renders "no reason given" from the absence; `reason` (mechanical) and `decision:"cancel"` unchanged). Redacted at the edge.
- History (`status.tasks_cancelled` columns): `task_id, user_id, title, event_seq, cancelled_ts, cancelled_by, reason, cause` — `cancelled_by` two-leg per R1 (NULL only when every leg is empty); `reason` = human words or the frozen literal `no reason given`; `cause` = plain-words ending, no citation tokens, distinct per mechanical shape.
- Frozen payload literals: `human cancel (4.5)` (`$.detail.cause`), detail keys `cause/actor/ladder_invoked/ask_id/reason` (last two omitted-when-empty), served key `human_reason`, honest-absence literal `no reason given`, refusal code `reason_too_long`.

## 4. ⚙ settings and constants

⚙ consumed by registry name: **none**. This packet reads no ⚙ key. `cancelReasonMaxRunes = 280` is a STRUCTURAL CONSTANT with its reason (R10) — the §37 OQ6 pattern — and is flagged for the deferred settings-tab pass. No new ⚙ key is introduced (any executor deviation from that is a STOP-and-refer, not a choice).

## 5. Files expected to change

Production: `internal/run/canceldetail.go` · `internal/stage/cancel.go` (thread reason: `CancelRun`, `CancelTask`, `CancelTaskAtCard`, `cancelOne`, `cancelDetail`) · `internal/stage/answer.go` (`answerCancel`) · `internal/stage/ladderanswer.go` (`ladderCancel`) · `internal/stage/surface.go` (`Surface.CancelRun`/`CancelTask`) · `internal/api/actions.go` (`CancelSurface` iface, body decode, cap) · `internal/api/reads.go` (leg-A SELECT + `TaskDecision.HumanReason` + `redactTaskDecisions`) · `internal/intake/cards.go` (`Answer.Note`) · `internal/intake/answer.go` (note plumb on the two cancel-shaped answers; `Cancel` param) · `internal/history/catalog.go` (the one entry's SQL). `internal/shell` needs no edit (the surface satisfies the widened interface by construction — verify, don't assume).

Adopted components touched: **none** — no engine, no npm, no Go dependency, no lock entry. Say so at landing with the counters.

Tests: the two committed red files (`internal/history/cancelwhy_rw19_test.go`, `internal/api/cancelwhy_rw19_test.go`) turn green; the executor's own red commit adds the capture-side tests (§7 T8–T13); plus the R4/R9 property tests and the R11 pins.

## 6. Sanctioned edits to pre-existing tests (by name, and nothing else)

The executor may not modify pre-existing tests EXCEPT these exact compile-only edits, forced by the R4–R6 signature threading (grep-proven complete this session):

1. `internal/api/cancelliterals_rw18_test.go` — the two `run.CancelDetail(...)` call expressions gain the new trailing reason argument (`""` where the existing assertions stand; a non-empty value may be asserted only in NEW test functions). No existing assertion changes.
2. `internal/stage/cancel_internal_test.go` — call sites of `sk.CancelRun`/`sk.CancelTask` gain the trailing `""` argument. No assertion changes; the NO-AUTO-KILL wall test's target/sanction sets are NOT edited (the surface names do not change).
3. `internal/api/actions_test.go` — `fakeCancel`'s two method signatures gain the reason parameter so the fake keeps satisfying `api.CancelSurface`. It may RECORD the value; assertions on it go in new test functions only.

If threading forces any edit beyond these three files, STOP and refer to the coordinator — do not widen the sanction yourself. `canceldecision_test.go`, `cancelmint_rw18_test.go` (both packages), `cancelcatalog_rw18_test.go`, `cancelledask_test.go`, `catalog_test.go`, `oversightreads_test.go` must pass byte-unmodified.

**Coordinator ruling 2026-08-18 (the executor referred, per the rule above; sanction widened by the coordinator, not the executor):**

4. `internal/intake/cancelmint_rw18_test.go` — ONE compile-only edit: the single `f.p.Cancel(...)` call (line 104) gains the trailing `""` argument, forced by the ratified-OQ4 threading of `Pipeline.Cancel`. No assertion changes. This supersedes the file's listing in the byte-unmodified set above for exactly that one line; every assertion in it still binds.
5. Ratified after the fact, by name: the executor's single edit to `internal/stage/cancel_internal_test.go` at the `sk.cancelOne` call site (`""` inserted; the raced-cancel assertion untouched) — the same compile-only class item 2 sanctioned for that exact file, on a method item 2 did not name.

## 7. Acceptance — headline, checklist, tests

**Headline:** a person who cancels work can say why in one line at every cancel affordance; the why (or its honest absence) and the who reach both the task page and the History row from the durable record alone; and the History row stops contradicting the task page on pre-parity history.

Checklist (each independently checkable):
- [ ] History pre-parity row (ledger record only) serves the human in `cancelled_by` — T1 (RED, committed).
- [ ] History `reason` serves the human words when given; the frozen `no reason given` when not — T2/T3 (RED, committed).
- [ ] History `cause` serves plain words, no citation tokens, distinct per mechanical shape — T4 (RED, committed).
- [ ] History: one row per cancelled task; structured actor wins over a conflicting ledger spelling — T5 (control, committed, stays green).
- [ ] History: owner scope holds through the ledger leg, three ways — T6 (committed; red on attribution, green on row counts).
- [ ] Task page serves `human_reason` from `$.detail.reason`; mechanical `reason` untouched — T7 (RED, committed).
- [ ] Absent reason = absent key, never blank — T7b (control, committed).
- [ ] `human_reason` passes the redaction edge; stored row stays raw — T7c (committed; red on the served-through direction).
- [ ] HTTP run cancel with a body reason lands it on the minted transition's `$.detail.reason` — T8 (executor red commit).
- [ ] Task cancel writes the same reason on EVERY sibling transition; ladder-card cancel note ditto through `CancelTaskAtCard` — T9 (executor).
- [ ] Verify-card cancel note → `$.detail.reason` beside the `ask_id` — T10 (executor; extend the `driveToOpenCapHit` harness shape of `cancelmint_rw18_test.go` in a NEW file).
- [ ] Intake approval-card cancel note and SPEC-DOUBT rethink note → `$.detail.reason` — T11 (executor; the `newFix` harness, new file).
- [ ] Over-cap reason → 400 `reason_too_long`, no transition minted — T12 (executor).
- [ ] R4 additive-only payload property (`testing/quick`): keys ⊆ frozen five; `cause` byte-exact; `ask_id`/`reason` present iff non-empty — T13 (executor, property-based).
- [ ] R9 one-act-one-row property over shapes and k runs — T14 (executor, property/table-driven).
- [ ] Landing counters: no ⚙ / event-type / migration / dependency / catalog-count movement (R14); `go build ./...` + full `go test -p 1 ./...` green; gofmt/vet clean.

**Committed RED (63deabc), failing today for the right reasons (verified this session):**
- `internal/history/cancelwhy_rw19_test.go` — `TestHistoryServesThePreParityCancelsHuman` (cancelled_by = ""), `TestHistoryCancelReasonIsHonestWhenAbsent` (serves the rule citation), `TestHistoryServesTheHumanReasonWhenOneWasGiven` (serves the mechanical reason), `TestHistoryCancelCauseIsPlainWords` (no `cause` column), `TestHistoryCancelRowsStayOwnerScoped` (member's row unattributed); controls green: `TestHistoryServesThePostParityCancelOnceWithTheStructuredActorWinning`.
- `internal/api/cancelwhy_rw19_test.go` — `TestTaskDecisionsServeTheHumanReasonFromTheDetail`, `TestServedHumanReasonIsRedactedAtTheEdge` (both: `human_reason` = ""); control green: `TestTaskDecisionsHumanReasonIsAbsentNotEmptyWhenNoneWasGiven`.
- Left as specs (T8–T14): they require the R4–R6 signatures/fields to COMPILE; Amendment-A lets inert type surface make red tests compile, but minting that surface is implementation, which is not the grounding agent's to do. The executor commits them red first, then closes the window.

Fixture note for T1/T5-shape tests: the pre-parity fixture is the walk's own record pair (transition actor `platform`, reason carrying `(4.5)`, no detail; `ledger_update` `ledger.decide` whose last entry is human-authored `requester cancelled …`) — see `seedPreParityCancel` in the committed file. SQLite's `$.a[#-1]` path syntax was probe-verified against the pinned `modernc.org/sqlite` during grounding (2026-08-18: `json_extract('{"a":[…]}','$.a[#-1].x')` returns the last element); the executor keeps the SQL qualification identical to reads.go's Go-side reading (last decisions entry, human author, the two prefixes).

## 8. CONVENTIONS that bind this packet

§3 (stdlib testing only; Amendment-A red-window mechanics — scope: this packet's paths; build stays green; window declared in the commit message and closed by the implementation commit) · §5 (commit subject format; explicit pathspec staging; packet sessions never push, never touch `Docs/`/`Spec/`/`Research/`/`P3/STATE.md`) · §37 (catalog structural rules; honest absences are stated, never zeroed; SQL literals pinned to their producers by test; no new ⚙ — structural constants carry their reasons) · §57 (requester-facing drafting rules bind the `cause` sentences and the refusal message) · §58 (+ drains: one cancel mapping, one implementation; the NO-AUTO-KILL wall is file-scoped and enumerated — widening a walled surface means widening its wall, and this packet must NOT widen it) · §60 (content is never silently altered — refuse over truncate) · §14 reading 9 as restated in `internal/stage/cancel.go`'s header (the ratified S02.3 mapping: running/draining→completed, parked→finalized, queued→finalized, claimed/new 409, terminal honest no-op) — this packet changes NOTHING about the mapping, only what the record carries.

## 9. Open questions for the coordinator (each with a recommendation)

- **OQ1 — where the plain-words mapping lives, per surface.** Spec text (S14.10, S15.5) is silent on serve-side prose vs view-side rendering. GROUNDED RECOMMENDATION, split: (i) History row — serve-side, in the SQL (R3): the assistant path has no view layer between row and reader, and §37 already puts honest-absence strings in served rows; (ii) task page — NO new served `cause_plain` field: the row carries the machine code (`decision:"cancel"`), `human_reason`, and the absence posture; the SPA renders plain words from the code (§48: classification from machine code; rendering is the frontend pipeline's lane per the scope wall). A second serve-side mapping would be a second spelling of the same sentences. The committed red tests assume (i) only.
- **OQ2 — intake `Answer.Note` outside the two cancel-shaped answers.** Recommendation: ignored at v0 with a comment (honoring it everywhere is the verify `noteSuffix` symmetry, but it exceeds "the cancel's why" and touches every intake ledger mint's wording — a different packet's blast radius).
- **OQ3 — the bound: 280 runes, refuse-not-truncate, code `reason_too_long`, no bound on the landed card `note`.** Recommendation: as stated (R10). If the coordinator prefers a different number it stays a structural constant either way.
- **OQ4 — `Pipeline.Cancel` gains the reason while unrouted.** Recommendation: yes (R13) — mint parity is the constructor's contract; routing is out of scope.

## 10. Process

Executor pipeline per SKILL.md amendments A/E: (1) its own red commit for T8–T14 (message declares the window), (2) the implementation commit that turns ALL RW-19 tests green and closes the window, naming the §6 sanctioned edits it exercised. Tests SERIAL (`go test -p 1 ./...`); no frontend battery; no world, no local model stack; $0 — every test here is tier-F/hermetic. Stage files by explicit pathspec only; never `git add -A` (the tree holds long-standing unstaged operator files). Do not push. A spec conflict found mid-build is a STOP and a referral, never a silent resolution (§8 posture).
