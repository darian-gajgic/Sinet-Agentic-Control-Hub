# P3-GF14 — grounded brief: the detached drive, the honest live channel, the canonical currency case, one stakes truth, and the receipts-drawer prose

**Packet:** P3-GF14 (cut from the 2026-08-28 live design review, `P3/design/gf9-review-findings-2026-08-28.md` — items M1+M2, M3, M8, M10, M11).
**Grounded:** 2026-08-31, fresh-context grounding agent. Read set: S02, S05, S06, S10, S15 in full (drafts canonical); CONVENTIONS §1–§22, §30, §38–§39, §54–§60, §70; the GF9 findings file in full; `P3/design/b6-gate-operator-findings-r5-2026-08-23.md` §C in full; `P3/briefs/P3-GF13-keep-ledger.md` (LIVE); the kept evidence world `~/.sinet-gf9-review` (read-only: control.log, platform.db via a scratchpad copy, the task-2 artifacts); the current code (intake, stage, api, metering, sse, reads).
**This brief is single-use.** It matches the code at the grounding commit and EXPIRES at packet landing (SKILL amendment D). No prior packet brief was read; the ONE live companion is the GF13 keep ledger.

---

## 0. What was witnessed (the evidence, first-hand from this grounding)

All timestamps UTC (the review narrates local = UTC+2). Task 1 `t-3df81201c0ab0cbe` ("A one-page price list for my candle shop"), task 2 `t-cccd5dc7d00a4f64` (thank-you note), world `~/.sinet-gf9-review` — KEPT EVIDENCE, never write to it, never start its plane.

- **M2, exactly:** ask `intake:…:3` (the earned open point) committed open 03:23:19 (seq 69–72: checkpoint → ledger → running→parked "waiting for your answer on the open points…" → `intake.state`). The requester answered it 03:25:19.146 with the six candle names and BARE-NUMBER prices ("Honey Glow 8, Sea Salt 9, … Winter Pine 11"); the answer beat resumed gen 3 (seq 73), drafted (engine 03:25:19→03:25:43, seq 76–89), and was still driving — building the approval card, no event for 2 m 18 s — when the page reloaded at 03:28:01.680: `ERROR stage: intake answer died mid-drive; crashing the run for the recovery ladder … err="storage: begin immediate: context canceled"` (control.log:101; seq 94 running→crashed "stage dispatch failed"). Ladder forked `…intake.g4` 03:30:19 (seq 97); the re-drive re-paid the plan ceremony; the approval card served 03:40:20 (ask `:4`) — a ~12-minute, re-billed heal for a page reload the card itself invites ("You can leave; nothing is lost").
- **M1, exactly:** between the gate-open commit 03:23:19 and the answer 03:25:19 the served card did not paint; the page claimed "listening". The SSE server loop (`internal/api/sse.go`) is a 250 ms poll ticker + nudge over `run_events` — a committed event cannot be missed server-side while the goroutine lives and the connection stands, so the wedge is connection-death-unnoticed or client-side handling; both are why S15.12's rule exists ("a disconnected or catching-up surface says so — stale data never poses as live"). The DOM read the review quotes at ~03:26:30 saw the post-answer working panel — honest waiting on the THEN-live drive that 03:28's reload killed. M1 and M2 are one wound: the wedge forced the reload, and the reload was lethal.
- **M3, exactly:** the redriven plan carried "price (8, 9, 9, 10, 8, 11 respectively)" with currency unstated ANYWHERE — no open point, no out-loud assumption. Only the contest (ask `:4`: "Show the prices as euros with the euro sign") made it euros. The P47-1 rule HAD fired (state: `data_bearing: [{rule_id P47-1, cue "price"}]`) — but P47 obliges research nodes, not unit questions; nothing else owns the gap.
- **M11, exactly:** task 2's final state: `tier: high`, `floor_tier: none`, `family: content`. The HIGH is pipeline.go:403's classifier-abstain fail-closed posture — set before the requester answered the family card, never revisited after. The Stage-3 critique then emitted a REVISE blocker: "TIER MISMATCH … downgrade to 'low' (proposed_tier below)" (`artifacts/t-cccd5dc7d00a4f64/critique-plan-v1-r1.md:15`), and the planner "resolved" it BY PROSE: SPEC-v2.md:15 "…treated as a light task, not a high-stakes one" and :39 "[assumed default — resolved from the critique that the earlier high-stakes handling didn't fit…]". The chip served `Card.Tier` = high (honest to state); the spec prose served low (model-authored); both verbs demanded the PIN. One card, two truths — and the model got to author one of them.
- **M8, exactly:** `internal/metering/receipt.go:251` serves `Reason = "no price table loaded at v0 (UNPRICED, never a silent $0 — S10.1)"`; `:234` serves `MeasuredStageSeam = "aggregate switches to the measured-median stage per Spec/benchmark-preregistration-v1.md §13 once the benchmark machinery (S14, B5) has enough pairs; threshold registered there, not restated"`; `:22` `DirectUseFormulaRef = "Spec/benchmark-preregistration-v1.md §13"`. The drawer renders all three verbatim. None has a GF13 keep-ledger row (they slipped the census via the deferred bare-token class, ledger "Deferred" item 3).
- **M10, exactly:** `internal/api/reads.go:1003` serves ONE absence line for every receipt-less run: "no receipt yet — one is written when the work finishes" — false on a terminal run. The FE (`web/src/TaskDetail.tsx:1259`, also `:1168`) papers over it with its own hardcoded guess, whose demo-seed clause misattributes on a fresh world. The wire lies, so the FE invents; fix the wire.

## 1. Requirements

### R1 — The drive's lifetime is detached from the viewer's connection (M2)

**S-refs:** S02.3 (parked/resume semantics; "every transition is a run_events append"), S02.5 (the ladder is the fallback for a DEAD driver, not the routine cure), S06.1 ("Gates wait… Answering a card resumes the pipeline in place (4.3)"), S15.6 ("Answering resumes the paused work in place"), S15.2 ("the REST response confirms, the event feed is the truth surfaces render from"). Nothing in ratified spec text ties a drive to an HTTP request's lifetime.

1. Once an answer/advance beat's **resume commit** has taken the run to `running`, the drive MUST run to its own end — next gate (park), split, or terminal — regardless of the caller's context being cancelled. The caller's death may cost the RESPONSE, never the WORK. The detach lives **inside each driving seam** (the §56 in-seam doctrine: no call site can forget): `intake.Pipeline.Answer` and `intake.Pipeline.Advance` (the post-commit drive runs on `context.WithoutCancel` — values kept, cancellation dropped), `stage.Skeleton.afterIntake` (the answer continuation), and the stage answer beats that drive inline — `AnswerVerifyAsk`'s resumed drain and `AnswerLadderAsk`'s retry re-entry (`internal/stage/answer.go`, `internal/verify` untouched unless its drain re-reads the caller ctx).
2. **Pre-commit behavior is unchanged:** a caller that dies before the resume commit keeps today's refusal mapping (`mapDriveErr` pre-commit → 409 conflict; the card stands; re-reading is the remedy).
3. **The crash posture stays for genuine failures:** a drive that errors for a real cause (engine, storage, seam) still takes `mapDriveErr`'s corpse + `advance_crashed_recovering`; the lease keeper still beats under the drive (§54); the human cancel verbs (§39) and the NO-AUTO-KILL wall are untouched. What changes is only that viewer-connection death stops being a cause of drive death.
4. **The synchronous response contract is unchanged for a live caller:** the handler still waits and returns the fresh state. (No background-goroutine early return is required or wanted — the simplest detach that satisfies the invariant wins.)
5. **CONVENTIONS record:** this REVERSES §56's second bullet ("the drive itself stays attached, and abort-mid-drive is a CRASH"). That bullet was a packet-level "weighed and decided" disposition, not spec text, and both its premises are gone: the SPA now HAS the still-composing face (the waiting panel the review watched for minutes), and the operator-ratified review names detachment as the fix. The executor appends a dated CONVENTIONS section (next free number) recording the reversal with these grounds — §56's first bullet (endings outlive the request) and every other clause stand, and the ladder remains the fallback for real process death. Never edit §56 in place (append-mostly file).

### R2 — The live channel proven, and staleness owned (M1)

**S-refs:** S15.3 (snapshot-then-tail; re-snapshot on gap, never patch blind), S15.12 ("stale data never poses as live"), S14.3 via CONVENTIONS §30 (frame contract: `id:` = event_seq, topics, 250 ms poll + nudge, ⚙ `obs.sse_keepalive`).

1. **Wire-proof test (backend half):** an acceptance test proves that the gate-open commit of an intake beat (its `intake.state` + `run.state_changed` appends) reaches an already-connected `/events` subscriber (`?topics=inbox` and the unfiltered relay) within a bounded number of poll intervals. If this test FAILS, the wedge is server-side — fix it here. If it passes (expected: the loop is poll-based), it lands as the standing pin that the backend delivered and M1's residue is client-side.
2. **FE seam (NOTED, not built here — the FRONTEND.md lane owns `web/src/*`):** (a) the page must detect a dead/stale EventSource and say so instead of "listening" (S15.12), re-snapshotting on reconnect (S15.3); (b) the dead "listening · N s" readout (finding L1) is theirs; (c) with R1 landed, a reload becomes a harmless self-heal — the honest remedy the FE may even offer.
3. No new transport, no `/events` contract change, no new ⚙.

### R3 — The canonical currency case cannot pass silently (M3)

**S-refs:** S06.5 (ask-don't-assume: a CONSEQUENTIAL ambiguity — one that would change an AC — raises exactly one question; non-consequential become logged assumptions), S06.6 ("an explicit assumptions list"; "Unresolved points carry NEEDS-CLARIFICATION markers; an artifact with open markers cannot reach approval"), r5 §C rules C1 (gap-finding is the point), C5 (multiple choice wherever a finite set exists — "currency is the canonical case"), C6 (open points at interview standard — "currency becomes the worked example"). Backend/planner content, per the packet cut — the card surface is already at the bar.

1. **The deterministic backstop (the half that guarantees):** after every pair emission (draft and revise), platform code checks: IF the pair's requester-facing content (ACs, steps, deliverable/outcome text) carries bare numeric quantities in money context (the money-context cue set reuses the P47-1 lexicon already in `internal/intake/triggers.go` — "price/fee/cost/…"), AND no currency token (symbol €/$/£/¥, ISO code, or currency word — euro/dollar/pound/franc/eur/usd/…) appears anywhere in the request, the resolutions/answers, the pair's assumptions, its clarifications, or the registry slice, THEN the platform appends a platform-authored NEEDS-CLARIFICATION entry ("Which currency are these prices in?" in plain words) to the pair's clarifications. Existing machinery then does the rest: the marker blocks approval (S06.6) and serves as an open point at interview standard.
   - The served currency question carries a **finite option set** (a small platform-authored currency list; free text always available; no invented recommendation — the platform has no fact to recommend from). **Skip converts to the honest out-loud assumption**: the prices will be shown exactly as supplied, as bare numbers with no currency shown — listed on the card like every assumption.
   - **Settled facts stay settled** (§60/GF12): the backstop NEVER fires when any currency token already appears in the scanned set — an answered currency question, a standing currency assumption, a "€8" in the request each suppress it, permanently. It fires at most once per task.
   - **Placement:** this is a pair-acceptance validator beside `Plan.Validate`/the marker machinery (the §60 "semantic guards belong to the ratified validators" family), NOT a fifth spine check — S06.7's spine list stays closed at (a)–(d) (the §59-R10 precedent). **Recorded reading, flagged for the coordinator's spot-check** (§8 below).
2. **The planner teaching (the half that catches it early):** `pairSchema` (`internal/stage/engines.go`) gains the gap-finding rule in the style of its existing rules: a quantity that enters the deliverable with its unit unstated anywhere — currency on prices is the canonical case — is a consequential ambiguity: raise it as a NEEDS-CLARIFICATION question or carry it as an out-loud assumption naming the assumed unit; never emit the bare numbers with the unit silently unstated. Plain-words rules (GF13) unchanged.
3. No taxonomy/question-set edits (that would trip §57's governance ceremony — out of scope; the backstop is not a must-know slot).

### R4 — One card, one stakes truth (M11)

**S-refs:** S06.2 (classifier proposes tier; "Classifier failure fails closed: the task is TREATED as high-stakes"), S06.4 ("automatic adjustments are monotone upward…; lowering happens only by explicit requester action and never below a floor"), S06.8 (verdicts: TIER-UP raises; no downward verdict exists), S06.9 (card content contract). Landed surface: `State.Tier/FloorTier` (state.go:137), fail-closed at pipeline.go:403, classifier assign at :381, trivial-clamp at :396, TIER-UP at :1409–1418, `LowerTier` at answer.go:1018 — **exists, zero callers, unreachable from any wire**; `Card.Tier` served (cards.go:216); the family answer (answer.go:350) re-keys the taxonomy and never revisits the tier.

1. **The abstain posture settles when its cause is removed (the task-2 chip):** when the tier is the classifier-abstain fail-closed HIGH (provenance-tracked, see 4 below) and the requester answers the family card, the pipeline re-runs the classification ONCE with the settled family as input (additive member on `intake.TriageInput`; the local duty schema gains it — `internal/local` owns the shape, §26 wall). A valid proposal ASSIGNS the tier exactly as first classification does (pipeline.go:381 semantics — this is Stage 0 completing, not an S06.4 adjustment): floors still clamp upward, a trivial proposal outside the band lands at low (:396 mirror), the band is never re-entered, and the settle is recorded as a platform-authored ledger decision in plain words. A classifier that still abstains leaves HIGH standing. One shot; never re-run on later answers. **Recorded reading, flagged for the coordinator's spot-check** (§8).
2. **A downward tier opinion from the critique is a REQUESTER decision, never a REVISE finding:** the critique schema (engines.go ~:665) is taught that "this tier is too high" rides `proposed_tier` (on any verdict kind), never a findings entry; the pipeline, on a `ProposedTier` ranking BELOW the current tier, records it on the verdict (pipeline.go:1345 already does) and serves it on the approval card's stakes block (3 below) — it never enters the REVISE package, is never handed to the planner to "resolve", and never moves the tier by itself. (The planner cannot author the band; task 2's SPEC-v2 prose is the defect class this kills.)
3. **The card serves the stakes truth whole:** interview/clarification/approval cards gain a platform-authored `stakes` block beside `Card.Tier`: `{tier, origin, plain_reason, proposed_lower?, can_lower}` — origin ∈ {`fail-closed` (the platform could not read the request; unknown is handled as high until told otherwise), `classifier`, `requester`, `floor` (with the floor classes)}, `proposed_lower` the critique's pending downward proposal if any, `can_lower` true when a requester lowering is legal (tier above the floor, not into trivial, phase pre-approval). Plain words throughout; no S-refs in served text (GF13). FE chip rendering from this block is a **seam for the FRONTEND.md lane** — including retiring any copy that promises settling the backend does not deliver.
4. **Tier provenance is state:** `State.TierSource` (additive, the `FamilySource` pattern) records who set the standing tier; it gates 1 (only `fail-closed` re-derives) and feeds 3.
5. **The lowering door opens:** the approval card accepts the action `lower_stakes` with a target tier (`Answer` gains the field, additive), routed to the existing `Pipeline.LowerTier` — requester-only, floor-clamped, never into trivial, phase-guarded, ledger-recorded (all already in the verb). On success the card re-serves at the new tier **and the ask snapshot is refreshed** so `askCardMeta` (surface.go) reads the new tier — the PIN demand must follow the settled tier, not the stale snapshot. `TierSource` becomes `requester`. The door is the S06.4-sanctioned explicit requester action; it needs no PIN (it releases nothing outward; the same reading the verify decision cards take, §16).

### R5 — The receipts drawer speaks plain words (M8)

**S-refs:** S10.10 (the done-directly figure; "cites that form and does not restate its numbers"), S15.5 (the RATIFIED labels: "direct-use estimate (heuristic)" / "measured (benchmark n=…)" — G2 D2.8), BENCH-REG §13 (registered text; numbers change only via §17), GF13 (requester surfaces plain-words; the keep ledger is live and a keep needs a row).

1. `DirectUseEstimate.Label` stays byte-exact `"direct-use estimate (heuristic)"` — ratified, never touched.
2. `DirectUseEstimate.Reason` (receipt.go:251) and `.MeasuredStageSeam` (:234) are rewritten as plain sentences for a non-IT requester: what the number is, why it is unpriced (no price table is loaded yet, and the platform never pretends a price it does not have — never a silent $0), and that the figure switches to a measured one once the platform has gathered enough real comparison runs. NO file paths, section ids, spec tokens, or packet ids in served prose; NO number from the registration restated (say "enough", never the threshold). Citations stay welcome in code comments (house style).
3. `DirectUseEstimate.FormulaRef` stays as machine provenance (the registered-formula pointer, `formula_ref` in JSON) — **the executor adds a keep row to `P3/briefs/P3-GF13-keep-ledger.md`** for `DirectUseFormulaRef` (site receipt.go:22; reason: registered-formula provenance, a machine member never meant as prose). The FE must stop rendering it inside the drawer's prose line — **FRONTEND.md-lane seam, noted**.
4. Sibling strings in `internal/metering/pricestore.go` (:313, :318 …) are operator settings-surface refusals (GF13 §D class) — OUT of this packet's scope; the drawer's own fields only.

### R6 — The missing-receipt line speaks to the run's own ending (M10)

**S-refs:** S10.1 (receipts materialize at the run's terminal transition), CONVENTIONS §55 (`crashed` is terminal-but-supersedable, settles queue-only, NO receipt; each successor ends with its own receipt — D4), §38 (absences are rendered with their reason), S15.12 (honesty).

1. `projector.taskRuns` (reads.go:981–1007) serves a **state-aware** `receipt_absent` reason:
   - non-terminal run → the current line (it is true there);
   - `crashed` with a successor (`runs.parent_run_id` lineage) → plain words naming the break and the continuation, INCLUDING the successor run id (e.g. "this leg broke partway and the platform continued the work in a fresh run (<successor id>) — what this leg consumed is in the ledger, and the finishing run carries the receipt");
   - `crashed` without a successor (ladder pending/tombstoned), `finalized`, `died-at-gate`, `tombstoned` → an honest per-state sentence that never promises a receipt that will not come;
   - the demo-seed clause NEVER appears on the wire (the wire cannot know it; the FE guess dies with the served truth).
2. **FE seam (NOTED):** `web/src/TaskDetail.tsx:1259` renders the served reason instead of its hardcoded override; the `:1168` empty-state guess is the same family — both are the FRONTEND.md lane's, listed for it.

## 2. Seams to respect (and the stubs for phases not here)

- **`web/src/*` is another lane's, live, RIGHT NOW** — the working tree carries its uncommitted work (`web/src/Intake.tsx`, `api.ts`, `door.test.tsx`, `index.css`) plus operator hunks in `P3/gates/B6-clickthrough.sh`. NEVER edit, stage, or revert anything under `web/` or any dirty/untracked operator path (`Presentation/`, `Research/Presentation/`, `Sinet-Logo.jpeg`, `tools/*`, `P3/gates/rw10-heal.sh`). Every FE half above is a NOTED seam: R2.2, R4.3 chip, R5.3 drawer prose, R6.2.
- `internal/history` frozen; `internal/watchdog` untouched (§31 import wall; §56's silence rule unchanged); the NO-AUTO-KILL wall (`TestCancelIsReachableFromTheHTTPVerbsOnly`) — R1 adds no new cancel entry point; widening a walled surface means widening its wall in the same commit (§58 r2 R-1) if any wall is touched (none should be).
- Question-set/taxonomy seeds untouched (§57 governance: digests + own-Ensure ceremony — out of scope).
- `internal/local` owns duty schemas (§26 wall): R4.1's TriageInput family member crosses that seam additively; stage supplies the value, local owns the shape.
- Adopted code untouched; no new dependency; `components.lock` unchanged.
- The `/events` frame contract (§30) unchanged; `internal/redact` edge files unchanged.

## 3. ⚙ settings

- **Consumed (existing, by dotted key):** nothing new — the touched paths already read `obs.sse_keepalive` (SSE), `freshness.max_age`, the S06 intake keys. R1 deliberately invents NO drive-timeout/detach knob (S18 ratifies none; the ladder + watchdog own runaway containment).
- **No new ⚙ key.** The R3 lexicons (money-context cues, currency tokens) and the R4 currency/stakes vocabularies are structural code data under the standing settings-tab flag (§7/§9/§11 precedent) — a future tunable goes through the registry via S00.9 + S18 sweep, never a constant promotion here.
- Settings index byte-unchanged (118/33 tally test must stay green).

## 4. Files expected to change

- `internal/intake/answer.go` (family-answer re-derive hook; `lower_stakes` action; Answer field), `internal/intake/pipeline.go` (drive detach in Answer/Advance; critique downward routing; currency backstop call; TierSource), `internal/intake/state.go` (TierSource), `internal/intake/cards.go` (stakes block; currency question options), `internal/intake/intake.go` (TriageInput family member; Verdict handling if needed), a new `internal/intake/currency.go` (or beside the validators) for the backstop.
- `internal/stage/engines.go` (pairSchema gap-finding rule; critique-schema tier teaching), `internal/stage/surface.go`/`internal/stage/answer.go` (afterIntake + verify/ladder answer-beat detach; askCardMeta snapshot refresh interplay), `internal/stage/skeleton.go` only if the drive boundary lives there.
- `internal/metering/receipt.go` (R5 prose).
- `internal/api/reads.go` (R6 taskRuns), `internal/api/sse.go` only if R2.1 finds a server defect.
- `P3/CONVENTIONS.md` (append the GF14 section incl. the §56-reversal record), `P3/briefs/P3-GF13-keep-ledger.md` (the FormulaRef keep row).
- Tests: the committed-red files below plus whatever the executor adds.

## 5. Adopted components touched

None. (No engine, no npm, no new Go module.)

## 6. Acceptance checklist (each item concretely checkable)

1. A page reload (context cancellation) after an answer's resume commit no longer crashes the run: the drive completes to its next gate; no `run.state_changed → crashed` with a context-canceled cause; the answered ask stays answered; the next card is served and readable after a re-snapshot. [R1; red test T1]
2. The same invariant holds for `Advance`, `afterIntake`, and the verify/ladder answer beats (executor-materialized tests). [R1]
3. A dated CONVENTIONS section records the §56 reversal with its grounds; §56 itself is byte-unchanged. [R1.5]
4. Gate-open events provably reach a connected `/events` subscriber (test committed; green = wire proven, red = wire fixed). [R2.1]
5. The FE seam notes (R2.2, R4.3, R5.3, R6.2) appear in the packet report for the FRONTEND.md lane; no `web/` file changed. [R2/R4/R5/R6]
6. The canonical currency case cannot reach the approval card silently: bare-number prices with no currency token anywhere yield an open currency point (finite options, free text, skip→out-loud bare-numbers assumption) or a standing currency assumption; a currency token anywhere suppresses the backstop; it fires at most once. [R3; red test T2]
7. `pairSchema` carries the unit-completeness gap-finding rule; the GF13 plain-words rule is byte-unchanged. [R3.2]
8. A classifier-abstain HIGH settles on the family answer via one re-classification (floors clamp, band never re-entered, platform ledger decision recorded); a still-abstaining classifier leaves HIGH. [R4.1; red test T3]
9. A critique `proposed_tier` below the current tier never enters the REVISE package and is served on the approval card's stakes block. [R4.2; red test T5]
10. Cards serve the `stakes` block (tier/origin/plain_reason/proposed_lower/can_lower); `Card.Tier` unchanged for old readers. [R4.3; T5 asserts]
11. `lower_stakes` on the approval card lowers via `LowerTier`'s own rules, refreshes the ask snapshot (PIN demand follows the new tier), records the human decision. [R4.5; red test T4]
12. Receipt drawer prose: Label byte-exact; Reason + MeasuredStageSeam plain (no `Spec/`, no `§`, no S/B/P47 tokens, no registered numbers); FormulaRef unchanged with its GF13 ledger row added. [R5; red test T6]
13. `receipt_absent` is state-aware; a terminal receipt-less run never gets a promised-receipt line; crashed-with-successor names the successor; "demo" never on the wire. [R6; red test T7]
14. Full battery green at landing: `go build ./...`, `go test -p 1 ./...` (serial — operator directive), lockgate; no new skips; settings tally 118/33 intact; no paid calls (every test hermetic/tier-F).

## 7. Acceptance tests — committed RED at grounding (`gf14red` files)

Committed by the grounding agent, failing for the right reason (Amendment-A carve-out, CONVENTIONS §3: packet paths only, build stays green, window declared in the commit message, closed by the implementation commit). **Executor: materialize any spec below not already committed; never modify the committed ones** (deviations are declared, not edited in).

- **T1 `TestGF14AnswerDriveOutlivesItsCaller`** (`internal/intake/gf14red_test.go`) — the `fix` harness; standard walk to the interview; the fake planner's Draft cancels the caller's context (the reload, staged deterministically mid-drive, after the resume commit); `Pipeline.Answer` must SUCCEED, the state must reach the approval card, the run must be parked, the draft must have run exactly once. Fails today: `storage: begin immediate: context canceled` kills the beat. *Property intent (executor): the invariant is per-cancellation-point — extend table-driven over later injection points (post-critique, post-spine) the same way.*
- **T2 `TestGF14CanonicalCurrencyCaseIsCaught`** (same file) — request "A one-page price list… I will give you the exact prices" (P47-1 fires); fake planner emits the pair with AC text "price (8, 9, 9, 10, 8, 11 respectively)", no currency anywhere; walk to the gate. PASS = a clarification card whose currency question carries ≥2 options, OR an assumptions entry resolving currency out loud. Fails today: the approval card opens with the bare numbers unremarked. Companion negative (executor): with "€8" in the request text the backstop must NOT fire — the suppression property over every scanned location.
- **T3 `TestGF14AbstainedStakesSettleWhenTheFamilyAnswerArrives`** (same file) — classifier errs (abstain) → family card, tier HIGH (control asserted); classifier then healthy proposing content/low; answer the family; the returned state's tier must be LOW. Fails today: stays HIGH forever.
- **T4 `TestGF14LowerStakesIsARequesterDoorOnTheApprovalCard`** (same file) — classified HIGH walk to approval; `Answer` with raw `{"action":"lower_stakes","tier":"standard"}` must succeed, tier `standard`, approval card still open. Fails today: `ErrBadAnswer`.
- **T5 `TestGF14CritiqueDownwardProposalReachesTheRequesterNotTheDrain`** (same file) — critic verdict `tier_up` with `ProposedTier: low` at a standard-tier walk; at the approval card: tier unchanged, zero revise calls, and the ask row's stored snapshot carries the stakes block naming the pending proposal (`"proposed_lower":"low"`). Fails today: no stakes block exists.
- **T6 `TestGF14ReceiptProseCarriesNoCitations`** (`internal/metering/gf14red_test.go`) — `directUse` on an unpriced consumption: Label byte-exact; Reason non-empty; Reason and MeasuredStageSeam free of `Spec/`, `§`, and S/B/P47-token shapes (the GF13 widened-scan regexes); FormulaRef byte-exact (the pinned keep). Fails today on both prose fields.
- **T7 `TestGF14ReceiptAbsenceSpeaksToTheRunsOwnEnding`** (`internal/api/gf14red_test.go`) — seed a task with a crashed run + its `parent_run_id` successor and a running run; GET the task detail; the crashed run's `receipt_absent` must not promise a future receipt, must name the successor run id, must not say "demo"; the running run keeps the honest future line. Fails today: one line for all.
- **Executor-materialized (specs, not committed red):** the R2.1 SSE wire-proof (subscribe `/events?topics=inbox`, drive a gate-open through the api backend, assert the `intake.state`/`run.state_changed` frames arrive with `id:` = their event_seq within a bounded read window — may legitimately land green, and green is a result: the wire is proven); the R1 stage-level detach tests (afterIntake, verify `revise_with_guidance` drain, ladder retry) on the `internal/stage` harness; the T2 suppression property; a `lower_stakes` PIN-interplay test at the stage surface (after lowering below high, the approval answer must not demand the PIN — askCardMeta reads the refreshed snapshot).

## 8. Recorded readings and the one reversal — coordinator spot-check gates BEFORE the executor launches

1. **§56 reversal (R1):** operator-ratified direction vs a packet-level CONVENTIONS ruling whose premises no longer hold. Not a spec conflict — no ratified S-text binds a drive to a request. Recorded, appended, never silently rewritten.
2. **R3 backstop placement:** a deterministic currency check as a pair-acceptance validator implements S06.6's marker/assumption contract for the operator-named canonical case (r5 §C1/C5/C6) WITHOUT widening the closed S06.7 spine list. If the coordinator reads this as spine-widening instead, it is S00.9 amendment territory and the packet ships R3.2 (prompt teaching) alone — say so, don't improvise.
3. **R4.1 re-derive:** S06.2's fail-closed clause covers classifier FAILURE; completing the classification once the requester removes the failure's cause is Stage 0 finishing, not an S06.4 automatic adjustment (which governs adjustments to a CLASSIFIED tier). The direct-assign precedent is pipeline.go:381; the already-served promise is W10's "it settles as the questions are answered/chosen". If the coordinator reads S06.4 strictly instead, R4.1 is cut and the fail-closed HIGH settles only through R4.5's requester door — the card then says WHY it is high (R4.3 origin `fail-closed`) so one truth still holds.

## 9. CONVENTIONS constraints binding this packet

§2 (⚙ discipline; doc-comment style — no research narration), §3 (stdlib testing; `t.TempDir()`; Amendment-A red-window mechanics), §5 (commit format `P3-GF14: …`; explicit pathspecs; never touch `Docs/ Spec/ Research/ P3/STATE.md`; packet sessions never push), §6 (events: one WriteTx, validate-before-persist, generation fencing), §8 (FSM edges only via run.Store; ladder discipline), §14 (intake conventions; the ledger never nests in an intake WriteTx), §16 (answer-beat choreography; ask close + transition one tx), §54 (lease renewal rides the driver), §55 (fork lineage; StripForkSuffix), §56 (everything EXCEPT the reversed bullet), §57 (taxonomy governance — stay out), §59 (requester prose is for the person), §60 (completion doctrine untouched; validators own semantic guards), §70 + GF13 ledger (citation discipline, keep-rows). Scope guardrail: no features, refactors, or abstractions beyond this brief; simplest thing that works; validate at system boundaries only.
