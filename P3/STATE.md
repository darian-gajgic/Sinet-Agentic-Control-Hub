# P3 implementation — live state

> Any session advancing the build: **read `P3/HANDOFF.md` first** (≤8K orientation — where the build is, the next act, open operator items, host state), then this file, then follow `.claude/skills/p3-implementation/SKILL.md` (amendment F: budgeted sittings, gate files, bounded reads). **This file holds CURRENT state only** — standing directives, the phase board, the OPEN packet queues, the operator hands-on table — and it is the single source of truth for build coordination; it outranks `HANDOFF.md` (a dated snapshot; if the two disagree, this file wins and the handoff gets corrected). **History lives in `P3/STATE-HISTORY.md`** — closed packet queues, the dated log (append one entry ≤600 chars per step there), and retired handoff snapshots — grep it, never read it whole. Update this file before and after every step. The contract is `Spec/core-architecture-v1.md` (v1, frozen, tag `spec-v1`; drafts canonical); the campaign record in `Research/` is a closed archive.

**Phase status:** B0 CLOSED 2026-07-19 · B1 CLOSED 2026-07-20 · B2 CLOSED 2026-07-20 · B3 CLOSED 2026-07-22 · B4 CLOSED 2026-07-23 · B5 CLOSED 2026-07-28 · B6 CLOSED 2026-08-04 (gate records `P3/gates/B<N>-report.md`). Post-B6 campaigns all LANDED: the UI rework (RW-1…RW-19 + builder rounds, exit walks 2026-08-18), the planning rework (GF3…GF15, exit cold walk 8/8 2026-08-31), the LN lane expansion (LN-1…LN-10a, 2026-08-27). OPEN: the harness queue (H-0 done 2026-09-22 → H-1 → H-2), the task-quality + deliverable-surface queue (TQ/SIT, executing), and the operator's two waiting streams (the LN lane sitting + its 15-item gate batch; the B6 gate stream F1a/F1b + W1–W4). Narrative of every closed phase and campaign: `P3/STATE-HISTORY.md`.

## Standing operator directives (cross-phase)

- **2026-09-22 — Continuous-run harness (chat, authoritative: "Okay, good … in the next session, we do this change first").** The build runs as a chain of budgeted FRESH coordinator sittings driven by an EXTERNAL loop, never by one long session; design = `P3/design/continuous-run-harness-proposal-2026-09-22.md` (ratified with its recommended defaults D-a…D-f: headless `-p` sittings; desktop `notify-send`; tmux first, systemd+linger later by approval; budget 3 landings / 4 h; Opus 5 sittings while the Fable limit is active; gates answerable by editing the gate file). Harness packets H-0..H-2 are the FIRST act of the next session, before any packet work. Every coordinator sitting obeys R1–R7: sitting budget; gate batches written to `P3/gates/` BEFORE presentation; brief spot-checks delegated to a fresh agent; launch prompts = packet deltas over on-disk stage templates; agent chat reports capped (full report as a file); landing text drafted by the evaluator/finalizer; HANDOFF rewritten, never appended.
- **2026-09-16 — Model-routing directive (TQ-F9, chat, authoritative): retire `claude-sonnet-5` and `claude-opus-4-8` from the task pipeline; execution → Kimi K3 (lanes `kimi`/`kimi-cli`), planning + judge → Opus 5; until a Kimi key is placed, execution resolves to Opus 5 on `anthropic` with the S07.5 self-family-judging flag on every receipt.** The specifics (first-choice kimi lane, interim posture, the P-T06-5 golden-set re-run on the new judge, rubric v3) are ratified at the rework-sitting gate item B7 (`P3/gates/rework-sitting-gate.md`); packet P3-TQ-5 applies them. Raises the LN key-ceremony sitting to top priority — it is the only thing that ends the interim.
- **2026-07-22 — Four-stage packet pipeline (quality-first; ratified in chat; supersedes single-agent packet execution from P3-B4-2 onward).** Every packet runs grounding → executor → evaluation → finalizing per `.claude/skills/p3-implementation/SKILL.md` §Work packets: fresh-context stages, all coordinator-launched; `P3/briefs/` holds the handoff artifact which doubles as the evaluation rubric; executor is `opus` ALWAYS (judge ≥ executor AND judge ≠ executor per the D3-ratified research; classifier immunity mid-run); grounding/evaluation Fable — all-`opus` on S10/S11-dense packets and lossless `opus` fallback on any classifier trip; findings drain [F1..Fn] round-capped at 2. Research basis: Anthropic's three-agent harness (planning/generation/evaluation + structured handoff artifacts; InfoQ 2026-04), official "Prompting Claude Fable 5" (fresh-context verifiers outperform self-critique; benign-cyber false positives → Opus routing), D3 record `P3/gates/B3-report.md` §7. Autonomy contract: the operator starts ONE session ("continue implementation"); it stops only at phase gates, spec conflicts, and operator hands-on items.
- **2026-07-20 — Codor harvest: adopt the copy rows, coherence-verified.** Operator directed (chat): adopt the copy candidates from `Research/18-codor-harvest-addendum.md`, but only after verifying coherence with the architecture. Verification ran same day — **verdicts + binding conditions in Research/18 §7** (the authoritative record). Effect on packet cuts: **B3 cut MUST include C1-recitation** (per-turn delivery of ledger `state`/`next_actions` via a PostToolUse `sinet engine-hook` trigger over the existing S03.4 ctl-dir airlock — NO new socket/credential; first step = the pre-registered ~$0.02 PostToolUse `additionalContext` probe, B1-4 method; consumes ⚙ `context.recitation_interval_turns`; all seven §7-C1 conditions bind) and **C3 conformance rows**; **B5/B6 cuts include C2** (serve-side redaction of observability projections + redact-before-match search; constant-on, never applied to deliverable content); **C5 discretionary** (advisory copy-aside context peek). Explicitly NOT adopted: C1-ext (cancel-notice/operator-note — S00.9 amendment territory), C4 (parked at the S04 ≥50-item fan-out row). Nothing from codor is ever a dependency.
- **2026-07-20 — "Settings tab must show AND allow changing everything."** Operator founding requirement (restated at the B1 gate): every operator-relevant number is visible and editable in the settings surface — including numbers S18 deliberately left as **code constants**: auth numbers (TTL/PIN attempts/lockout — B0 gate D2), scheduler structural constants (lane-concurrency default 1 / poll 500 ms / aging biases — B1 gate §4.4), price-table bits, and any others surfaced. **Deferred by the operator ("fix later") — NOT a B2 blocker.** Resolution path: an S00.9 amendment adding the non-⚙ numbers to S18 as **clamped** ⚙ settings (clamps keep invariants — e.g. aging biases can't break the S10.7 no-starvation guarantee). Natural home: around B6 settings UI (S15.9) or a dedicated pass. **This directive makes the "keep as constants" gate decisions (B0 D2, B1 §4.4) INTERIM, not final.** Also in auto-memory `settings-tab-see-change-everything`.

**2026-07-20 evening — D2.2 EXECUTED (this = the parallel cert session; full record in `P3/gates/B0-cert-steps.md` §Execution record):** front chain LIVE end-to-end — `tailscale serve` (tailnet-only, stale Nexus-era 8777 entry reset) → Caddy 2.6.2-14 on 127.0.0.1:8481 (organ-unit lock entry added; runbook Caddyfile had a wildcard-bind defect, fixed in place: `http://:8481` + `bind 127.0.0.1`) → the production unit on 8482. Production-posture verification exact per runbook (health ✓, `authenticated:false`+hint ✓, /events cookie-less → 401 ✓). KEY FINDING: the cert was **already issued 2026-07-19 21:27 UTC** (leftover serve config × machine rename) — CT publication predated the gate; operator item 7 SIGNED 2026-07-20, covering it retroactively. LATER SAME EVENING: steps 11–12 DONE — production operator `darian` bootstrapped (create landed with the placeholder PIN; caught by coordinator probe, rotated via `POST /api/auth/pin` step-up, other sessions invalidated), SSE-with-session streams the full auth audit trail live. **Runbook CLOSED**; step-13 crt.sh observation DONE 2026-07-20 next-morning (1 cert, 2 CT entries, CN/SAN = FQDN only, notBefore 2026-07-19 21:27:21 UTC — exact match; record in B0-cert-steps.md) — **runbook fully closed**.

**2026-07-20 evening — B2 gate D2 ANSWERED + items 1/3 EXECUTED (host now touched, by decision):** units installed (`sinet-control`+`sinet-broker` active on 127.0.0.1:8482, `/var/lib/sinet`, journald cap; portpool file-only — stub mode; broker StateDirectory defect found+fixed = `P3-B2-fix4`), srt 0.0.66 installed+discovered (ADOPTED path; version-print quirk in lock notes), cert running in the operator's parallel session (backend 8482). D2.4+7 → dedicated hardening session (**must include the AppArmor userns grant for the service context — probe available=false as service, new finding**); D2.5 → with escrow pre-B4; D2.6 → after suspend probe. Production users table empty — operator bootstrap pending. Gate remainder: D3 seeds, D4 readings, D6 probes, and the D1-record items (3 demo readings + the P3-B2-5 close-now-vs-hold pick).

## Phase board (S19.5)

| Phase | Scope (owning sections) | Status |
|---|---|---|
| B0 | Spine: sinet-control process, platform.db, event log, settings registry, auth stack, five seams [S01]; run FSM, checkpoint/effect journal, recovery ladder [S02]; adoption manifest + CI lock-gate [S16] | **CLOSED 2026-07-19** |
| B1 | Execution substrate: one adapter + one lane [S03]; metering ledger + limit taxonomy + scheduler skeleton [S10]; per-run sandbox C0–C2 + credential broker [S11]; B1 spike battery [S19.5] | **CLOSED 2026-07-20** |
| B2 | Pipeline: intake→spec→plan [S06]; context ledger [S05]; verification [S07] — closes the walking skeleton (gate = live demo) | **CLOSED 2026-07-20** |
| B3 | Workforce & memory: worker registry + composer battery + routing [S08]; memory/knowledge + write gate [S09]; + S04 spawn/helper slice (reading below) + codor C1/C3 (standing directive) | **CLOSED 2026-07-22** |
| B4 | Deliverables & local tier: deliverable/review/git/backup [S13]; local-model stack + GPU broker + duty aliases [S12] | **CLOSED 2026-07-23** |
| B5 | Observability & evals: watchdogs, conformance registry, benchmark machinery, queryable history [S14] — incl. the [schema] STUDY watch row (G4 follow-up) + the dead-man-canary V2-leg upgrade (B4-7 OQ5 carry: unblocked by the local tier + entailment checker; scheduling/alerts are S14's) | **CLOSED 2026-07-28** — gate record `P3/gates/B5-report.md` §8 FINAL: D1 three adoptions ratified · D2 EXECUTED (promptfoo 0.121.19 + changedetection.io 0.55.8 installed at pins, script green 11/0; `sinet-watchlist.service` deferred to hardening) · D3 all four canaries DISARMED at v0 (behavioral itemization tabled as the arming precondition) · D4 floor 0.84 ratified, paid sweep held for B6 · D5 no family-12 amendment, driver = named scope · D6 **35 readings ratified en bloc** (binding precedent) |
| B6 | Frontend: React SPA consuming every API [S15; FC-v1] | **CLOSED 2026-08-04 — gate record `P3/gates/B6-report.md` §9 FINAL: D1–D8 all answered (D1/D4/D7 ratified; D2 authorized-for-bring-up; D3 conditional — declare-the-how FIRST, Nexus + GitHub as sources; D5 as recommended, taste delegated; D6 coordinator-executed via `sudo -v` after the UI batch; D8 ratified 2026-08-04 with the brief pulled forward by D3's condition). Directed post-gate batch: design declaration → fourteen controls → D8 pass → D6 upgrade → bring-up (S19.6).** |

## Harness packet queue (cut 2026-09-22, OPERATOR-ORDERED: "in the next session, we do this change first"; design = `P3/design/continuous-run-harness-proposal-2026-09-22.md`)

Process/tooling packets, not spec-bound product code: light path (coordinator + at most one opus executor); H-1's classifier needs tests. Order H-0 → H-1 → H-2. The operator's hands-on items are in H-2 only.

| Packet | Scope | Acceptance | Status |
|---|---|---|---|
| H-0 | Runbook + contracts: SKILL.md amendment F = R1–R7 + the sitting contract (design §3.1); `P3/run/sitting-prompt.md` (fixed per-sitting contract: budget 3 landings / 4 h, `status.json` rule, "never ask — write a gate file", wind-down sequence); on-disk stage prompt templates `P3/prompts/{grounding,execute,evaluate,finalize,spotcheck}.md` so launch prompts carry only packet deltas; agent chat-report cap ≤1,200 chars (full report as a file in the worktree); gate-file contract (`P3/gates/<name>.md` committed BEFORE presentation, `## Answers`, `answered: yes` marker); FRONTEND.md checkpoints become gate files too; **HANDOFF.md rewritten to ONE current orientation ≤8K (R7), the old ⏭️ paragraphs folded into this log**. | A fresh session reading only HANDOFF + the templates can run a sitting; each assembled launch prompt ≤2K; HANDOFF ≤8K. | **done 2026-09-22** (this sitting: SKILL.md amendment F = R1–R8 incl. the new R8 bounded-reads rule; `P3/prompts/` ×5 + README; `P3/reports/`; `P3/run/sitting-prompt.md`; gate-file + `status.json` contracts; FRONTEND checkpoints as gate files; STATE split → `STATE-HISTORY.md`; HANDOFF 7.1K) |
| H-1 | `P3/run/sitting.sh` (one sitting: `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0 timeout --signal=INT --kill-after=15m 5h claude -p "continue implementation" --model claude-fable-5-1 --permission-mode auto --permission-prompts none --append-system-prompt-file P3/run/sitting-prompt.md --output-format stream-json --verbose > P3/run/log/…`), `P3/run/loop.sh` (flock; `P3/run/STOP`; orphan reap; classify from the JSON result + git state, NEVER the exit code; limit → sleep until the parsed reset or backoff 15 min→1 h; Fable limit → next sitting `--model claude-opus-5`; GATE/BLOCKED → `notify-send` + poll the gate marker every 10 min; DONE → exit; 3 crashes or 3 sittings without a new main commit → stop + notify), the `status.json` contract (`outcome` CONTINUE/GATE/BLOCKED/DONE, `landed`, `next`, `gate`, `ended`), classifier unit tests on canned results (limit text, crash, CONTINUE, GATE). `--max-turns` is NOT in 2.1.278's `--help`: the wall clock is the hard rail. `P3/run/log/` + `status.json` gitignored. | Tests green; `loop.sh --dry-run` prints the right decision for every canned result. | **done 2026-09-22** (`P3/run/{lib,sitting,loop,test-classify}.sh` + `fixtures/` + README; 25/25 classifier checks; smoke sitting on `claude-fable-5-1[1m]` OK; stubbed `loop.sh --once` verified CONTINUE / CRASH / GATE (+ desktop notification) / LIMIT→opus switch / STOP; `--max-turns 600` secondary rail; reap self-match hazard fixed) |
| H-2 | Bring-up: (a) supervised dry run — the operator starts `tmux new -s p3loop 'P3/run/loop.sh'` and watches ONE sitting (its first act = the TQ-8 evaluation relaunch); verify the idle ceiling, `status.json`, the log, the notification; (b) one unattended overnight run; morning review = landings per sitting, transcript size per sitting, limit handling; (c) then propose the `systemd --user` unit + `loginctl enable-linger sinep` (host change → proposed-then-approved). | One unattended night with ≥1 landing per sitting and no manual restart. | queued (hands-on) |

## Task-quality + deliverable-surface rework packet queue (cut 2026-09-17 from the sitting's findings SIT-F1..F4 + TQ-F1..F9; **gate answered 2026-09-17 — see `P3/gates/rework-sitting-gate.md` §Answers (A3/A4/A6/B9 still open, explanations owed); executing in Part D order**)

Triage record (root causes verified against code + spec 2026-09-17) lives in the gate file Part C; the recommended order in Part D (LN key ceremony first — TQ-F9 makes it the only thing that ends the self-family-judging interim).

| Packet | Title | Pipeline | Status |
|---|---|---|---|
| P3-TQ-5 | The model rerouting (TQ-F9): duty-map seats planning/judge → Opus 5, execution → K3 on the kimi lane first-choice + Opus 5 alternate; `rubric-software` v3 with the Opus 5 `JudgePin` + the P-T06-5 golden-set re-run (26 cases, $0 cash on the subscription lane, pre-registered stop line) + floors/length-bias re-measured; serialize-by-deny E3 leg re-run on the new executor seat; model ids live-verified | backend four-stage (+ measurement rider) | **done** (`b1279e0` grounding · `0594bec` reds · `7a21c73` commit 1 · riders 1+2 PASS · `d9dee2d` commit 2 · `c88c84c` drain r1 · eval PASS · re-check PASS · landing battery 48/0/0 · brief EXPIRED · CONVENTIONS §73 · landed 2026-09-17; ledger: S07.5 MAY-swap unimplemented, second run_events query per receipt read, covered-but-seatless lane unnamed; the interim self-family judging ends only when the operator places a Kimi key) |
| P3-TQ-1 | The artifact-snapshot race (TQ-F1): S02.4d's snapshot must not fail on the engine's transient `*.tmp.<pid>.<hash>` files — a healthy stage never crashes the run on the platform's own bookkeeping | backend four-stage, small | **executing in the worktree** (`cb5eff2` grounding on branch `worktree-agent-a1d594b76e5908bc5`: brief 7 reqs / 5 tests, 3 red + 2 green pins, PATH-shim git as the test seam, no production hook; spot-check PASS; fix = bounded retry of the identical `git add -A` on exit 128 only, `snapshotAddAttempts = 3` structural; executor DONE `93f7470` on the branch — 5/5 TQ1 green, package 54 PASS, mutation probe bound→1 re-reds all three, `workspace.go` 39+/4- + one new gate-test file; **evaluation PASS** (nothing above nit; 6 mutations + 5 composition probes; acceptance file byte-identical to grounding; diff confined as claimed) → **drain r1 LANDED `fadee73`** (F1 `%w`-chain pin, mutation-verified; F3 non-retried wording + no dangling separator) → **re-check PASS** (M6 reds exactly the new pin; acceptance file byte-identical; gate-file edits only strengthen) → **done** (merged `b0bc3bf`; brief EXPIRED; CONVENTIONS §72; coordinator landing battery 48 pkgs `-p 1` exit 0, 0 FAIL, 0 SKIP, lockgate 40, gofmt/vet clean; landed 2026-09-17) |
| P3-TQ-2 | Fork-from-last-checkpoint (TQ-F2): S02.5's DEAD → supersede rung resumes at the last good stage checkpoint with the worktree reset to that snapshot, never a whole-plan re-drive over a dirty tree; + the connective run-surface line for TQ-F8 | backend four-stage | **grounded `5841338`** (worktree branch `worktree-agent-a513684d27c5037af`: brief 11 reqs / 8 red tests incl. a 12-subtest (n,k,mode) property table; step-granular resume k = leading steps done WITH a step-close snapshot as evidence_ref; `RestoreSnapshot` restores FORWARD (ff-only branch, dead run's snapshots stay reachable), reset --hard rejected; fork seed keeps S-1..S-k done; resume arm in executeInstructions; narration via reason/detail on both runs; two additive TaskRunView fields; no new event type/⚙/migration; spot-check PASS → **executor DONE `e62a3d4`** (base `905d1b8`; 7 source files incl. new `forkresume.go`; project/shell/api/recovery whole packages green, recovery ZERO diff; stage filter green; property table 12/12; dash/citation probe clean; 4 declared deviations: `crash` delegates to `crashWith`, checklist 6 repo-backed-scoped, `forkResumeDetail` name, `ApprovedPair` read before the transition) → **evaluation FAIL on lows only — behavior VERIFIED on every probe incl. a fork of a fork; all 4 deviations legitimate; F2 → drain r1 `4b9db94` → re-check PASS → merged `c697c54` (CONVENTIONS §76; brief EXPIRED) → **landing battery RED on ONE stale source scan** (`TestTheExecuteLegChecksThePauseBetweenSessions` — literals for the drive loop and the session call moved; behavior intact: pause check between loop head and session) → coordinator inline fix of the scan literals → **done** (combined landing battery 48/0/0, gofmt/vet clean, lockgate 40; landed 2026-09-18); F1 (wrote-nothing gate compares shas, a k=0 restore mints a tree-equal sha-different commit) → ledger; F3 record wording "done WITH evidence_ref" → at landing**; merge forward over TQ-3/v4.1 at landing) |
| P3-TQ-3 | Bootstrap stage contracts decided from the tree (TQ-F4): `bootstrapV1` stops marking every Done-when UNVERIFIABLE-HERE — contracts decidable from the tree (write-set globs, named files, structural facts) are decided at V1 by deterministic or fresh-context model read, never from the executor's report (A14 as written; A16 (3) restates) | backend four-stage | **executing in the worktree** (`2cf5b14` grounding on branch `worktree-agent-a5d4801c09d981cca`: brief 14 reqs / 10 red tests + 2 inert fields; spot-check PASS; deterministic-only — W write-set globs vs tree, N backtick-quoted path spans w/ removal guard, precedence §3.4, FAIL → one AC-BLOCKER finding via the plan's Coverage map (ONE line in skeleton.go); OQ-1 bootstrap-only, OQ-2 → P3-TQ-6, OQ-3 PASS semantics ratified; **executor DONE `bbde6f9`** — TQ3 10/10, verify 108 PASS (+2 TQ-5 commit-2 reds at its stale base), `contract.go` 528 lines + internal tests, ONE line in skeleton.go; **deviation RULED**: two GF4 e2e tests correctly escalate on an over-declared fixture (`e2e_test.go:266` promises `note.md`, the fake executor writes nothing) → sanctioned by name: the fake executor writes the file; **rebased onto main conflict-free (grounding `5731d0f`, impl `b8dd07a`, fixture `61bc53e`) — one fixture site `e2e_test.go:88-99`, filtered stage 24/24 incl. both formerly-red GF4 tests + all GF5/RW-14, verify 110/110; **evaluation FAIL — 1 MED (brief-inherited span rule decides routes/idioms like `/cart`, `next/image`, `24/7`) + 5 LOW + nits, mechanism confirmed by 4 mutations; **drain r1 LANDED `ed449ce`** (new span rule: 11 of 14 impostors previously decided → 0, every real path still decided; union/`**`/unbounded pins; word-bounded guard; symlinked root honest; relative unreadable Detail; absolute write globs → undecided `plan:write-set` — executor's call CONFIRMED); verify 121/121, stage filter 30/30; **re-check FAIL — 1 low-medium (the `**` zero-segment pin was VACUOUS: `public/**` vs `public/a.jpg` consumes one segment) + 3 low (extension arm still admits `e.g/i.e`/`a/b.`/`#`/`\`/`..`; `\b` treats `-` as a boundary; root-unreadable Detail renders `.`) + 1 nit (write set `/`); mechanism confirmed → **drain r2 LANDED `07fe192`** (fresh finalizer: real zero-segment pin; `#`/`\`/`..`/trailing-`.` rejected + a real extension required (`example.com/index.html` deliberately kept path-shaped; one-letter extensions undecided); hyphen-aware guard; root-unreadable wording; write set `/` → `plan:write-set`) → final re-check FAIL on residuals (r2 guard regression: `-`/`_` as word chars on BOTH sides unguarded `soft-deleted`; dotted/capitalised first segments; empty segment) → **coordinator inline `989a6d7`** (asymmetric boundary; first-segment shape rule; both pins mutation-verified) → **done** (merged `9322167`; brief EXPIRED; CONVENTIONS §74; landing battery 48/0/0, gofmt/vet clean, lockgate 40; landed 2026-09-17) |
| P3-TQ-4 | The execution rung at bootstrap (A16; TQ-F3 + TQ-F7 platform half): rescan-capture of detected commands with `detected` provenance → evidence rungs in the verification sandbox; the platform-authored DOM-asserting walk of the frozen given/when/then ACs against the deliverable served inside the C1 netns; the generated-frontend feedback rule into playbook + rubric content | backend four-stage, large | **SPLIT: TQ-4a executing in the worktree** (`84b5ee9` grounding on branch `worktree-agent-a9b3666950565ca3a`: brief 621 lines / 18 reqs / 16 red tests; spot-check PASS; capture side `Detected` member + `Dev` slot + read-only idempotent `RescanDetected` + `EffectiveCommands`; ladder side detected `BootstrapPack` with `Provenance: detected`, rungs run cheap-first by the existing C2 network-off runner, preconditions decided from the tree (a `vite build` with no `node_modules` → UNVERIFIABLE-HERE, `node --test` runs), evidence-only, never graduates; walk interface + honest `walk:no-browser-adopted`; playbook seed-2 under a minted Ensure; **executor DONE `d3a58c5`** (two clean rebases; R1–R16 + R18; 16/16 acceptance; verify/project/shell/api/memory/worker green; `scan.go` + lock byte-unchanged; webshop fixture: `detected:build` unrunnable-honest, `detected:test` PASS, e2e `walk:no-browser-adopted`); **STOP on R17 RULED: rubric v4 is NOT in this packet — it lands as its own packet WITH the B13 golden-set re-run** (three landed tests pin v3/4 items; sanctioned then); deviation ACCEPTED (`bootstrapv1_gf4_internal_test.go` arity update); **evaluation FAIL — F1 HIGH: the platform-built detected pack (`project_seams.go`) is untested (graduating mutation + rescan short-circuit both pass shell+stage); F2 MED no governance battery for `EnsureTQ4KnowledgeGovernance`; F3 MED three stale load-bearing doc invariants; F4 MED RULED: per-slot composition from `EffectiveCommands` with per-CHECK origin (owner vs detected) — one hand-captured slot must not drop the detected rungs; F5/F6 LOW → **drain r1 LANDED `59303a8`** (composition-root tests: 4 mutations each trip; per-slot composition w/ `Check.Origin`; §57 governance battery; doc invariants; F5 NARROWED + accepted) → re-check FAIL on one LOW (the playbook governance's later-decisions guard unpinned) → coordinator inline `7035f8b` (revert-to-seed-1 pin, the only failure under the guard mutation) → **done** (merged `d99cdf0`; brief EXPIRED; CONVENTIONS §79; landing battery 48/0 (the two live local-classifier tests skip — stack absent); landed 2026-09-18)) · **TQ-4b = gate item B14** (browser adoption; loopback-in-netns unverified; C1 vs C2) |
| P3-TQ-6 | A ladder step-contract FAIL must route (found by the TQ-3 grounding, C7/OQ-2): nothing reads `V1Result.Steps` except the event row and the judge slice, so a failed non-AC check bound to a step is a finding that dies in a log (S07.7 forbids); reuse `contractFinding` from TQ-3 in `stepContracts` (pack path) + decide whether the tree predicate also applies to the ladder's N-A steps (TQ-3 OQ-1, widening the A16 wording — coordinator/operator reading) + a judge axis-1 PASS that contradicts a V1 contract FAIL on the same AC must raise CHECK-INTEGRITY (S07.5's disagreement sentence, realized today only for `ACKey`-bound check outcomes — TQ-3 eval nit) | backend four-stage, small | **executing in the worktree** (`5cb5b43` grounding on branch `worktree-agent-afe7b6badfa5a1196`: brief 12 reqs / 7 red tests (2 property); spot-check PASS; pack-path FAIL → the shared `contractFinding`; `ContractDisagreements` pure detector → CHECK-INTEGRITY blocker, never the drain, judge never overridden; no coverage map → nothing guessed; **live-value caveat: production `packChecks` binds no StepID, so R1 has no live effect until packs bind steps — the LIVE P46 instance is P3-TQ-7**; **executor DONE `8fdcdc5`** (rebased onto `ede4971` clean; 3 source files +105/−3; TestTQ6 7/7; verify 132 pass; stage filter 31 pass; no deviations) → **evaluation FAIL on test gaps only** (19 probes conform: no double-mint at bootstrap, FromV1 excluded end-to-end, carded once per key across 3 rounds, disagreement-only round → SHIP-with-notes never REVISE; unpinned: dedupe-by-anchor, the Unknown guard, multi-check Detail, slice purity) → drain r1 `451cc7f` → **re-check PASS** (each mutation trips exactly its pin; wording token-free) → coordinator inline nit `f668cc8` (closing clause "Verification and the review disagree") → **done** (merged `b82a0ed`; brief EXPIRED; CONVENTIONS §77; combined landing battery 48/0/0; landed 2026-09-18); ledger: a contract disagreement alone never blocks (unreachable for a valid pair — intake rejects coverage outside the frozen ACs); the ACKey disagreement quarantines, the contract one does not (R6)) |
| P3-TQ-7 | The live P46 instance (found by the TQ-6 grounding): a failed captured check with no StepID and no ACKey mints NO finding today (`v1.go` `CheckFailed` → nothing), so a failed build/lint/test check is recorded and judged and SHIP stays possible — an S07.5 citation-rule reading is owed (TQ-6 brief §9 OQ-1 lists four candidate readings: the check's declared finding category as a blocker; a ladder FAIL as its own criterion; note-only; REOPEN-SPEC) — **READING RECORDED 2026-09-18 (clearly implied; presented at the gate as a reading, not an amendment):** S07.1 "V0/V1 kill broken output before any paid call" — a failed captured check is a V1 KILL, so the round cannot SHIP; the S07.5 citation rule ("every blocker MUST cite the frozen criterion or an axis-2 rubric item") governs the JUDGE's [F1..Fn]; platform-raised V1 machine findings cite the CHECK that failed (`check:<id>`, its declared category — `packChecks` already declares the AC-blocker route) and `validateFindings` admits that criterion for V1-origin findings ONLY (goalposts stay fixed: the pack is the project's own frozen, verified-on-stamped bar); a REVISE round follows, CAP-HIT card at cap; readings (a) note-only [toothless — contradicts S07.1], (b) bind every check to every step [multiplies checks], (d) first-upstream FAIL for every step [blunt] rejected | backend four-stage, small | **grounded `9ec357c`** (worktree branch `worktree-agent-ad7dc455246a0933c`: brief 15 reqs / 8 decisions / 9 tests (7 red, 2 guards); spot-check PASS; mint site = `RunV1`'s `CheckFailed` arm, one finding per failed OWNER outcome {blocker, the check's declared category, criterion = anchor = `check:<id>`, plain words + a 2048-byte bounded output tail}; origin = an UNEXPORTED `Finding` member (the judge's JSON cannot forge it) admitted by `validateFindings` for V1-origin only; REVISE via the unchanged `ComputeVerdict`, the finding rides the retry package in `Text`, round-stable key → convergence → CAP-HIT; guard by `Provenance == detected` (TQ-4a), never `Posture`; only a non-zero exit kills (a runner error stays CHECK-INTEGRITY); D8 RATIFIED: AC-bound failed checks cite `check:<id>` too (uniform shape); one sanctioned pre-existing edit by name: `TestTQ6NoCoverageMapMeansNoDisagreementAndANote`'s SHIP-with-notes tail was the P46 premise; **executor DONE `450121f`** (rebased clean, the one expected hunk resolved to main's typed `Provenance`; 5 files under `internal/verify`; TestTQ7 9/9; verify 158; stage `-skip` green; shell green; 6 mutations each red; the origin mark crosses JSON in neither direction; deviation ADJUDICATED LEGITIMATE) → **evaluation FAIL — F1 MED (the per-check guard fact unpinned: posture/pack-provenance guard mutations pass; a mixed graduated pack is the missing pin), F2 LOW (declared category unpinned), F3 LOW (`boundedTail` drops a whole line at an exact boundary; line-boundary start unpinned); 9/13 mutations tripped; spec/ruling conformance CONFIRMED (forged origin keys demoted, judge still called, key round-stable, retry package carries the tail) → drain r1 `c213c41` → re-check PASS → **done** (merged `ba52254`; brief EXPIRED; CONVENTIONS §80; landing battery 48/0 (live GPU tests skipping — stack absent); landed 2026-09-18)) |
| P3-TQ-8 | The judge's input slice for repo-backed deliverables (S07.5 "the artifact + its diff against the previous revision"): today the judge receives the full step report with `Diff=""` (`engines.go:923-926`); after SIT-1 the tree diff is computable host-side — feed the inventory + per-file diff (bounded) and the companion report; deferred by SIT-1's grounding to TQ-4, which declined it as outside A16 | backend four-stage, small | **grounded `d7c9238`** (worktree branch `worktree-agent-a0253ce364edb86af`: brief 9 reqs / 9 red tests incl. a 150-case property test; for a tree revision the judge's `verify/artifact` = inventory + full content of modified/renamed files + omitted lists, `verify/diff` = the shown per-file diffs (added files ARE their diff), the report → a new unquotable `verify/executor-report` item; `ValidateAxis1` quotes against artifact+diff only (today INVERTED: a report quote passes, a diff quote is forced Unknown); bound `JudgeArtifactBytesCap` = 192 KiB derived from the 200k window × ⚙ `context.stage_fit_target` 0.50, file-boundary cut, omitted files named; source = the review store's tree lane through a new `verify.ChangeSource` seam implemented by `stage.reviewSink`, pins from the revision rows, a lost pin fails the round before any judge call; cost shape unchanged; `RoundRecord.JudgeSaw` recorded keep-forever; content-pinned slices byte-identical; no ⚙ (the cap is a structural constant → settings-tab ledger); one sanctioned edit `v2_test.go:40` (a call gains the slice param); spot-check PASS → **executor DONE `387de17`** (rebased clean; the seam + renderer + `Quotable` + the new `verify/executor-report` item + `judge_saw` on the verdict row; pipeline/v2 hunks clear of TQ-4a/TQ-7 regions; verify 168/1, stage 214/0; 8/9 acceptance — the 9th asserts the legacy `verify.round` name renamed to `verdict.recorded` → one-token grounding fix `86946c7` (verify 169/0, stage 214/0, TQ8 9/9) → **EVALUATION DIED at the Fable limit 2026-09-21 before any probe (still reading spec) — RELAUNCH FRESH (opus if Fable is limited) on the worktree at `86946c7`, then merge → CONVENTIONS §81 → brief EXPIRED → landing battery**) |
| RUBRIC-V4 | Rubric v4 (the TQ-F7 `axis2/frontend-feedback` item) landed TOGETHER with its golden-set re-run so the bundle's rates are its own (gate B13): content bump + the coordinator-run rider on Opus 5 + the three landed test edits sanctioned by name (`seeds_test.go:26`, `tq5_rubric_test.go:23/:47`, `evals/tq5_floors_test.go:24`) + floors if the bound moves | small four-stage + measurement rider | queued (after gate B13 answered; after TQ-4a lands) |
| P3-TQ-4c | Detected rungs in a GRADUATED mixed pack (found by the TQ-7 evaluation, N1): a failing detected rung yields clean SHIP + verified with ZERO findings, and first-upstream-failure attribution CROSSES origins (the owner's downstream rung is UNVERIFIABLE-HERE attributed to a detected failure) — no human-visible sink (S07.7); fix: attribution never crosses origins, and a failed detected rung in a graduated pack surfaces as a NOTE (evidence, never a kill) | backend four-stage, small | **grounded `48cf2d6`** (branch `worktree-agent-a08ed49b57e61379b`: 13 reqs / 10 tests (7 red + 3 guards incl. an 8×8 slot-composition proof); attribution per origin LANE (an owner failure blocks later owner AND detected rungs; a detected failure never blocks an owner rung; `stepContracts` gets the owner lane); a failed detected rung → a NOTE (declared category, criterion "", anchor `check:detected:<slot>`, plain words, `fromCheck`-marked) → SHIP-with-notes, items verified, no card; the note also rides at bootstrap; one `v2.go:429` clause exempts `fromCheck` notes from round>1 suppression; two sanctioned landed-test edits by name (`tq7_kill_test.go` checkBlockers severity filter; `tq7drain_test.go` mixed absence → "no BLOCKER"); ledger: `BootstrapPostureNote` wording stale post-A16, §79/§80 "mints no finding" sentences need the landing amendment; **SPOT-CHECK OWED; executor after TQ-8 merges (shared verify files)**) |
| P3-SIT-1 | Code as the deliverable, backend (SIT-F1/F2): repo-backed work minted as `code` pinned to the snapshot; file inventory + per-file host-side unified diff served from the platform store refs (rev-1 vs pre-task base, N vs N−1 per S13.1/S13.2); the step report as a companion object | backend four-stage | **executing in the worktree** (`668867a` grounding on branch `worktree-agent-a448031b7bb67f899`: brief 17 reqs / 18 tests (15 red + 3 guards incl. a 14-pair property battery over a real store); spot-check PASS; defect at the mint (`review_sink.go` types the row before the snapshot seam runs), fix = snapshot first + dtype `code` iff pin && software, report as companion; tree lane keyed on the PIN (content lanes byte-identical), read-only hermetic project verbs, additive detail `change` + `compare?path=` + `files` verb, 3 structural caps, no migration (dtype identity-immutable; repo-backed derived from `snapshot_sha`), accept untouched; vitest debt OWED (goldens move); judge tree-diff → TQ-4; **executor DONE `cbbf8ac`** (rebased clean; 19 files; mint snapshot-before-ensure, dtype `code` iff pin && software; tree lane keyed on the pin; hermetic plumbing verbs; `reviewStore.Tree` at the composition root; detail `change` + `compare?path=` + `files` verb; goldens regenerated with ZERO drift; tsc + vite build green; vitest 859/860; 17/18 acceptance green) → sanctioned fixes `bab2e6f` (positive base probe + README-not-rendered; the `files` route as a declared sweep gap in the normalized `{}` form, counts 1→2) → **evaluation FAIL (F1 HIGH lost pin never `content_drift` — 500 w/ git text on compare, 404 fall-through to the companion on `files`; F2 MED directory path leaks git stderr; F3 MED anchor `file_path` unvalidated reaches exec, judge anchors could fail the findings record; F4 MED-LOW wiring test assigns the seam itself; F5–F8 LOW caps/hunk pins, false 400, overwritten nil-tree label, prefix-diffing over-cap blobs; F9/F10 nits) — every claim execution-proven; mechanism verified (5 mutations caught, read-only audit clean, gitdiff-parser parses the bodies, no sha oracle) → **drain r1 LANDED `ba891b4`** (typed `ErrPinMissing` → `content_drift` on all four reads; `cat-file -t` before reads; one `checkTreePath` rule at the API, model anchors degrade; real wiring test; at-cap cases + hunk-cut pin; honest nil-seam shapes; full-blob diffs cut; `--literal-pathspecs`; deviation ACCEPTED: one-sided over-cap files serve a truthful prefix) → re-check PASS → **done** (merged `409de5a`; brief EXPIRED; CONVENTIONS §78; landing batteries Go 48/0/0 + web tsc/860/860/build green; landed 2026-09-18); riders to SIT-2: `kind` from the inventory (F9), the `./` ingress normalisation nit, the receipt `judge` line (TQ-5 R4), `parent_run_id`/`ending` (TQ-2 R10) |
| P3-SIT-2 | Code as the deliverable, frontend (SIT-F1/F2/F4 + SIT-F3 copy + TQ-F8 narrative): file inventory, per-file diff, revision navigation, the report demoted to "what the worker says it did", honest preview-absence copy (whose gap, what to do instead), crash→fork narrated; + render the receipt's additive `judge` line (P3-TQ-5 R4 follow-up) | FRONTEND.md | **CHECKPOINT 1 PENDING — the product map v4 is written (`P3/design/product-map.md` §9–§17, commit `1861ef8`, with before-frames under `rework-screens/sit2/`); the builder waits for the operator's approval + Q1–Q4** (single Fable author on `main`; reference = a COPY of the sitting world (`~/.sinet-sit2-builder`, :8489, the real webshop deliverable) + the Nexus review look; riders: TQ-F8 crash→successor thread via `parent_run_id`/`ending`, the receipt `judge` line, `kind` from the inventory, the `files` sweep gap closed, `./` normalisation, SIT-F3 honest-absence copy) |
| P3-SIT-3 | The preview substrate promoted (SIT-F3, gate B9=a): S11.4/S11.8 host units + socket-proxyd lifecycle + Caddy admin routes + the spawn/probe/route activation in `internal/preview`; host changes proposed → approved → applied at a sitting | backend four-stage ×2 + host session | waiting on gate B9 (explanation owed to the operator) |
| v4.1 | `quality_bar` weight 8 → 12 as a taxonomy supersession (gate A2=a) | small four-stage (a behavior change: what standard tier asks) | **evaluation FAIL on 1 LOW test gap (the `decisionRecorded` guard unpinned — the guard deleted leaves memory green while an operator's gate revert would be re-applied every boot) + nits; code VERIFIED (frozen v4 byte-identical to the 55f2886 seed, GF7 digest unchanged, floor math reproduced, 7 governance probes pass) → drain r1 `1a141af` → re-check PASS → **done** (merged `0d011ad`; brief EXPIRED; CONVENTIONS §75; landing battery 48/0/0; landed 2026-09-18) (`e6989af` grounding: brief 10 reqs / 10 tests (8 red + 2 guards); spot-check PASS; mechanism = weight+version+provenance in the seed, v4 FROZEN as `gf7taxonomy_v4.go` (GF7 digest unchanged), minted `EnsureV41TaxonomyGovernance` supersession, shell wiring; floor math PROVEN: card 1 at every tier = the four 12s incl. quality_bar; **CONSEQUENCE surfaced to the operator: at standard, quality_bar DISPLACES language_locale (9) into the planner's assumptions** — v4 asked the language question at standard, v4.1 does not (only a further weight edit or a floor move ⚙→S18 keeps both); executor launched 2026-09-17) |

## Operator hands-on items (carried from G4)

| # | Item | Due at |
|---|---|---|
| 1 | Z.AI dashboard prompt-unit calibration (5-step recipe in P2-S1 report §Blocked) | B1 (metering trust) |
| 2 | ts.net hostname pick — **FULLY DONE 2026-07-19: `sinet`** (spec amendment A1) **and Tailscale machine renamed** (was `sinep-predator`); FQDN `sinet.tailfd0b1e.ts.net`. First cert issuance remains a B0-gate step | done |
| 3 | Root/reboot/suspend probe batch — **reboot-survival DONE + `Persistent=` catch-up reboot leg DONE 2026-07-20** (both match spec; result files in `P3/measurements/`); remaining: catch-up suspend leg (pair with battery-drain suspend; probe timer left installed) + user.slice freeze/thaw | B0/B1 gate |
| 4 | Week-one push drill on household phones | first deploy |
| 5 | (Optional) GitHub Verified badge — signing-key upload | anytime |
| 6 | age identity escrow (S13.10) — **DONE 2026-07-22.** Live identity `~/.config/sinet/age-identity.txt` (0600), recipient pubkey `age1n0p60jcwpzwlqky48wdy8jpcvn695halrjmnmcg50pxp5qwhrpmqsrhatk` (B4-3 reads identity/recipient from this path, never from prose); age v1.3.1 CLIs user-level (ceremony tooling, not a platform dep — B4-3 adopts the age Go module via the rail). Operator ran the guided ceremony (paper copy + passphrase-encrypted copy, script-verified round-trip) and declared complete (free-text, authoritative): encrypted copy on operator-held off-host storage device; cloud upload declined — off-host requirement satisfied. Staging copy verified deleted from host; live-key fingerprint re-verified. The B4-3 restore drill decrypts with escrow material = the designed live proof | done |
| 7 | Observables-register sign-off — **SIGNED 2026-07-20** (operator "go" at the D2.2 cert execution; 3-row v0 register — rows 1/3 walked explicitly; row 2 push metadata noted as not-yet-live). Nuance recorded: the cert/CT publication had already happened 2026-07-19 21:27 UTC via the leftover serve config — signature covers it retroactively (B0-cert-steps execution record) | done |

## Log

The dated log is `P3/STATE-HISTORY.md` §Log (append-only, newest first, ≤600 chars per entry). Grep it by date or packet id; never read it whole.
