# P3 implementation — live state

> Any session advancing the build: read this file, then follow `.claude/skills/p3-implementation/SKILL.md`. Update this file before and after every step — it is the single source of truth for build coordination. The contract is `Spec/core-architecture-v1.md` (v1, frozen, tag `spec-v1`); the campaign record in `Research/` is a closed archive.

**Phase status:** **B3 CLOSED 2026-07-22 — gate record `P3/gates/B3-report.md` §7 FINAL: all five decisions closed+executed** (D1 engine 2.1.216, canary PASS; D2 amendments A2+A3 applied, A4 drafted awaiting a future ok; **D3 advisor split ratified + APPLIED `e06f0a4` — planning=opus-4-8, execution=sonnet-5, judge=opus-4-8**; D4 playbook seed ratified; D5 readings en bloc). **B4 OPEN — queue cut 2026-07-22 below** (7 packets, S13 before S12 per S19.5 order: B4-1 schema/review/drain → B4-2 git/registry → B4-3 accept/snapshots **opus** → B4-4 previews → B4-5 serving/aliases → B4-6 GPU broker/VRAM **opus** → B4-7 calibration/battery/measurements incl. both D3 riders). **P3-B4-1 DONE + validated (`e60ddfb`); P3-B4-2 DONE + validated (`5056c62` + drain `a860445`, evaluation PASS after 1 round); P3-B4-3 DONE + validated (`2218954` + drain `92d855b`, evaluation PASS after 1 round); P3-B4-4 DONE + validated (`372d2ba` + `6789033` + `5024709`, evaluation PASS after 2 rounds — the first packet to use the full cap); **P3-B4-5 DONE + validated (`aeefd3b` + drain `089c759`, evaluation PASS after 1 round; landing + handoff at the end of this block).** Superseded in-flight detail kept for history: brief (`7b44c0b`, 29 reqs / 25-item rubric), NINE readings coordinator-RATIFIED (llamaswap-unit-generated; tie-break-on-`utility` flagged as a possible gate-time S00.9 registry amendment; consumer-class split; checkpoint-row $0 lane override; true-$0 vs S10.3 guard; models-as-platform-data; structural-config 7th instance; busctl subprocess; embedder end-to-end excluded), executor DONE (`aeefd3b`, 36 files +3660; pins llama-swap v241 / llama.cpp b10085 / Ollama v0.32.1 standby; toolchain resolved to path (b) user-level CUDA+build; GameMode probe EXECUTED — session-bus unreachable from system user, scripts leg carries the duty; Gemma license conflict FLAGGED as designed; **DECLARED DEVIATION UNDER COORDINATOR TRIAGE: model pulls + CUDA build + real-model tier-L smoke deferred to gate — coordinator preliminarily REJECTS the deferral (legs are sanctioned user-level per brief §6, no genuine blocker claimed; queue-row headline is the ratified contract) — drain will direct completion unless evaluation surfaces a genuine blocker**), drain round 1 APPLIED (`089c759`, 23 files +707/−186): sanctioned legs EXECUTED — CUDA 12.9 toolkit user-level + gcc-14 via micromamba (host gcc-15 > CUDA ceiling) + llama.cpp b10085 built sm_120; REAL GPU tier-L smoke PASS (json_schema enforcement vs hostile prompt, logprobs, $0 D7 row w/ model sha + engine build, eager-unload VRAM release); 9B/4B/Flan-T5 pulled+hashed; GENUINE upstream blockers recorded honestly (Arctic/Gemma/Granite = HF per-IP rate limit after ~15 GB — retry at bring-up; Bespoke = 401 license-gate = operator act; DeBERTa = no GGUF exists) — none on the v0 default path; scratch+weights in `~/.sinet-b45` (38 GB, outside repo); F2–F12 all applied (b10085 preserves schema property order — confirmed live, no two-call needed). Evaluation re-check: **PASS** — all 12 verified live by the evaluator's own GPU runs (round-1 scope for the record: user-level cmake+CUDA-toolkit+sm_120 build, ~30 GB pulls w/ sha256s, REAL tier-L smoke, + F2 helper-run metering context, F3 real tier-R/L asserts, F4+F11 durable admission-flag full verb pair, F5 reason-first ordered grammars live-verified, F8 spot-check→`intake-triage` per S12.4 text, F9 bounded serving ctx, F6 doc/record incl. host facts [stock persistenced --no-persistence-mode active; operator's ollama.service v0.30.10 RUNNING — gate records, not platform acts], F10+F12 folded; **F7 RATIFIED reading: abstain binds classification-shaped duties only, Help exempt** — S12.4's own rationale), session 2026-07-22(c). NETWORK SANCTION + hard host gate as recorded. **SESSION 2026-07-22(c) ENDED HERE per the operator's stop-after-B4-5 directive (deliberate stop, NOT a failure-ladder strike; context-budget handoff). NEXT ACT via "continue implementation": launch P3-B4-6 grounding (ALL-opus packet per the routing line). B4 is 5/7 done.** **B4-5 state the next session inherits:** local stack LIVE-CAPABLE user-level (llama-swap v241 installed; llama.cpp b10085 CUDA-built sm_120 via user-level CUDA 12.9 + micromamba gcc-14; 9B/4B/Flan-T5 pulled + sha256-hashed; tier-L GPU smoke proven green twice — executor + evaluator; the coordinator's env-less run exercised the sanctioned-skip dev default correctly); scratch + weights in `~/.sinet-b45` (38 GB, outside repo — cache-location ratification = gate item; disk now ~117 GB free). **Three model seats pending, none on the v0 default path:** Arctic/Gemma/Granite = HF per-IP rate limit — RE-PULL at next session/bring-up + manifest re-hash (the leftover throttled curls were KILLED at landing; they had restarted from byte 0); Bespoke-MiniCheck = HF 401 license-gate (OPERATOR account act); DeBERTa-class = no GGUF exists (pick revisit at B4-7). `/tmp/llamaswap-test/` dir awaits operator `rm` (its server process killed at landing; the rm itself was permission-denied to the coordinator). `ollama.service` observed INACTIVE at landing (active at boot; self-stopped — not a platform act). Nit residuals recorded: tier-L order-preservation check is Logf-not-assert (HARDEN in the S12.10 bump procedure at B4-7); the gamemode snippet's gate-install step must carry `SINET_LOCAL_STATE`; Ollama-standby #10 RATIFIED as activation-time (entry still listed at the gate for visibility). **LIVE-HOST HAZARD carried into B4-4: the production front chain (serve→Caddy 8481→unit 8482) is LIVE on this machine — packet tests must be hermetic, never touching the live Caddy admin API/config; socket-proxyd units GENERATED never installed.** **Coordinator reading (runbook ambiguity rule):** S12.1 consumer class (a) — engine runs ON local models via the opencode adapter — has no v0 consumer (Z.AI lane parked; v0 palette routes execution to paid seats; every S12.4 registry duty is class-(b) "no engine session involved") → the second engine adapter is NOT in the B4 cut; it attaches when a consumer exists (Z.AI activation or a local execution seat). Consistent with S19.5 B1 "one adapter + one lane"; no S19.5 row names the second adapter. **B4 environment facts (checked 2026-07-22):** 161 GB free (S12.3 downloads ~30 GB incl. bakeoff + measurement alternates — fine, check again before pulls); **no nvcc on host** → B4-5 live-resolves the llama.cpp toolchain path (prebuilt CUDA binaries / project container image — docker present / user-level toolkit; **never any driver/kernel/system-CUDA change — hard gate**; a genuine host-package need = flagged operator decision); gamemoded present → the TBD-P3 GameMode probe is live-executable; GPU = RTX 5070 Ti Laptop 12227 MiB (pool12 as spec'd). **Escrow (G4 item 6) DONE 2026-07-22 (item row below) — B4-3's live snapshot leg is UNBLOCKED** (dev-mode uses local bare-repo fixtures; identity/recipient consumed from `~/.config/sinet/age-identity.txt`, never from prose). `sinet-llamaswap.service` will be GENERATED, not installed (B0 precedent; install = B4 gate or hardening session). Host posture unchanged: production units live (`sinet-control`+`sinet-broker` on 127.0.0.1:8482; front chain serve→caddy 2.6.2-14→unit; cert runbook fully closed), srt 0.0.66 installed; service-context confined runs stay blocked by the AppArmor userns finding until the hardening session (egress substrate + run@ + userns grant + `socat`; operator scheduling, needs sudo). Probe homework re-deferred (suspend leg ↔ battery-drain hour; freeze/thaw ↔ hardening). Demo dirs + `tools/dbpeek/` sanctioned for operator `rm` at leisure. Z.AI calib still parked (no Z.AI lane).

**Model routing (superseded 2026-07-22 by the four-stage pipeline directive below):** per stage — executor/finalizer `opus` on EVERY packet; grounding/evaluation inherit (Fable), `opus` when the packet is S10/S11-dense and as lossless fallback on any classifier trip; coordinator Fable at max. Full pipeline: `.claude/skills/p3-implementation/SKILL.md` §Work packets.

## Standing operator directives (cross-phase)

- **2026-07-22 — Four-stage packet pipeline (quality-first; ratified in chat; supersedes single-agent packet execution from P3-B4-2 onward).** Every packet runs grounding → executor → evaluation → finalizing per `.claude/skills/p3-implementation/SKILL.md` §Work packets: fresh-context stages, all coordinator-launched; `P3/briefs/` holds the handoff artifact which doubles as the evaluation rubric; executor is `opus` ALWAYS (judge ≥ executor AND judge ≠ executor per the D3-ratified research; classifier immunity mid-run); grounding/evaluation Fable — all-`opus` on S10/S11-dense packets and lossless `opus` fallback on any classifier trip; findings drain [F1..Fn] round-capped at 2. Research basis: Anthropic's three-agent harness (planning/generation/evaluation + structured handoff artifacts; InfoQ 2026-04), official "Prompting Claude Fable 5" (fresh-context verifiers outperform self-critique; benign-cyber false positives → Opus routing), D3 record `P3/gates/B3-report.md` §7. Autonomy contract: the operator starts ONE session ("continue implementation"); it stops only at phase gates, spec conflicts, and operator hands-on items.
- **2026-07-20 — Codor harvest: adopt the copy rows, coherence-verified.** Operator directed (chat): adopt the copy candidates from `Research/18-codor-harvest-addendum.md`, but only after verifying coherence with the architecture. Verification ran same day — **verdicts + binding conditions in Research/18 §7** (the authoritative record). Effect on packet cuts: **B3 cut MUST include C1-recitation** (per-turn delivery of ledger `state`/`next_actions` via a PostToolUse `sinet engine-hook` trigger over the existing S03.4 ctl-dir airlock — NO new socket/credential; first step = the pre-registered ~$0.02 PostToolUse `additionalContext` probe, B1-4 method; consumes ⚙ `context.recitation_interval_turns`; all seven §7-C1 conditions bind) and **C3 conformance rows**; **B5/B6 cuts include C2** (serve-side redaction of observability projections + redact-before-match search; constant-on, never applied to deliverable content); **C5 discretionary** (advisory copy-aside context peek). Explicitly NOT adopted: C1-ext (cancel-notice/operator-note — S00.9 amendment territory), C4 (parked at the S04 ≥50-item fan-out row). Nothing from codor is ever a dependency.
- **2026-07-20 — "Settings tab must show AND allow changing everything."** Operator founding requirement (restated at the B1 gate): every operator-relevant number is visible and editable in the settings surface — including numbers S18 deliberately left as **code constants**: auth numbers (TTL/PIN attempts/lockout — B0 gate D2), scheduler structural constants (lane-concurrency default 1 / poll 500 ms / aging biases — B1 gate §4.4), price-table bits, and any others surfaced. **Deferred by the operator ("fix later") — NOT a B2 blocker.** Resolution path: an S00.9 amendment adding the non-⚙ numbers to S18 as **clamped** ⚙ settings (clamps keep invariants — e.g. aging biases can't break the S10.7 no-starvation guarantee). Natural home: around B6 settings UI (S15.9) or a dedicated pass. **This directive makes the "keep as constants" gate decisions (B0 D2, B1 §4.4) INTERIM, not final.** Also in auto-memory `settings-tab-see-change-everything`.

**2026-07-20 evening — D2.2 EXECUTED (this = the parallel cert session; full record in `P3/gates/B0-cert-steps.md` §Execution record):** front chain LIVE end-to-end — `tailscale serve` (tailnet-only, stale Nexus-era 8777 entry reset) → Caddy 2.6.2-14 on 127.0.0.1:8481 (organ-unit lock entry added; runbook Caddyfile had a wildcard-bind defect, fixed in place: `http://:8481` + `bind 127.0.0.1`) → the production unit on 8482. Production-posture verification exact per runbook (health ✓, `authenticated:false`+hint ✓, /events cookie-less → 401 ✓). KEY FINDING: the cert was **already issued 2026-07-19 21:27 UTC** (leftover serve config × machine rename) — CT publication predated the gate; operator item 7 SIGNED 2026-07-20, covering it retroactively. LATER SAME EVENING: steps 11–12 DONE — production operator `darian` bootstrapped (create landed with the placeholder PIN; caught by coordinator probe, rotated via `POST /api/auth/pin` step-up, other sessions invalidated), SSE-with-session streams the full auth audit trail live. **Runbook CLOSED**; step-13 crt.sh observation DONE 2026-07-20 next-morning (1 cert, 2 CT entries, CN/SAN = FQDN only, notBefore 2026-07-19 21:27:21 UTC — exact match; record in B0-cert-steps.md) — **runbook fully closed**.

**2026-07-20 evening — B2 gate D2 ANSWERED + items 1/3 EXECUTED (host now touched, by decision):** units installed (`sinet-control`+`sinet-broker` active on 127.0.0.1:8482, `/var/lib/sinet`, journald cap; portpool file-only — stub mode; broker StateDirectory defect found+fixed = `P3-B2-fix4`), srt 0.0.66 installed+discovered (ADOPTED path; version-print quirk in lock notes), cert running in the operator's parallel session (backend 8482). D2.4+7 → dedicated hardening session (**must include the AppArmor userns grant for the service context — probe available=false as service, new finding**); D2.5 → with escrow pre-B4; D2.6 → after suspend probe. Production users table empty — operator bootstrap pending. Gate remainder: D3 seeds, D4 readings, D6 probes, and the D1-record items (3 demo readings + the P3-B2-5 close-now-vs-hold pick).

## Phase board (S19.5)

| Phase | Scope (owning sections) | Status |
|---|---|---|
| B0 | Spine: sinet-control process, platform.db, event log, settings registry, auth stack, five seams [S01]; run FSM, checkpoint/effect journal, recovery ladder [S02]; adoption manifest + CI lock-gate [S16] | **CLOSED 2026-07-19** |
| B1 | Execution substrate: one adapter + one lane [S03]; metering ledger + limit taxonomy + scheduler skeleton [S10]; per-run sandbox C0–C2 + credential broker [S11]; B1 spike battery [S19.5] | **CLOSED 2026-07-20** |
| B2 | Pipeline: intake→spec→plan [S06]; context ledger [S05]; verification [S07] — closes the walking skeleton (gate = live demo) | **CLOSED 2026-07-20** |
| B3 | Workforce & memory: worker registry + composer battery + routing [S08]; memory/knowledge + write gate [S09]; + S04 spawn/helper slice (reading below) + codor C1/C3 (standing directive) | **CLOSED 2026-07-22** |
| B4 | Deliverables & local tier: deliverable/review/git/backup [S13]; local-model stack + GPU broker + duty aliases [S12] | **OPEN** |
| B5 | Observability & evals: watchdogs, conformance registry, benchmark machinery, queryable history [S14] — incl. the [schema] STUDY watch row (G4 follow-up) | pending |
| B6 | Frontend: React SPA consuming every API [S15; FC-v1] | pending |

## B0 packet queue

Statuses: `pending` → `running` → `review` → `done`.

| Packet | Title | Read-first sections | Acceptance (headline) | Status |
|---|---|---|---|---|
| P3-B0-1 | Repo scaffold + adoption rail + CONVENTIONS | S01 (esp. S01.5 release artifact), S16, S19.5 | Go module builds a stub `sinet` binary; CI (build + test + lock-gate) green; initial `components.lock`; `P3/CONVENTIONS.md` authored from the sections | done |
| P3-B0-2 | platform.db + settings registry + event log | S01.9–S01.10, S02 (schema/tables), S18 (key index) | DB bootstrap with ratified pragmas + migrations; declare-once registry (clamps, audit events, schema emission); append-only event log with `event_seq`; unit tests | done |
| P3-B0-3 | sinet-control shell + API/SSE skeleton + five seams | S01 full | Dev-mode process serves health + the one SSE endpoint; watchdog wiring; seams as package boundaries; systemd unit files *generated, not installed* (host changes wait for the gate) | done |
| P3-B0-4 | Run FSM + checkpoint/effect journal + recovery ladder | S02 full | FSM with derived-not-stored stalled state; checkpoint rows; two-phase effect journal; recovery ladder + leases/generation fencing; **kill-9 harness v1** in the test suite | done |
| P3-B0-5 | Auth stack (tailnet wall → header hint → sessions + PIN) | S01 (auth), S15.2 (API posture) | Session + PIN machinery with tests; identity-header parsing in dev mode; cert/hostname steps documented for the B0 gate (needs operator item 2) | done |

**B0 gate: CLOSED** — record in `P3/gates/B0-report.md` (5 decisions: adoptions ratified; auth constants stay; host installs → B2; inhibitor wiring → B2; readings ratified en bloc).

## B1 packet queue (cut 2026-07-19 from S19.5 + S03/S10/S11 scopes)

| Packet | Title | Read-first sections | Acceptance (headline) | Model | Status |
|---|---|---|---|---|---|
| P3-B1-1 | D3 adapter contract + first adapter + one lane | S03 full; S16.3 engine rows; S02.4 (checkpoint blocks it must fill) | Adapter contract as the seam; one engine adapter + one lane working dev-mode end-to-end (spawn → events → checkpoint rows); conformance suite v1 asserting behavior at the pin; engine lock entries via the rail | inherit | done |
| P3-B1-2 | Metering ledger + limit taxonomy + scheduler skeleton | S10 full; S02.2 (queue/lanes/receipts tables) | Usage→ledger from checkpoint rows; limit-event taxonomy; scheduler claim loop (CAS, lanes, ceilings) admitting runs dev-mode; receipts materialized per run-end | **opus** | done |
| P3-B1-3 | Per-run sandbox C0–C2 + credential broker | S11 full; S01.2 (broker unit posture) | Sandbox compose C0–C2 with probes; `sinet broker` mode real (per-person stores, injection at spawn); run units confined before real work | **opus** | done |
| P3-B1-4 | B1 spike battery (serialize-by-deny reconfirm ≡ parallel-gate fallback measurement; PreCompact/injection mechanics; auto-memory containment) | S19.5–S19.6 spike rows; S03/S05/S09 anchors | Each spike: result file in `P3/measurements/`, pass/fail vs pre-registered expectation, spec TBD markers resolved or escalated | **opus** | done |
| P3-B1-3R | **srt adoption remediation** — adopt sandbox-runtime as ratified composition core; native = funeral-plan fallback | S11.1, S16.1–S16.4; existing `internal/sandbox` | srt is the primary composition path (config-gen + CLI wrap); native retained as labeled fallback (no dev-mode regression); srt lock entry (live-verified pin, Apache-2.0); CONVENTIONS §12 mislabeling fixed; srt host-activation deferred to B2; battery+lockgate green | **opus** | done |

## B2 packet queue (cut 2026-07-20 from S19.5 + S05/S06/S07 scopes — the walking skeleton)

Model routing: none of S05/S06/S07 are S10/S11 security-dense → all inherit (Fable), unless a packet's deep-read pulls in S11/S10 internals.

| Packet | Title | Read-first sections | Acceptance (headline) | Status |
|---|---|---|---|---|
| P3-B2-1 | Context ledger + fresh-context-per-stage | S05 full; S02.2 (`run_events` `ledger_update`); B1-4 PreCompact/injection result | Ledger structure + revisions persisted as `ledger_update` events (self-contained D7 payload); fresh-context-per-stage assembly; deterministic injection channel (per the B1-4 spike); unit tests | done |
| P3-B2-2 | Intake → spec → plan pipeline | S06 full; S05 (ledger seam); S02.8 (`artifact_claims` at plan approval) | Intake→spec→plan stages producing durable artifacts; interview taxonomy seed (TBD-P3, S19.5); plan approval writes the write-set claim; dev-mode end-to-end to an approved plan | done |
| P3-B2-3 | Verification (two-axis + retry drain + escalation) | S07 full; S02.7 (effects gate ≠ quality gate) | V0–V2 two-axis verification; findings→retry drain (round-capped); tested escalation ladder; entailment machinery ships idle (software launch domain); quality-verdict-never-releases-effects wiring | done |
| P3-B2-4 | Walking-skeleton wiring + live demo harness | S05–S07, S06, S02, S03/S10/S11 seams | intake→plan→**execute (B1 adapter+sandbox+scheduler)**→verify→checkpoint→receipt wired end-to-end dev-mode; the demo script that IS the B2 gate | done |

**B2 measurement rows (S19.6):** entailment calibration set (planted pairs + TPR/TNR bar; also sets `verification.entailment_sample_rate`) + golden-set seed per launch domain [S07] — attach at B2-3/gate. **B2 gate = walking-skeleton live demo + the batched host-install decisions** (systemd units, cert runbook, Caddy, egress substrate, srt host activation, age/sops crypto, logind inhibitor) — all deferred here from the B0/B1 gates.

**B1 measurement rows (S19.6):** operator probe batch → `PROBES.md` (homework, any time before B1 gate); anthropic unified-header utilization-scale + `7d_oi` observation window opens once B1-2's overlay reads real headers (coordinator+operator, before park-timing trust).

## B3 packet queue (cut 2026-07-20 from S19.5 + S08/S09 scopes + the codor standing directive; P3-B2-5 first per the B2 close-mode answer)

Model routing: none of S07.7/S08/S09/S04-slice/S05.3 are S10/S11 security-dense → all inherit (Fable); Opus only as a per-packet re-cut on a demonstrated safeguard trip (standing policy). **Coordinator reading (runbook ambiguity rule):** S04 carries no S19.5 phase row; its spawn/helper/stage-sequencing slices build here at B3 where their named consumers land (S08.8 "spawn mechanics are S04's"; S05.3 recitation mechanism; B2-4 readings 1/10 predicted exactly this) — the remaining S04 scope (mass mechanical fan-out ≥50) stays parked post-v0 per S19.4.

| Packet | Title | Read-first sections | Acceptance (headline) | Status |
|---|---|---|---|---|
| P3-B2-5 | S07.7 resume-in-place — answering non-intake asks | S07.6–S07.7; S02.3/S02.5 (resume, generation, ratified cancel mapping); S06.9 + `intake/answer.go` precedent; gate report §6 | accept_best_effort / revise_with_guidance / cancel applied via the answer API on verify-class asks (CAP-HIT first); pipeline resumes in place, generation-fenced; guidance → numbered findings into a FRESH bounded round (caps/convergence still bind); intake answers regression-clean; planted-route e2e per verb; `~/sinet-demo` card readable as fixture reference, never modified | done |
| P3-B3-1 | S09 memory/knowledge substrate + write gate + seed governance | S09 full; S05.4 (Sources seam); S02.2; B1-4 spike result (auto-memory find) | knowledge schema migration (entries/proposals/adoptions + git knowledge dirs); layer/writer capability enforcement (workers structurally cannot write L2); server-scoped FTS5 `knowledge_search` + standing cross-user-read-must-fail conformance test; house/project/user injection via S05.4 with ⚙ budgets + `over_budget_dropped` manifests + curation cards; manual L2 + provenance/removal/true-deletion + precedence labels + write-time conflict cards (contradiction screen = B4 seam); v1 stations DORMANT (schema only); S09.9 containment incl. config-root `memory/` wipe/redirect (closes TBD-P3 auto-memory containment); B2 seed objects re-homed under S09.10 governance with the B2-gate ratification recorded; 12 ⚙ `memory.*` consumed | done |
| P3-B3-2 | S08 worker store + guardrail split + per-invocation compile + composer battery | S08.1–S08.7, S08.9–S08.10; S03.5 lowering; S09 (overlay + playbook homes); S11.6 class table (read-only) | store tables migration + template file format (md+YAML) + static-only skills; guardrails EXCLUSIVELY control-plane rows, structural lint reject on guardrail fields in any file; per-invocation hash-pinned compile through S03 lowering (sole-controller preserved); template→overlay→instance on S09 machinery; four-station battery (schema lint + instruction-pattern screen; permission audit vs v0 ceiling table + Rule-of-Two; ⚙-capped sandboxed dry run; approval-as-diff card); first-N counters; `domains` rows + degraded-mode structural enforcement; kind=automation minimal dialect (parser + on-demand executor, no model in loop); import battery; 4 ⚙ `workers.*` consumed | done |
| P3-B3-3 | S08.8 routing + S04 spawn/helper slice + no-fit/gap + serialize-by-deny reconfirm | S08.8; S04 (spawn mechanics, helper topology, D6 shape); S03.2; S12 duty seams; B1-4 spike-1 harness | deterministic selection (selector match → tie-break seam → model/effort vs duty maps), subscription-coverage-bound (metered list EMPTY, D5: pressure never dollars); `routing.decided` with plain_reason; selection visible + re-route/pin on the plan card pre-execution; no-fit two-stage card (generalist default / compose-when-earned / gap advice) + gap records; helper routing same pipeline, reports land in coordinator L0 (D6); stage dispatcher consumes real selection (DefaultModel stub + 200k window constant retire to worker rows); serialize-by-deny reconfirm on the chosen default worker → measurement file closes TBD-BRINGUP(parallel-gate fallback) or escalates | done |
| P3-B3-4 | Codor C1-recitation channel + C3 conformance rows (C5 optional) | Research/18 §7 (ALL seven C1 conditions bind); S05.3–S05.4; S03.4; B1-4 result file; CONVENTIONS §12–§13 | PostToolUse probe FIRST (~$0.02, B1-4 method, result file in `P3/measurements/`; PASS → per-pin canary row, FAIL → stage-boundary-only fallback, both spec-satisfying); recitation = platform-authored ledger `state`/`next_actions` at pinned version via ctl-dir pending-file (atomic rename, read-and-consume) → `additionalContext`; ⚙ `context.recitation_interval_turns` consumed (turns-dueness from run events; hook = delivery valve only); every delivery manifested + journaled; PreToolUse untouched; settings-ro leak-test re-asserted; C3: interaction-boundary crash cases + error-output-never-becomes-stage-output negative row + dated engine-fact evidence notes; C5 advisory copy-aside peek only if slack | done |
| P3-B3-5 | S08.6 composer generation + S05.3 stage-split execution (B3 remainder, coordinator-cut 2026-07-21 — discovered by B3-3's remainder sweep: neither sat in any packet's acceptance list) | S08.6 (one-shot rule, inputs-by-policy); S09.10 (composer-playbook object); S05.3 (overflow→split contract); B2-4 budget watcher; B3-2 battery entry points | composer one-shot generation behind an engine seam (ceremony, planning-model class per duty map; fake-engine tests, ZERO paid) producing draft template + requested grants + dry-run sample + golden seed INTO the B3-2 battery; composer playbook seeded as a governed S09.10 house object (content = B3 gate ratification item); no-fit card compose verb wired end-to-end; S05.3 stage-split execution (accepted proposal → real sub-stage briefs via ledger, budgets re-checked; overflow e2e) | done |

## B4 packet queue (cut 2026-07-22 from S19.5 + S13/S12 full-reads + carried seams (B3-report §4) + B2-3 deferral record + D3 gate riders)

Model routing (2026-07-22 pipeline directive): every packet's executor+finalizer = **opus**; grounding+evaluation inherit (Fable) — except B4-3 (S13.6 broker git verbs + S13.10 crypto/escrow = S11 broker internals) and B4-6 (S12.6 sandbox-bridge/token plane = S11 deep surface), where ALL FOUR stages run **opus** per the standing memory rule; evaluation additionally falls back to opus, losslessly, on any classifier trip. Paid legs live ONLY in B4-7 (pre-registered riders + battery escalation smokes) and B4-5's one tier-L local smoke ($0 by construction). Sequential by default; S13 packets (1–4) before S12 packets (5–7) per S19.5 listing order — this also gives the operator escrow lead time before B4-3's live snapshot leg.

| Packet | Title | Read-first sections | Acceptance (headline) | Model | Status |
|---|---|---|---|---|---|
| P3-B4-1 | S13.1–S13.4 deliverable/revision/comment schema + per-type review behavior + findings→retry drain — **done + validated (`e60ddfb`)** | S13.1–S13.4; FC-v1 §2; S02.2/S02.9; S07.6 retry package (B2-3 `verify/rework.go` precedent); S08.7 approval-as-diff station (B3-2); S05 brief seam | Migration 0007 (`deliverables`/`deliverable_revisions`/`review_comments`): immutable revisions 1..N, lineage never compressed, content pins both kinds (snapshot-sha + content-addressed object dir + `refs/sinet/deliverable/` namespace), minting wired at the S07 verification handoff; per-type comparison behavior host-side (line diff via revision pins; PDF extracted-text MIT-only via the rail; image trio metadata; binary hash cards; labeled fallbacks — no type refused); anchor model: server-side re-validation, 5-step re-anchoring ladder, ORPHAN explicit + still-drains, per-revision anchor status, no-comment-without-render-location invariant, escape-first contract recorded; lifecycle open→consumed batch stamps; exactly ONE drain path numbering [F1..Fn] into the next attempt's brief (human comments + verify findings one schema; delivery never conditional on anchoring); composer approval-as-diff rides the schema; ⚙ `review.anchor_drift_lines` consumed | inherit | done |
| P3-B4-2 | S13.5 + S13.7 git topology + snapshot commits + project/repo registry + carried memory seams — **done + validated (`5056c62`+`a860445`)** | S13.5, S13.7; S02.10, S02.4(d), S02.6; S09.6 + B3-report §4(b); S06 intake resolution seam; S05 pinned constraints; `memory.Committer` seam (B3-1) | Branch-per-pipeline + platform-owned tree-level snapshot commits (junk-excluded) at checkpoint/stage/round boundaries as the S02.4(d) artifact ref; never amend/force-push under review; fresh-branch-per-attempt + retention GC rules; utility checkouts (lock/prune/never-delete-dirty); `repo_registry` migration + onboarding-as-task (register→clone→scan→draft conventions/commands/danger-zones→owner D10 approval); registry feeds intake resolution, ledger pinned constraints, workspace creation, broker protected-ref policy data, preview commands; `memory.Committer` live (knowledge-dir git-on-approval); S09.6 cited-entry versions join the S02.6 freshness fingerprint (carried seam closed) | pipeline: exec opus | done |
| P3-B4-3 | S13.6 + S13.9 + S13.10 broker-mediated accept + follow-ups + encrypted snapshots/restore drill — **done + validated (`2218954`+`92d855b`)** | S13.6, S13.9, S13.10; S02.7 (class-A exemplar), S02.8; S11 broker (B1-3); S01.7 timers; G2 D2.5/D2.6 | Accept = one High-tier action through the effect journal (applies-cleanly depth-1 queue → merge card 3-option, platform squash → one attributed commit, deterministic trailers displayed pre-accept, broker CAS `--force-with-lease=<ref>:<expect>` + `--atomic`, lease failure → back to merge card never blind retry); **P-T12-1 conformance row: non-accept push to a protected ref observes broker-side refusal**; SSH signing (operator on; all-or-nothing rule enforced; gitsign never); enrollment steps documented; S13.9 `successor_of` links + intake presets + claims/freshness interplay; S13.10 pipeline durable-set→`VACUUM INTO`→text dump→tar|zstd|age→one blob→CAS push, `snapshot_ledger` SHA-256 rows in DB AND in-archive, keep-N; restore drill: fetch→ledger verify FAIL-CLOSED→decrypt with ESCROW identity→rebuild→integrity+S02.9 invariants; age/zstd adoptions via the rail (live-verified pins, Go-module preferred); dev-mode = local bare-repo fixtures; **the LIVE first snapshot push blocks on operator escrow (item 6)**; ⚙ `backup.*` consumed | pipeline: ALL opus | running |
| P3-B4-4 | S13.8 previews — **done + validated (`372d2ba`+`6789033`+`5024709`)** | S13.8; S01 (portpool file-stub, Caddy admin API, front chain); S11 composition (read-only consumer); S02 worktree-at-rev; S13.7 commands | Runner heuristics (package.json→pnpm/npm, pyproject→uv, mise.toml→mise, index.html→static) executed inside the settled sandbox stack; zero-mutation: overlay upper / throwaway clone, teardown deletes, reviewed workspace untouchable (tested); netns port probing (config never trusted; multi-port → picker data); portpool consumption (file-stub OK dev-mode); Caddy admin-API route add/remove with WebSocket upgrade headers + `allowedHosts` injection, port-based never path-prefix; `systemd-socket-proxyd` idle-stop lifecycle (unit files GENERATED); before-vs-after dual instances (accepted worktree-at-rev + candidate) with synced-navigation data contract; ttyd CLI lane via the rail (or the honest explicit no-preview state per type); Low-tier reversible, tailnet-front-chain exposure only; ⚙ `preview.idle_stop`/`preview.max_concurrent` consumed | pipeline: exec opus | running |
| P3-B4-5 | S12.1–S12.4 + S12.8 local serving stack + model set + duty-alias registry — **done + validated (`aeefd3b`+`089c759`)** | S12.1–S12.4, S12.8; S03.2 (lane facts; adapter reading in phase status); S16 rail; S06.10; S01.2 unit slot; G3 Def.8/D3.5 | llama-swap + llama.cpp adopted at live-verified pins (user-level install/build; toolchain path live-resolved per phase-status env facts; **no driver/kernel/system-CUDA changes ever**; genuine host-package need → flagged operator decision); S12.3 model set downloaded with card-verified licenses (disk check first; embedder NOT pulled — Def.8 gate closed); config-gen: GPU-UUID placement, pool12 swap group, `resident` empty, per-model TTL ⚙; eager-unload verb (stop admissions + unload; card + CLI); GameMode hook config + D-Bus signal subscription + **TBD-P3(GameMode user-context probe) result file** (gamemoded present — live-executable); `sinet-llamaswap.service` GENERATED not installed; duty-alias registry: ⚙ `local.alias.<duty>` map, platform-plane /v1 client (schema-at-engine `json_schema`/GBNF, temp-0 greedy for classification shapes, explicit abstain members, free-text-then-constrained, length caps), every call a $0 D7 ledger row + zero-allowance receipt lines (pressure floor); utility seat joins the duty map + routing TieBreak seam gets the real local-alias impl (B3-3 degradation retires where consumers exist); fake-/v1 test surface + ONE live tier-L local smoke ($0) | pipeline: exec opus | done |
| P3-B4-6 | S12.6 + S12.7 GPU broker + VRAM ledger/admission/preemption | S12.6, S12.7, S12.8 (admission ⚙); S11 (bridge injection, class table); S10 admission-policy hook (B1-2 seam); S12.2 | Two planes: platform direct loopback (management verbs ONLY here); sandboxed plane per-run bearer tokens minted at spawn (duty-alias-only model field, allowlist, rate/size limits, expiry=run lifetime) over a per-sandbox loopback/unix bridge injected by the sandbox runtime; `/v1/models` returns the run's allowlist only; logprobs excluded for sandboxed callers (⚙ `local.broker.sandbox_logprobs` off); token→run→owner resolution writes every call to the D7 ledger; VRAM ledger machinery: UUID pool enumeration, **sleep-gate every read** (`runtime_status` first — a suspended GPU is never polled), device-truth `memory.free`, admission = live reading ≥ ledger + ⚙ guard band, post-kill recovery verified before next admission; kill-not-freeze: SIGTERM → ⚙ grace → SIGKILL mandatory path; battery-mode admission ⚙ `local.battery.gpu_admission`; scheduler GPU-admission policy hook filled (B1-2 seam); C0-never/C1-grant/C3-tighter enforcement per the S11.6 class table | pipeline: ALL opus | pending |
| P3-B4-7 | S12.5 + S12.9 + S12.10 confidence/calibration + T15 battery + consolidated B4 measurements + D3 riders | S12.5, S12.9, S12.10; S09 (contradiction seam, B3-1); S07 + B2-3 seeds + `P3/measurements/2026-07-20-verification-seeds-deferral.md`; B1-4 spike-1 harness; 7.3/S08 revalidation flags | T15 battery harness versioned in-repo (~30–50 labeled cases per registry duty, synthetic seeds; `llama-perplexity --kl-divergence` quant check); margin→isotonic→threshold calibration keyed (duty, model hash, engine build); swap gate: S12.10 promotion rule + **swap⇒recalibrate hard block (P-T15-1)** + 7.3 revalidation firing S08 flags; then EXECUTE on this machine (AC power, G3 Def.8 result files in `P3/measurements/`): workhorse bakeoff (Qwen3.5-9B vs Gemma-4-12B-QAT), VRAM-ledger calibration (per-model footprints, hybrid+MUX headroom, kill→free latencies), CPU-tier throughput floor, contradiction-screen P/R (`memory.ContradictionScreen` seam goes LIVE), entailment thresholds (Guardian vs MiniCheck on Sinet-domain pairs; Flan-T5 CPU floor verdict; sets ⚙ `verification.entailment_sample_rate` + the mandatory-coverage bar — closes the B2 TPR/TNR deferral), per-duty confidence calibration; **D3 gate riders (paid, pre-registered):** P-T06-5 golden-set run on the ratified opus-4-8 judge + the B2-3 length-bias check; serialize-by-deny E3 leg on the sonnet-5 executor (B1-4 harness scale) | pipeline: exec opus | pending |

**B4 measurement rows (S19.6):** the consolidated G3 Def.8 battery above (bakeoff, VRAM calibration, CPU floor, confidence calibration, contradiction P/R [S12, S09]) + entailment thresholds (S12.9 row 5) + the carried B2 rows (TPR/TNR, judge calibration, length bias) + both D3 riders — all land at B4-7; GameMode TBD-P3 probe lands at B4-5.

## Operator hands-on items (carried from G4)

| # | Item | Due at |
|---|---|---|
| 1 | Z.AI dashboard prompt-unit calibration (5-step recipe in P2-S1 report §Blocked) | B1 (metering trust) |
| 2 | ts.net hostname pick — **FULLY DONE 2026-07-19: `sinet`** (spec amendment A1) **and Tailscale machine renamed** (was `sinep-predator`); FQDN `sinet.tailfd0b1e.ts.net`. First cert issuance remains a B0-gate step | done |
| 3 | Root/reboot/suspend probe batch — **reboot-survival DONE + `Persistent=` catch-up reboot leg DONE 2026-07-20** (both match spec; result files in `P3/measurements/`); remaining: catch-up suspend leg (pair with battery-drain suspend; probe timer left installed) + user.slice freeze/thaw | B0/B1 gate |
| 4 | Week-one push drill on household phones | first deploy |
| 5 | (Optional) GitHub Verified badge — signing-key upload | anytime |
| 6 | age identity escrow (S13.10) — **DONE 2026-07-22.** Live identity `~/.config/sinet/age-identity.txt` (0600), recipient pubkey `age1n0p60jcwpzwlqky48wdy8jpcvn695halrjmnmcg50pxp5qwhrpmqsrhatk` (B4-3 reads identity/recipient from this path, never from prose); age v1.3.1 CLIs user-level (ceremony tooling, not a platform dep — B4-3 adopts the age Go module via the rail). Operator ran the guided ceremony (paper copy + passphrase-encrypted copy, script-verified round-trip) and declared complete (free-text, authoritative): encrypted copy on operator-held off-host storage device; cloud upload declined — off-host requirement satisfied. Staging copy verified deleted from host; live-key fingerprint re-verified. The B4-3 restore drill decrypts with escrow material = the designed live proof | done |
| 7 | Observables-register sign-off — **SIGNED 2026-07-20** (operator "go" at the D2.2 cert execution; 3-row v0 register — rows 1/3 walked explicitly; row 2 push metadata noted as not-yet-live). Nuance recorded: the cert/CT publication had already happened 2026-07-19 21:27 UTC via the leftover serve config — signature covers it retroactively (B0-cert-steps execution record) | done |

## Log

- 2026-07-22 — **P3-B4-5 DONE + validated → LANDED, pushed → SESSION 2026-07-22(c) HANDOFF (operator stop directive honored).** Packet = `aeefd3b` (36 files +3660/−34) + drain r1 `089c759` (23 files +707/−186; never amended). Pipeline record: grounding `7b44c0b` (9 readings ratified) → executor green battery but REJECTED deferral of the sanctioned heavy legs → evaluation FAIL (12 findings; independently confirmed no genuine blocker; found the helper-metering bug, hollow tier-R, half-wired GameMode pair, unrealized reason-first rule, spot-check mis-alias) → drain r1 EXECUTED the legs (user-level CUDA 12.9 + micromamba gcc-14 + b10085 sm_120 build; 9B/4B/Flan-T5 pulled+hashed; REAL GPU tier-L smoke) + all fixes → re-check **PASS** (evaluator ran tier-R AND tier-L itself on the GPU; hashes matched its own sha256sum; 401/throttle blockers probed genuine; host-gate re-audit clean; 4 residuals: leftover processes → coordinator killed at landing + /tmp dir to operator; Ollama-#10-at-activation → RATIFIED; Logf order check → B4-7 bump-procedure hardening; snippet env → gate-install step). Coordinator landing: independent battery 33 pkgs 0 FAIL; `-race` clean on all 7 touched; lockgate 17 entries; 118 tally; smoke evidence = two independent live GPU runs this session + coordinator's sanctioned-skip dev-default confirmation. Zero paid engine calls all packet ($0 local smokes only). **Session totals 2026-07-22(c): FOUR packets landed + validated (B4-2, B4-3, B4-4, B4-5 — 12 packet commits), every evaluation round caught real defects behind green batteries (15+9+22+12 upheld findings; zero false positives across all four), 2 spec-fact conflicts surfaced honestly (Gemma license cell; S12.4 spot-check row), 5 adoptions landed at live pins (age, zstd, ttyd, llama-swap, llama.cpp + Ollama standby), pipeline directive proven.** Next session: B4-6 grounding (ALL-opus), then B4-7, then the B4 gate presentation with the accumulated batch. → NINE READINGS RATIFIED → EXECUTOR LAUNCHED (opus).** Brief: 29 reqs, 25-item rubric, no blockers; 3 lock entries expected (llama-swap organ + llama.cpp engine at live pins, Ollama standby per S16.2) → 17; 4 ⚙ consumed (`local.alias` TypeMap, both TTLs, `local.gamemode_hook`); models manifest = platform data (S16.1), weights never committed; Gemma card-vs-spec license cell = flagged-conflict path (R8); toolchain = live decision tree (prebuilt CUDA → user-level toolkit+build → docker image; every host-package edge stop-and-flag); GameMode probe result file per Def.8; ONE $0 tier-L smoke. Ratifications logged in phase status; reading 2 (tie-break on `utility`) carries a possible gate-time S00.9 registry-amendment alternative — operator's call at the gate. Executor: long-running (downloads ~30 GB after disk re-check + possible source build). (Fable-inherit; background).** S12.1–S12.4 + S12.8 local serving stack + model set + duty-alias registry. Read-first per queue row (+ G3 Def.8/D3.5 records; S03.2 lane facts; the phase-status S12.1 consumer-class reading BINDS — no second engine adapter in this cut). Sanctioned network legs: llama.cpp/llama-swap live pin resolution (toolchain path live-resolved — no nvcc on host: prebuilt CUDA binaries / project container via docker / user-level toolkit; NEVER driver/kernel/system-CUDA), S12.3 model pulls with card-verified licenses (disk re-check first; embedder NOT pulled — Def.8 closed). GameMode TBD-P3 probe live-executable (gamemoded present) → result file in P3/measurements/. `sinet-llamaswap.service` GENERATED never installed. ONE live tier-L local smoke ($0). Grounding must scope: user-level-only installs, the flagged-operator-decision path for any genuine host-package need, ⚙ `local.*` families consumed, and the B3-3 TieBreak/utility-seat retirement wiring.
- 2026-07-22 — **P3-B4-4 DONE + validated → LANDED, pushed.** Packet = `372d2ba` (+3965) + drain r1 `6789033` (+859/−247) + drain r2 `5024709` (+169/−35); never amended. Pipeline record: grounding `adb74e4` (3 readings ratified) → evaluation FAIL (22 findings, all upheld) → r1 all-22 applied → re-check: 22/22 genuine but NEW observed N1 flake + F3 residual → r2 (cap) → FINAL re-check **PASS** (flake gone across 9 reproduction batches; fail-closed checkout assert; nits-only remainder recorded: setup-guard skip, benign double-release, cosmetic List visibility, dead-code argv belt, TempDir fallback). Coordinator landing: independent battery 32 pkgs 0 FAIL; `-race` clean preview/portpool/cli/shell/api; lockgate 14 entries/15 deps; 118 tally; spot-reads bindStatic advance + squatter-release + checkoutFS wiring. First full-cap packet (2 drain rounds); zero paid calls; host untouched (ttyd pinned 1.7.7, NOT installed). Gate batch += ttyd #10 + host install; static socket-unit pairs + portpool install; live Caddy admin config; tailscale preview-range mapping. → FINAL re-check launched.** R2-1: preview/shell test packages on disjoint ranges (47700–19 / 47800–19, neither on the 47600 default) + `bindStatic` EADDRINUSE-advance (squatter held reserved, next allocated, exhaustion→at-capacity; deterministic pre-bind test; parallel ×8 clean); R2-2: TestZeroMutation direct-asserts the utility checkout released clean (manager-side checkout write now reddens on its own); R2-3: List() comment matches shipped lock+link, contention test 40 fresh rounds/invocation with honest comment, F7 propagate-branch test. Reported battery green ×2. Cap reached: re-check survivor policy = coordinator-inline. (none weakened; portpool atomicity interleaving-walked + 100x-race-proven by the evaluator; all caps-gated legs ran live PASS incl. hard-fail probe, overlay flags, real-lifecycle zero-mutation) → narrow FAIL → DRAIN ROUND 2 launched (the cap; coordinator-inline after).** New: **N1 (low-med, OBSERVED red once in six batteries)** — preview + shell test packages share the structural default range 47600–47619, run in parallel, file-stub reservations are per-package-private so the OS port is the unseen shared resource, and `launchStatic` hard-errors on EADDRINUSE instead of advancing; disposition: disjoint per-package test ranges via the F18 env machinery + EADDRINUSE→next-port in the launch loop (production squatter robustness), test the advance. **F3 residual (low)**: post-launch revision-checkout pristineness asserted only by construction; disposition: direct hash/emptiness assert in the real-lifecycle test. Nits N2–N4: stale tombstone comments + unencoded "100x" claim; pre-live List visibility (cosmetic, optional); F7 propagate-branch test. Round-2 scope is exactly these; wholesale re-check confirmed round 1 drain-only (no deps/⚙/routes/adopted-code/host). → evaluation RE-CHECK launched (same agent).** Executor follow-up (`372d2ba` untouched): F1 `Manager.RenderUnits` reads ⚙ by key; F2 CLI lane clones; F3 zero-mutation via REAL lifecycle + symlink hashing; F4 probe hard-fails; F5 production tree-walk probe; F6 truthful reasons; F7 ErrBadInput split; F8 t.Setenv pin; F9 StopWhenUnneeded dropped w/ reasoning; F10 lane coverage; F11 atomic per-port-lock reclaim (100x -race contention test); F12 trim guard; F13 symlink-safe checkoutFS; F14 portpool AST wall; F15 overlay behavior-assert; F16 state-dir scratch; F17 api holds surface; F18–F22 folded. Reported: 32 pkgs 0 FAIL, lockgate 14, 118 tally, front chain untouched. Re-check scope: each F resolved per disposition (not weakened), no new defects, wholesale drain-only diff, battery independent. F1–F22 ALL upheld (zero false positives, 3rd packet running) → DRAIN ROUND 1 (SendMessage to executor; cap 2).** Verified solid: battery green independently, host-hazard audit clean (no live admin/8481/8482/tailscale/unit-install anywhere; ttyd absent from host; loopback asserts real), live netns probe PASS, ttyd 1.7.7 pin re-verified, §8 readings honored, heuristics/registry-precedence/Caddy-client/static-lane/events/mode-table all proven. Above-nit findings + dispositions: F1 ⚙ `preview.idle_stop` DEAD in production (RenderProxydUnits zero non-test callers) → wire registry-read unit rendering into the surface + test; F2 CLI lane composes the read-only checkout as rw C2 → interpose the clone like dev-server; F3 zero-mutation proof bypasses Manager lifecycle + hashTree symlink-blind → wrap the real launch→teardown (caps-gated) + hash symlinks; F4 live-probe genuine-failure-as-SKIP → hard fail when caps present + bwrap started; F5 tree-walk probe is test-only, production probe single-pid (would read HOST listen table incl. 8481 when consumed) → promote walk to production fn, live test consumes IT, fix §25/report overstatement; F6 "no code change to go live" reason text false for dev-server/CLI (deferred posture itself accepted per reading 5) → truthful reasons/comments; F7 LaunchComparison swallows real AcceptedRevision errors → ErrBadInput=single-instance, others propagate; F8 composition test can dial live admin via ambient SINET_PREVIEW_CADDY_ADMIN → t.Setenv pin + guard; F9 StopWhenUnneeded on the proxy is wrong systemd semantics (spec names mechanism, not placement) → per-semantics fix + doc, live proof stays bring-up; F10 launchCLI zero manager-level coverage → lane tests; F11 portpool stale-reclaim cross-process race → atomic rename-claim; F12 whitespace-only registry command panic → trim+empty-argv guard; F13 static server follows symlinks out of the checkout (host files to tailnet) → symlink-refusing FS wrapper + test. F14–F22 low/nit all folded (portpool AST pin; overlay behavior-assert or honest comment; clones under state-root scratch not /tmp; previewSurf actually held by api (acceptSurf pattern); shell range override; neutral port literal in serve_test; capacity check under lock; Stop partial-failure retryable; routing-disabled reason string). → EVALUATION LAUNCHED (stage 3, Fable-inherit, background; opus fallback armed).** One commit, 27 files +3965/−22: `internal/preview` (dispositions + 4-lane heuristics w/ registry-command precedence; zero-mutation checkout+clone; proc-net TREE-walk port probe — bwrap monitor sits in host netns, live probe leg PASS not skipped; Caddy admin client contained in-module, port-based never path-prefix; socket-proxyd unit TEXT generated-never-installed; lifecycle manager consuming both preview ⚙ keys; before-vs-after synced-nav DATA contract; ttyd lane probe-gated) + `internal/portpool` (file-stub allocator + `sinet portpool` loopback/UDS daemon mode — reserved mode table now EMPTY) + shell composition wiring (compiled-in). Reported: 32 pkgs 0 FAIL, -race portpool/preview/cli/shell, lockgate 14 entries (+1 ttyd 1.7.7 MIT full S16.4 walk), 118 tally, zero network/paid, no deviations. Gate flags confirmed: ttyd #10 + host install; structural admin-endpoint/port-range config. Evaluation gets brief `adb74e4` + range + report; then triage/drain → landing., 24 reqs / 25-item rubric, no blockers) → THREE READINGS RATIFIED → EXECUTOR LAUNCHED (opus).** Ratified (runbook ambiguity rule): (1) socket-activation production shape = FIXED small port range + static `.socket`+proxyd unit pairs installed ONCE at the gate — S11.8's complete grant explicitly withholds `manage-unit-files`, so dynamic per-preview units are impossible without an anti-recommended S00.9 grant widening; packet emits parameterized unit text, install = gate step. (2) ttyd SHIPS v0 probe-gated (S13.8 body names the CLI lane normative; only desktop-GUI deferred); no S16.3 row → S16.4 net-new (zstd precedent); lock entry in-packet, #10 + host install = gate, either outcome zero code change. (3) Caddy admin endpoint + port range = structural deployment config (S18 ratifies exactly 2 preview keys; 6th settings-tab-precedent instance). Readings 4–5 (previews-are-not-runs; settled-sandbox-surface) = direct spec application, noted. Gate batch grows: ttyd #10 + host install; static unit-pair + portpool install; live Caddy admin config; tailscale serve preview-range mapping; hardening session precedes production previews. Executor duties: ttyd pin live-verify (no host install), bwrap overlay-flag behavior-assert on host 0.11.1, netns proc-probe live row (caps-gated), no-8481/8482 tripwire, zero network/paid. (Fable-inherit per routing; background).** S13.8 previews. Read-first per queue row (S13.8; S01 portpool file-stub/Caddy admin API/front chain; S11 composition read-only; S02 worktree-at-rev; S13.7 commands). Hazard flagged in the prompt: LIVE production Caddy on this host — tests hermetic (never the live admin API); units GENERATED never installed; ttyd = potential rail adoption OR the honest no-preview state (grounding decides from spec text); zero paid calls.
- 2026-07-22 — **P3-B4-3 DONE + validated → LANDED, pushed.** Packet = `2218954` (41 files +5222) + drain `92d855b` (20 files; 2218954 never amended). Pipeline: grounding `8d7e82a` → executor FAIL evaluation (F1–F9 upheld: unreachable accept surface, missing depth-1 stacking, per-call signing flag, undriven replay, timer-clamp gap + nits) → round-1 full fix → re-check **PASS** (wholesale drain-only scope verified — the classifier-outage round got its dedicated sole-reviewer pass; regression scan: all round-0 security properties byte-unchanged; N1–N3 benign observations recorded). Coordinator landing: independent battery 30 pkgs 0 FAIL; `-race` green all 11 touched pkgs; gofmt/vet/build clean; lockgate 13 entries/15 deps; 118 tally; personal source spot-reads (classifier-outage proportionality): `kindOf` returns kind-only never decrypting, `buildAcceptSurface` real-broker/real-store seams, api.go handles-no-routes. Rail: +2 entries (age v1.3.1 + klauspost/compress v1.19.1, live-verified; operator #10 → B4 gate). Gate batch grows: adoption ratifications ×2, trailer-constant ratification, enrollment pubkey upload, snapshot-repo designation, broker git-key enrollment, rotation ceremony. Zero paid calls. Second pipeline packet: FAIL→drain→PASS again caught real defects (reachability class this time) behind a green battery. → evaluation RE-CHECK launched (same opus agent).** Executor follow-up (2218954 untouched): F1 `shell/accept_seams.go` `buildAcceptSurface` over production stores/broker seams, handle held by `internal/api` for B6 (no endpoints), `internal/accept` now in `go list -deps ./cmd/sinet`, composition test incl. sibling-accept fire; F2 per-project in-flight registry + `projectOnto` (HEAD-plus-first both legs tested); F3 `SigningPosture` from broker git-key presence via secret-free `OpHasKey`, per-call flag REMOVED, mixed-history-impossible test; F4 self-sufficient class-A payload + `ReDrive`/`ReDriveApproved` wired at shell startup after `ladder.Reconcile`, same-pinned-sha + ref-moved→card both tested; F5 comments; F6 ratified-reading doc + both-sides-survive test; F7 generation total over the ⚙ clamp (7h/9h/25h tested, wrap documented); F8 accept import-wall AST test; F9 exact FTS shadow enumeration + loud unknown-prefix failure. Reported: 30 pkgs 0 FAIL, -race 11 pkgs, lockgate 13 no new deps, 118 tally, zero network/paid. **Harness classifier outage during the executor run — no automated safety review; re-check agent told it is the sole reviewer and instructed to verify the diff WHOLESALE (only drain work, no scope additions) on top of the per-finding dispositions + full regression scan.** F1–F9 ALL upheld → DRAIN ROUND 1 (SendMessage to executor; cap 2).** Verified SOLID by evaluation (opus): P-T12-1 broker-side refusal genuine (remote ref unmoved), git key never leaves (OpResolve/OpSign refused for git-ssh-key), CAS-always incl. unprotected refs, lease-reject→merge card+Failed, SSHSIG crypto-verified via git verify-commit, no key material (grep), zero network, secrets-excluded test plants+asserts, drill fail-closed, pins independently proxy-verified (age v1.3.1/hpke v0.4.0/compress v1.19.1 — all latest), both deviations justified, migration 0009 + snapshot/drill wiring clean. Findings: **F1 (MED)** accept/follow-up unreachable — internal/accept absent from `go list -deps ./cmd/sinet`, no production constructor call, FireSiblingAccept fires only from unwired code; disposition: wire Accepter+FollowUp at the shell composition root with real seams (compiled-in + composition-test per the B4-2 F9 precedent), HTTP surface stays the named B6 seam; **F2** depth-1 HEAD-plus-first stacking = spec-verbatim behavior, implement + test (approved-in-flight first → second validates onto first's result; first-fails → revalidate); **F3** signing posture must be a per-user durable fact (operator=on day one; never caller-supplied per-call) — structural no-mixed-state + test; **F4** crash-window replay: production reconcile must re-drive the push against the SAME pinned expect-sha — driver + test (crash→reconcile→same-sha; lease-fail variant→card); **F5** stale internal/stage doc comments; **F6** merged-tree-on-moved-HEAD reading RATIFIED (raw-tree would silently discard interim work — contradicts S13.6's own no-silent-overwrite; log-recorded) — add the clean-divergence test + doc; **F7** every clamp-valid backup interval must generate a valid unit deterministically (no fail inside clamps; honest documented wrap); **F8** internal/accept AST wall test (R29); **F9** DumpFrom shadow-skip made exact-or-loud. F10 observation only. → EVALUATION LAUNCHED (stage 3, opus per the ALL-opus routing, background).** One commit, 41 files +5222/−12: new `internal/accept` (applies-cleanly depth-1 → 3-option merge card → pinned deterministic trailers → platform squash → class-A journal effect before broker CAS push → review→accepted + sibling-accept fire); broker git verbs (OpPush/OpSignData/git-ssh-key kind, CAS-always, protected-ref-requires-authorization, SSHSIG — key never leaves) + the mandatory P-T12-1 refusal test; new `internal/backup` + storage snapshot triad (VacuumInto/DumpFrom/RebuildFromDump/CheckRestored; tar|zstd|age; snapshot_ledger DB+in-archive; fail-closed drill w/ escrow-identity decrypt + S02.9 invariants); migration 0009 (snapshot_ledger + bidirectional task_successor_of); intake.FollowUp presets; run.FireSiblingAccept; sinet snapshot/restore-drill modes + generated Persistent=true timers. Pins LIVE-verified via proxy.golang.org: filippo.io/age v1.3.1 (transitive = filippo.io/hpke — live-verify caught the brief's edwards25519 guess), klauspost/compress v1.19.1 CGO_ENABLED=0-confirmed. Reported battery: 30 pkgs 0 FAIL, -race 10 touched pkgs, lockgate 13 entries/15 deps, 118 tally, gates untouched, key-material grep clean, zero network/paid. TWO declared deviations for evaluation scrutiny: (a) accept orchestration in NEW `internal/accept` (R27 premise about stage's imports didn't hold — stage is func-seamed); (b) storage triad additions (driver-seam-mandated; brief §4 non-exhaustive). Coordinator verified: range 8595408..HEAD = exactly `2218954`; tree clean modulo the two operator-disposal dirs. → coordinator sanity pass OK + TWO BOUNDARY READINGS RATIFIED → EXECUTOR LAUNCHED (stage 2, opus, background).** Brief: 29 requirements, 26-item rubric, no hard conflicts; live-verified seam audit (gates ClassA + journal verbs; broker stdlib+x/sys only; project ProtectedRefs/utility-checkouts/hermetic core; review StateAccepted enum sans verb; run.EvaluateFreshness SiblingAccept consumer shipped; 0009 next; backup.* keys declared-unconsumed since B0-2; age S16.3 row present, zstd absent; escrow identity present, contents never read). **Coordinator ratifications (runbook ambiguity rule, logged):** (1) zstd enters via the S16.4 onboarding checklist — S16.3 pre-registers no zstd row; S16.4 IS the ratified path for that, operator #10 at the B4 gate (CONVENTIONS §4). (2) S13.6's "trailer text in the settings registry" ships as STRUCTURAL string constants + rendered-trailer pinning in the effect payload at approval — S18's frozen 118-key index ratifies no trailer key; identical to the four prior ratified instances (B0 auth constants, §7 sseBatchSize, B1 scheduler constants, B4-2 junk/GC rules) under the standing settings-tab directive; INTERIM pending the clamped-S18 amendment; B4-gate ratification item. **Operator hands-on batch for the B4 gate (from brief §6):** enrollment pubkey→GitHub upload; private snapshot-repo designation/creation + remote URL; broker git-key enrollment; annual `backup.repo_rotation` ceremony. LIVE first snapshot push = bring-up step, pure-config by construction, executor performs no network push. Executor duties beyond the template: live-verify age+zstd exact pins against live sources, full S16.4 walks, tests on temp keypairs only, real identity file never read. (ALL-opus pipeline per the standing S10/S11-density rule; effort max, background).** S13.6 + S13.9 + S13.10: broker-mediated accept + follow-ups + encrypted snapshots/restore drill. Read-first per queue row incl. the B2-gate D2.5/D2.6 records in `P3/gates/`; code grounding: `internal/broker` + `internal/sandbox` (B1-3 broker posture), `internal/gates` (S02.7 — accept is the class-A exemplar), `internal/review` + `internal/project` (B4-1/B4-2 surfaces: `ProtectedRefs` read, utility checkouts, revision refs, snapshot topology), migrations → 0009. Escrow DONE → live first-push leg unblocked; grounding must scope dev-mode (local bare fixtures) vs the LIVE leg precisely from the records and FLAG any operator hands-on step (e.g. backup remote creation) rather than assume it. Expected adoptions: age + zstd Go modules via the rail — pins live-verified at executor time, full S16.4 walks, operator #10 ratification queued to the B4 gate. Zero paid engine calls; the live leg is a git push of an encrypted blob, scoped explicitly in the brief.
- 2026-07-22 — **P3-B4-2 DONE + validated → LANDED, pushed.** Packet = `5056c62` (35 files +4131/−22) + drain round 1 `a860445` (26 files +1458/−157; `5056c62` never amended). Pipeline record: grounding `52696de` → executor (opus) green battery but evaluation **FAIL — 15 findings, all upheld at triage, zero false positives** → round-1 full fix → re-check **PASS** (every finding genuinely resolved with real-chain tests; no weakened assertions; nothing new above nit). Coordinator landing checklist: independent battery `-count=1` 28 pkgs 0 FAIL; independent `-race` green on all 7 touched pkgs (project/review/intake/stage/shell/memory/run); gofmt/vet/build clean; lockgate 11 entries / 12 go.mod deps covered; 118-key tally intact; spot line-reads of the F2 sink loud-failure, F7 `ErrUnprotectedRevision` pre-destructive enforcement, and F3/F4 `WorkspaceCwd` execute-leg guard + `SetWorkspaceRef` — all spec-conformant; CONVENTIONS §23 pure append, accurate to the round-1 shape (hermetic per-invocation git, per-attempt base refs, byte-exact base blobs, collision-free paths, loud citation resolution). Residual nit-level notes RECORDED, deliberately not drained: N1 shell seam methods mirror-tested not direct (unavoidable import direction — shell imports stage); N2 the driver-checkpoint-row×real-sha link proven as stub-seam test + real-seam e2e separately, not one composed assert; N3 F11's DB-error branch not fault-injected. Zero paid calls across the packet. First four-stage-pipeline packet complete: the FAIL→drain→PASS cycle caught 15 real defects behind a fully green battery — the quality-first directive earning its cost on packet 1.
- 2026-07-22 — **P3-B4-2 drain round 1 APPLIED (`a860445`) → evaluation RE-CHECK launched (same evaluation agent, SendMessage).** Executor follow-up commit (`5056c62` untouched, never amended): 26 files +1458/−157. Per-finding: F1 `.onboard` run role + `dispatchOnboard` + durable owner-answered ask (`internal/stage/onboard.go`; e2e register→clone→scan→draft-on-ask→owner-approve→active, non-owner refused); F2 repo-backed snapshot failure loud at the sink, content-pin lane intact; F3 `run.SetWorkspaceRef` at creation + durable intake-match resolution, `MatchForIntakeAny` REMOVED (owner-scoped structurally); F4 cwd swap execute-leg-only, intake stays workspace-less (asserted); F5 `git cat-file blob` byte-exact base; F6 per-attempt base refs, fresh attempt from moved HEAD; F7 `ErrUnprotectedRevision` — GC refuses unprotected branch deletes; F8/F9 real-chain e2e `TestRegisteredProjectChainAndSnapshot` (registry→ledger§2 + driver checkpoint + sink round-boundary + ref through real seams); F10 `ErrCitationUnresolved` loud; F11 ErrNotFound-only vanish, DB errors propagate; F12 real Gate+GitCommitter file_commit fill; F13 real-HEAD fingerprint; F14/F15 lock-semantics + orphan-flag tests; F16 a–e fixed, (f) ratified no-change. Executor re-audited its own report accuracy (the round-0 inaccuracy called out). Reported battery: 28 pkgs 0 FAIL, `-race` 7 touched pkgs, lockgate 11, 118-tally, zero new deps/keys. Re-check scope: each F genuinely resolved (not weakened), no new defects from the fixes, battery independently re-run; verdict PASS/FAIL. ALL 15 findings upheld (zero false positives) → DRAIN ROUND 1 launched (SendMessage to the live executor agent; cap 2 rounds).** Evaluation (Fable, no classifier trip) independently reproduced the green battery, then found: **F1** R5/rubric-2 undeclared omission — onboarding shipped as direct store verbs, no stage run role, no durable ask (S13.7 "itself a task"); **F2** reviewSink swallows Snapshot errors → repo-backed mint indistinguishable from honest content-pin NULL + retention ref silently skipped (breaks R20 premise; §14 never-faked); **F3** `runs.workspace_ref` never written — per-call fuzzy TITLE re-match (intake matched title+text; mid-pipeline project flip possible; `BaseContent` via owner-agnostic `MatchForIntakeAny` = cross-user review-base leak); **F4** workspace cwd + snapshots unguarded across ALL session legs (intake/planner ceremony materializes worktrees + snapshots the run branch pre-approval; R34 scopes to execute legs; executor report claim inaccurate); **F5** `BaseContent` TrimSpace corruption (probe-proven: git-show returns blobs verbatim; leading blanks stripped → line shift → P-T12-2 silent mis-anchoring class; empty→"\n"; binary through TrimSpace); **F6** fresh attempt branches from OLD immutable base ref — S13.5 base-moved trigger unhandled in substance, doc contradicts code, test blind; **F7** retention GC `branch -D` without verifying minted-revision refs exist (comment asserts, nothing enforces; F2 windows make it live); **F8** rubric-5 e2e used a fake registry (R10 demands the real chain); **F9** composition layer (project_seams 195 lines + all stage wiring) zero coverage — rubric 9/10 driver/sink paths unproven through real adapters; **F10** citations silently dropped on resolver error/empty (escape all future drift); **F11** per-key DB error conflated with vanished (false drift; ErrNoRows split needed) + real-resolver path untested; **F12** GitCommitter never driven through memory.Gate composed; **F13** repo-HEAD fingerprint untested above project pkg; **F14** "created locked" vacuous proxy test; **F15** orphan-flag lane untested. **F16 nits folded in** (§35→R35 cites; scan `<default-branch>` literal placeholder reaching ledger §2 — coordinator rates this above-nit, fix; deliverableTaskID comment; mustNow dead code; sanitize collision) **except (f) no-change→HEAD accepted as a valid R18 tree-pinning reading (coordinator ratifies, log-recorded)**. Coordinator dispositions in the drain: F2 = loud failure through the existing sink error path (mint never proceeds to a fake state) unless spec text directs an explicitly-marked degrade; F3 = write workspace_ref at creation, resolve via it + the durable intake match, owner-scoped always; F4 = execute-leg guard; F6 = per-attempt base from CURRENT default HEAD; F7 = refusal unless every minted revision ref-protected; F10 = unresolvable citation at approval = loud capture failure (S09.6 reading, executor verifies). Evaluation's verified-clean list noted (migration walls, never-amend tripwire+behavioral, junk-exclusion, fill-once, byte-identity, AST walls, zero deps/⚙, adopted git untouched). Re-check by the SAME evaluation agent after the executor's round-1 report. → EVALUATION LAUNCHED (stage 3, Fable-inherit, background; opus fallback armed on classifier trip).** Executor (opus) shipped in one commit, 35 files +4131/−22: new `internal/project` (storage+eventlog+stdlib only, AST-pinned) with migration 0008 (versioned immutable captures, no `snapshot_ledger`), onboarding-as-task (clone/init→scan→draft→owner-D10→active), branch-per-pipeline worktrees + hermetic tree-level junk-excluded snapshot commits (no-change→prior sha; never amend/force/stash/jj), utility checkouts + GC + minted-revision platform refs, `GitCommitter`; wiring: review `MintInput.SnapshotSHA` + fill-once + `BaseContent`→`Compare(0,1)`, intake `Plan.CitedEntries` byte-identity + cited-entry drift + repo-HEAD fingerprint, stage `Driver.Snapshot` + reviewSink snapshot/ref + guarded execute-leg workspace, shell composition (`projectSeams`). Executor-reported battery: build/vet/gofmt clean, `go test ./... -count=1` 28 pkgs 0 FAIL incl. `-race` on project/review/intake/stage/shell, lockgate 11 entries, 118-key tally green; zero paid calls; honest note: content-pin lanes keep `snapshot_sha` NULL (brief R19's own lane split), repo-backed flow proven by a composition e2e. Coordinator verified: range da7d7d3..HEAD = exactly `5056c62`; tree clean modulo the two operator-disposal dirs. Evaluation gets brief + range + executor report, verdict PASS/FAIL; coordinator triage + drain next; landing checklist (independent battery re-run) before push. → coordinator sanity pass OK → EXECUTOR LAUNCHED (stage 2, `model: opus`, background).** Brief `P3/briefs/P3-B4-2.md`: 35 requirements (R1–R35) with S-refs, 26-item rubric (§6), 6 boundary readings (§7), zero new ⚙ keys / zero new deps declared, no spec conflicts. Grounding agent verified all cited code seams against tool results (0007 `snapshot_sha` fill-once + `RevisionRef`, `memory.Committer` gate.go:24, `Driver.Snapshot` driver.go:46, `run.Fingerprint.CitedEntryVersions` freshness.go already-shipped comparison, intake `Registry`/`Fingerprint` seams, ledger `SetConstraints` piping, sandbox workspace bind, recovery step-7 untouched) and re-ran the full battery green before committing. Coordinator checks: commit contains exactly the one brief file; headline↔rubric coverage complete; B4-3/B4-4 scope held to named seams (no `snapshot_ledger`, no broker code, no preview runner); S11.3 worktree-now/overlay-with-sandbox reading matches the phase-status posture. Executor prompt = SKILL template + scope guardrail + zero-paid-calls (fixtures/fake-engine only) + explicit-path staging. Evaluation (stage 3, Fable-inherit) follows on executor completion.
- 2026-07-22 — **P3-B4-2 grounding LAUNCHED** (first packet through the four-stage pipeline; stage 1, Fable-inherit, effort max, background). Entry checks: tree clean except the two operator-disposal dirs; `go build ./...` green; no open gate. Grounding writes+commits `P3/briefs/P3-B4-2.md` (checklist = evaluation rubric, per briefs/README). Prompt carries: queue-row read-first set + acceptance headline verbatim; adjacent-S13 boundary guard (S13.6/S13.9/S13.10 = B4-3, S13.8 = B4-4 — seams only); code grounding pointers (migration 0007 `snapshot_sha` fill-once seam + `refs/sinet/deliverable/` from B4-1, `memory.Committer` B3-1, `Driver.Snapshot` per CONVENTIONS §10, S02.6 freshness in `internal/run`, intake resolution, ledger pinned constraints, sandbox workspace consumer); ⚙-by-registry-key + name-referenced-seam-keys duty; no-new-deps expectation (git host CLI already a lock entry, B4-1); dev-mode local-fixture git posture; spec-conflict = stop-and-report. Next on brief landing: coordinator sanity pass → executor launch (`model: opus`).
- 2026-07-22 — **PROCESS DIRECTIVE: four-stage packet pipeline ratified + installed (operator, in-chat; quality-first).** From P3-B4-2 every packet runs grounding → executor → evaluation → finalizing per the rewritten SKILL.md §Work packets (fresh-context coordinator-launched stages; `P3/briefs/` = handoff artifact + evaluation rubric; executor `opus` ALWAYS; grounding/evaluation Fable with all-opus on B4-3/B4-6 and lossless opus fallback on classifier trips; drain [F1..Fn] cap 2; landing checklist replaces coordinator-inline validation). Research basis in the standing-directive bullet above (Anthropic three-agent harness; "Prompting Claude Fable 5"; D3 record §7). STATE routing lines + B4 Model cells updated; `P3/briefs/README.md` seeded. Entry-state verified this session: B4-1 done + validated (`e60ddfb`; STATE handoff `b98d0dc`), queue row already flipped by the handoff session; tree clean except the two operator-disposal dirs (`Presentation/`, `tools/dbpeek/`). Next continue-session's first act: launch P3-B4-2 **grounding**.
- 2026-07-22 — **P3-B4-1 done → SESSION HANDOFF (operator direction: stop after B4-1, big context; deliberate stop, NOT a failure-ladder strike).** Commit `e60ddfb` (36 files, +4604), validated at FULL depth: independent battery `-count=1` — 27 packages green (new `internal/review`) — + independent `-race` green on review/verify/stage; gofmt/vet clean; lockgate **11 entries** (+2 this packet: `PDF text extraction` = github.com/ledongthuc/pdf pseudo-version pin, BSD-3, zero deps, full S16.4 walk incl. pdfcpu-unfit/AGPL-excluded rationale; `git (host CLI)` os-mechanism at first consumption; **operator #10 ratification for BOTH queued to the B4 gate**). Line-read in full: migration 0007 (S13.1 families faithful; open dtype set = no type refused; monotone `current_revision` + no-delete/immutability trigger walls; both content-pin kinds with exact CHECKs; `snapshot_sha`/`verdict_ref` honest fill-once seams for B4-2/S07; ONE human+finding schema, one-way batch-stamped consumption FORCED by CHECKs; per-revision anchor table, placements immutable), drain.go (single tx; blockers-first-then-insertion numbering; orphan/file-degraded still delivered; not-open-at-drain race guard; run-scoped `review.drained` event; empty set = no event), anchor.go (5-rung ladder exact; born-orphan structurally absent, matching the birth CHECK; diff-failure falls through to text steps, never a silent drop; deterministic `searchNear`, empty quotes never match; rev-1 old-side honestly deferred to S13.5; `ParseFindingAnchor` never refuses shape — P-T12-2), verify/review.go (mint one-per-round idempotent at the handoff; CHECK-INTEGRITY excluded from the review stream per §15; guidance same channel; nil = pre-S13 posture, composition root always wires). ⚙ `review.anchor_drift_lines` registered + registry-consumed. **Coordinator-verified reading (the agent's flagged deviation): the resumed drain delivering parked still-open findings alongside guidance IS S13.4's categorical "every open comment" — supersedes B2-5's guidance-only retry package; notes travel in delivery, blockers alone trigger rounds (S07.6×S13.4). Clearly implied; §22-logged.** Zero paid calls. **Per operator direction this session stops here: P3-B4-2 (S13.5+S13.7) deliberately NOT launched — any next session resumes via "continue implementation", first act = launch B4-2.**
- 2026-07-22 — **D3 APPLIED → B3 GATE CLOSED (all five decisions) → B4 OPENED, queue cut → P3-B4-1 launched.** Apply commit `e06f0a4` per the ratified record (no re-presentation, per the gate's own disposition): `DefaultDutyMap` → planning=opus-4-8, execution=sonnet-5, judge=opus-4-8; 7 seat-coupled sites moved (routing.go map+doc comment; routing_test execution-resolution, mechanical-degradation, map-shape asserts; route_e2e execution assert; compose_e2e untouched — composer rides the unchanged planning seat); `SelfFamily` checked = vendor-family semantics, honestly TRUE before and after, unaffected. Validated: battery `-count=1` 26 pkgs green, `-race` green worker+stage, gofmt/vet clean, lockgate 9 entries zero new deps. Gate report §7 finalized (D3 row APPLIED; FINAL status line — B3 CLOSED). **B4 queue cut** from S19.5 + S13/S12 FULL coordinator reads + carried seams (B3-report §4: S09.6-fingerprint consumer → B4-2; `memory.Committer` → B4-2; `memory.ContradictionScreen` live → B4-7) + B2-3 deferral rows (TPR/TNR, judge calibration, length bias → B4-7) + both D3 riders (→ B4-7). Coordinator reading logged in phase status: opencode/local-lane engine adapter NOT in the cut (no v0 consumer; S12.4 duties are all class-(b) no-engine-session; Z.AI parked; attaches when a consumer exists). Model routing: B4-3 + B4-6 opus-pinned (S11 broker/bridge internals), rest inherit. Environment pre-checks recorded (161 GB free; no nvcc → toolchain live-resolve in B4-5 with the hard no-driver/kernel/CUDA gate restated; docker + gamemoded present; GPU 12227 MiB pool12). **Escrow now live homework — blocks only B4-3's LIVE first snapshot push.** P3-B4-1 launched (Fable-inherit, effort max, background): S13.1–S13.4 review data model + drain; migration 0007; zero paid calls (fake-engine e2es per precedent).
- 2026-07-22 — **B3 gate session: 4 of 5 decisions closed+executed; D3 re-opened on operator direction; deliberate stop at 48% context.** Operator free-text: "1. ok, 2. ok, 3. why we only us claude haiku and not glm-5.2 or opus 4.8?, 4. ok, 5. ok" → D3 walkthrough → "3b but take opus 4.8 as executor and Sonnet 5 as judge" → coordinator applied it → operator superseded same session (research-must-decide + not-this-session). EXECUTED: (1) S03.3 engine bump 2.1.215→2.1.216, pin+lock lockstep, battery green, per-pin canary PASS live (~14s, <$0.01); (2) S00.9 amendments A2+A3 committed — changelog rows + all 7 marker sites annotated in drafts AND assembled spec (coordinator found a THIRD stale measurement-closed marker during execution: TBD-P3(PreCompact/injection, closed at B1-4) — NOT applied, drafted as A4 in gate report §7 awaiting a future ok); (4) playbook provenance → ratified record (seeds_test updated); (5) readings en bloc recorded. INTERIM commit stands: duty seats exec/planning=opus-4-8 judge=sonnet-5 + fixtures at 2.1.216 (green, unratified). **D3 research findings (live, 2026-07-22):** Anthropic's tested advisor pattern = Opus advises/plans + Sonnet default executor (≈11% cost reduction AND ≈2% benchmark gain vs all-Opus; Haiku only simple/high-volume) [mindstudio.ai advisor-strategy series; claude.com choosing-the-right-claude-model]; LLM-as-judge 2026 practice = cross-model judge vs the EXECUTOR, self-preference bias −38%..+90% ArenaHard and grows with capability, calibrate vs humans [futureagi.com llm-as-judge-best-practices-2026; arxiv 2410.21819, 2604.22891]; API's own advisor-pairing constraint: advisor/judge capability ≥ executor (executor sonnet-5 → advisor opus-4-8) [claude-api skill, advisor tool table]; Max-plan economics: Opus≈5× Haiku weight, Sonnet≈3×, plus a SEPARATE Sonnet-specific weekly cap = dedicated extra Sonnet pool [truefoundry/portkey/explainx claude-limits 2026]. Preliminary derived mix for next-session ratification: **planning=opus-4-8, execution=sonnet-5, judge=opus-4-8**. Everything committed+pushed; next-session entry sequence in the phase-status block. NOT done this session by direction: D3 final apply, B4 queue cut, B4-1 launch.
- 2026-07-22 — **P3-B3-5 done → B3 GATE OPENED** (commit `7dba4ef`, validated at FULL depth: independent battery `-count=1` — 26 packages green — + independent `-race` on all eight touched packages; gofmt/vet clean; lockgate 9 entries, zero new deps; line-read of composer.go, split.go, compose.go, playbook.go, runner/adapters/driver diffs, intake compose verb, memory seed against S08.6 + S09.10 + S05.3 — no contradictions. Verified structurally: one-shot rule (ErrComposeRejected never retried; battery strictly after via dispatchCompose — composer cannot see results; jsonSession bounce = envelope only), inputs-by-policy exhaustive (spec+gap+required-playbook+empty-v0-palette+composeReference rendered from the SAME authorities the battery enforces — no drift), drafting model = platform fact never model output, provenance complete (composer/playbook@ver/gap ref/OriginComposed → gap `composed`); split: overflow scope corrected to per-planned-stage (the S05.3 sentence verbatim), auto-accept = fully-logged platform decision citing the overflow event seq, pause verb via new OnSession control seam (json:"-", delivered on all three drive paths, single-goroutine), split-vs-ask disposition exact (parked ∧ no-ask ∧ requested), park record DISCARDED per fresh-context rule (successor = fresh brief at bumped ledger_version, e2e-asserted), pause-races-completion honestly loses, one split per planned stage structurally bounded, per-sub-stage recompile via CompileForRun with TighterClass; playbook: governed seed `seed-composer-playbook` under the §17 idempotency/no-resurrection/ErrNoOperator discipline with provenance stating ratification PENDING; Pin promotion keeps TestPinMatchesLock lockstep. CONVENTIONS §21 accurate; 12 readings all clearly-implied. Zero paid calls.) **B3 gate report written: `P3/gates/B3-report.md`** — 5 decisions (engine 2.1.216 bump rec:now; TBD bookkeeping ×2 with drafted S00.9 entries; duty map rec:ratify; playbook seed rec:ratify; readings en bloc) + standing items (escrow now live homework for B4). Awaiting operator.
- 2026-07-22 — **P3-B3-5 LAUNCHED** (S08.6 composer one-shot generation + S05.3 stage-split execution — the coordinator-cut B3 remainder and LAST B3 packet; Fable-inherit, effort max, background). Read-first per queue row: S08.6 + S09.10 + S05.3 + CONVENTIONS §18–§20 priors + B3-2 battery entry points + B3-3 compose-verb surface + B2-4 budget watcher + ledger machinery. Zero paid (fake-engine only); battery still gates adoption (generation feeds INTO it, never around it); composer-playbook seed content flagged as a B3 gate ratification item. B3 gate report follows its validation.
- 2026-07-22 — **P3-B3-4 done** (commit `6802f7a`, validated at FULL depth: independent battery `-count=1` — 26 packages green — + independent `-race` on adapters/claudecli/stage/ledger; gofmt/vet clean; lockgate 9 entries, zero new deps; line-read of the valve (claudecli/recite.go), the reciter (stage/recite.go), and ledger/recite.go in FULL against Research/18 §7 + S05.3–S05.4 + S03.4 — **all seven §7-C1 conditions verified in code AND test-pinned**: (1) scope=recitation-only, (2) platform-authored content at pinned ledger_version via deterministic pure-lookup RecitationText, (3) every delivery manifested from FIRES only for every session disposition, server-side re-render hash-verify with hash_mismatch/unknown_version dispositions + fire-before-speak abort — the engine-writable exchange dir can neither forge nor silence the trace, (4) PreToolUse untouched: hook.go zero-diff + byte-identity test, (5) probe consumed never re-paid, canary rows = the SINET_B3_4=1 sanctioned-skip live test, (6) dueness platform-computed from per-call usage events, hook = stateless valve, (7) settings-ro re-asserted, ROConfig-exact test. Lowering refuses tool-less recitation loudly; ctl dir stays the ONE rw bind; CLI hook events exclusive; S05.4 manifest field set complete. Coordinator-probed suspicion of park/resume double-manifest DISSOLVED against driver contracts (sole Recitation setter = stage runner; DriveStage bars stage-session parks) — logged as a **future-composition seam note**: if a gated/parkable run ever recites, recordRecitations' full-log re-read needs consume-on-manifest or S02.5-style manifest dedup. Cosmetic nit, no revision round: stage-side readReciteFires is silent on corrupt lines/read errors (delivered-* evidence remains; valve-side reader strict). C5 not built — sanctioned discretionary drop. CONVENTIONS §20 added.) Zero paid calls this packet. Engine 2.1.216-vs-pin-2.1.215 stays a B3 gate item; the gated canary is that bump's re-check.
- 2026-07-21 — **P3-B3-4 RELAUNCHED** (continue-implementation session per the handoff; Fable-inherit, effort max, background). Entry check: git dirt == documented WIP inventory exactly (+ the two known unrelated untracked dirs `Presentation/`, `tools/dbpeek/`); `go build ./...` green with the WIP. Relaunch prompt = runbook template + relaunch specifics: probe result file consumed (do-NOT-re-run made explicit), WIP inventory handed over (roll forward or rewrite = agent's call), remaining-scope list from the handoff (lowering wiring, platform-side reciter with dueness/pinned-version/manifests+journaling, settings-ro leak-test re-assert, C3 rows, C5 only-if-slack), zero paid calls, explicit-path-only staging (Presentation/, tools/dbpeek/, STATE.md excluded). B3-5 pending after it; gate items unchanged.
- 2026-07-21 — **Session handoff (operator: "pause here, make sure every task stops properly").** P3-B3-4's background agent STOPPED by coordinator mid-implementation (it had finished the probe + started the delivery valve; TaskList confirms nothing else runs). Disposition: (1) probe result file COMPLETE — PASS, all three pre-registered expectations confirmed live at 2.1.216, spend $0.0271 vs $0.10 stop line, four per-pin canary rows recorded — COMMITTED this handoff as durable paid evidence (relaunch consumes it, never re-pays); (2) partial code left IN TREE UNCOMMITTED per the never-discard-silently rule — `internal/adapters/adapters.go` (+11, the `CompiledWorker.Recitation` seam) and `internal/adapters/claudecli/recite.go` (275 lines, pending-file read-and-consume valve doc'd to the §7-C1 conditions); tree BUILDS GREEN with the WIP; still missing from the packet: lowering wiring, the platform-side reciter (dueness from run events + pinned-version authoring + manifests/journaling), settings-ro leak-test re-assert, all C3 rows, all tests. (3) Packet → `pending`, relaunch is the next session's first act via "continue implementation" (deliberate stop, NOT a failure-ladder strike). Everything else committed+pushed through `09a7179`; B3-5 pending after B3-4; gate items unchanged (engine 2.1.216 bump decision, TBD spec-bookkeeping ×2, duty-map ratification, composer-playbook seed). Open operator items unchanged: sanctioned `rm` of the three demo dirs + `tools/dbpeek/`, hardening-session scheduling (now incl. `socat`), re-deferred probes.
- 2026-07-21 — **P3-B3-4 LAUNCHED** (codor C1-recitation + C3 rows; Fable-inherit, background) **+ P3-B3-5 CUT** (S08.6 composer generation + S05.3 stage-split execution — the B3-3 remainder sweep found neither in any packet's acceptance list; both squarely in-phase scope, so the coordinator derived the packet per the runbook; queue row above; runs after B3-4).
- 2026-07-21 — **P3-B3-3 done** (commit `cb3d53d`, validated at FULL depth: independent battery `-count=1` — 26 packages green — + independent `-race` on worker/stage/intake; gofmt/vet/lockgate clean, zero new deps; line-read routing.go pipeline (S08.8 order exact: FTS5 selector match → TieBreaker seam nil-degrading with reason → duty maps under subscription coverage, metered branch structurally refuses — test-pinned; pressure-never-dollars documented in code), migration 0006, helpers e2e (ALL FIVE S04.5 conformance MUSTs present: depth-3 refuse, lateral refuse, native-spawn refuse, over-budget refuse, mid-run kill → sibling salvage), intake/route.go card visibility+override+no-fit, DefaultModel + 200k window RETIRED to duty-map seat rows; §19 readings all clearly-implied incl. duty-map-as-code-data and de-facto-judge-not-a-P-T06-5-bump; stagger key deliberately unconsumed with sound justification — no simultaneous-launch batch surface exists at v0). **Reconfirm measurement: PASS on the S08-designated default worker `claude-haiku-4-5` — TBD-BRINGUP(parallel-gate fallback) ≡ TBD-P3(serialize-by-deny reconfirm) CLOSED** (`P3/measurements/2026-07-21-serialize-by-deny-reconfirm-s08.md`; E1 clean park, E3 6/6 faithful single-call parks ~+1 turn, round-trip live; E2 favorable drift persists at 2.1.216; spend $0.1304 vs $0.50 stop line). **B3 GATE ITEMS accumulating:** (1) engine pin↔installed delta AGAIN — installed 2.1.216 vs pin 2.1.215, operator S03.3 deliberate-bump decision (P-T14-1 mass-revalidation is empty: no production workers); (2) spec-text bookkeeping of the two measured-closed TBD markers (auto-memory containment at B3-1, parallel-gate fallback here) — S00.9 dated changelog entries on operator approval, coordinator drafts at the gate; (3) v0 duty map ratification (all paid seats `claude-haiku-4-5`, utility seat absent until S12/B4 — §19); (4) composer playbook seed ratification (arrives with B3-5). (S08.8 routing + S04 spawn/helper slice + no-fit/gap + serialize-by-deny reconfirm; Fable-inherit, background). Read-first per queue row: S08.8 + S04 (spawn/helper/D6) + S03.2 + S12 duty seams + B1-4 spike-1 harness. Includes the pre-registered PAID reconfirm measurement (spike-1 harness re-run on the chosen default worker, established B1-4 scale — the one paid leg of the B3 queue; result file per G3 Def.8 closes TBD-BRINGUP(parallel-gate fallback) or escalates).
- 2026-07-21 — **P3-B3-2 done** (commit `e625643`, validated at FULL depth: independent battery `-count=1` — 26 packages green — + independent `-race` on worker/automation/memory/shell; gofmt/vet/lockgate clean, zero new deps; line-read migration 0005 (S08.1 field lists faithful; guardrails-frozen trigger "changes only via a new approval; first-N only counts down"; raw-SQL attempted-violation battery), lint.go structural guardrail reject (whole-key normalized, template+skill+overlay channels, conservative-toward-reject), compile.go (one-unit sha256 ConfigHash incl. overlay+instance refs; Task*-family refused at compile AND lowering — sole-controller; tamper check against row hash), ceiling.go (4 v0 rows exactly per S08.6 table; Rule-of-Two via sandbox.AdmitRuleOfTwo), battery.go (DryEngine seam, ⚙ cap, approval-as-diff → Approve, GoldenSeed{verified:false} R15-OQ4), memory/overlay.go (dormancy = absence of writers, gate untouched, L1 excluded; worker never imports memory — seam at composition root, AST-pinned), automation dialect (no-model-in-loop = import-graph fact, AST-pinned); CONVENTIONS §18 readings all clearly-implied incl. flags-gate-approval vs Rule-of-Two-hard-reject and degraded-schedule_attachable/auto-accept-at-v1-layer). S08.8 seams left named for B3-3: DryEngine production dispatch, CompileForRun, gap-proposal surface, stage.DefaultModel + window constant retirement. (S08 worker store + guardrail split + per-invocation compile + composer battery; Fable-inherit, background). Read-first per queue row: S08.1–S08.7 + S08.9–S08.10, S03.5 lowering, S09 overlay/playbook homes, S11.6 class table READ-ONLY (slim slice; Opus re-cut only on a demonstrated safeguard trip per standing policy).
- 2026-07-20 — **P3-B3-1 done** (commit `9e791cd`, validated at FULL depth: independent battery `-count=1` — 24 packages green — + independent `-race` on memory/ledger/claudecli/stage; gofmt/vet/lockgate clean, 9 entries, zero new deps; line-read migration 0004 in full (S09.2 field lists faithful; observation⟺L1 CHECK; tombstone/no-delete/immutability triggers incl. the one NULL→hash `file_commit` fill; FTS5 external-content over ENTRIES ONLY), gate.go person/authorize/validateDraft (humans-only via users-row requirement, D10 house=operator, worker_overlay dormant, observation refusal IS L1 dormancy), store.go Search (server-side scope predicate from runs.owner + claim-derived project; sanitized quoted FTS terms; miss events), the adversarial cross-user conformance test (FTS operators/quote escapes/SQL fragments all fail; claim opens project scope), containment.go (wipe ONLY platform-supplied config-root `memory/` contents at session START, resume exempt, empty-ref no-op — can never touch an ambient real config), seeds provenance (six house entries, origin imported, B2-report §7 D3 record), Numbers() + per-assembly budget reads = all 12 ⚙; CONVENTIONS §17 six readings all clearly-implied). **TBD-P3(auto-memory containment) CLOSED.** Named B4 seams: `memory.Committer` (S13 git-on-approval), `memory.ContradictionScreen` (S12 duty). **Composition seam noted for B4:** S09.6's cited-project-truth-entry versions joining the S02.6 freshness fingerprint has no consumer until plans can cite entries (project registry/S13) — carry into the B4 cut. (S09 memory/knowledge substrate; Fable-inherit per B3 routing, background agent). Read-first per queue row: S09 full + S05.4 + S02.2 + spike result `P3/measurements/2026-07-20-auto-memory-containment.md`; zero paid calls expected (substrate packet, fake-engine where a session is even needed).
- 2026-07-20 — **P3-B3-fix1** (commit `019f723`, coordinator inline — the established CI-fix pattern): the 3 sandbox tests made red by the B2-gate srt host install are now hermetic vs host srt state (unusable-override pin to the native path in the two live tests; PATH-scrubbed absent leg in the discovery test). Test-only; present-but-broken srt platform semantics deliberately left to the hardening session (operator call). **Full battery 23 packages green `-count=1`** — first fully-green local battery since the srt install; CI unaffected (no srt there).
- 2026-07-20 — **P3-B2-5 done** (commit `2caa810`, validated at FULL depth: independent battery `-count=1` — 22 packages ok — + `-race` green on stage+verify; lockgate 9 entries (9th = the pre-existing caddy organ-unit entry from the D2.2 session, packet added ZERO deps); line-read of verify/answer.go + stage/answer.go full, pipeline.go drainSeed refactor, skeleton verifyTerminal, surface routing, engines revision-persist + run-id fix, all against S07.6–S07.7/S02.3/S02.5/S06.9; e2e assertions checked — gen 0→1→2 bumps, `ErrStaleGeneration` on the pre-park zombie session, effects row untouched, convergence tripping across the park; `~/sinet-demo` mtime-verified untouched). Shipped: park-on-card (supersedes B2-4 completed-on-card; ALL drain-terminal categories park, only the rework-family three-verb cards CAP-HIT/AC-BLOCKER/SANITY-BLOCKER answerable at B2-5, others reject loudly), the three verbs on the one answer endpoint (`ask-verify-`/`canary-` prefix routing; D10 owner check; verbs validated against the card's ISSUED choices), durable best-effort pin (every rework revision persists; resume loads + hash-verifies; legacy pre-B2-5 cards → `ErrNotResumable`), guidance→numbered blocker findings into the S07.6 retry package (requester-comment channel; never re-merged post-rework; continued round numbering, fresh ⚙ cap per human grant — S06.7(a) precedent — suppression+convergence seeded across the park), cancel = §14 reading 9, crash-for-ladder on mid-drain errors, fork-suffix-safe judge/revise session run ids. 8 section-cited readings in the commit body; CONVENTIONS §16. **Validation finding (NOT the packet's): `internal/sandbox` red on this host — 3 pre-existing failures I reproduced at the base commit in a clean worktree: the B2-gate srt install left srt discoverable but NON-FUNCTIONAL (runtime dep `socat` missing — so the B2 "srt host activation" was incomplete: discovery verified, live wrap never ran) and `TestProbeSrtDiscovery` hard-asserts a srt-free host. Plan: coordinator test-hermeticity fix inline (the established CI-fix pattern — tests must not couple to host srt state; platform present-but-broken semantics deliberately NOT changed inline, that call belongs to the hardening session with the operator); `socat` install added to the hardening-session batch.** (fresh session per the handoff; Fable-inherit, effort max, background agent). Prompt per runbook template: read-first S07.6–S07.7 + S02.3/S02.5 + S06.9 + `intake/answer.go` precedent + gate report §6; zero paid engine calls (fake-engine e2es per the B2-4 precedent); `~/sinet-demo` strictly read-only fixture (DB inspection only via copy). Coordinator validation prep (S07.6–S07.7 + gate §6 line-read) runs while the packet does.
- 2026-07-20 — **Session handoff (operator direction: run the implementation in a NEW session).** P3-B2-5's background agent STOPPED CLEAN mid-read (zero file changes; tree verified clean; deliberate stop, NOT a failure-ladder strike) — packet back to `pending`, relaunch is the next session's first act via "continue implementation". Everything else this session is committed+pushed through `828d022`: cert step 13 done, all six B2-gate decisions answered+ratified, B2 CLOSED, B3 OPEN, queue cut. Open operator items unchanged: two sanctioned `rm` commands (failed-demo dirs, dbpeek), hardening-session scheduling, re-deferred probes.
- 2026-07-20 — **B2 GATE CLOSED → B3 OPENED; P3-B2-5 launched.** Operator "ok" ratified D3 (all five seed files, walked item-by-item) + D4 (49 readings en bloc) → all six gate decisions answered; gate record §7 finalized. B3 queue cut (table above) from S19.5 + S08/S09 + the codor standing directive: B2-5 → B3-1 (S09) → B3-2 (S08 store) → B3-3 (routing + S04 slice) → B3-4 (C1-recitation + C3). S04-placement reading logged (queue header). Demo-dir cleanup: failed-run `rm` DECLINED by permission mode → exact command handed to operator (phase status); `~/sinet-demo` kept as the B2-5 live fixture. Cert runbook step 13 done this session (crt.sh observed, record in B0-cert-steps.md); `tools/dbpeek/` still awaiting operator disposal (delete was declined earlier).
- 2026-07-20 — **Gate answers, first batch (operator free-text: "D1 ok. D2 ok. D3, D4 more details. D5 do it later." — mapping = the presented Decisions 1–5, recorded in gate report §7):** D1 demo-readings RATIFIED (all three); close-mode = CLOSE NOW with P3-B2-5 first in B3 (executes when D3/D4 land); D6 probes RE-DEFERRED with intent (suspend leg ↔ battery-drain hour, freeze/thaw ↔ hardening session, not a B3 blocker, inhibitor stays after-suspend-probe). D3/D4: operator requested content detail before ratifying — full walkthrough delivered (5 seed files item-by-item from taxonomy.go/triggers.go/seeds.go; 49 readings one-line each from the four packet commit bodies). Gate stays OPEN pending D3+D4 verdicts.
- 2026-07-20 — **Cert runbook step 13 DONE (coordinator, gate-close session):** crt.sh confirms the single cert (serial `0591e4f2…`, Let's Encrypt YE2, CN/SAN = `sinet.tailfd0b1e.ts.net` only, notBefore 2026-07-19 21:27:21 UTC, notAfter 2026-10-17; 2 CT entries 22:25 UTC — precert+leaf). Exact match to the pre-issuance finding; register rows 1+3 live as signed (item 7). B0-cert-steps fully closed. Same session sweep: units `sinet-control`/`sinet-broker`/`caddy` all active, health green direct AND through the full chain, CI green on main. `tools/dbpeek/` (untracked transient diagnostic from the bootstrap session, self-labeled delete-after-use) flagged — coordinator delete was declined; left for operator disposal. Gate walkthrough re-presented (D1 readings + close-vs-hold, D3, D4, D6).
- 2026-07-20 — **Session end (gate-demo session): B2 GATE STAYS OPEN — demo criterion fulfilled and recorded (report §6), D1–D6 answers deferred by the operator to a coming session.** Operator directives recorded: (1) they will NOT hand-verify via shell — **their personal acceptance test moves to the B6 gate as a pure UI click-through** (also in auto-memory `gate-presentation-style`); future demos: coordinator drives, operator judges the record. (2) Next session entry = the runbook gate-open path: re-present D1–D6 compactly (recommendations already in the 2026-07-20 walkthrough: close B2 with P3-B2-5 first in B3; D2 staged ①②→next session with sudo, ③④→dedicated, ⑤→escrow, ⑥⑦→suspend probe; D3/D4 ratify; D6 re-defer). Demo control plane STOPPED cleanly; state dirs preserved: `~/sinet-demo` (run 3 live state incl. the open CAP-HIT card — keep until gate closes), `~/sinet-demo-run1-failed`, `~/sinet-demo-run2-failed` (defect traces). Demo PIN in `~/sinet-demo/demo-pin.txt` (demo-only). Cleanup `rm -rf ~/sinet-demo*` only AFTER the gate record closes.
- 2026-07-20 — **B2 gate demo, run 3: PASSED — D1 record written (gate report §6, commit `1146654`); gate walkthrough presented to the operator; awaiting D1–D6 free-text answers.** Run 3 walked the whole D-line live on the fixed binary: intake (4 real answers, Clearance 62.5, force-proceed) → 2-round bounded critique → approval (4 ACs, PIN step-up 401→200) → execute (3 sessions, haiku.txt genuinely written — permission fix proven) → verify (round-1 REVISE with unknowns AC-1/AC-3 — Unknown-escape fix firing live → rework → V0 diff-empty caught the no-op revision → one-regeneration bound → durable CAP-HIT card with SLA) → kanban attention with card visible (view fix `f7be92c` proven post-restart) → receipts ceremony/execution/verification 4/3/3 calls all-unpriced tier-1-measured → durability leg across a binary upgrade, integrity ok, read-back identical. The attention ending is the script's sanctioned branch and the honest one (AC-1 judge-undecidable until B4 check packs). Five demo-found defects total across 3 runs, all fixed+pushed (`8f89677`, `0732d33`, `f7be92c`); 3 readings presented for D1 ratification; **P3-B2-5 proposed** (S07.7 resume-in-place: answering non-intake asks — B2-3 delivery shortfall). ~29 haiku calls total, subscription lane, ~36% 7-day utilization, no overage.
- 2026-07-20 — **B2 gate demo, run 2: FAILED in verify — three more defects found live; fixed (`0732d33`); run 3 next.** Run 2 reached approval through a RICHER path than run 1 (marker clarification card + critic SPEC-DOUBT card with a genuine spec contradiction → adjust_spec → re-interview → SPEC-v4/PLAN-v4 with 5 ACs — all ratified S06 routes exercised live), approved with PIN step-up, executed 3 sessions **with the permission fix working**. Then: axis-1 judge replied prose instead of the JSON contract → verify run CRASHED (one flaky reply = run death); S02.5 ladder forked correctly but **every fork was unroutable** — runRole suffix-matched the raw id, `.g1` broke it → instant crash per fork → tombstone at max_attempts with zero real retries; tombstoned lineage sat under kanban "verifying" indefinitely (sink violation at the lineage level; the rich tombstone card is a recorded B0-4 deferral to B5/B6 — but the column must not lie meanwhile). Fixes: fork-suffix-aware runRole; bounded JSON re-ask (jsonRetryLimit=1, house bounce-once) on planner/critic/judge seams; derived-not-stored attention kanban overlay. 3 new tests; battery 23 pkgs + race green. Run-2 state preserved (`~/sinet-demo-run2-failed`). Wasted paid spend: 1 judge call. Meta: the 10-min bash waiter died silently at its timeout — run-3 wait switches to a persistent Monitor.
- 2026-07-20 — **B2 gate demo, first run: FAILED at step 13 — two real defects found live; fixed (`8f89677`); re-run pending.** Operator present; operator directive: coordinator drives everything. Steps 1–12 passed (with a demo-script step-5 typo fixed in place: `role` mandatory) — boot/probes/auth/SSE-spine/intake-interview/Clearance/force-proceed→assumptions-centerpiece approval card/critique-bounded-to-open_findings/PIN step-up 401→approve/claims/`.approved` freeze/3-exec-sessions/checkpoint rows with live ledger-revision blocks all exactly per script. Then: (1) exec sessions had no PermissionMode → headless Write prompt auto-denied → the executor's permission plea got filed as the deliverable (engine had composed a valid haiku; transcript proves it); (2) verify round 1: judge all-3-ACs Unknown → **verdict SHIP, kanban done** — undecided criteria died silently; B2-3's pinned reading ("Unknown alone is no blocker") overturned as contradicting S07.7 sinks-only + S06 criterion-never-disappears. **Coordinator reading (runbook ambiguity rule): Unknown AC ⇒ synthesized blocker-class AC-BLOCKER finding (stable key `unknown:AC-n`) into the ratified drain — rework chance, recurrence→convergence→CAP-HIT card; ratification presented at this gate under D1.** Fixes: `SessionInput.PermissionMode` plumbed, execute pins `acceptEdits` (consent≠boundary, S11.1); `verify.UnknownEscapes` + pipeline wiring; pinned test rewritten (`TestUnknownACNeverShips`) + `TestUnknownResolvedByReworkShips` added; 3 files with pre-existing gofmt drift formatted (B2-4's "gofmt green" was stale on them). Battery 23 pkgs + race + lockgate green. First-run state dir preserved as trace (`~/sinet-demo-run1-failed`). Bonus sighting: B1-2 unified-header overlay read real headers (`allowed_warning`, 7-day utilization 0.36) — S19.6 observation window is live.
- 2026-07-19 — P3 machinery created (skill + this STATE + CLAUDE.md pointer) in the G4-closing session; B0 opened, queue cut from S19.5 + S01/S02/S16 scopes. First build packet deliberately left for a fresh session.
- 2026-07-19 — **Host toolchain:** operator reported Go 1.26 installed, but no Go existed anywhere on the host (PATH, /usr/local, snap, dpkg, version managers all checked). Coordinator installed **Go 1.26.5** user-level: sha256-verified official tarball → `~/.local/go`, wrapper scripts `~/.local/bin/{go,gofmt}` (already on PATH). No root, no host-level change; reversible by deleting those paths. P3-B0-1 launched.
- 2026-07-19 — **Operator item 2 closed: ts.net hostname = `sinet`** (operator pick, chat session). Recorded as spec amendment **A1** (S00.9 post-G4 changelog started; S01.8 TBD-OPERATOR closed in draft + assembled spec). All build code/config referencing the ts.net name uses `sinet` from now on. **Same day, operator request: Tailscale machine renamed** `sinep-predator` → `sinet` (`tailscale set --hostname=sinet`); live FQDN `sinet.tailfd0b1e.ts.net`. Any household device that referenced `sinep-predator` by name must use `sinet` now. First cert still unissued — deliberately left for the B0 gate; nothing is in CT logs yet.
- 2026-07-20 — **Codor harvest analysis + coherence check (operator-requested, research session — not a build packet).** External repo `rjx18/codor` analyzed against the frozen spec → `Research/18-codor-harvest-addendum.md` (commit `05d7ec1`): 5 copy rows / 16 don't-copy rows / convergence register; codor never a dependency (S16 spine rule). Operator then directed adoption of the copy rows contingent on architecture coherence; coherence check ran same session (S03+S05 full line-reads, B1-4 spike result, S01/S04/S13/S14/S15/S18 greps, S11 full-read slice via Opus per model-routing rule) → addendum §7. Headline outcomes: C1 transport CORRECTED from codor's socket+token shape to Sinet's own S03.4 ctl-dir airlock + `sinet engine-hook` PostToolUse trigger (no new socket/credential/principal; injection contract already B1-4-proven; PostToolUse probe pre-registered ~$0.02); C1 v0 scope narrowed to recitation ONLY (cancel-notice/operator-note = amendment territory, NOT adopted); C2 confirmed spec-silent → additive, observability projections only; no S00.9 amendment needed for the adopted set. Standing directive added (above) so B3/B5/B6 cuts inherit the conditions. B2 gate untouched and still open. `P3/gates/B2-report.md` (decisions D1–D6) + `P3/gates/B2-demo-script.md` (literal demo, ~$0.05–0.30 paid legs) written and committed. Next session: present the gate plain-language, record answers, close, open B3.
- 2026-07-20 — **P3-B2-4 done** (commit `e578f83`; coordinator-inline completion per the failure ladder — the packet agent was killed TWICE by host session crashes; its surviving ~3.2k-line tree (battery-green) was rolled forward, line-read in FULL by the coordinator against S05.3–S05.4/S06.10/S07.5–S07.6/S02.4, gaps filled — commit message + demo script authored by coordinator). Shipped: `internal/stage` (role-run dispatcher `<task>.intake/.execute/.verify` chaining approval→execute→verify; stage runner — fresh session per stage from ledger-assembled briefs with PlacePinned+SessionStart channel, DriveStage on running runs, budget watcher consuming ⚙ `context.stage_fit_target`/`stage_overflow_threshold` → `context.overflow` events with stage-split/re-plan proposals; EnginePlanner/EngineCritic/EngineJudge/engineRevise prompt-contract seam impls — critic artifact-only, judge fed the exact clean-context slice, rework fed the exact retry package, self-family honestly flagged), claude-cli adapter registered behind the Dispatcher seam (deferred since B1-2), `scheduler.SettleRun` (gate-completed runs settle + ceremony receipts), metering purpose derivation from role suffixes (ceremony/verification/execution itemization, fork-safe), API walking-skeleton surface (submit/task/advance/answer/receipt; S01.9 same-request PIN step-up on High-tier approval/delta; dev identity cannot step up), adapters `OnEvent` post-persist observer + `ResultText` (in-process only, persisted stays bounded), `intake.ApprovedPair` (D10 read seam), pre-tx source collection in `ledger.Assemble` (single-connection deadlock avoidance, atomicity kept). 16 tests incl. `TestWalkingSkeletonE2E` (full D-line, fake engine, zero paid). 13 readings in commit body. Non-⚙ constants recorded (window 200k, DefaultModel, execTools, kanban strings, markers). Recitation ⚙ deliberately unconsumed (no per-turn channel — B1-4 spike; S04/B3). **Tier-L live smoke NOT run — the permission prompt was declined during a session crash; honored, not retried; the gate demo is the live proof.** Zero paid calls spent all night.
- 2026-07-20 — **P3-B2-4 launched** (Fable-inherit, background — LAST build packet): walking-skeleton wiring intake→plan→execute (B1 adapter+sandbox+scheduler)→verify→checkpoint→receipt dev-mode E2E + the literal gate demo script; adapter registration in runDispatcher (deferred there since B1-2); intake Planner/Critic + verify Judge/Revise seams wired to engine sessions; stage-runtime ⚙ `context.*` consumption; at most ONE tier-L live smoke (established B1-1 precedent), full live pipeline saved for the gate demo itself.
- 2026-07-20 — **P3-B2-3 done** (commit `616f497`, validated at FULL depth: independent battery `-count=1` + race green, lockgate 8/zero new deps, no migration; line-read pipeline.go drain + rework.go full, route table, effects proofs, seeds; 62 tests incl. all 8 planted-defect route e2es). Shipped: `internal/verify` — V0 pre-gates (hard-kill + ratified one-regeneration bound → CAP-HIT card, tool-failure fails OPEN with logged incident), V1 software check-pack runner over `adapters.Confiner` at C2 (network-off via empty netns TODAY; **flagged B4 obligation: stays network-off once egress substrate lands, P-T06-2**; platform-side verdicts, stage contracts PASS/FAIL/N-A/UNVERIFIABLE-HERE, first-upstream-failure, quarantine, verified-on stamps), did-research gate (bounded re-run → card, UNVERIFIABLE-HERE loud when counters unattributable), V2 dual-axis judge seam fed ONLY by ledger clean-context assembly (per-AC binary + extractive evidence enforced on PASS + Unknown escape; V1 facts never overridden — disagreement → CHECK-INTEGRITY card + quarantine, excluded from rework), bounded rework (EXACT retry package, fresh session/round, finding keys, note suppression after round 1, both convergence disjuncts under ⚙ patience, CAP-HIT with full round history), S07.7 escalation (8-category route table, total, sinks-only; SLA ⚙ consumed; canary shape + silence watch, drill helper — scheduling → B5; worker-flaw ticket = stubbed S08 seam), entailment idle + 3 SEED artifacts (calibration 10 pairs, golden 26 cases incl. clean controls, rubric bundle judge-pinned-as-class), `verify.*` events (round = keep-forever) + SetVerified wiring, **effects separation proven 3 ways** (import-graph conformance test — verify NEVER imports gates; SHIP-over-live-journal e2e; escalation-writes-no-effects). All 11 `verification.*` ⚙ consumed. Non-⚙ constants: v0RegenerationLimit=1, convergenceSimilarity=0.9, launch-domain roster, placeholder lexicon, check class C2. Deferral record `P3/measurements/2026-07-20-verification-seeds-deferral.md` (TPR/TNR + judge-calibration + length-bias → B4). 14 section-cited readings; CONVENTIONS §15. GATE ITEMS: seed-content ratification (WriteSeed dumps); network-off B4 obligation.
- 2026-07-20 — **P3-B2-2 done** (commit `45d4328`, validated at FULL depth: independent battery `-count=1` + race green, lockgate 8 entries/zero new deps, no migration — `artifact_claims` existed since 0001; line-read pipeline.go+spine.go full, state/answer/artifact core paths, key checks vs S06 §§2–10; 25 dedicated tests incl. forced P46 coverage-escalation + SPEC-DOUBT routes, band one-way exit, floors, TIER-UP re-entry, resume-from-artifact, claims collision, delta measurement hook, D10 requester-only). Shipped: `internal/intake` — Stage-0 triage (P47 rules first, classifier add-only, fail-closed high, registry slice, band all-4-conditions per-user ⚙ cost cap), Stage-1 interview (taxonomy-carried coverage, G1 P8 Clearance, ≤4-question cards, no fixed caps, force-proceed→visible assumptions, 1.7 single-question escalations), SPEC/PLAN pair (md+frontmatter+sidecar, atomic immutable drafts, stable AC-n/S-n keys, dual phrasing, markers block approval), Stage-2 spine (keyed coverage + ⚙-bounded autofix→card, plan-content floor re-check, size-delta, research bounce-once→card), Stage-3 critique seam (artifact-only, PASS/REVISE/SPEC-DOUBT/TIER-UP, ⚙-bounded), Stage-4 approval (S06.9 card contract, assumptions centerpiece, idempotent ledger pins via B2-1 ops, `.approved.md` freeze, staleness flag never-blocking via S02 EvaluateFreshness), S02.8 claims (⚙ `claims.default_write`, conservative glob-intersection, waiting+surfaced collisions), delta-only re-approval with complete P-T05-2 hook, S05.4 Sources.Plan impl, event-sourced state (`intake.state`). KEY CHECK RESOLVED: consumed `freshness.max_age` is S18's ratified folded alias of `intake.approval_stale_hours` (S18.4 R2) — no deviation. Seeds (software=ClarifyCodeBench 10-type from arXiv:2607.00711 live-verified, generic, P47 S06.3 table) are operator-editable versioned files — content ratification = B2 GATE ITEM. Non-⚙ constant: `maxQuestionsPerCard=4` (spec-structural). 10 section-cited readings, CONVENTIONS §14.
- 2026-07-20 — **P3-B2-1 done** (commit `33e562c`, validated at FULL depth: independent battery `-count=1` + race green, lockgate 8 entries/zero new deps; line-read of ledger.go/store.go/assemble.go/sessionstart.go + every diff against S05/S02.2/S02.4(c); 22 dedicated tests incl. replay/AtVersion, write rules, generation fencing, payload cap, multi-run continuation, stage-close gate, clean-context, byte-verbatim pinned placement, loud-fail hook, Driver seam). Shipped: `internal/ledger` — S05.1 document + write rules; event-sourced Store (`ledger_update` full-doc D7 payloads, no ledger table per S02.2; session Verbs with pinned/verified writes structurally unreachable; control-plane ops; `CheckpointRef` → Driver.LedgerRevision fills S02.4(c) `{ledger_version,sha256}`); S05.4 Assemble (pure lookup, stability-sort house→project→user→task→stage, explicit precedence labels, one `context.manifest` event per assembly, clean-context exception, shim-drift never silent) with recovery = same path pinned to a revision; Claude-lane injection channel per the B1-4 spike (BriefText + PlacePinned + SessionStart hook matcher `startup|resume|compact` re-emitting verbatim additionalContext, fires log → reinjection manifests). Zero new deps, no migration, no new ⚙ consumed (`context.*` stage-runtime keys flagged to B2-2+/B2-4, `conventions_max_lines` to S1.6). 12 section-cited readings in commit body + CONVENTIONS §13. PreCompact *blocking* deliberately unwired — containment stays primary (S05.7). Sole cosmetic nit (renderSection unreachable-error string) noted, not worth a revision round.
- 2026-07-20 — **Operator overnight directive** (chat, before sleep): run autonomously as far as proper development allows — drive the B2 queue through validation to the gate REPORT; pause only where operator input is genuinely required (the B2 gate itself: live demo + host-install batch). Model policy restated: everything on Fable; Opus only as a per-packet pin where Fable demonstrably trips the dual-use safeguard false positive, and back to Fable immediately after. No host changes, no machine suspend, paid engine calls only at the established tier-L one-minimal-call scale.
- 2026-07-20 — **P3-B2-1 launched** (first B2 packet; session back on Fable, effort max; background agent, model inherit per B2 routing). Read-first: S05 full + S02.2 (`ledger_update`) + CONVENTIONS + B1-4 spike result `2026-07-20-precompact-injection-mechanics.md`. Lands in `internal/ledger` (the B0-3 stub seam, CONVENTIONS §7); fills the B1-1 Driver checkpoint ledger-revision seam per S05/S02.4.
- 2026-07-20 — **Operator probe batch: reboot parts DONE** (operator + coordinator, live, ~03:00–03:18). Probe 1 reboot-survival: corpse `active (exited)` pre-shutdown → after boot `Unit sinet-probe-corpse.service could not be found.` — **corpses do not survive reboot, as spec expects (P-T07-2)**. Probe 2 `Persistent=` catch-up, reboot leg: first firing seeded 03:10:42 (recorded deviation — temporary `OnActiveSec=1` drop-in, removed + verified clean before reboot); machine off across the 03:15 slot (shutdown ~03:14, boot 03:16:33); **exactly one catch-up fired 03:16:39, ~6 s post-boot, pre-login (Linger=yes)**; grid resumed (NEXT 03:30). Bonus reading: systemd 259 touches the Persistent stamp at timer *activation* — even a never-fired timer has a catch-up baseline. Probe timer deliberately left installed for the suspend leg (pairs with the battery-drain 1 h suspend); multi-missed-slot single-fire rider self-records at a future longer-off boot. Remaining homework: suspend leg + Probe 3 freeze/thaw. Result files: `P3/measurements/2026-07-20-reboot-survival.md`, `2026-07-20-persistent-catchup.md`.
- 2026-07-20 — **CI fix (coordinator, inline):** the srt-packet push went red on the `test` step (run on `f78877e`) though the full battery was green **locally** — `internal/sandbox/srt_test.go:TestSrtStubReceivesConfigAndArgv` used the REAL probed caps (this host has bwrap 0.11.1; the ubuntu-24.04 CI runner does not), so `Confine`→`caps.require()`→`ErrNoBwrap` fatally failed only in CI. Fixed by faking the boundary caps (`co.caps = {Bwrap,Userns,Srt,SrtPath:stub}`) — the exact hermetic pattern its passing sibling `TestConfineSelects…` already uses; the test's purpose (stub-srt config+argv) needs no real bwrap. Audited every other sandbox test: all gated by `Caps().Available()` sanctioned-skips (CI-green since B1-3), sole offender was this one new test. Local battery/gofmt/vet/lockgate green post-fix; **CI GREEN on `5593ed9` — main is clean.** (Note: earlier run `44d0cd7` = `cancelled`, not failed — CI `cancel-in-progress` concurrency when `f78877e` superseded it. GitHub API had a sustained 503/504 outage during this window; `git push` and eventual CI runs were unaffected.)
- 2026-07-20 — **P3-B1-3R done → B1 GATE CLOSED → B2 OPENED.** srt remediation validated (commit `55b80bd`): coordinator re-ran battery 19 pkgs + race sandbox green, lockgate 8 entries; **srt pin 0.0.66 independently re-verified** vs GitHub releases (v0.0.66, 2026-07-17); lock entry read (Apache-2.0 path-scoped at commit 03bcd3d, full S16.4 checklist, funeral-plan=native, churn watch); `BuildPlan` srt/native routing + `SrtConfigFor` schema mapping + corrected CONVENTIONS §12 read and sound; `sinet control` boots clean + stops clean. B2 queue cut (S05→S06→S07→skeleton demo, all inherit). B2 gate = walking-skeleton demo + the batched host installs. **P3-B2-1 NOT launched** — operator paused to switch the session back to Fable now that all Opus-pinned packets (B1-2/3/4 + B1-3R) are done; B2's whole queue is Fable-inherit, so resume via "continue implementation".
- 2026-07-20 — **B1 gate answered (operator, step-by-step); acting on it.** (1) **Engine bump DONE** 2.1.214→2.1.215 via S03.3 (commit `fef17c0`): conformance green at new pin, `claudecli.Pin`+lock moved in lockstep, S16.3 row text unchanged (lock is the living pin per S16.2), 2.1.215 fixes the parallel-gate trap, revalidation trigger hits an empty worker set. (2) **srt = Path A (adopt)** → **P3-B1-3R launched** (opus). (3) crypto→B2. (4) structural constants→keep now, under the standing settings directive. (5) readings→ratified en bloc. Gate record written to `P3/gates/B1-report.md`. B1 closes when B1-3R validates.
- 2026-07-20 — **srt live status verified** (operator-requested, GitHub API + web, `anthropic-experimental/sandbox-runtime`): real, **not archived**, actively maintained (pushed 2026-07-18), **Apache-2.0** (matches S16.3), **TypeScript/Node CLI**, 4.7k★, **135 open issues**. Latest **v0.0.66** (2026-07-17). **Still 0.0.x, releases every 1–3 days, "APIs and config formats may evolve"** (research preview). Bearing on A/B: adoptable (Node already required for the claude engine; S01.5 sanctions consuming JS organs as external processes) and healthy → Path A feasible; but the 0.0.x/rapid-churn/not-the-boundary profile is the real basis for a Path-B amendment. Coordinator recommendation → operator (see next).
- 2026-07-20 — **B1-gate item 2 RECLASSIFIED (spec-conformance defect, not a ratify-item):** operator challenged the sandbox-runtime deviation; coordinator re-read S11.1 → **srt is a BINDING adoption (G2 D2.2)**, direct composition is only the S16.3 funeral plan. B1-3 silently shipped the fallback instead of adopting = a spec-conformance violation (adopt-don't-fork gate-ratification + stop-and-amend rules), and dropped srt's positive-allowlist seccomp contribution. Coordinator under-caught it (B1-3 validated at battery+evidence depth, no line read of the srt reasoning). "Ratify" recommendation withdrawn. Remediation paths in gate report item 2: (A) adopt srt per spec / (B) deliberate S00.9 amendment; hinges on live srt status. Contained (dev-mode only, no host install). **Awaiting operator direction; other B1-gate items unaffected.**
- 2026-07-20 — **Operator directive recorded** (see Standing operator directives, top of file): the settings tab must expose + allow editing of EVERY operator-relevant number, including the ones shipped as code constants; deferred to a later clamped-S18 amendment; makes the constants-as-code gate calls interim. Saved to auto-memory too. Does not change any B1-gate answer; noted for the settings work.
- 2026-07-20 — **P3-B1-4 done** (commit `f8977cd`, validated: battery green, changes are spike instrumentation only — no feature scope; 3 result files meet G3 Def.8 discipline, read + confirmed). Spend ~$0.27/$5. **Spike 1 serialize-by-deny: PASS-PROVISIONAL** (spec pins no default worker until S08/B3 → reconfirm on `haiku`, re-run harness committed; TBD-BRINGUP(parallel-gate fallback) stays OPEN for S08). **Spike 2 PreCompact/injection: PASS, TBD RESOLVED** toward contain (PreCompact can even block — bonus; SessionStart source:"compact" + additionalContext re-injection confirmed). **Spike 3 auto-memory: PASS posture** (per-user-isolated config-root writes, GA memory tool default-off). **B1-gate finds (reported, not patched):** (a) installed 2.1.215 *honors* first-defer on parallel gated turns → the S03.4 ~20% silent-fallback trap does NOT reproduce at 2.1.215 (GateFallback detector retained dormant; add to the per-pin canary at the 2.1.214→2.1.215 bump); (b) auto-memory's config-root `memory/` is a separate channel the B1 lowering doesn't yet wipe/redirect (P2-S5 refinement → B3/S09). **B1 GATE OPENED.**
- 2026-07-20 — **P3-B1-3 done** (commit `fcc29c0`, validated: battery 19+ pkgs, race sandbox/broker green, lockgate 7 entries, hygiene grep clean; depth = battery + evidence review). `internal/sandbox` (C0–C2 compose, probes, seccomp-BPF, Landlock seam, Rule-of-Two/P-T05-1 checks, `sinet run-launch` S11.8) + `internal/broker` (per-person AES-256-GCM store, SO_PEERCRED, resolve/sign split, spawn injection); live confinement + broker round-trip evidence in packet report; run@ ExecStart closed. +1 os-mechanism lock entry (probe re-cleared 2026-07-20). **B1-gate items:** (a) `sandbox-runtime` S16.3 row NOT materialized — built its funeral-plan posture (direct kernel-primitive composition) directly; operator ratifies this S16 deviation; (b) age (+sops decision) adoption for at-rest broker crypto — dev uses stdlib AES-GCM; coheres with escrow item 6; (c) egress substrate (nftables+proxy), credential-injection proxy, Landlock live enforcement, run@ install = documented host-change seams deferred to B2+. Readings CONVENTIONS §12.
- 2026-07-19 — **P3-B1-2 done** (commit `bbd5847`, validated: coordinator battery 20 pkgs + race metering/scheduler green, lockgate 6 entries, zero new deps; validation depth = battery + evidence review, no line-level code read this packet). `internal/metering` (checkpoint-fed consumption ledger, effective-dated PriceTable seam, receipts, pressure gauge, unified-header overlay with `TrustParkTiming()` hard-false pending S19.6 observation) + `internal/scheduler` (real Admission: Enqueue→CAS-claim under per-(user,lane) slots→Dispatcher seam; five-class limit classifier; priority ladder+aging). E2E dev-mode: created→queued→claimed→fake-engine→checkpoint→receipt (UNPRICED, §13-cited). **B1-gate items added:** (a) price table ships EMPTY — genai-prices data.json vendoring + hash pin is a flagged S16.3 follow-up (operator #10); (b) non-⚙ structural constants (lane-concurrency default 1, aging biases, scheduler poll) — S18 ratifies none, flagged; (c) Z.AI prompt-unit calibration seam idle (operator item 1). Readings in commit body.
- 2026-07-19 — **P3-B1-1 done** (commit `c87035b`, validated: independent battery 15/15 pkgs + race on adapters green; lockgate 6 entries; key-hygiene grep clean — sole hit is the env-scrub test fixture). D3 contract + Anthropic/claude-CLI lane dev-mode end-to-end; **live smoke on real engine passed** (1 haiku call $0.016: spawn→events→checkpoint→engine_sessions; TTL-bucketed usage; copy-aside). Claude CLI lock entry materialized at binding pin 2.1.214. **For B1 gate: installed engine is 2.1.215 vs pin 2.1.214** (tier-R conformance passed on 2.1.215; `--max-turns` gone from --help yet parse-accepted) — reconciling is an operator S03.3 bump decision. B1-4 flags recorded (serialize-by-deny turn-window heuristic; defer/budget fixture re-record; per-call-flush vs terminal-totals usage note → B1-2). 12 readings in commit body (headline: Anthropic lane first — S03.2 lists+specifies it first; S19.5 leaves the lane unnamed).
- 2026-07-19 — **B0 GATE CLOSED** (operator, step-by-step walkthrough, free-text answers — full record in gate file): D1 adoptions + posture readings RATIFIED; D2 auth numbers stay constants; D3 all host installs deferred to B2; D4 inhibitor wiring deferred to B2; D5 readings ratified en bloc. **B1 OPENED**; 4-packet queue cut from S19.5 (S03 → S10 opus → S11 opus → spike battery opus, per model-routing rule); `P3/measurements/PROBES.md` issued for operator item 3; P3-B1-1 flipped to running at launch.
- 2026-07-19 — **P3-B0-5 done** (commit `7fc639e`, validated). `internal/auth` (argon2id PHC PINs with golden-vector conformance test; server-side sessions, owner-attributed; per-device trusted auto-login grants — shared-device default per G3 Def.1; verify-at-act step-up, no elevation window; every auth act an event-log append), `/api/auth/*` + protected-route split on the B0-3 identity seam, migration `0003_auth_stack` (sessions/grants = deletable current state; history in events), `P3/gates/B0-cert-steps.md` (13 literal operator steps, syntax verified vs host tailscale 1.98.8, CT-log warning tied to S01.8), CONVENTIONS §9. Second rail adoption: **golang.org/x/crypto v0.54.0** (S01.9 names argon2id; stdlib has none) — coordinator independently confirmed @latest on proxy; x/sys companion bump 0.46→0.47 with module coverage staying on the driver entry (no dup claim). Coordinator: battery 13/13 + race green; S01.9 read against the shipped stack — all three layers + AuthZ posture match. **Key finding, confirmed against spec text: S01.9 ⚙-flags nothing and S18 ratifies no auth keys** → session TTL 30 d / 5 attempts / 15 min lockout ship as documented code constants; making them operator-tunable requires an S00.9 amendment adding S18 rows (gate decision). 13 readings in commit body; x/crypto #10 ratification queued for gate. Maintenance-mode HTTP mutation surface now unblocked (B1 item).
- 2026-07-19 — **P3-B0-4 done** (commit `8a25ca6`, validated). `internal/run` (S02.3 FSM — every edge spec-anchored in code, implied edges labeled; one-tx transitions via AppendTx; parked→running resume bumps generation), `internal/gates` (S02.4 checkpoints; S02.7 two-phase journal: payload_hash pinned at approval, executing-before-provider-call, saved-plan expiry/drift → re-approval, classes A–D, D→unknown+card), `internal/recovery` (S02.5 ladder: observe/classify ALIVE-WEDGED-FINISHED-DEAD, harvest dedup (session_id,message_id), fork gen+1 with takeover fence + dispatch-id partial-UNIQUE CAS, max_attempts/stale_finalize bounds, suspend-aware leases + wake_grace, ask re-observation, in-doubt effect resolution), migration `0002_run_recovery_lineage` (parent_run_id/dispatch_id/recovery_attempts — 0001 immutable per CONVENTIONS §6). Coordinator: battery + race green; kill-9 harness re-run verbosely (SIGKILLed children, integrity ok, interrupted tx invisible, fencing inert); ladder verified live on the pre-existing B0-3 demo state dir (migration 0002 applied to existing DB; pass-complete log). All ⚙ keys confirmed ratified S18 rows. 9 readings in commit body. **Deferred, for B0 gate list: logind PrepareForSleep/inhibitor D-Bus wiring** (P-T07-1 pre-sleep flush path exists via shutdown-parity; the inhibitor needs a D-Bus adoption decision or manual impl — operator call at a gate).
- 2026-07-19 — **P3-B0-3 done** (commit `4bf3415`, validated). `internal/shell` (S01.6 ladder: bootstrap → registry attach/reapply → fail-closed loopback lint P-T13-2 → recovery-ladder stub (B0-4) → READY/watchdog → admission seam (B1); SIGTERM clean shutdown; maintenance switch ⚙ drain_grace; WAL-truncate loop), `internal/api` (`/api/health`, `/events` SSE with id=event_seq + Last-Event-ID/after_seq resume + ⚙ obs.sse_keepalive; identity seam dev-impl pending B0-5), `internal/sdnotify` (stdlib unixgram), seam stubs scheduler/gates/ledger/adapters, `sinet units` (6 S01.2 files incl. journald drop-in; engine@/run@ DRAFTs; A1 hostname; generated-never-installed). Zero new deps. Coordinator: battery + race green; **live demo re-run independently** — health JSON ok, SSE replayed `platform.started` (id:1) from cursor, SIGTERM → exit 0 with ordered shutdown log, `0.0.0.0` bind refused fail-closed exit 1, units rendered to scratch only. 10 readings in the commit body (headline: provisional lifecycle event names pending S14/B5; dev posture = absent systemd env; cursorless SSE tails from head). `sseBatchSize=256` correctly a plain constant (S18 ratifies no such key — documented in-code).
- 2026-07-19 — **P3-B0-2 done** (commit `89b20ac`, validated). `internal/storage` (WAL/FULL/BEGIN-IMMEDIATE/integrity-check per S02.1; `user_version` one-tx migrations; full S02.2 12-family schema + settings tables with append-only triggers), `internal/eventlog` (global `event_seq`, generation fencing vs `runs.generation`, validate-before-persist, ⚙ payload cap, same-tx `AppendTx`), `internal/settings` (all 118 S18 keys — tests pin the S18.5 tallies 118/33/per-owner; clamps; operator-owned bounds; one-tx audit row + `settings.changed`; JSON-Schema emission). First Go dependency through the rail: **modernc.org/sqlite v1.54.0** — coordinator independently confirmed `@latest` on proxy.golang.org; lock entry covers all 10 transitive modules with per-module licenses; libc pin-coupling documented. Coordinator re-ran battery: build/test(-count=1)/gofmt/vet/lockgate green; migration SQL, storage.go, eventlog.go read in full against S02.1–S02.3 (runs.state CHECK = exactly S02.3's stored states; stalled/wedged correctly derived-only); S18 tallies + `entailment_sample_rate` TBD-BRINGUP-default-0 verified against spec text. 8 clearly-implied readings logged by the packet (agent final message, repeated in CONVENTIONS §6); **driver adoption S16.4 #10 ratification queued for B0 gate** alongside B0-1's readings.
- 2026-07-19 — **P3-B0-1 done** (commit `3577f77`, validated). Scaffold per S01.5 (single multi-call binary, `cmd/sinet` + `internal/`), `components.lock` + `internal/lockfile` + `tools/lockgate`, SHA-pinned CI, `P3/CONVENTIONS.md`. Coordinator re-ran the battery independently: build/test/gofmt/vet/lockgate all green; action SHA pins independently verified against the GitHub API (both = tag v7.0.0). **Readings logged** (runbook ambiguity rule — each clearly implied by spec text): (1) lock serialization = strict pretty-printed JSON — S16.2 explicitly makes serialization "P3's choice", exercised now because B0 requires the manifest from the first dependency; dated in CONVENTIONS §4. (2) CI actions (`actions/checkout`, `actions/setup-go`) recorded as SHA-pinned `toolchain` lock entries — constituents of the ratified S01.11 CI mechanism, not running units/bundled deps; extra rigor beyond the S16.2 CI rule's scope. Formal S16.4 #10 operator ratification of both readings queued for the **B0 gate report**. (3) CI runner pinned `ubuntu-24.04` (26.04 runner image still preview — floating/preview labels never used).
